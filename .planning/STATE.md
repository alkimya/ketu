# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-06)

**Core value:** Cycle calculations must be correct, tested, and performant
**Current focus:** Milestone v1.1 Flexibility & Houses — Phase 12 (Release Preparation v1.1.0) IN PROGRESS (1/4 plans)

## Current Position

Phase: 12 of 12 (Release Preparation v1.1.0) — IN PROGRESS (1/4 plans)
Plan: 1 of 4 complete (12-01 version-bump-and-sync). Next: 12-02 changelog-completion.

**Plan 12-01 complete.** Two-source version pattern flipped atomically `1.0.0` → `1.1.0`: `pyproject.toml` line 7 (`version = "1.1.0"`) + `ketu/__init__.py` line 55 (`__version__ = "1.1.0"`). Single commit `81f2dc3` on `gsd/v1.1-milestone` — exactly 2 files / 2 insertions / 2 deletions; no other edits, no reformatting. Three-gate verification all green: (1) `tests/test_version.py -v` → 2 passed (sync gate `importlib.metadata.version('ketu') == ketu.__version__`); (2) `pytest tests/ -q` → **724 passed** (zero drift vs Phase 11 baseline — confirms version-string bump is a pure no-op for everything except the sync test); (3) `mypy ketu/ --strict` → `Success: no issues found in 40 source files`. Phase-level contract assertion `m.version('ketu') == ketu.__version__ == '1.1.0'` runs green (executed beyond the test gate as belt-and-braces). **Deviation Rule 3 (blocking, environmental)**: First `pytest tests/test_version.py` run after the bump FAILED with `Version mismatch: package metadata=1.0.0, ketu.__version__=1.1.0` — editable installs materialize `pyproject.toml [project].version` into `{sitepackages}/ketu-X.Y.Z.dist-info/` AT INSTALL TIME, so `importlib.metadata.version("ketu")` kept reporting `1.0.0` until `pip install -e . --no-deps` regenerated dist-info (`Uninstalling ketu-1.0.0` → `Successfully installed ketu-1.1.0`). Pattern documented in SUMMARY for future major bumps; no plan defect. **Deviation Rule 3 (blocking, environmental)**: venv `bin/pytest` and `bin/mypy` shebangs hardcode `/home/loc/workspace/solaris/ketu/venv/bin/python3` (project relocated from `solaris/ketu/` to `ketu/`); worked around by invoking `/home/loc/workspace/ketu/venv/bin/python -m pytest` / `python -m mypy` directly — venv `python` symlink to `/usr/bin/python3` still resolves, only the script wrappers are stale. No source change. **REL-01 closed**: both version locations declare `1.1.0`, sync gate green. Test count headline **724 passed** captured for Plan 12-04 GH release-notes copy. Plan 12-02 (CHANGELOG completion) unblocked. Commit: `81f2dc3` on `gsd/v1.1-milestone`.

Phase: 11 of 12 (CLI Refactor & Integration) — **COMPLETE (6/6 plans)**
Plan: 6 of 6 complete (11-01 cli-parser-scaffolding, 11-02 harmonics-spec-validator, 11-03 houses-subcommand, 11-04 aspects-cmd-formatters-introspection, 11-05 entry-point-repoint-legacy-removal, 11-06 byte-identical-regression)
Status: **Plan 11-06 complete (Option A pivot).** Original plan target was "byte-identical to v1.0" subprocess regression; user resolved at human-verify checkpoint with Option A — re-pin fixture to **current v1.1 output** because Phase 8 (Lilith calibration: `Lilith Sagittarius 23º27'41"` v1.1 vs `Lilith Gemini 23º21'31"` v1.0 at J2000 UTC) and Phase 9 (default CLASSICAL aspects + `# Aspect set:` resolved-config header on stderr) deliberately diverged from v1.0 astronomy. CLI-03 reinterpreted from "byte-identical to v1.0 backward contract" → "self-stable forward contract" (catches future format drift; loses the v1.0 backward guarantee that was already lost in reality). Fixture: `tests/cli/fixtures/v1_1_reference_output.txt` (2125 bytes, 52 lines, sha256 `067fa67672d2e3c727a30612364e4b9bb1699401768f4a8fc4819a0e951785ed`, captured from `gsd/v1.1-milestone` HEAD). Test: `tests/cli/test_v1_1_reference_byte_stable.py` (214 lines, 5 tests). Plan-specified 3 tests; added 2 (encoding-locked-to-U+00BA + stderr-structurally-clean) because Phase 8 + Phase 9 most-recently changed exactly those surfaces. Subprocess invocation `[sys.executable, "-m", "ketu", "--harmonics", "all", "aspects", "--date", "2000-01-01T12:00:00Z"]` exercises the entry point repointed in Plan 11-05 (CLI-01) AND the `cmd_aspects` + `display.print_aspects` single-source-of-truth wired in Plan 11-04 (CLI-02). Failure messages cite likely drift causes (header leak to stdout, missing Aspect Timing Example block, format change, degree-symbol flip U+00BA→U+00B0) AND include regeneration recipe `python -m ketu ... > FIXTURE` for intentional bumps. Module docstring on test file documents the Option A pivot transparently — future readers understand why the contract pins v1.1 forward, not v1.0 backward. Stale uncommitted `v1_0_legacy_output.txt` from prior executor deleted as housekeeping. 724 tests pass (719 baseline from Plan 11-05 + 5 new); mypy `--strict` clean on new test file; 0 regressions. **Phase 11 closes at 6/6**: all six CLI-* requirements covered (CLI-01 Plan 11-05; CLI-02 Plans 11-02 + 11-04; CLI-03 this plan; CLI-04 Plan 11-03; CLI-05 Plan 11-04; CLI-06 Plan 11-04). Phase 12 (Release Preparation v1.1.0) unblocked. Commits `2331fa8` (Task 1 — fixture capture) + `0f2c43c` (Task 2 — subprocess regression test) on `gsd/v1.1-milestone`.

**Plan 11-05 complete.** Both entry points repointed to `ketu.cli:main` atomically: `pyproject.toml [project.scripts] ketu = "ketu.cli:main"` (was `ketu.display:main`) AND `ketu/__main__.py: from ketu.cli import main; raise SystemExit(main())` — same commit, per research §Pitfall 7. `pyproject.toml [tool.setuptools].packages` adds `"ketu.cli"`. `python -m pip install -e .` regenerates the venv `ketu` console script wrapper to point at `ketu.cli:main`; verified via `head -5 $(which ketu)` showing `from ketu.cli import main`. `ketu/display.py:main()` deleted (~30 lines); `__all__` shrinks to `["print_positions", "print_aspects"]`; six imports trimmed (`datetime`, `zoneinfo`, `body_id`, `find_aspects_between_dates`, `utc_to_julian`, `julian_to_utc` — all exclusively used by deleted `main()`); module docstring rewritten to "Library formatters... legacy interactive `main()` prompt was removed in Phase 11; the argparse-based CLI lives in `ketu.cli`." File ~85 lines (was ~123). `tests/test_ketu.py:TestMain` (1 test) deleted as planned; `from ketu.display import print_positions, print_aspects, main` shrinks to drop `main`. **Deviation Rule 3 (blocking)**: `tests/test_coverage_improvements.py` also imported `main` (line 23) and contained `TestMainCLI` class (lines 236-295, 4 more tests exercising legacy interactive prompt via `monkeypatch.setattr("builtins.input", ...)`); plan flagged `test_ketu.py` only. Detected via `grep -rn "from ketu.display import.*main" tests/` BEFORE running pytest. Both TestMainCLI class and import line removed. Total test count delta = -5 (724 → 719), not -1 as plan estimated. **CLI-01 fully closed**: `grep -rn "from ketu.display import.*main\|ketu\.display\.main\|ketu\.display:main" ketu/ tests/ docs/` returns only the historical comment in `__main__.py` describing what was removed. Both `ketu --help` and `python -m ketu --help` print argparse usage with NO interactive prompt. `python -m ketu` (no args) prints argparse help to stdout, returns exit code 0. `python -m ketu --list-aspect-sets` lists 4 presets. 719 tests pass; mypy `--strict` clean across `ketu/cli/` + `ketu/display.py` + `ketu/__main__.py` (10 source files); 0 regressions. **GPG signing skipped** per Plan 11-01 environmental note (per-commit `-c commit.gpgsign=false`, no global config change). Plan 11-06 (byte-identical regression) fully unblocked: subprocess `python -m ketu --harmonics all aspects --date ...` now routes through new CLI; `result.returncode` propagated correctly via `SystemExit(main())` wrapper; v1.0 'º' (U+00BA) format string preserved in `display.print_aspects` (Plan 11-04 single source of truth, untouched here).

**Plan 11-04 complete.** `ketu/cli/aspects_cmd.py` (107 lines) implements `cmd_aspects(args)` dispatcher: resolves `--harmonics` (None → CLASSICAL via `resolve_aspect_set(None)`), emits resolved-config header to stderr (CLI-06), calls `display.print_positions` + `display.print_aspects(jd, aspects=mask)` (single source of truth for v1.0 format strings — BLOCKER 1 fix), always emits the v1.0 'Aspect Timing Example' Sun-Moon trailing block (research §Open Question 2 resolution). `_preset_label_for_mask` uses `np.array_equal` for exact bit-for-bit preset detection (no sum-count heuristic). `ketu/cli/formatters.py` (53 lines) implements `emit_resolved_config(mask, preset_name, house_system)` writing `# Ketu v1.1.0` + optional `# Aspect set: NAME (N aspects: ...)` + optional `# House system: NAME` to STDERR. Preserves CLI-03 stdout-pristine contract. `ketu/cli/introspection.py` (60 lines) implements `cmd_list_aspect_sets` (lists 4 presets {classical, traditional, extended, all} with descriptions and angles) + `cmd_list_house_systems` (lists 3 systems with polar-fallback hint). `ketu/display.py:print_aspects` extended with optional `aspects= None` kwarg forwarded to `calculate_aspects`; preserves U+00BA `º` byte-for-byte; `from .core import aspects` aliased to `_CORE_ASPECTS` to free the parameter name; mypy `--strict` clean via `TYPE_CHECKING` import of `AspectSetSpec`. `ketu/cli/houses_cmd.py:cmd_houses` extended with `emit_resolved_config(mask=None, preset_name=None, house_system=args.system)` call BEFORE `parse_iso_utc` (BLOCKER 2 fix; CLI-06 closed for houses subcommand). `ketu/cli/parser.py` rewired: imports `cmd_aspects`, `cmd_list_aspect_sets`, `cmd_list_house_systems`; `set_defaults(func=cmd_aspects)` replaces `_stub_aspects`; introspection short-circuits call real implementations; **all 3 stub functions deleted; zero stubs remain in parser.py**; unused `sys` import removed. `tests/cli/test_aspects_cmd.py` (135 lines, 9 tests across 6 classes): default-classical + header on stderr; --harmonics all → 'extended' label; Aspect Timing Example always emitted (3 cases); --harmonics 0,4,7,9,13 == --harmonics classical (mask byte-equality); bare integer rejected with code 2; **aspects block uses U+00BA `º` AND not U+00B0 `°` (BLOCKER 1 regression detector)**. `tests/cli/test_introspection.py` (45 lines, 6 tests): --list-aspect-sets lists 4 presets with angles; --list-house-systems lists 3 systems with hint; both work without subcommand (Pitfall 1). `tests/cli/test_resolved_header.py` (105 lines, 9 tests): aspects header on stderr only; 'Aspect set: classical/extended' labels; count + angles in header; **houses emits `# House system: NAME` on stderr (BLOCKER 2 verification, 3 systems)**; houses doesn't emit 'Aspect set:' line; under --harmonics all no '# ' line on stdout (CLI-03 spirit-check). `tests/cli/test_parser.py` 3 stub-marker assertions retrofitted to real-content. 724 tests pass (700 baseline + 24 new); mypy `--strict` clean across `ketu/cli/` + `ketu/display.py` (9 source files); 0 regressions. Manual smoke: `from ketu.cli import main; main(['--harmonics', 'all', 'aspects', '--date', '2000-01-01T12:00:00Z'])` produces 14-aspect output with U+00BA `º` chars to stdout; `# Ketu v1.1.0` + `# Aspect set: extended (...)` to stderr only. `from ketu.cli import main; main(['houses', ...])` produces `# Ketu v1.1.0` + `# House system: placidus` to stderr. **No deviations** (one tiny docstring rephrase in `display.print_aspects` to satisfy the plan's own verify rule that U+00B0 must be absent from the function source — semantically identical, no code change). **GPG signing skipped** per environmental note. **Plan 11-03 superseded.** `ketu/cli/_dates.py` (74 lines) lands `parse_iso_utc(value: str) -> float` ISO 8601 → JD parser with unconditional Python-3.10 'Z' shim (`s = s[:-1] + "+00:00"` BEFORE `datetime.fromisoformat(s)`; idempotent on Py3.11+), naive=UTC convention (matches `utc_to_julian`), non-UTC offset normalisation via `dt.astimezone(timezone.utc)`, delegates final JD via `ketu.ephemeris.time.utc_to_julian` (no parallel JD math). Raises `SystemExit` with helpful message on invalid input (cleaner than ArgumentTypeError for post-parse callers). `ketu/cli/houses_cmd.py` (82 lines) lands `cmd_houses(args)` dispatcher: `parse_iso_utc(args.date)` → `ketu.calculate_houses(jd, lat, lon, system, polar_fallback)` (registry dispatch INSIDE; zero if/elif on system name) → 12 cusps + ASC + MC printed via `_format_cusp(cusp_deg)` helper that delegates DMS to `dd_to_dms` and sign names to `ketu.core.signs`. Output shape: `'House N: SIGN  DD°MM'SS" (NNN.NNNN°)'` — DMS for humans, raw decimal degrees in parens for machine parseability + self-validating end-to-end test. `ketu/cli/parser.py` modified: `from .houses_cmd import cmd_houses` import + `p_houses.set_defaults(func=cmd_houses)` replaces `_stub_houses`; `_stub_houses` function deleted. `tests/cli/test_dates.py` (165 lines, 10 tests): TestZShim (3 — Z accepted on every Python version + +00:00 equivalent + explicit Py3.10 path), **TestZShimForceExercised (2 — MAJOR 4 fix)** monkeypatches `_dates_mod.datetime` to a fake whose `fromisoformat` rejects 'Z' (emulates Py3.10); shim must still produce success because Z is replaced BEFORE the fake call. **Sanity-tested by manually deleting the shim line — test FAILS with SystemExit; restored shim → green.** Negative control test confirms the monkeypatch is meaningful. TestNaiveDatetime (1) + TestNonUTCOffset (1) + TestErrors (3). `tests/cli/test_houses_cmd.py` (97 lines, 15 tests): TestHousesCmdMatchesPythonAPI (9 parametric: 3 systems {placidus/koch/porphyry} × 3 locations {Paris/Sydney/Greenwich}) — regex extracts raw decimals from output, compares to `calculate_houses(...)` Python API at 1e-3° tolerance. **CLI-04 success criterion 4 closed end-to-end.** TestHousesCmdFlags (3) + TestHousesCmdPolar (2: lat=80° default raises; --polar-fallback porphyry succeeds) + TestHousesCmdISOZShim (1). `tests/cli/test_parser.py::test_main_houses_dispatches_to_func` retrofitted (Plan 11-03 marker → real `'House Cusps' / 'ASC:' / 'MC :'` stdout assertions). 700 tests pass (675 baseline + 25 new); mypy --strict clean on `ketu/cli/` (5 source files); 0 regressions. Manual smoke: Paris 2026-05-06T12:00:00Z Placidus → ASC at Leo 28°35'31" (148.5921°), MC at Taurus 19°9'49" (49.1638°). **No deviations** from plan (two tiny reference-text adjustments documented in SUMMARY: `dd_to_dms` returns ndarray not tuple → int-coerce per element; unused `as exc` removed in polar-raise test). **GPG signing skipped** per Plan 11-01 environmental note. **Note**: `python -m ketu houses ...` does NOT yet route through new CLI — `__main__.py` still imports legacy `display.main()`; Plan 11-05 (entry point repoint + legacy removal) handles that swap. Manual smoke testing via direct `from ketu.cli import main; main([...])` invocation.
Last activity: 2026-05-07 — Plan 12-01 executed; commit `81f2dc3` (atomic version bump 1.0.0 → 1.1.0 in `pyproject.toml` + `ketu/__init__.py`) on `gsd/v1.1-milestone`. **Phase 12 opened, 1/4 plans complete. REL-01 closed.**

Progress: [██████████] v1.0 complete; v1.1 4/5 phases complete + Phase 12 in progress (Phase 8: 5/5 ✓, Phase 9: 6/6 ✓ VERIFICATION 5/5, Phase 10: 6/6 ✓ VERIFICATION 5/5, Phase 11: 6/6 ✓ VERIFICATION 6/6, Phase 12: 1/4 — version bump done, changelog/upgrading/publish pending)

## Performance Metrics

**Velocity (v1.0 reference baseline):**

- Total plans completed (v1.0): 16
- v1.1 plans completed: 24 (Phase 8: 08-01..08-05; Phase 9: 09-01, 09-02, 09-03, 09-04a, 09-04b, 09-05; Phase 10: 10-01..10-06; Phase 11: 11-01..11-06; Phase 12: 12-01)

**By Phase (v1.1):**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 8. Lilith Verification & Fix | 5 | ~22m 18s | ~4m 28s |
| 9. Configurable Aspects | 6 | ~44m 58s | ~7m 30s |
| 10. Houses Module | 6 | ~58m 10s | ~9m 42s |
| 11. CLI Refactor & Integration | 6 | ~34m 52s | ~5m 49s |
| 12. Release Preparation v1.1.0 | 1 | ~1m 44s | ~1m 44s |

*Updated after each plan completion*
| Phase 08 P01 | 2min | 1 tasks | 1 files |
| Phase 08 P02 | 2m 5s | 2 tasks | 1 files |
| Phase 08 P03 | 4m 31s | 2 tasks | 1 files |
| Phase 08 P04 | 10m 1s | 4 tasks | 4 files |
| Phase 08 P05 | 3m 41s | 2 tasks | 2 files |
| Phase 09 P03 | 1m 35s | 1 tasks | 1 files |
| Phase 09 P02 | 5m 25s | 3 tasks | 3 files |
| Phase 09 P01 | 6m 00s | 2 tasks | 1 files |
| Phase 09 P04a | 6m 34s | 2 tasks | 1 files |
| Phase 09 P04b | 6m 3s | 1 tasks | 3 files |
| Phase 09 P05 | 19m 21s | 2 tasks | 3 files |
| Phase 10 P01 | 9m 1s | 2 tasks | 4 files |
| Phase 10 P02 | 4m 5s | 2 tasks | 3 files |
| Phase 10 P03 | 7m 48s | 3 tasks | 8 files |
| Phase 10 P04 | 5m 57s | 2 tasks | 2 files |
| Phase 10 P05 | 19m 19s | 3 tasks | 5 files |
| Phase 10 P06 | 12m 0s | 3 tasks | 11 files |
| Phase 11 P01 | 5m 18s | 2 tasks | 5 files |
| Phase 11 P02 | 2m 46s | 2 tasks | 4 files |
| Phase 11 P03 | 3m 52s | 2 tasks | 6 files |
| Phase 11 P04 | 5m 18s | 2 tasks | 10 files |
| Phase 11 P05 | 2m 38s | 2 tasks | 5 files |
| Phase 11 P05 | 2m 38s | 2 tasks | 5 files |
| Phase 11 P06 | ~15m 0s | 2 tasks | 2 files |
| Phase 12 P01 | 1m 44s | 2 tasks | 2 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table. Recent decisions affecting current work:

- Default aspects = 5 majors in v1.1 — Pro/classical default; ML harmonics opt-in via `--harmonics`
- Houses module starts with Placidus + Koch — two systems prove extensibility; others plug in later
- Verify Lilith before fixing — confirm bug exists and quantify error before changing formula
- Vectorize everything new — houses + harmonics must be batchable over date arrays
- `core.aspects` stays length-14 append-only (Kala uses positional indexing — non-negotiable)
- `pysweph>=2.10.3.6` is test-only dependency (AGPL-safe; no runtime deps added)
- [Phase 08]: Test-only optional extra pattern: AGPL-licensed pysweph lives under [project.optional-dependencies].test, never in [project].dependencies — proven empirically with two-venv install test
- [Phase 08-01]: Investigation-first ordering enforced — `docs/LILITH_DEFINITION.md` (contract for harness/fix) lands BEFORE any code change; tolerance derived arithmetically (0.01 deg ≈ 129 min mean apogee drift), not a round-number convention; History section pre-seeded with literal Plan 04 sentinel for atomic update
- [Phase 08]: Plan 04 branch = FORMULA-CORRECTION (empirical max |delta| = 179.94 deg, ~18000x tolerance); suspected root cause: epoch constant 83.3532 off by 180 deg (Ketu computes perigee, swe expects apogee); residual after +180 deg correction is 0.111 deg (still 11x tolerance) -- secondary frame/rate term to address
- [Phase 08]: Cross-check harness pattern locked: pytest.importorskip module-level gate (no binding) + separate import for mypy --strict + manual swe.version check (since pysweph __version__ is int date stamp) + always-pass diagnostic Python script alongside assertion test to capture deltas regardless of pass/fail; defensive index-based tuple unpack for foreign C-extension return shape
- [Phase 08-04]: Single source of truth for Lilith constants — 5 private module-level constants `_LILITH_MEAN_*` and `_LILITH_PERTURB_*` in `ketu/ephemeris/orbital.py`; all 4 plumbing sites (orbital.py x2, planets.py x2) reference them by name. Eliminates v1.0's 4-site duplication drift risk
- [Phase 08-04]: Pure linear correction insufficient — secondary residual ~0.124 deg has dominant 1095-day (3 sidereal years) sinusoidal signature. v1.1 formula adds one trig perturbation term (linear secular + 1 sin), fitted via joint Nelder-Mead NLS over 55K daily samples 1900-2050. Pattern: when a mean-element formula misses external reference by sinusoidal residual, add the dominant FFT-identified perturbation
- [Phase 08-04]: REGRESSION_TOLERANCE_DEG = 0.005 — half of user-facing 0.01 deg, ~1.85x post-fit max (0.002693). Pins agreement margin without hardcoding Ketu output (research §"Anti-patterns" preserved). Test-only threshold; production users still bound by 0.01 deg contract
- [Phase 08-04]: avg_speeds[12] uses `round(_LILITH_MEAN_RATE_DEG_PER_DAY, 6)` — preserves 6-decimal `avg_speeds` dict convention while inheriting from source-of-truth named constant
- [Phase 08]: [Phase 08-05] Magnitude consistency invariant locked across CHANGELOG/UPGRADING/LILITH_DEFINITION at 6-decimal precision: pre-fix 179.936579 deg (user-visible breaking-change), post-fix 0.002693 deg (5 dates) and 0.007815 deg (55K daily samples) -- both <0.01 deg tolerance. Per-date table in UPGRADING live-computed from get_lilith_position rather than copied from Plan 04 SUMMARY
- [Phase 08]: [Phase 08-05] Deviation from pure Chapront secular linear stated transparently in CHANGELOG and UPGRADING (NOT minimized): both files explicitly state v1.1 ships 'linear secular term + 1 sin() perturbation', not a raw ELP-2000 polynomial. Per execution-context note from Wave 3 orchestrator
- [Phase 09]: [Phase 09-03]: core.aspects invariant pinned via sha256 byte fingerprint (c5bd1773...9afb359) + per-row name/angle/coef + length 14 + dtype.names. Mutation test verified surgical row-drift detection. Defense-in-depth pattern: any future drift in rows 0-13 fails with informative messages identifying the affected row index
- [Phase 09]: [Phase 09-02]: ketu/aspects/presets.py — three frozen length-14 np.bool_ masks (CLASSICAL=5, TRADITIONAL=7, EXTENDED=14) with single-call resolve_aspect_set() dispatching on six input types (None / str preset case-insensitive / Sequence[str|int] / np.ndarray bool|int). Defensive bool-rejection in Sequence prevents silent [True, False] -> [1, 0] index coercion. ASP-06 forward-looking rule documented (no caches today materialize filtered aspects, but Wave 2 must hash mask.tobytes() if adding any). 100% test coverage on presets.py (56 tests).
- [Phase 09]: [Phase 09-01]: ASP-08 v1.0 baseline frozen at `.planning/phases/09-configurable-aspects/baseline-v1.0.json` (git_sha=049a9e7, aspect_set=extended, mean[365]=200.87ms cv=1.62%, 50 iter × 3 batch sizes, drift=3.56% PASS <5% gate). `tests/benchmark_aspects_batch.py` ships --aspect-set flag from day 1 and mismatch-rejection in --compare mode (no silent baseline-vs-comparison drift). Wave-1 parallel collision: identical script content authored independently by Plan 09-01 and committed via Plan 09-02 commit `78085d1` — md5 verified byte-identical, no merge needed.
- [Phase 09]: [Phase 09-04a]: `ketu/aspects/calculator.py` refactor threads `aspects: AspectSetSpec = None` (default → CLASSICAL via resolver) through 4 public multi-aspect APIs. Module-level `from ketu.core import aspects` renamed to `aspects as _CORE_ASPECTS` to free the parameter name. 2 hot loops rewritten to `for k, i_asp_val in enumerate(selected_indices)` emitting canonical `int(i_asp)` (NOT `k`) — Kala positional contract preserved. Resolver runs ONCE per API call; in `calculate_aspects_batch` ABOVE the per-date loop (never per-date). `find_aspects_between_dates` passes filtered `selected_angles` to downstream `find_all_aspects` (no leak of unselected aspects). LRU-cache audit: 2 sites (`_cached_planet_position_batch`, `body_properties`), both safe — neither materializes filtered aspect output. `calculate_aspects` reuses out-of-scope scanner `get_aspect` with post-hoc filter via `selected_indices_set` — preserves single source of truth for scalar match logic without rewriting scanner. 479 tests pass with NO retrofit needed (all assertions use loose count/structure checks or sibling-parity); mypy --strict clean; interrogate 100%.
- [Phase 09]: [Phase 09-04b]: Four hardcoded `["Conjunction", "Sextile", "Square", "Trine", "Opposition"]` literal default lists (1 in windows.py find_aspects_timeline, 1 in timelines.py generate_aspect_timeline, 2 in transits.py find_transits_to_position + compare_dates_transits) replaced with `list(_CLASSICAL_NAMES)` derivation from CLASSICAL preset. Per file: ONE module-level import block + `_CLASSICAL_NAMES = tuple(aspects["name"][i].decode() for i in np.where(_CLASSICAL_MASK)[0])`. Approach A locked (preserve `aspects_list: list[str]` shape; do NOT widen to AspectSetSpec — v1.2+ scope). `find_aspect_window` (single-aspect API) and `lunar_calendar.BIG_FIVE` untouched. Single-source-of-truth invariant verified by provenance smoke test: all three modules resolve to canonical 5-major tuple. 479 tests pass; mypy --strict clean. Pre-existing interrogate gap (85.2% < 90% in nested callbacks) verified unrelated to this plan via git-stash baseline comparison.
- [Phase 09]: [Phase 09-05]: ASP-07 verified by 9-test `TestAspectPresetsIntegration` class extending `tests/test_aspect_presets.py` — covers `calculate_aspects` / `_vectorized` / `_batch` / `find_aspects_between_dates` with CLASSICAL/TRADITIONAL/EXTENDED + default-flip + canonical-i_asp + EXTENDED-superset assertions. ASP-08 verified by `benchmark-comparison.json`: Phase 9 EXTENDED is -10% to -15% FASTER than v1.0 baseline on every batch size (30/90/365); CLASSICAL is -33% to -41% faster (matches research's 30-65% inner-loop speedup prediction). HARD GATE passed with margin; no CONDITIONAL band needed.
- [Phase 09]: [Phase 09-05]: Hot-loop perf regression discovered and auto-fixed (deviation Rule 1). Plan 09-04a's calculator refactor introduced per-date Python-scalar casts (`int(i_asp)` / `float(angle)` / `float(coef)`) AND per-date orb-array recomputation `(orbs_body1+orbs_body2)/2 * coef` — paid `n_dates × n_aspects` times instead of `n_aspects` times. Initial 200-iter benchmark showed +6-7% regression on EXTENDED. Fix: hoist all per-aspect work above per-date loop — pre-cast lists `selected_iasp_ints` / `selected_angles_f` / `selected_coefs_f` + pre-multiplied `selected_orbs_per_aspect = [pair_orb_sums * c for c in selected_coefs_f]` (date-independent). Result: regression turned into -8% to -15% improvement. Pattern locked: any future hot-loop refactor of `calculate_aspects_batch` MUST be benchmarked at ≥200 iterations against baseline-v1.0.json before merging — the 50-iter compare default is noise-dominated at 5% gate width. Fix commit `b847176`.
- [Phase 10]: [Phase 10-01]: LST/obliquity precision audit verdict TIGHTEN. Root cause was a mean-vs-apparent semantic mismatch, NOT a polynomial-precision issue: ketu's `sidereal_time()` returned mean GMST while `swe.sidtime` (and `swe.houses_armc`) consume apparent GST. Fix = mean GMST + equation of equinoxes per Meeus 2nd ed. eq. 12.6 (`EE = nut_lon × cos(eps_mean)`), reusing existing `nutation()` and `mean_obliquity()` from `coordinates.py` — no new dep, no new formula, signature `(jd, longitude=0.0) -> float` preserved. Empirical drift collapsed: GST 16.4″ → 2.05″, polar ASC at lat=66.5° 227″ → 8.7″ (HOU-01 60″ spec comfortably met). `tests/houses/` subpackage established (`__init__.py` marker + `test_lst_obliquity_precision.py` with 4 parametrized tests = 16 cases). TOL_GMST_ARCSEC = 5.0 (NOT plan-suggested 1.0): 4-term truncated nutation series in `coordinates.nutation()` leaves ~2″ residual; full IAU 2000A is v1.2 scope. TOL_POLAR_ASC_ARCSEC = 50.0 (10″ headroom vs HOU-01 60″ spec). Polar boundary fence at lat=66.5° intentionally includes 2024-06-21 Placidus singularity sample to expose near-circumpolar instability for Plan 10-05 Porphyry-fallback design. Pattern locked: `swe.houses_armc(armc, lat, eps, b'P')` isolation for primitive-drift fences — separates Placidus-implementation errors from input-precision errors; Plan 10-04/10-05 will reuse.
- [Phase 10]: [Phase 10-02]: Oracle harness landed in `tests/houses/conftest.py` — `SYSTEM_BYTES` dict (`{placidus: b'P', koch: b'K', porphyry: b'O'}`) centralises Pitfall 8 (bytes-vs-str) at the boundary; `swe_oracle(jd, lat, lon, system)` slices `cusps_t[1:13]` solving Pitfall 7 (13-tuple/0-indexed) once. `swe_oracle_armc(armc, lat, eps, system)` complements via `swe.houses_armc` so Plans 03/04/05 can isolate algorithm error from sidereal-time error. `reference_charts` session-scoped fixture: 10 entries (Greenwich, Paris, Sydney, Tokyo, BuenosAires, Equator, NewYork@1900, Reykjavik@2050, polar 70°/80° at J2000) — exactly meets HOU-09 floor with no slack. `tests/houses/fixtures/reference_charts.json` (641 lines, 20 KB, sha256 `b7762c9b7c255f6d8ecb382853d0cdacd7a5a1347964d3a377493790c6769581`, swisseph 2.10.03) snapshots 10×3 = 30 entries; polar Placidus/Koch marked `{error: 'swisseph.houses_ex: error', polar: True}`, polar Porphyry produces full closed-form cusps even at lat=80° (the polar fallback path Plan 10-05 must reproduce). 6 pure-infra smoke tests with snapshot-vs-live tolerance 1e-9 deg (deterministic swisseph; any drift = environmental change worth flagging). Snapshot script is one-off scaffolding (NOT committed; lived at `snapshot_reference_charts_tmp.py` then deleted) — only the JSON output is tracked. Wave 3 (Plans 10-04 and 10-05) can run in parallel reading the same JSON without modifying it. Deviation: 1 mypy-only float-cast on `mean_obliquity()` return for `swe_oracle_armc(eps: float)` parameter — vectorised return type `float | np.ndarray` flagged by `--strict`.
- [Phase 10]: [Phase 10-04]: `ketu.houses.placidus` lands the first house-system implementation. `placidus_cusps(armc, lat, eps) → ndarray (..., 12)` registered via `@register("placidus")` — `calculate_houses` dispatch needs zero edits when Plan 10-06 wires it. Mask-based vectorized fixed-point iteration on RA: `active = ~converged & ~np.isnan(RA)` updates only un-converged elements per inner step; converged ones frozen so they don't pollute others (HOU-08 anti-pattern: never wrap scalar Placidus in `for jd in jds:`). `MAX_ITER=50` (HOU-03 spec; max actually used = 35 across 32 cases) and `TOL_DEG=1e-7` (research §"Don't Hand-Roll") locked by invariant tests. Modular convergence metric `abs(((RA_new - RA + 180) % 360) - 180)` handles 0°/360° wrap (Pitfall 3); polar boundary detected by `ascensional_difference` returning NaN (Pitfall 6) and propagated through the iteration into output cusps as the failure signal — at lat=80° houses 2/3/5/6/8/9/11/12 NaN, houses 1/4/7/10 remain finite (closed-form ASC/IC/DESC/MC do not iterate). Per-cusp formulas in dispatch dict `_CUSP_FORMULAS = {11: ..., 12: ..., 2: ..., 3: ...}` (research §Anti-Pattern 1: no if-elif ladder); cusps 5/6/8/9 = exact 180° opposites of 11/12/2/3. Inline ASC/MC re-derivation from `(armc, lat, eps)` rather than calling `compute_ascmc(jd, lat, lon)` — registry signature is `(armc, lat, eps)`, the closed-form arctan2 cost is ~10 µs, and `compute_ascmc` would require a JD we don't have. **Deviation Rule 1**: plan reference text §`_ra_to_lambda` had `λ = atan2(sin(RA)·cos(ε), cos(RA))` — `cos(ε)` in numerator. Empirically off by ~277 arcmin (~17000× spec) at J2000_Paris cusp 11. Correct ecliptic-resident transformation is `λ = atan2(sin(RA), cos(RA)·cos(ε))` (`cos(ε)` in denominator), matching `_ecliptic.ra_to_lambda` exactly. After fix: 8/8 non-polar charts <1 arcmin oracle agreement (worst 51.49″ at 2050_Reykjavik, mirroring Plan 10-03's worst-case ASC drift exactly — input-precision artifact propagating through closed-form ASC into iterated cusps, NOT iteration bug; <10″ on other 7 charts). Single-arg `arctan` for declination retained (declination is in `[-π/2, +π/2]` by definition; Pitfall 2 applies to RA / longitude — angles spanning the full circle — not to declination); documented inline so future readers don't "fix" to `arctan2`. Vectorized vs scalar parity: 0.00e+00° max delta on 3-chart batch (HOU-08 satisfied). 555 tests pass (538 + 17 new); 100% coverage on `placidus.py`; mypy --strict clean. **Pattern locked for Plan 10-05**: per-cusp formula table as `dict[int, Callable]`; mask-based fixed-point with NaN-as-failure-signal; modular convergence metric. Untracked `koch.py` / `porphyry.py` from parallel-wave Plan 10-05 work coexist on disk but are not yet imported by `__init__.py`, so end-of-plan `SYSTEMS` contains only `'placidus'`.
- [Phase 10]: [Phase 10-05]: `ketu.houses.koch` and `ketu.houses.porphyry` complete the v1.1 milestone three-system house module. **Koch is closed-form (NOT iterative)** — the plan reference text described an iterative `OA → RA` fixed-point trisection that is algorithmically wrong (off by ~50 arcmin at J2000_Paris); the canonical swisseph `swehouse.c` case `'K'` formula is closed-form and bit-exact: `sina = sin(MC) · sin(eps) / cos(lat)`, `c = arctan(tan(lat) / cosa)`, `ad3 = arcsin(sin(c) · sina) / 3`, then `cusp_k = Asc1(armc + offset_k ± multiple·ad3, lat, sin_eps, cos_eps)` for cusps 11/12/2/3. **Porphyry needs polar ASC swap** — at lat > polar circle the closed-form ASC may emerge in the wrong quadrant (180° from where swisseph places it); detect via signed `acmc = ((asc - mc + 540) % 360) - 180 < 0` and reflect ASC by 180° before trisection (mirrors `swehouse.c` case `'O'` lines 1310-1316). Without this fix, our Porphyry was bit-exact at non-polar charts but off by 180° at lat=70/80 — caught by `test_porphyry_algorithm_matches_oracle_armc_at_all_latitudes`. **Two-tier oracle test strategy** (algorithm tier via `swe_oracle_armc` + end-to-end tier via snapshot) resolves the eps_mean vs eps_true precision question without modifying Plan 10-03's `compute_ascmc`: algorithm tier supplies same inputs and validates bit-exact correctness (≤4×10⁻¹⁰ arcsec on all 10 reference charts including polar lat=70/80); end-to-end tier inherits whatever eps semantic compute_ascmc uses today (`eps_mean`) and pins the resulting drift envelope. **Reykjavik (lat 64.1°N) at 148″ snapshot drift** is dominated by ~7.4″ eps_mean vs eps_true drift amplified by Koch's `cos(lat)` divisor in the `sina = sin(MC) · sin(eps) / cos(lat)` formula — pinned at 3 arcmin in `test_koch_reykjavik_within_inherited_precision_floor` so any future Plan 10-03 upgrade to `eps_true` (which would collapse this to ~1″) is caught. Polar-safety helpers: `polar_circle(jd) = 90 − mean_obliquity(jd)` (time-varying, drifts +70.7″ over J1900→J2050 — Pitfall 4 regression catcher pinned at >5e-3° drift threshold), `is_polar(lat, jd)` with `POLAR_EPS_TOL = 1e-9` margin per research §Open Question 4 (avoids false-positive convergence at the formal boundary, Pitfall 6). NaN-propagation routing contract: Placidus AND Koch produce NaN cusps at `|lat| > polar_circle`, Porphyry remains finite even at lat=89° — caller can `np.isnan(cusps).any()` to route via `polar_fallback`. Plan 10-06 just needs to (a) auto-import the system modules in `ketu/houses/__init__.py`, (b) wire `calculate_houses` to use `get_system + compute_ascmc + is_polar` for `polar_fallback` routing, (c) implement `house_of`. 597 tests pass (555 + 42 new); 100% coverage on `koch.py` and `porphyry.py`; mypy --strict clean across 5 files.
- [Phase 10]: [Phase 10-03]: `ketu.houses` subpackage scaffold landed — `HOUSES_DTYPE` (9 fields: jd/lat/lon/system/cusps[12]/asc/mc/armc/vertex; outer (N,) → cusps (N,12)) + `HighLatitudeError` (ValueError subclass; carries lat/system/polar_lat with helpful "polar_fallback='porphyry'" hint in message) + `SYSTEMS` registry dict + `@register("name")` decorator (lowercase normalisation) + `get_system(name)` dispatch (case-insensitive lookup; ValueError-with-options on unknown). `compute_ascmc(jd, lat, lon)` returns dict[asc, mc, armc, vertex, eps] vectorised over arbitrary broadcast shape; all four angles via `np.arctan2` (Pitfall 2). `compute_armc = (sidereal_time(jd, 0.0) + lon) % 360` — explicit decomposition matching Pitfall 5 audit form; lifts scalar `sidereal_time` via list-comp (microseconds for 1000 charts). Vertex formula required deviation Rule 1: plan reference text was `atan2(cos(armc), -[sin(eps)tan(90-lat) + cos(eps)sin(armc)])` which yields the **antivertex** (off by ~168° from oracle); correct form evaluates ASC closed-form at `armc + 180` (anti-meridian) with co-latitude — empirical cross-check at J2000_Paris: 0.42″ vs oracle 190.1198°. 28 new tests (5 dtype + 5 registry + 18 ascmc): worst-case oracle deltas — ASC 51.5″ @ 2050_Reykjavik, MC 1.2″ @ J2000_BuenosAires, ARMC 1.9″ @ Reykjavik, Vertex 15.5″ @ J2000_Tokyo (all <60″ HOU-01 spec / <300″ Vertex advisory). 538 tests pass (510 + 28); mypy --strict clean across 8 files; `grep "swisseph" ketu/houses/` returns nothing (pure-NumPy contract preserved). `pyproject.toml` updated with `ketu.houses`; `ketu/__init__.py` re-exports `HOUSES_DTYPE`, `HighLatitudeError`, `HOUSE_SYSTEMS` (renamed from `SYSTEMS` to avoid clobbering top-level globals), `calculate_houses`, `house_of`. Stubs raise `NotImplementedError` with explicit "wired in Plan 10-06" message — signature + docstring contract stable now. Deviation Rule 3: mypy-strict typed-local pattern (`result: np.ndarray = np.rad2deg(...) % 360.0; return result`) required because numpy ufunc % returns `Any`; pattern reused across `_ecliptic.py` and `ascmc.py`. Pattern locked for Plans 10-04 / 10-05: registry signature `(armc, lat, eps) -> cusps[..., 12]`; Placidus/Koch/Porphyry plug in via `@register` without touching `calculate_houses`.
- [Phase 10]: [Phase 10-06]: Phase 10 closed. `ketu/houses/api.py` lands the public bodies of `calculate_houses(jd, lat, lon, system, polar_fallback)` and `house_of(planet_lon, cusps)`, replacing Plan 10-03's `NotImplementedError` stubs. `calculate_houses` dispatches via `SYSTEMS[system.lower()]` (no inline if/elif — registry pattern only); detects polar elements with `is_polar(lat_b, jd_b)` over the broadcast shape; routes via `polar_fallback={raise|porphyry}` — `raise` raises `HighLatitudeError(lat, system_lower, polar_lat)` driven by the first offending element via `np.argmax(flat_mask)`; `porphyry` calls `porphyry_cusps(armc, lat_b, eps)` and substitutes per-element via `np.where(polar_mask[..., np.newaxis], cusps_porphyry, cusps_requested)`. `house_of` uses eastward modular metric: `diffs = (planet_lon - cusps + 360) % 360`; `spans = (np.roll(cusps, -1, axis=-1) - cusps + 360) % 360`; `in_house = diffs < spans`; `argmax + 1` returns int32 in {1..12} broadcast over both `planet_lon` (...,) and `cusps` (..., 12). Convention pinned by `test_house_of_planet_at_cusp_is_in_that_house`: cusps[i] BEGINS house i+1; planet at cusps[5] is in house 6. `ketu/houses/__init__.py` rewired with registration-trigger imports `from . import placidus, koch, porphyry` (noqa: F401 marker on each — these "unused" imports are load-bearing without them SYSTEMS is empty at module load). HOU-10 closed: `calculate_house_cusps` v0.x equal-house placeholder deleted from `ketu/ephemeris/planets.py` (~40 lines), `ephemeris/__init__.py` (import + __all__), `tests/test_planets_coverage.py` (TestCalculateHouseCusps class — 6 tests removed). CHANGELOG [1.1.0] gained `### Removed (BREAKING)` entry pointing to `ketu.calculate_houses` migration and `### Added` entry covering HOU-02..HOU-10. HOU-09 closed: `make houses-coverage` reports 96.75% on `ketu.houses` (308 stmts, 10 missed in `_ecliptic.py` only, all other modules at 100%). 638 tests pass (591 + 47 new in test_house_of.py and test_integration.py); mypy --strict clean across 32 source files; `grep "swisseph" ketu/` empty. **Two deviations Rule 1 (bugs)**: (1) `calculate_houses` polar gate falsely raised when `system='porphyry'` was explicitly requested at polar lat — Porphyry IS the polar fallback path, so the gate self-exempts via `any_polar = polar_mask.any() and system_lower != 'porphyry'`; (2) `tests/houses/conftest.py` had `import numpy as np` AFTER `pytest.importorskip("swisseph")` — pyswisseph C extension links numpy at load; coverage.py's import hooks combined with the late numpy import caused double-numpy-import `_NoValueType` corruption that crashed `numpy.amax` from inside swisseph oracle tests, surfacing only when 47 new tests grew the suite past a threshold. Fix: move numpy import before importorskip + use two-step Makefile coverage gate (`pytest --cov` then `coverage report --include='ketu/houses/*' --fail-under=95`) instead of `--cov=ketu.houses` sub-package source mode (which triggers the same bug from a different angle). Pattern locked: registry-trigger imports require explicit `noqa: F401` markers; coverage gate uses two-step include filter to scope without sub-package source mode; conftest must import numpy before any C-extension importorskip.
- [Phase 11]: [Phase 11-01]: ketu/cli/ subpackage scaffold lands argparse tree + main() entry with set_defaults(func=...) dispatch pattern (no if-elif ladder) + getattr(args, 'func', None) fallback to print_help() (Pitfall 4 prevented). add_subparsers(required=False) so top-level introspection flags work without subcommand. --harmonics declared with type=str placeholder; Plan 11-02 swaps in parse_harmonics_spec validator. Stub dispatchers print Plan-N marker to stderr as breadcrumbs for follow-on plans. tests/cli/ mirror established with invoke_main in-process fixture (subprocess testing reserved for Plan 11-06 byte-identical only). 16 tests pin parser shape + main() dispatch contract. GPG signing failed in sandbox (pinentry GUI unavailable); commits 9000733+2a14421 unsigned via per-commit -c commit.gpgsign=false (no global config change).
- [Phase 11]: [Phase 11-02]: parse_harmonics_spec validator delegates to ketu.aspects.presets.resolve_aspect_set — thin tokenizer pattern (CLI module picks input shape, domain module resolves to mask). 'all' is a CLI-layer alias for 'extended' implemented by string-rewrite before delegating (keeps resolver focused on canonical names). Comma-detection-before-int-parse is the mechanism that rejects bare integers like '12' (Pitfall 5 / REQUIREMENTS.md line 101); test coverage proves rule applies to ANY bare integer including '0' and '9' (would-be-valid in a list). Validator raises argparse.ArgumentTypeError so argparse renders 'error: argument --harmonics: <msg>' with SystemExit(2) — no traceback (Pitfall 9). 21 tests across 5 classes (preset names + comma-lists + bare-int rejection + invalid inputs + argparse end-to-end). 675 tests pass (654 + 21); mypy --strict ketu/cli/ clean across 3 source files. Default --harmonics=None left unresolved at validator layer; Plan 11-04's aspects_cmd dispatcher resolves None → CLASSICAL.
- [Phase 11]: [Phase 11-03]: parse_iso_utc Z-shim mechanically force-exercised on every Python version via monkeypatch of module-level _dates_mod.datetime to a fake whose fromisoformat rejects 'Z' (emulates Py3.10). Sanity-tested by deleting the shim line — test FAILS with SystemExit, restored shim → green. Pattern locked: any future CLI plan needing ISO 8601 dates can reuse parse_iso_utc OR copy TestZShimForceExercised pattern. cmd_houses delegation contract: parse_iso_utc → calculate_houses (registry dispatch inside) → _format_cusp via dd_to_dms + signs; zero if/elif on system name; zero parallel JD math. Output shape pinned: 'House N: SIGN  DD°MM'SS" (NNN.NNNN°)' — DMS for humans, raw decimal degrees in parens for machine parseability + self-validating end-to-end test (regex extract → assertion vs Python API). 9 parametric (3 systems × 3 locations) test cases close CLI-04 success criterion 4 end-to-end at 1e-3° tolerance.
- [Phase 11]: [Phase 11-04]: BLOCKER 1 fix via single source of truth — `display.print_aspects` extended with optional `aspects= None` kwarg forwarded to `calculate_aspects`; `cmd_aspects` calls `display.print_aspects(jd, aspects=mask)` (the library helper) NOT a hand-rolled format-string loop. The v1.0 'º' (U+00BA, MASCULINE ORDINAL INDICATOR) format string lives in exactly ONE place; CLI-03 byte-identity (Plan 11-06) is automatic. Verified by `test_aspects_output_uses_u00ba_degree_char` (regression detector that fails earlier than Plan 11-06's full byte-identical fixture diff). Module-level `from .core import aspects` aliased to `_CORE_ASPECTS` to free the parameter name (same rename pattern Plan 09-04a established in `aspects/calculator.py`). BLOCKER 2 fix: `cmd_houses` now calls `emit_resolved_config(mask=None, preset_name=None, house_system=args.system)` BEFORE `parse_iso_utc` — closes ROADMAP success criterion 5 ('every CLI invocation echoes resolved-config header') for the houses subcommand. Pinned by 4 stderr tests in TestHousesResolvedConfigHeader. `_preset_label_for_mask` uses `np.array_equal` for exact bit-for-bit preset detection (MINOR 6 polish absorbed; no sum-count mislabeling — `--harmonics 0,1,4,5,7` no longer mis-labels as 'classical'). Aspect Timing Example trailing block ALWAYS emitted (research §Open Question 2): v1.0 emitted it for all aspect sets; preserves CLI-03 byte-identity for `--harmonics all` AND gives non-'all' users the same demo block. Resolved-config header to STDERR (CLI-06): `# Ketu v1.1.0` + `# Aspect set: NAME (N aspects: ...)` + `# House system: NAME` — preserves CLI-03 stdout-pristine contract under `--harmonics all` (verified by `test_no_hash_lines_in_stdout`). Header uses U+00B0 '°' (DEGREE SIGN), NOT U+00BA — header is on stderr, not part of CLI-03 byte-identity contract. Stub-marker test breadcrumb retired: 3 stub-marker assertions in `tests/cli/test_parser.py` retrofitted to real-content assertions (presets in stdout / systems in stdout / 'Aspect set:' in stderr). 724 tests pass (700 + 24); mypy --strict clean across `ketu/cli/` + `ketu/display.py` (9 source files). Plan 11 progresses to 4/6 plans (67%); Plan 11-05 (entry-point repoint + legacy removal) unblocked.
- [Phase 11]: [Phase 11-05]: Both entry points repointed atomically in single commit (research §Pitfall 7) — pyproject.toml [project.scripts] ketu = 'ketu.cli:main' AND ketu/__main__.py 'from ketu.cli import main' must change together. python -m pip install -e . regenerates the venv ketu console-script wrapper. SystemExit(main()) wrapper required for Plan 11-06's subprocess result.returncode contract.
- [Phase 11]: [Phase 11-05]: Public-symbol deletion checklist (Rule 3 deviation pattern). Plan flagged tests/test_ketu.py:TestMain (1 test) only; sister file tests/test_coverage_improvements.py:TestMainCLI (4 tests) shared the same 'from ketu.display import main' import. Once display.main was deleted, the sister-file import would have failed at pytest COLLECTION time (ImportError before any test ran), surfacing in --collect-only or first-test failure cascade. Caught BEFORE running pytest via grep audit. Pattern locked: when deleting public symbol, grep ALL of ketu/, tests/, docs/ for 'from {module} import {symbol}' before pytest runs. Test count delta = -5 (724 → 719), not -1 plan-estimated.
- [Phase 11]: [Phase 11-06]: **Option A pivot** — original plan target "byte-identical to v1.0 reference" reinterpreted at human-verify checkpoint as "self-stable forward contract pinning current v1.1 output". Phase 8 (Lilith calibration: Sagittarius 23º27'41" v1.1 vs Gemini 23º21'31" v1.0 at J2000 UTC) and Phase 9 (default CLASSICAL aspects + `# Aspect set:` resolved-config header on stderr) deliberately diverged from v1.0 astronomy, making the original "byte-identical to v1.0" contract mathematically infeasible. User decision Option A formalizes the divergence: re-pin to current v1.1 output, rename fixture/test/class to v1.1 forward semantics, document the pivot in the test file's module docstring so future readers don't dig through .planning/.
- [Phase 11]: [Phase 11-06]: Forward-contract regression test naming convention — when pinning current output as a regression baseline (NOT a historical reference), name fixture/test after the version being pinned forward (`v1_1_reference_*`), NOT the version-being-compared-against (`legacy_*`). Honest naming saves future debugging time.
- [Phase 11]: [Phase 11-06]: Stderr policy = structurally-clean, NOT empty. Plan 11-04 pins `# Ketu v1.1.0` + `# Aspect set: extended (...)` resolved-config header on stderr (CLI-06 contract). Asserting "every non-empty stderr line starts with `#`" catches leaked print() calls without breaking CLI-06. Pattern locked for future CLI tests.
- [Phase 11]: [Phase 11-06]: Five-test breakdown extends plan-specified three: (1) fixture-exists-and-nonempty, (2) byte-identical-to-fixture (main contract), (3) stderr-is-structurally-clean (no print leaks), (4) stderr-contains-aspect-set-header (CLI-06 belt-and-suspenders), (5) degree-symbol-locked-to-U+00BA (encoding-convention pin via `data.count(b'\xc2\xba') > 20` AND `count(b'\xc2\xb0') == 0`). Two extra tests added because Phase 8 + Phase 9 most-recently changed exactly the encoding+stderr surface, which is also where the next breaking change is most likely to land.
- [Phase 11]: [Phase 11-06]: Failure-message regeneration hint — byte-stable test that fires on intentional drift includes (a) common-cause hints, (b) regeneration command `python -m ketu --harmonics all aspects --date {DATE} > {FIXTURE}`, (c) unified diff. Three extra lines of fail() boilerplate save 10 min of detective work per drift. Pattern locked for any future byte-stable regression test.
- [Phase 11]: [Phase 11-06]: **Phase 11 closed at 6/6 plans.** All six CLI-* requirements covered (CLI-01 Plan 11-05; CLI-02 Plans 11-02 + 11-04; CLI-03 this plan; CLI-04 Plan 11-03; CLI-05 Plan 11-04; CLI-06 Plan 11-04). Phase 12 (Release Preparation v1.1.0) unblocked.
- [Phase 12]: Plan 12-01 atomic two-file version bump: 1.0.0 -> 1.1.0 in pyproject.toml + ketu/__init__.py committed as 81f2dc3; 724 tests stable; mypy --strict clean; sync gate (tests/test_version.py) green
- [Phase 12]: Editable-install metadata refresh pattern documented (Rule 3): bumping pyproject [project].version requires re-running pip install -e . --no-deps to regenerate venv dist-info before importlib.metadata reflects the new version; without it test_version_matches_metadata fails despite both source files being in sync

### From v1.0 milestone (carried context)

- Pure NumPy contract: no new runtime deps allowed
- Mypy strict mode enforced in CI; numpydoc-style docstrings on public API; interrogate ≥95%
- Trusted publishing OIDC configured for PyPI releases
- Test isolation pattern: pytest tmp_path fixture for file I/O
- Error message convention: ValueError with received value + valid options
- Two-layer caching is intentional: LRU for single-point, EphemerisCache for batch
- v1.0 milestone roadmap archived to `.planning/milestones/v1.0-ROADMAP.md`

### Roadmap Evolution

- v1.0 milestone complete (8 phases including 2.1 insertion, 16 plans, ketu 1.0.0 on PyPI 2026-02-12)
- v1.1 milestone planned: 5 phases (8-12), 33 requirements, parallelizable 8/9/10 then 11 then 12

### Pending Todos

None yet.

### Blockers/Concerns

- **Kala aspect-count dependency unverified** (Phase 9 risk) — confirm with Kala maintainer that `KetuAdapter` either tolerates `EXTENDED` opt-in or is updated before Phase 9 merge
- ~~**LST/obliquity precision audit** (Phase 10 first task)~~ **CLOSED by Plan 10-01 (commits `0cb3ad0` + `bcfdcf6`).** Verdict TIGHTEN; `sidereal_time()` upgraded to apparent GST; max GST drift 2.05″, max polar ASC error 8.7″ — well inside HOU-01 <60″ spec.
- ~~**Placidus near-circumpolar instability** (Phase 10-05 design concern)~~ **CLOSED by Plan 10-05 (commits `1448ece` + `bd15c7b`) and confirmed end-to-end by Plan 10-06.** Porphyry fallback substitutes per-element under `polar_fallback='porphyry'` via `np.where`; `test_calculate_houses_polar_porphyry_substitutes_for_polar_only` and sibling Koch test both pin no-NaN at lat=70/80 alongside mid-lat Placidus/Koch in same vectorised batch. `test_calculate_houses_polar_default_raises_high_latitude_error` pins the default-raise contract.
- **`eps_mean` vs `eps_true` precision floor at high latitudes** (deferred to v1.2) — `compute_ascmc` returns `eps_mean` from `mean_obliquity(jd)`; swisseph internally uses `eps_true`. The ~7.4″ delta amplifies through Koch's `cos(lat)` divisor to ~148″ at Reykjavik (lat 64.1°N) end-to-end. Pinned at 3 arcmin in Plan 10-05's `test_koch_reykjavik_within_inherited_precision_floor` and reused by Plan 10-06's `test_calculate_houses_all_3_systems_match_oracle` parametric fence (`NON_POLAR_TOL_ARCMIN = 3.0`). A future v1.2 upgrade to `eps_true` (Plan TBD) would collapse all three systems to ~1″ at Reykjavik; the inherited-precision tests will catch any regression at upgrade time.

## Session Continuity

Last session: 2026-05-07 (Plan 11-06 execution — Option A pivot; Phase 11 Wave 6 — byte-identical regression / forward contract; CLI-03 closed; **Phase 11 closed at 6/6**)
Stopped at: Completed `11-06-byte-identical-regression-PLAN.md` under Option A pivot. Phase 11 at 6/6 plans (CLOSED). Original plan target "byte-identical to v1.0 reference" reinterpreted as "self-stable forward contract pinning current v1.1 output" because Phase 8 (Lilith calibration) and Phase 9 (default CLASSICAL aspects + resolved-config header) deliberately diverged from v1.0 astronomy. Fixture: `tests/cli/fixtures/v1_1_reference_output.txt` (2125 bytes, 52 lines, sha256 `067fa67672d2e3c727a30612364e4b9bb1699401768f4a8fc4819a0e951785ed`, captured from `gsd/v1.1-milestone` HEAD via `python -m ketu --harmonics all aspects --date 2000-01-01T12:00:00Z > FIXTURE`). 41× U+00BA `º`, 0× U+00B0 `°`. Test: `tests/cli/test_v1_1_reference_byte_stable.py` (214 lines, 5 tests; mypy `--strict` clean): fixture-sanity, byte-identical, stderr-structurally-clean, stderr-contains-aspect-set-header, degree-symbol-locked-to-U+00BA. Plan-specified 3 tests; added 2 (encoding + stderr-clean) because Phase 8 + Phase 9 most-recently changed exactly those surfaces. Module docstring documents the Option A pivot transparently. Failure messages cite likely drift causes + provide regeneration recipe `python -m ketu ... > FIXTURE` for intentional bumps. 724 tests pass (719 baseline from Plan 11-05 + 5 new); 0 regressions. Stale uncommitted `v1_0_legacy_output.txt` from prior executor deleted. Commits `2331fa8` (Task 1 — fixture capture) + `0f2c43c` (Task 2 — subprocess regression test) on `gsd/v1.1-milestone`. **Resume file:** None

Previous session: 2026-05-07 (Plan 11-05 execution; Phase 11 Wave 5 — entry-point repoint + legacy removal; CLI-01 closed)
Stopped at (Plan 11-05): Completed `11-05-entry-point-repoint-legacy-removal-PLAN.md`. Phase 11 at 5/6 plans. Both entry points (`ketu` console script via `pyproject.toml [project.scripts]` AND `python -m ketu` via `ketu/__main__.py`) repointed atomically to `ketu.cli:main` in single commit (research §Pitfall 7). `pyproject.toml [tool.setuptools].packages` adds `"ketu.cli"`. `python -m pip install -e .` regenerates the venv `ketu` console-script wrapper to point at `ketu.cli:main`; verified via `head -5 $(which ketu)` showing `from ketu.cli import main`. `ketu/__main__.py` uses `raise SystemExit(main())` — argparse return code propagates to OS process exit code (required by Plan 11-06's subprocess `result.returncode` check). `ketu/display.py:main()` deleted (~30 lines); `__all__` shrinks to `["print_positions", "print_aspects"]`; six imports trimmed (`datetime`, `zoneinfo`, `body_id`, `find_aspects_between_dates`, `utc_to_julian`, `julian_to_utc` — all exclusively used by deleted `main()`); module docstring rewritten to "Library formatters... legacy interactive `main()` prompt was removed in Phase 11; the argparse-based CLI lives in `ketu.cli`." File ~85 lines (was ~123). `tests/test_ketu.py:TestMain` (1 test) deleted as planned; `from ketu.display import print_positions, print_aspects, main` shrinks to drop `main`. **Deviation Rule 3 (blocking)**: `tests/test_coverage_improvements.py` also imported `main` (line 23) and contained `TestMainCLI` class (lines 236-295, 4 more tests exercising legacy interactive prompt via `monkeypatch.setattr("builtins.input", ...)`); plan flagged `test_ketu.py` only. Detected via `grep -rn "from ketu.display import.*main" tests/` BEFORE running pytest. Both `TestMainCLI` class and import line removed. Total test count delta = -5 (724 → 719), not -1 plan-estimated. **CLI-01 fully closed**: NO interactive `input()` prompt anywhere in ketu/. Both `ketu --help` and `python -m ketu --help` print argparse usage with no interactive prompt. `python -m ketu` (no args) prints argparse help to stdout, returns exit code 0. `python -m ketu --list-aspect-sets` lists 4 presets. 719 tests pass; mypy `--strict` clean across `ketu/cli/` + `ketu/display.py` + `ketu/__main__.py` (10 source files); 0 regressions. **GPG signing skipped** per Plan 11-01 environmental note (per-commit `-c commit.gpgsign=false`, no global config change). Plan 11-06 (byte-identical regression) fully unblocked: subprocess `python -m ketu --harmonics all aspects --date ...` now routes through new CLI; v1.0 'º' (U+00BA) format string preserved in `display.print_aspects` (Plan 11-04 single source of truth, untouched here). Commits `6067a49` (Task 1 — pyproject.toml + ketu/__main__.py atomic entry-point repoint) + `b1ea9cd` (Task 2 — display.main() deleted + tests/test_ketu.py:TestMain + tests/test_coverage_improvements.py:TestMainCLI deleted) on `gsd/v1.1-milestone`. Resume file: `.planning/phases/11-cli-refactor-integration/11-06-byte-identical-regression-PLAN.md` (next unblocked plan; Phase 11 closing wave; depends on 11-05 only).

Previous session: 2026-05-07 (Plan 11-04 execution; Phase 11 Wave 4 — aspects cmd + formatters + introspection)
Stopped at (Plan 11-04): Completed `11-04-aspects-cmd-formatters-introspection-PLAN.md`. Phase 11 at 4/6 plans. `ketu/cli/aspects_cmd.py` (107 lines) + `formatters.py` (53 lines) + `introspection.py` (60 lines) wire the `aspects` subcommand end-to-end (CLI-02), the resolved-config header to STDERR (CLI-06; both `aspects` AND `houses` subcommands now), and the introspection commands `--list-aspect-sets` / `--list-house-systems` (CLI-05). `ketu/display.py:print_aspects` extended with optional `aspects= None` kwarg forwarded to `calculate_aspects` — preserves the v1.0 'º' (U+00BA, MASCULINE ORDINAL INDICATOR) format string byte-for-byte; module-level `from .core import aspects` aliased to `_CORE_ASPECTS` (frees the parameter name); mypy `--strict` clean via `TYPE_CHECKING` import of `AspectSetSpec`. **BLOCKER 1 fix**: `cmd_aspects` calls `display.print_aspects(jd, aspects=mask)` (the library helper) NOT a hand-rolled format-string loop. Single source of truth for the v1.0 'º' format string; CLI-03 byte-identity (Plan 11-06) is automatic. **BLOCKER 2 fix**: `cmd_houses` now calls `emit_resolved_config(mask=None, preset_name=None, house_system=args.system)` BEFORE `parse_iso_utc` — closes ROADMAP success criterion 5 ('every CLI invocation echoes resolved-config header') for the houses subcommand. `_preset_label_for_mask` uses `np.array_equal` for exact bit-for-bit preset detection (MINOR 6 polish absorbed). Aspect Timing Example trailing block ALWAYS emitted (research §Open Question 2). `ketu/cli/parser.py` rewired: imports real dispatchers; `set_defaults(func=cmd_aspects)` replaces `_stub_aspects`; introspection short-circuits call real implementations; **all 3 stub functions deleted; zero stubs remain in parser.py**; unused `sys` import removed. `tests/cli/test_aspects_cmd.py` (135 lines, 9 tests across 6 classes): default-classical + header on stderr; `--harmonics all` → 'extended' label; Aspect Timing Example always emitted (3 cases); `--harmonics 0,4,7,9,13` == `--harmonics classical` (mask byte-equality); bare integer rejected with code 2; **aspects block uses U+00BA `º` AND not U+00B0 `°` (BLOCKER 1 regression detector)**. `tests/cli/test_introspection.py` (45 lines, 6 tests across 3 classes): --list-aspect-sets lists 4 presets with angles; --list-house-systems lists 3 systems with hint; both work without subcommand (Pitfall 1). `tests/cli/test_resolved_header.py` (105 lines, 9 tests across 3 classes): aspects header on stderr only (CLI-06); 'Aspect set: classical/extended' labels; count + angles in header; **houses emits `# House system: NAME` on stderr (BLOCKER 2 verification, 3 systems)**; houses doesn't emit 'Aspect set:' line; under --harmonics all no '# ' line on stdout (CLI-03 spirit-check). `tests/cli/test_parser.py` 3 stub-marker assertions retrofitted to real-content. 724 tests pass (700 baseline + 24 new); mypy `--strict` clean across `ketu/cli/` + `ketu/display.py` (9 source files); 0 regressions. Manual smoke: `from ketu.cli import main; main(['--harmonics', 'all', 'aspects', '--date', '2000-01-01T12:00:00Z'])` produces 14-aspect output to stdout (Bodies Positions + Bodies Aspects + Aspect Timing Example) with U+00BA `º` chars; resolved-config header `# Ketu v1.1.0` + `# Aspect set: extended (...)` lands on stderr only. `from ketu.cli import main; main(['houses', '--date', '2000-01-01T12:00:00Z', '--lat', '48.85', '--lon', '2.35', '--system', 'placidus'])` produces `# Ketu v1.1.0` + `# House system: placidus` on stderr. **No deviations** (one tiny docstring rephrase in `display.print_aspects` to satisfy the plan's own verify rule that U+00B0 must be absent from the function source — semantically identical, no code change). **GPG signing skipped** per environmental note. **Note**: `python -m ketu aspects ...` does NOT yet route through new CLI — `__main__.py` still imports legacy `display.main()`; Plan 11-05 (entry point repoint + legacy removal) handles that swap. Manual smoke testing via direct `from ketu.cli import main; main([...])` invocation. Commits `d90cf6b` (Task 1 — display.print_aspects extension + ketu/cli/aspects_cmd.py + formatters.py + introspection.py) + `e27b376` (Task 2 — parser.py wiring + houses_cmd.py emit_resolved_config + 24 new CLI tests + 3 stub-marker retrofits) on `gsd/v1.1-milestone`. Resume file: `.planning/phases/11-cli-refactor-integration/11-05-entry-point-repoint-legacy-removal-PLAN.md` (next unblocked plan).

Previous session (Plan 11-03): Phase 11 at 3/6 plans. `ketu/cli/_dates.py` (74 lines) implements `parse_iso_utc(value: str) -> float` ISO 8601 → JD parser with unconditional Python-3.10 'Z' shim (`s = s[:-1] + "+00:00"` BEFORE `datetime.fromisoformat(s)`; idempotent on Py3.11+), naive=UTC convention (matches `utc_to_julian`), non-UTC offset normalisation via `dt.astimezone(timezone.utc)`, delegates final JD via `ketu.ephemeris.time.utc_to_julian` (no parallel JD math). Raises `SystemExit` with helpful message on invalid input. `ketu/cli/houses_cmd.py` (82 lines) implements `cmd_houses(args)` dispatcher: `parse_iso_utc(args.date)` → `ketu.calculate_houses(jd, lat, lon, system, polar_fallback)` (registry dispatch INSIDE; zero if/elif on system name) → 12 cusps + ASC + MC printed via `_format_cusp(cusp_deg)` helper that delegates DMS to `dd_to_dms` and sign names to `ketu.core.signs`. Output shape: `'House N: SIGN  DD°MM'SS" (NNN.NNNN°)'` — DMS for humans, raw decimal degrees in parens for machine parseability. `ketu/cli/parser.py` modified: `from .houses_cmd import cmd_houses` + `p_houses.set_defaults(func=cmd_houses)` replaces `_stub_houses`; `_stub_houses` function deleted. `tests/cli/test_dates.py` (165 lines, 10 tests across 5 classes): `TestZShim` (3), **`TestZShimForceExercised` (2 — MAJOR 4 fix)** monkeypatches `_dates_mod.datetime` to a fake whose `fromisoformat` rejects 'Z' (emulates Py3.10) so the shim is mechanically force-exercised on every Python version including 3.11+ where native `fromisoformat` would otherwise hide a deleted shim. Negative control test confirms the monkeypatch is meaningful. **Sanity-tested by deleting the shim line — test FAILS with SystemExit; restored shim → green; proves test is load-bearing.** TestNaiveDatetime (1) + TestNonUTCOffset (1) + TestErrors (3). `tests/cli/test_houses_cmd.py` (97 lines, 15 tests): TestHousesCmdMatchesPythonAPI (9 parametric: 3 systems × 3 locations) — regex extracts raw decimals from output, compares to `calculate_houses(...)` Python API at 1e-3° tolerance. **CLI-04 success criterion 4 closed end-to-end.** TestHousesCmdFlags (3) + TestHousesCmdPolar (2: lat=80° default raises; --polar-fallback porphyry succeeds) + TestHousesCmdISOZShim (1). `tests/cli/test_parser.py::test_main_houses_dispatches_to_func` retrofitted (Plan 11-03 marker → real `'House Cusps' / 'ASC:' / 'MC :'` stdout assertions). 700 tests pass (675 baseline + 25 new); mypy --strict clean on `ketu/cli/` (5 source files); 0 regressions. Manual smoke: Paris 2026-05-06T12:00:00Z Placidus → ASC at Leo 28°35'31" (148.5921°), MC at Taurus 19°9'49" (49.1638°). **No deviations** from plan (two tiny reference-text adjustments documented in SUMMARY: `dd_to_dms` returns ndarray not tuple → int-coerce per element; unused `as exc` removed in polar-raise test). **GPG signing skipped** per Plan 11-01 environmental note. **Note**: `python -m ketu houses ...` does NOT yet route through new CLI — `__main__.py` still imports legacy `display.main()`; Plan 11-05 (entry point repoint + legacy removal) handles that swap. Manual smoke testing via direct `from ketu.cli import main; main([...])` invocation. Commits `6113552` (Task 1 — _dates.py + 10 tests with monkeypatch shim force-exercise) + `b4f8a4a` (Task 2 — houses_cmd.py + parser wiring + 15 end-to-end tests) on `gsd/v1.1-milestone`.

Previous session: 2026-05-07 (Plan 11-02 execution; Phase 11 Wave 1 — harmonics spec validator).
Plan 11-02 complete. `ketu/cli/harmonics_spec.py` (118 lines) implements `parse_harmonics_spec(value: str) -> npt.NDArray[np.bool_]` argparse type validator delegating to `ketu.aspects.presets.resolve_aspect_set` (Phase 9 deliverable). Five spec branches: empty/whitespace → `ArgumentTypeError`; preset name (case-insensitive, whitespace-stripped: classical/traditional/extended/all) → delegate; comma-present → split, strip, int-coerce, delegate; bare integer (no comma) → reject as ambiguous; unrecognized → reject. `'all'` is a CLI-layer alias for `'extended'` (string-rewrite BEFORE delegating). Comma-detection-before-int-parse is the mechanism enforcing bare-integer rejection (Pitfall 5 / REQUIREMENTS.md line 101). Resolver `ValueError` re-raised as `argparse.ArgumentTypeError` so argparse renders `error: argument --harmonics: <msg>` with `SystemExit(2)` (Pitfall 9 covered). `args.harmonics` is now length-14 `np.bool_` mask or `None`. 675 tests pass (654 + 21 new); mypy --strict clean on `ketu/cli/` (3 source files); 0 regressions. Commits `f65faf8` (Task 1 — validator module) + `e21bb0e` (Task 2 — parser wiring + 21 unit tests) on `gsd/v1.1-milestone`.

Earlier session: 2026-05-07 (Plan 11-01 execution; Phase 11 Wave 1 starting — CLI scaffolding).
Phase 10 closed at 6/6 plans, 4 waves. `ketu/houses/api.py` (218 lines) lands the public bodies of `calculate_houses(jd, lat, lon, system, polar_fallback)` and `house_of(planet_lon, cusps)`, replacing Plan 10-03's `NotImplementedError` stubs. SYSTEMS dispatch via `get_system(name.lower())` (no inline if/elif); polar routing via `is_polar(lat_b, jd_b)` + `polar_fallback={raise|porphyry}` — `raise` raises `HighLatitudeError(lat, system_lower, polar_lat)` driven by first offending element via `np.argmax(flat_mask)`; `porphyry` substitutes via `np.where(polar_mask[..., np.newaxis], cusps_porphyry, cusps_requested)` per-element. `house_of` uses eastward modular metric with argmax for first True; vectorised over both planet_lon (...,) and cusps (..., 12); int32 result in {1..12}; convention "cusps[i] BEGINS house i+1". HOU-10 closed: `calculate_house_cusps` v0.x equal-house placeholder removed from `ketu/ephemeris/planets.py`, `__init__.py`, and `tests/test_planets_coverage.py` (TestCalculateHouseCusps class, 6 tests deleted); CHANGELOG [1.1.0] gained `### Removed (BREAKING)` entry pointing to `ketu.calculate_houses` migration plus `### Added` entry covering HOU-02..HOU-10. HOU-09 closed: `make houses-coverage` reports 96.75% on `ketu.houses` (308 stmts, 10 missed in `_ecliptic.py` only — all other modules at 100%). 638 tests pass (591 + 47 new in test_house_of.py and test_integration.py); mypy --strict clean across 32 source files; `grep "swisseph" ketu/` empty. **Two deviations Rule 1 (bugs)**: (1) `calculate_houses` polar gate falsely raised when `system='porphyry'` was explicitly requested at polar lat — Porphyry IS the polar fallback path (mathematically defined at all lats); fix exempts `system_lower == 'porphyry'` from the gate via `any_polar = polar_mask.any() and system_lower != 'porphyry'`; (2) `tests/houses/conftest.py` had `import numpy as np` AFTER `pytest.importorskip("swisseph")` — pyswisseph C extension links numpy at load; coverage.py's import hooks combined with the late numpy import caused double-numpy-import `_NoValueType` corruption that crashed `numpy.amax` from inside swisseph oracle tests, surfacing only when 47 new tests grew the suite past a threshold. Fix: move numpy import before importorskip + use two-step Makefile coverage gate (`pytest --cov` then `coverage report --include='ketu/houses/*' --fail-under=95`) instead of `--cov=ketu.houses` sub-package source mode (which triggers the same bug from a different angle). Commits `6c0e3f1` (Task 1 — calculate_houses + house_of) + `8e86dcd` (Task 2 — calculate_house_cusps removed) + `e0c0bf6` (Task 3 — integration + house_of tests, HOU-09 gate) on `gsd/v1.1-milestone`. Pre-existing branch state (Kala aspect-count dependency unverified) carried forward.
Resume file: `.planning/phases/11-cli-refactor-integration/11-04-aspects-cmd-formatters-introspection-PLAN.md` (next unblocked plan). Plan 11-04 replaces `_stub_aspects` (consumes the length-14 `np.bool_` mask landed by Plan 11-02 directly via `args.harmonics`), `_stub_list_aspect_sets`, `_stub_list_house_systems` — three `Plan 11-04` markers in `tests/cli/test_parser.py` must be updated. Plan 11-04's `aspects_cmd` can reuse `parse_iso_utc` directly from `ketu.cli._dates` (the plan-stated shared use case). Plan 11-04's pattern should mirror `cmd_houses`: parse date → call public API (`calculate_aspects`) → format → print to stdout. Plan 11-05 (entry point repoint + legacy removal) depends on 11-04 finishing first — will swap `__main__.py` from `from .display import main` → `from .cli import main`, repoint `[project.scripts]` in `pyproject.toml`, and delete `ketu/display.py:main()` (CLI-01 fully closed). Plan 11-06 (byte-identical regression) can use the established `tests/cli/conftest.py:FIXTURES_DIR` Path constant and now exercise both `--harmonics` flag (Plan 11-02) and full `houses` subcommand (Plan 11-03) in subprocess invocations.

---
*State initialized: 2026-02-12*
*Last updated: 2026-05-07 — Plan 11-06 complete (Option A pivot); **Phase 11 CLOSED at 6/6 plans**. Original plan target "byte-identical to v1.0 reference" reinterpreted at human-verify checkpoint as "self-stable forward contract pinning current v1.1 output" — Phase 8 Lilith calibration + Phase 9 default CLASSICAL aspects deliberately diverged from v1.0, making the v1.0 byte-identical contract mathematically infeasible. Fixture `tests/cli/fixtures/v1_1_reference_output.txt` (2125 bytes, 52 lines, sha256 067fa676...) captured from current branch via `python -m ketu --harmonics all aspects --date 2000-01-01T12:00:00Z`. Test `tests/cli/test_v1_1_reference_byte_stable.py` (214 lines, 5 tests; mypy --strict clean): fixture-sanity, byte-identical, stderr-structurally-clean, stderr-contains-aspect-set-header, degree-symbol-locked-to-U+00BA. Plan-specified 3 tests; added 2 because Phase 8 + Phase 9 most-recently changed exactly those surfaces. Module docstring documents Option A pivot transparently. Failure messages include regeneration recipe for intentional bumps. 724 tests pass (719 baseline + 5 new); 0 regressions. Stale `v1_0_legacy_output.txt` from prior executor deleted. Commits `2331fa8` (Task 1 — fixture) + `0f2c43c` (Task 2 — subprocess regression test). **All 6 CLI-* requirements closed**: CLI-01 (Plan 11-05), CLI-02 (Plans 11-02 + 11-04), CLI-03 (this plan), CLI-04 (Plan 11-03), CLI-05 (Plan 11-04), CLI-06 (Plan 11-04). Phase 12 (Release Preparation v1.1.0) unblocked.*
