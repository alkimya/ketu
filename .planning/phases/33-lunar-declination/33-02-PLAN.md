---
phase: 33-lunar-declination
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - ketu/charts/core.py
  - ketu/charts/api.py
  - tests/charts/test_dtype.py
autonomous: true

must_haves:
  truths:
    - "CHART_DTYPE carries a body_decl field, (14,) f8, additive, mirroring body_lats"
    - "compute_chart populates out['body_decl'] for every body — not left as np.empty garbage"
    - "chart['body_decl'][b] equals standalone declination(jd, b) to machine precision"
    - "the CHART_DTYPE layout ratchet test guards the additive body_decl field (names, shape, kind/itemsize, subarray shapes)"
    - "body count stays frozen at 14; the prior 13->14 ratchet still passes"
    - "the CHART_DTYPE #: doc block lists body_decl (interrogate/numpydoc see the new field)"
  artifacts:
    - path: "ketu/charts/core.py"
      provides: "body_decl in CHART_DTYPE + updated #: doc block"
      contains: "body_decl"
    - path: "ketu/charts/api.py"
      provides: "compute_chart computes & assigns out['body_decl'] from already-fetched body_lons/body_lats + true_obliquity(jd_b)"
      contains: "out[\"body_decl\"]"
    - path: "tests/charts/test_dtype.py"
      provides: "DECL-08 ratchet: body_decl in field-name tuple, subarray-shape, kind/itemsize, vectorized & 0-d shape assertions"
      contains: "body_decl"
  key_links:
    - from: "ketu/charts/api.py:compute_chart"
      to: "ketu.ephemeris.coordinates chain + true_obliquity"
      via: "eps_b = true_obliquity(jd_b); chain on body_lons/body_lats with eps broadcast over the (14,) axis; out['body_decl'] = decl"
      pattern: "out\\[\"body_decl\"\\]"
    - from: "tests/charts/test_dtype.py"
      to: "CHART_DTYPE"
      via: "field-name tuple, subarray-shape param, kind/itemsize param all include body_decl"
      pattern: "body_decl"
---

<objective>
Add `body_decl` to `CHART_DTYPE` and populate it inside `compute_chart` from the positions it
already fetches, then extend the dtype ratchet test to guard the additive field.

Purpose: DECL-07 (chart wiring) + DECL-08 (ratchet). Every chart — and Returns, which call
`compute_chart` directly — gains declination for free with no downstream recomputation. The
`np.empty` allocation means an un-assigned field is GARBAGE, so the assignment is mandatory.
Output: `("body_decl", "f8", (14,))` in `CHART_DTYPE` (+ updated `#:` doc block), an
`out["body_decl"] = ...` assignment in `compute_chart`, and extended ratchet assertions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lunar-declination/33-RESEARCH.md

# Plan 01 added declination()/the coordinate-chain usage — this plan verifies chart body_decl
# against standalone declination() and reuses the same chain inline.
@.planning/phases/33-lunar-declination/33-01-SUMMARY.md

# Source anchors (verified 2026-06-03):
@ketu/charts/core.py
@ketu/charts/api.py
@tests/charts/test_dtype.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add body_decl to CHART_DTYPE + update the #: doc block</name>
  <files>ketu/charts/core.py</files>
  <action>
In `CHART_DTYPE` (def at lines 87-102), insert `("body_decl", "f8", (14,)),` immediately AFTER
`("body_speeds", "f8", (14,)),` (line 94) — i.e. append it to the body-axis group, before
`cusps`. (Placement is safe internally: all consumers read by NAME, verified — Pitfall 3.)

Update the `#:` doc block (lines 48-86) so interrogate/numpydoc see the new field:
  - Change "Fields (14 total..." (line 50) to "Fields (15 total...".
  - After the `body_speeds` bullet (lines 57-58), add a `body_decl` bullet:
    "``body_decl`` (f8, (14,)): Equatorial declination δ per body, degrees, in [−90, +90].
     North positive, south negative. Computed from each body's ecliptic (λ, β) via the
     coordinates chain using instantaneous obliquity ε(jd) (true_obliquity)."
  - After the existing v1.3 13→14 note (lines 85-86), add a v1.5 note:
    "The ``body_decl`` field was appended additively by v1.5 (lunar declination). The body
     COUNT stays 14; this is a dtype-version bump (new field), not an axis change. Downstream
     positional-offset / .view() consumers (Kala) must adapt — documented, not fixed here."
DO NOT reorder existing fields; DO NOT change body count (stays 14).
  </action>
  <verify>
`source venv/bin/activate && python -c "from ketu.charts.core import CHART_DTYPE; print(CHART_DTYPE.names); f=CHART_DTYPE.fields['body_decl']; print(f[0].shape, f[0].base.kind, f[0].base.itemsize)"`
prints names containing 'body_decl' (after body_speeds, before cusps) and `(14,) f 8`.
  </verify>
  <done>
`CHART_DTYPE` has `body_decl` as `(14,)` f8 right after `body_speeds`; the `#:` doc block lists it (count 15) with a v1.5 additive-bump note; body count unchanged at 14.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate out["body_decl"] in compute_chart</name>
  <files>ketu/charts/api.py</files>
  <action>
`compute_chart` (line 196) allocates `out = np.empty(leading_shape, dtype=CHART_DTYPE)` (line
361) — an unassigned field is UNINITIALIZED MEMORY (garbage, NOT zero — Pitfall 5). It already
has `body_lons, body_lats, body_speeds = _vectorised_body_properties(jd_b)` (line 352, arrays
shaped `S + (14,)`).

Add imports at the top (the module does NOT currently import these — verify and add to the
existing `from ketu.ephemeris...` import area, lines ~37-42):
  `from ketu.ephemeris.coordinates import (spherical_to_rectangular, ecliptic_to_equatorial,
   rectangular_to_spherical, true_obliquity)`

In the assembly block (after `out["body_speeds"] = body_speeds` at line 372, before
`out["cusps"]` at line 373), compute and assign `body_decl` from the ALREADY-FETCHED arrays —
do NOT re-call `calc_planet_position_batch` or loop over S (Pitfall 2):
  eps_b = true_obliquity(jd_b)                      # shape S (array-safe at runtime — verified)
  x, y, z = spherical_to_rectangular(body_lons, body_lats, 1.0)   # each shape S+(14,)
  xe, ye, ze = ecliptic_to_equatorial(x, y, z, eps_b[..., None])  # broadcast ε over the (14,) axis
  _, decl, _ = rectangular_to_spherical(xe, ye, ze)
  out["body_decl"] = decl

`ketu.charts.*` is NOT in the relaxed mypy override — this block must be strictly typed
(`--strict` clean). `eps_b[..., None]` adds a trailing axis so ε broadcasts across the 14-body
axis; for scalar `jd` (leading_shape ()), `eps_b` is 0-d and `eps_b[..., None]` is shape (1,)
which broadcasts against (14,) — confirm shapes at implementation time and adjust the
`[..., None]` if needed so it works for BOTH scalar and array `jd`. If mypy complains about
`true_obliquity`'s `jd: float` hint over an array, narrow with an explicit `np.asarray(...)`
or a local typed alias rather than weakening the function's hint (do NOT edit
`true_obliquity` itself).
  </action>
  <verify>
`source venv/bin/activate && python -c "
import numpy as np
from ketu.charts.api import compute_chart
from ketu.calculations import declination, utc_to_julian
from ketu.aspects.presets import get_aspect_set
from datetime import datetime, timezone
jd = utc_to_julian(datetime(2025,1,15,12,0,tzinfo=timezone.utc))
ch = compute_chart(jd, 48.85, 2.35, 'placidus')
# body_decl must equal standalone declination() per body, not garbage
ref = np.array([float(declination(jd, b)) for b in range(14)])
got = np.asarray(ch['body_decl'])
print('max abs diff', float(np.max(np.abs(got - ref))))
assert np.max(np.abs(got - ref)) < 1e-9, 'body_decl drift'
print('OK', got[1])
"`
(Adjust the `compute_chart` signature/aspects arg to match the real one if it requires an
aspects argument.) Diff < 1e-9, no garbage.
`make mypy` passes (ketu.charts.* strictly typed).
  </verify>
  <done>
`compute_chart` assigns `out["body_decl"]` from the already-fetched `body_lons`/`body_lats` +
`true_obliquity(jd_b)` (no S-loop, no re-fetch); chart `body_decl` matches standalone
`declination()` to < 1e-9 for all 14 bodies, scalar AND array `jd`; mypy --strict clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: DECL-08 ratchet — extend tests/charts/test_dtype.py for body_decl</name>
  <files>tests/charts/test_dtype.py</files>
  <action>
Extend the existing ratchet assertions (do NOT remove any; body count stays 14):
  - `test_dtype_has_expected_field_names` (line 44): add `"body_decl"` to the `expected` tuple,
    positioned right after `"body_speeds"` (the `"body_lons", "body_lats", "body_speeds",` line
    becomes `"body_lons", "body_lats", "body_speeds", "body_decl",`). Order must match the dtype.
  - `test_dtype_subarray_shapes` parametrize (lines 57-66): add `("body_decl", (14,)),`.
  - `test_dtype_scalar_field_kinds` parametrize (lines 77-98): add `("body_decl", "f", 8),` to
    the f8 subarray group (alongside body_lons/body_lats/body_speeds).
  - `test_dtype_supports_vectorized_construction` (lines 116-128): add
    `assert arr["body_decl"].shape == (5, 14)`.
  - `test_dtype_scalar_zero_dim_construction` (lines 131-139): add
    `assert elem["body_decl"].shape == (14,)`.
  - `test_body_count_frozen_at_fourteen` (line 218): leave at 14 — body COUNT is unchanged.
Add a docstring (or extend an existing one) noting that `body_decl` is the v1.5 additive
dtype-version bump (parallel to the v1.3 13→14 ratchet), the field is additive (not an axis
change), and Kala's positional/`.view()` impact is DOCUMENTED, not fixed here. This ratchet must
"go red" if the field is ever removed or reshaped.
  </action>
  <verify>
`source venv/bin/activate && pytest tests/charts/test_dtype.py -v` all pass, including the
new `body_decl` parametrize cases and the unchanged `test_body_count_frozen_at_fourteen`.
  </verify>
  <done>
`tests/charts/test_dtype.py` asserts `body_decl` in the field-name tuple (after body_speeds),
its (14,) subarray shape, f8 kind/itemsize, and (5,14)/(14,) construction shapes; the 14-body
count ratchet still passes; a docstring documents the v1.5 additive bump.
  </done>
</task>

</tasks>

<verification>
- `make test` passes (100% coverage; new compute_chart branch covered by the chart `body_decl` test).
- `make mypy` passes (ketu.charts.* strictly typed — the new declination block included).
- `make doc-gates` passes (the CHART_DTYPE `#:` doc block lists `body_decl`).
- A test proves chart `body_decl` == standalone `declination()` (no `np.empty` garbage, Pitfall 5).
- `git diff` shows body COUNT unchanged at 14; no fields reordered.
</verification>

<success_criteria>
- DECL-07 (chart half): every `compute_chart` output carries a populated `body_decl` matching standalone `declination()`; Returns inherit it for free (call `compute_chart`).
- DECL-08: the `CHART_DTYPE` layout ratchet guards the additive `body_decl` field (names, shape, kind/itemsize, construction shapes).
- `body_decl` derived from already-fetched positions (no S-loop, no double fetch) and is vectorized.
</success_criteria>

<output>
After completion, create `.planning/phases/33-lunar-declination/33-02-SUMMARY.md`
</output>
