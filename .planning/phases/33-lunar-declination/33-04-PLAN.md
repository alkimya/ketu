---
phase: 33-lunar-declination
plan: 04
type: execute
wave: 4
depends_on: ["33-01"]
files_modified:
  - docs/source/api.md
  - docs/source/concepts.md
  - docs/source/changelog.md
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/concepts.po
autonomous: true

must_haves:
  truths:
    - "The 4 public functions (declination, declination_velocity, is_ascending_declination, is_out_of_bounds) are documented in api.md, mirroring the is_retrograde style"
    - "concepts.md explains the aspect-centric montant/descendant framing (draconic month ~27.21 d, OOB nodal cycle) and the explicit β-vs-δ distinction"
    - "The changelog has a v1.5 entry covering declination + the additive body_decl dtype bump (Kala-impact noted)"
    - "FR .po for api + concepts are TRANSLATED (not English-fallback) and RECOMPILED to .mo before release"
  artifacts:
    - path: "docs/source/api.md"
      provides: "4 declination functions in the Calculations section"
      contains: "declination"
    - path: "docs/source/concepts.md"
      provides: "δ / montant-descendant / OOB / β-vs-δ concept section"
      contains: "declination"
    - path: "docs/source/changelog.md"
      provides: "v1.5 declination entry + body_decl additive dtype note"
      contains: "declination"
    - path: "docs/locale/fr/LC_MESSAGES/api.po"
      provides: "Translated FR msgstr for the new api.md content (no empty msgstr)"
      contains: "declination"
    - path: "docs/locale/fr/LC_MESSAGES/concepts.po"
      provides: "Translated FR msgstr for the new concepts.md content (no empty msgstr)"
      contains: "declinaison"
  key_links:
    - from: "docs/source/api.md / concepts.md"
      to: "docs/locale/fr/LC_MESSAGES/*.po"
      via: "make -C docs gettext && update-po regenerates msgids; translate msgstr; build-mo compiles"
      pattern: "msgstr"
---

<objective>
Document the declination feature en + fr — the four public functions, the aspect-centric
montant/descendant framing, the β-vs-δ distinction, the OOB concept, and the changelog entry —
with the FR `.po` translated AND recompiled to `.mo` (not left English-fallback).

Purpose: DECL-09. The feature must be discoverable and the β-vs-δ distinction explicit (Pitfall
1: "ascending Moon" means different things for β and δ). FR docs must ship translated for the
v1.5 PyPI release (MEMORY: project_fr_translations_before_release).
Output: EN prose in `api.md` + `concepts.md` + `changelog.md`, translated FR `.po` for
`api`/`concepts`, recompiled `.mo`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lunar-declination/33-RESEARCH.md
@.planning/research/DECLINATION.md

# Plan 01 settled the 4 function names/signatures/semantics being documented here.
@.planning/phases/33-lunar-declination/33-01-SUMMARY.md

# Source anchors (verified 2026-06-03):
@docs/source/api.md
@docs/source/concepts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN docs — api.md (4 functions) + concepts.md (montant/descendant, β-vs-δ, OOB) + changelog</name>
  <files>docs/source/api.md, docs/source/concepts.md, docs/source/changelog.md</files>
  <action>
EN source is MyST/Sphinx markdown.

api.md — the Calculations section starts at line 21; `is_retrograde` is documented at line 91.
Document the four new functions in the SAME style/format as `is_retrograde`/`is_ascending`
(public path `from ketu.calculations import ...` — they are NOT top-level `ketu.` exports;
api.md:3 already states "All public functions use submodule import paths"):
  - `declination(jdate, body)` → δ degrees [−90,+90], north +/south −; reuses the coordinates
    chain (Meeus 13.4 equivalent); scalar + vectorized over a jdate array.
  - `declination_velocity(jdate, body)` → dδ/dt °/day (forward FD, step 0.01).
  - `is_ascending_declination(jdate, body)` → True when dδ/dt > 0 (montante).
  - `is_out_of_bounds(jdate, body)` → True when |δ| > ε(jd) (true_obliquity).
If api.md uses autodoc directives (e.g. ``` ```{eval-rst} .. autofunction:: ``` ``` or
`{py:function}`), mirror that mechanism so the numpydoc docstrings render; if it's hand-written
prose, mirror that. Match whatever `is_retrograde` does.

concepts.md — the ecliptic-latitude (β) concept is at line 16. Add a declination section
(reusable text from the brief §2/§3) covering:
  - δ = equatorial declination (distinct from ecliptic latitude β); EXPLICIT β-vs-δ distinction
    — "ascending in β" ≠ "ascending in δ"; they flip on different days (Pitfall 1). `is_ascending`
    is the β-trajectory; `is_ascending_declination` is the biodynamic montant/descendant
    (δ-trajectory of the Moon).
  - montant/descendant = sign of dδ/dt; the Moon's δ cycle period ≈ draconic/nodal month 27.21 d
    (≈ 27.32 d tropical recurrence). Aspect-centric framing (Loc's biodynamics: trajectory of the
    Moon's own δ, NOT zodiacal sign conventions).
  - Out-of-bounds (OOB): |δ| > ε; the Moon goes OOB on a ~18.6-year nodal cycle (major lunar
    standstill). Threshold uses instantaneous ε(jd).

changelog.md — add a v1.5 entry: the four declination functions; the additive `body_decl` field
in `CHART_DTYPE` (note: additive dtype-version bump, body count unchanged at 14, downstream
positional/`.view()` consumers such as Kala must adapt — documented, not auto-migrated);
`is_ascending` (β) unchanged.

Keep `is_ascending`'s existing β documentation UNCHANGED (do not rewrite it as the biodynamic
ascending Moon — that role is now the δ trajectory's). Any `>>>` examples added in prose must be
consistent with the doctest gate (or use non-executed code fences if the .md isn't doctested —
check whether docs/source is in the doctest scope; the Makefile `doctest` target runs
`--doctest-modules ketu/`, so .md prose is likely NOT doctested, but verify).
  </action>
  <verify>
`source venv/bin/activate && make -C docs html` builds with no new warnings/errors referencing
the declination additions; the four function names and "out-of-bounds"/"montant"/"declination"
appear in the built `docs/build/html` (grep). `git diff docs/source/api.md` shows the 4 functions
documented in the is_retrograde style.
  </verify>
  <done>
api.md documents all 4 functions (is_retrograde style), concepts.md has the δ/montant-descendant/
OOB/β-vs-δ section, changelog.md has the v1.5 entry; `make -C docs html` clean; `is_ascending`
β doc unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: FR translation — update .po, translate msgstr, recompile .mo</name>
  <files>docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/concepts.po</files>
  <action>
Run the i18n flow (docs/Makefile targets verified: gettext, update-po, build-mo):
  1. `make -C docs gettext`        # regenerate POT from the updated EN .md
  2. `make -C docs update-po`      # sphinx-intl update -l fr → new msgids appear in api.po/concepts.po
  3. Translate EVERY new/empty `msgstr` in `api.po` and `concepts.po` into French (rigorous,
     idiomatic — match the existing FR register in these files; tutoiement style consistent with
     the rest). Do NOT leave English-fallback empty msgstr (MEMORY:
     project_fr_translations_before_release — FR must be translated, not fallback, before the
     v1.5 PyPI release). Key terms: declination = "déclinaison"; out-of-bounds = "hors limites";
     montant/descendant = keep "montant"/"descendant" (biodynamic terms); ecliptic latitude β
     vs declination δ distinction must survive translation.
  4. `make -C docs build-mo`       # compile .mo so the FR build is not stale
  5. `make -C docs html-fr` (or `html-all`) to confirm the FR build picks up the translations.
If `docs/migrate_translations.py` is part of the established flow, use it as the existing
phases did (check how prior FR translation phases ran it). Commit the updated `.po` (and `.mo`
if tracked — check `git status`/`.gitignore` for whether `.mo` is version-controlled; if `.mo`
is gitignored, the build step regenerating it is sufficient and CI/RTD recompiles).
  </action>
  <verify>
`grep -c 'msgstr ""' docs/locale/fr/LC_MESSAGES/api.po docs/locale/fr/LC_MESSAGES/concepts.po`
shows no NEW empty msgstr for the declination msgids (the header `msgstr ""` is fine; check the
declination-related msgids specifically are translated).
`source venv/bin/activate && make -C docs html-fr` builds; grep the FR build output for
"déclinaison" to confirm the translation rendered (not English-fallback).
  </verify>
  <done>
`api.po` and `concepts.po` have French `msgstr` for all new declination msgids (no
English-fallback), `.mo` recompiled, FR build renders "déclinaison"; ready for the v1.5 release.
  </done>
</task>

</tasks>

<verification>
- `make -C docs html` and `make -C docs html-fr` both build clean.
- The 4 functions are in api.md; the β-vs-δ distinction + montant/descendant + OOB are in concepts.md; changelog has the v1.5 entry.
- FR `.po` for api + concepts are translated (no English-fallback for the new msgids) and `.mo` recompiled.
- `is_ascending` β documentation unchanged.
</verification>

<success_criteria>
- DECL-09: declination documented en + fr — 4 public functions + aspect-centric montant/descendant framing (draconic ~27.21 d, OOB nodal cycle) + explicit β-vs-δ distinction; FR translated AND recompiled (not English-fallback) ahead of the v1.5 release.
</success_criteria>

<output>
After completion, create `.planning/phases/33-lunar-declination/33-04-SUMMARY.md`
</output>
