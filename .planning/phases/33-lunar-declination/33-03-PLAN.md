---
phase: 33-lunar-declination
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - ketu/composite/api.py
  - tests/composite/test_calculate_composite.py
  - tests/returns/test_lunar_return.py
autonomous: true

must_haves:
  truths:
    - "calculate_composite explicitly populates out['body_decl'] (not the silent 0.0 zero-fill trap)"
    - "composite body_decl is derived from the composite λ,β via the same coordinates chain (self-consistent)"
    - "a composite test asserts body_decl != 0 for an off-equator body (catches the zero-fill regression)"
    - "Returns inherit body_decl for free (they call compute_chart) — verified by a test, not just assumed"
    - "Synastry needs no change (emits a non-CHART grid dtype) — no body_decl expected there"
  artifacts:
    - path: "ketu/composite/api.py"
      provides: "out['body_decl'] derived from composite body_lons/body_lats + true_obliquity"
      contains: "out[\"body_decl\"]"
    - path: "tests/composite/test_calculate_composite.py"
      provides: "DECL-07 composite: body_decl populated, non-zero off-equator, matches chain"
      contains: "body_decl"
    - path: "tests/returns/test_lunar_return.py"
      provides: "DECL-07 returns: returned chart carries a populated body_decl"
      contains: "body_decl"
  key_links:
    - from: "ketu/composite/api.py:calculate_composite"
      to: "ketu.ephemeris.coordinates chain + true_obliquity"
      via: "derive δ from out['body_lons']/out['body_lats'] (already assigned), assign out['body_decl']"
      pattern: "out\\[\"body_decl\"\\]"
    - from: "tests/composite/test_calculate_composite.py"
      to: "out['body_decl']"
      via: "assert not all-zero for an off-equator body"
      pattern: "body_decl"
---

<objective>
Close the asymmetric downstream propagation of `body_decl`: explicitly populate it in Composite
(which otherwise silently zero-fills it), and add tests confirming Returns inherit it for free.

Purpose: DECL-07 crux. Composite allocates `np.zeros((), CHART_DTYPE)` and copies fields BY NAME,
so an un-named `body_decl` stays `0.0` everywhere — semantically "every body on the equator",
which looks valid and passes silently (Pitfall 4). Returns call `compute_chart` and inherit
`body_decl` for free (Plan 02); Synastry emits its own non-CHART dtype and needs nothing.
Output: an `out["body_decl"] = ...` assignment in `calculate_composite`, a composite test
asserting non-zero off-equator declination, and a returns test asserting inherited `body_decl`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lunar-declination/33-RESEARCH.md

# Plan 02 added body_decl to CHART_DTYPE + compute_chart; this plan propagates it to the
# downstream consumers (composite needs explicit work, returns are free).
@.planning/phases/33-lunar-declination/33-02-SUMMARY.md

# Source anchors (verified 2026-06-03):
@ketu/composite/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate out["body_decl"] in calculate_composite (RECOMMENDED: derive from composite λ,β)</name>
  <files>ketu/composite/api.py</files>
  <action>
`calculate_composite` allocates `out = np.zeros((), dtype=CHART_DTYPE)` (line 221) and copies
fields BY NAME. `out["body_lons"]` (line 235) and `out["body_lats"]` (lines 238-241) are
assigned (circular midpoint of λ, arithmetic mean of β). Because the new `body_decl` field is
NOT assigned, it stays `0.0` for every body — the silent zero-fill trap (Pitfall 4).

Per the research recommendation (Open Question 1, option (a) — derive via the chain for
self-consistency, NOT midpoint-of-parents'-δ, NOT documented-0.0): immediately AFTER the
`out["body_speeds"] = ...` assignment (ends line 245), derive `body_decl` from the composite
λ,β that were just assigned, using the same chain Plan 02 used in compute_chart:

  Add imports at the top (mirror the existing import area): `from ketu.ephemeris.coordinates
  import (spherical_to_rectangular, ecliptic_to_equatorial, rectangular_to_spherical,
  true_obliquity)`.

  Then:
    eps = true_obliquity(float(out["jd"]))   # composite jd already assigned at line 225
    x, y, z = spherical_to_rectangular(out["body_lons"], out["body_lats"], 1.0)  # each (14,)
    xe, ye, ze = ecliptic_to_equatorial(x, y, z, eps)
    _, decl, _ = rectangular_to_spherical(xe, ye, ze)
    out["body_decl"] = decl

`out` is 0-d (shape ()), so `out["body_lons"]` is shape (14,) and a scalar ε broadcasts cleanly —
no `[..., None]` needed here. `ketu.composite.*` typing: confirm whether it's in the relaxed
mypy override; if NOT, keep this block `--strict` clean (cast/asarray as needed; do NOT touch
`true_obliquity`'s hint). Add a one-line comment noting this is the self-consistent derivation
(δ of the composite midpoint chart), parallel to how `body_lats` is the midpoint — NOT a midpoint
of the parents' declinations.
  </action>
  <verify>
`source venv/bin/activate && python -c "
import numpy as np
from ketu.composite.api import calculate_composite
from ketu.charts.api import compute_chart
from ketu.calculations import utc_to_julian
from datetime import datetime, timezone
jd_a = utc_to_julian(datetime(1990,4,20,12,0,tzinfo=timezone.utc))
jd_b = utc_to_julian(datetime(1985,11,3,6,0,tzinfo=timezone.utc))
ca = compute_chart(jd_a, 48.85, 2.35, 'placidus')
cb = compute_chart(jd_b, 40.71, -74.0, 'placidus')
comp = calculate_composite(ca, cb)   # adjust signature if it differs
d = np.asarray(comp['body_decl'])
print('body_decl', np.round(d,3))
assert np.any(np.abs(d) > 0.01), 'body_decl is all-zero (zero-fill trap not closed)'
print('OK non-zero')
"`
(Adjust `calculate_composite` call signature to the real one.) Prints non-zero declinations.
`make mypy` passes.
  </verify>
  <done>
`calculate_composite` assigns `out["body_decl"]` derived from the composite λ,β via the chain;
the field is no longer the silent 0.0 zero-fill; mypy clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Composite propagation test — body_decl non-zero + matches chain</name>
  <files>tests/composite/test_calculate_composite.py</files>
  <action>
Add a test (mirror the existing composite test style/fixtures in this file) for DECL-07 composite:
  - Build a composite of two charts with an off-equator body (the Moon at a high-|δ| date works).
  - Assert `comp["body_decl"]` is NOT all-zero (`np.any(np.abs(comp["body_decl"]) > 0.01)`) —
    this is the anti-regression for the zero-fill trap (Pitfall 4): a composite test that passes
    while `body_decl` is all zeros is the failure mode to prevent.
  - Assert `comp["body_decl"]` equals the chain re-derived inline from `comp["body_lons"]`,
    `comp["body_lats"]`, and `true_obliquity(comp["jd"])` to < 1e-9 (proves self-consistency,
    not midpoint-of-parents).
  - Assert shape is (14,).
Reference the v1.5 decision in the docstring (Open Question 1 resolved to "derive via chain").
  </action>
  <verify>
`source venv/bin/activate && pytest tests/composite/test_calculate_composite.py -k "decl" -v` passes.
`pytest tests/composite/ --cov=ketu.composite --cov-report=term-missing -q` — the new body_decl branch in composite/api.py is covered.
  </verify>
  <done>
A composite test asserts `body_decl` is non-zero off-equator and matches the inline chain
re-derivation to < 1e-9; the composite coverage gate stays at 100%.
  </done>
</task>

<task type="auto">
  <name>Task 3: Returns inheritance test (free) + confirm Synastry no-op</name>
  <files>tests/returns/test_lunar_return.py</files>
  <action>
Returns call `compute_chart` directly, so they inherit `body_decl` for free (Anchor 8). Add a
test (mirror the existing lunar-return test style/fixtures) that:
  - Computes a lunar return chart via the public returns API.
  - Asserts the returned array's dtype is `CHART_DTYPE` (has a `body_decl` field) and that
    `body_decl` is populated (matches a standalone `declination()` at the return's `jd` for a
    sample body to < 1e-9, OR at minimum is not the uninitialized-garbage / all-NaN case — prefer
    the standalone-match assertion since compute_chart uses np.empty).
This is the DECL-07 "inherited for free" verification — proving the inheritance, not assuming it.

Synastry: NO code change and NO new test required — it emits its own grid dtype
(`body_a/body_b/orb/...`), not a CHART_DTYPE, and declination aspects are out of scope. If a
quick confirmation is cheap, you MAY assert in passing that a synastry result's dtype has no
`body_decl` field (documents the deliberate no-op), but this is optional — do not expand scope.
  </action>
  <verify>
`source venv/bin/activate && pytest tests/returns/test_lunar_return.py -k "decl" -v` passes.
  </verify>
  <done>
A returns test confirms the returned chart carries a populated `body_decl` (matching standalone
`declination()`); Synastry confirmed unaffected (no change needed).
  </done>
</task>

</tasks>

<verification>
- `make test` passes (100% coverage; composite body_decl branch covered).
- `make mypy` passes.
- Composite `body_decl` is provably non-zero off-equator (zero-fill trap closed) and self-consistent with the chain.
- Returns inheritance proven by test; Synastry unaffected.
</verification>

<success_criteria>
- DECL-07 (downstream): Composite explicitly populates `body_decl` (derived via chain, self-consistent — Open Question 1 resolved); Returns inherit it for free (tested); Synastry no-op confirmed.
- The composite zero-fill trap (Pitfall 4) has an explicit anti-regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/33-lunar-declination/33-03-SUMMARY.md`
</output>
