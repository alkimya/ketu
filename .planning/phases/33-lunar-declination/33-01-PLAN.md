---
phase: 33-lunar-declination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/calculations.py
  - tests/test_declination.py
  - tests/test_coordinates_coverage.py
autonomous: true

must_haves:
  truths:
    - "declination(jdate, body) returns δ in degrees (north +, south −, in [−90,+90]) for any of the 14 bodies"
    - "declination called on a NumPy jdate array returns an array element-wise (no Python loop in the hot path)"
    - "declination_velocity(jdate, body) returns dδ/dt °/day via forward finite difference, step 0.01 d, no wraparound"
    - "is_ascending_declination(jdate, body) is True exactly when dδ/dt > 0 — distinct from the unchanged β-based is_ascending"
    - "is_out_of_bounds(jdate, body) is True exactly when |δ| > true_obliquity(jd)"
    - "A regression test pins declination() to machine-precision agreement with the ecliptic_to_equatorial -> rectangular_to_spherical chain AND Meeus 13.4"
    - "is_ascending (β) stays byte-for-byte UNCHANGED"
  artifacts:
    - path: "ketu/calculations.py"
      provides: "declination, declination_velocity, is_ascending_declination, is_out_of_bounds + all 4 added to __all__"
      contains: "def declination"
    - path: "tests/test_declination.py"
      provides: "Scalar + vectorized + OOB + montant/descendant + all-14-bodies unit tests (DECL-01,02,04,05,06)"
      contains: "def test_"
    - path: "tests/test_coordinates_coverage.py"
      provides: "DECL-03 equivalence regression: declination() == chain == Meeus 13.4 to machine precision"
      contains: "Meeus"
  key_links:
    - from: "ketu/calculations.py:declination"
      to: "ketu.ephemeris.coordinates chain"
      via: "spherical_to_rectangular -> ecliptic_to_equatorial(true_obliquity) -> rectangular_to_spherical, take element [1]"
      pattern: "rectangular_to_spherical"
    - from: "ketu/calculations.py:declination_velocity"
      to: "declination"
      via: "self-computed forward FD (declination(jd+0.01,b)-declination(jd,b))/0.01"
      pattern: "0\\.01"
    - from: "ketu/calculations.py:is_ascending_declination"
      to: "declination_velocity"
      via: "bool(declination_velocity(jdate, body) > 0)"
      pattern: "declination_velocity"
    - from: "ketu/calculations.py:is_out_of_bounds"
      to: "true_obliquity"
      via: "abs(declination(jd,b)) > true_obliquity(jd)"
      pattern: "true_obliquity"
---

<objective>
Add the four public declination functions to `ketu/calculations.py`, reusing the existing
`coordinates.py` rectangular chain (no new astronomy), plus their unit/regression tests.

Purpose: This is the foundational new surface of Phase 33 (DECL-01,02,03,04,05,06). Every
later plan (chart wiring, composite, docs) depends on these functions existing and being
correct.
Output: `declination`, `declination_velocity`, `is_ascending_declination`, `is_out_of_bounds`
in `calculations.py` (+ `__all__`), a new `tests/test_declination.py`, and a DECL-03
equivalence regression added to `tests/test_coordinates_coverage.py`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lunar-declination/33-RESEARCH.md
@.planning/research/DECLINATION.md

# Source anchors (verified 2026-06-03):
@ketu/calculations.py
@ketu/ephemeris/coordinates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four declination functions to calculations.py</name>
  <files>ketu/calculations.py</files>
  <action>
Add four functions after `is_ascending` (which ends at line 438 — DO NOT TOUCH `is_ascending`,
lines 413-438; it reads `lat_velocity` = β-speed and must stay byte-for-byte unchanged).

Imports: at the top of the module add the coordinate-chain names. The module already imports
from `ketu.ephemeris`; add `spherical_to_rectangular`, `ecliptic_to_equatorial`,
`rectangular_to_spherical`, `true_obliquity` from `ketu.ephemeris.coordinates` (verify the
existing import style in the file header and mirror it; they live in
`ketu/ephemeris/coordinates.py`).

1. `declination(jdate, body)` — DECL-01, DECL-02. Reuse the chain (LOCKED, per STATE.md — NOT
   the Meeus one-liner):
       lam = long(jdate, body)          # ecliptic longitude (deg)
       bet = lat(jdate, body)           # ecliptic latitude (deg)
       x, y, z = spherical_to_rectangular(lam, bet, 1.0)
       eps = true_obliquity(jdate)      # instantaneous ε (deg) — per STATE.md, NOT mean_obliquity
       xe, ye, ze = ecliptic_to_equatorial(x, y, z, eps)
       _, decl, _ = rectangular_to_spherical(xe, ye, ze)
       return decl                      # element [1] = latitude on the equatorial sphere = δ
   The whole chain is vectorized (verified max|Δ|=7.1e-15 over arrays), so a NumPy `jdate`
   array flows through element-wise with NO Python loop. `long`/`lat` call scalar
   `body_properties` — for the standalone array path use the chain directly on arrays; do NOT
   wrap in `np.array([... for jd in jds])` (Pitfall 2). Note: for a scalar `jdate`, `long`/`lat`
   return scalars and the chain returns a scalar — fine. For an array `jdate`, `long`/`lat`
   built on scalar `body_properties` are scalar-only; the simplest correct vectorized path is to
   fetch λ,β via the chain inputs that ARE array-safe. Confirm at implementation time whether
   `long`/`lat` broadcast over a jdate array; if they do NOT, obtain array λ,β from the
   vectorized batch path (`calc_planet_position_batch` in ketu/ephemeris/planets.py:608, columns
   0 and 1) so the array call stays loop-free. The scalar path MUST use `long`/`lat` for
   consistency with the rest of the module.
   δ ∈ [−90,+90] (rectangular_to_spherical line 86 uses arcsin). North +, south −.

2. `declination_velocity(jdate, body)` — DECL-04. Self-compute the forward FD (it CANNOT
   read-from-tuple like `lat_velocity` does — `body_properties` has NO `decl_speed` field):
       return (declination(jdate + 0.01, body) - declination(jdate, body)) / 0.01
   Step constant 0.01 mirrors the package-wide `jd_delta` used by `lat_velocity`'s underlying FD.
   NO longitude-style ±180 wraparound (δ is bounded, monotone through zero — unlike lon_speed).

3. `is_ascending_declination(jdate, body)` — DECL-05. The equatorial PARALLEL of `is_ascending`:
       return bool(declination_velocity(jdate, body) > 0)
   True when dδ/dt > 0 (montante). This is a DISTINCT physical quantity from `is_ascending`
   (β-rise) — they flip on different days. MUST NOT collide with or modify `is_ascending`.

4. `is_out_of_bounds(jdate, body)` — DECL-06:
       return bool(abs(declination(jdate, body)) > true_obliquity(jdate))
   Uses INSTANTANEOUS ε(jd) via `true_obliquity` (per STATE.md; the brief recommended
   mean_obliquity — OVERRIDDEN, honor STATE.md). True exactly when |δ| > ε.

Docstrings: mirror the numpydoc style of `lat` (lines 243-268) and `is_ascending`
(lines 413-438) EXACTLY — Parameters / Returns / Examples with a RUNNABLE `>>>` block (doctest
gate is BLOCKING). Use the same datetime/utc_to_julian preamble the other docstrings use; pick a
fixed date (e.g. 2025-01-15 12:00 UTC) and compute the literal expected output so `make doctest`
passes under ELLIPSIS+NORMALIZE_WHITESPACE. For `is_out_of_bounds`, the Moon at most dates is
in-bounds (False); pick the date accordingly. Body ID convention in Parameters: "Body ID (0-13)"
(14 bodies incl. Chiron).

Then add all four names to `__all__` (lines 508-533). Insert them in the "Body functions"
block, right after `"is_ascending",` (line 525): `declination`, `declination_velocity`,
`is_ascending_declination`, `is_out_of_bounds`. Do NOT add to `ketu/__init__.py` (LOCKED — the
public path is `from ketu.calculations import declination`, matching the documented
submodule-import convention; `is_ascending`/`lat_velocity` are themselves not top-level-exported).
  </action>
  <verify>
`source venv/bin/activate && python -c "from ketu.calculations import declination, declination_velocity, is_ascending_declination, is_out_of_bounds; from ketu.calculations import utc_to_julian; from datetime import datetime, timezone; import numpy as np; jd = utc_to_julian(datetime(2025,1,15,12,0,tzinfo=timezone.utc)); print('scalar', round(float(declination(jd,1)),4)); print('array', np.round(declination(np.array([jd, jd+1.0]), 1), 4)); print('vel', round(float(declination_velocity(jd,1)),4)); print('asc', is_ascending_declination(jd,1)); print('oob', is_out_of_bounds(jd,1))"`
returns a scalar δ, a length-2 array, a velocity, and two bools (oob False for the Moon here).
`make doctest` passes (new `>>>` examples run). `python -c "import ketu.calculations as c; assert all(n in c.__all__ for n in ['declination','declination_velocity','is_ascending_declination','is_out_of_bounds'])"`.
`git diff ketu/calculations.py` shows `is_ascending` (lines 413-438) UNCHANGED.
  </verify>
  <done>
All four functions importable from `ketu.calculations`, present in `__all__`, scalar AND array
`jdate` work for `declination` (array path loop-free), doctests pass, `is_ascending` byte-for-byte
unchanged, NOT added to `ketu/__init__.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: DECL-03 equivalence regression — declination() == chain == Meeus 13.4</name>
  <files>tests/test_coordinates_coverage.py</files>
  <action>
Add a regression test pinning machine-precision (`max|Δ|`) agreement between THREE computations
of δ, vectorized over a date array AND a sample of bodies:
  (a) `declination(jdate, body)` (the new function),
  (b) the explicit coordinates chain inline
      (`spherical_to_rectangular(λ,β,1) -> ecliptic_to_equatorial(...,true_obliquity(jd)) ->
      rectangular_to_spherical`, element [1]),
  (c) the direct Meeus eq. 13.4: `δ = degrees(arcsin(sin β·cos ε + cos β·sin ε·sin λ))` with
      β,λ in radians and ε = true_obliquity(jd) in radians.
Assert `np.max(np.abs(a - b))` and `np.max(np.abs(a - c))` are below a machine-precision
tolerance (e.g. `< 1e-9`; research measured 7.1e-15). Use `true_obliquity` for ε in BOTH (b) and
(c) so the comparison is apples-to-apples (per STATE.md lock; the brief's mean_obliquity rec is
overridden). Cover a date array (e.g. `np.linspace` over a year, ~50 points) and at least the Moon
plus one more body. Place it near the existing coordinate-coverage tests; mirror their style.
Add a docstring referencing DECL-03 and the STATE.md `true_obliquity` lock.
  </action>
  <verify>
`source venv/bin/activate && pytest tests/test_coordinates_coverage.py -k "decl or meeus or equiv" -v` passes (case-insensitive on the new test's name).
  </verify>
  <done>
A test asserts `declination()` agrees with both the explicit coordinates chain and the direct
Meeus 13.4 formula to < 1e-9 over a date array and multiple bodies; it passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Unit + vectorization tests for DECL-01,02,04,05,06</name>
  <files>tests/test_declination.py</files>
  <action>
Create `tests/test_declination.py` mirroring the structure/imports of `tests/test_ketu.py`
(`test_is_ascending` at line 335 is the template). Cover:
  - DECL-01 scalar: `declination(jd, body)` for a fixed date returns a float in [−90,+90];
    sign convention (north +, south −) sanity (e.g. Sun near solstice has |δ| ≈ ε).
  - DECL-02 vectorized: `declination(jd_array, body)` returns an array of the same shape, and is
    element-wise equal to the per-element scalar calls (proves no loop semantics drift). Assert
    the result is a `np.ndarray` for array input.
  - DECL-01 all 14 bodies: loop body_id 0..13 (incl. Chiron id 13), assert each returns a finite
    δ in [−90,+90]. (Chiron via BODY_STRATEGIES — confirm it computes without error.)
  - DECL-04 velocity: `declination_velocity(jd, body)` is finite and matches a manual forward FD
    `(declination(jd+0.01,b)-declination(jd,b))/0.01` exactly (it IS that, so assert equality);
    assert NO wraparound artifact near δ→0 crossings (velocity stays smooth/small, not ±360-ish).
  - DECL-05 montant/descendant: `is_ascending_declination(jd, b)` returns True iff
    `declination_velocity(jd, b) > 0`; pick a Moon date known ascending and one descending (or
    derive both from the velocity sign at two dates ~7 d apart within the ~27.21 d draconic cycle).
    Add an explicit assertion that `is_ascending_declination` and `is_ascending` (β) are DISTINCT
    — find at least one date where they DISAGREE for the Moon (proves they are independent
    quantities, Pitfall 1).
  - DECL-06 OOB: `is_out_of_bounds` returns False for the Moon at an in-bounds date (|δ| < ε) and
    True at an out-of-bounds date (the Moon's δ exceeds ε ~±28.5° during major lunar standstill,
    e.g. around 2025 — find a date where |δ(Moon)| > true_obliquity(jd) and assert True; assert
    False where |δ| < ε). Verify against `true_obliquity` (not mean).
Aim for 100% line/branch coverage of the four new functions (coverage gate fail_under=100). If
any defensive branch is unreachable, prefer a test over a pragma.
  </action>
  <verify>
`source venv/bin/activate && pytest tests/test_declination.py -v` all pass.
`pytest tests/test_declination.py tests/test_coordinates_coverage.py --cov=ketu.calculations --cov-report=term-missing -q` shows the four new functions fully covered (no missing lines in the declination block).
  </verify>
  <done>
`tests/test_declination.py` exists and passes; scalar + array + all-14-bodies + velocity + montant/descendant (incl. β-vs-δ disagreement) + OOB true/false cases covered; the four new functions reach 100% coverage.
  </done>
</task>

</tasks>

<verification>
- `make test` (full suite, 100% coverage gate) passes — no regression on the prior 1539 tests.
- `make doctest` passes (new docstring `>>>` examples run under ELLIPSIS+NORMALIZE_WHITESPACE).
- `make doc-gates` passes (interrogate ≥95 sphinx-style + numpydoc strict on the 4 new public functions).
- `make mypy` passes (ketu.calculations is in the relaxed override, so the `true_obliquity` `jd: float` hint won't block).
- `git diff` shows `is_ascending` and `core.aspects` UNCHANGED; nothing added to `ketu/__init__.py`.
</verification>

<success_criteria>
- DECL-01: `declination(jdate, body)` returns δ ∈ [−90,+90] for all 14 bodies (scalar).
- DECL-02: same call vectorizes over a NumPy `jdate` array with no Python loop in the hot path.
- DECL-03: regression test pins machine-precision (< 1e-9) agreement with the coordinates chain AND Meeus 13.4.
- DECL-04: `declination_velocity` = forward FD step 0.01, no wraparound.
- DECL-05: `is_ascending_declination` True iff dδ/dt > 0, distinct from β-based `is_ascending`.
- DECL-06: `is_out_of_bounds` True iff |δ| > true_obliquity(jd).
- All four functions in `ketu.calculations.__all__`; `is_ascending` byte-for-byte unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/33-lunar-declination/33-01-SUMMARY.md`
</output>
