---
phase: 24-chiron
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - tests/ephemeris/test_chiron_regression.py
autonomous: true

must_haves:
  truths:
    - "calc_planet_position(jd, 13) longitude matches pinned Swiss reference within 0.01° across 1950-2050"
    - "Reference longitudes are hardcoded (no pyswisseph, no seas_18.se1 at test time)"
    - "7 dates span Chiron's ~50.7yr period (one per ~10-20yr) including J2000 and a future date"
  artifacts:
    - path: "tests/ephemeris/test_chiron_regression.py"
      provides: "CHIR-03 pinned-reference accuracy regression test"
      min_lines: 25
  key_links:
    - from: "tests/ephemeris/test_chiron_regression.py"
      to: "ketu.ephemeris.planets.calc_planet_position"
      via: "parametrized assert delta < 0.01"
      pattern: "calc_planet_position"
---

<objective>
Pin Swiss-Ephemeris reference Chiron longitudes captured during generation and assert the
runtime evaluator agrees within the spike-validated 0.01° bound across 1950-2050. Zero pyswisseph
at test time — matches the composite/oracle pinned-constant convention.

Purpose: CHIR-03 — regression test pins reference values across dates over the ~50.7yr period.
Output: tests/ephemeris/test_chiron_regression.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-chiron/24-RESEARCH.md
@.planning/phases/24-chiron/24-01-SUMMARY.md
@tests/composite/test_oracle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the pinned-reference Chiron accuracy regression test</name>
  <files>tests/ephemeris/test_chiron_regression.py</files>
  <action>
Create `tests/ephemeris/test_chiron_regression.py` following the template in 24-RESEARCH Gap 3 and
the pinned-constant convention of tests/composite/test_oracle.py.

1. Define `_CHIRON_REFS: list[tuple[float, float]]` with the 7 (jd, expected_lon_deg) tuples
   CAPTURED in 24-01-SUMMARY (generator `--dump-refs`). Dates/jds:
   2433282.5 (1950-01-01), 2440587.5 (1970-01-01), 2447892.5 (1990-01-01), 2451545.0 (J2000),
   2455197.5 (2010-01-01), 2462501.5 (2030-01-01, future), 2469807.5 (2050-01-01).
   Use the EXACT longitudes from 24-01-SUMMARY — do not recompute (no pyswisseph at test time).
   Add a comment block documenting capture date + source (pyswisseph + seas_18.se1, retflag 260
   Moshier fallback acceptable) and that tolerance 0.01° is 11.6× looser than measured 0.000861°.
2. `TOLERANCE_DEG = 0.01`.
3. `@pytest.mark.parametrize("jd, expected_lon", _CHIRON_REFS)` test calling
   `calc_planet_position(jd, 13)`, computing wrap-aware `delta = abs(pos[0] - expected_lon)` with
   `if delta > 180: delta = 360 - delta`, asserting `delta < TOLERANCE_DEG` with an informative
   message (actual vs ref vs delta).
4. numpydoc-style docstring referencing CHIR-03.
5. IMPORTANT: if the lru_cache on calc_planet_position interferes, the test still works (data is
   static); no cache_clear needed.

If 24-01-SUMMARY does not contain the captured tuples, re-run
`SE_EPHE_PATH=<dir> python tools/gen_chiron_coeffs.py --dump-refs` to obtain them, then pin.
  </action>
  <verify>pytest tests/ephemeris/test_chiron_regression.py -v  → 7/7 pass, every delta < 0.01°.</verify>
  <done>7 parametrized cases pass; references hardcoded; no pyswisseph/seas_18.se1 imported at test time.</done>
</task>

</tasks>

<verification>
- pytest tests/ephemeris/test_chiron_regression.py -v → 7/7 green.
- `grep -n "swisseph\|importorskip" tests/ephemeris/test_chiron_regression.py` empty (pure pinned constants).
- Full suite + 100% coverage gate still holds: pytest tests/ -q.
</verification>

<success_criteria>
- CHIR-03 satisfied: pinned references over 1950-2050, all within 0.01°, zero pyswisseph at test time.
</success_criteria>

<output>
After completion, create `.planning/phases/24-chiron/24-04-SUMMARY.md` recording the pinned
reference table and the observed max delta across the 7 dates.
</output>
