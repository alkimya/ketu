---
phase: 24-chiron
plan: 05
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - tests/ephemeris/test_chiron_integration.py
  - ketu/__init__.py
autonomous: true

must_haves:
  truths:
    - "compute_chart output includes Chiron at index 13 in body_lons/body_lats/body_speeds"
    - "Aspect detection (aspect_matrix) covers the Chiron row/column"
    - "generate_cycle_series works with a Chiron pair (e.g. Sun-Chiron)"
    - "100% coverage gate, numpydoc validate, interrogate>=95%, make doctest all still pass"
  artifacts:
    - path: "tests/ephemeris/test_chiron_integration.py"
      provides: "CHIR-05 smoke tests: chart/aspect/cycle include Chiron"
      min_lines: 30
  key_links:
    - from: "tests/ephemeris/test_chiron_integration.py"
      to: "ketu.charts.compute_chart"
      via: "assert body_lons.shape[-1]==14 and Chiron index populated"
      pattern: "compute_chart"
    - from: "tests/ephemeris/test_chiron_integration.py"
      to: "ketu.cycles.generate_cycle_series"
      via: "Sun-Chiron pair"
      pattern: "Chiron"
---

<objective>
Prove Chiron participates in the full chart/aspect/cycle machinery with no special-casing
(CHIR-05), and confirm all quality gates still hold at 14 bodies. Also surface the breaking
13→14 contract in user-facing docs/version metadata where appropriate.

Purpose: CHIR-05 + verify the 100% coverage / numpydoc / doctest gates survive the ratchet.
Output: integration smoke tests + any docstring/module-doc touch-ups.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-chiron/24-RESEARCH.md
@ketu/charts/api.py
@ketu/cycles
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Chiron integration smoke tests (chart + aspect + cycle)</name>
  <files>tests/ephemeris/test_chiron_integration.py</files>
  <action>
Create `tests/ephemeris/test_chiron_integration.py` proving Chiron flows through the machinery:

1. test_compute_chart_includes_chiron: build a chart (use existing compute_chart helper/signature —
   read ketu/charts/api.py for the exact call, mirror an existing chart test's setup). Assert
   `chart["body_lons"].shape[-1] == 14`, the Chiron index (13) longitude is finite and in [0,360),
   and matches `calc_planet_position(jd, 13)[0]` within a small tolerance.
2. test_aspect_matrix_covers_chiron: assert `chart["aspect_matrix"].shape == (14, 14)` and that the
   Chiron row/column participates (e.g. diagonal handling consistent with other bodies; at least
   one off-diagonal entry computed for the Chiron row). Mirror an existing aspect_matrix test.
3. test_cycle_series_with_chiron: call `generate_cycle_series(timestamps, "Sun", "Chiron")` (read
   ketu/cycles for the exact API) and assert it returns a CYCLE_DTYPE array with body2=="Chiron"
   and finite separation/normalized values. This proves the cycle machinery accepts Chiron by name
   with no special-casing.
4. test_calculate_all_positions_has_14: assert `calculate_all_positions(jd)` (or its array result)
   has length/row-count 14 and the index-13 entry is Chiron.

Use existing test fixtures/helpers where present; keep assertions behavior-focused (CHIR-05 is
"participates with no special-casing").
  </action>
  <verify>pytest tests/ephemeris/test_chiron_integration.py -v passes; assertions confirm 14-body chart, (14,14) aspect matrix, Sun-Chiron cycle series.</verify>
  <done>Chiron participates in compute_chart, aspect_matrix, generate_cycle_series, calculate_all_positions — all green.</done>
</task>

<task type="auto">
  <name>Task 2: Docstring/version touch-ups + verify all quality gates hold</name>
  <files>ketu/__init__.py</files>
  <action>
1. In `ketu/__init__.py`: if it has a module docstring or __all__/body-count summary mentioning
   "13 bodies", update to 14 and note Chiron. Read it first — if no such mention exists, make no
   change (do not invent edits; this file is listed in case it carries a count). Leave __version__
   at 1.3.0 (per decision: public API additive, version stays 1.3.0).
   NOTE: any other docstring count mentions (planets.py calc_planet_position 0-13, core.py module
   doc, and charts/api.py "_vectorised_body_properties loops over 14 canonical bodies") are updated
   in plan 24-03 Task 1 — do NOT redo them here; only fix __init__.py if needed. As a final guard,
   grep the source for stale "13 canonical"/"13 astronomical"/"0-12" docstring text and fix any
   straggler (these should already be zero after 24-03).
2. Run ALL quality gates and confirm green (this is the gate-keeping task for the phase):
   - `pytest tests/ -q` (full suite, 100% pass at 14 bodies)
   - coverage 100%: `pytest tests/ --cov=ketu --cov-report=term-missing` (fail_under=100, zero new
     pragma; if chiron.py shows missing lines they were meant to be covered by 24-02 — flag in SUMMARY)
   - numpydoc validate + interrogate>=95% (run the project's configured command, e.g. via Makefile
     target or the verify_mypy.sh / make targets — check Makefile for `doctest`, `numpydoc`, `lint`)
   - `make doctest` (52+ doctests gate)
   - mypy --strict clean (use the repo's verify_mypy.sh if present, else `mypy --strict ketu`)
   If any gate fails, fix within scope (e.g. add a missing docstring to a new public symbol) and
   re-run. If a failure is structural and out-of-scope, document it precisely in the SUMMARY.
  </action>
  <verify>pytest tests/ -q green; coverage report shows 100%; make doctest passes; mypy --strict clean; numpydoc/interrogate gates pass.</verify>
  <done>All v1.3 quality gates green at 14 bodies; __init__.py count (if any) updated; version stays 1.3.0.</done>
</task>

</tasks>

<verification>
- pytest tests/ -q full suite green at 14 bodies.
- Coverage 100% (fail_under=100), numpydoc validate, interrogate>=95%, make doctest, mypy --strict all pass.
- Chiron appears in compute_chart, aspect_matrix, generate_cycle_series, calculate_all_positions.
</verification>

<success_criteria>
- CHIR-05 satisfied: Chiron participates across chart/aspect/cycle with no special-casing.
- All quality gates hold post-ratchet; version remains 1.3.0.
</success_criteria>

<output>
After completion, create `.planning/phases/24-chiron/24-05-SUMMARY.md` confirming all 5 CHIR
requirements satisfied, gate results, and a note for Phase 26 UPGRADING (Ketu↔Kala 13→14 break).
</output>
