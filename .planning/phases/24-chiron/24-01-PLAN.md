---
phase: 24-chiron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/gen_chiron_coeffs.py
  - ketu/data/__init__.py
  - ketu/data/chiron_coeffs.npz
autonomous: true

must_haves:
  truths:
    - "An offline generator produces chiron_coeffs.npz using pyswisseph (build-only) and seas_18.se1"
    - "The committed .npz contains lon/lat/dist Chebyshev coeffs + seg metadata in the locked layout"
    - "The generator self-validates max|Δλ| < 0.01° before writing the .npz"
    - "pyswisseph is imported ONLY inside the generator (tools/), never under ketu/"
  artifacts:
    - path: "tools/gen_chiron_coeffs.py"
      provides: "Offline Chiron Chebyshev coefficient generator (pyswisseph build-only)"
      min_lines: 120
    - path: "ketu/data/__init__.py"
      provides: "Empty file making ketu.data a package"
    - path: "ketu/data/chiron_coeffs.npz"
      provides: "Embedded Chebyshev coefficients (lon/lat/dist) for Chiron 1950-2050"
  key_links:
    - from: "tools/gen_chiron_coeffs.py"
      to: "ketu/data/chiron_coeffs.npz"
      via: "np.savez_compressed --output"
      pattern: "savez_compressed"
---

<objective>
Build the offline Chiron Chebyshev coefficient generator and produce the embedded
`.npz` artifact that all downstream runtime code reads. This is the foundation: no
runtime evaluator can be tested until the `.npz` exists and is committed.

Purpose: CHIR-01 — coeffs are generated by an offline pyswisseph-build-only generator
and shipped as a `.npz` inside the package; runtime stays 100% NumPy.
Output: `tools/gen_chiron_coeffs.py`, `ketu/data/__init__.py`, committed
`ketu/data/chiron_coeffs.npz`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-spike-chiron/23-DECISION.md
@.planning/phases/23-spike-chiron/spike_chiron_chebyshev.py
@.planning/phases/24-chiron/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the offline generator tools/gen_chiron_coeffs.py</name>
  <files>tools/gen_chiron_coeffs.py</files>
  <action>
Create `tools/gen_chiron_coeffs.py` — a hardened production version of the spike script
`.planning/phases/23-spike-chiron/spike_chiron_chebyshev.py`. It runs ONCE at build time
with the pyswisseph `[test]` extra installed and `seas_18.se1` available; it is NEVER
imported by the package and NEVER collected by pytest (lives in tools/, outside
testpaths=["tests"]).

LOCKED parameters from 23-DECISION.md — do NOT change these:
- Range: 1950-01-01 .. 2050-01-01 UTC via `swe.julday(1950,1,1,0.0)` and `swe.julday(2050,1,1,0.0)`.
- seg_len = 32.0 days, degree = 10, n_segments = ceil(total_days/32) = 1142.
- 3 quantities: lon (unwrapped before fit), lat, dist.
- Fit: `Chebyshev.fit(t_fit, y_fit, degree, domain=[-1.0, 1.0])` with n_fit = degree+8 nodes,
  t in [-1,1] mapped to [jd_s, jd_s+seg_len].
- Oracle call: `swe.set_ephe_path(<dir with seas_18.se1>)` then
  `swe.calc_ut(jd, swe.CHIRON, swe.FLG_SWIEPH | swe.FLG_SPEED)`; lon=xx[0], lat=xx[1], dist=xx[2].
  retflag 260 (Moshier fallback) is ACCEPTABLE per 23-DECISION §3.2.

Requirements (differences from spike):
1. argparse with `--output` (default `ketu/data/chiron_coeffs.npz`) and `--help` docstring.
2. Add `--dump-refs` flag: print pinned reference longitudes for 7 dates
   (jd values: 2433282.5, 2440587.5, 2447892.5, 2451545.0, 2455197.5, 2462501.5, 2469807.5)
   as a copy-pasteable Python list of (jd, lon) tuples for plan 24-04. Each lon is `xx[0] % 360.0`.
3. Reuse the spike's per-quantity sampling (unwrap longitude, raw lat/dist) and per-segment
   `Chebyshev.fit`. Collect all coeffs into arrays:
   lon_coeffs/lat_coeffs/dist_coeffs each shape (1142, 11), float64; seg_starts shape (1142,) float64
   (jd_s for each segment = jd0 + si*32).
4. Write with `np.savez_compressed(output, lon_coeffs=..., lat_coeffs=..., dist_coeffs=...,
   seg_starts=..., seg_len=np.float64(32.0), degree=np.int32(10),
   jd_start=np.float64(jd0), jd_end=np.float64(jd1))`. Exact array names per 23-DECISION §4.
5. VALIDATION GATE before saving: re-evaluate every segment on a dense validation grid using a
   pure-NumPy `np.polynomial.chebyshev.chebval(t, coeffs[si])` evaluator (NOT the Chebyshev object),
   compute max|Δλ| vs oracle, and ABORT with sys.exit(1) if max|Δλ| >= 0.01°. Print the achieved
   max|Δλ| (expected ~0.000861°).
6. swisseph imported ONLY inside functions (matching spike), never at module top-level.
7. numpydoc-style docstrings on all functions; module docstring explaining build-only nature.
8. French docstrings/comments are fine (Sophie persona) — match the spike's style.

Do NOT add the generator to pyproject packages or any import path. It is standalone tooling.
  </action>
  <verify>python -c "import ast; ast.parse(open('tools/gen_chiron_coeffs.py').read())" succeeds; `grep -n "import swisseph" tools/gen_chiron_coeffs.py` shows imports only inside function bodies (indented), not at column 0.</verify>
  <done>Generator parses, has argparse --output/--dump-refs, locked params seg=32/deg=10/1142 segments, validation gate aborting on max|Δλ|>=0.01°, savez_compressed with the 8 named arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Create ketu/data package and generate + commit the .npz</name>
  <files>ketu/data/__init__.py, ketu/data/chiron_coeffs.npz</files>
  <action>
1. Create empty `ketu/data/__init__.py` (required so setuptools recognises ketu.data as a package).
2. Run the generator to produce the artifact:
   `SE_EPHE_PATH=<dir containing seas_18.se1> python tools/gen_chiron_coeffs.py --output ketu/data/chiron_coeffs.npz`
   The spike used `/home/loc/workspace/rahu/kerykeion/kerykeion/sweph` (contains seas_18.se1, 217 KB).
   pyswisseph must be installed (it is in the `[test]` extra). If pyswisseph or seas_18.se1 is
   missing, install/locate them first — this is a build-only step.
3. Confirm the generator's validation gate printed max|Δλ| < 0.01° (expected ~0.000861°).
4. Verify the .npz loads and has correct shapes/dtypes in pure NumPy:
   `python -c "import numpy as np; d=np.load('ketu/data/chiron_coeffs.npz'); assert d['lon_coeffs'].shape==(1142,11); assert d['seg_starts'].shape==(1142,); assert float(d['seg_len'])==32.0; assert int(d['degree'])==10; print('OK', sorted(d.files))"`
5. Run `--dump-refs` and SAVE the printed (jd, lon) tuples into the SUMMARY for plan 24-04 to pin.

Do NOT add packaging config here (pyproject/MANIFEST) — that is done in plan 24-03 alongside
the rest of the wiring. This plan only produces and commits the data artifact + package marker.
  </action>
  <verify>The np.load assertion one-liner prints "OK ['degree', 'dist_coeffs', 'jd_end', 'jd_start', 'lat_coeffs', 'lon_coeffs', 'seg_len', 'seg_starts']"; `ls -la ketu/data/chiron_coeffs.npz` shows a file (< 303 KB).</verify>
  <done>ketu/data/__init__.py exists (empty); ketu/data/chiron_coeffs.npz exists with locked layout and validated accuracy; 7 reference (jd, lon) tuples captured in SUMMARY for 24-04.</done>
</task>

</tasks>

<verification>
- `pytest --collect-only 2>&1 | grep "tools/"` is empty (generator not collected).
- `grep -rn "import swisseph" ketu/` returns nothing (AGPL ratchet: no runtime swisseph).
- The .npz is committed and loadable in pure NumPy with the locked array names/shapes/dtypes.
</verification>

<success_criteria>
- tools/gen_chiron_coeffs.py exists, self-validates < 0.01°, never imports swisseph at module level.
- ketu/data/__init__.py (empty) and ketu/data/chiron_coeffs.npz committed.
- Reference (jd, lon) tuples for 7 dates captured for plan 24-04.
</success_criteria>

<output>
After completion, create `.planning/phases/24-chiron/24-01-SUMMARY.md` — include the 7 captured
(jd, lon) reference tuples verbatim so plan 24-04 can pin them.
</output>
