---
phase: 24-chiron
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - ketu/core.py
  - ketu/ephemeris/planets.py
  - ketu/charts/core.py
  - ketu/composite/api.py
  - ketu/cache/ephemeris_cache.py
  - ketu/synastry/core.py
  - ketu/aspects/transits.py
  - pyproject.toml
  - MANIFEST.in
autonomous: true

must_haves:
  truths:
    - "calc_planet_position(jd, 13) returns Chiron position via BODY_STRATEGIES['Chiron']"
    - "Chiron appears in SWE_IDS[13], BODY_INDICES['Chiron'], and calculate_all_positions output"
    - "CHART_DTYPE subarrays are (14,) and (14,14); composite/cache/synastry counts updated"
    - "ketu.data is packaged (pyproject packages + package-data) and MANIFEST ships the .npz in sdist"
    - "No special-casing of Chiron beyond the per-body strategy entry (CHIR-05)"
  artifacts:
    - path: "ketu/ephemeris/planets.py"
      provides: "BODY_INDICES/SWE_IDS/BODY_STRATEGIES/avg_speeds entries for Chiron"
      contains: "Chiron"
    - path: "ketu/core.py"
      provides: "14th body row (Chiron) in bodies array"
      contains: "Chiron"
    - path: "ketu/charts/core.py"
      provides: "CHART_DTYPE with (14,) and (14,14) subarrays"
    - path: "pyproject.toml"
      provides: "ketu.data package + package-data *.npz"
      contains: "ketu.data"
  key_links:
    - from: "ketu/ephemeris/planets.py"
      to: "ketu/ephemeris/chiron.py"
      via: "import _chiron_scalar, _chiron_vec into BODY_STRATEGIES"
      pattern: "_chiron_scalar"
    - from: "pyproject.toml"
      to: "ketu/data/chiron_coeffs.npz"
      via: "package-data ketu.data = *.npz"
      pattern: "ketu.data"
---

<objective>
Wire Chiron into every source insertion point and execute the breaking 13→14 bodies-axis
ratchet across all source-side dtype/shape/count constants, plus packaging config. After this
plan, Chiron is body_id=13 everywhere in the library and ketu.data ships in the wheel and sdist.
This is the controlled breaking change (D-08).

Purpose: CHIR-02 (six insertion points), CHIR-04 (bodies axis 13→14, source side), CHIR-05
(no special-casing), and CHIR-01 packaging (ketu.data in wheel/sdist).
Output: updated source modules + pyproject.toml + MANIFEST.in. (Test-side count audit is plan 24-03b
NOT split here — handled within this plan's Task 3 to keep the breaking change atomic.)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-chiron/24-RESEARCH.md
@.planning/phases/23-spike-chiron/23-DECISION.md
@ketu/ephemeris/planets.py
@ketu/core.py
@ketu/charts/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Chiron in planets.py + core.py (the 6 insertion points)</name>
  <files>ketu/ephemeris/planets.py, ketu/core.py</files>
  <action>
In `ketu/ephemeris/planets.py`:
1. Import the strategy fns at top with the other ephemeris imports:
   `from .chiron import _chiron_scalar, _chiron_vec` (module-level import is fine — chiron.py has
   NO swisseph import, so the AGPL ratchet is honored).
2. BODY_INDICES (L35-49): add `"Chiron": 13,`.
3. SWE_IDS (L52-66): add `13: "Chiron",  # Centaur, embedded Chebyshev coeffs`.
4. BODY_STRATEGIES (L310-324): add `"Chiron": _BodyCalc(_chiron_scalar, _chiron_vec),`.
   This is the ONLY per-body wiring — no special-casing elsewhere (CHIR-05).
5. calc_planet_position error message (L352): change `Valid range: 0-12` to `Valid range: 0-13`.
6. calc_planet_position docstring (L341): change `Planet ID (0-12)` to `Planet ID (0-13)`.
7. calculate_speed_ratio avg_speeds dict (~L579-592): add `13: 0.01946,` (Chiron mean motion
   ≈ 360/(50.7yr*365.25) °/day). This is the real 6th insertion point — NOT ORBITAL_ELEMENTS.
   DO NOT add a Chiron row to ORBITAL_ELEMENTS / _elements.py (Chebyshev body, no Keplerian formula).

In `ketu/core.py`:
8. Append a Chiron row to the `bodies` structured array (L66-83) following the existing row format
   (id=13, name "Chiron", and whatever orb/other fields the dtype defines — match the Lilith row's
   shape; use a sensible orb consistent with outer/centaur bodies, e.g. same as Lilith/nodes).
   Read the existing bodies dtype and rows first to match field-for-field.
9. Update core.py module docstring (L14-25): "13 astronomical bodies" → "14", add Chiron (id 13)
   to the body-ID list.

Verify the router works end-to-end after wiring.
  </action>
  <verify>python -c "from ketu.ephemeris.planets import calc_planet_position, SWE_IDS, BODY_INDICES; assert SWE_IDS[13]=='Chiron'; assert BODY_INDICES['Chiron']==13; p=calc_planet_position(2451545.0,13); assert 0<=p[0]<360; print('Chiron lon', p[0])" ; python -c "import ketu.core as c; assert len(c.bodies)==14; print('bodies', len(c.bodies))"</verify>
  <done>calc_planet_position(jd,13) returns Chiron; SWE_IDS/BODY_INDICES/BODY_STRATEGIES/avg_speeds wired; core.bodies has 14 rows; no ORBITAL_ELEMENTS row added.</done>
</task>

<task type="auto">
  <name>Task 2: Update source-side dtype/shape/count constants + packaging</name>
  <files>ketu/charts/core.py, ketu/composite/api.py, ketu/cache/ephemeris_cache.py, ketu/synastry/core.py, ketu/aspects/transits.py, pyproject.toml, MANIFEST.in</files>
  <action>
Apply the dtype/shape ripple from 24-RESEARCH Gap 6 (these do NOT auto-propagate):

1. `ketu/charts/core.py` CHART_DTYPE: change every `(13,)` → `(14,)` and both `(13, 13)` → `(14, 14)`.
   Affected fields: body_lons, body_lats, body_speeds (each (13,)→(14,)); aspect_matrix, aspect_orbs
   (each (13,13)→(14,14)). Read the dtype first; change ONLY body-count subarrays — do not touch any
   field whose size is the aspect-type count (14) which is unrelated.
2. `ketu/composite/api.py`: `_BODY_COUNT = 13` (≈L80) → `14`.
3. `ketu/cache/ephemeris_cache.py`: `BODY_COUNT = 13` (≈L29) → `14`; add `"Chiron": 13` to BODY_IDS dict.
4. `ketu/synastry/core.py`: `SYNASTRY_BODY_COUNT: int = 15` → `16` (14 canonical + ASC + MC).
5. `ketu/aspects/transits.py`: `list(range(13))` (≈L480) → `list(range(len(SWE_IDS)))`
   (import/use SWE_IDS so it auto-tracks). If SWE_IDS not already imported there, add the import.
6. `pyproject.toml`:
   - packages list (L61): append `"ketu.data"`.
   - [tool.setuptools.package-data] (L63-64): add `"ketu.data" = ["*.npz"]` line under the `ketu = [...]`.
7. `MANIFEST.in`: add `recursive-include ketu/data *.npz` (after the existing recursive-include lines).

NOTE: `ketu/charts/api.py` _BODY_COUNT = len(_CANONICAL_BODIES) and calculate_all_positions
range(len(SWE_IDS)) AUTO-PROPAGATE — do NOT hardcode-edit them. returns/ and parts/ also auto-propagate.

Build-check the sdist includes the .npz:
`python -m build --sdist 2>/dev/null && tar tzf dist/*.tar.gz | grep chiron_coeffs.npz` (or
`python -c "import tomllib; ..."` sanity if build unavailable — at minimum verify pyproject parses).
  </action>
  <verify>python -c "from ketu.charts.core import CHART_DTYPE; assert CHART_DTYPE['body_lons'].shape==(14,); assert CHART_DTYPE['aspect_matrix'].shape==(14,14); print('CHART_DTYPE OK')"; python -c "from ketu.synastry.core import SYNASTRY_BODY_COUNT; assert SYNASTRY_BODY_COUNT==16"; python -c "from ketu.cache.ephemeris_cache import BODY_COUNT, BODY_IDS; assert BODY_COUNT==14 and BODY_IDS['Chiron']==13"; python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'ketu.data' in d['tool']['setuptools']['packages']"</verify>
  <done>CHART_DTYPE (14,)/(14,14); composite 14; cache 14 + Chiron in BODY_IDS; synastry 16; transits range(len(SWE_IDS)); ketu.data packaged; MANIFEST ships .npz.</done>
</task>

<task type="auto">
  <name>Task 3: Update ALL test body-count / axis-index assertions (the ratchet)</name>
  <files>tests/test_ketu.py, tests/charts/test_dtype.py, tests/test_planets_coverage.py, tests/test_transits.py, tests/test_transits_coverage.py, tests/composite/test_calculate_composite.py, tests/charts/test_compute_chart.py, tests/charts/test_aspect_matrix.py, tests/synastry/test_dtype.py, tests/synastry/test_calculate_synastry.py, tests/synastry/test_applying.py</files>
  <action>
Apply the authoritative audit from 24-RESEARCH Gap 5. THREE classes — handle each correctly:

BODY-COUNT (13→14), update:
- tests/test_ketu.py:110 — `len(bodies) == 13` → `== 14`; RENAME test
  `test_body_count_frozen_at_thirteen` → `test_body_count_frozen_at_fourteen` (and its docstring).
- tests/charts/test_dtype.py:218-235 — `_BODY_COUNT == 13` → `14` (+ docstring).
- tests/charts/test_dtype.py:60-74 — `("body_lons", (13,))` etc → `(14,)`; `(13,13)` → `(14,14)`.
- tests/charts/test_dtype.py:116-129 — `.shape == (5, 13)` → `(5, 14)`.
- tests/charts/test_dtype.py:132-139 — `.shape == (13,)` → `(14,)`.
- tests/test_planets_coverage.py:178 `len(positions)==13`→14; :375 `range(13)`→`range(14)`; :531 `range(13)`→`range(14)`.
- tests/test_transits.py:152,178 `len(natal)==13`→14.
- tests/test_transits_coverage.py:307 `len(natal)==13`→14.
- tests/composite/test_calculate_composite.py:27 `range(13)`→`range(14)`; also extend `_BODY_NAMES`
  list with "Chiron" if it enumerates body names.
- tests/charts/test_compute_chart.py:138 `range(13)`→`range(14)`; :184 `range(13)`→`range(14)`.
- tests/charts/test_aspect_matrix.py:123,173,344,368 `range(13)` / `combinations(range(13),2)`→14.
- tests/synastry/test_dtype.py:122-127 `SYNASTRY_BODY_COUNT==15`→16.

AXIS-INDEX (synastry ASC/MC shift: OLD ASC=13/MC=14 → NEW ASC=14/MC=15 after Chiron occupies 13):
- tests/synastry/test_calculate_synastry.py:140-141, 225,227,235,237, 384 — `==13`(ASC)→`==14`,
  `==14`(MC)→`==15`. Read each assertion's surrounding context to confirm it is the ASC/MC axis
  (not a body count) before flipping. Be precise: ASC 13→14, MC 14→15.
- tests/synastry/test_applying.py:135-136,167-168 — `==13`(ASC)→`==14`, `==14`(MC)→`==15`.

DO NOT CHANGE (ASPECT-COUNT, unrelated — leave at 14/13):
- tests/charts/test_dtype.py:169 `aspect_matrix[0,0,1]==13` (Opposition aspect index) — KEEP.
- tests/test_aspect_presets.py:46-47,67,80,89,209 (14 aspect types) — KEEP.
- tests/synastry/test_dtype.py:172 `aspect_type==13` (Opposition index) — KEEP.

After edits run the full suite. Every change here is the deliberate ratchet; the suite must be
green at 14 bodies.
  </action>
  <verify>pytest tests/ -q  → all green at 14 bodies. `grep -rn "frozen_at_thirteen" tests/` returns nothing (renamed). `grep -rn "== 13\b" tests/charts/test_dtype.py` only shows the aspect-index line at ~169.</verify>
  <done>Full suite green at 14 bodies; body-count assertions = 14; synastry axis ASC=14/MC=15; aspect-count assertions untouched; freeze test renamed to _fourteen.</done>
</task>

</tasks>

<verification>
- pytest tests/ -q passes (full suite green at 14 bodies).
- `grep -rn "import swisseph" ketu/` empty (AGPL ratchet honored).
- mypy --strict clean on changed modules; numpydoc/interrogate gates pass.
- compute_chart / calculate_all_positions / aspect detection include Chiron (verified by 24-05).
</verification>

<success_criteria>
- Chiron wired at all 6 insertion points, no special-casing.
- Bodies axis is 14 everywhere; CHART_DTYPE/composite/cache/synastry/transits updated.
- ketu.data packaged in wheel + sdist; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/24-chiron/24-03-SUMMARY.md` listing the exact
old→new values changed (body counts, axis indices, dtype shapes) for traceability and for the
Phase 26 UPGRADING note (Ketu↔Kala 13→14 positional contract break).
</output>
