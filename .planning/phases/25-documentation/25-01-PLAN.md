---
phase: 25-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/conf.py
  - docs/source/index.md
  - docs/source/api.md
  - docs/source/quickstart.md
  - docs/source/concepts.md
  - docs/source/examples.md
  - docs/source/architecture.md
  - docs/source/changelog.md
  - docs/source/migration.md
  - docs/source/installation.md
  - docs/source/performance.md
  - docs/source/contributing.md
  - docs/source/acknowledgments.md
autonomous: true

must_haves:
  truths:
    - "Every code example in api.md and quickstart.md uses correct submodule import paths (no `ketu.long()`-style top-level access)"
    - "api.md documents the v1.1/v1.2/v1.3 subpackages (ketu.aspects configurable sets, ketu.houses, ketu.charts, ketu.synastry, ketu.composite, ketu.returns, ketu.parts) and Chiron body_id=13"
    - "index.md body table lists Chiron and the toctree owns the 5 new-page slots so 25-02's pages are not orphaned"
    - "conf.py version/release reads 1.3.0"
    - "changelog.md has v1.1.0, v1.2.0, v1.3.0 sections; migration.md has v1.0→v1.1→v1.2→v1.3 sections including the 13→14 body breaking change"
    - "English HTML build produces no NEW warnings vs the 1-warning baseline"
  artifacts:
    - path: docs/source/conf.py
      provides: "Version bumped to 1.3.0"
      contains: "1.3.0"
    - path: docs/source/index.md
      provides: "Updated toctree (with 5 new page slots) + Chiron in body table + corrected Quick Example imports"
      contains: "chiron"
    - path: docs/source/api.md
      provides: "Hand-written API reference for all v1.3 subpackages with correct import paths"
      contains: "from ketu.charts import compute_chart"
    - path: docs/source/changelog.md
      provides: "v1.1/v1.2/v1.3 changelog entries"
      contains: "1.3.0"
  key_links:
    - from: docs/source/index.md
      to: "houses.md, relational_charts.md, predictive_charts.md, arabic_parts.md, chiron.md"
      via: "User Guide toctree entries"
      pattern: "relational_charts"
    - from: docs/source/api.md
      to: "ketu.charts / ketu.houses / ketu.parts public API"
      via: "documented import paths matching RESEARCH §Real Public API Surface"
      pattern: "from ketu\\."
---

<objective>
Bring the 12 frozen `docs/source/` pages (stuck at v1.0, 2026-02-12) up to the full current v1.1/v1.2/v1.3 API surface. Fix every broken `ketu.long()`-style top-level import (those functions are NOT in `ketu.__all__`), document the eight feature areas shipped since v1.0, bump `conf.py` to 1.3.0, and wire the five new-page slots into `index.md`'s toctree so Plan 25-02's pages will not orphan.

This plan satisfies DOC-10.

Purpose: The existing pages document an API that no longer works as written. Users copying examples from `quickstart.md`/`api.md` hit `AttributeError`. Eight major features have zero docs.
Output: 12 updated `.md` pages + `conf.py`; English Sphinx build clean (no new warnings).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-documentation/25-RESEARCH.md

# RESEARCH is authoritative. Key constraints repeated here so the executor cannot miss them:
#
# 1. BROKEN VENV SHEBANGS — never call `sphinx-build`/`sphinx-intl` scripts directly.
#    Build English HTML ONLY via:
#      python3 -m sphinx.cmd.build -b html docs/source docs/build/html
#
# 2. api.md is HAND-WRITTEN (no autodoc). Every subpackage documented manually.
#
# 3. Correct import paths (RESEARCH §"Real Public API Surface") — examples MUST use these:
#      from ketu.calculations import long, lat, body_sign, is_retrograde, positions, body_name
#      from ketu.ephemeris.time import utc_to_julian, local_to_utc
#      from ketu.aspects import get_aspect, calculate_aspects, get_orb, CLASSICAL, TRADITIONAL, EXTENDED, resolve_aspect_set, AspectSetSpec
#      from ketu.houses import calculate_houses, house_of, HOUSES_DTYPE, SYSTEMS, HighLatitudeError, register
#      from ketu.charts import compute_chart, is_day_chart, CHART_DTYPE
#      from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE
#      from ketu.composite import calculate_composite, circular_midpoint
#      from ketu.returns import solar_return, lunar_return
#      from ketu.parts import PARTS, calculate_part, calculate_all_parts, register, get_part
#      from ketu.display import print_positions, print_aspects
#      from ketu.cli import main
#    ketu top-level STILL exports: bodies, aspects, signs, HOUSES_DTYPE, HighLatitudeError,
#      HOUSE_SYSTEMS, calculate_houses, house_of, __version__.
#
# 4. Chiron = body_id=13 (14th body). No new module; accessed via long(jd, 13) etc. Range 1950-2050.
#
# 5. Doc examples are display-only ```python``` code blocks (MyST). NOT pytest-collected,
#    NOT `>>>` doctests. Keep sphinx.ext.doctest OUT of scope. "Runnable" = correct against real API.
#
# 6. conf.py is frozen at "1.0.0" (lines 14-15). Bump to "1.3.0".
#    Prefer "New in v1.3"-style prose over hardcoding a release number elsewhere.
#    Do NOT touch ketu/__init__.py or pyproject.toml (Phase 26 owns the package version bump).
#
# 7. Standard example conventions: jd = 2451545.0 (J2000), lat=48.8566 lon=2.3522 (Paris),
#    structured-array field access result["field_name"].
#
# 8. examples.md has emoji literals inside python strings causing highlight warnings — when
#    editing examples.md, move emoji OUT of code-block strings (into prose) to avoid the warnings.

@docs/source/index.md
@docs/source/api.md
@docs/source/conf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken imports + bump version + wire toctree (entry pages)</name>
  <files>docs/source/conf.py, docs/source/index.md, docs/source/quickstart.md, docs/source/installation.md, docs/source/concepts.md</files>
  <action>
  conf.py: change `release = "1.0.0"` and `version = "1.0.0"` (lines 14-15) to `"1.3.0"`. Do NOT change anything else in conf.py (do NOT add sphinx.ext.doctest, do NOT add -W).

  index.md:
  - In the "Main Features → Supported Celestial Bodies" table, add a Chiron row (Symbol ⚷, Orb 4°, Average Speed ~0.018°/day; phrase the speed as approximate). Keep the existing 12 rows.
  - In the User Guide toctree, insert the five new page names so 25-02's pages are not orphaned. Final User Guide toctree order:
      installation
      quickstart
      concepts
      examples
      houses
      relational_charts
      predictive_charts
      arabic_parts
      chiron
      API <api>
      changelog
    (Leave the Developer Guide toctree unchanged.)
  - Fix the "Quick Example" code block: replace the `import ketu` / `ketu.utc_to_julian(dt)` / `ketu.print_positions(jday)` / `ketu.print_aspects(jday)` calls with correct imports:
      from ketu.ephemeris.time import utc_to_julian
      from ketu.display import print_positions, print_aspects
    and call them as module functions (utc_to_julian(dt), print_positions(jday), print_aspects(jday)).
  - Update the "Overview" bullet list to mention the new capabilities (house systems, full charts, synastry, composite charts, solar/lunar returns, Arabic Parts, Chiron) — one added bullet line each, keep prose in English.

  quickstart.md: replace EVERY `ketu.<fn>()` top-level call with the correct submodule import + call (see Context import table). In particular `ketu.long`, `ketu.lat`, `ketu.body_sign`, `ketu.is_retrograde`, `ketu.positions`, `ketu.utc_to_julian`, `ketu.local_to_utc`, `ketu.calculate_aspects`, `ketu.get_aspect`, `ketu.print_positions`, `ketu.print_aspects`. Add ONE new short snippet showing `from ketu.charts import compute_chart` building a Paris chart at jd=2451545.0 and reading `chart["asc"]` and `chart["body_lons"][0]` (Sun). Keep examples as ```python``` blocks (no `>>>`).

  installation.md: update any `1.0.0` version string to `1.3.0`; mention the `[dev]` extra (`pip install ketu[dev]`) for contributors.

  concepts.md: update the supported-body count to include Chiron (14 bodies). Add a short "House systems" concept subsection (one or two paragraphs naming the six systems: placidus, koch, porphyry, whole_sign, equal, regiomontanus) and a short "Sect (day vs night chart)" subsection (used by Arabic Parts). Keep the existing aspect/orb/retrogradation content. Do not add code that uses broken imports.
  </action>
  <verify>
  grep -n "1.3.0" docs/source/conf.py    # version + release both 1.3.0
  ! grep -nE "ketu\.(long|lat|body_sign|is_retrograde|positions|utc_to_julian|local_to_utc|calculate_aspects|get_aspect|print_positions|print_aspects)\(" docs/source/quickstart.md docs/source/index.md   # no broken top-level calls remain
  grep -nE "relational_charts|predictive_charts|arabic_parts|chiron|houses" docs/source/index.md   # all 5 new slots wired
  grep -ni "chiron" docs/source/index.md docs/source/concepts.md
  </verify>
  <done>conf.py version=release="1.3.0"; index.md toctree lists all 5 new pages + Chiron in body table + fixed Quick Example imports; quickstart.md has zero broken top-level calls and a compute_chart snippet; installation.md says 1.3.0; concepts.md covers 14 bodies + house systems + sect.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite api.md for the full v1.3 subpackage surface</name>
  <files>docs/source/api.md</files>
  <action>
  api.md is hand-written (no autodoc). Rewrite/extend it so every public API uses the CORRECT import paths from the Context table (no `ketu.long()`-style access).

  Restructure into sections, each with a short signature + a minimal correct ```python``` example:
  - "Top-level exports" — list what `ketu` actually re-exports: bodies, aspects, signs, HOUSES_DTYPE, HighLatitudeError, HOUSE_SYSTEMS, calculate_houses, house_of, __version__.
  - "Calculations" (ketu.calculations) — long, lat, body_sign, is_retrograde, positions, body_name. Example using `long(jd, 13)` to show Chiron access.
  - "Time" (ketu.ephemeris.time) — utc_to_julian, local_to_utc.
  - "Aspects" (ketu.aspects) — get_aspect, calculate_aspects, get_orb, and the configurable aspect-set presets CLASSICAL / TRADITIONAL / EXTENDED, AspectSetSpec, resolve_aspect_set. Note `calculate_aspects(jdate, l_bodies, aspects=None)` accepts an aspect-set spec (New in v1.1).
  - "Houses" (ketu.houses) — calculate_houses(jd, lat, lon, system="placidus", polar_fallback="raise"), house_of(planet_lon, cusps), HOUSES_DTYPE (fields jd, lat, lon, system, cusps[12], asc, mc, armc, vertex), SYSTEMS (the 6 names), HighLatitudeError, register. (New in v1.1/v1.2.)
  - "Charts" (ketu.charts) — compute_chart(jd, lat, lon, system="placidus", aspects=None, polar_fallback="raise"), is_day_chart(jd, lat, lon), CHART_DTYPE (fields jd, lat, lon, system, body_lons[14], body_lats[14], body_speeds[14], cusps[12], asc, mc, armc, vertex, aspect_matrix[14,14], aspect_orbs[14,14]). (New in v1.2.)
  - "Synastry" (ketu.synastry) — calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered"), SYNASTRY_DTYPE (body_a, body_b, lon_a, lon_b, aspect_type, orb, applying, orb_limit). (New in v1.2.)
  - "Composite" (ketu.composite) — calculate_composite(chart_a, chart_b, system="placidus") -> CHART_DTYPE, circular_midpoint(lon_a, lon_b). (New in v1.2.)
  - "Returns" (ketu.returns) — solar_return(natal_jd, natal_lat, natal_lon, target_year, return_lat=None, return_lon=None, system="placidus"), lunar_return(natal_jd, natal_lat, natal_lon, target_jd, return_lat=None, return_lon=None, system="placidus"). Note the asymmetry: solar takes target_year:int, lunar takes target_jd:float. (New in v1.2.)
  - "Arabic Parts" (ketu.parts) — PARTS registry (fortune, spirit, marriage), calculate_part(part_name, chart), calculate_all_parts(chart, parts=None), register, get_part, PartSpec; note Fortune/Spirit are sect-aware, Marriage is fixed. (New in v1.2.)
  - "Display / CLI" — print_positions, print_aspects (ketu.display); main (ketu.cli).
  - "Chiron (body_id=13)" — short note: 14th body, range 1950-2050, accessed via the standard calculation functions with body_id=13; CHART_DTYPE body axis is 14-wide (index 13 = Chiron). (New in v1.3.)

  For cross-references to the new dedicated pages, use MyST links: [Houses](houses.md), [Relational charts](relational_charts.md), [Predictive charts](predictive_charts.md), [Arabic Parts](arabic_parts.md), [Chiron](chiron.md).

  All examples ```python``` blocks, no emoji-in-strings.
  </action>
  <verify>
  ! grep -nE "ketu\.(long|lat|body_sign|is_retrograde|positions|utc_to_julian|local_to_utc|calculate_aspects|get_aspect|get_orb|print_positions|print_aspects)\(" docs/source/api.md   # no broken top-level calls
  grep -c "from ketu\." docs/source/api.md    # uses correct submodule imports (expect many)
  grep -nE "compute_chart|calculate_synastry|calculate_composite|solar_return|lunar_return|calculate_part|CHART_DTYPE" docs/source/api.md   # new APIs documented
  grep -ni "chiron" docs/source/api.md
  </verify>
  <done>api.md documents all 11 sections with correct submodule imports, the new DTYPEs/functions, the configurable aspect presets, and Chiron body_id=13; zero broken `ketu.<fn>()` examples.</done>
</task>

<task type="auto">
  <name>Task 3: Update narrative + developer pages and verify clean English build</name>
  <files>docs/source/examples.md, docs/source/architecture.md, docs/source/changelog.md, docs/source/migration.md, docs/source/performance.md, docs/source/contributing.md, docs/source/acknowledgments.md</files>
  <action>
  examples.md: fix the broken top-level `ketu.<fn>()` calls (same import-path corrections as quickstart). MOVE any emoji literals out of python-string code (e.g. phase labels) into prose or comments above the block, so the Python lexer stops warning. Add 1-2 new realistic examples for the new features (e.g. compute_chart + calculate_synastry, or a solar_return), using correct imports.

  architecture.md: replace the v0.3-era module tree with the v1.3 layout. Add the new subpackages: ketu/aspects/, ketu/houses/, ketu/charts/, ketu/synastry/, ketu/composite/, ketu/returns/, ketu/parts/, ketu/ephemeris/ (with chiron.py + data/chiron_coeffs.npz). One-line description each. Note pyswisseph is a test/validation-only dependency, not a runtime dependency (runtime is pure NumPy).

  changelog.md: add new top sections above v1.0.0, newest first: v1.3.0 (Chiron 14th body body_id=13, CHART_DTYPE 13→14 axis), v1.2.0 (compute_chart/CHART_DTYPE, synastry, midpoint composite, solar+lunar returns, Arabic Parts, additional house systems whole_sign/equal/regiomontanus), v1.1.0 (configurable aspect sets CLASSICAL/TRADITIONAL/EXTENDED, placidus/koch/porphyry house systems, HOUSES_DTYPE). Keep the existing v1.0.0 and earlier entries.

  migration.md: add upgrade sections v1.0→v1.1, v1.1→v1.2, v1.2→v1.3. The v1.2→v1.3 section MUST call out the breaking change: the CHART_DTYPE body axis expanded from 13 to 14 bodies (D-08); consumers indexing body arrays by fixed count must adapt; Chiron is index 13. ALSO fix the pre-existing broken cross-references: the four `[UPGRADING.md](../../UPGRADING.md)` / `[CHANGELOG.md](../../CHANGELOG.md)` links point outside the Sphinx source tree and warn in the fr build — replace them with in-docs links to [changelog](changelog.md) (and remove the dead UPGRADING reference or point it at changelog.md), so no myst.xref_missing warnings remain.

  performance.md: fix module paths in examples to the real ones (e.g. `from ketu.aspects import calculate_aspects`); ensure no broken top-level `ketu.<fn>()` calls remain.

  contributing.md: update the architecture tree to v1.3; state the coverage target is 100%; clarify pyswisseph is used only in tests/validation, not at runtime.

  acknowledgments.md: optional minor touch — may add a brief mention of v1.1/v1.2/v1.3 milestones. Keep changes minimal.

  After all edits across Tasks 1-3, build the English HTML and confirm no NEW warnings vs the 1-warning baseline:
    python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-01-en-build.log
    grep -c "WARNING" /tmp/25-01-en-build.log   # MUST be <= 1
  If the count exceeds 1, read the log, identify the offending page/line (likely a new broken xref, an orphaned page, or emoji-in-code), fix it, and rebuild until <= 1. Note: houses.md/relational_charts.md/predictive_charts.md/arabic_parts.md/chiron.md don't exist yet (25-02 creates them) — the toctree entries will produce "toctree contains reference to nonexisting document" warnings UNTIL 25-02 runs. That is EXPECTED in this plan's isolated build; record the count but treat ONLY non-toctree-missing warnings as failures here. (Final clean-build gate is enforced in 25-03 after all pages exist.)
  </action>
  <verify>
  python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-01-en-build.log >/dev/null
  grep "WARNING" /tmp/25-01-en-build.log | grep -v "nonexisting document\|toctree" | wc -l   # MUST be <= 1 (only display_version)
  ! grep -nE "ketu\.(long|lat|calculate_aspects|utc_to_julian|print_positions)\(" docs/source/examples.md docs/source/performance.md
  grep -nE "1\.1\.0|1\.2\.0|1\.3\.0" docs/source/changelog.md
  grep -ni "13.*14\|14.*bod\|chiron" docs/source/migration.md
  </verify>
  <done>examples.md/performance.md have no broken imports and no emoji-in-code warnings; architecture.md + contributing.md show the v1.3 module tree; changelog.md has v1.1/v1.2/v1.3; migration.md has the three upgrade sections incl. the 13→14 breaking-change note and no broken UPGRADING/CHANGELOG xrefs; English build shows ≤1 non-toctree warning.</done>
</task>

</tasks>

<verification>
- conf.py version and release are "1.3.0".
- No `docs/source/*.md` example uses a `ketu.<fn>()` top-level call that isn't a real `ketu` export.
- index.md toctree contains houses, relational_charts, predictive_charts, arabic_parts, chiron.
- api.md documents ketu.aspects presets, ketu.houses, ketu.charts, ketu.synastry, ketu.composite, ketu.returns, ketu.parts, and Chiron body_id=13 with correct import paths.
- changelog.md has v1.1.0/v1.2.0/v1.3.0; migration.md has the upgrade sections + 13→14 note.
- English build: `python3 -m sphinx.cmd.build -b html docs/source docs/build/html` shows no NEW warnings except expected "nonexisting document" toctree warnings for the not-yet-created 25-02 pages (≤1 real warning, the pre-existing display_version one).
</verification>

<success_criteria>
DOC-10 satisfied: all 12 frozen pages reflect the v1.1/v1.2/v1.3 surface with correct, runnable-against-real-API examples; conf.py at 1.3.0; index toctree owns the 5 new-page slots; English build clean modulo the pre-existing display_version warning and the expected (transient) missing-toctree-target warnings for pages 25-02 will add.
</success_criteria>

<output>
After completion, create `.planning/phases/25-documentation/25-01-SUMMARY.md`
</output>
