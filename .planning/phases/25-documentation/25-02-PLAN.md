---
phase: 25-documentation
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - docs/source/houses.md
  - docs/source/relational_charts.md
  - docs/source/predictive_charts.md
  - docs/source/arabic_parts.md
  - docs/source/chiron.md
autonomous: true

must_haves:
  truths:
    - "Five new Sphinx pages exist: houses.md, relational_charts.md, predictive_charts.md, arabic_parts.md, chiron.md"
    - "Each new page has at least one runnable (correct against real API) ```python``` example using the correct submodule imports"
    - "houses.md documents the six house systems + calculate_houses + house_of + HOUSES_DTYPE + HighLatitudeError/polar_fallback"
    - "relational_charts.md documents compute_chart/CHART_DTYPE + calculate_synastry/SYNASTRY_DTYPE + calculate_composite/circular_midpoint"
    - "predictive_charts.md documents solar_return + lunar_return incl. the target_year(int) vs target_jd(float) asymmetry and relocation"
    - "arabic_parts.md documents PARTS (fortune/spirit/marriage), calculate_part, calculate_all_parts, register, and sect-awareness"
    - "chiron.md documents Chiron as body_id=13, range 1950-2050, accessed via long(jd, 13) and chart body_lons[13]"
    - "All five pages are reachable via the index.md toctree (no orphan warning) — toctree slots owned by 25-01"
  artifacts:
    - path: docs/source/houses.md
      provides: "House systems page"
      contains: "calculate_houses"
      min_lines: 40
    - path: docs/source/relational_charts.md
      provides: "Synastry + composite + chart page"
      contains: "calculate_synastry"
      min_lines: 50
    - path: docs/source/predictive_charts.md
      provides: "Solar + lunar returns page"
      contains: "solar_return"
      min_lines: 40
    - path: docs/source/arabic_parts.md
      provides: "Arabic Parts / Hermetic Lots page"
      contains: "calculate_part"
      min_lines: 40
    - path: docs/source/chiron.md
      provides: "Chiron 14th-body page"
      contains: "body_id=13"
      min_lines: 30
  key_links:
    - from: "docs/source/{houses,relational_charts,predictive_charts,arabic_parts,chiron}.md"
      to: "ketu.{houses,charts,synastry,composite,returns,parts,calculations}"
      via: "correct submodule import paths in ```python``` examples"
      pattern: "from ketu\\."
    - from: "docs/source/index.md toctree"
      to: "the five new pages"
      via: "toctree slots created in 25-01"
      pattern: "relational_charts"
---

<objective>
Create the five new Sphinx pages for the major feature areas shipped since v1.0 — house systems, relational charts (synastry + composite), predictive charts (returns), Arabic Parts, and Chiron — each with runnable examples that use the correct submodule import paths. The toctree slots are created by Plan 25-01; this plan only authors the page files.

This plan satisfies DOC-11.

Purpose: These eight feature areas have ZERO documentation today. New users have no entry point for charts, synastry, returns, Arabic Parts, or Chiron.
Output: 5 new `docs/source/*.md` pages, wired into the index toctree (slots from 25-01), building clean in English.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-documentation/25-RESEARCH.md

# DEPENDENCY: 25-01 must run first — it adds the five page names to the index.md toctree
#   (installation, quickstart, concepts, examples, houses, relational_charts,
#    predictive_charts, arabic_parts, chiron, API <api>, changelog). This plan creates
#   the page files those slots point to. Do NOT re-edit index.md here (25-01 owns it).
#
# CONSTRAINTS (from RESEARCH — do not violate):
# - Build ONLY via: python3 -m sphinx.cmd.build -b html docs/source docs/build/html
#   (venv sphinx-build script has a broken shebang — never call it directly).
# - Pages are MyST Markdown (.md). Examples are display-only ```python``` blocks (NOT `>>>`,
#   NOT pytest-collected). Keep sphinx.ext.doctest OUT of scope.
# - "Runnable" = correct against the real API with correct submodule imports. NO `ketu.<fn>()`
#   top-level access for functions not in ketu.__all__.
# - NO emoji literals inside python-string code (causes lexer highlight warnings).
# - Standard example values: jd = 2451545.0 (J2000), lat=48.8566, lon=2.3522 (Paris).
#   Show structured-array field access result["field_name"].
# - Cross-link pages with MyST links: [API](api.md), [Concepts](concepts.md), etc.
#
# EXACT PUBLIC API (RESEARCH §"Real Public API Surface") — use verbatim:
#   ketu.houses: calculate_houses(jd, lat, lon, system="placidus", polar_fallback="raise"),
#     house_of(planet_lon, cusps), HOUSES_DTYPE {jd, lat, lon, system, cusps[12], asc, mc, armc, vertex},
#     SYSTEMS {porphyry, placidus, koch, whole_sign, equal, regiomontanus}, HighLatitudeError, register
#   ketu.charts: compute_chart(jd, lat, lon, system="placidus", aspects=None, polar_fallback="raise"),
#     is_day_chart(jd, lat, lon), CHART_DTYPE {jd, lat, lon, system, body_lons[14], body_lats[14],
#     body_speeds[14], cusps[12], asc, mc, armc, vertex, aspect_matrix[14,14], aspect_orbs[14,14]}
#   ketu.synastry: calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered"),
#     SYNASTRY_DTYPE {body_a, body_b, lon_a, lon_b, aspect_type, orb, applying, orb_limit}
#   ketu.composite: calculate_composite(chart_a, chart_b, system="placidus") -> CHART_DTYPE,
#     circular_midpoint(lon_a, lon_b)
#   ketu.returns: solar_return(natal_jd, natal_lat, natal_lon, target_year, return_lat=None,
#     return_lon=None, system="placidus"), lunar_return(natal_jd, natal_lat, natal_lon, target_jd,
#     return_lat=None, return_lon=None, system="placidus")
#   ketu.parts: PARTS {fortune, spirit, marriage}, calculate_part(part_name, chart),
#     calculate_all_parts(chart, parts=None), register, get_part, PartSpec.
#     Formula signature: (asc_lon, sun_lon, moon_lon, venus_lon) -> float. Fortune/Spirit sect-aware.
#   Chiron: body_id=13; from ketu.calculations import long, lat; long(2451545.0, 13) ≈ 251.6125.
#     Range 1950-2050, max error 0.005695°. chart["body_lons"][13] is Chiron.

@docs/source/concepts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author houses.md and chiron.md</name>
  <files>docs/source/houses.md, docs/source/chiron.md</files>
  <action>
  Create houses.md (House Systems, New in v1.1/v1.2):
  - Title + intro paragraph on what house systems are and why they differ.
  - Section listing the six SYSTEMS: porphyry, placidus, koch, whole_sign, equal, regiomontanus (one line each).
  - calculate_houses signature + a runnable Paris example:
      from ketu.houses import calculate_houses, house_of, SYSTEMS, HOUSES_DTYPE, HighLatitudeError
      jd = 2451545.0
      h = calculate_houses(jd, 48.8566, 2.3522, system="placidus")
      h["cusps"]; h["asc"]; h["mc"]
  - house_of example: place a planet longitude into a house using the cusps array (vectorised over multiple longitudes).
  - HOUSES_DTYPE field list (jd, lat, lon, system, cusps[12], asc, mc, armc, vertex).
  - HighLatitudeError + polar_fallback: explain `polar_fallback="raise"` default vs alternative fallback behaviour at high latitudes.
  - "Next Steps" links: [Relational charts](relational_charts.md), [API](api.md).

  Create chiron.md (Chiron — the 14th Body, New in v1.3):
  - Title + intro: Chiron added as the 14th body, body_id=13.
  - State range 1950-2050 (embedded Chebyshev coefficients), max error 0.005695° (sub-arcminute), pure-NumPy evaluation (no swisseph at runtime).
  - Access via standard calculation functions:
      from ketu.calculations import long, lat
      jd = 2451545.0   # J2000
      chiron_lon = long(jd, 13)   # ~251.6125
      chiron_lat = lat(jd, 13)
  - Chiron in a chart: compute_chart returns body_lons[14]; index 13 is Chiron:
      from ketu.charts import compute_chart
      chart = compute_chart(jd, 48.8566, 2.3522)
      chart["body_lons"][13]   # Chiron longitude
  - Breaking-change note (link to [migration](migration.md)): CHART_DTYPE body axis expanded from 13 to 14 (D-08); index-by-fixed-count consumers must adapt.
  - "Next Steps" links: [API](api.md), [Migration](migration.md).

  All examples ```python``` blocks; no emoji-in-strings.
  </action>
  <verify>
  test -f docs/source/houses.md && test -f docs/source/chiron.md
  grep -nE "calculate_houses|house_of|HOUSES_DTYPE|polar_fallback|placidus|regiomontanus" docs/source/houses.md
  grep -nE "body_id=13|long\(.*13\)|body_lons\"\]\[13\]|1950-2050|2050" docs/source/chiron.md
  ! grep -nE "ketu\.(long|lat|calculate_houses|house_of|compute_chart)\(" docs/source/houses.md docs/source/chiron.md
  </verify>
  <done>houses.md documents the 6 systems + calculate_houses/house_of/HOUSES_DTYPE/HighLatitudeError with a runnable Paris example; chiron.md documents body_id=13, range 1950-2050, long(jd,13) and chart["body_lons"][13], plus the 13→14 breaking-change note. No broken top-level imports.</done>
</task>

<task type="auto">
  <name>Task 2: Author relational_charts.md, predictive_charts.md, arabic_parts.md</name>
  <files>docs/source/relational_charts.md, docs/source/predictive_charts.md, docs/source/arabic_parts.md</files>
  <action>
  Create relational_charts.md (Relational Charts: Synastry + Composite, New in v1.2):
  - Intro: relating two charts.
  - compute_chart section: signature + CHART_DTYPE field list (jd, lat, lon, system, body_lons[14], body_lats[14], body_speeds[14], cusps[12], asc, mc, armc, vertex, aspect_matrix[14,14], aspect_orbs[14,14]); runnable example building two charts (two different jds/locations).
  - is_day_chart(jd, lat, lon) one-line note (sect helper).
  - Synastry section: calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered"); SYNASTRY_DTYPE field list; runnable example iterating results (body_a, body_b, aspect_type, orb, applying).
  - Composite section: calculate_composite(chart_a, chart_b, system="placidus") -> CHART_DTYPE; circular_midpoint(lon_a, lon_b) utility with a short example (e.g. midpoint of 350° and 10° = 0°, illustrating the wraparound).
  - "Next Steps" links: [Predictive charts](predictive_charts.md), [Arabic Parts](arabic_parts.md), [API](api.md).

  Create predictive_charts.md (Predictive Charts: Solar + Lunar Returns, New in v1.2):
  - Intro: return charts.
  - solar_return(natal_jd, natal_lat, natal_lon, target_year, return_lat=None, return_lon=None, system="placidus") -> CHART_DTYPE; runnable example for target_year=2026.
  - lunar_return(natal_jd, natal_lat, natal_lon, target_jd, return_lat=None, return_lon=None, system="placidus") -> CHART_DTYPE; runnable example searching from a given target_jd.
  - Emphasise the API asymmetry: solar_return takes target_year (int); lunar_return takes target_jd (float).
  - Relocation: return_lat/return_lon=None → natal location; non-None → relocated return chart.
  - UTC-only contract note.
  - "Next Steps" links: [Relational charts](relational_charts.md), [API](api.md).

  Create arabic_parts.md (Arabic Parts / Hermetic Lots, New in v1.2):
  - Intro: lots derived from chart points; sect-aware.
  - PARTS registry: fortune, spirit, marriage (built-in). Note Fortune & Spirit invert their day/night formula by sect; Marriage is fixed.
  - calculate_part(part_name, chart) -> float (longitude); calculate_all_parts(chart, parts=None) -> dict[str,float]. Runnable example: build a chart with compute_chart, then calculate Fortune/Spirit/Marriage and calculate_all_parts.
  - Formula signature note: (asc_lon, sun_lon, moon_lon, venus_lon) -> float.
  - register(name, day_formula, night_formula, description) example adding a custom part.
  - "Next Steps" links: [Relational charts](relational_charts.md), [API](api.md).

  All examples ```python``` blocks with correct submodule imports; no emoji-in-strings.
  </action>
  <verify>
  test -f docs/source/relational_charts.md && test -f docs/source/predictive_charts.md && test -f docs/source/arabic_parts.md
  grep -nE "calculate_synastry|SYNASTRY_DTYPE|calculate_composite|circular_midpoint|CHART_DTYPE" docs/source/relational_charts.md
  grep -nE "solar_return|lunar_return|target_year|target_jd" docs/source/predictive_charts.md
  grep -nE "calculate_part|calculate_all_parts|fortune|spirit|marriage|register" docs/source/arabic_parts.md
  ! grep -nE "ketu\.(compute_chart|calculate_synastry|solar_return|calculate_part)\(" docs/source/relational_charts.md docs/source/predictive_charts.md docs/source/arabic_parts.md
  </verify>
  <done>relational_charts.md covers compute_chart/CHART_DTYPE + synastry + composite; predictive_charts.md covers both returns with the target_year/target_jd asymmetry + relocation; arabic_parts.md covers PARTS + calculate_part/all + register + sect-awareness. All use correct imports.</done>
</task>

<task type="auto">
  <name>Task 3: Build English HTML and confirm the five pages are no longer orphaned</name>
  <files>docs/source/houses.md, docs/source/relational_charts.md, docs/source/predictive_charts.md, docs/source/arabic_parts.md, docs/source/chiron.md</files>
  <action>
  With 25-01's toctree slots now backed by real files, build the English HTML and confirm zero NEW warnings vs the 1-warning baseline:
    python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-02-en-build.log
    grep -c "WARNING" /tmp/25-02-en-build.log
  Expected: ≤ 1 (only the pre-existing display_version theme warning). The "nonexisting document" toctree warnings from 25-01's isolated build MUST now be gone because the five pages exist.
  If any NEW warning appears (orphan, broken xref between the new pages, bad MyST link target, emoji-in-code), read the log, fix the offending page, and rebuild until the count is ≤ 1.
  Confirm each new page produced an HTML output file:
    ls docs/build/html/houses.html docs/build/html/relational_charts.html docs/build/html/predictive_charts.html docs/build/html/arabic_parts.html docs/build/html/chiron.html
  </action>
  <verify>
  python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-02-en-build.log >/dev/null
  grep -c "WARNING" /tmp/25-02-en-build.log   # MUST be <= 1
  ! grep -i "nonexisting document\|isn't included in any toctree" /tmp/25-02-en-build.log   # no orphan/missing-target warnings
  ls docs/build/html/houses.html docs/build/html/relational_charts.html docs/build/html/predictive_charts.html docs/build/html/arabic_parts.html docs/build/html/chiron.html
  </verify>
  <done>English build emits ≤1 warning (only display_version), no orphan/missing-toctree-target warnings, and all five new pages have rendered HTML output.</done>
</task>

</tasks>

<verification>
- Five new pages exist under docs/source/ and each has correct-import ```python``` examples.
- houses.md / relational_charts.md / predictive_charts.md / arabic_parts.md / chiron.md cover the API surface listed in must_haves.
- English build (`python3 -m sphinx.cmd.build -b html docs/source docs/build/html`) emits ≤1 warning (display_version only); no orphan or nonexisting-document warnings.
- HTML output files exist for all five pages.
</verification>

<success_criteria>
DOC-11 satisfied: new Sphinx pages exist for relational charts, predictive charts/returns, Arabic Parts, and Chiron (plus a houses page), each with runnable examples; all wired into the index toctree (slots from 25-01) and building clean in English.
</success_criteria>

<output>
After completion, create `.planning/phases/25-documentation/25-02-SUMMARY.md`
</output>
