---
phase: 25-documentation
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - docs/locale/fr/LC_MESSAGES/houses.po
  - docs/locale/fr/LC_MESSAGES/relational_charts.po
  - docs/locale/fr/LC_MESSAGES/predictive_charts.po
  - docs/locale/fr/LC_MESSAGES/arabic_parts.po
  - docs/locale/fr/LC_MESSAGES/chiron.po
  - docs/locale/fr/LC_MESSAGES/index.po
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/quickstart.po
  - docs/locale/fr/LC_MESSAGES/concepts.po
  - docs/locale/fr/LC_MESSAGES/examples.po
  - docs/locale/fr/LC_MESSAGES/architecture.po
  - docs/locale/fr/LC_MESSAGES/changelog.po
  - docs/locale/fr/LC_MESSAGES/migration.po
  - docs/locale/fr/LC_MESSAGES/installation.po
  - docs/locale/fr/LC_MESSAGES/performance.po
  - docs/locale/fr/LC_MESSAGES/contributing.po
  - docs/locale/fr/LC_MESSAGES/acknowledgments.po
autonomous: true

must_haves:
  truths:
    - "The gettext pipeline was run via python3 -m invocations (NOT the broken venv sphinx-build/sphinx-intl scripts)"
    - "New .po files exist for the five new pages (houses, relational_charts, predictive_charts, arabic_parts, chiron)"
    - "Edited existing pages had their .po entries refreshed (fuzzy/obsolete entries from changed paragraphs are resolved, not left dangling)"
    - "Compiled .mo files exist and are non-empty for all pages"
    - "English HTML build is clean (≤1 warning) and French HTML build is clean (no NEW warnings vs the documented baseline)"
  artifacts:
    - path: docs/locale/fr/LC_MESSAGES/chiron.po
      provides: "French translation catalog for the new Chiron page (English-fallback acceptable)"
      contains: "msgid"
    - path: docs/locale/fr/LC_MESSAGES/relational_charts.po
      provides: "French translation catalog for relational charts page"
      contains: "msgid"
  key_links:
    - from: "docs/source/*.md (final English source)"
      to: "docs/locale/fr/LC_MESSAGES/*.po"
      via: "python3 -m sphinx.cmd.build -b gettext + sphinx-intl update"
      pattern: "msgid"
    - from: "docs/locale/fr/LC_MESSAGES/*.po"
      to: "docs/locale/fr/LC_MESSAGES/*.mo + French HTML"
      via: "sphinx-intl build + python3 -m sphinx.cmd.build -D language=fr"
      pattern: "\\.mo"
---

<objective>
Regenerate the French translation through the existing gettext infrastructure — extract msgids from the FINAL English source (after 25-01 + 25-02), update/create the `.po` catalogs via sphinx-intl, compile `.mo` files, and verify both the English and French Sphinx builds are clean (no new warnings vs the documented baseline). No manual duplication; no use of the obsolete `migrate_translations.py`.

This plan satisfies DOC-12.

DECISION (recorded per RESEARCH Open Question 3): The Phase 25 scope is **regeneration of the pipeline**, not full hand-translation. Concretely:
  - NEW pages' `.po` files are created by the pipeline and LEFT with English-fallback (empty msgstr is acceptable; the fr build renders English for them). Substantive French translation of the new pages is deferred (volunteer / future phase).
  - EXISTING pages: any `#, fuzzy` entries introduced by 25-01's paragraph edits MUST be resolved — either re-translated to French if a clear prior French translation exists, or have the fuzzy flag removed (accepting English fallback) so no stale/incorrect French text ships. Do NOT leave dangling fuzzy entries.
This decision is explicit and testable: the gate is "pipeline regenerated + both builds clean", NOT "100% French coverage".

Purpose: The French docs must reflect the regenerated v1.3 English source via the gettext pipeline, and the site must build clean in both languages.
Output: Updated/new `.po` catalogs for all pages, compiled `.mo` files, clean en + fr builds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-documentation/25-RESEARCH.md

# DEPENDENCY: MUST run AFTER 25-01 and 25-02 — gettext extracts msgids from the FINAL
#   English source. Running earlier would capture stale/incomplete content.
#
# CRITICAL — broken venv shebangs. NEVER call sphinx-build or sphinx-intl scripts directly.
#   Use these EXACT invocations (RESEARCH §Command Sequence), all from the docs/ directory
#   (or repo root with the docs/source path forms shown):
#
#   Extract POT (run after English source is final):
#     python3 -m sphinx.cmd.build -b gettext docs/source docs/build/gettext
#
#   Update fr PO catalogs (creates new .po for new .pot; refreshes existing):
#     python3 -c "from sphinx_intl.commands import main; import sys; sys.argv = ['sphinx-intl', 'update', '-p', 'docs/build/gettext', '-l', 'fr', '-d', 'docs/locale']; main()"
#
#   Compile MO:
#     python3 -c "from sphinx_intl.commands import main; import sys; sys.argv = ['sphinx-intl', 'build', '-d', 'docs/locale']; main()"
#
#   Build English HTML:
#     python3 -m sphinx.cmd.build -b html docs/source docs/build/html
#
#   Build French HTML:
#     python3 -m sphinx.cmd.build -b html -D language=fr docs/source docs/build/html-fr
#
# DO NOT use docs/migrate_translations.py — it is obsolete (needs a docs/fr/ dir that no
#   longer exists). The real pipeline is gettext -> sphinx-intl update -> edit .po ->
#   sphinx-intl build. (Noted explicitly so the plan-checker does not flag the migrate tool.)
#
# BASELINE WARNING COUNTS (RESEARCH §"What Builds Clean Means"):
#   English build: 1 pre-existing warning (display_version theme option).
#   French build: 27 pre-existing warnings, of which the structural ones are ~5
#     (display_version + broken UPGRADING/CHANGELOG xrefs in migration.md) — note 25-01 already
#     fixed the migration.md xrefs and the examples.md emoji warnings, so the fr count should DROP.
#   GATE: NO NEW warnings. Target: en ≤ 1, fr ≤ 5 (and ideally lower now that 25-01 fixed xrefs/emoji).
#   No -W flag is added.
#
# French build does NOT fail on untranslated strings (empty msgstr -> English fallback). Verified.

@docs/Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract POT + update fr .po catalogs from final English source</name>
  <files>docs/locale/fr/LC_MESSAGES/houses.po, docs/locale/fr/LC_MESSAGES/relational_charts.po, docs/locale/fr/LC_MESSAGES/predictive_charts.po, docs/locale/fr/LC_MESSAGES/arabic_parts.po, docs/locale/fr/LC_MESSAGES/chiron.po, docs/locale/fr/LC_MESSAGES/index.po, docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/quickstart.po, docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/changelog.po, docs/locale/fr/LC_MESSAGES/migration.po</files>
  <action>
  Extract the gettext POT files from the FINAL English source (25-01 + 25-02 complete), then update the French catalogs. Run from repo root using the exact invocations:

    python3 -m sphinx.cmd.build -b gettext docs/source docs/build/gettext
    python3 -c "from sphinx_intl.commands import main; import sys; sys.argv = ['sphinx-intl', 'update', '-p', 'docs/build/gettext', '-l', 'fr', '-d', 'docs/locale']; main()"

  This:
  - Creates NEW .po files for the five new pages: houses.po, relational_charts.po, predictive_charts.po, arabic_parts.po, chiron.po (all msgids untranslated — English-fallback, which is the recorded DECISION for new pages).
  - Refreshes EXISTING .po files for pages edited by 25-01 (index, api, quickstart, concepts, examples, architecture, changelog, migration, installation, performance, contributing, acknowledgments), marking changed paragraphs as `#, fuzzy` and removed ones as obsolete (`#~`).

  Confirm the five new .po files now exist:
    ls docs/locale/fr/LC_MESSAGES/houses.po docs/locale/fr/LC_MESSAGES/relational_charts.po docs/locale/fr/LC_MESSAGES/predictive_charts.po docs/locale/fr/LC_MESSAGES/arabic_parts.po docs/locale/fr/LC_MESSAGES/chiron.po

  Then inventory fuzzy entries introduced in existing catalogs:
    grep -rl "#, fuzzy" docs/locale/fr/LC_MESSAGES/*.po
  Leave the resolution of those fuzzy entries to Task 2. (Do NOT compile .mo yet.)
  </action>
  <verify>
  test -f docs/locale/fr/LC_MESSAGES/houses.po && test -f docs/locale/fr/LC_MESSAGES/relational_charts.po && test -f docs/locale/fr/LC_MESSAGES/predictive_charts.po && test -f docs/locale/fr/LC_MESSAGES/arabic_parts.po && test -f docs/locale/fr/LC_MESSAGES/chiron.po
  grep -c "msgid" docs/locale/fr/LC_MESSAGES/chiron.po   # > 0
  test -d docs/build/gettext   # POT extracted
  </verify>
  <done>POT extracted from final source; five new .po catalogs created (English-fallback); existing catalogs refreshed with fuzzy/obsolete markers; fuzzy inventory captured for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve fuzzy entries, compile .mo, verify clean en + fr builds</name>
  <files>docs/locale/fr/LC_MESSAGES/index.po, docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/quickstart.po, docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/changelog.po, docs/locale/fr/LC_MESSAGES/migration.po, docs/locale/fr/LC_MESSAGES/examples.po, docs/locale/fr/LC_MESSAGES/architecture.po, docs/locale/fr/LC_MESSAGES/installation.po, docs/locale/fr/LC_MESSAGES/performance.po, docs/locale/fr/LC_MESSAGES/contributing.po, docs/locale/fr/LC_MESSAGES/acknowledgments.po</files>
  <action>
  Resolve every `#, fuzzy` entry in the existing catalogs (per the recorded DECISION: NO dangling fuzzy entries ship). For each fuzzy entry:
    - If the new English msgid is essentially the same concept as the preserved old French msgstr, keep/adjust the French translation and REMOVE the `#, fuzzy` flag.
    - If the paragraph changed materially and no good French exists, set msgstr to "" (English fallback) and REMOVE the `#, fuzzy` flag (so stale/incorrect French does not ship).
  New pages' catalogs are intentionally left untranslated (English-fallback) — do NOT spend effort hand-translating them.

  Identify fuzzy entries with:
    grep -rln "#, fuzzy" docs/locale/fr/LC_MESSAGES/*.po
  and edit those files to clear the flags. After editing, confirm none remain:
    grep -rc "#, fuzzy" docs/locale/fr/LC_MESSAGES/*.po | grep -v ":0$" || echo "no fuzzy remaining"

  Compile the MO catalogs:
    python3 -c "from sphinx_intl.commands import main; import sys; sys.argv = ['sphinx-intl', 'build', '-d', 'docs/locale']; main()"

  Build both languages and check warning counts against baseline:
    python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-03-en.log
    python3 -m sphinx.cmd.build -b html -D language=fr docs/source docs/build/html-fr 2>&1 | tee /tmp/25-03-fr.log
    grep -c "WARNING" /tmp/25-03-en.log   # MUST be <= 1
    grep -c "WARNING" /tmp/25-03-fr.log   # MUST be <= 5 (and should be lower now that 25-01 fixed xrefs/emoji)

  If either count exceeds its target, read the log, identify the offending page/line, fix it (in the .po, or escalate a source-page fix that 25-01/25-02 missed), and rebuild until within target. Confirm .mo files are non-empty:
    ls -la docs/locale/fr/LC_MESSAGES/*.mo
  </action>
  <verify>
  grep -rc "#, fuzzy" docs/locale/fr/LC_MESSAGES/*.po | grep -v ":0$" && exit 1 || echo "no fuzzy"   # zero fuzzy entries remain
  python3 -m sphinx.cmd.build -b html docs/source docs/build/html 2>&1 | tee /tmp/25-03-en.log >/dev/null
  test "$(grep -c WARNING /tmp/25-03-en.log)" -le 1
  python3 -m sphinx.cmd.build -b html -D language=fr docs/source docs/build/html-fr 2>&1 | tee /tmp/25-03-fr.log >/dev/null
  test "$(grep -c WARNING /tmp/25-03-fr.log)" -le 5
  ls docs/locale/fr/LC_MESSAGES/chiron.mo docs/locale/fr/LC_MESSAGES/relational_charts.mo
  </verify>
  <done>Zero dangling fuzzy entries; .mo files compiled and non-empty for all pages incl. the five new ones; English build ≤1 warning; French build ≤5 warnings (no new warnings vs baseline).</done>
</task>

</tasks>

<verification>
- Pipeline run via python3 -m invocations only (no direct sphinx-build/sphinx-intl scripts; no migrate_translations.py).
- Five new .po files exist (houses, relational_charts, predictive_charts, arabic_parts, chiron).
- No `#, fuzzy` entries remain in any docs/locale/fr/LC_MESSAGES/*.po.
- .mo files compiled and non-empty.
- English build: `python3 -m sphinx.cmd.build -b html docs/source docs/build/html` → ≤1 WARNING.
- French build: `python3 -m sphinx.cmd.build -b html -D language=fr docs/source docs/build/html-fr` → ≤5 WARNINGs (no new vs baseline).
</verification>

<success_criteria>
DOC-12 satisfied: the French translation is regenerated through the existing gettext infra (no manual duplication, no migrate_translations.py); new pages get pipeline-created .po catalogs (English-fallback per the recorded decision); existing-page fuzzy entries are resolved; both English and French builds complete clean (no new warnings vs baseline).
</success_criteria>

<output>
After completion, create `.planning/phases/25-documentation/25-03-SUMMARY.md`
</output>
