---
phase: 30-chiron-range-1900-2100
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
must_haves:
  truths:
    - "Pre-flight build check passes: pyswisseph imports and seas_18.se1 is findable (covers 1800-2400)"
    - "max|Δλ| vs Swiss Ephemeris measured over the FULL 1900-2100 range BEFORE any .npz is committed"
    - "The 1900-1910 post-perihelion edge is densely sampled (not averaged) and its per-segment worst case is reported"
    - "A degree decision is recorded: degree=10 kept (if it passes <0.01°) OR raised to 12 (if it fails); if degree=12 ALSO fails at 1900.0 the workflow STOPS and asks the user"
    - "The verdict (measured max|Δλ|, final seg/degree params, decision) is written to PROJECT.md Key Decisions + STATE.md decision log"
    - "The ephemeral spike script is thrown away — nothing committed under tools/ ketu/ tests/ pyproject from the spike"
  artifacts:
    - path: .planning/PROJECT.md
      provides: "Key Decisions table row [Phase 30-01] with measured max|Δλ| and final params"
      contains: "Phase 30-01"
    - path: .planning/STATE.md
      provides: "Decision log entry [Phase 30-01] with spike verdict and new .npz range metadata"
      contains: "Phase 30-01"
  key_links:
    - from: "ephemeral spike script (/tmp)"
      to: "tools/gen_chiron_coeffs.py generate_all_coefficients + validate_coefficients"
      via: "in-memory override of g._DEGREE/g._N_FIT, NO edit to committed file"
      pattern: "g\\._DEGREE"
    - from: "spike measurement"
      to: ".planning decision log"
      via: "recorded degree_final + max|Δλ| consumed by Plan 30-02"
      pattern: "Phase 30-01"
---

<objective>
BLOCKING SPIKE (hard gate). Measure Chiron Chebyshev accuracy over the widened 1900–2100 range against Swiss Ephemeris — with dense sampling of the high-curvature 1900–1910 post-perihelion edge — and decide the final Chebyshev degree, BEFORE any `.npz` is regenerated or committed. The spike is ÉPHÉMÈRE: the measurement script is created, run, its verdict recorded in the decision log, then thrown away. Nothing is committed under `tools/`, `ketu/`, `tests/`, or `pyproject` from this plan.

Purpose: The perihelion (~1895–1896) sits just below the new 1900 lower bound, so 1900–1905 is the aftermath of maximum orbital curvature — exactly where the locked seg=32/degree=10 params (validated at 0.000861° for 1950–2050 in Phase 23) are most at risk of breaching the < 0.01° gate. This plan resolves that open question empirically and GATES all of Plan 30-02. It also encodes the explicit degree-decision branch and the STOP-and-ask-user escalation if even degree=12 fails at 1900.0 (range-narrowing = scope change, needs user approval).

Output: A recorded decision-log entry (PROJECT.md + STATE.md) carrying `max|Δλ|`, final `seg`/`degree` params, and the verdict (degree 10 kept or raised to 12). No source/test/data files modified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-chiron-range-1900-2100/30-CONTEXT.md
@.planning/phases/30-chiron-range-1900-2100/30-RESEARCH.md
@tools/gen_chiron_coeffs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the ephemeral spike — pre-flight build check + dense 1900–2100 measurement at degree=10</name>
  <files>/tmp/chiron_spike_30.py (ephemeral — NEVER committed)</files>
  <action>
Create `/tmp/chiron_spike_30.py` (NOT under the repo tree — keep it ephemeral, throwaway, NEVER staged or committed). Activate the venv first: `cd /home/loc/workspace/ketu && source venv/bin/activate`.

The script MUST execute these steps in strict order and print a clear PASS/FAIL verdict:

1. **PRE-FLIGHT BUILD CHECK (FIRST step — fail early with a clear message):**
   - `import swisseph as swe` inside a try/except; if ImportError, print `PRE-FLIGHT FAILED: pyswisseph not installed (pip install -e '.[test]')` and `sys.exit(1)`.
   - `EPHE_PATH = os.environ.get("SE_EPHE_PATH", "/home/loc/workspace/rahu/kerykeion/kerykeion/sweph")`; `swe.set_ephe_path(EPHE_PATH)`.
   - Probe Chiron at J2000.0: `xx, retflag, errmsg = swe.calc_ut(2451545.0, swe.CHIRON, swe.FLG_SWIEPH | swe.FLG_SPEED)`. If `retflag < 0`, print `PRE-FLIGHT FAILED: seas_18.se1 not findable at {EPHE_PATH} (need coverage 1800-2400)` and `sys.exit(1)`. Confirm `swe.CHIRON == 15` and print `retflag` (expect 260 = Moshier fallback, acceptable — same as Phase 23/24).
   - Also probe the two new edges JD_1900=`swe.julday(1900,1,1,0.0)` and JD_2100=`swe.julday(2100,1,1,0.0)` to confirm the oracle answers (retflag >= 0) at both bounds.

2. **GENERATE at degree=10 over 1900–2100 via in-memory override (NO edit to the committed generator):**
   - `sys.path.insert(0, "/home/loc/workspace/ketu/tools")` then `import gen_chiron_coeffs as g`.
   - Set `g._DEGREE = 10`; `g._N_FIT = g._DEGREE + 8`; keep `g._SEG_LEN = 32.0` (uniform — do NOT introduce per-region params).
   - `jd0 = swe.julday(1900,1,1,0.0)` (expect 2415020.5); `jd1 = swe.julday(2100,1,1,0.0)` (expect 2488069.5).
   - `lon_c, lat_c, dist_c, seg_s = g.generate_all_coefficients(jd0, jd1)` — expect n_segs=2283 (ceil(73049/32)).

3. **DENSE EDGE REPORT on 1900–1910 (segments 0–10) — per-segment worst case, NO averaging:**
   - For `si in range(min(11, len(seg_s)))`: rebuild `jd_e = min(seg_s[si] + g._SEG_LEN, jd1)`, `actual_len = jd_e - seg_s[si]`; sample 500 points across `t in [-1,1]`, map to JD, evaluate `numpy.polynomial.chebyshev.chebval(t, lon_c[si]) % 360.0` vs oracle `swe.calc_ut(jd, swe.CHIRON, swe.FLG_SWIEPH|swe.FLG_SPEED)[0][0] % 360.0`, wrap-aware error `min(|Δ|, 360-|Δ|)`. Print `seg {si} JD={seg_s[si]:.1f}: max|Δλ|={...:.6f}°`. This surfaces any local gradient spike near the perihelion aftermath that a global average would mask.

4. **FULL VALIDATION GATE:** `max_lon_err, max_lat_err, max_dist_err, worst_jd = g.validate_coefficients(lon_c, lat_c, dist_c, seg_s, jd0, jd1)` (built-in pure-NumPy vs-oracle, 200 pts/seg). Print `FINAL degree=10: max|Δλ|={max_lon_err:.6f}° worst_jd={worst_jd:.2f}  GATE={'PASS' if max_lon_err < 0.01 else 'FAIL'}`.

Run it: `python /tmp/chiron_spike_30.py 2>&1 | tee /tmp/chiron_spike_30.out`.

DO NOT regenerate or overwrite `ketu/data/chiron_coeffs.npz` in this task — measurement only, no `.npz` written (do not call `g.write_npz`).
  </action>
  <verify>`python /tmp/chiron_spike_30.py` prints a PRE-FLIGHT block (retflag at J2000/1900/2100), a DENSE EDGE REPORT for segments 0–10, and a `FINAL degree=10: max|Δλ|=...° GATE=PASS|FAIL` line. The script lives only under /tmp.</verify>
  <done>Pre-flight build check passed; max|Δλ| at degree=10 over 1900–2100 is measured and recorded in /tmp/chiron_spike_30.out, including the per-segment worst case for the 1900–1910 edge; a clear PASS or FAIL verdict for the < 0.01° gate is printed.</done>
</task>

<task type="auto">
  <name>Task 2: Degree decision branch — keep 10, or raise to 12 and re-measure</name>
  <files>/tmp/chiron_spike_30.py (ephemeral — NEVER committed)</files>
  <action>
Interpret the degree=10 result from Task 1 and apply the LOCKED fallback strategy (uniform params only — one degree, one seg_len; NO adaptive per-region params; seg_len reduction is a last-resort uniform lever only if needed):

- **IF degree=10 GATE=PASS (max|Δλ| < 0.01° everywhere, edge included):** Decision = `degree_final=10, seg_len=32.0`. Record `max|Δλ|` and `worst_jd`. Proceed to Task 3 (no re-measurement needed).

- **IF degree=10 GATE=FAIL on any segment (especially the 1900–1910 edge):** Apply Lever #1 — raise degree to 12. In the spike script set `g._DEGREE = 12; g._N_FIT = g._DEGREE + 8` (keep `g._SEG_LEN = 32.0`), re-run `generate_all_coefficients` + the dense edge report + `validate_coefficients`. Print `FINAL degree=12: max|Δλ|=...° GATE=...`.
  - **IF degree=12 GATE=PASS:** Decision = `degree_final=12, seg_len=32.0`. Record `max|Δλ|` and `worst_jd`. Proceed to Task 3.
  - **IF degree=12 GATE=FAIL specifically at/near 1900.0 (i.e. even degree=12 with uniform params cannot hold < 0.01° at the lower bound):** Do NOT proceed. Do NOT narrow the range. This is a scope/requirement change (raising the lower bound to ~1905) and requires explicit user approval per the LOCKED decision — escalate via the checkpoint in Task 3. (Optionally, before escalating, try one uniform `seg_len` reduction at degree=12 — Claude's-Discretion lever, e.g. seg_len=24.0 or 16.0, UNIFORM across the whole range — and record whether it recovers the gate; include that data point in the escalation so the user decides with full information. NEVER use per-region/adaptive params.)

Record the chosen `degree_final`, `seg_len_final`, `max|Δλ|`, and `worst_jd` in `/tmp/chiron_spike_30.out` for Task 3 to transcribe. These values are the contract Plan 30-02 consumes.
  </action>
  <verify>`/tmp/chiron_spike_30.out` contains a final line stating the decision: either `DECISION: degree_final=10 ...` / `DECISION: degree_final=12 ...` with a PASS, OR `DECISION: ESCALATE — degree=12 fails at 1900.0, range-narrowing needs user approval`.</verify>
  <done>A definitive degree decision exists: degree=10 kept, OR degree=12 adopted (with re-measured max|Δλ| < 0.01°), OR the escalation condition is detected and flagged for the Task 3 checkpoint. Params stayed uniform throughout (one degree, one seg_len).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Record verdict in decision log — OR STOP and ask user if the gate cannot hold at 1900.0</name>
  <decision>Spike verdict: record the passing degree decision and proceed to regeneration (Plan 30-02), OR — only if even degree=12 (and a uniform seg_len reduction) cannot hold < 0.01° at 1900.0 — STOP and ask the user whether to narrow the lower bound (e.g. to ~1905), which is a requirement/scope change.</decision>
  <context>
The LOCKED decision states the lower bound 1900 is FIRM and that raising it is a last-resort requiring explicit user approval. The spike's whole purpose is to resolve, with measured data, whether uniform-param Chebyshev holds < 0.01° on the post-perihelion 1900–1910 edge.

**Automation done before this checkpoint:** Tasks 1–2 ran the full measurement (pre-flight + dense edge + gate) and produced a definitive `degree_final`/`max|Δλ|` decision (or detected the escalation condition).

**If the gate PASSED (degree=10 or degree=12):** This is NOT a true user-decision pause — record the verdict in the decision log and let Plan 30-02 proceed. Concretely:
- Add a row to the PROJECT.md Key Decisions table (the table starts at line 168; append a new row, matching the existing `| Decision | Rationale | Outcome |` 3-column format):
  `| [Phase 30-01] Chiron range 1900–2100 spike: degree={degree_final}, seg=32d, max|Δλ|={X.XXXXXX}° < 0.01° | Perihelion ~1895–96 just below 1900 bound; dense 1900–1910 edge sampling confirms uniform-param fit holds | ✓ {degree kept at 10 / raised to 12} (v1.4) |`
- Add a decision-log line to STATE.md under `### Decisions` (the section begins near line 64; follow the existing `- [Phase NN-NN]: ...` bullet style):
  `- [Phase 30-01]: Chiron range 1900-2100 spike: max|Δλ|={X.XXXXXX}°, params seg=32d/degree={degree_final}, gate PASS < 0.01°; new .npz = 2283 segs, jd_start=2415020.5, jd_end=2488069.5. Ephemeral spike thrown away (spike-only). Plan 30-02 regenerates with degree={degree_final}.`
- Then resume autonomously (resume-signal "recorded").

**If the gate FAILED at 1900.0 even at degree=12 (escalation condition from Task 2):** This is a genuine STOP. Present the measured data (degree=10 max|Δλ|, degree=12 max|Δλ|, any uniform seg_len-reduction result, and the worst JD near 1900) and ask the user to choose. Do NOT regenerate the `.npz`, do NOT narrow the range without approval.
  </context>
  <options>
    <option id="proceed">
      <name>Gate PASSED — record verdict and proceed</name>
      <pros>Uniform-param fit holds < 0.01° across 1900–2100 including the perihelion edge; lower bound stays firm at 1900; Plan 30-02 regenerates with the recorded degree_final</pros>
      <cons>None — this is the expected path</cons>
    </option>
    <option id="raise-bound">
      <name>Gate FAILS at 1900.0 even at degree=12 — narrow lower bound (e.g. ~1905)</name>
      <pros>Recovers < 0.01° accuracy by excluding the un-fittable post-perihelion segment(s)</pros>
      <cons>Requirement/scope change — the CHIR-09/10/11 goal explicitly targets 1900; needs user sign-off; documentation of the bound must change downstream</cons>
    </option>
    <option id="other-lever">
      <name>Gate FAILS at 1900.0 — try a different uniform lever before narrowing</name>
      <pros>Keeps 1900 bound firm; e.g. further uniform seg_len reduction at degree=12</pros>
      <cons>May still not converge; risks over-engineering; user weighs vs simply narrowing the bound</cons>
    </option>
  </options>
  <resume-signal>If gate PASSED: the verdict is recorded in PROJECT.md + STATE.md and execution resumes autonomously (signal "recorded"). If gate FAILED at 1900.0: select "raise-bound" (and state the new lower bound), "other-lever", or provide direction.</resume-signal>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>
Branch on the Task 2 verdict:

- **Gate PASSED (degree=10 or degree=12):** Do NOT pause for the user — record the verdict and resume. Append a row to the PROJECT.md Key Decisions table (table starts ~line 168, 3-column `| Decision | Rationale | Outcome |` format): `| [Phase 30-01] Chiron range 1900–2100 spike: degree={degree_final}, seg=32d, max|Δλ|={X.XXXXXX}° < 0.01° | Perihelion ~1895–96 just below 1900 bound; dense 1900–1910 edge sampling confirms uniform-param fit holds | ✓ {degree kept at 10 / raised to 12} (v1.4) |`. Append a STATE.md decision-log line under `### Decisions` (~line 64, `- [Phase NN-NN]: ...` style): `- [Phase 30-01]: Chiron range 1900-2100 spike: max|Δλ|={X.XXXXXX}°, params seg=32d/degree={degree_final}, gate PASS < 0.01°; new .npz = 2283 segs, jd_start=2415020.5, jd_end=2488069.5. Ephemeral spike thrown away (spike-only). Plan 30-02 regenerates with degree={degree_final}.` Resume autonomously (signal "recorded").

- **Gate FAILED at 1900.0 even at degree=12 (escalation from Task 2):** STOP. Do NOT regenerate the .npz, do NOT narrow the range. Present the measured data (degree=10 and degree=12 max|Δλ|, any uniform seg_len-reduction result, worst JD near 1900) and the three options above; wait for the user to choose raise-bound / other-lever / direction. Narrowing the lower bound is a scope change requiring explicit approval.

Finally: delete the ephemeral spike script — `rm -f /tmp/chiron_spike_30.py` — and confirm nothing was staged under tools/ ketu/ tests/ pyproject (spike-only).
  </action>
  <verify>On PASS: PROJECT.md and STATE.md each contain a new `[Phase 30-01]` line with the measured max|Δλ| and degree_final; `git status --porcelain tools/ ketu/ tests/ pyproject.toml` is EMPTY. On FAIL-at-1900: execution is paused with the measured data and options presented to the user.</verify>
  <done>Verdict recorded in PROJECT.md + STATE.md (PASS path) and execution resumes; OR the workflow halted and asked the user with measured data (FAIL-at-1900 path). Ephemeral spike script removed; no source/test/data/pyproject files staged.</done>
</task>

</tasks>

<verification>
- Pre-flight build check ran and passed (pyswisseph imports, seas_18.se1 findable, retflag printed at J2000/1900/2100).
- max|Δλ| over 1900–2100 measured at degree=10 with a per-segment dense report for segments 0–10 (the 1900–1910 edge).
- A definitive degree decision exists (10 kept, or 12 adopted with re-measured pass), OR the STOP-and-ask escalation fired with measured data presented to the user.
- Decision-log entry written to BOTH PROJECT.md (Key Decisions table) and STATE.md (`### Decisions`) with measured max|Δλ| and final seg/degree params — only when the gate passed.
- Confirm the spike script lives ONLY under /tmp and nothing was staged: `git status --porcelain tools/ ketu/ tests/ pyproject.toml` returns EMPTY (no source/test/data/pyproject changes from this plan). Only `.planning/PROJECT.md` and `.planning/STATE.md` are modified.
- Sanity: `python -c "import swisseph as swe; print(swe.julday(1900,1,1,0.0), swe.julday(2100,1,1,0.0))"` prints `2415020.5 2488069.5`.
</verification>

<success_criteria>
- Blocking spike executed BEFORE any `.npz` regeneration; max|Δλ| vs Swiss Ephemeris recorded including the 1900–1910 perihelion-aftermath edge.
- Final params decided under the uniform-param rule (degree 10 kept, or raised to 12; seg_len uniform); if even degree=12 fails at 1900.0, the workflow STOPPED and asked the user (no silent range-narrowing).
- Verdict (max|Δλ|, seg, degree, decision) recorded in PROJECT.md Key Decisions + STATE.md decision log.
- Spike is spike-only: the measurement script is thrown away; nothing committed under tools/ ketu/ tests/ pyproject.
</success_criteria>

<output>
After completion, create `.planning/phases/30-chiron-range-1900-2100/30-01-SUMMARY.md` recording the measured max|Δλ| at degree=10 (and degree=12 if tried), the per-segment edge results for 1900–1910, the final `degree_final`/`seg_len_final`, and the recorded decision. This SUMMARY is what Plan 30-02 reads to know which degree to regenerate with.
</output>
