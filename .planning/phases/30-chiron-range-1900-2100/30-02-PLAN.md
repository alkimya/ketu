---
phase: 30-chiron-range-1900-2100
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - tools/gen_chiron_coeffs.py
  - ketu/data/chiron_coeffs.npz
  - ketu/ephemeris/chiron.py
  - tests/ephemeris/test_chiron_unit.py
  - tests/ephemeris/test_chiron_regression.py
autonomous: true
must_haves:
  truths:
    - "tools/gen_chiron_coeffs.py setup_oracle range is 1900-2100 (jd0=julday(1900,...), jd1=julday(2100,...)) and _REF_JDS includes new wing dates"
    - "ketu/data/chiron_coeffs.npz is regenerated: jd_start=2415020.5, jd_end=2488069.5, 2283 segments, degree = the spike-decided degree_final"
    - "The Phase 24-04 actual_len last-segment fix in chiron.py:113 is PRESERVED (verified, not re-implemented) — new last segment actual_len = 25.0 days"
    - "ketu/ephemeris/chiron.py contains ZERO pyswisseph/swisseph import (pure-NumPy runtime preserved)"
    - "test_chiron_unit.py shape/degree assertions updated ATOMICALLY with the .npz to (2283, degree_final+1) and int(degree)==degree_final"
    - "Regression refs keep all 1950-2050 pins AND add >=1 pre-1950 (e.g. 1920) + >=1 post-2050 (e.g. 2080) + >=1 in 1900-1910 if spike showed risk — all < 0.01°"
    - "Out-of-range silent-clamp behavior unchanged: tests at the bounds (1900.0, 2100.0 inside, valid) and just outside (just before 1900, just after 2100) pass"
    - "Full test suite green at 100% coverage; numpydoc gate passes (stale 1950/2050 docstrings refreshed)"
  artifacts:
    - path: tools/gen_chiron_coeffs.py
      provides: "Permanent generator: 1900-2100 range in setup_oracle + extended _REF_JDS + degree_final if raised"
      contains: "swe.julday(1900, 1, 1, 0.0)"
    - path: ketu/data/chiron_coeffs.npz
      provides: "Regenerated Chebyshev coeffs spanning 1900-2100, 2283 segments"
      min_lines: 1
    - path: tests/ephemeris/test_chiron_regression.py
      provides: "Re-pinned _CHIRON_REFS: old 1950-2050 refs + new pre-1950/post-2050 wing refs, all < 0.01°"
      contains: "_CHIRON_REFS"
    - path: tests/ephemeris/test_chiron_unit.py
      provides: "Shape/degree assertions matching the regenerated .npz (2283, degree_final+1)"
      contains: "2283"
  key_links:
    - from: "tools/gen_chiron_coeffs.py setup_oracle (lines 110-111)"
      to: "ketu/data/chiron_coeffs.npz"
      via: "generate_all_coefficients(jd0,jd1) over 1900-2100 then write_npz with built-in <0.01° gate"
      pattern: "swe\\.julday\\(2100"
    - from: "ketu/ephemeris/chiron.py _eval_chiron_qty (line 113)"
      to: "data['jd_end'] = 2488069.5"
      via: "actual_len = min(seg_starts[si]+seg_len, jd_end) - seg_starts[si] (Phase 24-04 fix, reads jd_end from .npz)"
      pattern: "actual_len = min\\("
    - from: "tools/gen_chiron_coeffs.py --dump-refs"
      to: "tests/ephemeris/test_chiron_regression.py _CHIRON_REFS"
      via: "oracle longitudes captured and pinned as (jd, lon) tuples"
      pattern: "--dump-refs"
---

<objective>
Regenerate the embedded Chiron Chebyshev `.npz` over 1900–2100 using the spike-decided degree (from Plan 30-01's recorded verdict), update the single breaking shape/degree test ATOMICALLY with the data, re-pin the accuracy regression to span 1900–2100 (keep the 1950–2050 refs, add wing refs), refresh stale docstrings, and verify the full suite is green at 100% coverage with pure-NumPy runtime intact.

Purpose: This is the data-extension half of Phase 30. The blocking spike (Plan 30-01) has already proven uniform-param accuracy and recorded `degree_final`. Now the permanent generator gets its range edits (CHIR-10), the `.npz` is regenerated with the built-in < 0.01° gate, the regression references span the new range (CHIR-11), and the out-of-range silent-clamp contract is locked with bound + just-outside tests. The Phase 24-04 last-segment `actual_len` fix and the zero-pyswisseph runtime are preserved (verified, not touched).

Output: Regenerated `ketu/data/chiron_coeffs.npz` (2283 segs, jd_start=2415020.5, jd_end=2488069.5, degree=degree_final), updated generator, updated unit + regression tests, refreshed docstrings — full suite green at 100% coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-chiron-range-1900-2100/30-CONTEXT.md
@.planning/phases/30-chiron-range-1900-2100/30-RESEARCH.md
@.planning/phases/30-chiron-range-1900-2100/30-01-SUMMARY.md
@tools/gen_chiron_coeffs.py
@ketu/ephemeris/chiron.py
@tests/ephemeris/test_chiron_unit.py
@tests/ephemeris/test_chiron_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit generator (range + _REF_JDS + degree_final), regenerate .npz, capture new wing refs</name>
  <files>tools/gen_chiron_coeffs.py, ketu/data/chiron_coeffs.npz</files>
  <action>
First read `.planning/phases/30-chiron-range-1900-2100/30-01-SUMMARY.md` to retrieve `degree_final` (10 or 12) and the recorded params. Activate venv: `cd /home/loc/workspace/ketu && source venv/bin/activate`.

**1a — Permanent generator edits (these ARE committed, distinct from the ephemeral spike script):**
- In `setup_oracle()` change the two hardcoded range lines (currently `jd0 = swe.julday(1950, 1, 1, 0.0)` and `jd1 = swe.julday(2050, 1, 1, 0.0)`, ~lines 110–111) to `swe.julday(1900, 1, 1, 0.0)` and `swe.julday(2100, 1, 1, 0.0)`. These are the ONLY place jd0/jd1 are computed.
- Extend `_REF_JDS` (lines ~61–69): KEEP all 7 existing JDs verbatim (1950→2050), and ADD new wing dates so `--dump-refs` emits the full extended pinning list in one shot. At minimum add `swe.julday(1920,1,1,0.0)` (1920, pre-1950 wing) and `swe.julday(2080,1,1,0.0)` (2080, post-2050 wing). If Plan 30-01's edge report showed risk in 1900–1910, ALSO add `swe.julday(1905,1,1,0.0)` (or 1900) so a high-gradient ref is pinned. Use hardcoded JD float literals consistent with the existing style (you may compute them once via `python -c "import swisseph as swe; print(swe.julday(1920,1,1,0.0))"`), and keep `_REF_JDS` sorted ascending. Update the comment above `_REF_JDS` to list the new dates.
- **IF degree_final == 12 (only if the spike raised it):** change `_DEGREE: int = 10` → `_DEGREE: int = 12` (line 56). `_N_FIT = _DEGREE + 8` recomputes automatically. Update the "11 coefficients" / "LOCKED 23-DECISION" comment to note the Phase 30 range extension raised degree to 12 for the wider 1900–2100 span. IF degree_final == 10, leave `_DEGREE` untouched.
- Refresh the generator's stale "1950-2050" docstrings/comments to "1900-2100" where they describe the range (module docstring lines ~10–17, `setup_oracle` docstring, `generate_all_coefficients` docstring line ~296). Cosmetic but keeps maintainers correct.

**1b — Regenerate the .npz (built-in < 0.01° gate enforces correctness):**
- Run `python tools/gen_chiron_coeffs.py` (writes to default `ketu/data/chiron_coeffs.npz`). The script's own validation gate exits non-zero if max|Δλ| >= 0.01° — so a successful exit IS the gate. Watch for `Validation OK : max|Δλ|=...° < 0.01°` and `Générateur terminé avec succès.`.
- Verify the regenerated file: `python -c "import numpy as np; d=np.load('ketu/data/chiron_coeffs.npz'); print('segs', d['lon_coeffs'].shape, 'deg', int(d['degree']), 'jd0', float(d['jd_start']), 'jd1', float(d['jd_end']))"`. Expect shape `(2283, degree_final+1)`, degree=degree_final, jd_start=2415020.5, jd_end=2488069.5.

**1c — Capture the extended reference longitudes for re-pinning:**
- Run `python tools/gen_chiron_coeffs.py --dump-refs 2>&1 | tee /tmp/chiron_refs_30.out`. This prints `(jd, lon)` tuples for ALL `_REF_JDS` (old + new). Save this output — Task 3 transcribes the new wing tuples into the regression test (and re-confirms the existing ones match).

DO NOT touch `ketu/ephemeris/chiron.py` here — the evaluator needs no change (verified next task).
  </action>
  <verify>`np.load('ketu/data/chiron_coeffs.npz')` shows shape `(2283, degree_final+1)`, `int(degree)==degree_final`, `jd_start==2415020.5`, `jd_end==2488069.5`. The generator ran to "Générateur terminé avec succès." (built-in gate passed). `/tmp/chiron_refs_30.out` lists ref tuples for old + new dates.</verify>
  <done>Generator permanently edited for the 1900–2100 range (+ degree_final if raised), .npz regenerated with 2283 segments passing the built-in < 0.01° gate, and the extended dump-refs output captured for pinning.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve actual_len fix + pure-NumPy runtime, update breaking shape/degree test atomically, refresh chiron.py docstrings</name>
  <files>ketu/ephemeris/chiron.py, tests/ephemeris/test_chiron_unit.py</files>
  <action>
Retrieve `degree_final` from the 30-01 SUMMARY (same value used in Task 1).

**2a — VERIFY (do NOT re-implement) the Phase 24-04 actual_len fix:**
- Confirm `ketu/ephemeris/chiron.py` line ~113 still reads `actual_len = min(seg_starts[si] + seg_len, jd_end) - seg_starts[si]`. It reads `jd_end` from `data["jd_end"]` (now 2488069.5), so it auto-adapts to the new last-segment length (25.0 days). DO NOT change this line. Confirm with: `python -c "import inspect, ketu.ephemeris.chiron as c; print('actual_len = min' in inspect.getsource(c._eval_chiron_qty))"` → True.

**2b — VERIFY pure-NumPy runtime (zero pyswisseph) — guard, do not break:**
- `grep -nE "swisseph|pyswisseph|import swe|\\bswe\\b" ketu/ephemeris/chiron.py` MUST return NOTHING. If anything appears, that is a regression — do not add any swisseph import. (The .npz is generated offline by the build-only generator; the runtime evaluator stays pure NumPy.)

**2c — Update the ONE breaking test atomically with the regenerated .npz** (`tests/ephemeris/test_chiron_unit.py`, `test_load_chiron_data_shapes`, lines ~55–61):
- Change the four shape assertions from `(1142, 11)` / `(1142,)` to `(2283, degree_final+1)` / `(2283,)`. For degree_final=10 that is `(2283, 11)` and `(2283,)`; for degree_final=12 that is `(2283, 13)` and `(2283,)`.
- Change `assert int(data["degree"]) == 10` (line ~61) to `== degree_final`.
- Update the test docstring (lines ~43–44) "1142 segments, degree 10 (11 coefficients ...)" to reflect 2283 segments and the actual degree/coeff count.
- Also refresh any cosmetic stale-range comments in this file the research flagged (e.g. line ~108 "natural 1950-2050 data ... ~0.019°/day"): change "1950-2050" → "1900-2100". These are comments, no assertion change.

**2d — Refresh stale 1950/2050 docstrings in chiron.py:**
- Update the description-only docstring mentions of "1950"/"2050" (lines ~36, ~75, ~85, ~106) to "1900"/"2100". The research confirmed NO runnable doctest in those sections hardcodes the old shape, so doctests are unaffected — but verify after editing that `python -m pytest --doctest-modules ketu/ephemeris/chiron.py -q` still passes (numpydoc gate is BLOCKING).
  </action>
  <verify>`grep -nE "swisseph|\\bswe\\b" ketu/ephemeris/chiron.py` → empty. `pytest tests/ephemeris/test_chiron_unit.py -q` passes (shapes now (2283, degree_final+1)). `python -m pytest --doctest-modules ketu/ephemeris/chiron.py -q` passes. actual_len line unchanged.</verify>
  <done>actual_len fix verified intact; zero swisseph import confirmed; the single breaking shape/degree test updated to match the regenerated .npz; stale 1950/2050 docstrings/comments refreshed; doctests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin regression refs (1950-2050 kept + wings added), test bounds + just-outside clamp, full suite + 100% coverage</name>
  <files>tests/ephemeris/test_chiron_regression.py</files>
  <action>
**3a — Re-pin the accuracy regression** (`tests/ephemeris/test_chiron_regression.py`, `_CHIRON_REFS`, lines ~41–49):
- KEEP all 7 existing 1950–2050 tuples verbatim (center non-regression). The regenerated .npz must still satisfy them (re-confirm against `/tmp/chiron_refs_30.out`: if any existing value drifted beyond 0.01°, investigate — but degree/range extension at the center should not move 1950–2050 longitudes; the oracle is identical).
- ADD the new wing tuples from `/tmp/chiron_refs_30.out` (oracle = Swiss Ephemeris via `--dump-refs`): at minimum `(jd_1920, lon)` and `(jd_2080, lon)` with their `# 1920-01-01 retflag=...` / `# 2080-01-01 retflag=...` comments, plus the 1900–1910 high-gradient ref (e.g. 1905) IF Plan 30-01 flagged edge risk. Keep `_CHIRON_REFS` sorted by JD ascending. Use the EXACT 6-decimal longitudes printed by dump-refs (these are frozen constants — no pyswisseph at test time).
- Update the module docstring (lines ~12–13) "1950-01-01 through 2050-01-01, covering the full embedded .npz range" → "1900-01-01 through 2100-01-01" and the count note. Update the capture-metadata comment block (date, that the range is now 1900–2100). TOLERANCE_DEG stays 0.01.

**3b — Bounds + just-outside clamp tests (lock the range contract):**
- Confirm the existing `test_clamp_below_range` / `test_clamp_above_range` in `tests/ephemeris/test_chiron_unit.py` (relative offsets from `seg_starts[0]` / `seg_starts[-1]`) still pass unchanged — they verify the silent-clamp behavior is preserved after regeneration. Do NOT change error semantics (silent clamp stays; no ValueError).
- Ensure coverage of the NEW bounds: add (if not already implied by the wing refs) a pinned ref AT 1900.0 boundary handling — the 1900-01-01 JD (2415020.5) is inside/at the lower edge and must give a finite longitude < 0.01° from oracle. The 2100 upper edge: JD just below jd_end is valid. Add to the regression file a small assertion (or rely on a 1900-01-01 / near-2100 wing ref) confirming a finite, in-range longitude exactly at the bounds, and that a JD just outside (e.g. jd_start-1.0, jd_end+1.0) returns a finite clamped longitude (no exception, no NaN) — mirroring the existing clamp tests but anchored to the new range. Keep it minimal and pure-NumPy (use `calc_planet_position` / the chiron evaluator, NOT pyswisseph).

**3c — Full verification (the phase gate):**
- `pytest tests/ -q` — entire suite green (expect ~1531+ tests, including new wing parametrize cases).
- Coverage MUST stay 100%: `pytest tests/ --cov=ketu --cov-report=term-missing` (or the project's `make coverage` / pyproject `fail_under=100`) — confirm 100%, no missing lines introduced.
- numpydoc / doctest gate: run the project's doctest gate (`make doctest` if present, else `python -m pytest --doctest-modules ketu -q`) — green.
- Final cleanliness check: `git status --porcelain` shows ONLY the 5 declared files modified (generator, .npz, chiron.py, the two test files) plus `.planning/` — and NO leftover spike artifacts (the /tmp spike script and /tmp/*.out are outside the repo, untracked).
  </action>
  <verify>`pytest tests/ -q` all green; coverage report shows 100% (fail_under=100 satisfied); doctest gate green; `_CHIRON_REFS` contains the 7 original + >=2 new wing tuples (1920 + 2080, +1905 if flagged) all under 0.01°; clamp tests pass; `git status` shows only the declared files + .planning.</verify>
  <done>Regression spans 1900–2100 (old refs kept + wings added, all < 0.01°); bounds + just-outside clamp contract verified (silent clamp preserved, no error-semantics change); full suite green at 100% coverage with numpydoc/doctest gates passing.</done>
</task>

</tasks>

<verification>
- `ketu/data/chiron_coeffs.npz`: shape `(2283, degree_final+1)`, `degree==degree_final`, `jd_start==2415020.5`, `jd_end==2488069.5` — built-in generator gate passed (< 0.01°).
- `tools/gen_chiron_coeffs.py`: setup_oracle range is 1900/2100; `_REF_JDS` extended with wing dates (1920, 2080, +1905 if flagged); `_DEGREE` = degree_final.
- `ketu/ephemeris/chiron.py`: actual_len fix (line ~113) UNCHANGED; `grep -E "swisseph|\bswe\b"` returns empty (pure-NumPy runtime preserved).
- `tests/ephemeris/test_chiron_unit.py`: shape assertions `(2283, degree_final+1)` / `(2283,)`, `int(degree)==degree_final` — updated atomically with the .npz.
- `tests/ephemeris/test_chiron_regression.py`: 7 original 1950–2050 refs kept + >=1 pre-1950 + >=1 post-2050 wing refs added (+ high-gradient ref if spike flagged risk), all < 0.01°; bounds + just-outside clamp behavior verified unchanged.
- `pytest tests/ -q` green; coverage 100% (fail_under=100); doctest/numpydoc gate green.
- `git status --porcelain` shows only the 5 declared files + `.planning/`; no spike artifacts under the repo tree.
</verification>

<success_criteria>
- `ketu/data/chiron_coeffs.npz` regenerated over 1900–2100 (jd_start/jd_end reflect the new range; 2283 segments; degree=degree_final from the spike); built-in < 0.01° gate passed; Phase 24-04 actual_len fix preserved; runtime stays 100% pure NumPy (no pyswisseph in chiron.py).
- Regression tests re-pinned to span 1900–2100: original 1950–2050 refs kept + >=1 pre-1950 + >=1 post-2050 added (+ 1900–1910 ref if the spike showed risk); `test_chiron_regression.py` passes the < 0.01° gate on the full new range.
- Out-of-range silent-clamp behavior preserved and tested at the bounds (1900.0, 2100.0) and just outside; no change to error semantics.
- Full suite green at 100% coverage; numpydoc/doctest gates green; the single breaking shape/degree test updated atomically with the .npz.
</success_criteria>

<output>
After completion, create `.planning/phases/30-chiron-range-1900-2100/30-02-SUMMARY.md` recording: final degree used, regenerated .npz metadata (segs/jd_start/jd_end/file size), the wing reference dates pinned and their oracle longitudes, confirmation the actual_len fix and pure-NumPy runtime were preserved, and the final test/coverage results.
</output>
