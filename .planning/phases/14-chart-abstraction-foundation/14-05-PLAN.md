---
phase: 14-chart-abstraction-foundation
plan_number: 5
plan_name: doc-gates-coverage-and-makefile
depends_on: [2, 3, 4]
parallelizable_with: []
requirements: [CHART-05]
---

# Plan 14-05 — Doc gates finaux + couverture ≥95 % + cible Makefile

## Goal

Verrouiller les gates qualité Phase 13 sur le sous-package finalisé `ketu/charts/` :
1. Couverture ≥95 % sur `ketu/charts/` (ratchet identique à v1.1 houses), wirée via une cible Makefile dédiée `make charts-coverage` (mirror exact de `make houses-coverage`).
2. `interrogate ketu/` reste ≥95 % (déjà garanti par les Plans 01-04 ; ce plan re-vérifie globalement et corrige toute régression).
3. `numpydoc lint` clean sur les 3 fichiers `ketu/charts/*.py` finalisés (toutes sections Parameters/Returns/Raises/Notes/Examples présentes ; les Examples vectorisés exigés par success criterion 14.2 sont visibles).
4. Polish final : enrichissement des docstrings publiques avec section `See Also` cross-references, harmonisation du wording avec la voix Sophie (français pour PLAN.md, anglais pour le code).

Pas de nouveau code de production. Pas de nouveaux tests fonctionnels (les Plans 02/03/04 ont déjà couvert les comportements). Ce plan est le finishing-touch ratchet.

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-CONTEXT.md` § « Discrétion » bullet 3 (coverage exclusion likely none needed) + canonical refs Phase 13 doc gates.
2. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-RESEARCH.md` § 7 (doc gates checklist), § 8 plan 5.
3. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-PATTERNS.md` § 7.4-7.7 (Examples positions, numpydoc sections, coverage config, mypy strict).
4. `/home/loc/workspace/ketu/Makefile` lignes 21-39 — gabarit `houses-coverage` à mirorer (avec le commentaire sur le bug NumPy reload).
5. `/home/loc/workspace/ketu/pyproject.toml` lignes 76-80 (markers pytest), 82-97 (coverage config), 99-131 (interrogate + numpydoc).
6. `/home/loc/workspace/ketu/ketu/houses/api.py` lignes 35-97 — gabarit docstring numpydoc-clean de qualité v1.1 (référence pour le polish des docstrings `compute_chart` et `is_day_chart`).
7. État courant de `ketu/charts/api.py` après merge des Plans 02, 03, 04.

## Files

**Crée :** aucun nouveau fichier.

**Modifie :**
- `/home/loc/workspace/ketu/Makefile` (ajoute la cible `charts-coverage` et l'enregistre dans `.PHONY`).
- `/home/loc/workspace/ketu/pyproject.toml` (ajoute le marker pytest `charts_coverage_gate`, mirror du `houses_coverage_gate` ligne 79).
- `/home/loc/workspace/ketu/ketu/charts/api.py` (polish final docstrings : sections `See Also`, vérification Examples vectorisés présents, harmonisation wording).
- `/home/loc/workspace/ketu/ketu/charts/__init__.py` (vérification : exemple vectorisé présent au module-level docstring per PATTERNS §7.4).
- `/home/loc/workspace/ketu/ketu/charts/core.py` (vérification : section "Why a structured array" est bien titrée et complète per success criterion 14.5).

**Lit (référence) :**
- `/home/loc/workspace/ketu/Makefile` (target `houses-coverage` à mirorer).
- `/home/loc/workspace/ketu/ketu/houses/api.py` (gabarit docstring v1.1).
- Le repo entier après Plans 02/03/04 mergés (état pré-validation finale).

## Tasks

### Task 1 — Audit docstrings finalisés `ketu/charts/api.py`

Re-lire chaque docstring de fonction publique (`compute_chart`, `is_day_chart`) et vérifier :

- **Summary line** présente, impérative, ≤ 79 chars (PR01).
- **Parameters** : tous les params typés et décrits (PR02-PR10).
- **Returns** : présent, typé (RT01-RT05).
- **Raises** : présent pour `compute_chart` (mentionne `HighLatitudeError` propagée depuis `calculate_houses`).
- **Notes** : section présente, contient :
  - `compute_chart` : note D-16 sur la boucle Python sur S, note D-07 default CLASSICAL, note UTC-only sur jd.
  - `is_day_chart` : note D-13 sunrise-inclusive, note D-15 polar safety Porphyry, note D-14 « houses 7-12 = day ».
- **Examples** : présent et CONTIENT un exemple vectorisé (success criterion 14.2 et PATTERNS §7.4) :
  - `compute_chart` : montrer scalaire ET vectorisé `np.array([...])` → vérifier `chart.shape`, `chart["body_lons"].shape`, `chart["aspect_matrix"].shape` dans le doctest.
  - `is_day_chart` : montrer scalaire ET vectorisé.
- **See Also** : section AJOUTÉE par ce plan si absente, cross-references vers fonctions reliées :
  - `compute_chart` → `ketu.houses.calculate_houses`, `ketu.aspects.calculate_aspects_vectorized`, `ketu.charts.is_day_chart`.
  - `is_day_chart` → `ketu.charts.compute_chart`, `ketu.houses.house_of`.

Si le docstring de `compute_chart` contient encore une note résiduelle de 14-02 (« aspects parameter currently ignored ») non nettoyée par 14-03, la SUPPRIMER ici.

### Task 2 — Audit docstring `ketu/charts/core.py` (CHART_DTYPE)

Vérifier :
- La section `Notes` titrée « Why a structured array? » est présente et contient les 4 points (success criterion 14.5).
- Les commentaires `#:` sphinx au-dessus de `CHART_DTYPE` documentent les 14 champs avec shapes et sentinelles.
- Le docstring du module mentionne la convention sentinel `chart["aspect_matrix"] >= 0` au moins une fois (CONTEXT § Specifics ligne 142).
- L'ordre canonique des 13 corps est documenté (Sun=0..Lilith=12, frozen par D-08).

Si une de ces conditions manque, la corriger ici.

### Task 3 — Audit docstring `ketu/charts/__init__.py`

Vérifier :
- Le docstring contient un exemple vectorisé visible dès l'`__init__.py` (PATTERNS §7.4 : « success criterion 14.2 visible à l'`__init__` ET à la fonction »).
- La section `See Also` pointe vers les modules internes consommés (`ketu.houses.calculate_houses`, `ketu.aspects.calculate_aspects_vectorized`).
- `__all__` est strictement trié alphabétiquement (PATTERNS §7.3).

### Task 4 — Ajouter la cible `charts-coverage` au `Makefile`

Mirror exact de `houses-coverage` (Makefile lignes 21-39). Ajouter :

```makefile
.PHONY: test test-fast houses-coverage charts-coverage doc-gates mypy clean
```

Et la nouvelle cible :

```makefile
## charts-coverage: Run the CHART-05 ≥95% coverage gate scoped to ketu.charts.
##
## Mirror of `houses-coverage` (HOU-09). Same two-step pattern to avoid
## the NumPy `_NoValueType` reload bug triggered when coverage.py uses
## `source=ketu.charts` (sub-package). With `source=ketu` (full package)
## coverage works cleanly. We run pytest once with the project-wide
## `ketu` source (no `--cov-fail-under` since most modules are
## unexercised by tests/charts/), then scope the threshold check to
## `ketu/charts/*` via `coverage report --include`.
charts-coverage:
	$(PYTHON) -m pytest tests/charts/ -o addopts="" --cov --cov-report= --cov-fail-under=0
	$(PYTHON) -m coverage report --include='ketu/charts/*' --fail-under=95 -m
```

L'insérer juste après la cible `houses-coverage` pour la lisibilité, avant `doc-gates`.

### Task 5 — Ajouter le marker pytest `charts_coverage_gate` au `pyproject.toml`

Mirror de `houses_coverage_gate` (pyproject.toml ligne 79). Éditer la liste `markers` :

```toml
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "houses_coverage_gate: HOU-09 95% coverage gate for ketu.houses (run via Makefile target `make houses-coverage`)",
    "charts_coverage_gate: CHART-05 95% coverage gate for ketu.charts (run via Makefile target `make charts-coverage`)",
]
```

Ce marker peut rester non-utilisé pour l'instant (aucun test ne le porte) ; il documente l'existence du gate dans le pyproject pour grep-ability et symétrie avec houses.

### Task 6 — Vérifier qu'aucun carve-out n'existe pour `ketu.charts.*` dans mypy / coverage

Inspecter `pyproject.toml` :
- `[tool.mypy.overrides]` ligne 144-153 : la liste de modules carve-out NE DOIT PAS contenir `ketu.charts.*`. Si elle le contient (introduit par erreur dans un plan précédent), la retirer. Pinne « `ketu/charts/` reste mypy --strict clean dès le départ » (CLAUDE.md cross-cutting v1.2, PATTERNS §7.7).
- `[tool.coverage.run]` ligne 84-88 : la liste `omit` NE DOIT PAS contenir `ketu/charts/*`.
- `[tool.interrogate]` ligne 101-106 : la liste `exclude` NE DOIT PAS contenir `ketu/charts*`.

Si l'une de ces listes contient une entrée pour `ketu.charts`, la retirer dans ce plan. Si tout est clean, **NE PAS modifier `pyproject.toml`** au-delà du marker pytest de la Task 5.

### Task 7 — Exécuter le gate de couverture et corriger les éventuels gaps

Lancer :

```bash
cd /home/loc/workspace/ketu
make charts-coverage
```

Si le gate échoue (couverture < 95 %) :
- Identifier les lignes non couvertes via la sortie `-m` (term-missing).
- **Stratégie de correction par ordre de préférence** :
  1. **Ajouter un test ciblé** dans `tests/charts/` qui couvre la branche manquante. C'est la solution propre.
  2. Si la branche est défensive (genre `if some_unreachable: raise ValueError(...)`), ajouter `# pragma: no cover` (cf. exclude_lines existants pyproject.toml ligne 92-97). À utiliser PARCIMONIEUSEMENT et avec une justification en commentaire.
  3. Si la branche est legitimately unreachable (impossibility), refactorer pour la supprimer.
- **Anti-pattern** : élargir `[tool.coverage.run].omit` pour exclure des fichiers `ketu/charts/*` est INTERDIT — tout `ketu/charts/*.py` est in-scope (CHART-05).

Re-lancer `make charts-coverage` jusqu'à exit 0.

### Task 8 — Vérification finale doc gates locale + simulation CI

Lancer la séquence complète qu'attend la CI :

```bash
cd /home/loc/workspace/ketu

# 1. Suite charts complète
python -m pytest tests/charts/ -v -x

# 2. Doc gates (mirror de make doc-gates)
make doc-gates

# 3. mypy --strict
make mypy

# 4. Couverture ≥95 % sur ketu/charts/
make charts-coverage

# 5. Suite globale (sanity régression)
python -m pytest tests/ --no-cov -x

# 6. Couverture globale projet (gate ≥70 % par tooling, mais on vérifie qu'on n'a pas régressé)
python -m pytest tests/ --cov=ketu --cov-report=term-missing --cov-fail-under=70

# 7. Doctest — vérifier que les Examples des docstrings publiques s'exécutent
python -m pytest --doctest-modules ketu/charts/ -v

# 8. Smoke import depuis fresh subprocess
python -c "
from ketu.charts import compute_chart, is_day_chart, CHART_DTYPE
import numpy as np
chart = compute_chart(np.array([2451545.0, 2470204.0]), np.array([48.86, 64.15]), np.array([2.35, -21.94]))
assert chart.shape == (2,)
assert chart['body_lons'].shape == (2, 13)
assert chart['aspect_matrix'].shape == (2, 13, 13)
assert is_day_chart(np.array([2451545.0, 2470204.0]), np.array([48.86, 64.15]), np.array([2.35, -21.94])).shape == (2,)
print('Final smoke OK')
"
```

Toutes les commandes doivent retourner exit 0.

**Doctest gotcha** : le pytest `--doctest-modules` peut échouer si les Examples docstrings contiennent des blocs non-deterministes (e.g. `compute_chart(np.array([2451545.0]))` retournant une array dont la repr varie selon la version numpy). Si c'est le cas :
- **Option 1 (préférée)** : marquer les blocs `>>> ...  # doctest: +SKIP` ou `+ELLIPSIS`.
- **Option 2** : restreindre l'exemple à montrer la shape uniquement (`>>> chart.shape\n(2,)`), pas les valeurs.
Documenter le choix retenu en commentaire dans le docstring.

## Verification gates

```bash
cd /home/loc/workspace/ketu

# Le gate principal du plan
make charts-coverage  # MUST exit 0 (≥95 %)

# Doc gates
make doc-gates        # MUST exit 0

# mypy strict
make mypy             # MUST exit 0

# Suite globale stable
python -m pytest tests/ --no-cov -x  # MUST exit 0

# Suite charts focalisée
python -m pytest tests/charts/ -v -x  # MUST exit 0

# Doctest — Examples docstring exécutables
python -m pytest --doctest-modules ketu/charts/  # MUST exit 0

# Final canary smoke
python -c "from ketu.charts import compute_chart, is_day_chart, CHART_DTYPE; print('OK')"
```

## Done criteria

- [ ] CHART-05 satisfait : `make charts-coverage` exit 0 (couverture ≥95 % sur `ketu/charts/`).
- [ ] Success criterion 14.4 satisfait : couverture ≥95 % sur `ketu/charts/` (ratchet identique HOU-09).
- [ ] Success criterion 14.4 (volet numpydoc) satisfait : `numpydoc lint` clean sur les 3 fichiers `ketu/charts/*.py`.
- [ ] Success criterion 14.5 visible : section « Why a structured array? » dans `ketu/charts/core.py` module docstring, vérifiée par test ratchet en 14-01 ET re-confirmée par audit visuel en 14-05.
- [ ] Success criterion 14.2 visible dans les docstrings : exemples vectorisés présents dans `ketu/charts/__init__.py` (module docstring) ET dans `compute_chart` + `is_day_chart` (function docstrings).
- [ ] `Makefile` contient la cible `charts-coverage` avec le commentaire explicatif sur le bug NumPy reload (mirror exact de `houses-coverage`).
- [ ] `pyproject.toml` contient le marker pytest `charts_coverage_gate` (mirror `houses_coverage_gate`).
- [ ] Aucun carve-out `ketu.charts.*` dans `[tool.mypy.overrides]`, `[tool.coverage.run].omit`, `[tool.interrogate].exclude`.
- [ ] `pytest --doctest-modules ketu/charts/` exit 0 (les Examples des docstrings publiques s'exécutent).
- [ ] `interrogate ketu/` ≥95 % (déjà garanti par les plans précédents, re-vérifié ici).
- [ ] `mypy --strict ketu/` clean (incl. `ketu.charts.*`).
- [ ] Suite globale `pytest tests/` reste verte (zéro régression).
- [ ] Plan terminal de la phase ; après merge, CHART-01..05 sont tous Done.

## Atomic commit message

```
feat(14-05): wire CHART-05 coverage gate + final doc-gate polish

- Add `make charts-coverage` Makefile target (mirror of houses-coverage,
  same two-step pattern to avoid NumPy `_NoValueType` reload bug)
- Add `charts_coverage_gate` pytest marker in pyproject.toml (symmetry
  with houses_coverage_gate)
- Final docstring polish on ketu/charts/api.py:
  - See Also cross-references added (compute_chart ↔ calculate_houses /
    calculate_aspects_vectorized / is_day_chart ; is_day_chart ↔
    compute_chart / house_of)
  - Vectorised Examples confirmed in compute_chart and is_day_chart
    (success criterion 14.2 visible at API surface)
  - Cleaned residual Plan-02/Plan-03 transition notes
- Audit ketu/charts/core.py: "Why a structured array" section confirmed
  (success criterion 14.5)
- Audit ketu/charts/__init__.py: vectorised example confirmed at module
  docstring level
- Confirmed no mypy/coverage/interrogate carve-outs for ketu.charts.*
- pytest --doctest-modules ketu/charts/ green (Examples are executable)

Covers: CHART-05
Refs: success criteria 14.2, 14.4, 14.5, mirror of HOU-09 v1.1 gate
```

## Rollback note

`git revert <commit_sha>` :
- Restore `Makefile` (suppression de la cible `charts-coverage`).
- Restore `pyproject.toml` (suppression du marker `charts_coverage_gate`).
- Annule le polish docstrings (les Examples vectorisés et See Also disparaissent ; les fonctions restent fonctionnelles).

**Effet de bord du revert** : la couverture ≥95 % n'est plus vérifiée par CI (puisque la cible `charts-coverage` n'existe plus). Les Plans 02-04 restent fonctionnels et leurs tests passent toujours, mais le **gate CHART-05** n'est plus enforced. Si revert nécessaire, re-lancer `make charts-coverage` à la main avant de re-commit pour valider que la couverture n'a pas régressé sous 95 %.

Aucun consommateur en aval de Phase 14 ne dépend du Makefile target ; ce revert est purement local à la phase.
