---
phase: 14-chart-abstraction-foundation
plan_number: 1
plan_name: subpackage-skeleton-and-dtype
depends_on: []
parallelizable_with: []
requirements: [CHART-01, CHART-02]
---

# Plan 14-01 — Squelette `ketu/charts/` + `CHART_DTYPE`

## Goal

Poser la fondation contractuelle de la phase : créer le sous-package `ketu/charts/` (`__init__.py`, `core.py`, `api.py` stub) avec `CHART_DTYPE` complet (14 champs, D-01..D-06) et tests structurels qui pinnent les noms de champs, les shapes et les sentinelles. Une fois ce plan mergé, les Plans 14-02/03/04 peuvent travailler en parallèle sans drift sur le contrat de dtype. Doc-gates (`interrogate`, `numpydoc validate`) doivent rester verts dès ce premier commit.

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-CONTEXT.md` — D-01 à D-08 (layout dtype, sentinelles, body axis frozen).
2. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-RESEARCH.md` § 1 (squelette), § 2 (CHART_DTYPE snippet + section "Why structured array"), § 8 plan 1.
3. `/home/loc/workspace/ketu/.planning/phases/14-chart-abstraction-foundation/14-PATTERNS.md` § 1, § 2, § 7.1, § 7.3, § 7.8 (setuptools !), § 8.1 (anti-pattern dataclass).
4. `/home/loc/workspace/ketu/ketu/houses/__init__.py` (lignes 1-53) — gabarit re-export.
5. `/home/loc/workspace/ketu/ketu/houses/core.py` (lignes 1-80) — gabarit `core.py` (style commentaires `#:`, annotation `np.dtype`).
6. `/home/loc/workspace/ketu/tests/houses/test_dtype.py` (intégral) — gabarit tests structurels.
7. `/home/loc/workspace/ketu/pyproject.toml` lignes 60-61 (packages list EXPLICITE), 99-131 (interrogate + numpydoc config).
8. `/home/loc/workspace/ketu/ketu/core.py` § `bodies` — confirmer l'ordre canonique des 13 corps (Sun=0..Lilith=12).

## Files

**Crée :**
- `/home/loc/workspace/ketu/ketu/charts/__init__.py`
- `/home/loc/workspace/ketu/ketu/charts/core.py`
- `/home/loc/workspace/ketu/ketu/charts/api.py` (stub avec signatures + `NotImplementedError`)
- `/home/loc/workspace/ketu/tests/charts/__init__.py` (vide)
- `/home/loc/workspace/ketu/tests/charts/test_dtype.py`

**Modifie :**
- `/home/loc/workspace/ketu/pyproject.toml` (ligne 61 : ajouter `"ketu.charts"` à la liste `packages`).

**Lit (référence) :**
- `/home/loc/workspace/ketu/ketu/houses/{__init__,core,api}.py` (gabarits).
- `/home/loc/workspace/ketu/tests/houses/test_dtype.py` (gabarit tests).

## Tasks

### Task 1 — Ajouter `ketu.charts` à `pyproject.toml`

Édite `pyproject.toml` ligne 61 :

```toml
packages = ["ketu", "ketu.ephemeris", "ketu.aspects", "ketu.cycles", "ketu.cache", "ketu.houses", "ketu.charts", "ketu.cli"]
```

`ketu.charts` est inséré entre `ketu.houses` et `ketu.cli` (ordre alphabétique partiel respecté). **Override explicite vs CONTEXT.md ligne 131** : la config est une liste explicite, pas un find-packages — sans cette ligne, `pip install ketu` n'embarquera pas le sous-package (cf. PATTERNS § 7.8).

### Task 2 — Écrire `ketu/charts/core.py` avec `CHART_DTYPE` + section "Why structured array"

Crée le fichier avec :

1. **Module docstring** structuré numpydoc-clean :
   - Summary line : « Core types for the charts subpackage. »
   - Paragraphe « Defines :data:`CHART_DTYPE` … » (mirror `ketu/houses/core.py:1-15`).
   - **Section dédiée `Notes` titrée `"Why a structured array?"`** (success criterion 14.5). Quatre points (cf. RESEARCH §2 fin) :
     1. ML-interop NumPy-first — Kala consomme via indexation positionnelle (axis (13,) garanti par D-08).
     2. Batchabilité — `np.empty(S, dtype=CHART_DTYPE)` vs S dataclasses Python ; `np.save`/`np.load`-friendly.
     3. Self-describing — chaque chart porte son `(jd, lat, lon, system)` (D-04) ; synastry/composite/return n'ont pas à transporter le contexte séparément.
     4. Inline houses (D-03) — pas de nesting, valeurs scalaires/subarrays courts.

2. `from __future__ import annotations` puis `import numpy as np`.

3. **`CHART_DTYPE`** avec annotation explicite `np.dtype` et commentaires `#:` au-dessus (gabarit `houses/core.py:20-34`) :

```python
#: Structured dtype for a fully-resolved natal chart.
#:
#: Fields (14 total, ordre logique : metadata → bodies → houses → aspects):
#:     - ``jd`` (f8): Julian Date, UT.
#:     - ``lat`` (f8): Geographic latitude, degrees.
#:     - ``lon`` (f8): Geographic longitude (east-positive), degrees.
#:     - ``system`` (U10): House system requested (e.g. "placidus").
#:     - ``body_lons`` (f8, (13,)): Ecliptic longitudes per body, degrees [0, 360).
#:     - ``body_lats`` (f8, (13,)): Ecliptic latitudes per body, degrees.
#:     - ``body_speeds`` (f8, (13,)): Longitude speeds per body, deg/day. Negative => retrograde.
#:     - ``cusps`` (f8, (12,)): 12 house cusps, degrees [0, 360).
#:     - ``asc`` (f8): Ascendant, degrees [0, 360).
#:     - ``mc`` (f8): Medium Coeli, degrees [0, 360).
#:     - ``armc`` (f8): Right Ascension of MC, degrees [0, 360).
#:     - ``vertex`` (f8): Vertex, degrees [0, 360).
#:     - ``aspect_matrix`` (i1, (13, 13)): canonical aspect index in [0, 13];
#:           ``-1`` means "no aspect"; symmetric (matrix[i,j]==matrix[j,i]);
#:           diagonal == ``-1`` (a body has no aspect with itself).
#:     - ``aspect_orbs`` (f4, (13, 13)): orb in degrees; ``NaN`` means
#:           "no orb" (matches aspect_matrix == -1); symmetric; diagonal == ``NaN``.
#:
#: Caller mask one-liner :
#:     ``mask = chart["aspect_matrix"] >= 0``  # or ``~np.isnan(chart["aspect_orbs"])``
#:
#: Body axis order (axis (13,)) follows ``ketu.core.bodies`` :
#:     0=Sun, 1=Moon, 2=Mercury, 3=Venus, 4=Mars, 5=Jupiter, 6=Saturn,
#:     7=Uranus, 8=Neptune, 9=Pluto, 10=Rahu, 11=Ketu, 12=Lilith.
#:
#: This axis is FROZEN by D-08 (Kala positional contract). Adding bodies
#: (e.g. Chiron) is a v1.3 BREAKING change.
CHART_DTYPE: np.dtype = np.dtype([
    ("jd",            "f8"),
    ("lat",           "f8"),
    ("lon",           "f8"),
    ("system",        "U10"),
    ("body_lons",     "f8", (13,)),
    ("body_lats",     "f8", (13,)),
    ("body_speeds",   "f8", (13,)),
    ("cusps",         "f8", (12,)),
    ("asc",           "f8"),
    ("mc",            "f8"),
    ("armc",          "f8"),
    ("vertex",        "f8"),
    ("aspect_matrix", "i1", (13, 13)),
    ("aspect_orbs",   "f4", (13, 13)),
])
```

4. **PAS de `HighLatitudeError`** dans `ketu/charts/core.py` — l'erreur polaire remonte via `calculate_houses` (D-11 pass-through). Re-importer depuis `ketu.houses` UNIQUEMENT si on l'expose dans `__all__` du subpackage ; sinon laisser propager (recommandation PATTERNS §2 Skip).

5. **PAS de `@dataclass class Chart:`** — anti-pattern PATTERNS §8.1, dette `cycles/calculator.py`. `core.py` contient UNIQUEMENT `CHART_DTYPE`.

### Task 3 — Écrire `ketu/charts/api.py` (stub)

Crée le fichier avec :
- Module docstring (« Public API for the charts subpackage : compute_chart and is_day_chart. »).
- `from __future__ import annotations` + imports nécessaires (Literal, Union, numpy, AspectSetSpec depuis `ketu.aspects.presets`).
- Type alias `ArrayLike = Union[float, np.ndarray]` (mirror `houses/api.py:25`).
- Signatures des deux fonctions publiques avec docstrings numpydoc COMPLETS (sections Parameters, Returns, Raises, Notes, Examples — tous obligatoires car fonctions exportées) :

```python
def compute_chart(
    jd: ArrayLike,
    lat: ArrayLike,
    lon: ArrayLike,
    system: str = "placidus",
    aspects: AspectSetSpec = None,
    polar_fallback: Literal["raise", "porphyry"] = "raise",
) -> np.ndarray:
    """Compute a fully-resolved natal chart in one vectorisable call.

    [docstring complet — Parameters / Returns / Raises / Notes / Examples
     scalaire + vectorisé ; cf. RESEARCH §3 et PATTERNS §3.1 pour les
     sections obligatoires. Précise UTC-only sur jd. Précise que
     polar_fallback est passé tel quel à calculate_houses. Stub body
     raise NotImplementedError("compute_chart implemented in 14-02 + 14-03").]
    """
    raise NotImplementedError(
        "compute_chart will be wired in plan 14-02 (positions+houses) "
        "and plan 14-03 (aspect_matrix)."
    )


def is_day_chart(
    jd: ArrayLike,
    lat: ArrayLike,
    lon: ArrayLike,
) -> np.ndarray:
    """Return True when the Sun is at or above the horizon (sunrise inclusive).

    [docstring complet — cf. PATTERNS §3.2 et RESEARCH §4. Précise D-13
     sunrise-inclusive et D-15 polar safety via Porphyry interne. Stub
     raise NotImplementedError("implemented in plan 14-04").]
    """
    raise NotImplementedError("is_day_chart will be wired in plan 14-04.")
```

**IMPORTANT — docstrings COMPLETS dès le stub** : c'est la seule façon de garder `numpydoc validate` vert et `interrogate ≥95%` vert pendant que les Plans 14-02/03/04 sont en cours. Les Plans suivants éditeront le corps de la fonction sans toucher aux docstrings (ou les enrichiront). Pas de placeholder « [TODO docstring] ».

**Type alias et imports** : ne PAS redéfinir `AspectSetSpec` localement ; importer `from ketu.aspects.presets import AspectSetSpec` (PATTERNS §8.6, D-10).

### Task 4 — Écrire `ketu/charts/__init__.py`

Crée le fichier en mirorant `ketu/houses/__init__.py:1-53` :

```python
"""Chart abstraction subpackage — fully-resolved natal charts in one call.

Public API surface (CHART-01 of v1.2 milestone):

- :func:`compute_chart` — Compute a fully-resolved natal chart (positions,
  houses, aspects) in one vectorisable call.
- :func:`is_day_chart` — Sect helper ; True if the Sun is at or above the
  horizon (sunrise inclusive), polar-safe via internal Porphyry fallback.
- :data:`CHART_DTYPE` — Structured array layout for chart results.

Examples
--------
>>> from ketu.charts import compute_chart, is_day_chart, CHART_DTYPE
>>> import numpy as np
>>> jd = np.array([2451545.0, 2470204.0])
>>> lat = np.array([48.86, 64.15])
>>> lon = np.array([2.35, -21.94])
>>> chart = compute_chart(jd, lat, lon)
>>> chart.shape, chart["body_lons"].shape, chart["aspect_matrix"].shape
((2,), (2, 13), (2, 13, 13))

See Also
--------
ketu.houses.calculate_houses : House cusps used internally by ``compute_chart``.
ketu.aspects.calculate_aspects_vectorized : Aspect engine projected into
    ``aspect_matrix``.

Notes
-----
``ketu.charts`` is composition only ; it does not introduce new math.
The ``CHART_DTYPE`` body axis (13,) is frozen (D-08) — adding Chiron
is a v1.3 BREAKING change. The ``polar_fallback`` parameter is a
pass-through to :func:`ketu.houses.calculate_houses` (D-11).
"""
from __future__ import annotations

from .api import compute_chart, is_day_chart
from .core import CHART_DTYPE

__all__ = [
    "CHART_DTYPE",
    "compute_chart",
    "is_day_chart",
]
```

**PAS de bloc `from . import placidus  # noqa: F401`** — pas de registry dans `ketu/charts/` (PATTERNS §1 « Skip », pitfall RESEARCH §Pitfall 2).

**PAS d'édition de `ketu/__init__.py` top-level** — callers passent par `from ketu.charts import …` (CONTEXT § Integration Points ligne 129).

### Task 5 — Écrire `tests/charts/__init__.py` (vide)

Marker pytest. Une seule ligne possible : `"""Test package for ketu.charts."""`.

### Task 6 — Écrire `tests/charts/test_dtype.py` — tests structurels

Mirror `tests/houses/test_dtype.py` adapté à CHART_DTYPE. Couvre :

1. **`test_public_imports_resolve`** — `from ketu.charts import CHART_DTYPE, compute_chart, is_day_chart` + assertion que chacun est non-None et du bon type (`np.dtype`, callable, callable). Pinne CHART-01.

2. **`test_dtype_has_expected_field_names`** — `CHART_DTYPE.names == ("jd", "lat", "lon", "system", "body_lons", "body_lats", "body_speeds", "cusps", "asc", "mc", "armc", "vertex", "aspect_matrix", "aspect_orbs")`. Tuple strict, ordre exact.

3. **`test_dtype_subarray_shapes`** — paramétrisé sur `[("body_lons", (13,)), ("body_lats", (13,)), ("body_speeds", (13,)), ("cusps", (12,)), ("aspect_matrix", (13, 13)), ("aspect_orbs", (13, 13))]`. Pour chaque field : `CHART_DTYPE.fields[name][0].shape == expected`.

4. **`test_dtype_scalar_field_kinds`** — paramétrisé sur les types attendus :
   - `f8` : jd, lat, lon, asc, mc, armc, vertex, body_lons, body_lats, body_speeds, cusps.
   - `U10` : system.
   - `i1` : aspect_matrix.
   - `f4` : aspect_orbs.

5. **`test_dtype_supports_vectorized_construction`** — `arr = np.zeros(5, dtype=CHART_DTYPE)` ; assert `arr.shape == (5,)`, `arr["body_lons"].shape == (5, 13)`, `arr["aspect_matrix"].shape == (5, 13, 13)`. Round-trip d'assignment.

6. **`test_dtype_scalar_zero_dim_construction`** — `arr = np.zeros((), dtype=CHART_DTYPE)`, vérifier que `arr["body_lons"].shape == (13,)`, `arr["aspect_matrix"].shape == (13, 13)`.

7. **`test_dtype_string_field_capacity`** — `arr = np.zeros(1, dtype=CHART_DTYPE)` ; `arr["system"] = "placidus"` puis `arr["system"] = "regiomontanus"` (12 chars > U10 → tronqué à 10 chars `"regiomonta"` ; documenter ce comportement attendu — c'est aligné avec houses).

8. **`test_dtype_aspect_matrix_accepts_negative_one_sentinel`** — `arr = np.full((3, 13, 13), -1, dtype=np.int8)` puis `chart_arr["aspect_matrix"] = arr` ; vérifier round-trip. Pinne D-06.

9. **`test_dtype_aspect_orbs_accepts_nan_sentinel`** — `arr = np.full((3, 13, 13), np.nan, dtype=np.float32)` ; round-trip + `np.isnan(chart_arr["aspect_orbs"]).all()`. Pinne D-06.

10. **`test_dtype_module_docstring_mentions_why_structured_array`** — `import ketu.charts.core ; assert "Why a structured array" in ketu.charts.core.__doc__`. Ratchet pour success criterion 14.5.

11. **`test_dtype_no_dataclass_chart_in_core`** — `import inspect, ketu.charts.core ; assert not any(inspect.isclass(o) and not issubclass(o, BaseException) for _, o in inspect.getmembers(ketu.charts.core))` (ou variante : assert qu'il n'y a pas d'attribut nommé `Chart`). Ratchet anti-pattern PATTERNS §8.1.

12. **`test_no_runtime_swisseph_import`** — assertion stricte que `import ketu.charts` n'introduit AUCUN nom commençant par `swe_` ou `swisseph` dans `sys.modules` triggered par `ketu.charts.*`. Pattern à mirorer depuis `tests/houses/test_integration.py:214-237`. Ratchet AGPL boundary (PATTERNS §8.5).

### Task 7 — Stubs de tests pour les fonctions encore non-implémentées

Ajoute dans `tests/charts/test_dtype.py` (ou un `tests/charts/test_stubs.py` dédié — discrétion) deux tests `pytest.raises(NotImplementedError)` qui appellent `compute_chart(2451545.0, 48.86, 2.35)` et `is_day_chart(2451545.0, 48.86, 2.35)` et confirment qu'ils lèvent `NotImplementedError`. Ces tests seront **supprimés** par les Plans 14-02 et 14-04 quand l'implémentation arrivera.

Marquer chaque test avec un commentaire `# Stub test — to be removed in plan 14-0X`.

## Verification gates

Toutes ces commandes DOIVENT passer (exit 0) avant commit :

```bash
cd /home/loc/workspace/ketu

# 1. Tests dtype passent
python -m pytest tests/charts/ -v -x

# 2. interrogate ≥95% sur ketu/ (incl. nouveau ketu/charts/)
python -m interrogate ketu/

# 3. numpydoc validate clean sur ketu/charts/
python -m numpydoc lint ketu/charts/__init__.py ketu/charts/core.py ketu/charts/api.py

# 4. mypy --strict clean sur ketu/charts/
python -m mypy --strict ketu/charts/

# 5. Suite complète n'a rien cassé (le restant du repo doit être stable)
python -m pytest tests/ --no-cov -x

# 6. Vérifier que pyproject.toml liste bien ketu.charts
python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'ketu.charts' in d['tool']['setuptools']['packages'], 'ketu.charts missing from packages list'"

# 7. Smoke import depuis un fresh subprocess
python -c "from ketu.charts import CHART_DTYPE, compute_chart, is_day_chart; import numpy as np; assert CHART_DTYPE.names[:4] == ('jd','lat','lon','system'); print('OK')"
```

## Done criteria

- [ ] CHART-01 satisfait : `from ketu.charts import compute_chart, is_day_chart, CHART_DTYPE` résout depuis un fresh Python ; subpackage présent dans `pyproject.toml` packages list.
- [ ] CHART-02 satisfait (structure) : `CHART_DTYPE` a 14 champs nommés dans l'ordre canonique, subarray shapes (13,), (12,), (13,13) corrects, sentinelles documentées dans le docstring.
- [ ] Success criterion 14.5 satisfait : module docstring de `ketu/charts/core.py` contient une section nommée « Why a structured array » avec les 4 points de motivation.
- [ ] `interrogate ketu/` retourne ≥95 % (tous les nouveaux modules + fonctions stubbed sont docstring-documentés).
- [ ] `numpydoc lint` retourne clean sur les 3 fichiers `ketu/charts/*.py`.
- [ ] `mypy --strict ketu/charts/` retourne clean (sans carve-out dans `pyproject.toml`).
- [ ] `pytest tests/charts/test_dtype.py -v` : 12+ tests verts (les 2 stubs `NotImplementedError` inclus).
- [ ] `pytest tests/` global suite reste verte (régression check).

## Atomic commit message

```
feat(14-01): scaffold ketu.charts subpackage with CHART_DTYPE

- Add ketu/charts/{__init__,core,api}.py with public API stubs
- Define CHART_DTYPE (14 fields: metadata + body subarrays + houses inline +
  aspect matrix + orbs) per D-01..D-06
- Module docstring includes "Why a structured array" section (success
  criterion 14.5)
- Aspect-matrix sentinels documented: -1 (no aspect) / NaN (no orb)
- Body axis (13,) frozen per D-08 (Kala positional contract)
- Add ketu.charts to pyproject.toml packages list (explicit, not auto-discover)
- tests/charts/test_dtype.py: 12 structural tests (field names, shapes,
  sentinels, AGPL boundary, no dataclass anti-pattern, NotImplementedError stubs)
- compute_chart and is_day_chart raise NotImplementedError; bodies wired
  in plans 14-02/03/04

Covers: CHART-01, CHART-02 (structure)
Refs: D-01, D-02, D-03, D-04, D-05, D-06, D-08, success criterion 14.5
```

## Rollback note

`git revert <commit_sha>` annule complètement ce plan : suppression de `ketu/charts/`, `tests/charts/`, retour de `pyproject.toml` à son état précédent. Aucun import existant ne dépend encore de `ketu.charts` (Plans 14-02..05 sont les premiers consommateurs), donc le revert est sans effet de bord.
