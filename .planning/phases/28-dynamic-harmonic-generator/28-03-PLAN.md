---
phase: 28-dynamic-harmonic-generator
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - ketu/synastry/api.py
  - ketu/cycles/calculator.py
  - tests/test_dynamic_synastry_cycles.py
autonomous: true

must_haves:
  truths:
    - "calculate_synastry(chart_a, chart_b, dynamic_specs=specs) returns dynamic rows in SYNASTRY_DTYPE with aspect_type = -2"
    - "Synastry dynamic orb is (_BODY_ORBS_16[i] + _BODY_ORBS_16[j]) / 2 * dyn_coef * factor — same formula as static, dyn coef from spec"
    - "Synastry static-then-dynamic, first-aspect-wins preserved via the existing matched mask (no pair double-counted)"
    - "generate_cycle_series(..., dynamic_specs=specs) includes dynamic aspect angles in nearest_aspect / in_aspect / aspect_orb detection"
    - "generate_cycle_series(..., dynamic_specs=None) is byte-identical to today (purely additive change)"
    - "Both consumers reuse generate_harmonic_aspects output directly; neither consults core.aspects table for the dynamic angle/coef"
  artifacts:
    - path: "ketu/synastry/api.py"
      provides: "dynamic_specs threading into calculate_synastry with aspect_type=-2 dynamic rows"
      contains: "dynamic_specs"
    - path: "ketu/cycles/calculator.py"
      provides: "dynamic_specs threading into generate_cycle_series (extends MAJOR_ASPECTS/COEFFS at call time)"
      contains: "dynamic_specs"
    - path: "tests/test_dynamic_synastry_cycles.py"
      provides: "Tests for synastry dynamic rows + cycles dynamic detection + None-path invariance"
      contains: "calculate_synastry"
  key_links:
    - from: "ketu/synastry/api.py"
      to: "dynamic_specs rows"
      via: "second loop over dyn rows after static loop, orb via _BODY_ORBS_16 * dyn_coef * factor, aspect_type=-2"
      pattern: "dynamic_specs"
    - from: "ketu/cycles/calculator.py generate_cycle_series"
      to: "extended aspect candidate set"
      via: "concatenate dyn angles onto MAJOR_ASPECTS and dyn coefs onto COEFFS for this call"
      pattern: "dynamic_specs"
---

<objective>
Wire `dynamic_specs` into the two cross-surface consumers that use their OWN detection
mechanisms (NOT `calculate_aspects`): `calculate_synastry` (per-pair vectorized orb
mask over the 256 ordered pairs) and `generate_cycle_series` (complex-number nearest-
aspect proximity). Both must reuse the generator's `(angle, coef)` rows directly so
dynamic aspects flow through synastry and cycles per ROADMAP success criterion #3.

Purpose: A user passing `dynamic_specs` to synastry gets dynamic rows in
`SYNASTRY_DTYPE` (orb from `_BODY_ORBS_16` × dynamic coef), and a cycle series
generated with dynamic specs includes dynamic aspect detections — without either path
ever assuming the 14-row `core.aspects` table for a dynamic aspect.

Output: `ketu/synastry/api.py` and `ketu/cycles/calculator.py` each accepting
`dynamic_specs=None`; new tests written to its OWN file `tests/test_dynamic_synastry_cycles.py`
(separate from plan 28-02's `tests/test_dynamic_harmonics.py` to avoid a parallel write
conflict in Wave 2 — see NOTE below).

Covers requirement: ASP-07 (dynamic aspects flow through cycles and synastry; orb from
`core.bodies['orb']` / `_BODY_ORBS_16` × dynamic coef; no path assumes the 14-row table
for a dynamic aspect).

NOTE: This plan shares NO file with plan 28-02. Plan 28-02 edits `ketu/aspects/calculator.py`
and appends tests to `tests/test_dynamic_harmonics.py`; this plan edits
`ketu/synastry/api.py` + `ketu/cycles/calculator.py` and writes its tests to a SEPARATE
file `tests/test_dynamic_synastry_cycles.py`. With zero file overlap, both Wave-2 plans
run concurrently with no race on any source or test file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-dynamic-harmonic-generator/28-CONTEXT.md
@.planning/phases/28-dynamic-harmonic-generator/28-RESEARCH.md

# Files you edit — read fully; the static detection patterns to extend are referenced by line:
@ketu/synastry/api.py
@ketu/cycles/calculator.py

# The generator landed by plan 28-01 (import DynamicAspectSpec; consume its rows):
@ketu/aspects/harmonics.py

# Synastry orb source (SYNASTRY_FACTOR, _BODY_ORBS_16) and dtype (aspect_type is i1):
@ketu/synastry/orbs.py
@ketu/synastry/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: dynamic_specs in calculate_synastry (aspect_type = -2 dynamic rows)</name>
  <files>ketu/synastry/api.py, tests/test_dynamic_synastry_cycles.py</files>
  <action>
Import the type alias: `from ketu.aspects.harmonics import DynamicAspectSpec`. Add
`dynamic_specs: DynamicAspectSpec = None` as the LAST parameter of `calculate_synastry`
(after `mode`). Normalize a list-or-array spec to one array at entry (None when empty).

Extend the detection AFTER the existing static `for i_asp in selected_indices:` loop
(lines 286-329). When `dynamic_specs is not None`, run a SECOND loop over the dynamic
rows, mirroring the static body of lines 286-329 EXACTLY but with dynamic angle/coef:
- `dyn_ang = float(dyn_row["angle"])`, `dyn_coef = float(dyn_row["coef"])`.
- `orbs_pair = (_BODY_ORBS_16[i_flat] + _BODY_ORBS_16[j_flat]) / 2.0 * dyn_coef * factor`
  (identical formula to line 294-298, dyn_coef substituted — ASP-07 orb rule).
- The generator never emits 0°, so NO conjunction special-case: always use the
  non-conjunction branch — `in_orb = (np.abs(dist - dyn_ang) <= orbs_pair) & (~matched)`,
  `delta = dyn_ang - dist`.
- `if not np.any(in_orb): continue`.
- `rel_speed = speed_a - speed_b; applying = (np.sign(delta) * rel_speed) > 0`.
- Write into `out`: `out["aspect_type"][in_orb] = -2` (i1 sentinel; -2 fits int8 —
  Pitfall 2), `out["orb"][in_orb] = delta[in_orb].astype(np.float32)`,
  `out["applying"][in_orb] = applying[in_orb]`,
  `out["orb_limit"][in_orb] = orbs_pair[in_orb].astype(np.float32)`,
  `matched |= in_orb`.
This keeps static-first / dynamic-second and first-aspect-wins via the shared `matched`
mask. The mode dispatch at lines 331-338 is unchanged — filtered mode returns rows with
`aspect_type >= 0` AND the dynamic rows (since -2 < 0, ADJUST the filtered predicate:
filtered mode must now keep rows where `aspect_type >= 0 OR aspect_type == -2`, i.e.
`aspect_type != -1`). Update the filtered-mode line to `out[out["aspect_type"] != -1]`
so dynamic rows (-2) are NOT dropped while non-aspected (-1) rows still are. Verify
dense mode still returns all 256 rows.

Update the numpydoc: document `dynamic_specs`, the `aspect_type = -2` dynamic sentinel
(distinct from `-1` = no-aspect in dense mode), and that filtered mode now keeps
`aspect_type != -1`.

**Tests** (write to the NEW file tests/test_dynamic_synastry_cycles.py — create it in this
plan; do NOT touch tests/test_dynamic_harmonics.py, which plan 28-02 owns in Wave 2. 100%
coverage of new branches):
- `test_synastry_dynamic_specs_change_filtered_set` (STRUCTURAL — deterministic, no
  reliance on a chance H7 occurrence): build two charts via `ketu.charts.compute_chart`,
  call `calculate_synastry(a, b, dynamic_specs=None, mode="filtered")` and
  `calculate_synastry(a, b, dynamic_specs=generate_harmonic_aspects(7), mode="filtered")`.
  Assert both return SYNASTRY_DTYPE. The dynamic call's filtered set is a SUPERSET of the
  None call's set (static rows are always present; -2 rows can only be added), so assert
  `len(dyn_filtered) >= len(none_filtered)` and that every `aspect_type == -2` row carries
  `orb_limit == (_BODY_ORBS_16[i]+_BODY_ORBS_16[j])/2*coef*factor` within tolerance for ITS
  body pair (validates the orb formula on whatever -2 rows exist, zero or more — does NOT
  require any to exist on this specific date pair).
- `test_synastry_dynamic_rows_appear_on_grid` (EXISTENCE — guarantees a hit without a
  hard-coded epoch): scan a small grid of chart dates (e.g. `for jd_b in np.linspace(
  2451545.0, 2451545.0 + 365.0, 24)`: build chart `b` at `jd_b`, keep `a` fixed), call
  `calculate_synastry(a, b, dynamic_specs=generate_harmonic_aspects(7))`, and accumulate
  the count of `aspect_type == -2` rows across the grid. Assert the AGGREGATE count > 0
  (across ~1 year of partner dates, at least one of the 256 body-pairs lands within an H7
  orb on at least one date — guaranteed, not chance-dependent on one epoch). For the first
  date that yields a -2 row, assert that row's `orb_limit` matches the expected formula.
- `test_synastry_dense_still_256`: `mode="dense"` returns exactly 256 rows with
  dynamic specs present.
- `test_synastry_filtered_keeps_dynamic`: filtered mode includes -2 rows and excludes
  -1 rows.
- `test_synastry_none_unchanged`: `dynamic_specs=None` returns the same result as the
  current (no-arg) call — pin via array_equal on aspect_type/orb for a fixed chart pair.
  </action>
  <verify>
source venv/bin/activate
python - <<'PY'
import numpy as np
from ketu.charts import compute_chart
from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE
from ketu.aspects.harmonics import generate_harmonic_aspects
a = compute_chart(2451545.0, 48.86, 2.35)
specs = generate_harmonic_aspects(7)
# STRUCTURAL invariants on a single fixed pair (no reliance on a chance H7 hit):
b0 = compute_chart(2451900.0, 40.71, -74.01)
r0 = calculate_synastry(a, b0, dynamic_specs=specs)
assert r0.dtype == SYNASTRY_DTYPE
assert len(calculate_synastry(a, b0, dynamic_specs=specs, mode="dense")) == 256
# filtered superset: dynamic can only ADD -2 rows, never drop static rows
f_none = calculate_synastry(a, b0, dynamic_specs=None, mode="filtered")
f_dyn  = calculate_synastry(a, b0, dynamic_specs=specs, mode="filtered")
assert len(f_dyn) >= len(f_none), (len(f_dyn), len(f_none))
# None path byte-identical
base = calculate_synastry(a, b0)
assert np.array_equal(base["aspect_type"], calculate_synastry(a, b0, dynamic_specs=None)["aspect_type"])
# EXISTENCE via a date grid (guaranteed hit, not a single chance epoch):
total_dyn = 0
for jd_b in np.linspace(2451545.0, 2451545.0 + 365.0, 24):
    b = compute_chart(float(jd_b), 40.71, -74.01)
    r = calculate_synastry(a, b, dynamic_specs=specs)
    total_dyn += int((r["aspect_type"] == -2).sum())
assert total_dyn > 0, "no dynamic synastry rows across a 1-year grid!"
print("synastry dynamic OK; dyn rows over grid:", total_dyn)
PY
pytest tests/test_dynamic_synastry_cycles.py -k synastry -v
make doctest 2>&1 | tail -5
  </verify>
  <done>
`calculate_synastry(..., dynamic_specs=specs)` returns `SYNASTRY_DTYPE` rows with
`aspect_type = -2`, orb_limit from `_BODY_ORBS_16 × dyn_coef × factor`. Dense mode still
256 rows; filtered mode keeps -2 and drops -1. `dynamic_specs=None` is unchanged. New
synastry tests pass; `make doctest` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: dynamic_specs in generate_cycle_series (extend MAJOR_ASPECTS / COEFFS at call time)</name>
  <files>ketu/cycles/calculator.py, tests/test_dynamic_synastry_cycles.py</files>
  <action>
Import the type alias: `from ketu.aspects.harmonics import DynamicAspectSpec`. Add
`dynamic_specs: DynamicAspectSpec = None` as the LAST parameter of
`generate_cycle_series` (after `cache`). Also thread it through
`generate_multi_cycle_series` (add the same optional param and forward it on the
`generate_cycle_series(...)` call at line 380) so multi-pair callers can use it.

The cycles aspect proximity (lines 280-318) uses a complex-number nearest-aspect
mechanism over `MAJOR_ASPECTS = [0,60,90,120,180,240,270,300,360]` with a parallel
`COEFFS` array — NOT the `core.aspects` table. To integrate dynamic aspects ADDITIVELY:

- Inside the `if include_aspects:` block, BEFORE computing `dist_matrix_rad` (line 285),
  build the EFFECTIVE aspect set:
    - When `dynamic_specs is None`: `effective_aspects = MAJOR_ASPECTS`,
      `effective_aspects_z = MAJOR_ASPECTS_Z`, `effective_coeffs = COEFFS` — IDENTICAL
      computation to today (byte-for-byte; Pitfall 7).
    - When `dynamic_specs is not None`: normalize specs to one array; build full-circle
      dynamic angles. IMPORTANT: the generator emits folded angles in [0,180]; the
      cycles candidate set spans the FULL circle (it lists 240/270/300 as the waning
      mirrors). To keep cycles semantics, for each dynamic angle `θ` in `[0,180]` also
      add its waning mirror `360 - θ` (when `θ` is not 0/180) with the SAME coef, so a
      separation on either side of the cycle is detectable. Concatenate:
        `effective_aspects = np.concatenate([MAJOR_ASPECTS, dyn_angles_fullcircle]).astype(np.float32)`,
        `effective_coeffs  = np.concatenate([COEFFS, dyn_coeffs_fullcircle]).astype(np.float32)`,
        `effective_aspects_z = degrees_to_complex(effective_aspects)`.
- Replace the three uses of `MAJOR_ASPECTS*` in lines 285-311 with the `effective_*`
  variables: `dist_matrix_rad = np.angle(z_ratios[:, None] / effective_aspects_z[None, :])`;
  `nearest_aspects = effective_aspects[nearest_indices]`;
  `flat_indices = nearest_indices + np.arange(n_samples) * len(effective_aspects)`;
  `orb_coeffs = effective_coeffs[nearest_indices]`.
  Keep the `np.where(nearest_aspects == 360, 0, nearest_aspects)` normalization on the
  result['nearest_aspect'] write (folds the 360 candidate back to 0).
- The `dynamic_specs is None` branch MUST be unchanged from current behaviour (existing
  cycle tests and the generate_cycle_series doctest must still pass).

Update the numpydoc for `dynamic_specs` on both functions (BLOCKING gate). Keep the
existing doctest intact; if you add a dynamic doctest, use `round()` and assert on
structural facts (dtype names / shape), not fragile float separations.

**Tests** (append to the NEW file tests/test_dynamic_synastry_cycles.py created in Task 1,
100% coverage of new branches):
- `test_cycles_dynamic_detects_h7`: generate a Sun/Moon (or Sun/Mars) series over a
  window known to cross an H7 angle; call with `dynamic_specs=generate_harmonic_aspects(7)`;
  assert at least one row has `nearest_aspect` equal to one of the H7 angles (or its
  mirror) AND `in_aspect == True` for that row. (Widen the window / pick a fast pair to
  guarantee a hit; assert membership in the H7 angle set with tolerance, not an exact jd.)
- `test_cycles_none_path_unchanged`: a fixed pair+window with `dynamic_specs=None`
  returns an array `array_equal` (on nearest_aspect, in_aspect, aspect_orb) to the
  current no-arg call — proves additivity.
- `test_cycles_dynamic_extends_candidate_set`: assert that with dynamic specs present
  the set of distinct `nearest_aspect` values can include an H7 angle that is NOT in
  MAJOR_ASPECTS (proving the candidate set was extended).
- `test_multi_cycle_dynamic_forwarded`: `generate_multi_cycle_series([...], ts,
  dynamic_specs=specs)` forwards the param (smoke: result dict built; at least one pair
  shows a dynamic nearest_aspect).
  </action>
  <verify>
source venv/bin/activate
python - <<'PY'
import numpy as np
from datetime import datetime, timedelta
from ketu.cycles import generate_cycle_series, MAJOR_ASPECTS
from ketu.aspects.harmonics import generate_harmonic_aspects
ts = [datetime(2025,1,1)+timedelta(days=i) for i in range(60)]
specs = generate_harmonic_aspects(7)
dyn = generate_cycle_series("Sun","Moon", ts, dynamic_specs=specs)
base = generate_cycle_series("Sun","Moon", ts)
# None path identical
b2 = generate_cycle_series("Sun","Moon", ts, dynamic_specs=None)
assert np.array_equal(base["nearest_aspect"], b2["nearest_aspect"]), "None path drifted!"
# dynamic candidate set extended: some nearest_aspect not in MAJOR_ASPECTS
extra = set(np.round(dyn["nearest_aspect"],1).tolist()) - set(MAJOR_ASPECTS.tolist())
print("dynamic-only nearest_aspect values:", sorted(extra))
assert extra, "candidate set was not extended with dynamic angles"
PY
pytest tests/test_dynamic_synastry_cycles.py -k cycle -v
pytest tests/ -q   # full suite + 100% coverage
make doctest 2>&1 | tail -5
  </verify>
  <done>
`generate_cycle_series(..., dynamic_specs=specs)` extends the nearest-aspect candidate
set with dynamic angles and detects them in `nearest_aspect` / `in_aspect` / `aspect_orb`;
`dynamic_specs=None` is byte-identical to today. `generate_multi_cycle_series` forwards
the param. New cycle tests pass; full suite green at 100% coverage; `make doctest` passes.
  </done>
</task>

</tasks>

<verification>
- `grep -n "dynamic_specs" ketu/synastry/api.py ketu/cycles/calculator.py` shows the
  param on calculate_synastry, generate_cycle_series, generate_multi_cycle_series.
- Synastry dynamic rows carry `aspect_type = -2`; filtered mode predicate is
  `aspect_type != -1` (keeps -2, drops -1); dense mode still 256 rows.
- Cycles `dynamic_specs=None` path is array-identical to the pre-change output.
- Neither file looks up `core.aspects` for the dynamic angle/coef (they consume the
  generator rows directly).
- `pytest tests/` green at 100% coverage; `make doctest` passes.
</verification>

<success_criteria>
ASP-07 satisfied: dynamic aspects flow through BOTH cycles and synastry. Synastry
returns `SYNASTRY_DTYPE` rows with `aspect_type = -2` and orb from
`_BODY_ORBS_16 × dynamic coef × factor`; cycles series generated with dynamic specs
include dynamic detections via the extended nearest-aspect candidate set. No code path
assumes the 14-row table for a dynamic aspect; the `dynamic_specs=None` paths are purely
additive (byte-identical to before).
</success_criteria>

<output>
After completion, create `.planning/phases/28-dynamic-harmonic-generator/28-03-SUMMARY.md`.
</output>
