---
phase: 22-ephemeris-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/ephemeris/planets.py
  - tests/test_planets_coverage.py
autonomous: true

must_haves:
  truths:
    - "Adding a new body to calc_planet_position requires registering one strategy entry, not adding an if-elif branch"
    - "Both scalar (calc_planet_position) and vectorized (calc_planet_position_batch) paths dispatch through the same per-body strategy table, so they cannot diverge"
    - "calc_planet_position_batch returns the correct Ketu value (matching the scalar path) instead of the wrong heliocentric value"
    - "Every existing ephemeris regression test passes unchanged (byte-stable for all bodies except batch-Ketu, which is corrected)"
  artifacts:
    - path: "ketu/ephemeris/planets.py"
      provides: "Per-body strategy registry (dict keyed by body name) feeding both scalar and batch dispatch; @lru_cache preserved on calc_planet_position; get_planet_name reduced to SWE_IDS lookup; calculate_all_positions iterates range(len(SWE_IDS))"
      contains: "BODY_STRATEGIES"
    - path: "tests/test_planets_coverage.py"
      provides: "New regression test asserting batch-Ketu == scalar-Ketu for body_id=11, plus a scalar-vs-batch agreement sweep over all 13 bodies"
      contains: "Ketu"
  key_links:
    - from: "calc_planet_position"
      to: "BODY_STRATEGIES[name].scalar"
      via: "dict lookup inside the @lru_cache wrapper"
      pattern: "BODY_STRATEGIES\\["
    - from: "calc_planet_position_batch"
      to: "BODY_STRATEGIES[name].vectorized"
      via: "dict lookup replacing the if-elif"
      pattern: "BODY_STRATEGIES\\["
---

<objective>
Extract the per-body if-elif dispatch in `ketu/ephemeris/planets.py` into a per-body strategy
registry that BOTH the scalar `calc_planet_position` and the vectorized
`calc_planet_position_batch` consume. This makes Phase 24 (Chiron) a one-line registry addition
instead of editing two parallel if-elif chains, and fixes a pre-existing batch-Ketu bug in the
process.

Purpose: Phase 22 exists solely to make the 14th body (Chiron, Phase 24) a clean addition.
A single strategy table shared by scalar + batch paths is the structure Phase 24 extends.

Output: `planets.py` with a `BODY_STRATEGIES` registry, a corrected batch-Ketu path, and a new
regression test pinning scalar/batch agreement (including Ketu).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-ephemeris-refactor/22-RESEARCH.md
@ketu/ephemeris/planets.py

# Strong numerical regression guard (must stay green after refactor):
@tests/test_vectorization.py
</context>

<constraints>
BINDING — read before writing code:
- PURE REFACTOR, byte-stable. The ONLY intended numerical change is the batch-Ketu correction
  (body_id=11 in batch mode). Every other body's scalar and batch output must be bit-for-bit
  identical to before. `tests/test_vectorization.py` (scalar-vs-vectorized < 1e-10 / < 1e-8) is
  the strongest guard and MUST stay green.
- PRESERVE `@lru_cache(maxsize=128)` on `calc_planet_position` (planets.py:69). Strategy callables
  are plain functions called from INSIDE the cached wrapper; do NOT decorate strategy callables.
- The post-dispatch aberration step stays in the routers, NOT inside strategies:
    - scalar (planets.py:191-195): `if planet_id >= 2: aberration_correction(...)` applied to lon/lat.
    - batch: aberration is ONLY applied in the regular-planet branch (planets.py:564-569), and only
      for `planet_id >= 2`. Sun/Moon/nodes/Lilith get NO aberration in batch mode. Preserve this
      EXACTLY — do not start applying aberration to nodes/Lilith in batch, or you break byte-stability.
- Do NOT touch `ketu/ephemeris/orbital.py` or `ketu/ephemeris/__init__.py` (those belong to Plan
  22-02; keeping files disjoint keeps the wave parallel-safe).
- Pure-NumPy runtime, no new runtime dependency. Type hints on every new function. UTC always.

PRE-EXISTING BUG (planets.py:530) — MUST FIX:
The batch fallback list `["Rahu", "NorthNode", "Lilith"]` omits `"Ketu"`. For body_id=11 (Ketu),
the batch path falls through to the `else` (regular-planet) branch and computes a heliocentric
position — WRONG. Verified numerically at JD 2451545.0: scalar Ketu lon = 305.118° (= Rahu+180,
correct), batch Ketu lon = 280.367° (wrong). No existing test asserts the wrong batch-Ketu value
(synastry/composite oracles exclude Ketu from expected body lists; only
tests/charts/test_compute_chart.py:138 compares chart-inline-Ketu vs batch-Ketu, and BOTH sides
use the same buggy batch path so they agree regardless). The strategy unification fixes this by
routing batch-Ketu through the same node-based strategy as scalar-Ketu. After the fix, batch-Ketu
will equal scalar-Ketu (305.118°) — which also shifts compute_chart's Ketu output; confirm the
suite stays green (it should, since no test pins the wrong value).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Build the per-body strategy registry and route the scalar path through it</name>
  <files>ketu/ephemeris/planets.py</files>
  <action>
Introduce a per-body strategy structure replacing the scalar if-elif (planets.py:93-189) and
prepare it to also serve the batch path in Task 2.

1. Add a lightweight strategy container near the top (after SWE_IDS, ~line 66). Use a
   `typing.NamedTuple` (no new runtime deps) with two fields:
     class _BodyCalc(NamedTuple):
         scalar: Callable[[float], tuple[float, float, float, float, float, float]]
         vectorized: Callable[[np.ndarray], tuple]  # returns 6 np.ndarrays
   Import `Callable, NamedTuple` from `typing`.

2. Extract one module-level scalar function per body kind, lifting the EXACT bodies of the current
   branches verbatim (no algebra changes — copy the math). Each returns the 6-tuple
   (lon, lat, dist, lon_speed, lat_speed, dist_speed) BEFORE aberration:
     - _sun_scalar(jd)    ← lines 93-108  (Earth-negation, jd_delta=0.01 finite diff)
     - _moon_scalar(jd)   ← lines 110-125 (get_moon_position + lon-wrap finite diff)
     - _rahu_scalar(jd)   ← lines 127-136 (get_lunar_nodes()[0]; speed -0.0529538083)
     - _ketu_scalar(jd)   ← lines 138-148 ((node+180)%360; speed -0.0529538083)
     - _lilith_scalar(jd) ← lines 150-160 (get_lilith_position; speed _LILITH_MEAN_RATE_DEG_PER_DAY)
   For regular planets use a factory to avoid 8 copies:
     def _make_planet_scalar(body_idx: int) -> Callable:
         def _scalar(jd: float):  ... body of lines 162-189 with body_idx bound ...
         return _scalar
   IMPORTANT (Pitfall 2): bind body_idx via the factory parameter (closure over the parameter is
   safe — it is a fresh binding per call); do NOT build closures in a loop capturing a loop variable.

3. Build the registry:
     BODY_STRATEGIES: dict[str, _BodyCalc] = {
         "Sun":    _BodyCalc(_sun_scalar,    _sun_vec),      # _*_vec stubs filled in Task 2
         "Moon":   _BodyCalc(_moon_scalar,   _moon_vec),
         "Rahu":   _BodyCalc(_rahu_scalar,   _rahu_vec),
         "Ketu":   _BodyCalc(_ketu_scalar,   _ketu_vec),
         "Lilith": _BodyCalc(_lilith_scalar, _lilith_vec),
         "Mercury": _BodyCalc(_make_planet_scalar(BODY_INDICES["Mercury"]), _make_planet_vec(BODY_INDICES["Mercury"])),
         "Venus":   ...  "Mars": ...  "Jupiter": ...  "Saturn": ...
         "Uranus":  ...  "Neptune": ...  "Pluto": ...
     }
   (You may define _*_vec in Task 2 and assemble the registry once both halves exist; keep ONE
   registry that both routers read.)

4. Rewrite `calc_planet_position` body (keep the `@lru_cache` decorator and signature untouched):
       planet_name = SWE_IDS.get(planet_id)
       if planet_name is None: raise ValueError(... unchanged message ...)
       lon, lat, dist, lon_speed, lat_speed, dist_speed = BODY_STRATEGIES[planet_name].scalar(jd)
       if planet_id >= 2:               # aberration stays in the router, unchanged
           dlon, dlat = aberration_correction(lon, lat, jd)
           lon += dlon; lat += dlat
       return np.array([lon, lat, dist, lon_speed, lat_speed, dist_speed])
   Keep the exact ValueError message: "unknown planet ID: {planet_id}. Valid range: 0-12".

5. Cheap Phase-24 enablers (do these now):
     - Replace `get_planet_name` hardcoded dict (lines 214-228) with:
         return SWE_IDS.get(planet_id, f"Unknown({planet_id})")
       (byte-identical behavior; SWE_IDS already has the same 13 entries).
     - In `calculate_all_positions`, change `for planet_id in range(13):` (line 248) to
       `for planet_id in range(len(SWE_IDS)):`.
  </action>
  <verify>
source venv/bin/activate && python -c "
import numpy as np
from ketu.ephemeris.planets import calc_planet_position, BODY_STRATEGIES, get_planet_name, calculate_all_positions
jd=2451545.0
# every body still computes; scalar path unchanged
for pid in range(13):
    assert calc_planet_position(jd, pid).shape==(6,)
assert get_planet_name(99)=='Unknown(99)'
assert len(calculate_all_positions(jd))==13
print('OK', sorted(BODY_STRATEGIES))
" && pytest tests/test_vectorization.py tests/test_ketu.py tests/test_planets_coverage.py -q
  </verify>
  <done>
`calc_planet_position` dispatches through `BODY_STRATEGIES[name].scalar`, `@lru_cache` retained,
aberration still applied post-dispatch for planet_id>=2, get_planet_name uses SWE_IDS,
calculate_all_positions uses range(len(SWE_IDS)); test_vectorization + test_ketu +
test_planets_coverage all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route the batch path through the registry and fix the Ketu batch bug</name>
  <files>ketu/ephemeris/planets.py</files>
  <action>
Extract the vectorized branch bodies (planets.py:484-577) into module-level functions and wire the
registry's `.vectorized` slot so `calc_planet_position_batch` dispatches through ONE table —
eliminating the scalar/batch divergence and the missing-Ketu bug.

1. Extract vectorized functions, lifting the EXACT branch bodies verbatim. Each returns the 6
   per-date arrays (lon, lat, dist, lon_speed, lat_speed, dist_speed) BEFORE the results-matrix
   assembly and BEFORE batch aberration:
     - _sun_vec(jd_array)    ← lines 484-499  (get_body_position_vectorized + Earth negation)
     - _moon_vec(jd_array)   ← lines 508-521  (get_moon_position_vectorized + np.where lon-wrap)
     - _make_planet_vec(body_idx) ← lines 535-562 (vectorized heliocentric→geocentric, finite diff)
   For the per-date bodies (Rahu, Ketu, Lilith) the current batch code loops and calls the scalar
   `calc_planet_position` (lines 530-533). Reproduce that EXACTLY as a helper so Rahu/Ketu/Lilith
   all share it:
     def _scalar_loop_vec(planet_id: int) -> Callable:
         def _vec(jd_array, _pid=planet_id):
             out = np.zeros((len(jd_array), 6))
             for i, jd in enumerate(jd_array):
                 out[i] = calc_planet_position(float(jd), _pid)
             return tuple(out[:, k] for k in range(6))
         return _vec
   Bind it for Rahu (pid 10), Ketu (pid 11), Lilith (pid 12). This is what fixes the bug: Ketu now
   takes the same correct scalar-loop path Rahu/Lilith already used, instead of falling through to
   the heliocentric regular-planet branch.
   NOTE on byte-stability: the previous code looped `results[i] = calc_planet_position(jd, planet_id, flags)`.
   `flags` is unused downstream (calc_planet_position ignores it functionally); passing the default
   is byte-identical. Keep passing `planet_id` through.

2. Rewrite `calc_planet_position_batch` body (signature unchanged):
       planet_name = SWE_IDS.get(planet_id)
       if planet_name is None: raise ValueError(... unchanged message ...)
       n_dates = len(jd_array)
       results = np.zeros((n_dates, 6))
       lon, lat, dist, lon_speed, lat_speed, dist_speed = BODY_STRATEGIES[planet_name].vectorized(jd_array)
       # Batch aberration: ONLY for regular planets (planet_id >= 2 AND not a per-date body).
       # Preserve current behavior EXACTLY — aberration was applied only in the old `else`
       # (regular-planet) branch, never for Sun/Moon/Rahu/Ketu/Lilith. The cleanest byte-stable
       # way: apply aberration inside _make_planet_vec's returned arrays is WRONG (it would double
       # with scalar). Instead keep aberration in the router but gate it so ONLY regular planets get
       # it: nodes/Lilith use the scalar-loop (which already applied aberration internally for
       # Lilith via calc_planet_position's planet_id>=2 path), and Sun/Moon must not get it.
       # SAFEST: do NOT apply aberration in the batch router at all for per-date bodies (scalar-loop
       # already did it), and apply it only for the regular-planet vec functions.
   Decide and document the cleanest equivalent. Recommended approach: have `_make_planet_vec` apply
   aberration itself (matching old lines 564-569 which lived in the regular-planet branch), since the
   scalar-loop bodies (Rahu/Ketu/Lilith) already get aberration via the inner calc_planet_position,
   and Sun/Moon vec functions must NOT apply it. Then the router just assembles results:
       results[:, 0]=lon; results[:, 1]=lat; results[:, 2]=dist
       results[:, 3]=lon_speed; results[:, 4]=lat_speed; results[:, 5]=dist_speed
       return results
   VERIFY this equivalence empirically (Task-3 test below) before declaring done — capture a
   before snapshot for all 13 bodies first (see verify), refactor, then assert byte-equality for
   ids 0-10 and 12, and assert the CORRECTED value for id 11.

3. Delete the now-dead if-elif in both routers. Confirm no `["Rahu", "NorthNode", "Lilith"]`
   literal remains anywhere in planets.py.
  </action>
  <verify>
source venv/bin/activate && python -c "
import numpy as np
from ketu.ephemeris.planets import calc_planet_position, calc_planet_position_batch
jds=np.array([2451545.0, 2455197.5, 2460000.0])
# scalar vs batch agreement for ALL 13 bodies (Ketu now correct too)
for pid in range(13):
    sc=np.array([calc_planet_position(float(j),pid) for j in jds])
    ba=calc_planet_position_batch(jds,pid)
    d=np.max(np.abs(sc[:, :3]-ba[:, :3]))
    assert d<1e-8, f'body {pid} scalar/batch disagree: {d}'
# explicit Ketu correctness: batch must equal scalar (= Rahu+180)
k_sc=calc_planet_position(2451545.0,11)[0]
k_ba=calc_planet_position_batch(np.array([2451545.0]),11)[0,0]
rahu=calc_planet_position(2451545.0,10)[0]
assert abs(k_ba-k_sc)<1e-9 and abs(k_ba-(rahu+180)%360)<1e-9, (k_ba,k_sc,rahu)
print('OK batch-Ketu fixed:', k_ba)
" && pytest tests/ -q
  </verify>
  <done>
`calc_planet_position_batch` dispatches through `BODY_STRATEGIES[name].vectorized`; scalar and batch
agree < 1e-8 for all 13 bodies; batch-Ketu equals scalar-Ketu (= Rahu+180); no
`["Rahu", "NorthNode", "Lilith"]` literal remains; full `pytest tests/` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests pinning scalar/batch agreement and the Ketu fix</name>
  <files>tests/test_planets_coverage.py</files>
  <action>
Add tests that (a) lock the previously-untested batch-Ketu path so it can never regress, and
(b) assert the strategy table keeps scalar and batch in lockstep for every body.

1. Add `test_batch_ketu_matches_scalar`: for several JDs (e.g. 2451545.0, 2455197.5, 2460000.0),
   assert `calc_planet_position_batch(np.array([jd]), 11)[0]` equals `calc_planet_position(jd, 11)`
   within 1e-9 on lon/lat/dist, AND that the longitude equals `(rahu_lon + 180) % 360` within 1e-9
   (Ketu = South Node). Include a comment citing the pre-existing bug (batch fallback list at the
   old planets.py:530 omitted "Ketu") so future readers know why this test exists.

2. Add `test_scalar_batch_agreement_all_bodies`: loop body_id in range(13), assert max abs diff on
   columns 0-2 (lon/lat/dist) between the scalar sweep and `calc_planet_position_batch` is < 1e-8.
   This pins the strategy table's scalar/vectorized twins against drift for every registered body —
   the guard Phase 24 will rely on when it adds Chiron.

3. Optionally add `test_body_strategies_cover_all_swe_ids`: assert
   `set(BODY_STRATEGIES) == set(SWE_IDS.values())` so an unregistered body fails loudly (helps
   Phase 24 catch a half-added Chiron).

Use the existing import style at the top of tests/test_planets_coverage.py (it already imports
calc_planet_position_batch). Add `BODY_STRATEGIES`/`SWE_IDS` imports as needed.
  </action>
  <verify>
source venv/bin/activate && pytest tests/test_planets_coverage.py -q -k "ketu or agreement or strategies" -v && pytest tests/ -q && pytest --cov=ketu --cov-report=term-missing --cov-fail-under=100 tests/ -q 2>&1 | tail -5
  </verify>
  <done>
New tests pass; full suite green; `--cov-fail-under=100` satisfied (the new Ketu vectorized path is
now exercised, closing the coverage that the old dead batch-Ketu path never had).
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` fully green.
- `pytest --cov=ketu --cov-fail-under=100 tests/` passes (100% coverage maintained).
- `tests/test_vectorization.py` passes unchanged (scalar-vs-vectorized < 1e-10 / < 1e-8) — proves
  no float drift for the 12 unchanged bodies.
- Byte-stability: scalar output for all 13 bodies unchanged; batch output unchanged for ids 0-10
  and 12; batch id 11 (Ketu) is CORRECTED to match scalar (intended, the only numerical change).
- `grep -n '\["Rahu", "NorthNode", "Lilith"\]' ketu/ephemeris/planets.py` returns nothing.
- `ketu/ephemeris/orbital.py` and `ketu/ephemeris/__init__.py` untouched (git diff shows only
  planets.py + tests/test_planets_coverage.py changed by this plan).
</verification>

<success_criteria>
- Success Criterion 1 met: the per-body if-elif is replaced by a strategy registry; registering a
  new body is a `BODY_STRATEGIES[...] = _BodyCalc(...)` entry, not a branch.
- REF-01 covers BOTH scalar and batch paths from one table; they cannot diverge.
- The Ketu batch bug is fixed with dedicated test coverage.
- get_planet_name → SWE_IDS lookup; calculate_all_positions → range(len(SWE_IDS)).
- @lru_cache preserved; ephemeris regression suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/22-ephemeris-refactor/22-01-SUMMARY.md`
</output>
