---
phase: 21-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_coverage_improvements.py
autonomous: true

must_haves:
  truths:
    - "Every gap line from the RESEARCH inventory is exercised by a real test (zero new pragmas)"
    - "_ecliptic.py ra_to_lambda/lambda_to_ra are tested via known astronomical values AND a ra->lambda->ra round-trip"
    - "house_of() has direct test coverage and the bad-polar_fallback ValueError branch is hit"
  artifacts:
    - path: "tests/test_coverage_improvements.py"
      provides: "Catch-all regression tests closing all non-orbital-guard coverage gaps"
      contains: "ra_to_lambda"
  key_links:
    - from: "tests/test_coverage_improvements.py"
      to: "ketu/houses/_ecliptic.py"
      via: "import + assert known values + round-trip"
      pattern: "ra_to_lambda|lambda_to_ra"
    - from: "tests/test_coverage_improvements.py"
      to: "ketu/houses/api.py"
      via: "house_of() direct call + invalid polar_fallback"
      pattern: "house_of"
---

<objective>
Close every coverage gap identified in 21-RESEARCH.md EXCEPT the orbital.py div/0
guard lines (those are owned by 21-02, which adds + covers them). Deliver QAL-10's
test work: targeted real tests, zero `# pragma: no cover`. The `_ecliptic.py` ~64%
outlier (RA<->lambda converters) gets known-value asserts plus a round-trip identity.

This plan does NOT flip the coverage gate — that flip lands in 21-04 after BOTH
this plan and 21-02 have landed their tests (prevents a red-gate window in CI).

Purpose: Establish a clean, fully-covered baseline before any engine surgery.
Output: New test cases in tests/test_coverage_improvements.py covering all
documented gaps.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-quality/21-RESEARCH.md
@.planning/phases/21-quality/21-CONTEXT.md
@ketu/houses/_ecliptic.py
@ketu/houses/api.py
@tests/test_coverage_improvements.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover _ecliptic.py RA<->lambda converters and houses/api.py gaps</name>
  <files>tests/test_coverage_improvements.py</files>
  <action>
    Add a test class (e.g. `class TestEclipticConverters:`) targeting the ~64%
    outlier `ketu/houses/_ecliptic.py` (missing lines 43-47 `ra_to_lambda`,
    69-73 `lambda_to_ra`). Import via `from ketu.houses._ecliptic import
    ra_to_lambda, lambda_to_ra`.
      - Known-value asserts: at the equinoxes RA == lambda (0 deg and 180 deg),
        and at the solstices (90/270 deg) lambda differs from RA per obliquity.
        Use mean obliquity eps ~= 23.4392911 deg (or import the project's eps
        helper if one exists — grep ketu/ephemeris for obliquity). Wrap inputs as
        np.asarray; assert with np.testing.assert_allclose(..., atol=1e-6) on
        rounded/known values.
      - Round-trip identity: for a spread of lambdas (e.g. np.linspace(0,350,12)),
        assert ra_to_lambda(lambda_to_ra(lam, eps), eps) ~= lam (mod 360) within
        atol=1e-6. Handle the 360-wrap (compare via np.isclose on (a-b+180)%360-180).
    In the SAME class or a sibling class, cover `ketu/houses/api.py`:
      - Line 120: call calculate_houses(...) with an invalid `polar_fallback`
        value (e.g. "bogus") and assert it raises ValueError (pytest.raises).
        Use a valid jd/lat/lon — reuse a JD like 2451545.0 and lat/lon scalars.
      - Lines 241-254: call `house_of(...)` directly (it currently has zero direct
        coverage). Grep its signature in ketu/houses/api.py first; pass a body
        longitude + computed house cusps (call calculate_houses to get cusps, or
        pass jd/lat/lon per the real signature) and assert it returns an int house
        number in 1..12.
    Do NOT add any `# pragma: no cover`. Do NOT touch source files.
  </action>
  <verify>
    venv/bin/python -m pytest tests/test_coverage_improvements.py -q --no-cov 2>&1 | tail -5
    venv/bin/python -m pytest tests/ --cov=ketu.houses --cov-report=term-missing -q --no-cov-on-fail 2>&1 | grep -E "_ecliptic.py|houses/api.py"
  </verify>
  <done>
    _ecliptic.py shows 100% (no missing 43-47, 69-73); houses/api.py lines
    120 and 241-254 no longer in the missing list. New tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cover aspects, cycles, ephemeris time/normalize, planets gaps</name>
  <files>tests/test_coverage_improvements.py</files>
  <action>
    Add targeted tests for the remaining non-orbital-guard gaps from RESEARCH.
    For each, write the minimal real test that drives the missing branch (read the
    surrounding source first to learn the exact trigger condition):
      - ketu/aspects/core.py: 68-69, 185, 336, 380, 409, 428 (error/return branches).
      - ketu/aspects/timelines.py: 431, 443, 452, 472, 497-499.
      - ketu/aspects/windows.py: 343, 350, 449, 458, 466.
      - ketu/calculations.py: 170, 172, 174 — call body_name() for the true-Node /
        mean-Apogee aliases (grep the alias names in ketu/calculations.py / core.py).
      - ketu/cycles/calculator.py: 222. (Lines 26-29 are the ImportError
        CACHE_AVAILABLE=False branch — see Task 3.)
      - ketu/ephemeris/time.py: 88 (Julian-calendar pre-1582 branch — pass a date
        before 1582-10-15), 369 (gst<0 branch — find an input making gst negative).
      - ketu/ephemeris/orbital.py: 227 ONLY — normalize_angle with a negative input
        (e.g. normalize_angle(-30) == 330). Do NOT touch the arcsin/div-0 sites here
        (21-02 owns those).
      - ketu/ephemeris/planets.py: 354, 362, 448.
      - ketu/houses/koch.py: 132-133, regiomontanus.py: 152-153 (polar mask NaN-out —
        feed a high latitude beyond the polar circle), porphyry.py: 100 (is_polar
        ndarray path — pass array lat with a polar element).
      - ketu/cache/ephemeris_cache.py: 391 (month==12 rollover — exercise the Dec
        boundary).
      - ketu/cli/harmonics_spec.py: 80-81, 93; aspects_cmd.py: 65; houses_cmd.py: 74;
        synastry_cmd.py: 70 (CLI arg/error branches — invoke the spec/cmd functions
        with the triggering args; grep how existing CLI tests call them).
      - ketu/complex.py: 421 (CycleRatio negative-radians branch).
    Group into coherent test classes. Keep each assertion meaningful (assert the
    actual returned/raised behavior, not just "ran"). Zero pragmas, zero source edits.
  </action>
  <verify>
    venv/bin/python -m pytest tests/test_coverage_improvements.py -q --no-cov 2>&1 | tail -5
    venv/bin/python -m pytest tests/ --cov=ketu --cov-report=term-missing -q 2>&1 | grep -E "TOTAL|core.py|timelines.py|windows.py|calculations.py|time.py|planets.py|koch.py|regiomontanus.py|porphyry.py|ephemeris_cache.py|harmonics_spec.py|aspects_cmd.py|houses_cmd.py|synastry_cmd.py|complex.py"
  </verify>
  <done>
    All listed lines drop out of the term-missing report (except orbital.py
    arcsin guard sites owned by 21-02, and display.py:28 owned by 21-04
    exclude_lines, and cycles/calculator.py 26-29 owned by Task 3). Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cover the CACHE_AVAILABLE=False ImportError branch (calculator.py 26-29)</name>
  <files>tests/test_coverage_improvements.py</files>
  <action>
    Cover ketu/cycles/calculator.py lines 26-29 — the `except ImportError:`
    branch setting CACHE_AVAILABLE=False. Read lines 1-40 of calculator.py to see
    the exact import being guarded. Drive the branch with importlib reload under a
    simulated missing module: use `unittest.mock.patch.dict(sys.modules, {...})` to
    set the cache import target to None (or use a `builtins.__import__` monkeypatch
    that raises ImportError for that specific module name), then
    `importlib.reload(ketu.cycles.calculator)` and assert
    `calculator.CACHE_AVAILABLE is False`. CRITICAL: in a finally block, restore
    sys.modules and reload calculator again so the real module state is restored for
    other tests (otherwise downstream tests using the cache break). Verify by
    asserting CACHE_AVAILABLE is True after teardown.
  </action>
  <verify>
    venv/bin/python -m pytest tests/test_coverage_improvements.py -q --no-cov 2>&1 | tail -5
    venv/bin/python -m pytest tests/ --cov=ketu.cycles --cov-report=term-missing -q 2>&1 | grep "calculator.py"
  </verify>
  <done>
    calculator.py lines 26-29 no longer in missing list; full suite still green
    after the reload (no state leakage). 
  </done>
</task>

</tasks>

<verification>
- venv/bin/python -m pytest tests/ -q passes (no regressions).
- venv/bin/python -m pytest tests/ --cov=ketu --cov-report=term-missing -q shows
  the ONLY remaining missing lines are: orbital.py arcsin guard sites (21-02 adds
  + covers them) and display.py:28 (21-04 exclude_lines). No `# pragma: no cover`
  added anywhere.
</verification>

<success_criteria>
QAL-10 test work complete: every documented gap (minus 21-02's guard lines and
display.py:28) covered by a real test. _ecliptic.py at 100% via known-value +
round-trip tests. Zero new pragmas. Gate flip deferred to 21-04.
</success_criteria>

<output>
After completion, create `.planning/phases/21-quality/21-01-SUMMARY.md`
</output>
