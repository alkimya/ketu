---
phase: 21-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/__init__.py
  - ketu/calculations.py
  - ketu/houses/api.py
  - ketu/charts/api.py
  - ketu/composite/api.py
  - ketu/synastry/api.py
  - ketu/parts/api.py
  - ketu/returns/solar.py
  - ketu/returns/lunar.py
  - Makefile
  - .github/workflows/tests.yml
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "All broken doctests are fixed with LIVE-captured correct values (calculations.py, __init__.py, houses/api.py house_of)"
    - "Every +SKIP doctest on the public API surface is replaced with a real runnable example (or deterministic assertion)"
    - "Public-API docstrings carry Notes covering accuracy-vs-Swiss + edge cases"
    - "A new Makefile `doctest` target + CI step (3.13 only) run --doctest-modules and pass"
  artifacts:
    - path: "Makefile"
      provides: "doctest target running --doctest-modules with --no-cov"
      contains: "doctest"
    - path: ".github/workflows/tests.yml"
      provides: "CI doctest step on 3.13"
      contains: "doctest-modules"
    - path: "pyproject.toml"
      provides: "doctest_optionflags ELLIPSIS + NORMALIZE_WHITESPACE"
      contains: "doctest_optionflags"
  key_links:
    - from: "Makefile doctest target"
      to: "ketu/ source modules"
      via: "pytest --doctest-modules --no-cov"
      pattern: "doctest-modules"
    - from: ".github/workflows/tests.yml"
      to: "Makefile/pytest"
      via: "new CI step gated on 3.13"
      pattern: "doctest"
---

<objective>
Deliver QAL-12: give public-API docstrings qualitative depth (runnable doctest-style
Examples + accuracy/edge-case Notes), FIX the currently-broken doctests, replace all
`# doctest: +SKIP` on the public surface with real runnable examples, and wire a NEW
`--doctest-modules` gate (Makefile target + CI step, 3.13 only).

Per CONTEXT (LOCKED): English; real CI-collected doctests (`>>>` + actual outputs);
determinism via fixed dates/JD + `round(x, 2)` or ELLIPSIS; Notes cover accuracy vs
Swiss + edge cases (precision, supported date range, limit behavior). Scope =
ketu/__init__.py __all__ + subpackage api.py surfaces.

CRITICAL ORDERING: the existing addopts in pyproject.toml deliberately OMIT
--cov-fail-under and must NOT gain --doctest-modules (respect partial runs). The
doctest gate is a SEPARATE invocation (Makefile target + CI step).

Purpose: Docstrings become trustworthy, runnable, and CI-enforced.
Output: Fixed/expanded docstrings across the public API + a doctest gate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-quality/21-RESEARCH.md
@.planning/phases/21-quality/21-CONTEXT.md
@ketu/__init__.py
@ketu/calculations.py
@ketu/houses/api.py
@Makefile
@.github/workflows/tests.yml
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all broken doctests with live-captured values</name>
  <files>ketu/__init__.py, ketu/calculations.py, ketu/houses/api.py</files>
  <action>
    Fix the doctests RESEARCH flagged as failing. For EACH, run the snippet live in
    the venv to capture the CURRENT correct output, then paste it verbatim:
      - ketu/calculations.py: 14/15 doctests fail (stale expected values — e.g. Sun
        longitude 294.82 -> 295.59 for 2025-01-15). Re-run every `>>>` block live
        (use venv/bin/python -c '...'); update each expected line. Where output is a
        long float, prefer `round(x, 2)` in the example so the expected value is
        stable; where output is an array/struct, assert on `.shape` / dtype field
        names rather than full repr.
      - ketu/__init__.py: module-level doctest fails (`'Sun'` vs `Sun`). Fix the
        expected repr to match actual (quote the string if the value is a str repr).
      - ketu/houses/api.py: the `house_of` doctest expects 2 but returns 1. Re-run
        live and correct the expected house number.
    Verify each fixed docstring runs clean under --doctest-modules. Do NOT use
    +SKIP to dodge a hard case — make it deterministic instead (round / ELLIPSIS /
    structural assert). Keep changes doc-only (no logic changes).
  </action>
  <verify>
    venv/bin/python -m pytest --doctest-modules ketu/calculations.py ketu/__init__.py ketu/houses/api.py --no-cov -q 2>&1 | tail -10
  </verify>
  <done>
    --doctest-modules passes for calculations.py, __init__.py, houses/api.py with
    live-correct expected values. No +SKIP introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace +SKIP with runnable examples + add Notes across the public API surface</name>
  <files>ketu/charts/api.py, ketu/composite/api.py, ketu/synastry/api.py, ketu/parts/api.py, ketu/returns/solar.py, ketu/returns/lunar.py, ketu/houses/api.py, ketu/calculations.py</files>
  <action>
    Across the public API surface (top-level __all__ symbols + subpackage api.py
    surfaces per RESEARCH §5), do TWO things:
    (A) Replace every `# doctest: +SKIP` on these public functions with a REAL
        runnable example. Targets confirmed by grep: charts/api.py (compute_chart,
        is_day_chart), composite/api.py (calculate_composite, circular_midpoint),
        synastry/api.py (calculate_synastry), parts/api.py (calculate_part,
        calculate_all_parts), returns/solar.py (solar_return), returns/lunar.py
        (lunar_return). For functions whose output is a date/struct that is unstable
        to print, assert on deterministic facets: `.shape`, dtype field names, an
        int house number, or `round(value, 2)`. Use a fixed JD (e.g. 2451545.0) or
        `utc_to_julian(datetime(2000,1,1,12,tzinfo=ZoneInfo("UTC")))` so examples
        are deterministic. Each example must actually pass under --doctest-modules.
        (NOTE: returns/_solve.py, parts/registry.py, charts/__init__.py also carry
        +SKIP per grep — fix those that are part of the public/imported surface;
        leave purely-internal ones if fixing them is disproportionate, but do not
        leave a +SKIP on any symbol exported from a public api.py / __all__.)
    (B) Add or enrich a numpy-style `Notes` section on each public function covering:
        accuracy vs Swiss Ephemeris (approximate magnitude), supported date range,
        and at least one edge case / limit behavior (e.g. polar latitudes for houses,
        r->0 handling for positions, normalization range). Keep it qualitative and
        accurate — do NOT invent numbers; if unsure of the Swiss-delta magnitude,
        state it qualitatively. Do NOT add Chiron accuracy notes (Phase 25 — out of
        scope). numpydoc ignores EX01 so Examples aren't gate-required, but Notes
        must keep `numpydoc lint` + `interrogate >=95%` clean.
  </action>
  <verify>
    grep -rn "doctest: +SKIP" ketu/charts/api.py ketu/composite/api.py ketu/synastry/api.py ketu/parts/api.py ketu/returns/solar.py ketu/returns/lunar.py
    venv/bin/python -m pytest --doctest-modules ketu/ --no-cov --ignore=ketu/lunar_calendar.py --ignore=ketu/__main__.py -q 2>&1 | tail -10
    make doc-gates 2>&1 | tail -5
  </verify>
  <done>
    No +SKIP remains on any public api.py symbol; --doctest-modules passes for the
    whole ketu/ tree; doc-gates (interrogate + numpydoc) still green; Notes present
    on each public function.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the doctest gate — Makefile target, CI step, pytest optionflags</name>
  <files>Makefile, .github/workflows/tests.yml, pyproject.toml</files>
  <action>
    Add the NEW doctest gate WITHOUT touching addopts (must keep partial runs working
    and must NOT add --doctest-modules to addopts):
      - pyproject.toml [tool.pytest.ini_options]: add
        `doctest_optionflags = ["ELLIPSIS", "NORMALIZE_WHITESPACE"]`. Do NOT add or
        change --cov-fail-under here (that flip is 21-04's job; this plan only adds
        doctest config).
      - Makefile: add a `.PHONY` `doctest` target (also add it to the .PHONY line)
        running:
        `$(PYTHON) -m pytest --doctest-modules ketu/ --no-cov --ignore=ketu/lunar_calendar.py --ignore=ketu/__main__.py`
        with a `## doctest:` comment explaining --no-cov avoids the NumPy
        _NoValueType reload bug (consistent with existing Makefile comments).
      - .github/workflows/tests.yml: add a NEW step gated `if: matrix.python-version
        == '3.13'` (place it near the other 3.13-only gates, e.g. after the numpydoc
        step), titled e.g. "Doctest gate (--doctest-modules)", running the same
        pytest --doctest-modules invocation (or `make doctest`).
  </action>
  <verify>
    make doctest 2>&1 | tail -8
    grep -n "doctest_optionflags" pyproject.toml
    grep -n "doctest" Makefile
    grep -n "doctest-modules\|Doctest" .github/workflows/tests.yml
  </verify>
  <done>
    `make doctest` passes locally; CI has a 3.13-only doctest step; pyproject has
    doctest_optionflags; addopts unchanged (no --doctest-modules, no
    --cov-fail-under added by this plan).
  </done>
</task>

</tasks>

<verification>
- venv/bin/python -m pytest --doctest-modules ketu/ --no-cov --ignore=ketu/lunar_calendar.py --ignore=ketu/__main__.py -q passes.
- make doc-gates still green (interrogate >=95% + numpydoc clean).
- No +SKIP on any public api.py / __all__ symbol.
- addopts in pyproject.toml unchanged.
</verification>

<success_criteria>
QAL-12 complete: broken doctests fixed with live values, +SKIP replaced with real
runnable examples on the public surface, Notes (accuracy-vs-Swiss + edge cases)
added, and a CI-enforced --doctest-modules gate wired via a new Makefile target +
3.13 CI step. No Chiron docs (Phase 25), no logic changes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-quality/21-03-SUMMARY.md`
</output>
