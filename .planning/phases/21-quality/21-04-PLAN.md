---
phase: 21-quality
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - pyproject.toml
  - .github/workflows/tests.yml
autonomous: true

must_haves:
  truths:
    - "pytest --cov reports 100% project coverage and the 100% gate holds (fail_under=100 + CI --cov-fail-under=100)"
    - "display.py:28 (if TYPE_CHECKING import) is excluded via [tool.coverage.report] exclude_lines, NOT a pragma"
    - "The zero-pragma policy is preserved (no `# pragma: no cover` added anywhere in the phase)"
  artifacts:
    - path: "pyproject.toml"
      provides: "fail_under=100 + exclude_lines entry for TYPE_CHECKING"
      contains: "fail_under = 100"
    - path: ".github/workflows/tests.yml"
      provides: "CI coverage gate flipped to --cov-fail-under=100"
      contains: "cov-fail-under=100"
  key_links:
    - from: "pyproject.toml [tool.coverage.report]"
      to: "ketu/display.py:28"
      via: "exclude_lines 'if TYPE_CHECKING:'"
      pattern: "TYPE_CHECKING"
    - from: ".github/workflows/tests.yml"
      to: "coverage gate"
      via: "--cov-fail-under=100 on 3.13"
      pattern: "cov-fail-under=100"
---

<objective>
Flip the project coverage gate to 100% — the FINAL step of QAL-10. Runs in Wave 2,
AFTER 21-01 (all gap tests) and 21-02 (div/0 guard lines + r->0 test) have landed,
so the 100% gate goes green in one shot with no red-gate window in CI.

Per CONTEXT (LOCKED): lift `fail_under` 70 -> 100 in pyproject.toml; KEEP existing
`omit` (ketu/__main__.py, ketu/lunar_calendar.py); per-subpackage 95% Makefile gates
stay as-is. The one runtime-unreachable line — display.py:28 `if TYPE_CHECKING:` —
is handled via `[tool.coverage.report] exclude_lines` (config, NOT a pragma; preserves
the zero-pragma policy).

Purpose: Lock in 100% project coverage as a CI-enforced baseline.
Output: fail_under=100 + TYPE_CHECKING exclude_lines + CI gate flipped to 100.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-quality/21-RESEARCH.md
@.planning/phases/21-quality/21-CONTEXT.md
@pyproject.toml
@.github/workflows/tests.yml
@ketu/display.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude TYPE_CHECKING import line and lift fail_under to 100</name>
  <files>pyproject.toml</files>
  <action>
    In pyproject.toml under [tool.coverage.report]:
      - Add `"if TYPE_CHECKING:"` to the existing `exclude_lines` list. This excludes
        ketu/display.py line 28 (the runtime-unreachable `if TYPE_CHECKING:` import
        guard) WITHOUT a `# pragma: no cover` — preserving the zero-pragma policy.
        (Confirm display.py:28 is exactly `if TYPE_CHECKING:` first; if the text
        differs slightly, match the actual conditional.)
      - Change `fail_under = 70` to `fail_under = 100`.
    Leave the `omit` list untouched (ketu/__main__.py, ketu/lunar_calendar.py stay).
    Do NOT touch addopts. Do NOT add any pragma.
  </action>
  <verify>
    grep -n "fail_under\|TYPE_CHECKING\|exclude_lines" pyproject.toml
    venv/bin/python -m pytest tests/ --cov=ketu --cov-report=term-missing --cov-fail-under=100 -q 2>&1 | tail -15
  </verify>
  <done>
    fail_under=100; "if TYPE_CHECKING:" in exclude_lines; omit unchanged; the
    --cov-fail-under=100 run PASSES (TOTAL 100%, display.py:28 no longer counted).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip the CI coverage gate to 100</name>
  <files>.github/workflows/tests.yml</files>
  <action>
    In the "Check coverage threshold" step (gated `if: matrix.python-version ==
    '3.13'`), change `--cov-fail-under=70` to `--cov-fail-under=100`. Leave the
    main "Run tests" step and all other steps unchanged. This is the CI enforcement
    of the 100% project gate.
  </action>
  <verify>
    grep -n "cov-fail-under" .github/workflows/tests.yml
  </verify>
  <done>
    The 3.13 coverage-threshold CI step uses --cov-fail-under=100; no other step
    altered.
  </done>
</task>

</tasks>

<verification>
- venv/bin/python -m pytest tests/ --cov=ketu --cov-fail-under=100 -q PASSES (full
  project at 100%, zero pragmas added across the phase).
- pyproject.toml: fail_under=100, exclude_lines has "if TYPE_CHECKING:", omit intact.
- CI 3.13 gate flipped to 100.
</verification>

<success_criteria>
QAL-10 fully delivered: 100% project coverage, CI-enforced on 3.13 and via
pyproject fail_under, the single unreachable TYPE_CHECKING line excluded by config
(not pragma), and the zero-pragma policy intact.
</success_criteria>

<output>
After completion, create `.planning/phases/21-quality/21-04-SUMMARY.md`
</output>
