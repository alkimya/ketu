---
phase: 40-declination-speed-field-chart-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/charts/core.py
  - ketu/calculations.py
  - tests/charts/test_dtype.py
autonomous: true
requirements: [DSPD-01, DSPD-04, DSPD-05]
must_haves:
  truths:
    - "np.dtype(CHART_DTYPE).names includes 'body_decl_speed' at index 8 (16 fields total)"
    - "DECL_STANDSTILL_EPS is importable from ketu.calculations and equals 0.001"
    - "The dtype ratchet in tests/charts/test_dtype.py passes with 16 fields pinned"
  artifacts:
    - path: "ketu/charts/core.py"
      provides: "body_decl_speed field in CHART_DTYPE"
      contains: "body_decl_speed"
    - path: "ketu/calculations.py"
      provides: "DECL_STANDSTILL_EPS public constant"
      contains: "DECL_STANDSTILL_EPS"
    - path: "tests/charts/test_dtype.py"
      provides: "16-field ratchet re-pin"
      contains: "body_decl_speed"
  key_links:
    - from: "tests/charts/test_dtype.py"
      to: "ketu.charts.core.CHART_DTYPE"
      via: "field-name / shape / kind assertions"
      pattern: "body_decl_speed"
---

<objective>
Lay the v1.8 foundation: append the `body_decl_speed` field to `CHART_DTYPE`,
define the public `DECL_STANDSTILL_EPS` standstill constant in `ketu.calculations`,
and intentionally break-then-re-pin the dtype ratchet so every downstream plan
can populate and read the new field.

Purpose: The field and the constant are the contract surface the rest of the
phase depends on. `compute_chart` (Plan 02) and `calculate_composite` (Plan 03)
both write `body_decl_speed`; the chart-level helper (Plan 02) reads
`DECL_STANDSTILL_EPS`. Nothing else can proceed until the dtype grows and the
ratchet is re-pinned (DSPD-04 — the break is intentional, like `body_decl` in v1.5).

Output: `CHART_DTYPE` at 16 fields, `DECL_STANDSTILL_EPS = 0.001` exported from
`ketu.calculations`, and the `tests/charts/test_dtype.py` ratchet green at 16 fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-declination-speed-field-chart-api/40-RESEARCH.md
@.planning/phases/40-declination-speed-field-chart-api/40-PATTERNS.md
@.planning/phases/40-declination-speed-field-chart-api/40-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (excluded from drift / source-grounding
verification — they do not exist before this phase):

- `body_decl_speed` — new field tuple `("body_decl_speed", "f8", (14,))` in `CHART_DTYPE` (`ketu/charts/core.py`)
- `DECL_STANDSTILL_EPS` — new module-level constant `= 0.001` in `ketu.calculations`, added to `__all__`

Downstream symbols produced by Plans 02 & 03 (listed here for the phase-wide source-grounding pass):
- `is_ascending_declination_chart` — new chart-level helper in `ketu/charts/api.py`, exported via `ketu/charts/__init__.py` (Plan 02)
- New/extended test files: `tests/charts/test_compute_chart.py` (`TestBodyDeclSpeed`), `tests/charts/test_chart_helpers.py`, `tests/composite/test_calculate_composite.py` (`TestBodyDeclSpeed`), `tests/test_declination.py` (standstill tests), synastry + returns pinning assertions (Plans 02 & 03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DECL_STANDSTILL_EPS constant to ketu.calculations</name>
  <files>ketu/calculations.py, tests/test_declination.py</files>
  <read_first>
    - `ketu/calculations.py` lines 490-555 — the `declination_velocity` and `is_ascending_declination` scalar neighbourhood (the constant's natural home; FD step 0.01 d lives here)
    - `ketu/calculations.py` lines 656-686 — the `__all__` export list (add the constant near `declination_velocity`)
    - `.planning/phases/40-declination-speed-field-chart-api/40-RESEARCH.md` § "Resolution 3: DECL_STANDSTILL_EPS numeric value" — empirical justification table (Sun solstice 0.00002, Moon standstill 0.00004 → neutral; Jupiter/Uranus in-motion 0.003-0.005 → not masked)
    - `.planning/phases/40-declination-speed-field-chart-api/40-PATTERNS.md` § "ketu/calculations.py — add DECL_STANDSTILL_EPS constant" — the `#:` numpydoc docstring style to mirror
    - `tests/test_declination.py` — existing import/value test patterns to mirror for the new standstill tests
  </read_first>
  <behavior>
    - Test 1 (DSPD-05): `from ketu.calculations import DECL_STANDSTILL_EPS` succeeds
    - Test 2 (DSPD-05): `DECL_STANDSTILL_EPS == 0.001`
    - Test 3 (DSPD-05): Sun at exact solstice (JD ~2460482.36) → `abs(declination_velocity(jd, 0)) <= DECL_STANDSTILL_EPS` (classifies neutral)
    - Test 4 (DSPD-05, non-masking): Jupiter mid-cycle (typical |dδ/dt| ~0.005) → `abs(declination_velocity(jd, 5)) > DECL_STANDSTILL_EPS` (NOT masked)
  </behavior>
  <action>
    Add a module-level constant `DECL_STANDSTILL_EPS: float = 0.001` (deg/day) to
    `ketu/calculations.py`, placed immediately before `declination_velocity` (line 495)
    or between `declination_velocity` and `is_ascending_declination`. Document it with a
    `#:` numpydoc comment block (numpydoc gate is BLOCKING) stating it is the public
    standstill-threshold contract: `|dδ/dt| <= DECL_STANDSTILL_EPS` ⇒ standstill (neutral),
    and cite the empirical justification from RESEARCH.md Resolution 3 (Sun solstice
    ~0.00002, Moon standstill ~0.00004 → neutral; Jupiter/Uranus in motion 0.003-0.005 →
    not masked; value sits above the ~0.000002 FD truncation floor and below any real
    in-motion reading). Per D-05/DSPD-05 the threshold lives IN Ketu so Rahu invents none.
    Add `"DECL_STANDSTILL_EPS"` to `__all__` (line 656) in the body-functions section near
    `"declination_velocity"`. Then add the four behavior tests to `tests/test_declination.py`
    (use `utc_to_julian` to build the Sun-solstice and Jupiter JDs as the existing tests do).
    Do NOT introduce a configurable Δt or any new API surface (out of scope).
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && source venv/bin/activate && python -c "from ketu.calculations import DECL_STANDSTILL_EPS; assert DECL_STANDSTILL_EPS == 0.001" && pytest tests/test_declination.py -k standstill -q</automated>
  </verify>
  <acceptance_criteria>
    - `from ketu.calculations import DECL_STANDSTILL_EPS` succeeds and the value `== 0.001`
    - `"DECL_STANDSTILL_EPS"` appears in `ketu.calculations.__all__`
    - `pytest tests/test_declination.py -k standstill -q` exits 0
    - The constant carries a `#:` docstring (numpydoc gate stays green)
    - No `Δt` parameter or new function added to the public surface
  </acceptance_criteria>
  <done>DECL_STANDSTILL_EPS = 0.001 is importable, exported, documented, and its value + standstill/non-masking classification are tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append body_decl_speed to CHART_DTYPE and re-pin the dtype ratchet</name>
  <files>ketu/charts/core.py, tests/charts/test_dtype.py</files>
  <read_first>
    - `ketu/charts/core.py` lines 85-111 — current `CHART_DTYPE` (15 fields) and the comment header documenting the additive-field / Kala positional-offset pattern
    - `.planning/phases/40-declination-speed-field-chart-api/40-PATTERNS.md` § "ketu/charts/core.py — add body_decl_speed field" — the exact target layout (field 8 = `body_decl_speed`)
    - `tests/charts/test_dtype.py` lines 50-153 — the FIVE ratchet locations to re-pin (each currently has a `body_decl` entry to mirror)
    - `.planning/phases/40-declination-speed-field-chart-api/40-RESEARCH.md` § "Pitfall 5: Ratchet test update count" — all five locations must be updated atomically
  </read_first>
  <behavior>
    - Test 1 (DSPD-01): `CHART_DTYPE.names` equals the 16-field tuple with `"body_decl_speed"` after `"body_decl"`
    - Test 2 (DSPD-04): `body_decl_speed` subarray shape is `(14,)`
    - Test 3 (DSPD-04): `body_decl_speed` base dtype kind `"f"`, itemsize `8`
    - Test 4 (DSPD-04): vectorised construction `np.zeros(5, dtype=CHART_DTYPE)["body_decl_speed"].shape == (5, 14)`
    - Test 5 (DSPD-04): 0-d construction `np.zeros((), dtype=CHART_DTYPE)["body_decl_speed"].shape == (14,)`
  </behavior>
  <action>
    Insert the tuple `("body_decl_speed", "f8", (14,))` into `CHART_DTYPE` in
    `ketu/charts/core.py` immediately after `("body_decl", "f8", (14,))` (line 103),
    making it field index 8 of 16 (15→16 fields). Extend the comment header (lines 89-94)
    to record that v1.8 appends `body_decl_speed` (dδ/dt, deg/day) — additive dtype-version
    bump, body COUNT stays 14, downstream positional-offset / `.view()` consumers (Kala)
    must adapt (documented, not fixed here). Mirror the verbatim v1.5 `body_decl` precedent.
    Then re-pin the ratchet in `tests/charts/test_dtype.py` at all FIVE locations per
    PATTERNS.md: (1) add `"body_decl_speed"` after `"body_decl"` in the `expected` tuple
    (lines 56-61) and update the docstring count "15 fields"→"16 fields" mentioning v1.8;
    (2) add `("body_decl_speed", (14,))` to the subarray-shapes parametrize; (3) add
    `("body_decl_speed", "f", 8)` to the field-kinds parametrize; (4) add
    `assert arr["body_decl_speed"].shape == (5, 14)` to vectorised-construction; (5) add
    `assert elem["body_decl_speed"].shape == (14,)` to 0-d construction. The ratchet break
    is intentional (DSPD-04) — re-pin, do not delete the assertions.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && source venv/bin/activate && python -c "from ketu.charts import CHART_DTYPE; n=CHART_DTYPE.names; assert n[8]=='body_decl_speed', n; assert len(n)==16, len(n)" && pytest tests/charts/test_dtype.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `np.dtype(CHART_DTYPE).names` includes `'body_decl_speed'` at index 8; `len(names) == 16`
    - `body_decl_speed` is `f8` with subarray shape `(14,)`
    - `pytest tests/charts/test_dtype.py -q` exits 0 with all five ratchet locations green at 16 fields
    - The comment header in `core.py` mentions the v1.8 `body_decl_speed` addition and Kala impact
  </acceptance_criteria>
  <done>CHART_DTYPE carries body_decl_speed at index 8 (16 fields); the dtype ratchet is re-pinned and green across all five locations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a numeric dtype field and a float constant. No I/O, no parsing, no user-controlled strings, no new external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | (all) | CHART_DTYPE field + DECL_STANDSTILL_EPS constant | accept | Pure numerical computation, no I/O, no parsing, no user-controlled input. The jd/lat/lon input-validation at the `ketu.charts` public surface (ASVS V5) is UNCHANGED by this plan. No high/medium threats. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (pure NumPy, already a dependency). No supply-chain surface introduced. |
</threat_model>

<verification>
- `pytest tests/charts/test_dtype.py tests/test_declination.py -q` → all green
- `from ketu.calculations import DECL_STANDSTILL_EPS` → 0.001
- `CHART_DTYPE.names[8] == "body_decl_speed"` and `len == 16`
- Numpydoc/interrogate gates stay green for the new constant docstring
</verification>

<success_criteria>
- CHART_DTYPE has 16 fields with `body_decl_speed` at index 8 (DSPD-01)
- Dtype ratchet re-pinned and green at 16 fields (DSPD-04)
- `DECL_STANDSTILL_EPS = 0.001` importable, exported, documented, tested (DSPD-05)
- Full suite remains green; 100% coverage maintained for changed lines
</success_criteria>

<output>
Create `.planning/phases/40-declination-speed-field-chart-api/40-01-SUMMARY.md` when done
</output>
