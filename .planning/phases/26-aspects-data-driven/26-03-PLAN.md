---
phase: 26-aspects-data-driven
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - CHANGELOG.md
  - UPGRADING.md
  - docs/source/concepts.md
  - docs/source/api.md
  - docs/locale/fr/LC_MESSAGES/concepts.po
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/changelog.po
autonomous: true

must_haves:
  truths:
    - "CHANGELOG has a [1.3.0] BREAKING entry for the aspect engine (default 5→7, new API, harmonic+symbol columns, minors opt-in)"
    - "UPGRADING has a v1.2 -> v1.3 section with the restore recipe (aspects='classical'→5, aspects='extended'→14), the new API, the minors-opt-in note, and a generic Kala note"
    - "concepts.md Harmonic Theory notes the default is now the 7 half-circle aspects and minors are opt-in (without duplicating the dual-base rule)"
    - "api.md documents aspects_for_harmonics, the harmonic+symbol columns, the conceptual coef==coefficient mapping, and fixes the stale EXTENDED-default claims"
    - "fr gettext catalogs are regenerated through the existing pipeline (msgstr may stay English-fallback); en+fr docs build clean"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[1.3.0] BREAKING aspect-engine entry"
      contains: "1.3.0"
    - path: "UPGRADING.md"
      provides: "v1.2 -> v1.3 migration section with restore recipe"
      contains: "v1.2 -> v1.3"
    - path: "docs/source/concepts.md"
      provides: "Harmonic Theory default-now-7 note + aspects_for_harmonics example + verified glyph/harmonic table"
      contains: "aspects_for_harmonics"
    - path: "docs/source/api.md"
      provides: "aspects_for_harmonics docs + harmonic/symbol columns + coef==coefficient note + fixed default"
      contains: "aspects_for_harmonics"
  key_links:
    - from: "docs/source/api.md aspect default prose"
      to: "the 7 half-circle default (not EXTENDED, not CLASSICAL)"
      via: "replace stale 'None → EXTENDED' / 'EXTENDED (default)'"
      pattern: "half-circle|TRADITIONAL|7 aspects"
    - from: "docs/locale/fr/LC_MESSAGES/concepts.po"
      to: "new English msgids from concepts.md"
      via: "make gettext + make update-po"
      pattern: "aspects_for_harmonics"
---

<objective>
Document the breaking public-surface change and regenerate the fr gettext catalogs — the
final piece that makes the v1.3.0 aspect contract shippable.

This plan is pure docs/prose + i18n regeneration (no code). It depends on the FINAL API and
default produced by Plans 01 (harmonic+symbol columns) and 02 (aspects_for_harmonics, default
5→7, CLI pinned to classical), so it documents what actually shipped.

Scope:
1. CHANGELOG.md — cut/append a `[1.3.0]` section with a BREAKING aspect-engine entry.
2. UPGRADING.md — new `## v1.2 -> v1.3` section (newest-first) with restore recipe + new API +
   minors-opt-in + generic Kala note.
3. docs/source/concepts.md — note the default is now 7 half-circle (reuse, don't duplicate the
   dual-base rule); add an aspects_for_harmonics example; verify the Aspect Types table's
   glyph + harmonic columns match the enriched core.aspects.
4. docs/source/api.md — document aspects_for_harmonics + the harmonic/symbol columns + the
   conceptual coef==coefficient mapping; FIX the stale `EXTENDED (default)` / `None → EXTENDED`
   claims (already wrong today) to the new 7 half-circle default.
5. Regenerate fr gettext (concepts.po / api.po / changelog.po) via the existing pipeline;
   msgstr may stay English-fallback (consistent with the pre-1.3.0 fr-translation memory note);
   build en + fr clean.

Purpose: Satisfies Success Criterion 4 (breaking change documented in CHANGELOG + UPGRADING;
concepts.md + api.md updated; fr gettext regenerated; full suite green at 100%).

Output: updated docs + regenerated fr catalogs + a clean en/fr build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-aspects-data-driven/26-CONTEXT.md
@.planning/phases/26-aspects-data-driven/26-RESEARCH.md

# Documents the final surface from 01 + 02
@.planning/phases/26-aspects-data-driven/26-01-SUMMARY.md
@.planning/phases/26-aspects-data-driven/26-02-SUMMARY.md

# Edit sites (verified byte-accurate at planning time)
@CHANGELOG.md
@UPGRADING.md
@docs/source/concepts.md
@docs/source/api.md
</context>

<decision_log>
LOCKED user decisions honored by this plan (do NOT revisit):
- Hard break, documented — NO deprecation alias. UPGRADING shows the restore recipe:
  aspects='classical' for the old 5, aspects='extended' for all 14.
- TWO-part default shift to call out in UPGRADING (the key subtlety): the CURRENT
  resolve_aspect_set default was CLASSICAL (5), not 7. So 1.3.0 BOTH (a) adds Semi-sextile +
  Quincunx to the implicit default AND (b) keeps H5/H9/H10 out. A caller with no aspects= now
  gets 7 instead of 5.
- CLI note: the bare CLI --harmonics default stays classical (5) — the library/API default is
  what moved to 7. Make this explicit so users aren't confused by the divergence.
- coef==coefficient: document the conceptual mapping (the roadmap's `coefficient` field is the
  implementation's `coef`); the table was NOT renamed.
- concepts.md: full pedagogical explanation but CALIBRATE against existing content — the
  dual-base rule (half-circle 1/2/3/6 divide 180°; full-circle 5/9/10 divide 360°) is ALREADY
  documented (concepts.md:70-126). Do NOT duplicate it; ADD the default-now-7 + minors-opt-in
  note + an aspects_for_harmonics compose example.
- Sextile=H3, Trine=H3; Semi-sextile=H6, Quincunx=H6 — the harmonic column the docs describe.
- 7 majors get the concepts.md glyphs; 7 minors are blank.
- fr msgstr may stay English-fallback (Phase 25 convention / pre-1.3.0 fr-translation note);
  do NOT run migrate_translations.py (legacy one-time tool).
- Do NOT bump the package version (__init__.py / pyproject.toml) — that is Phase 27 (REL-10).
  Only prepare the CHANGELOG [1.3.0] heading + UPGRADING prose. CHANGELOG already has a
  [Unreleased] BREAKING (cycles) entry — place the aspect entry under a [1.3.0] heading per the
  Keep-a-Changelog format already in the file.
- Kala: generic UPGRADING note only; Kala adapts post-release (NOT a release blocker). If Kala
  passes aspects= explicitly it is unaffected; if it relies on the implicit default it now sees
  7 instead of 5.
</decision_log>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG [1.3.0] BREAKING entry + UPGRADING v1.2 -> v1.3 section</name>
  <files>CHANGELOG.md, UPGRADING.md</files>
  <action>
CHANGELOG.md: the file currently has a `## [Unreleased]` section (line 9) with an existing
BREAKING (cycles `angular_separation`) entry. Add a `## [1.3.0]` section (Keep-a-Changelog
format, matching the existing style) — OR rename/cut the [Unreleased] heading to [1.3.0] if
the project convention is to collect the release there (check the file's existing pattern and
follow it; do NOT touch version constants). Under [1.3.0] add aspect-engine entries:
  - ### Added: `aspects_for_harmonics(harmonics)` — compose an aspect set from a list of
    harmonics (e.g. [1,2,3,6]); `harmonic` and `symbol` columns on `core.aspects`.
  - ### Changed / BREAKING: the default aspect set (when `aspects=None`) is now the 7
    half-circle aspects (harmonics 1,2,3,6: Conjunction, Semi-sextile, Sextile, Square, Trine,
    Quincunx, Opposition) — previously 5 (CLASSICAL). Full-circle minors (H5/H9/H10) are now
    opt-in. Note: the CLI bare `--harmonics` default stays classical (5). Mention the
    conceptual `coefficient`==`coef` mapping (the table was enriched, the field name kept).
  - Include a one-line restore pointer to UPGRADING.

UPGRADING.md: the newest section today is `## v1.1 -> v1.2`. Add a NEW `## v1.2 -> v1.3`
section ABOVE it (newest-first ordering — see the file header "ordered newest-first"). Content
with CONCRETE before/after code examples:
  - "Aspect engine changes (1.3.0)" subsection.
  - The TWO-part default shift: was CLASSICAL (5), now the 7 half-circle set — spell out the 7
    aspects and that this both adds Semi-sextile+Quincunx AND keeps H5/H9/H10 out.
  - Restore recipe (before/after):
      # old implicit default (5 majors):
      calculate_aspects(jd, aspects="classical")
      # all 14 (legacy v1.0 behaviour):
      calculate_aspects(jd, aspects="extended")
  - New API example:
      from ketu.aspects import aspects_for_harmonics
      mask = aspects_for_harmonics([1, 2, 3, 6])   # the new default, explicitly
      minors = aspects_for_harmonics([5, 9, 10])   # opt into the full-circle minors
  - Minors-now-opt-in note (EXTENDED still = all 14; no new preset).
  - CLI note: `ketu ... --harmonics` with no value still resolves to classical (5); only the
    library/API default moved to 7.
  - Generic Kala note: if you pass `aspects=` explicitly you are unaffected; if you relied on
    the implicit default you now get 7 instead of 5 — Kala adapts post-release; not a blocker.
Keep prose tight and example-driven; match the existing UPGRADING voice.
  </action>
  <verify>
source venv/bin/activate
grep -n "1.3.0" CHANGELOG.md
grep -n "v1.2 -> v1.3\|aspects_for_harmonics\|aspects=\"classical\"\|aspects=\"extended\"" UPGRADING.md
# Confirm version constants UNTOUCHED (Phase 27 owns the bump):
grep -n "version" pyproject.toml | head; python -c "import ketu; print(ketu.__version__)"
</verify>
  <done>
CHANGELOG has a [1.3.0] section with Added (aspects_for_harmonics + harmonic/symbol columns) +
BREAKING (default 5→7, minors opt-in, coef==coefficient note, CLI-stays-5 note). UPGRADING has
a `## v1.2 -> v1.3` section (newest-first) with restore recipe, new-API example, minors-opt-in,
CLI note, and Kala note. Package version constants UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update concepts.md + api.md for the new surface (fix stale defaults)</name>
  <files>docs/source/concepts.md, docs/source/api.md</files>
  <action>
docs/source/concepts.md:
- Harmonic Theory section (lines 70-126): the dual-base rule + per-harmonic breakdown is
  ALREADY correct and present — do NOT duplicate it. ADD a short note that the DEFAULT aspect
  set is now the 7 half-circle aspects (harmonics 1,2,3,6) and that the full-circle minors
  (H5/H9/H10) are opt-in.
- Configurable Aspect Sets section (lines 128-140, "New in v1.1"): update the stated default to
  the 7 half-circle set; add an `aspects_for_harmonics([1, 2, 3, 6])` compose example and note
  CLASSICAL (5) is the opt-in path for the "old default". Mention `aspects_for_harmonics` as the
  harmonic-composition sister to the presets.
- Aspect Types table (lines 164-183, columns Angle | Symbol | Harmonic | Coefficient): VERIFY
  the 7 major glyphs in the Symbol column match the enriched core.aspects["symbol"] EXACTLY
  (Conjunction ☌, Semi-sextile ⚺, Sextile ⚹, Square □, Trine △, Quincunx ⚻, Opposition ☍) and
  the Harmonic column matches the frozen mapping (Sextile=3, Trine=3, Semi-sextile=6,
  Quincunx=6); minors blank in the Symbol column. Fix any mismatch so the doc and the table
  agree.

docs/source/api.md:
- Aspects section (lines 160-227): document `aspects_for_harmonics(harmonics) -> np.bool_[14]`
  (signature, valid harmonics {1,2,3,5,6,9,10}, raises ValueError on others, returns a frozen
  length-14 mask). Document the new `harmonic` and `symbol` columns on core.aspects and the
  conceptual `coefficient`==`coef` mapping (the field is named `coef`).
- FIX the STALE default claims (already wrong today): line 183 `| EXTENDED | All 14 aspects
  (default) |` and line 211 `None → EXTENDED` → the real new default is the 7 half-circle set.
  Update the preset table so the default is clearly the 7 half-circle (TRADITIONAL), CLASSICAL
  is the 5-major opt-in, EXTENDED is all 14 (no longer labeled default).
- compute_chart section (line 356-364): FIX the stale `default: EXTENDED` (line 364) → the new
  7 half-circle default.
  </action>
  <verify>
source venv/bin/activate
grep -n "aspects_for_harmonics\|half-circle\|7 aspects" docs/source/concepts.md
grep -n "aspects_for_harmonics\|harmonic\|symbol\|coef" docs/source/api.md
# No stale EXTENDED-default left:
! grep -n "EXTENDED (default)\|None → EXTENDED\|default: EXTENDED" docs/source/api.md && echo "no stale default claims"
# Build the English docs clean:
cd docs && (make html SPHINXBUILD="python3 -m sphinx" 2>&1 | tail -20 || make html 2>&1 | tail -20)
</verify>
  <done>
concepts.md notes the default-now-7 + minors-opt-in (no rule duplication), has an
aspects_for_harmonics example, and its Aspect Types table glyph/harmonic columns match
core.aspects. api.md documents aspects_for_harmonics + harmonic/symbol columns + coef==coefficient,
and all three stale EXTENDED-default claims (lines 183, 211, 364) are corrected to the 7
half-circle default. English docs build clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate fr gettext catalogs + verify en/fr build</name>
  <files>docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/changelog.po</files>
  <action>
Regenerate the French gettext catalogs through the EXISTING pipeline so the new English
msgids (from concepts.md / api.md / the CHANGELOG) are merged in. msgstr may stay
English-fallback (do NOT translate — consistent with the Phase 25 convention and the
pre-1.3.0 fr-translation memory note). Do NOT run migrate_translations.py (legacy one-time
tool).

From docs/ (the Makefile sets VENVBIN ?= ../venv/bin so `make` should work without
activation). If the venv sphinx shebangs are broken (Phase 25 workaround), override the
binaries to module form:

  cd /home/loc/workspace/ketu/docs
  # verify module entrypoints first
  python3 -m sphinx --version
  python3 -m sphinx_intl --help | head -3
  # 1. extract translatable strings → build/gettext/*.pot
  make gettext   SPHINXBUILD="python3 -m sphinx"
  # 2. merge into the French PO files
  make update-po SPHINXBUILD="python3 -m sphinx" SPHINXINTL="python3 -m sphinx_intl"

Then verify both language builds:
  make html      SPHINXBUILD="python3 -m sphinx"   # en
  make html-fr   SPHINXBUILD="python3 -m sphinx" SPHINXINTL="python3 -m sphinx_intl"   # fr (build-mo + html -D language=fr)

Expect modified msgids in docs/locale/fr/LC_MESSAGES/concepts.po + api.po + changelog.po
(new entries for aspects_for_harmonics, the default-now-7 prose, the harmonic/symbol columns,
the [1.3.0] CHANGELOG entry). Commit the regenerated .po files; new msgstr entries staying
empty / English-fallback is acceptable. The build must stay at the Phase 25 baseline (en+fr
each at ~1 warning — no NEW warnings from these edits).
  </action>
  <verify>
cd /home/loc/workspace/ketu/docs
git status --porcelain locale/fr/LC_MESSAGES/*.po
grep -c "aspects_for_harmonics" locale/fr/LC_MESSAGES/api.po locale/fr/LC_MESSAGES/concepts.po 2>/dev/null || true
# en + fr build clean (no NEW warnings beyond the Phase 25 ~1-warning baseline)
make html    SPHINXBUILD="python3 -m sphinx" 2>&1 | tail -5
make html-fr SPHINXBUILD="python3 -m sphinx" SPHINXINTL="python3 -m sphinx_intl" 2>&1 | tail -5
</verify>
  <done>
concepts.po / api.po / changelog.po regenerated with the new msgids (msgstr may be empty /
English-fallback); en + fr builds complete at the Phase 25 warning baseline (no new warnings);
migrate_translations.py NOT run.
  </done>
</task>

</tasks>

<verification>
- CHANGELOG [1.3.0]: Added (aspects_for_harmonics + harmonic/symbol columns) + BREAKING (default 5→7, minors opt-in, coef==coefficient note, CLI-stays-5 note). Version constants untouched.
- UPGRADING `## v1.2 -> v1.3` (newest-first): two-part default shift explained, restore recipe (classical→5 / extended→14), new-API example, minors-opt-in, CLI note, generic Kala note.
- concepts.md: default-now-7 + minors-opt-in note (no dual-base duplication); aspects_for_harmonics example; Aspect Types table glyph + harmonic columns match core.aspects.
- api.md: aspects_for_harmonics + harmonic/symbol columns + coef==coefficient documented; all three stale EXTENDED-default claims fixed.
- fr gettext: concepts.po/api.po/changelog.po regenerated (English-fallback OK); en+fr build clean at the Phase 25 baseline; migrate_translations.py NOT run.
- Full test suite STILL green at 100% coverage (docs edits don't touch code, but run pytest as a final guard since this is the phase-closing plan).
</verification>

<success_criteria>
Success Criterion 4: the breaking change to the public aspect surface (default set,
coefficients, presets) is documented in CHANGELOG + UPGRADING; concepts.md Harmonic Theory +
api.md updated; fr gettext catalogs regenerated; full suite green at 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/26-aspects-data-driven/26-03-SUMMARY.md`
documenting: the CHANGELOG [1.3.0] + UPGRADING v1.2->v1.3 content, the concepts.md/api.md edits
(incl. the three stale-default fixes), the regenerated .po files, the en/fr build status, and
confirmation that the package version was NOT bumped (Phase 27 owns REL-10).
</output>
