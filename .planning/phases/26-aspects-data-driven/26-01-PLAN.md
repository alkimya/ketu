---
phase: 26-aspects-data-driven
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/core.py
  - tests/test_ketu.py
autonomous: true

must_haves:
  truths:
    - "core.aspects carries a harmonic column with the frozen per-aspect values (Sextile=H3, Trine=H3, Semi-sextile=H6, Quincunx=H6)"
    - "core.aspects carries a symbol column with the 7 major glyphs and blank for the 7 minors"
    - "The existing name/angle/coef bytes are unchanged (byte-fingerprint stable)"
    - "The table is still 14 rows in canonical order"
  artifacts:
    - path: "ketu/core.py"
      provides: "Enriched aspects structured array with harmonic (i4) + symbol (U4) columns"
      contains: "(\"harmonic\", \"i4\")"
    - path: "tests/test_ketu.py"
      provides: "Updated dtype-names test + per-row harmonic/symbol assertions + new harmonic+symbol fingerprint"
      contains: "EXPECTED_ASPECT_HARMONICS"
  key_links:
    - from: "ketu/core.py aspects dtype"
      to: "harmonic + symbol columns appended after coef"
      via: "dtype field order name,angle,coef,harmonic,symbol"
      pattern: "\\(\"coef\", \"f4\"\\), \\(\"harmonic\", \"i4\"\\), \\(\"symbol\", \"U4\"\\)"
    - from: "tests/test_ketu.py::test_aspects_dtype_names"
      to: "5-field dtype contract"
      via: "assert dtype.names == 5-tuple"
      pattern: "\\(\"name\", \"angle\", \"coef\", \"harmonic\", \"symbol\"\\)"
---

<objective>
Enrich the existing `core.aspects` structured array IN PLACE with two new columns —
`harmonic` (i4) and `symbol` (U4) — appended after `coef`, transcribing the harmonic
numbers and glyphs already documented in `docs/source/concepts.md`. Update the
`core.py` module docstring and the three contract tests that pin the aspect table
(dtype names, per-row values, byte fingerprint).

Purpose: This is the data foundation for the whole phase. `aspects_for_harmonics`
(Plan 02) reads the `harmonic` column; the symbol column becomes the canonical
glyph source of truth. The change is APPEND-ONLY: name/angle/coef bytes stay
bit-for-bit identical, so the v1.1 byte-fingerprint invariant for those three
columns is preserved.

Output: `core.aspects` with 5 dtype fields `(name, angle, coef, harmonic, symbol)`,
14 rows unchanged in order, plus updated/added tests reaching 100% coverage on the
new columns.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-aspects-data-driven/26-CONTEXT.md
@.planning/phases/26-aspects-data-driven/26-RESEARCH.md

# Edit sites (verified byte-accurate at planning time)
@ketu/core.py
@tests/test_ketu.py
</context>

<decision_log>
LOCKED user decisions honored by this plan (do NOT revisit):
- Extend the EXISTING structured array in core.py — NOT a NamedTuple/dataclass. Stay NumPy-first.
- Keep the field name `coef` (do NOT rename to `coefficient`). The roadmap's
  conceptual 5-tuple `(name, angle, harmonic, coefficient, symbol)` is satisfied by the
  implementation `(name, angle, coef, harmonic, symbol)`. The conceptual coef==coefficient
  mapping is documented later in api.md/CHANGELOG (Plan 03), NOT by a rename here.
- Append `harmonic` + `symbol` AFTER `coef` so the byte-fingerprint of name+angle+coef
  stays append-only-stable.
- Sextile=H3, Trine=H3, Semi-sextile=H6, Quincunx=H6 (User Decision 2; codebase/concepts.md
  convention — half-circle harmonics divide 180°). FROZEN per the RESEARCH "Per-aspect
  harmonic (FROZEN)" table.
- 7 majors get the concepts.md glyphs; 7 minors get blank "" (User Decision 3 — no tofu glyphs).
- symbol dtype width = U4 (RESEARCH recommendation — headroom, no truncation risk).
- No coefficient recalibration; coef values byte-stable.
</decision_log>

<tasks>

<task type="auto">
  <name>Task 1: Add harmonic + symbol columns to core.aspects (in place)</name>
  <files>ketu/core.py</files>
  <action>
Edit the `aspects = np.array([...])` literal at `ketu/core.py:89-108`. Append TWO new
values to EACH of the 14 rows — `harmonic` (int) then `symbol` (str) — and extend the
dtype tuple from `[("name", "S16"), ("angle", "f4"), ("coef", "f4")]` to
`[("name", "S16"), ("angle", "f4"), ("coef", "f4"), ("harmonic", "i4"), ("symbol", "U4")]`.

The 14 rows become EXACTLY (name, angle, coef UNCHANGED — append harmonic, symbol):

  ("Conjunction",  0,   1,     1,  "☌"),   # H1, ☌
  ("Semi-sextile", 30,  1 / 6, 6,  "⚺"),   # H6, ⚺
  ("Decile",       36,  1 / 10, 10, ""),         # H10, blank
  ("Novile",       40,  1 / 9, 9,  ""),          # H9, blank
  ("Sextile",      60,  1 / 3, 3,  "⚹"),   # H3, ⚹
  ("Quintile",     72,  1 / 5, 5,  ""),          # H5, blank
  ("Binovile",     80,  2 / 9, 9,  ""),          # H9, blank
  ("Square",       90,  1 / 2, 2,  "□"),   # H2, □
  ("Tredecile",    108, 3 / 10, 10, ""),         # H10, blank
  ("Trine",        120, 2 / 3, 3,  "△"),   # H3, △
  ("Biquintile",   144, 2 / 5, 5,  ""),          # H5, blank
  ("Quincunx",     150, 5 / 6, 6,  "⚻"),   # H6, ⚻
  ("Quadrinovile", 160, 4 / 9, 9,  ""),          # H9, blank
  ("Opposition",   180, 1,     1,  "☍"),   # H1, ☍

CRITICAL constraints:
- The first three values of every row (name, angle, coef) MUST be byte-identical to the
  current literal — do NOT touch coef expressions (`1 / 6`, `1 / 3`, etc.) or reorder rows.
- harmonic values are FROZEN per the decision log: Sextile=3, Trine=3, Semi-sextile=6,
  Quincunx=6. Do NOT use the naive 360/angle reading.
- The 7 minors (Decile, Novile, Quintile, Binovile, Tredecile, Biquintile, Quadrinovile)
  get symbol = "" (empty string). Only the 7 half-circle majors get a glyph.
- Use explicit `\uXXXX` escapes for the glyphs (avoids encoding ambiguity / editor issues).
  Codepoints: Conjunction U+260C, Semi-sextile U+26BA, Sextile U+26B9, Square U+25A1,
  Trine U+25B3, Quincunx U+26BB, Opposition U+260D.
- Keep the leading comment block at lines 87-88 accurate (mentions harmonics).

Then update the module docstring `aspects` block at `core.py:28-38`: under the
`aspects : numpy.ndarray` entry, change "Structured array of 14 major aspects with fields:"
field list from 3 fields to 5 — add:
  - harmonic (int): Harmonic number (half-circle 1/2/3/6 divide 180°; full-circle 5/9/10 divide 360°).
  - symbol (str): Unicode astrological glyph (majors only; minors are blank).
Keep the existing Examples block at lines 45-58 UNCHANGED (it uses field access
`aspects['angle'][...]` / `aspects['name'][...]` which is column-add-safe — verified no
doctest prints the whole array).
  </action>
  <verify>
source venv/bin/activate
python -c "from ketu.core import aspects as a; assert a.dtype.names==('name','angle','coef','harmonic','symbol'), a.dtype.names; assert len(a)==14; assert int(a['harmonic'][4])==3 and int(a['harmonic'][9])==3 and int(a['harmonic'][1])==6 and int(a['harmonic'][11])==6; assert a['symbol'][0]=='☌' and a['symbol'][13]=='☍' and a['symbol'][2]=='' ; print('OK')"
python -m sphinx 2>/dev/null; make doctest   # 56 doctests must still pass (no full-array repr)
  </verify>
  <done>
`core.aspects.dtype.names == ("name","angle","coef","harmonic","symbol")`; 14 rows;
harmonic column = [1,6,10,9,3,5,9,2,10,3,5,6,9,1]; symbol majors filled, minors blank;
`make doctest` green (56 doctests).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip dtype-names test + add per-row harmonic/symbol assertions + new fingerprint</name>
  <files>tests/test_ketu.py</files>
  <action>
Three edits in `tests/test_ketu.py` (the aspect-table contract tests):

1. `test_aspects_dtype_names` (lines 124-129): change the assertion from
   `("name", "angle", "coef")` to `("name", "angle", "coef", "harmonic", "symbol")` and
   update the message to reflect the v1.3 5-field schema.

2. Add two new module-level expected constants next to the existing
   `EXPECTED_ASPECT_NAMES`/`EXPECTED_ASPECT_ANGLES`/`EXPECTED_ASPECT_COEFS` (around lines
   25-75):
     EXPECTED_ASPECT_HARMONICS = [1, 6, 10, 9, 3, 5, 9, 2, 10, 3, 5, 6, 9, 1]
     EXPECTED_ASPECT_SYMBOLS = ["☌", "⚺", "", "", "⚹", "", "", "□", "", "△", "", "⚻", "", "☍"]
   Extend `test_aspects_structure` (lines 131-148) with two more per-row loops asserting
   `aspects_data["harmonic"][i] == EXPECTED_ASPECT_HARMONICS[i]` and
   `aspects_data["symbol"][i] == EXPECTED_ASPECT_SYMBOLS[i]` for all 14 rows. This pins the
   frozen harmonic mapping (guards against the naive 360/angle reading) AND covers the new
   columns for the 100% gate (Pitfall 4: the byte-fingerprint alone would NOT catch a wrong
   harmonic/symbol value).

3. `test_aspects_byte_fingerprint` (lines 150-161): KEEP the existing name+angle+coef hash
   and its `EXPECTED_ASPECT_FINGERPRINT_V1` assertion UNCHANGED (it must still pass —
   proves append-only stability). ADD a second hash that also folds in
   `aspects_data["harmonic"].tobytes()` and `aspects_data["symbol"].tobytes()`, assert it
   against a new `EXPECTED_ASPECT_FINGERPRINT_V13` constant. Compute the real hex by running
   the test once (it will fail with the actual digest), then paste that digest into the
   constant. Do NOT fabricate the hash — derive it from the actual array.

KEEP `test_aspects_length` (lines 117-122) UNCHANGED (still 14).
  </action>
  <verify>
source venv/bin/activate
pytest tests/test_ketu.py -k "aspects" -v
# All aspect tests green. Confirm the v1 fingerprint assertion still holds (name+angle+coef byte-stable).
pytest tests/test_ketu.py::TestCoreDataStructures::test_aspects_byte_fingerprint -v
  </verify>
  <done>
test_aspects_dtype_names asserts the 5-field tuple; test_aspects_structure checks all 14
harmonic + 14 symbol values; the original name+angle+coef fingerprint still matches
EXPECTED_ASPECT_FINGERPRINT_V1; a new v13 fingerprint pins harmonic+symbol. All aspect
tests pass.
  </done>
</task>

</tasks>

<verification>
- `core.aspects.dtype.names == ("name","angle","coef","harmonic","symbol")`.
- name/angle/coef bytes unchanged: EXPECTED_ASPECT_FINGERPRINT_V1 still matches.
- harmonic column frozen to [1,6,10,9,3,5,9,2,10,3,5,6,9,1]; symbol majors filled, minors blank.
- Full aspect-table test class green; `make doctest` green (56 doctests, no array-repr drift).
- 100% coverage on the new columns (per-row assertions cover every harmonic + symbol value).
</verification>

<success_criteria>
Success Criterion 1 (partial): aspects defined in ONE declarative table that now carries
name, angle, harmonic, coefficient(=coef), symbol — the data foundation for data-driven
detection + harmonic selection. name/angle/coef byte-stable; 14 rows; gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/26-aspects-data-driven/26-01-SUMMARY.md`
documenting: the final dtype, the frozen harmonic vector, the symbol vector, both
fingerprints (v1 unchanged + v13 new), and confirmation that name/angle/coef are byte-stable.
</output>
