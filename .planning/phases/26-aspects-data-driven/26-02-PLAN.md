---
phase: 26-aspects-data-driven
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - ketu/aspects/presets.py
  - ketu/aspects/__init__.py
  - ketu/aspects/calculator.py
  - ketu/charts/api.py
  - ketu/cli/aspects_cmd.py
  - tests/test_aspect_presets.py
  - tests/cli/test_cli_default_divergence.py
  - tests/charts/test_aspect_matrix.py
autonomous: true

must_haves:
  truths:
    - "aspects_for_harmonics([1,2,3,6]) returns the 7 half-circle mask; ([5,9,10]) returns the 7 minors; ([7]) raises ValueError"
    - "resolve_aspect_set(None) now returns the 7 half-circle set (was CLASSICAL 5)"
    - "calculate_aspects with no aspects= now yields the 7 half-circle aspects"
    - "compute_chart() with no aspects= now yields the 7 half-circle aspect matrix (the chart-layer default ratchet is re-pointed from classical to traditional)"
    - "The CLI bare --harmonics default stays 5 aspects (classical) — byte-stable contract preserved"
    - "EXTENDED still selects all 14; CLASSICAL still selects the curated 5 majors"
  artifacts:
    - path: "ketu/aspects/presets.py"
      provides: "aspects_for_harmonics function + harmonic-derived TRADITIONAL/EXTENDED + flipped resolver default"
      exports: ["aspects_for_harmonics"]
      contains: "def aspects_for_harmonics"
    - path: "ketu/aspects/__init__.py"
      provides: "Package-level export of aspects_for_harmonics"
      contains: "aspects_for_harmonics"
    - path: "ketu/cli/aspects_cmd.py"
      provides: "CLI None branch pinned to classical (preserves byte-stable CLI)"
      contains: "resolve_aspect_set(\"classical\")"
    - path: "tests/cli/test_cli_default_divergence.py"
      provides: "Regression locking CLI-default==classical(5) vs library-default==7"
    - path: "tests/charts/test_aspect_matrix.py"
      provides: "D-07 chart-layer default ratchet re-pointed from classical to traditional (7)"
      contains: "traditional"
  key_links:
    - from: "ketu/aspects/presets.py::aspects_for_harmonics"
      to: "core.aspects['harmonic'] column"
      via: "np.isin(_ASPECTS['harmonic'], requested)"
      pattern: "_ASPECTS\\[.harmonic.\\]"
    - from: "ketu/aspects/presets.py::resolve_aspect_set"
      to: "the 7 half-circle default"
      via: "default param = TRADITIONAL (harmonic-derived [1,2,3,6])"
      pattern: "default: npt.NDArray\\[np.bool_\\] = TRADITIONAL"
    - from: "ketu/cli/aspects_cmd.py None branch"
      to: "classical preset (5)"
      via: "resolve_aspect_set('classical') instead of resolve_aspect_set(None)"
      pattern: "resolve_aspect_set\\(.classical.\\)"
    - from: "ketu/charts/api.py::compute_chart(aspects=None)"
      to: "the 7 half-circle default aspect matrix"
      via: "None threads to resolve_aspect_set(None) -> TRADITIONAL"
      pattern: "aspects=.traditional."
---

<objective>
Add the harmonic-selection API and flip the library default — without disturbing any
retained aspect's numeric output and without shifting the CLI.

This plan delivers the behavioral core of the phase:
1. `aspects_for_harmonics(list[int]) -> np.bool_[14]` — a sister function to
   `resolve_aspect_set`, building a frozen mask from the new `harmonic` column with the
   same strict ValueError contract.
2. Redefine `TRADITIONAL`/`EXTENDED` on top of the harmonic table (single source of truth);
   keep `CLASSICAL` as a curated index list (it is NOT a pure harmonic set).
3. Flip `resolve_aspect_set`'s default from `CLASSICAL` (5) to the 7 half-circle set.
4. Pin the CLI bare `--harmonics` default to `"classical"` explicitly so the CLI stays
   byte-identical (the v1.0/v1.1 byte-stable contract) while the library default moves to 7.
5. Update the now-stale calculator + charts docstrings ("None resolves to CLASSICAL/5
   majors" → the 7 half-circle default).
6. Flip / add the contract tests, with full coverage on every new branch — including the
   chart-layer D-07 ratchet (`tests/charts/test_aspect_matrix.py`) which currently pins
   `compute_chart()`'s default to CLASSICAL and MUST be re-pointed to the new 7-default.

Purpose: Satisfies Success Criteria 2 (harmonic-composition API) and 3 (default = 7
half-circle, minors opt-in), while User Decision 1 keeps the CLI byte-stable.

Output: working `aspects_for_harmonics`, a 7-aspect library default, an unchanged CLI,
and a green suite at 100% coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-aspects-data-driven/26-CONTEXT.md
@.planning/phases/26-aspects-data-driven/26-RESEARCH.md

# This plan depends on 26-01 (the harmonic column must exist first)
@.planning/phases/26-aspects-data-driven/26-01-SUMMARY.md

# Edit sites (verified byte-accurate at planning time)
@ketu/aspects/presets.py
@ketu/aspects/__init__.py
@ketu/cli/aspects_cmd.py
@tests/test_aspect_presets.py
@tests/charts/test_aspect_matrix.py
</context>

<decision_log>
LOCKED user decisions honored by this plan (do NOT revisit):
- aspects_for_harmonics returns a FROZEN length-14 np.bool_ mask (same shape as presets /
  resolve_aspect_set) — drop-in, no new type through hot loops.
- Strict validation: valid harmonics = the set actually present in the table {1,2,3,5,6,9,10},
  computed data-driven from core.aspects["harmonic"]. aspects_for_harmonics([7]) raises
  ValueError, mirroring resolve_aspect_set's strict error contract.
- Presets redefined on top of the harmonic table (single source of truth):
  TRADITIONAL = aspects_for_harmonics([1,2,3,6]) (the 7 half-circle);
  EXTENDED = aspects_for_harmonics([1,2,3,5,6,9,10]) (all 14).
  CLASSICAL stays a CURATED index/name list ([0,4,7,9,13]) — it is NOT a pure harmonic set
  (keeps Sextile H3 but drops Semi-sextile/Quincunx H6), so it CANNOT be harmonic-derived
  (Pitfall 7). Leave it as-is.
- New default = the 7 half-circle aspects (reuse TRADITIONAL as the default constant —
  Discretion: avoids inventing a 4th preset name; TRADITIONAL already == the 7).
- Coefficients byte-stable; the data-driven refactor must NOT perturb numeric output for any
  aspect that survives in a given set. Do NOT touch the conjunction `if i_asp == 0` geometric
  special-case (calculator.py:225,363 + composite/synastry) — leave it alone.
- CLI pinned to classical (User Decision 1): change aspects_cmd.py None branch from
  resolve_aspect_set(None) to resolve_aspect_set("classical") so the CLI stays 5 aspects and
  the "classical" label stays correct after the LIBRARY default shifts to 7. CLI byte-stable
  tests must STAY GREEN unchanged (test_v1_1_reference_byte_stable.py, test_parser.py:117,
  test_harmonics_spec.py, test_resolved_header.py:35-36).
- Export aspects_for_harmonics from presets.py __all__ AND ketu/aspects/__init__.py
  (import block + __all__). NOTE: ketu/__init__.py does NOT re-export the presets surface;
  the package boundary for this API is ketu.aspects — do not add it to the top-level
  ketu/__init__.py.
- No new preset for minors; no CLI harmonic-number wiring (deferred); no absolute-orb field.
- Chart-layer default moves to 7 in lockstep with the library default: compute_chart(aspects=None)
  threads None -> calculate_aspects_vectorized(aspects=None) -> resolve_aspect_set(None), so once
  the resolver default flips to TRADITIONAL the chart default IS the 7 half-circle set. This is
  option (a) (decision-consistent with Success Criterion 3 and the 26-02 docstring fix): the
  D-07 chart-layer ratchet test is re-pointed from classical to traditional — NOT pinned back to 5.
</decision_log>

<atomicity_note>
ORDERING / ATOMICITY (User Decision 1 — CLI must stay byte-stable):
The library-default flip (Task 1) and the CLI pin to classical (Task 2) MUST land in the same
execution pass. Do NOT commit Task 1's resolver-default flip without Task 2's
`resolve_aspect_set("classical")` pin in `aspects_cmd.py` — if the flip ships alone, the CLI
bare `--harmonics` default would silently jump 5->7 and break the byte-stable contract. Tasks 1
and 2 are in the same plan and wave; keep them together. The existing CLI byte-stable suite
(`test_v1_1_reference_byte_stable.py`, `test_resolved_header.py`, `test_parser.py`,
`test_harmonics_spec.py`) is the regression guard and MUST stay green throughout — no new test
needed for atomicity (`test_cli_default_divergence.py` from Task 2 covers the steady state).
</atomicity_note>

<tasks>

<task type="auto">
  <name>Task 1: Add aspects_for_harmonics + redefine presets + flip resolver default + re-point chart-layer ratchet</name>
  <files>ketu/aspects/presets.py, ketu/aspects/__init__.py, ketu/aspects/calculator.py, ketu/charts/api.py, tests/charts/test_aspect_matrix.py</files>
  <action>
In `ketu/aspects/presets.py`:

1. Compute the valid-harmonic set data-driven from the table, near the top (after the
   `assert len(_ASPECTS) == 14` block, ~line 47):
     _VALID_HARMONICS: frozenset[int] = frozenset(int(h) for h in _ASPECTS["harmonic"])
   This yields {1, 2, 3, 5, 6, 9, 10} from the enriched table (do NOT hardcode the set —
   derive it, so it stays correct if the table ever changes).

2. Add the new function `aspects_for_harmonics` AFTER `resolve_aspect_set` (~after line 224,
   before `__all__`). Signature and contract:
     def aspects_for_harmonics(harmonics: Sequence[int]) -> npt.NDArray[np.bool_]:
   Behavior:
     - Iterate the input; for each item reject bool explicitly (bool is a subclass of int —
       mirror resolve_aspect_set lines 200-203), reject non-int types with a clear ValueError
       ("invalid harmonic ... (expected int)"), and reject any int not in _VALID_HARMONICS
       with a ValueError that lists the valid harmonics sorted (e.g.
       "unknown harmonic: 7. Valid harmonics: 1, 2, 3, 5, 6, 9, 10").
     - Build the mask via field access: `mask = np.isin(_ASPECTS["harmonic"], requested_list)`
       then convert the True positions to a FROZEN mask using the existing `_indices_to_mask`
       helper (so the returned array has writeable=False — Pitfall 8, consistent with presets).
       Equivalent: `_indices_to_mask(np.nonzero(np.isin(_ASPECTS["harmonic"], requested))[0])`.
     - An empty input list returns an all-False frozen mask (sum 0) — that is a valid, if
       empty, selection; do NOT raise on empty.
   Full numpydoc docstring (BLOCKING gate: numpydoc validate checks=["all",-EX01,-SA01,-ES01],
   interrogate >=95%): Summary, Parameters, Returns, Raises, Examples. The Examples block MUST
   be runnable doctests (counts toward `make doctest`), e.g.:
     >>> int(aspects_for_harmonics([1, 2, 3, 6]).sum())
     7
     >>> int(aspects_for_harmonics([5, 9, 10]).sum())
     7
     >>> int(aspects_for_harmonics([1]).sum())
     2
   Type hints: `Sequence[int] -> npt.NDArray[np.bool_]` (mypy --strict clean).

3. Redefine the presets on top of the harmonic table (single source of truth). Replace the
   hardcoded `_TRADITIONAL_INDICES`/`_EXTENDED_INDICES` derivation so:
     TRADITIONAL = aspects_for_harmonics([1, 2, 3, 6])
     EXTENDED    = aspects_for_harmonics([1, 2, 3, 5, 6, 9, 10])
   (Define these AFTER aspects_for_harmonics so the function exists; you may need to reorder
   so the function is defined before the preset constants, OR keep the constants where they
   are and assign them harmonic-derived values once the function exists — pick the layout that
   keeps a single clean definition.) Keep CLASSICAL = _indices_to_mask([0,4,7,9,13]) as a
   curated index list (NOT harmonic-derived — Pitfall 7). Both TRADITIONAL and EXTENDED MUST
   produce masks bit-identical to the current [0,1,4,7,9,11,13] and arange(14) — verify in
   the test plan. Update the module docstring (lines 1-32) to mention aspects_for_harmonics
   and that TRADITIONAL/EXTENDED are now harmonic-derived; note the default is now the 7
   half-circle set.

4. Flip the resolver default: `resolve_aspect_set(spec, default=TRADITIONAL)` at line 106 (was
   `= CLASSICAL`). Update the docstring `spec : ... None : use default (CLASSICAL by default)`
   (lines 124, 132-134) and the `>>> np.array_equal(resolve_aspect_set(None), CLASSICAL)` /
   `True` doctest (lines 152-153) to reflect the new default — change the doctest to compare
   against TRADITIONAL (keep it passing). Keep the other doctests (classical string, index
   list, name list) UNCHANGED.

5. Append `"aspects_for_harmonics"` to `presets.py:__all__` (line 227-233).

In `ketu/aspects/__init__.py`:
6. Add `aspects_for_harmonics` to the import block from `ketu.aspects.presets` (lines 59-65)
   and to `__all__` under the "Presets" group (lines 97-103).

In `ketu/aspects/calculator.py`:
7. Update the FOUR stale docstrings that say "None resolves to CLASSICAL (5 majors...)" at
   lines 100, 157, 275, 474 → "None resolves to the default 7 half-circle aspects
   (harmonics 1,2,3,6)". Prose only — do NOT touch any logic, the conjunction special-case, or
   numeric paths.

In `ketu/charts/api.py`:
8. Update the stale docstrings at lines 131, 225-228, 287-288 that claim aspects=None resolves
   to CLASSICAL (5 majors) → the new 7 half-circle default. Confirm charts call
   calculate_aspects (library default → now 7) and do NOT override the default; update prose to
   match the actual post-flip behavior. Prose only.

In `tests/charts/test_aspect_matrix.py` (chart-layer D-07 ratchet — MUST move with the flip):
9. Re-point the chart-layer default ratchet. `compute_chart(aspects=None)` threads None straight
   into `calculate_aspects_vectorized(aspects=None)` -> `resolve_aspect_set(None)` (api.py:200,
   176, 356), so once step 4 flips the resolver default, `compute_chart()`'s default aspect
   matrix becomes the 7 half-circle (TRADITIONAL) set, not CLASSICAL (5). The current ratchet
   test pins the OLD behavior and will FAIL after the flip. Apply option (a) (decision-consistent
   with Success Criterion 3 and the docstring fix in step 8): re-point the ratchet to traditional,
   do NOT pin it back to 5.
   a. Rename `test_aspect_matrix_default_aspects_is_classical` (lines 79-89) ->
      `test_aspect_matrix_default_aspects_is_traditional` (or `..._is_half_circle`). Assert
      `compute_chart(...)`'s default `aspect_matrix` AND `aspect_orbs` are byte-equal to
      `compute_chart(..., aspects="traditional")` (the 7 half-circle set) instead of
      `aspects="classical"`. Keep the existing `np.array_equal(..., equal_nan=True)` orbs check.
      Update the test docstring/assert-messages to say "aspects=None must equal
      aspects='traditional' (D-07)".
   b. Update the D-07 ratchet comments/docstrings that name CLASSICAL as the package default at
      `test_aspect_matrix.py:7` (module docstring "D-07: CLASSICAL is the package default"),
      and lines 37, 40, 79-80 — re-point each to the new package default (the 7 half-circle /
      TRADITIONAL set). KEEP the ratchet's intent (pin the package default at the chart layer);
      only re-point it at the correct new default. The `_CLASSICAL_INDICES` set/`_I_TRINE` etc.
      helpers at lines 38-40 are still used by OTHER tests in this file that pass
      `aspects="classical"` explicitly (e.g. the hand-validated charts, the subset test) — keep
      those helpers, just fix the comment at line 37/40 so it no longer claims CLASSICAL is the
      package DEFAULT (it is now the curated 5-set you opt into).
   c. AUDIT, DON'T ASSUME — confirm no OTHER test derives aspect CONTENT from the default. The
      other `compute_chart(...)`-without-`aspects=` calls in THIS file (symmetry lines 99/106,
      diagonal sentinels line 120, caller-mask line 232, scalar-shape line 252, batch line 282)
      assert structural/symmetry/sentinel/shape properties that hold for 5 OR 7 aspects — verify
      this still holds, do not assume. Grep `tests/` for any OTHER
      `compute_chart(...)`-without-`aspects=` or `calculate_aspects(...)`-without-`aspects=`
      assertion that derives aspect CONTENT (counts, specific i_asp cells) from the default and
      would break on 5->7. The checker noted synastry
      (`tests/synastry/test_calculate_synastry.py:110`) asserts a self-pair position and most
      composite oracles pass `aspects=` explicitly or assert positions (look safe) — CONFIRM,
      don't trust. After re-pointing, run `pytest tests/charts tests/synastry tests/composite -q`
      and resolve any other default-derived aspect-content assertion the SAME way (re-point to
      traditional). Document any additional file you touch in the 26-02-SUMMARY.
  </action>
  <verify>
source venv/bin/activate
python -c "from ketu.aspects import aspects_for_harmonics, TRADITIONAL, EXTENDED, CLASSICAL, resolve_aspect_set; import numpy as np; \
assert int(aspects_for_harmonics([1,2,3,6]).sum())==7; \
assert int(aspects_for_harmonics([5,9,10]).sum())==7; \
assert int(aspects_for_harmonics([1,2,3,5,6,9,10]).sum())==14; \
assert np.array_equal(aspects_for_harmonics([1,2,3,6]), TRADITIONAL); \
assert np.array_equal(aspects_for_harmonics([1,2,3,5,6,9,10]), EXTENDED); \
assert int(CLASSICAL.sum())==5; \
assert np.array_equal(resolve_aspect_set(None), TRADITIONAL); \
import pytest; \
exec('''\ntry:\n    aspects_for_harmonics([7])\n    raise SystemExit(\"no ValueError\")\nexcept ValueError:\n    pass\n'''); \
assert not aspects_for_harmonics([1,2,3,6]).flags.writeable; print('OK')"
make doctest    # aspects_for_harmonics doctests + updated resolver doctest must pass
mypy --strict ketu/aspects/presets.py
# Chart-layer ratchet re-pointed: default == traditional (7), no longer classical (5)
pytest tests/charts/test_aspect_matrix.py -v
# Audit-don't-assume: confirm no other default-derived aspect-content assertion broke
pytest tests/charts tests/synastry tests/composite -q
  </verify>
  <done>
aspects_for_harmonics composes [1,2,3,6]→7, [5,9,10]→7, [1,2,3,5,6,9,10]→14, [1]→2; raises
ValueError on [7] and on bool/non-int items; returns a frozen mask. TRADITIONAL/EXTENDED are
harmonic-derived and bit-identical to before; CLASSICAL stays 5. resolve_aspect_set(None) ==
TRADITIONAL. Exported from ketu.aspects. Calculator + charts docstrings updated. The chart-layer
D-07 ratchet test is renamed and asserts compute_chart() default == aspects="traditional" (7),
its D-07 comments re-point to the new package default, and tests/charts + tests/synastry +
tests/composite are green (any other default-derived aspect-content assertion found is re-pointed
and documented in SUMMARY). doctest + mypy --strict green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin CLI to classical + add CLI-divergence regression test</name>
  <files>ketu/cli/aspects_cmd.py, tests/cli/test_cli_default_divergence.py</files>
  <action>
NOTE (atomicity): this task MUST land in the same execution pass as Task 1 (see
&lt;atomicity_note&gt; above) — the library-default flip without this CLI pin would break the
v1.0/v1.1 byte-stable contract.

In `ketu/cli/aspects_cmd.py` (lines 84-87): the `args.harmonics is None` branch currently
calls `resolve_aspect_set(None)`. After Task 1 that would resolve to the NEW 7-aspect default
and silently change CLI output (breaking the v1.0/v1.1 byte-stable contract). Per User
Decision 1, change it to `resolve_aspect_set("classical")` so the CLI bare default stays the
5 classical aspects and the hardcoded `preset_label = "classical"` stays correct. Update the
inline comment to explain the intentional divergence: "CLI bare default stays classical (5)
for v1.0/v1.1 byte-stable contract; the LIBRARY default is the 7 half-circle set."

Create `tests/cli/test_cli_default_divergence.py` — a regression test that LOCKS the intended
divergence (so a future change can't silently re-converge them):
  - import resolve_aspect_set, CLASSICAL, TRADITIONAL from ketu.aspects.
  - test_library_default_is_seven_half_circle: assert resolve_aspect_set(None).sum() == 7 and
    np.array_equal(resolve_aspect_set(None), TRADITIONAL).
  - test_cli_bare_default_is_classical_five: drive the CLI default the way aspects_cmd does —
    assert resolve_aspect_set("classical").sum() == 5 and == CLASSICAL — i.e. the value the
    None-branch now resolves. Document in the test docstring that this divergence is
    intentional (CLI byte-stable, library moved to 7).
  - test_cli_and_library_defaults_differ: assert not np.array_equal(resolve_aspect_set("classical"),
    resolve_aspect_set(None)) — they MUST differ (5 vs 7).
Follow the existing tests/cli/ style and numpydoc/type-hint conventions (the suite is mypy
--strict). This does NOT need to invoke the argparse parser; it pins the two resolution paths.
  </action>
  <verify>
source venv/bin/activate
pytest tests/cli/ -v       # ALL existing CLI tests must STAY GREEN unchanged
pytest tests/cli/test_v1_1_reference_byte_stable.py tests/cli/test_parser.py tests/cli/test_harmonics_spec.py tests/cli/test_resolved_header.py -v
pytest tests/cli/test_cli_default_divergence.py -v
# Sanity: bare CLI still emits 5 aspects / "classical" label
python -m ketu 2026-01-01T00:00:00 2>&1 | head -20   # adjust invocation to the package entrypoint
  </verify>
  <done>
aspects_cmd None branch resolves "classical" (5); ALL existing CLI tests green unchanged
(byte-stable contract preserved); new divergence test asserts library default==7 (TRADITIONAL)
while CLI bare default==classical(5), and that the two differ.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip + extend the preset/default contract tests (full branch coverage)</name>
  <files>tests/test_aspect_presets.py</files>
  <action>
Edit `tests/test_aspect_presets.py`:

1. FLIP the three default-equals-classical tests:
   - `test_resolve_none_returns_classical` (lines 130-133) → rename to
     `test_resolve_none_returns_default_half_circle`; assert
     `np.testing.assert_array_equal(resolve_aspect_set(None), TRADITIONAL)` and that its sum
     is 7. (Import TRADITIONAL if not already imported.)
   - `test_default_equals_classical` (lines 489-503) → rename to
     `test_default_equals_traditional`; reassert that `calculate_aspects(jd)` (no aspects=)
     equals `calculate_aspects(jd, aspects=TRADITIONAL)` (was =CLASSICAL).
   - `test_find_aspects_between_dates_default_equals_classical` (lines 442-457) → rename to
     `..._default_equals_traditional`; reassert default == aspects=TRADITIONAL.

2. KEEP UNCHANGED but VERIFY:
   - `test_classical_mask_shape_and_sum` (lines 58-69): CLASSICAL sum==5, indices [0,4,7,9,13].
   - `test_traditional_mask_shape_and_sum` (lines 72-82): TRADITIONAL sum==7, indices
     [0,1,4,7,9,11,13] — confirm the harmonic-derived redefinition produces the identical mask.
   - The frozen-mask / mutation-raises tests (lines ~93-108) — aspects_for_harmonics must also
     be covered (see step 3).

3. ADD a new test block for `aspects_for_harmonics` with FULL branch coverage (BLOCKING:
   fail_under=100, zero pragma — every branch in the new function MUST be hit):
   - happy paths: [1,2,3,6]→sum 7 and == TRADITIONAL; [5,9,10]→sum 7; [1,2,3,5,6,9,10]→sum 14
     and == EXTENDED; [1]→{Conjunction,Opposition} sum 2; [3]→{Sextile,Trine} sum 2 (pins
     User Decision 2: Sextile=H3, Trine=H3); [6]→{Semi-sextile,Quincunx} sum 2; empty list []
     → all-False mask sum 0.
   - error paths (each raises ValueError, message check via pytest.raises(match=...)):
     [7] (harmonic not in table), [4] (not in table), a non-int item like ["3"] or [3.0],
     a bool item [True] (must be rejected, not treated as int 1).
   - frozen-mask test: assert aspects_for_harmonics([1]).flags.writeable is False and that
     mutating it raises ValueError (mirror the existing preset frozen-mask test).
   Mirror the error-path test density of the existing resolver tests (test_aspect_presets.py
   has them around lines 243-318).
  </action>
  <verify>
source venv/bin/activate
pytest tests/test_aspect_presets.py -v
pytest tests/ -q       # full suite green
pytest tests/ --cov=ketu --cov-report=term-missing --cov-fail-under=100   # 100% gate, zero misses on new branches
  </verify>
  <done>
The three default tests now assert TRADITIONAL (7); CLASSICAL/TRADITIONAL shape tests still
pass; aspects_for_harmonics has happy-path + every-error-path + frozen-mask coverage; full
suite green at 100% coverage with zero missed branches in the new function.
  </done>
</task>

</tasks>

<verification>
- `aspects_for_harmonics([1,2,3,6])==TRADITIONAL` (7); `([5,9,10])` sum 7; `([1,2,3,5,6,9,10])==EXTENDED` (14); `([3])` == {Sextile,Trine}; `([7])` raises ValueError; returns frozen mask.
- `resolve_aspect_set(None)==TRADITIONAL` (library default now 7); CLASSICAL still 5; EXTENDED still 14.
- `compute_chart()` default `aspect_matrix`/`aspect_orbs` == `compute_chart(..., aspects="traditional")` (7); the chart-layer D-07 ratchet is re-pointed from classical to traditional; tests/charts + tests/synastry + tests/composite green.
- CLI bare `--harmonics` default still 5 (classical) — ALL tests/cli/ green unchanged, byte-stable contract preserved; divergence regression test green.
- calculator.py (4 sites) + charts/api.py docstrings updated to the 7-default; no logic/numeric change; conjunction special-case untouched.
- aspects_for_harmonics exported from ketu.aspects (__init__ import + __all__) and presets.py __all__.
- Gates: full suite green, 100% coverage (zero missed branches), `make doctest` green, mypy --strict clean, numpydoc validate + interrogate>=95% pass for the new function.
</verification>

<success_criteria>
Success Criterion 2: an API composes an aspect set from a list of harmonics
(`aspects_for_harmonics([1,2,3,6])`) alongside CLASSICAL/TRADITIONAL/EXTENDED.
Success Criterion 3: the default aspect set is the 7 half-circle aspects (H1/2/3/6);
full-circle minors (H5/H9/H10) are opt-in. Library default flips 5→7 (and the chart layer
follows: compute_chart() default == 7); CLI stays 5 (User Decision 1). Retained aspects'
numeric output unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/26-aspects-data-driven/26-02-SUMMARY.md`
documenting: the aspects_for_harmonics contract + valid-harmonic set, the harmonic-derived
preset redefinition (with proof TRADITIONAL/EXTENDED masks are bit-identical to before), the
resolver default flip, the chart-layer D-07 ratchet re-point (classical->traditional) and any
OTHER default-derived aspect-content assertion found during the audit, the CLI pin-to-classical
divergence (and which CLI tests stayed green), and the docstring updates. Record the H3
sextile/trine freeze as a noted decision.
</output>
