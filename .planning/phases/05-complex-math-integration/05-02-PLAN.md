---
phase: 05-complex-math-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/cycles/calculator.py
  - ketu/aspects/core.py
  - ketu/aspects/timelines.py
  - ketu/aspects/calculator.py
  - ketu/complex.py
  - ketu/ephemeris/planets.py
  - ketu/lunar_calendar.py
  - tests/test_error_messages.py
autonomous: true

must_haves:
  truths:
    - "All ValueError raises include the received value and valid options where applicable"
    - "All TypeError raises include the received type name"
    - "Error message format is consistent: lowercase start, f-string with context, no trailing period"
    - "Cycle calculations return degree-valued fields in range [0, 360) with no complex-valued fields in output dtype"
  artifacts:
    - path: "tests/test_error_messages.py"
      provides: "Tests verifying error message format and content"
      min_lines: 40
  key_links:
    - from: "tests/test_error_messages.py"
      to: "ketu/cycles/calculator.py"
      via: "pytest.raises checking message content"
      pattern: "pytest.raises.*ValueError.*match"
---

<objective>
Standardize error messages across all modules and verify complex math integration.

Purpose: Ensure consistent, debuggable error messages (QAL-01) and verify that CPX-01 (complex math internally, degrees externally) is satisfied. Currently error messages vary from bare `f"Unknown body: {body}"` without suggestions to inconsistent capitalization. Users debugging issues need to see what they passed, what's valid, and where to look.

Output: Standardized error messages + verification tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complex-math-integration/05-RESEARCH.md

@ketu/cycles/calculator.py
@ketu/aspects/core.py
@ketu/aspects/timelines.py
@ketu/aspects/calculator.py
@ketu/complex.py
@ketu/ephemeris/planets.py
@ketu/lunar_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize error messages across all modules</name>
  <files>ketu/cycles/calculator.py, ketu/aspects/core.py, ketu/aspects/timelines.py, ketu/aspects/calculator.py, ketu/complex.py, ketu/ephemeris/planets.py, ketu/lunar_calendar.py</files>
  <action>
  Apply the standardized error message template across all modules. Follow Python convention: lowercase start, no trailing period, f-string with context.

  **Template for ValueError (unknown/invalid value):**
  ```python
  raise ValueError(f"unknown body: '{body}'. Valid bodies: {', '.join(valid_list)}")
  ```

  **Template for TypeError:**
  ```python
  raise TypeError(f"timestamps must be numpy array or list, got {type(value).__name__}")
  ```

  **Specific changes by file:**

  1. `ketu/cycles/calculator.py` line 106:
     - From: `raise ValueError(f"Unknown body: {body}")`
     - To: `raise ValueError(f"unknown body: '{body}'. Valid bodies: {', '.join(bodies['name'].astype(str))}")`
     Note: Use `bodies` structured array from ketu.core to get valid names.

  2. `ketu/cycles/calculator.py` line 159:
     - From: `raise ValueError(f"Unsupported timestamp dtype: {timestamps.dtype}")`
     - To: `raise ValueError(f"unsupported timestamp dtype: {timestamps.dtype}. Expected datetime64 ('M') or float ('f')")`

  3. `ketu/ephemeris/planets.py` line 81:
     - From: `raise ValueError(f"Unknown planet ID: {planet_id}")`
     - To: `raise ValueError(f"unknown planet ID: {planet_id}. Valid range: 0-12")`

  4. `ketu/ephemeris/planets.py` line 439:
     - From: `raise ValueError(f"Unknown planet ID: {planet_id}")`
     - To: `raise ValueError(f"unknown planet ID: {planet_id}. Valid range: 0-12")`

  5. `ketu/aspects/core.py` line 49:
     - From: `raise ValueError(f"Unknown aspect name: {aspect}")`
     - To: `raise ValueError(f"unknown aspect name: '{aspect}'. Valid aspects: {', '.join(a.decode() for a in aspects['name'])}")`

  6. `ketu/aspects/core.py` line 56:
     - From: `raise ValueError(f"Unknown aspect angle: {aspect}")`
     - To: `raise ValueError(f"unknown aspect angle: {aspect}. Valid angles: {', '.join(str(a) for a in aspects['angle'])}")`

  7. `ketu/aspects/timelines.py` line 414:
     - From: `raise ValueError(f"Unknown body: {body1}")`
     - To: `raise ValueError(f"unknown body: '{body1}'. Check BODY_INDICES for valid names")`

  8. `ketu/aspects/timelines.py` line 424:
     - From: `raise ValueError(f"Unknown body: {body2}")`
     - To: `raise ValueError(f"unknown body: '{body2}'. Check BODY_INDICES for valid names")`

  9. `ketu/aspects/timelines.py` line 437:
     - From: `raise ValueError(f"Unknown aspect: {aspect}")`
     - To: `raise ValueError(f"unknown aspect: '{aspect}'")`

  10. `ketu/aspects/timelines.py` line 451:
      - From: `raise ValueError(f"Invalid aspect type: {type(aspect)}")`
      - To: `raise ValueError(f"invalid aspect type: {type(aspect).__name__}. Expected str, int, float, or dict")`

  11. `ketu/aspects/calculator.py` line 256:
      - From: `raise ValueError(f"Unknown aspect value: {aspect_value}")`
      - To: `raise ValueError(f"unknown aspect value: {aspect_value}")`

  12. `ketu/complex.py` line 337:
      - From: `raise ValueError(f"Unknown aspect: {aspect}")`
      - To: `raise ValueError(f"unknown aspect: '{aspect}'. Valid aspects: {', '.join(ASPECTS.keys())} or {', '.join(ASPECTS_EXTENDED.keys())}")`
      Note: Be careful — the variable `aspect` at this point might be None (after failed lookup). Check: the code does `aspect = ASPECTS.get(aspect) or ASPECTS_EXTENDED.get(aspect)` and then checks `if aspect is None`. The original string name is lost. Fix by storing the original name first:
      ```python
      if isinstance(aspect, str):
          aspect_name = aspect
          aspect = ASPECTS.get(aspect) or ASPECTS_EXTENDED.get(aspect)
          if aspect is None:
              raise ValueError(f"unknown aspect: '{aspect_name}'. Valid aspects: {', '.join(list(ASPECTS.keys()) + list(ASPECTS_EXTENDED.keys()))}")
      ```

  13. `ketu/complex.py` line 356 (same pattern as above in `is_in_aspect`):
      - Same fix: store original name before lookup, use in error message.

  14. `ketu/complex.py` lines 434, 453, 484, 486: These are already descriptive ("Cannot compute mean of empty sequence", etc.). Leave as-is — they describe the constraint clearly.

  15. `ketu/lunar_calendar.py` lines 141, 174, 230: These are already descriptive with context. Leave as-is.

  **Important:** Do NOT change the logic of any function. Only change the error message strings and the variable capture pattern where needed (complex.py lines 337, 356).
  </action>
  <verify>
  Run: `cd /home/loc/workspace/solaris/ketu && source venv/bin/activate && pytest tests/ -x -q`

  Also verify error messages manually:
  ```python
  python -c "
from ketu.cycles.calculator import _get_body_id
try:
    _get_body_id('InvalidBody')
except ValueError as e:
    print(f'OK: {e}')

from ketu.ephemeris.planets import calc_planet_position
try:
    calc_planet_position(2451545.0, 99)
except ValueError as e:
    print(f'OK: {e}')
  "
  ```
  </verify>
  <done>All error messages follow consistent format: lowercase start, f-string with received value and valid options, no trailing period. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add error message tests and verify CPX-01</name>
  <files>tests/test_error_messages.py</files>
  <action>
  Create `tests/test_error_messages.py` with tests that:

  1. **Error message format tests** (QAL-01):
     - Test `_get_body_id("InvalidPlanet")` raises ValueError with "unknown body" and "Valid bodies"
     - Test `calc_planet_position(jd, 99)` raises ValueError with "unknown planet ID" and "Valid range"
     - Test `get_aspect_index("nonexistent")` raises ValueError with "unknown aspect name"
     - Test `generate_cycle_series` with bad dtype raises ValueError with "unsupported timestamp dtype"

  2. **CPX-01 verification tests** (complex math internal, degrees external):
     - Test that `generate_cycle_series("Sun", "Moon", timestamps)` returns structured array with degree-valued fields:
       - `angular_separation` values are in [0, 360)
       - `body1_lon` and `body2_lon` values are in [0, 360)
       - `cycle_progress` values are in [0, 1)
       - No complex-valued fields in output dtype
     - Test that CYCLE_DTYPE has no complex fields (all 'f4', 'f8', 'i1', 'i2', '?')
     - Test that `cycle_ratio_vectorized` and `complex_to_degrees` are used internally (import check — verify they exist in calculator module namespace)

  Use `pytest.raises(ValueError, match=...)` for error message assertions. Use partial regex matches (not exact strings) for robustness.

  Structure:
  ```python
  """Tests for standardized error messages (QAL-01) and complex math integration (CPX-01)."""
  import pytest
  import numpy as np
  from datetime import datetime, timezone, timedelta

  class TestErrorMessages:
      """Verify error messages follow standardized format."""

      def test_unknown_body_in_cycle_calculator(self):
          ...

      def test_unknown_planet_id(self):
          ...

      def test_unknown_aspect_name(self):
          ...

      def test_unsupported_timestamp_dtype(self):
          ...

  class TestComplexMathIntegration:
      """Verify CPX-01: complex math internal, degrees external."""

      def test_cycle_series_output_in_degrees(self):
          ...

      def test_cycle_dtype_no_complex_fields(self):
          ...

      def test_complex_functions_available_in_calculator(self):
          ...
  ```
  </action>
  <verify>
  Run: `cd /home/loc/workspace/solaris/ketu && source venv/bin/activate && pytest tests/test_error_messages.py -v`

  All tests should pass. Then run full suite: `pytest tests/ -x -q`
  </verify>
  <done>Error message tests verify standardized format. CPX-01 tests confirm complex math is internal only and all outputs are in degrees. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -n "raise ValueError" ketu/cycles/calculator.py ketu/ephemeris/planets.py ketu/aspects/core.py` — all messages lowercase, with context
2. `pytest tests/test_error_messages.py -v` — all error format tests pass
3. `pytest tests/ -x -q` — full test suite passes (241+ tests)
4. No complex-valued fields in CYCLE_DTYPE
</verification>

<success_criteria>
- All error messages follow consistent format (lowercase, f-string, context)
- tests/test_error_messages.py exists with error format and CPX-01 verification tests
- All 241+ existing tests pass (no regressions from message changes)
- CPX-01 verified: cycle output is degrees, complex math is internal
</success_criteria>

<output>
After completion, create `.planning/phases/05-complex-math-integration/05-02-SUMMARY.md`
</output>
