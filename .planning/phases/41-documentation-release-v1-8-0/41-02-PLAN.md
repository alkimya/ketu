---
phase: 41-documentation-release-v1-8-0
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/api.mo
  - docs/locale/fr/LC_MESSAGES/concepts.po
  - docs/locale/fr/LC_MESSAGES/concepts.mo
  - docs/locale/fr/LC_MESSAGES/changelog.po
  - docs/locale/fr/LC_MESSAGES/changelog.mo
  - fr/CHANGELOG.md
autonomous: true
requirements: [DSPD-07]
must_haves:
  truths:
    - "The new v1.8 EN strings (api/concepts/changelog) have French translations in the .po catalogs (no untranslated v1.8 msgstr)"
    - "No private-project name remains in any FR catalog (.po) or fr/CHANGELOG.md"
    - "fr/CHANGELOG.md carries a dated [1.8.0] FR entry mirroring the EN one"
    - "Every edited .po has a freshly recompiled .mo committed (no English fallback)"
    - "EN and FR Sphinx HTML builds complete at the 1-warning baseline (no NEW warnings)"
  artifacts:
    - path: "docs/locale/fr/LC_MESSAGES/concepts.po"
      provides: "FR translations of v1.8 concept content + name-clean"
      contains: "consommateurs en aval"
    - path: "docs/locale/fr/LC_MESSAGES/concepts.mo"
      provides: "recompiled FR concepts catalog"
    - path: "fr/CHANGELOG.md"
      provides: "[1.8.0] FR changelog entry"
      contains: "[1.8.0]"
  key_links:
    - from: "docs/locale/fr/LC_MESSAGES/concepts.po"
      to: "docs/source/concepts.md"
      via: "gettext msgid -> msgstr (sphinx gettext + sphinx-intl update)"
      pattern: "body_decl_speed"
    - from: "docs/locale/fr/LC_MESSAGES/concepts.mo"
      to: "docs/locale/fr/LC_MESSAGES/concepts.po"
      via: "sphinx-intl build recompile"
      pattern: "concepts"
---

<objective>
Translate the new v1.8 documentation into French, name-clean the FR gettext catalogs,
recompile the .mo binaries, and confirm EN and FR Sphinx builds are clean at the
1-warning baseline (D-07 steps 2-3).

Purpose: repo convention (PROJECT.md, every docs phase) is to COMMIT the recompiled .mo —
a .po-without-.mo ships stale English-fallback French docs. concepts.po (4 hits) and
changelog.po (2 hits) also carry private-project names the D-02 sweep must remove.
Depends on 41-01 because the EN msgids must be final before extraction/translation.

Output: French translations for the v1.8 surface; FR catalogs free of private-project
names; fresh .mo committed; fr/CHANGELOG.md [1.8.0] entry; EN+FR builds at the baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-documentation-release-v1-8-0/41-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW translation content + recompiled binaries (excluded from drift verification):
French msgstr for the v1.8 strings in api.po/concepts.po/changelog.po; recompiled .mo
binaries; the [1.8.0] dated FR entry in fr/CHANGELOG.md. The EN source was produced by 41-01.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract new EN strings, translate to French, name-clean the FR catalogs (D-02/D-07)</name>
  <files>docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/changelog.po</files>
  <read_first>
    - `docs/Makefile` — the `gettext` target + `sphinx-intl` path (VENVBIN=../venv/bin)
    - `docs/locale/fr/LC_MESSAGES/concepts.po:888, 897, 1417, 1424` — the "Kala" hits to name-clean
    - `docs/locale/fr/LC_MESSAGES/changelog.po:126, 131` — the "Kala impact"/"Impact Kala" hits
    - `fr/CHANGELOG.md:138, 209, 233` — existing FR phrasing "consommateurs en aval" to reuse
    - `41-CONTEXT.md` D-01 (FR = "consommateurs en aval"), D-02 (FR catalogs in sweep), D-07
  </read_first>
  <action>
    From docs/, run the `gettext` target then `sphinx-intl update -p build/gettext -l fr`
    (venv binaries per docs/Makefile VENVBIN) to add the new v1.8 msgids (api.md table rows,
    DECL_STANDSTILL_EPS / is_ascending_declination_chart sections, concepts.md dδ/dt + Δt +
    standstill + boundary content, the [1.8.0] changelog entry) as untranslated entries. Fill
    the French msgstr for every new v1.8 entry, preserving code identifiers (body_decl_speed,
    DECL_STANDSTILL_EPS, is_ascending_declination_chart), numbers (0.001, 0.01, 16) and
    code/doctest blocks byte-for-byte — translate only prose; use "consommateurs en aval" for
    "downstream consumers". Then name-clean the pre-existing "Kala" hits in concepts.po (~888,
    897, 1417, 1424) and changelog.po (~126, 131) in both msgid and msgstr, keeping substance.
    Preserve celestial-body names Rahu/Ketu/Lilith; frame the boundary principle in FR
    generically (no project names). Do NOT recompile .mo here — that is Task 2.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && test -z "$(grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' docs/locale/fr/LC_MESSAGES/concepts.po docs/locale/fr/LC_MESSAGES/changelog.po)" && grep -q 'body_decl_speed' docs/locale/fr/LC_MESSAGES/concepts.po && grep -q 'consommateurs en aval' docs/locale/fr/LC_MESSAGES/concepts.po</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' concepts.po changelog.po` returns nothing
    - The new v1.8 msgids appear with a non-empty French msgstr
    - "consommateurs en aval" used as the generic replacement; celestial-body names preserved
    - Code identifiers / numbers / code blocks byte-identical to the EN msgid
    - No new v1.8 entry left untranslated (empty msgstr)
  </acceptance_criteria>
  <done>The FR catalogs carry French translations for all v1.8 content and contain no private-project names.</done>
</task>

<task type="auto">
  <name>Task 2: Add [1.8.0] FR changelog, recompile .mo, build EN+FR docs at the 1-warning baseline (D-07)</name>
  <files>fr/CHANGELOG.md, docs/locale/fr/LC_MESSAGES/api.mo, docs/locale/fr/LC_MESSAGES/concepts.mo, docs/locale/fr/LC_MESSAGES/changelog.mo</files>
  <read_first>
    - `fr/CHANGELOG.md:1-45` — the header note + the [1.7.0] FR entry format to mirror
    - `fr/CHANGELOG.md:33, 138, 209, 233` — the "Kala" hits to name-clean while editing
    - `docs/Makefile` — the html target + `sphinx-intl build` recompile step; build EN then FR
    - `41-CONTEXT.md` D-07; `.planning/PROJECT.md` Key Decision "Commit recompiled .mo files"
  </read_first>
  <action>
    Add a dated "## [1.8.0] - DATE" entry to fr/CHANGELOG.md above [1.7.0] mirroring the EN
    [1.8.0] from 41-01 (Ajouts: champ body_decl_speed index 8 de CHART_DTYPE; constante
    publique DECL_STANDSTILL_EPS; helper is_ascending_declination_chart; Notes: version
    MINEURE-pas-correctif, accès par nom sûr, consommateurs positionnels / .view() doivent
    ré-épingler); substitute DATE with today. Name-clean the existing "Kala" hits (~33, 138,
    209, 233) to "consommateurs en aval", preserving substance and celestial-body names. Then
    recompile every edited catalog's .mo from its .po via `sphinx-intl build` (or msgfmt) with
    venv binaries and confirm the .mo are regenerated/staged. Finally build docs from docs/:
    EN (`make html`) and FR (`make html SPHINXOPTS="-D language=fr"`), confirming both complete
    at the 1-warning baseline with no NEW warnings. For any new MyST cross-doc link use the
    explicit-label `[text](#label)` form (PROJECT.md decision) so it resolves in both builds.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && test -z "$(grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' fr/CHANGELOG.md)" && grep -q '\[1.8.0\]' fr/CHANGELOG.md && for m in api concepts changelog; do test docs/locale/fr/LC_MESSAGES/$m.mo -nt docs/locale/fr/LC_MESSAGES/$m.po || exit 1; done && cd docs && make html >/tmp/en.log 2>&1 && make html SPHINXOPTS="-D language=fr" >/tmp/fr.log 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l 'Kala\|...' fr/CHANGELOG.md` returns nothing; a dated [1.8.0] FR entry exists
    - Each edited .mo is newer than its .po (recompiled and staged)
    - `make html` (EN) and `make html SPHINXOPTS="-D language=fr"` (FR) both succeed at the 1-warning baseline with no NEW warnings
    - The FR build renders the v1.8 content in French (no English fallback for the new sections)
  </acceptance_criteria>
  <done>fr/CHANGELOG.md has the name-clean [1.8.0] FR entry, the .mo binaries are recompiled and committed, and both EN+FR docs build clean at the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Translation strings + recompiled gettext binaries + a static docs build. No runtime behaviour or user-controlled input introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | Information disclosure | Private-project names in FR catalogs / rendered FR docs | mitigate | Task 1 name-cleans concepts.po + changelog.po and Task 2 fr/CHANGELOG.md; grep gates return zero. |
| T-41-04 | Tampering | Stale .mo shipping English-fallback French docs | mitigate | Task 2 recompiles every edited .mo and asserts .mo newer than .po; FR build verified at the baseline. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (translation + build only; sphinx/sphinx-intl already present in the venv). |
</threat_model>

<verification>
- `grep -l` over concepts.po / changelog.po / fr/CHANGELOG.md returns zero (D-02 FR sweep)
- New v1.8 msgids carry non-empty French msgstr
- Each edited .mo is newer than its .po (recompiled)
- EN + FR `make html` builds both succeed at the 1-warning baseline
- fr/CHANGELOG.md carries the dated [1.8.0] FR entry
</verification>

<success_criteria>
- v1.8 surface translated to French; FR catalogs name-clean (DSPD-07 FR half)
- Recompiled .mo committed — no English fallback in the FR build (D-07)
- EN+FR docs build clean at the 1-warning baseline (D-07 step 3)
- Dated [1.8.0] FR changelog entry present and name-clean
</success_criteria>

<output>
Create `.planning/phases/41-documentation-release-v1-8-0/41-02-SUMMARY.md` when done
</output>
