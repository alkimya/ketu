---
phase: 41-documentation-release-v1-8-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/synastry/__init__.py
  - ketu/synastry/core.py
  - ketu/aspects/calculator.py
  - ketu/houses/core.py
  - ketu/charts/core.py
  - docs/source/api.md
  - docs/source/concepts.md
  - docs/source/changelog.md
  - CHANGELOG.md
  - UPGRADING.md
autonomous: true
requirements: [DSPD-07]
must_haves:
  truths:
    - "No private-project name (Kala / Solaris / Surya / MarketStream / KetuAdapter / KetuDataAdapter) remains in any shipped/rendered EN artifact"
    - "Celestial-body names Rahu / Ketu / Lilith are still present where they name a body (astronomy terms preserved)"
    - "docs/source/api.md documents body_decl_speed in the CHART_DTYPE table, DECL_STANDSTILL_EPS, and the chart-level is_ascending_declination_chart helper (distinct from the v1.5 scalar)"
    - "docs/source/concepts.md documents dδ/dt meaning, the Δt=0.01 d FD step, the standstill contract, and the no-project-name boundary principle"
    - "CHANGELOG.md and docs/source/changelog.md carry a dated [1.8.0] entry with the MINOR-not-patch rationale"
    - "UPGRADING.md has a v1.7 -> v1.8 entry: dtype grows to 16 fields, named access safe, positional/.view() must adapt, re-pin PyPI — generic phrasing"
    - "All quality gates (numpydoc, interrogate >=95%, make doctest, mypy --strict, full pytest suite, 100% coverage) are green after the docstring edits"
  artifacts:
    - path: "ketu/aspects/calculator.py"
      provides: "name-cleaned docstrings (D-03)"
      contains: "downstream"
    - path: "ketu/charts/core.py"
      provides: "name-cleaned CHART_DTYPE docstring header (D-03)"
      contains: "downstream"
    - path: "docs/source/api.md"
      provides: "body_decl_speed + DECL_STANDSTILL_EPS + is_ascending_declination_chart EN docs"
      contains: "body_decl_speed"
    - path: "docs/source/concepts.md"
      provides: "dδ/dt concept + Δt step + standstill contract + boundary principle"
      contains: "body_decl_speed"
    - path: "CHANGELOG.md"
      provides: "[1.8.0] EN changelog entry"
      contains: "[1.8.0]"
    - path: "UPGRADING.md"
      provides: "v1.7 -> v1.8 migration entry (generic phrasing)"
      contains: "v1.7"
  key_links:
    - from: "docs/source/api.md"
      to: "ketu.charts.is_ascending_declination_chart"
      via: "documented chart-level helper distinct from the v1.5 scalar"
      pattern: "is_ascending_declination_chart"
    - from: "docs/source/concepts.md"
      to: "ketu.calculations.DECL_STANDSTILL_EPS"
      via: "documented standstill contract"
      pattern: "DECL_STANDSTILL_EPS"
---

<objective>
Document the v1.8 declination-speed surface in English and perform the full
name-clean sweep of every shipped/rendered EN artifact, then prove the quality
gates stay green after touching source docstrings.

Purpose: This is the public-library framing pass (D-01..D-06). Ketu must read as a
standalone PyPI library with zero private-project names — the new v1.8 surface docs
and the legacy cleanup both adopt the generic "downstream consumers" phrasing.
Because the cleanup touches `ketu/**.py` docstrings (D-03) that render into the
public Sphinx API docs via autodoc/numpydoc, the BLOCKING CI gates that were green
at Phase 40 close must be re-run and re-confirmed.

Output: name-cleaned source docstrings (13 lines, 5 files); body_decl_speed /
DECL_STANDSTILL_EPS / is_ascending_declination_chart documented in api.md +
concepts.md; the dated [1.8.0] EN changelog; the v1.7 -> v1.8 UPGRADING entry; all
six quality gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-documentation-release-v1-8-0/41-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW documentation content + changelog anchors (excluded from drift / source-grounding
verification — they do not exist before this phase):

- The `[1.8.0]` dated entry in `CHANGELOG.md` and `docs/source/changelog.md` (EN)
- The "v1.7 -> v1.8" section in `UPGRADING.md`
- The `body_decl_speed` (and `body_decl`) rows in the `docs/source/api.md` `CHART_DTYPE` table
- New api.md sections: `DECL_STANDSTILL_EPS` constant + `is_ascending_declination_chart` helper
- New concepts.md content: dδ/dt meaning, Δt=0.01 d FD-step rationale, standstill contract, boundary design principle
- A runnable example exercising `body_decl_speed` / `is_ascending_declination_chart` (D-05)

The v1.8 SYMBOLS being documented (`body_decl_speed`, `DECL_STANDSTILL_EPS`,
`is_ascending_declination_chart`) already exist — they shipped in Phase 40. This plan
only documents them; it does NOT change the calculation.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Name-clean the source docstrings (D-03) and re-run all quality gates</name>
  <files>ketu/synastry/__init__.py, ketu/synastry/core.py, ketu/aspects/calculator.py, ketu/houses/core.py, ketu/charts/core.py</files>
  <read_first>
    - `ketu/synastry/__init__.py:47` — "(Kala adapts to Ketu)"
    - `ketu/synastry/core.py:21, 46, 105` — "Kala (the downstream ML consumer) indexes" / "a Kala feature pipeline" / "(Kala adapts to Ketu)"
    - `ketu/aspects/calculator.py:168, 299, 423, 460, 536` — "preserve Kala contract" / "Downstream consumers (e.g. Kala) rely on this" (x3) / "Kala's positional contract"
    - `ketu/houses/core.py:10` — "downstream consumers (Kala) can"
    - `ketu/charts/core.py:20, 97, 102` — "Kala (the downstream ML consumer) indexes" / "downstream consumers (Kala) indexing by position" / "Downstream positional-offset / .view() consumers (Kala) must adapt"
    - `41-CONTEXT.md` D-01 / D-03 — replacement vocabulary and the celestial-body preservation rule
    - `Makefile` targets `doctest`, `doc-gates`, `mypy`, `test`
  </read_first>
  <action>
    Edit ONLY the 13 lines listed in read_first (the verified inventory). Apply these
    exact replacements, preserving surrounding technical meaning and any doctest
    examples byte-for-byte: "Kala (the downstream ML consumer)" -> "the downstream ML
    consumer"; "(Kala)" / "(e.g. Kala)" -> "(downstream consumers)"; "a Kala feature
    pipeline" -> "a downstream feature pipeline"; "Kala's positional contract" -> "the
    downstream positional contract"; "preserve Kala contract" -> "preserve the
    downstream contract"; "Kala adapts to Ketu" -> "downstream consumers adapt to Ketu".
    Do NOT touch any line that names Rahu, Ketu, or Lilith as a CELESTIAL BODY (ids
    10/11/12) — those astronomy terms stay (D-01 critical distinction). Do NOT edit
    tests/*.py (deferred) or CLAUDE.md (internal, not shipped). After editing, run the
    full gate sweep — these docstrings render into public API docs via autodoc/numpydoc
    (D-03 consequence), so every blocking gate must be re-confirmed green.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && source venv/bin/activate && test "$(grep -rl 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' ketu/ --include='*.py' | wc -l)" = "0" && make doc-gates && make doctest && make mypy && pytest tests/ -q && pytest --cov=ketu --cov-fail-under=100 -q tests/ >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' ketu/ --include='*.py'` returns nothing (zero files)
    - `grep -rc 'Rahu\|Ketu\|Lilith' ketu/core.py` is still > 0 (astronomy body names preserved — purge did not over-reach)
    - `make doc-gates` exits 0 (interrogate >=95% AND numpydoc validate green)
    - `make doctest` exits 0 (all 52+ doctests pass; no doctest example altered)
    - `make mypy` exits 0 (mypy --strict clean)
    - `pytest tests/ -q` exits 0 (full suite, ~1691 tests green)
    - coverage stays at 100% (`--cov-fail-under=100` exits 0)
  </acceptance_criteria>
  <done>The 13 docstring lines carry generic phrasing, no private-project name remains under ketu/, celestial-body names are preserved, and all six quality gates are green.</done>
</task>

<task type="auto">
  <name>Task 2: Name-clean the EN text changelogs + UPGRADING legacy (D-02) and add the v1.7 -> v1.8 UPGRADING entry (D-06)</name>
  <files>CHANGELOG.md, docs/source/changelog.md, UPGRADING.md</files>
  <read_first>
    - `CHANGELOG.md` — "Kala" hits at lines 30, 125, 201, 237, 421, 480, 586, 680, 720 and `KetuDataAdapter` at 681 (sweep to zero)
    - `docs/source/changelog.md` — "Kala" hits at lines 28 and 93
    - `UPGRADING.md` — "Kala"/"KetuDataAdapter" hits at lines 42, 88, 115, 219, 319, 340, 429, 435, 636, 696, 699, 718, 725
    - `UPGRADING.md:96-130` — the v1.4 -> v1.5 `body_decl` entry: the exact structural template (additive dtype change / named-access-safe / positional-must-adapt / verify-snippet) to mirror
    - `41-CONTEXT.md` D-02 (full legacy sweep, keep substance) and D-06 (UPGRADING v1.7->v1.8 content, generic phrasing)
  </read_first>
  <action>
    First swap every private-project name in CHANGELOG.md, docs/source/changelog.md,
    and UPGRADING.md for the generic equivalent per D-02, keeping each entry's
    technical substance identical — only the name changes: "Kala" -> "downstream
    consumers" (or "the downstream ML consumer" where singular reads better); "Kala
    impact"/"Kala guidance" headings -> "Downstream impact"/"Downstream guidance";
    "Kala's KetuDataAdapter"/"KetuDataAdapter" -> "the downstream adapter"; "Kala
    adapts to Ketu" -> "downstream consumers adapt to Ketu". Preserve celestial-body
    names Rahu/Ketu/Lilith. Then ADD the v1.7 -> v1.8 section at the TOP of UPGRADING.md
    (newest-first, above "## v1.6 -> v1.7"), mirroring the v1.4 -> v1.5 body_decl
    template: state CHART_DTYPE grows to 16 fields (body_decl_speed appended at index
    8), MINOR-not-patch because the byte layout grows, named-field access
    (chart["body_lons"]) UNAFFECTED, positional / .view() consumers must adapt and
    re-pin the PyPI version, and include a verify snippet asserting
    chart["body_decl_speed"].shape == (14,). Generic phrasing only — NO project names
    (D-06). Do NOT add the [1.8.0] changelog body here — that is Task 3.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && test -z "$(grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' CHANGELOG.md docs/source/changelog.md UPGRADING.md)" && grep -q 'v1.7' UPGRADING.md && grep -q 'body_decl_speed' UPGRADING.md && grep -Eq '16 fields|16-field' UPGRADING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' CHANGELOG.md docs/source/changelog.md UPGRADING.md` returns nothing (all three clean)
    - UPGRADING.md has a "## v1.7 -> v1.8" section ABOVE "## v1.6 -> v1.7" (newest-first preserved)
    - The new UPGRADING entry mentions body_decl_speed, "16 fields", MINOR, named-access-safe, and positional/.view()-must-adapt
    - The new UPGRADING entry contains a verify snippet asserting `chart["body_decl_speed"].shape == (14,)`
    - Celestial-body names Rahu/Ketu/Lilith still present where they name a body
    - No [1.8.0] changelog body added in this task (deferred to Task 3)
  </acceptance_criteria>
  <done>The three EN text docs are free of private-project names; the v1.7 -> v1.8 UPGRADING entry exists with generic phrasing and a verify snippet.</done>
</task>

<task type="auto">
  <name>Task 3: Document the v1.8 surface in api.md + concepts.md (D-04/D-05) and add the [1.8.0] EN changelog (D-04)</name>
  <files>docs/source/api.md, docs/source/concepts.md, docs/source/changelog.md, CHANGELOG.md</files>
  <read_first>
    - `docs/source/api.md:560-579` — the CHART_DTYPE plain-text field table (STALE: missing both body_decl and body_decl_speed; add both rows)
    - `docs/source/api.md:159-180` — the v1.5 is_ascending_declination(jdate, body) scalar section; put the new chart-level helper near it and contrast the two
    - `docs/source/api.md:925-970` — the β-vs-δ pitfall table + declination subsection (style/depth precedent)
    - `docs/source/concepts.md:410-481` — the v1.5 "Equatorial declination" + "CHART_DTYPE — body_decl field (New in v1.5)" sections: depth/placement template
    - `ketu/charts/core.py:63-103` — the authoritative body_decl_speed field docstring (deg/day, sign = montant/descendant, Δt=0.01 d, compare |value| vs DECL_STANDSTILL_EPS)
    - `ketu/charts/api.py:572-619` — is_ascending_declination_chart signature + int8 {−1,0,+1} return + shape (14,)/S+(14,)
    - `ketu/calculations.py:495-541` — DECL_STANDSTILL_EPS = 0.001 rationale + the declination_velocity Δt=0.01 d example to reuse as the runnable example seed
    - `CHANGELOG.md:1-35` and `docs/source/changelog.md:1-35` — the [1.7.0] entry format (Added/Changed/Notes, dated header) to mirror for [1.8.0]
  </read_first>
  <action>
    In docs/source/api.md: (a) add a body_decl row AND a body_decl_speed row to the
    CHART_DTYPE plain-text table (after body_speeds): "body_decl float64[14] equatorial
    declination δ (°)" and "body_decl_speed float64[14] declination velocity dδ/dt
    (°/day; sign = montant/descendant)"; (b) add a "### DECL_STANDSTILL_EPS" subsection
    stating value 0.001 deg/day, the |dδ/dt| ≤ EPS ⇒ neutral contract, and that it
    lives IN Ketu so consumers invent no threshold; (c) add a
    "### is_ascending_declination_chart(chart)" subsection documenting the int8
    {−1,0,+1} per-body return, shape (14,)/S+(14,), and a bold note it is DISTINCT
    from the v1.5 scalar is_ascending_declination(jdate, body) (bool from (jdate, body)),
    with a short runnable example reading chart["body_decl_speed"] and calling the
    helper (D-05; prefer a doctest-style fenced block consistent with the file).
    In docs/source/concepts.md: add a "CHART_DTYPE — body_decl_speed field (New in
    v1.8)" subsection near the v1.5 body_decl one, explaining dδ/dt meaning (deg/day,
    ↗/↘ reads off the sign, mirrors body_speeds for longitude), the Δt = 0.01 d
    forward-FD rationale (package-wide idiom, not configurable, no new API surface),
    and the DECL_STANDSTILL_EPS standstill contract (≈0 ⇒ neutral "—"). Frame the
    boundary as a LIBRARY DESIGN PRINCIPLE in generic terms (D-04/D-01): "Ketu computes
    all the astronomy (including the finite difference) and the standstill threshold;
    downstream consumers read a field and display it, computing no astronomy of their
    own" — WITHOUT naming any project. In CHANGELOG.md AND docs/source/changelog.md:
    add a dated "## [1.8.0] - <today>" entry above [1.7.0] with an Added section
    (body_decl_speed at CHART_DTYPE index 8; DECL_STANDSTILL_EPS public constant;
    is_ascending_declination_chart helper) and a Notes section giving the
    MINOR-not-patch rationale (dtype layout grows → named access safe, positional/.view()
    consumers must re-pin). Generic phrasing only.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && grep -q 'body_decl_speed' docs/source/api.md && grep -q 'DECL_STANDSTILL_EPS' docs/source/api.md && grep -q 'is_ascending_declination_chart' docs/source/api.md && grep -q 'body_decl_speed' docs/source/concepts.md && grep -Eq 'DECL_STANDSTILL_EPS|standstill' docs/source/concepts.md && grep -q '\[1.8.0\]' CHANGELOG.md && grep -q '\[1.8.0\]' docs/source/changelog.md && test -z "$(grep -l 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' docs/source/api.md docs/source/concepts.md)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/source/api.md CHART_DTYPE table includes a body_decl_speed row (and a body_decl row, closing the prior gap)
    - docs/source/api.md documents DECL_STANDSTILL_EPS and is_ascending_declination_chart, with an explicit "distinct from the v1.5 scalar is_ascending_declination(jdate, body)" note
    - docs/source/concepts.md documents dδ/dt meaning, the Δt=0.01 d FD step, and the standstill contract, with the boundary stated as a generic library-design principle (no project names)
    - CHANGELOG.md and docs/source/changelog.md each carry a dated [1.8.0] entry with Added + MINOR-not-patch Notes
    - The runnable example reads chart["body_decl_speed"] / calls is_ascending_declination_chart (D-05)
    - `grep -l 'Kala|Solaris|...'` over api.md + concepts.md returns nothing
  </acceptance_criteria>
  <done>The v1.8 surface is fully documented in EN (api.md table + constant + helper, concepts.md meaning + step + contract + boundary principle), and the dated [1.8.0] EN changelog exists in both changelog files, all name-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits documentation prose and source-code docstring comments. No runtime behaviour, parsing, I/O, or user-controlled input is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | Source docstrings rendered into public docs | mitigate | Name-clean edits touch docstrings autodoc renders publicly; Task 1 re-runs ALL blocking gates (numpydoc, interrogate, doctest, mypy --strict, full suite, 100% coverage) so a malformed docstring cannot ship silently. |
| T-41-02 | Information disclosure | Private-project names in a public artifact | mitigate | The plan removes private-project names from every shipped/rendered EN artifact; verified by `grep` gates returning zero. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (docs + docstrings only; pure NumPy already present). No supply-chain surface this plan. |
</threat_model>

<verification>
- `grep -rl` over ketu/**.py for private-project names returns zero (D-03 sweep)
- `grep -l` over the three EN text docs returns zero (D-02 sweep)
- `make doc-gates && make doctest && make mypy && pytest tests/ -q` all exit 0
- coverage stays at 100%
- api.md + concepts.md document the v1.8 surface; both changelog files carry [1.8.0]
- UPGRADING.md has the generic v1.7 -> v1.8 entry
</verification>

<success_criteria>
- All private-project names removed from source docstrings + EN docs + EN changelogs + UPGRADING (D-01/D-02/D-03); celestial-body names preserved
- body_decl_speed, DECL_STANDSTILL_EPS, is_ascending_declination_chart documented in EN at the v1.5 body_decl depth (D-04/D-05) — DSPD-07 (EN half)
- Dated [1.8.0] EN changelog + generic v1.7 -> v1.8 UPGRADING entry (D-06)
- All six quality gates green after the docstring edits (D-03 consequence)
</success_criteria>

<output>
Create `.planning/phases/41-documentation-release-v1-8-0/41-01-SUMMARY.md` when done
</output>
