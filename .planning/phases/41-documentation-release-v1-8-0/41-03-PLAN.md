---
phase: 41-documentation-release-v1-8-0
plan: 03
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - pyproject.toml
  - ketu/__init__.py
  - docs/source/conf.py
autonomous: false
requirements: [REL-01]
must_haves:
  truths:
    - "version == 1.8.0 in all THREE source-of-truth files (pyproject.toml, ketu/__init__.py, docs/source/conf.py release+version)"
    - "The human go/no-go relecture-validation checkpoint is honoured BEFORE any irreversible publish action"
    - "ketu==1.8.0 is live on PyPI via OIDC trusted publishing (push main + annotated tag v1.8.0)"
    - "Post-publish fresh-venv smoke FROM PyPI confirms body_decl_speed present + populated, DECL_STANDSTILL_EPS importable, no pyswisseph at runtime"
  artifacts:
    - path: "pyproject.toml"
      provides: "version = 1.8.0"
      contains: "1.8.0"
    - path: "ketu/__init__.py"
      provides: "__version__ = 1.8.0"
      contains: "1.8.0"
    - path: "docs/source/conf.py"
      provides: "release + version = 1.8.0"
      contains: "1.8.0"
  key_links:
    - from: "git tag v1.8.0"
      to: ".github/workflows/publish.yml"
      via: "tag push triggers OIDC publish (on.push.tags v*.*.*)"
      pattern: "v1.8.0"
    - from: "fresh venv"
      to: "pypi.org/project/ketu/1.8.0"
      via: "pip install ketu==1.8.0 FROM PyPI"
      pattern: "body_decl_speed"
---

<objective>
Bump the version to 1.8.0 in the three source-of-truth files, pause for the human
go/no-go gate, then ship ketu==1.8.0 to PyPI via OIDC (push main + tag) and run the
post-publish fresh-venv smoke FROM PyPI (D-07 steps 4-7, D-08).

Purpose: REL-01, the irreversible release. Per the LOCKED per-milestone decision
(PROJECT.md, every milestone since v1.5) the user personally reviews the whole milestone
before tag/publish — auto-publish is unacceptable, so the checkpoint is a HARD STOP.
Depends on 41-01 (EN docs/changelog) and 41-02 (FR + .mo + clean builds).

Output: 1.8.0 across the three files; the honoured go/no-go gate; ketu==1.8.0 live on
PyPI; a green post-publish smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-documentation-release-v1-8-0/41-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW release artifacts (excluded from drift verification): the version-bump diffs in
pyproject.toml / ketu/__init__.py / docs/source/conf.py; the annotated git tag v1.8.0;
the PyPI release ketu==1.8.0 (produced by publish.yml, not a repo file).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump the version to 1.8.0 in all three source-of-truth files</name>
  <files>pyproject.toml, ketu/__init__.py, docs/source/conf.py</files>
  <read_first>
    - `pyproject.toml:7` — `version = "1.7.0"`
    - `ketu/__init__.py:57` — `__version__ = "1.7.0"`
    - `docs/source/conf.py:14-15` — `release = "1.7.0"` AND `version = "1.7.0"`
    - `41-CONTEXT.md` D-07 step 4; `.planning/PROJECT.md` MINOR-not-patch rationale
  </read_first>
  <action>
    Change the version string "1.7.0" -> "1.8.0" in exactly four locations: pyproject.toml
    line 7, ketu/__init__.py line 57, docs/source/conf.py lines 14 (`release`) and 15
    (`version`). Touch nothing else. MINOR bump (not a patch) because CHART_DTYPE grew
    (body_decl_speed at index 8). Do NOT push or tag here.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && test -z "$(grep -l '1\.7\.0' pyproject.toml ketu/__init__.py docs/source/conf.py)" && grep -q '^version = "1.8.0"' pyproject.toml && grep -q '__version__ = "1.8.0"' ketu/__init__.py && grep -q 'release = "1.8.0"' docs/source/conf.py && grep -q '^version = "1.8.0"' docs/source/conf.py</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml line 7 is `version = "1.8.0"`; ketu/__init__.py line 57 is `__version__ = "1.8.0"`
    - docs/source/conf.py lines 14-15 are `release = "1.8.0"` and `version = "1.8.0"`
    - No `1.7.0` remains in any of the three files; no tag or push performed
  </acceptance_criteria>
  <done>All three source-of-truth files declare 1.8.0; no 1.7.0 remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human go/no-go relecture-validation gate (HARD STOP before irreversible publish)</name>
  <files>(none — review-only checkpoint)</files>
  <read_first>
    - `CHANGELOG.md`, `fr/CHANGELOG.md`, `docs/source/changelog.md`, `UPGRADING.md`
    - `41-CONTEXT.md` D-07 step 5; `.planning/PROJECT.md` Key Decisions (locked human go/no-go)
  </read_first>
  <what-built>
    Phase 41 is staged for release: EN docs + name-clean (41-01), FR translations + recompiled
    .mo + clean EN/FR builds (41-02), dated [1.8.0] changelogs (EN+FR), the v1.7 -> v1.8
    UPGRADING entry, and the 1.8.0 version bump. The engine (DSPD-01..06) shipped in Phase 40
    (verifier 6/6, 1691 tests, 100% coverage). Nothing pushed or tagged yet.
  </what-built>
  <how-to-verify>
    1. Review `git diff --stat` and the [1.8.0] entries (CHANGELOG.md, fr/CHANGELOG.md,
       docs/source/changelog.md) + the UPGRADING.md v1.7 -> v1.8 section.
    2. `grep -rl 'Kala\|Solaris\|Surya\|MarketStream\|KetuAdapter\|KetuDataAdapter' ketu/ docs/source/ docs/locale/ CHANGELOG.md fr/CHANGELOG.md UPGRADING.md` returns nothing.
    3. Celestial-body names Rahu/Ketu/Lilith intact.
    4. Spot-check rendered FR docs (v1.8 sections in French, no English fallback).
    5. version == 1.8.0 in the three files.
    Per the LOCKED decision, YOU personally review the whole milestone before the irreversible publish.
  </how-to-verify>
  <action>
    PAUSE. Do NOT push, tag, or publish. Present the milestone for the user's personal
    relecture-validation, then wait for explicit approval before Task 3 runs. No automated
    change — this is a blocking human gate.
  </action>
  <verify>
    <human-check>User replied "approuvé" / "approved" after reviewing the milestone; no push/tag/publish has occurred yet.</human-check>
  </verify>
  <resume-signal>Type "approuvé" / "approved" to authorise the irreversible push + tag + publish, or describe issues to fix first.</resume-signal>
  <done>The user personally reviewed the whole milestone and explicitly approved; nothing was pushed or tagged before approval.</done>
</task>

<task type="auto">
  <name>Task 3: Commit, push main + annotated tag v1.8.0 (OIDC publish), and run the post-publish smoke (D-08)</name>
  <files>(no repo source files — git push + tag + PyPI publish + smoke script)</files>
  <read_first>
    - `.github/workflows/publish.yml` — OIDC trusted publishing, triggered by `on.push.tags v*.*.*`, environment `pypi`, no token
    - `41-CONTEXT.md` D-07 steps 6-7 + D-08 (smoke contents)
    - `.planning/PROJECT.md` Context — "push BOTH main and the tag" (RTD follows main, PyPI follows the tag)
    - `UPGRADING.md` v1.7 -> v1.8 verify snippet (the body_decl_speed shape assertion to reuse)
  </read_first>
  <action>
    ONLY after the Task 2 approval. Commit all Phase 41 work, push main, then create + push an
    annotated tag `v1.8.0` (`git tag -a v1.8.0 -m "..."`) — pushing the tag triggers publish.yml
    -> OIDC publish. Push BOTH main and the tag (RTD follows origin/main, PyPI follows the tag —
    a known prior-release gotcha). Wait for the publish.yml run to succeed and ketu==1.8.0 to
    appear on PyPI. Write a smoke script at
    `.planning/phases/41-documentation-release-v1-8-0/smoke_v18.py` asserting D-08: (a)
    body_decl_speed in np.dtype(CHART_DTYPE).names; (b) a test chart's body_decl_speed finite +
    not all-zero; (c) `from ketu.calculations import DECL_STANDSTILL_EPS` works; (d) `import
    swisseph` raises ImportError (test-only) — print SMOKE_OK on success, sys.exit non-zero
    otherwise. Create a FRESH venv (NOT the project venv), `pip install ketu==1.8.0` FROM PyPI
    into it, and run the smoke script with that venv's python.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/ketu && git ls-remote --tags origin | grep -q 'refs/tags/v1.8.0' && python -m venv /tmp/ketu18smoke && /tmp/ketu18smoke/bin/pip install --no-cache-dir ketu==1.8.0 && /tmp/ketu18smoke/bin/python .planning/phases/41-documentation-release-v1-8-0/smoke_v18.py | grep -q SMOKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --tags origin` shows `refs/tags/v1.8.0`; origin/main pushed
    - publish.yml succeeds; `pip install ketu==1.8.0` from PyPI works in the fresh venv
    - smoke asserts body_decl_speed present + populated, DECL_STANDSTILL_EPS importable, `import swisseph` raises ImportError
    - smoke prints SMOKE_OK
  </acceptance_criteria>
  <done>ketu==1.8.0 is live on PyPI via OIDC; main + tag v1.8.0 pushed; the fresh-venv post-publish smoke passes (SMOKE_OK).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> PyPI | Reviewed source is built and published to a public index; the published artifact must match the human-reviewed source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Tampering | Published artifact vs reviewed source | mitigate | OIDC builds from the exact tagged commit; the Task 2 human go/no-go gate reviews the whole milestone before the tag is pushed; the post-publish smoke installs FROM PyPI and verifies the surface. |
| T-41-06 | Elevation of privilege | PyPI publish credentials | mitigate | publish.yml uses OIDC trusted publishing in the `pypi` environment — no static token to leak; id-token write scoped to the workflow. |
| T-41-SC | Tampering | runtime dependency surface | mitigate | The smoke asserts `import swisseph` FAILS at runtime — confirms pyswisseph stayed test-only and the NumPy-only runtime contract holds in the published wheel. |
</threat_model>

<verification>
- version == 1.8.0 in all three files; no 1.7.0 remains
- Task 2 checkpoint honoured (autonomous: false) before any push/tag
- `git ls-remote --tags origin` shows v1.8.0; origin/main pushed; publish.yml succeeds
- fresh-venv smoke FROM PyPI prints SMOKE_OK (body_decl_speed present + populated, DECL_STANDSTILL_EPS importable, no swisseph at runtime)
</verification>

<success_criteria>
- 1.8.0 in all three source-of-truth files (D-07 step 4) — REL-01
- Human go/no-go gate honoured before the irreversible publish (LOCKED decision)
- ketu==1.8.0 live on PyPI via OIDC (push main + tag) — REL-01
- Post-publish fresh-venv smoke green (D-08) — REL-01 + ROADMAP criterion 4
</success_criteria>

<output>
Create `.planning/phases/41-documentation-release-v1-8-0/41-03-SUMMARY.md` when done
</output>
