---
phase: 02.1-fix-moon-velocity-and-rename-vlong-api
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - ketu/calculations.py
  - ketu/aspects/windows.py
  - ketu/aspects/timelines.py
  - ketu/aspects/transits.py
  - ketu/cycles/calculator.py
  - ketu/export/chart.py
  - tests/test_ketu.py
  - tests/test_direct_new_moon.py
  - tests/test_coverage_improvements.py
  - tests/test_velocity_wrapping.py
  - UPGRADING.md
  - docs/source/api.md
autonomous: true

must_haves:
  truths:
    - "long_velocity(), lat_velocity(), dist_velocity_au() are the public API names"
    - "vlong, vlat, vdist_au no longer exist anywhere in the codebase (removed, not deprecated)"
    - "All internal call sites use the new names"
    - "All tests pass using the new function names"
    - "UPGRADING.md documents the rename with before/after examples"
  artifacts:
    - path: "ketu/calculations.py"
      provides: "Renamed velocity functions and updated __all__"
      contains: "long_velocity"
    - path: "UPGRADING.md"
      provides: "Migration guide for velocity function renames"
      contains: "long_velocity"
  key_links:
    - from: "ketu/aspects/windows.py"
      to: "ketu/calculations.py"
      via: "import long_velocity"
      pattern: "from ketu.calculations import.*long_velocity"
    - from: "ketu/aspects/timelines.py"
      to: "ketu/calculations.py"
      via: "import long_velocity"
      pattern: "from ketu.calculations import.*long_velocity"
    - from: "ketu/calculations.py:is_retrograde()"
      to: "ketu/calculations.py:long_velocity()"
      via: "internal function call"
      pattern: "long_velocity\\(jdate, body\\)"
---

<objective>
Rename vlong/vlat/vdist_au to long_velocity/lat_velocity/dist_velocity_au across the entire codebase.

Purpose: The "v" prefix is ambiguous (could mean "value" or "velocity"), causing real confusion in downstream Kala/Solaris integration where Claude interpreted `vlong` as "longitude value." Since we're heading to 1.0.0, we remove the old names entirely (no deprecation aliases) and document the migration in UPGRADING.md.

Output: All 15 call sites updated to new names, __all__ updated, UPGRADING.md updated, docs updated, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-fix-moon-velocity-and-rename-vlong-api/02.1-RESEARCH.md
@.planning/phases/02.1-fix-moon-velocity-and-rename-vlong-api/02.1-01-SUMMARY.md

@ketu/calculations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename velocity functions and update all call sites</name>
  <files>
    ketu/calculations.py
    ketu/aspects/windows.py
    ketu/aspects/timelines.py
    ketu/aspects/transits.py
    ketu/cycles/calculator.py
    ketu/export/chart.py
    tests/test_ketu.py
    tests/test_direct_new_moon.py
    tests/test_coverage_improvements.py
    tests/test_velocity_wrapping.py
  </files>
  <action>
Rename all three velocity functions. Old names are REMOVED entirely (no deprecation, heading to 1.0.0).

**1. `ketu/calculations.py` — Rename function definitions:**

- `def vlong(` → `def long_velocity(`
- `def vlat(` → `def lat_velocity(`
- `def vdist_au(` → `def dist_velocity_au(`
- Update docstrings to reflect new names (content unchanged, just confirm name in docstring header if any)
- Update `is_retrograde()` body: `vlong(jdate, body)` → `long_velocity(jdate, body)`
- Update `is_ascending()` body: `vlat(jdate, body)` → `lat_velocity(jdate, body)`
- Update `__all__` list: replace `"vlong"` → `"long_velocity"`, `"vlat"` → `"lat_velocity"`, `"vdist_au"` → `"dist_velocity_au"`

**2. Internal module call sites (imports + usage):**

- `ketu/aspects/windows.py` line 23: `vlong` → `long_velocity` (import), lines 307-308: `vlong(` → `long_velocity(`
- `ketu/aspects/timelines.py` line 27: `vlong` → `long_velocity` (import), lines 322-323: `vlong(` → `long_velocity(`
- `ketu/aspects/transits.py` line 31: `vlong` → `long_velocity` (import), line 341: `vlong(` → `long_velocity(`
- `ketu/cycles/calculator.py` line 13: `vlong` → `long_velocity` (import), grep for any `vlong(` calls and update
- `ketu/export/chart.py` line 24: `vlong` → `long_velocity` (import), grep for any `vlong(` calls and update

**3. Test files (imports + usage):**

- `tests/test_ketu.py` line 19: `vlong` → `long_velocity` (import), all `vlong(` calls → `long_velocity(`, update comment at line 149 `# long, lat, dist, vlong, vlat, vdist` → `# long, lat, dist, long_velocity, lat_velocity, dist_velocity`, update comment at line 316, update variable names `sun_vlong` → `sun_long_vel` etc (or keep old variable names — variable names are less critical, just update function calls)
- `tests/test_direct_new_moon.py` line 8: `vlong` → `long_velocity` (import), lines 24-25: `vlong(` → `long_velocity(`
- `tests/test_coverage_improvements.py` line 14: `vdist_au, vlat` → `dist_velocity_au, lat_velocity` (import), line 37: `vdist_au(` → `dist_velocity_au(`, line 43: `vdist_au(` → `dist_velocity_au(`, line 50: `vlat(` → `lat_velocity(`
- `tests/test_velocity_wrapping.py`: update `from ketu.calculations import vlong` → `from ketu.calculations import long_velocity`, update `vlong(` → `long_velocity(` calls, update test name/docstring reference from `vlong` to `long_velocity`

**4. Verify no remaining references:**

Run `grep -r "vlong\|vlat\|vdist_au" ketu/ tests/ --include="*.py"` — should return ZERO matches (excluding comments that say "renamed from vlong" etc).

Commit: `refactor(02.1-02): rename vlong/vlat/vdist_au to explicit velocity names`
  </action>
  <verify>
1. `grep -rn "\bvlong\b\|\bvlat\b\|\bvdist_au\b" ketu/ tests/ --include="*.py"` returns no matches in function definitions, imports, or calls (comments mentioning old names are OK)
2. `pytest tests/ -v` — ALL tests pass
3. `python -c "from ketu.calculations import long_velocity, lat_velocity, dist_velocity_au; print('OK')"` succeeds
4. `python -c "from ketu.calculations import vlong"` raises ImportError
  </verify>
  <done>
All 3 velocity functions renamed. All 15 call sites updated. All internal callers (is_retrograde, is_ascending) updated. __all__ updated. All tests pass. Old names no longer importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UPGRADING.md and API docs with rename</name>
  <files>
    UPGRADING.md
    docs/source/api.md
  </files>
  <action>
**1. UPGRADING.md — Add "Renamed Functions" section** after the "Import Changes" section:

```markdown
## Renamed Functions

### Velocity Functions

The ambiguous `vlong()`, `vlat()`, and `vdist_au()` functions have been renamed to explicit names that clearly indicate they return velocity (speed) values, not position values.

| Old name (v0.4.x) | New name (v1.0.0) | Returns |
|--------------------|-------------------|---------|
| `vlong(jd, body)` | `long_velocity(jd, body)` | Longitude speed (deg/day) |
| `vlat(jd, body)` | `lat_velocity(jd, body)` | Latitude speed (deg/day) |
| `vdist_au(jd, body)` | `dist_velocity_au(jd, body)` | Distance speed (AU/day) |

```python
# v0.4.x (NO LONGER WORKS)
from ketu.calculations import vlong, vlat, vdist_au
moon_speed = vlong(jd, 1)

# v1.0.0 (REQUIRED)
from ketu.calculations import long_velocity, lat_velocity, dist_velocity_au
moon_speed = long_velocity(jd, 1)
```

**Why the rename:** The "v" prefix was ambiguous — it could mean "value" or "velocity." The new names make it explicit that these functions return speed/velocity, not position values.
```

Also update the migration table in "Submodule Reference" to include the new names.

Also add to the "Quick Migration Checklist":
- `[ ] Rename `vlong()` → `long_velocity()`, `vlat()` → `lat_velocity()`, `vdist_au()` → `dist_velocity_au()`

**2. docs/source/api.md — Update API reference:**

- Replace the `### \`vlong()\`, \`vlat()\`, \`vdist_au()\`` heading with `### \`long_velocity()\`, \`lat_velocity()\`, \`dist_velocity_au()\``
- Update the function signatures in the code block
- Update the `body_properties()` return description: `[longitude, latitude, distance, vlong, vlat, vdist]` → `[longitude, latitude, distance, long_velocity, lat_velocity, dist_velocity]`

Commit: `docs(02.1-02): update UPGRADING.md and API docs for velocity rename`
  </action>
  <verify>
1. `grep -n "long_velocity" UPGRADING.md` returns matches (new section exists)
2. `grep -n "long_velocity" docs/source/api.md` returns matches (API docs updated)
3. `grep -n "vlong" UPGRADING.md` returns only in "Old name" column or migration examples, not as current API
4. `grep -n "vlong" docs/source/api.md` returns no matches (fully updated)
  </verify>
  <done>
UPGRADING.md has "Renamed Functions" section with migration table and code examples. API docs show new function names. No stale references to old names in current API documentation.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` — full suite passes
2. `grep -rn "\bvlong\b\|\bvlat\b\|\bvdist_au\b" ketu/ --include="*.py"` — zero matches
3. `python -c "from ketu.calculations import long_velocity, lat_velocity, dist_velocity_au"` — succeeds
4. `python -c "from ketu.calculations import vlong"` — ImportError
5. UPGRADING.md contains rename migration section
6. docs/source/api.md uses new function names
</verification>

<success_criteria>
All velocity functions use explicit `_velocity` naming. No references to old vlong/vlat/vdist_au names remain in source code. All tests pass. Documentation updated for external users.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-fix-moon-velocity-and-rename-vlong-api/02.1-02-SUMMARY.md`
</output>
