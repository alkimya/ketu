---
phase: 38-fictitious-point-orbs-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/core.py
  - ketu/aspects/calculator.py
  - tests/aspects/test_node_opposition_filter.py
autonomous: true
requirements: [ORB-01, ORB-02]
must_haves:
  truths:
    - "get_orb for Rahu, Ketu, or Lilith conjunction returns 2.0"
    - "get_orb for Rahu↔Sun conjunction returns 7.0 ((2+12)/2)"
    - "get_orb for Rahu↔Lilith conjunction returns 2.0 ((2+2)/2)"
    - "The vectorized, batch, calculate_aspects, and scalar get_aspect paths never emit a (Rahu, Ketu) Opposition row regardless of separation"
    - "Rahu↔Ketu conjunction, Rahu↔Sun opposition, and Ketu↔Lilith conjunction are still detected normally"
    - "_is_tautological_node_opposition is order-insensitive and keys on canonical i_asp == 13"
  artifacts:
    - path: "ketu/core.py"
      provides: "Rahu/Ketu/Lilith orb field = 2 in core.bodies (rows id 10/11/12)"
      contains: "(\"Rahu\", 10, 2,"
    - path: "ketu/aspects/calculator.py"
      provides: "_is_tautological_node_opposition pure helper + 3 module constants + call sites in _detect_aspects_for_date (x2), get_aspect, calculate_aspects (x2)"
      contains: "_is_tautological_node_opposition"
    - path: "tests/aspects/test_node_opposition_filter.py"
      provides: "Dedicated helper unit test + per-path integration tests (vectorized, batch, calculate_aspects, get_aspect)"
      contains: "_is_tautological_node_opposition"
  key_links:
    - from: "ketu/aspects/calculator.py::_detect_aspects_for_date"
      to: "_is_tautological_node_opposition"
      via: "guard on results.append in both static and dynamic emit loops"
      pattern: "_is_tautological_node_opposition"
    - from: "ketu/aspects/calculator.py::get_aspect"
      to: "_is_tautological_node_opposition"
      via: "returns None when helper is True (after body1>body2 swap)"
      pattern: "_is_tautological_node_opposition"
    - from: "ketu/core.py::bodies"
      to: "get_orb / synastry._BODY_ORBS_16 / cycles / composite / CLI"
      via: "data-driven read of bodies['orb']"
      pattern: "bodies\\[.orb.\\]"
---

<objective>
Land the v1.7 aspect-engine core: give Rahu (id 10), Ketu (id 11), and Lilith (id 12) a 2° orb in the single-source `core.bodies` table (ORB-01), and introduce ONE shared pure helper `_is_tautological_node_opposition` that suppresses the intra-chart Rahu↔Ketu Opposition (ORB-02). The helper is called from every natal emit path (the shared `_detect_aspects_for_date` chokepoint that backs both `calculate_aspects_vectorized` and `calculate_aspects_batch`, the slow public `calculate_aspects`, and the scalar `get_aspect`), but is NOT called from synastry. A dedicated unit test plus per-path integration tests pin both the suppress branch and the keep branches.

Purpose: ORB-01 + ORB-02 are the behavioural heart of the milestone — every downstream oracle rewrite (Plan 02) depends on this orb value and filter being in place. Concentrating the single-source data edit, the single-source helper, and the helper's own tests in one plan keeps the source-of-truth property (D-01) verifiable in isolation before the regression sweep runs.
Output: edited `ketu/core.py` (3 rows), edited `ketu/aspects/calculator.py` (1 new helper + 3 constants + up to 5 guarded emit sites), and new `tests/aspects/test_node_opposition_filter.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md
@.planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md
@ketu/core.py
@ketu/aspects/calculator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ORB-01 — flip Rahu/Ketu/Lilith orb 0→2 in core.bodies</name>
  <files>ketu/core.py</files>
  <read_first>
    - ketu/core.py lines 70-87 (the `bodies` structured array and its dtype)
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md section "ketu/core.py (model, ORB-01 edit site)"
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md decision ORB-01 (single-source edit)
  </read_first>
  <behavior>
    - get_orb(10, 10, 0) == 2.0 and get_orb(11, 11, 0) == 2.0 and get_orb(12, 12, 0) == 2.0 (point↔point conjunction orb)
    - get_orb(10, 0, 0) == 7.0 (Rahu↔Sun conjunction = (2+12)/2)
    - get_orb(10, 12, 0) == 2.0 (Rahu↔Lilith conjunction = (2+2)/2)
    - get_orb(13, 13, 0) == 4.0 (Chiron UNCHANGED — verify the planet rows were not touched)
    - get_orb(0, 0, 0) == 12.0 (Sun UNCHANGED)
  </behavior>
  <action>
    In `ketu/core.py`, in the `bodies` structured array, change the `orb` field (the 3rd tuple position, dtype `("orb", "f4")`) from `0` to `2` in EXACTLY these three rows: `("Rahu", 10, 0, -0.052954)` → `("Rahu", 10, 2, -0.052954)` (line 81), `("Ketu", 11, 0, -0.052954)` → `("Ketu", 11, 2, -0.052954)` (line 82), `("Lilith", 12, 0, 0.113)` → `("Lilith", 12, 2, 0.113)` (line 83). Do NOT touch the `id` (2nd position) or `speed` (4th position) fields, and do NOT touch any other row — Chiron (id 13, orb 4, line 84), Sun..Pluto (lines 71-80) stay frozen. This is the ONLY orb edit in the entire phase per D-01: `get_orb`, `synastry._BODY_ORBS_16`, cycles, composite, and CLI all read `bodies["orb"]` data-driven and inherit the change with zero further edits. Do not add or modify any consumer.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python -c "from ketu.aspects.calculator import get_orb; assert get_orb(10,10,0)==2.0 and get_orb(11,11,0)==2.0 and get_orb(12,12,0)==2.0 and get_orb(10,0,0)==7.0 and get_orb(10,12,0)==2.0 and get_orb(13,13,0)==4.0 and get_orb(0,0,0)==12.0; print('ORB-01 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner above prints `ORB-01 OK` and exits 0.
    - `grep -E '\("Rahu", 10, 2,|\("Ketu", 11, 2,|\("Lilith", 12, 2,' ketu/core.py` returns exactly 3 matches.
    - `grep -E '\("Chiron", 13, 4,' ketu/core.py` still returns 1 match (Chiron untouched).
    - No edits to any file other than ketu/core.py in this task.
  </acceptance_criteria>
  <done>The three fictitious-point rows carry orb 2; all planet rows and Chiron are unchanged; get_orb returns the data-driven values above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORB-02 — define the shared helper and write its dedicated unit test (RED→GREEN)</name>
  <files>ketu/aspects/calculator.py, tests/aspects/test_node_opposition_filter.py</files>
  <read_first>
    - ketu/aspects/calculator.py lines 88-167 (`_detect_aspects_for_date` emit loops, `get_orb` as the pure-helper analog to copy: full type hints + numpydoc)
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md section "ketu/aspects/calculator.py" (helper signature, constants, Kala canonical-i_asp contract) and section "NEW tests (helper unit + per-path integration)"
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md decisions D-01 (single shared pure helper), D-02 (canonical Rahu=10, Ketu=11, Opposition i_asp=13)
    - tests/synastry/test_orbs.py lines 1-110 (one-assertion-per-behaviour test style + numpydoc one-liner docstrings)
  </read_first>
  <behavior>
    - _is_tautological_node_opposition(10, 11, 13) is True (canonical Rahu→Ketu Opposition)
    - _is_tautological_node_opposition(11, 10, 13) is True (order-insensitive — get_aspect may pass swapped order)
    - _is_tautological_node_opposition(10, 11, 0) is False (Rahu↔Ketu conjunction must still emit)
    - _is_tautological_node_opposition(10, 0, 13) is False (Rahu↔Sun opposition must still emit)
    - _is_tautological_node_opposition(10, 11, -2) is False (dynamic rows carry i_asp == -2; structurally exempt)
    - _is_tautological_node_opposition(np.int32(10), np.int32(11), 13) is True (NumPy scalar args from body1_ids[idx] coerce correctly)
  </behavior>
  <action>
    Add three module-level constants near the top of the calculator helpers (no magic numbers): `_RAHU_ID = 10`, `_KETU_ID = 11`, `_OPPOSITION_IASP = 13` (canonical index into `core.aspects`, last row). Define a module-level pure function `_is_tautological_node_opposition(body1: int, body2: int, i_asp: int) -> bool` modelled on `get_orb`'s structure (full type hints, numpydoc Parameters/Returns — the numpydoc gate is BLOCKING). It returns `False` when `i_asp != _OPPOSITION_IASP`; otherwise it coerces both body args with `int(...)` (args may be `np.int32` from `body1_ids[idx]`, needed for `mypy --strict`) and returns True iff the pair equals `(_RAHU_ID, _KETU_ID)` in either order. It is `_`-prefixed/private, so do NOT add it to `__all__` (lines ~812-820 stay unchanged). Create `tests/aspects/test_node_opposition_filter.py` (add `tests/aspects/__init__.py` only if the package convention requires it — check sibling test dirs) with one test function per behaviour above (numpydoc one-liner docstrings, `-> None` annotations), covering all branches so the helper is 100% covered before any call site exists. Run the test file to confirm GREEN.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; pytest tests/aspects/test_node_opposition_filter.py -q &amp;&amp; mypy --strict ketu/aspects/calculator.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/aspects/test_node_opposition_filter.py -q` exits 0 with the new helper-unit tests passing.
    - `python -c "from ketu.aspects.calculator import _is_tautological_node_opposition as f; assert f(10,11,13) and f(11,10,13) and not f(10,11,0) and not f(10,0,13) and not f(10,11,-2); print('helper OK')"` prints `helper OK`.
    - `mypy --strict ketu/aspects/calculator.py` exits 0.
    - `grep -c '_OPPOSITION_IASP = 13' ketu/aspects/calculator.py` returns 1.
    - The helper is NOT present in `__all__`: `grep -A12 '__all__' ketu/aspects/calculator.py | grep -c _is_tautological_node_opposition` returns 0.
  </acceptance_criteria>
  <done>The shared helper exists, is fully unit-tested (suppress + all keep branches + order-insensitivity + np.int32 coercion), is mypy-strict clean, and is private (absent from __all__).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ORB-02 — wire the helper into all natal/scalar emit paths + per-path integration tests</name>
  <files>ketu/aspects/calculator.py, tests/aspects/test_node_opposition_filter.py</files>
  <read_first>
    - ketu/aspects/calculator.py lines 109-145 (`_detect_aspects_for_date` static loop lines 126-132 + dynamic loop lines 138-143), lines 170-197 (`get_aspect` with the body1>body2 swap at 188-189), lines 200-311 (`calculate_aspects` slow path, static appends at 286/290, dynamic at 303-305)
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md sections "Call site 1/2/3", the "Note for calculate_aspects (lines 200-311)" paragraph, and the "Canonical i_asp contract (Kala)" shared pattern
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md decisions D-02 (both emit sites), D-03 (get_aspect filters; order-insensitive after swap) and the PATTERN-MAPPER flag about calculate_aspects
    - tests/test_aspects_vectorization.py lines 39-76 (detection-result assertion idiom: sort by body1, body2, i_asp then assert on fields)
  </read_first>
  <behavior>
    - calculate_aspects_vectorized over a date where Rahu and Ketu are 180° apart emits NO (10, 11, 13) row, but a Rahu↔Sun opposition (10, 0, 13) when in orb IS present
    - calculate_aspects_batch over the same date(s) likewise drops (10, 11, 13)
    - calculate_aspects (slow public path) over the same date drops (10, 11, 13) and matches the vectorized result for the Rahu/Ketu pair
    - get_aspect(jdate, 10, 11) returns None when the separation is an Opposition; get_aspect(jdate, 11, 10) (swapped) also returns None
    - get_aspect(jdate, 10, 11) returns a conjunction tuple if Rahu and Ketu are ever within 2° conjunction orb (keep branch); get_aspect for a real Rahu↔Sun opposition returns the (10, 0, 13, ...) tuple (keep branch)
  </behavior>
  <action>
    DECISION (deliberate, per the PATTERN-MAPPER flag and D-03's consistency rationale): guard ALL public natal/scalar emit paths so no public Ketu path emits the tautological artefact. Insert the helper call at four logical sites, all keyed on the CANONICAL `i_asp` (NOT the selected-subset loop position `k`):
    (1) `_detect_aspects_for_date` static emit loop (lines 126-132): before `results.append(...)`, skip the append when `_is_tautological_node_opposition(int(body1_ids[idx]), int(body2_ids[idx]), i_asp)` is True. Opposition is i_asp 13 (last static row) so no other static aspect competes for the slot; you may still add the pair to `matched_pairs` so nothing re-emits, OR rely on the fact that no lower-priority static aspect matches the same separation — choose the placement that preserves the existing first-match-wins contract and document the choice in the SUMMARY.
    (2) `_detect_aspects_for_date` dynamic emit loop (lines 138-143): add the same guard for uniformity (D-01 single-source). Dynamic rows pass `-2`, so the helper returns False — document inline that dynamic rows are structurally exempt.
    (3) `get_aspect` (lines 188-197): after the `body1 > body2` swap, in the Opposition-matching branch, return `None` when `_is_tautological_node_opposition(body1, body2, i_asp)` is True (the helper is order-insensitive regardless).
    (4) `calculate_aspects` slow path (lines 200-311): guard the two static `aspects_data.append` sites (lines 286 and 290) with the same helper keyed on canonical `i_asp`; when suppressed, do NOT append and do NOT `break` for the conjunction case, but for the Opposition case treat it like the vectorized path (the row is simply not emitted). The dynamic append (303-305) carries -2 and is exempt.
    Add integration tests to `tests/aspects/test_node_opposition_filter.py` (one per path: vectorized, batch, calculate_aspects, get_aspect) using a Julian date where Rahu↔Ketu separation is an Opposition AND a real Rahu↔Sun opposition fires, so the keep branches are exercised — not just the suppress branch. Use the sort-then-assert idiom from tests/test_aspects_vectorization.py.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; pytest tests/aspects/test_node_opposition_filter.py -q &amp;&amp; mypy --strict ketu/aspects/calculator.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/aspects/test_node_opposition_filter.py -q` exits 0 with all four per-path integration tests passing.
    - `grep -c '_is_tautological_node_opposition' ketu/aspects/calculator.py` returns >= 5 (1 definition + 4 call sites; dynamic-loop guard makes it >= 5).
    - Behavior: for the chosen date, `calculate_aspects_vectorized`, `calculate_aspects_batch`, and `calculate_aspects` results contain NO row with `(body1, body2, i_asp) == (10, 11, 13)`, but DO contain the Rahu↔Sun opposition `(10, 0, 13)` when in orb; `get_aspect(jd, 10, 11)` and `get_aspect(jd, 11, 10)` both return None for that opposition.
    - `mypy --strict ketu/aspects/calculator.py` exits 0.
  </acceptance_criteria>
  <done>All four public natal/scalar paths are guarded by the single shared helper keyed on canonical i_asp; every path drops the (10,11,13) Opposition while keeping all other Rahu/Ketu/Lilith detections; integration tests pin both suppress and keep branches; mypy --strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — runtime) | Pure-NumPy library: no I/O, no network, no untrusted input, no auth. Inputs are numeric body IDs and Julian dates produced internally. The only meaningful boundary is the **data-correctness contract** with the downstream consumer Kala (canonical i_asp positional contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering (correctness-as-safety) | `_is_tautological_node_opposition` keying on `i_asp` | mitigate | An off-by-one on `_OPPOSITION_IASP` (e.g. 12 instead of 13) would silently suppress the WRONG aspect across the Kala contract. Mitigated by the module constant `_OPPOSITION_IASP = 13`, the dedicated unit test asserting `(10,11,13)→True` AND `(10,11,0)→False`, and the per-path integration test asserting the Rahu↔Sun opposition `(10,0,13)` and Rahu↔Ketu conjunction `(10,11,0)` are still emitted. |
| T-38-02 | Information Disclosure / Denial (silent data loss) | helper false-positive suppression | mitigate | A helper that suppressed a legitimate non-tautological detection = silent data loss for consumers. Mitigated by the keep-branch tests: `(11,10,13)→True` only for the node pair, and `(10,0,13)→False`, `(10,11,0)→False`, `(10,11,-2)→False` all asserted; integration tests verify a real Rahu↔Sun opposition survives. |
| T-38-03 | Tampering | `core.bodies` orb edit scope | mitigate | An accidental edit to a planet/Chiron orb would silently change frozen aspect math. Mitigated by Task 1's acceptance grep (exactly 3 matched rows; Chiron orb 4 still 1 match) and the get_orb assertions for Sun (12.0) and Chiron (4.0). |

No HIGH-severity threats: the surface is a pure-function library with no external attack vector. Per ASVS L1, the realistic risk is data-integrity/correctness toward the Kala consumer, which is fully covered by the unit + integration tests above. No blocking-human verification task (the type used for package-legitimacy gates) is required because no package installs occur (no new dependencies).
</threat_model>

<verification>
- `pytest tests/aspects/test_node_opposition_filter.py -q` exits 0.
- `mypy --strict ketu/aspects/calculator.py ketu/core.py` exits 0.
- `python -c` orb assertions (Task 1) and helper assertions (Task 2) print their OK markers.
- `grep -c '_is_tautological_node_opposition' ketu/aspects/calculator.py` >= 5.
- Full-suite gates (`pytest tests/` 0 failures, 100% coverage, mypy --strict over the whole package) are NOT asserted in this plan — they are the responsibility of Plan 02 after the regression sweep, because the orb change deliberately breaks ~31 existing oracle files that Plan 02 rewrites.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 satisfied: get_orb returns 2.0 for the points, 7.0 for Rahu↔Sun, 2.0 for Rahu↔Lilith.
- ROADMAP Success Criterion 2 satisfied: no path emits (Rahu, Ketu) + Opposition; every other Rahu/Ketu aspect is detected.
- ORB-01 and ORB-02 implemented as single-source edits (one core.py change, one shared helper).
- Helper is unit-tested in isolation and integration-tested on all four public natal/scalar paths.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols created by this plan (exclude from drift/source-grounding verification — they do not exist in the v1.6 baseline):
- `ketu/aspects/calculator.py::_is_tautological_node_opposition(body1: int, body2: int, i_asp: int) -> bool` — new private pure helper.
- `ketu/aspects/calculator.py::_RAHU_ID` (= 10), `_KETU_ID` (= 11), `_OPPOSITION_IASP` (= 13) — new module-level constants.
- `tests/aspects/test_node_opposition_filter.py` — NEW test module containing: the helper unit tests (one per branch) and the four per-path integration tests (vectorized, batch, calculate_aspects, get_aspect). All test function names in this file are newly created.
- (possibly) `tests/aspects/__init__.py` — only if the test-package convention requires it.

No new CLI flags. No struct/dataclass field additions. The `core.bodies` orb VALUES change (0→2 on rows 10/11/12) but the dtype is unchanged.
</artifacts_produced>

<output>
Create `.planning/phases/38-fictitious-point-orbs-engine/38-01-SUMMARY.md` when done. In it, record the exact Julian date used for the integration tests, and document the chosen `matched_pairs` placement decision for the suppressed static slot (Task 3 site 1).
</output>
