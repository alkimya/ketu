---
phase: 38-fictitious-point-orbs-engine
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - tests/synastry/test_orbs.py
  - tests/synastry/test_modes_idempotent.py
  - tests/synastry/fixtures/oracle_curie.json
  - tests/charts/test_aspect_matrix.py
  - tests/composite/test_oracle.py
  - tests/declination/test_find_aspects.py
  - tests/test_cycles_calculator.py
  - tests/cli/fixtures/v1_1_reference_output.txt
  - tests/cli/fixtures/harmonics_h7_reference_output.txt
autonomous: true
requirements: [ORB-03]
must_haves:
  truths:
    - "synastry_orb_limit(10,10,0), (11,11,0), (12,12,0) each return 1.0 (was 0.0), pinned in test_orbs.py"
    - "Any point↔planet / point↔point synastry orb-limit oracle is recomputed at the new 2° orb (e.g. Rahu↔Sun conjunction limit = 3.5)"
    - "tests/synastry/test_modes_idempotent.py diagonal invariants (16 self-pairs, all conjunctions, |orb|<1e-6) remain unchanged and still pass"
    - "Every test file referencing Rahu/Ketu/Lilith is green; new node aspects that appear are pinned deliberately, never silently accepted"
    - "pytest tests/ reports 0 failures, 100% coverage (fail_under=100), mypy --strict clean over the whole package"
  artifacts:
    - path: "tests/synastry/test_orbs.py"
      provides: "Rewritten self-pair orb-limit oracles 0.0→1.0 + recomputed point-involving oracles + refreshed docstrings"
      contains: "synastry_orb_limit(10, 10, 0) == 1.0"
    - path: "tests/synastry/test_modes_idempotent.py"
      provides: "Unchanged diagonal invariants (optionally refreshed stale docstring, assertions NOT weakened)"
      contains: "aspect_type == 0"
  key_links:
    - from: "tests/synastry/test_orbs.py"
      to: "ketu.synastry.orbs.synastry_orb_limit"
      via: "oracle assertions at the new 2° orb"
      pattern: "synastry_orb_limit"
    - from: "tests/ (full suite)"
      to: "ketu.core.bodies['orb'] + _is_tautological_node_opposition"
      via: "regression sweep re-pins every changed detection"
      pattern: "pytest"
---

<objective>
Make the entire test suite green against the new 2° orb and the Rahu↔Ketu Opposition filter from Plan 01. This is the ORB-03 work: rewrite the synastry orb-limit oracles (D-07) deliberately, keep the idempotent-modes diagonal invariants unchanged (D-06), and run the full ~31-file regression sweep so EVERY changed detection (new node aspects appearing at 2°, CLI snapshot shifts, oracle/fixture drift) is pinned by a deliberate human-readable assertion update — never silently accepted. The plan completes only when `pytest tests/` is 0 failures, coverage is 100%, and `mypy --strict` is clean.

Purpose: ORB-03 mandates "no silent oracle update." Plan 01 changed behaviour; this plan reconciles every dependent oracle to that behaviour with intent, documenting which detections newly appear (e.g. a Rahu/Lilith aspect now in 2° orb) versus which are unchanged. Synastry is explicitly NOT filtered (D-04/D-05) — its oracle changes are purely the 2° orb math.
Output: rewritten synastry test oracles, a refreshed idempotent docstring, recomputed CLI reference snapshots, and any other oracle/fixture files the sweep proves stale — all under `tests/` only (no `ketu/` source edits in this plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md
@.planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md
@.planning/phases/38-fictitious-point-orbs-engine/38-01-SUMMARY.md
@tests/synastry/test_orbs.py
@tests/synastry/test_modes_idempotent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 — rewrite synastry orb-limit oracles to the new 2° orb</name>
  <files>tests/synastry/test_orbs.py</files>
  <read_first>
    - tests/synastry/test_orbs.py FULLY (current oracle rows, especially lines 1-7 file docstring, 51-54 _BODY_ORBS_16 mirror test, 90-106 the three self-pair zero-orb tests, and any point-involving orb-limit oracle below line 109)
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md section "tests/synastry/test_orbs.py (test — oracle rewrite, D-07)"
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md decisions D-05 (synastry NOT filtered — pure orb math) and D-07 (this file owns the orb-limit value change)
    - ketu/synastry/orbs.py (synastry_orb_limit formula = natal formula × SYNASTRY_FACTOR 0.5; confirm the math)
  </read_first>
  <action>
    Rewrite the three self-pair oracles so each asserts the new value: `synastry_orb_limit(10, 10, 0) == 1.0`, `synastry_orb_limit(11, 11, 0) == 1.0`, `synastry_orb_limit(12, 12, 0) == 1.0` (new math: `(2+2)/2 × coef_conj(1) × factor(0.5) = 1.0`). Rename the three test functions away from `_zero_orb` (now misleading — e.g. `_two_degree_orb`) and rewrite their docstrings, which currently assert "zero natal orb"/"Rahu has zero natal orb in bodies['orb']" (lines 91-106) — that rationale is now FALSE; replace it with the new 2° rationale. Update the file-level docstring (lines 1-7) which cites "Rahu-Rahu = 0 deg" as a pinned ratchet → "Rahu-Rahu = 1 deg". Then scan the rest of the file for ANY point↔planet or point↔point orb-limit oracle (Rahu/Ketu/Lilith = ids 10/11/12) and recompute it at the new orb — e.g. a Rahu↔Sun conjunction limit becomes `(2+12)/2 × 1 × 0.5 = 3.5`. Do NOT edit the `_BODY_ORBS_16` canonical-mirror test (lines 51-54): it asserts `_BODY_ORBS_16[:14] == _BODIES["orb"]` structurally and tracks the new 2° automatically — leaving it proves data-driven propagation. Synastry is NOT filtered (D-04/D-05), so do not add any Rahu↔Ketu suppression assertion here.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; pytest tests/synastry/test_orbs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/synastry/test_orbs.py -q` exits 0.
    - `grep -c 'synastry_orb_limit(10, 10, 0) == 1.0\|synastry_orb_limit(11, 11, 0) == 1.0\|synastry_orb_limit(12, 12, 0) == 1.0' tests/synastry/test_orbs.py` returns 3 (modulo exact spacing — confirm all three flipped to 1.0).
    - `grep -c '== 0.0' tests/synastry/test_orbs.py` shows the three node self-pair `== 0.0` assertions are GONE (no orphaned zero-orb node oracle remains).
    - The `_BODY_ORBS_16` mirror test (lines ~51-54) is unedited and still passes.
    - No `tests/synastry/test_orbs.py` change references Rahu↔Ketu Opposition suppression (D-05 — synastry is not filtered).
  </acceptance_criteria>
  <done>The three node self-pair orb-limit oracles assert 1.0 with renamed functions and corrected docstrings; the file docstring is corrected; every point-involving orb-limit oracle is recomputed at 2°; the data-driven mirror test is untouched; test_orbs.py is green.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 — verify idempotent invariants hold; refresh stale rationale only</name>
  <files>tests/synastry/test_modes_idempotent.py</files>
  <read_first>
    - tests/synastry/test_modes_idempotent.py FULLY (especially the pinned block lines 107-131: 16 self-pair rows, aspect_type == 0, |orb| < 1e-6, and the rationale docstring lines 112-118)
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md section "tests/synastry/test_modes_idempotent.py (test — UNCHANGED, D-06)"
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md decision D-06 (keep invariants unchanged; may refresh stale docstring; MUST NOT weaken assertions)
  </read_first>
  <action>
    Run the file first to confirm the diagonal invariants still pass at 2° (they should: on a self-pair `dist == 0`, so the emitted orb `delta = -dist` stays 0 regardless of the now-non-zero orb_limit; the non-zero limit only reinforces "always conjunction"). Do NOT change any assertion: keep the 16 self-pair rows, `aspect_type == 0`, `|orb| < 1e-6`. The ONLY permitted edit is refreshing the now-stale docstring at lines 112-118 which says "Rahu/Ketu/Lilith have zero natal orbs" — update that prose to reflect the new 2° orb while keeping the same invariant rationale (self-pair dist==0 → orb stays 0). Do NOT add new assertions here (D-07 owns the orb-limit value change, in test_orbs.py). If the file passes unchanged, a docstring refresh is still recommended for accuracy but the assertions are the contract.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; pytest tests/synastry/test_modes_idempotent.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/synastry/test_modes_idempotent.py -q` exits 0.
    - The three invariants are intact: `grep -c 'aspect_type == 0' tests/synastry/test_modes_idempotent.py` is unchanged from baseline (>= 1), and the `|orb| < 1e-6` / 16-self-pair assertions are NOT weakened or removed.
    - Any prose mentioning "zero natal orb" for Rahu/Ketu/Lilith is either removed or corrected to the 2° reality (no stale rationale left).
  </acceptance_criteria>
  <done>The idempotent-modes diagonal invariants pass unchanged at 2°; only stale docstring rationale (if any) is refreshed; no assertion is weakened or added.</done>
</task>

<task type="auto">
  <name>Task 3: ORB-03 regression sweep — re-pin every changed detection; full-suite gates green</name>
  <files>tests/charts/test_aspect_matrix.py, tests/composite/test_oracle.py, tests/declination/test_find_aspects.py, tests/test_cycles_calculator.py, tests/cli/fixtures/v1_1_reference_output.txt, tests/cli/fixtures/harmonics_h7_reference_output.txt, tests/synastry/fixtures/oracle_curie.json</files>
  <read_first>
    - .planning/phases/38-fictitious-point-orbs-engine/38-PATTERNS.md section "Regression Sweep (planner-scoped)" (likely-affected oracle/fixture families to audit first)
    - .planning/phases/38-fictitious-point-orbs-engine/38-CONTEXT.md "Claude's Discretion" (sweep strategy is yours; the locked constraint is every changed detection deliberately pinned + final pytest tests/ 0 failures / 100% coverage / mypy --strict clean)
    - .planning/phases/38-fictitious-point-orbs-engine/38-01-SUMMARY.md (what Plan 01 changed, the integration date used)
    - pyproject.toml / setup.cfg coverage config (fail_under=100, exclude_lines) to understand the coverage gate
    - Each failing test/fixture file in full BEFORE editing it, so the new value is computed from the actual engine output, not assumed
  </read_first>
  <action>
    Run `pytest tests/` and `mypy --strict ketu/ tests/` to enumerate the actual breakage (the v1.6 baseline was 1654 tests green; the 2° orb + filter deliberately breaks several oracle/fixture/snapshot files). Work the failures file by file. The likely-affected families to audit first (PATTERNS.md): `tests/synastry/fixtures/*.json` + the synastry oracle tests, `tests/composite/test_oracle.py`, `tests/declination/test_find_aspects.py`, `tests/test_cycles_calculator.py`, `tests/charts/test_aspect_matrix.py`, and the byte-stable CLI snapshots `tests/cli/fixtures/*reference_output.txt` (new node aspects will appear, so the snapshots SHIFT). For each failure: read the current oracle, regenerate the expected value FROM the live engine output (e.g. capture the new CLI stdout, the new aspect-matrix rows, the new detection arrays), confirm the change is explained by either (a) a new in-2°-orb Rahu/Ketu/Lilith aspect appearing, or (b) the suppressed Rahu↔Ketu Opposition disappearing — and ONLY then update the oracle/fixture/snapshot, with a comment or commit note recording WHY it changed. Never blanket-regenerate without verifying each delta is a legitimate, expected consequence (no silent acceptance — ORB-03 mandate). If a delta is NOT explainable by (a) or (b), STOP and surface it as a possible engine bug rather than re-pinning it. The `files_modified` list is the expected set from PATTERNS.md; if the sweep proves additional files stale, edit them too and note the addition in the SUMMARY (do not edit any `ketu/` source — this plan is test-only; a source change would mean Plan 01 was incomplete and must be revisited). Finish by confirming the three hard gates.
  </action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; pytest tests/ &amp;&amp; mypy --strict ketu/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/` exits 0 with 0 failures (the report shows >= 1654 tests; new helper/filter tests add to the count).
    - Coverage is 100%: `pytest tests/ --cov=ketu --cov-fail-under=100` (or the project's configured `make test`/coverage invocation) exits 0; no new `# pragma: no cover` was introduced.
    - `mypy --strict ketu/ tests/` exits 0.
    - For every modified oracle/fixture/snapshot, the SUMMARY records the specific reason the value changed (new in-orb node aspect appeared, or Rahu↔Ketu Opposition suppressed) — no silent oracle update.
    - No file under `ketu/` was edited in this plan (git diff shows only `tests/` changes).
  </acceptance_criteria>
  <done>The full suite is green at 0 failures, 100% coverage, mypy --strict clean; every changed oracle/fixture/CLI snapshot is deliberately re-pinned with a documented reason; no source file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test-only) | This plan edits ONLY test oracles/fixtures/snapshots. No production code, no I/O at runtime, no untrusted input. The relevant "boundary" is the **regression-guarantee contract**: the test suite is the safety net proving the Plan 01 engine change did not silently corrupt any other detection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering / Repudiation (silent oracle drift) | regression-sweep oracle/fixture/snapshot updates | mitigate | The core ORB-03 risk: blindly regenerating oracles to "make it green" would mask a real engine regression (silent data corruption for Kala). Mitigated by Task 3's per-delta rule — every changed value must be explainable by (a) a new in-2°-orb node aspect or (b) the suppressed Rahu↔Ketu Opposition, with the reason recorded in the SUMMARY; unexplainable deltas must be surfaced as possible bugs, not re-pinned. |
| T-38-05 | Tampering | accidental `ketu/` source edit during sweep | mitigate | Editing source to force a green suite would hide that Plan 01 was incomplete. Mitigated by the "test-only, git diff shows only tests/ changes" acceptance criterion. |
| T-38-06 | Information Disclosure (coverage erosion) | new `# pragma: no cover` to dodge the 100% gate | mitigate | Mitigated by the "no new pragma" acceptance criterion and the `--cov-fail-under=100` gate; the negative branches of the Plan 01 helper are already covered by Plan 01's integration tests. |

No HIGH-severity threats: this is a test-only reconciliation of a pure-NumPy library with no external attack surface. Per ASVS L1, the realistic risk is integrity of the regression guarantee (no silent oracle drift), fully addressed by the per-delta verification rule. No package installs occur, so no blocking-human legitimacy checkpoint is required.
</threat_model>

<verification>
- `pytest tests/synastry/test_orbs.py -q` exits 0 (Task 1).
- `pytest tests/synastry/test_modes_idempotent.py -q` exits 0 with unchanged invariants (Task 2).
- `pytest tests/` exits 0, 0 failures (Task 3).
- Coverage 100% (fail_under=100), no new pragmas.
- `mypy --strict ketu/ tests/` exits 0.
- `git diff --name-only` shows only files under `tests/`.
</verification>

<success_criteria>
- ROADMAP Success Criterion 3 satisfied: test_orbs.py and test_modes_idempotent.py pass with oracles rewritten to the new 2° orb; no oracle update is silent.
- ROADMAP Success Criterion 4 satisfied: all test files referencing Rahu/Ketu/Lilith are green; `pytest tests/` 0 failures, 100% coverage maintained, mypy --strict clean.
- ORB-03 fully implemented: synastry inherits the new orb (data-driven, no synastry source edit), oracles rewritten deliberately, full regression sweep green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

This plan creates NO new production symbols. It produces:
- Rewritten/renamed test functions in `tests/synastry/test_orbs.py` (the former `*_zero_orb` node self-pair tests, renamed to reflect the 2° orb) — these are new function names but test-only.
- Refreshed docstring text in `tests/synastry/test_modes_idempotent.py` (no new assertions).
- Recomputed oracle/fixture/snapshot VALUES across the regression-sweep files (`tests/cli/fixtures/*reference_output.txt`, `tests/synastry/fixtures/*.json`, composite/declination/cycles/aspect-matrix oracles). These are data-value changes, not new symbols.

No new CLI flags, no struct/dataclass fields, no `ketu/` source changes. The only new code symbols in this PHASE are those declared in Plan 01's artifacts section (the helper, its constants, and the new test module).
</artifacts_produced>

<output>
Create `.planning/phases/38-fictitious-point-orbs-engine/38-02-SUMMARY.md` when done. List EVERY oracle/fixture/snapshot file that changed and, for each, the one-line reason (new in-orb node aspect appeared / Rahu↔Ketu Opposition suppressed). Confirm the final `pytest tests/` count, coverage %, and mypy --strict status.
</output>
