---
phase: 13-doc-gates-and-ci-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - pyproject.toml
  - ketu/__init__.py
  - ketu/__main__.py
  - ketu/core.py
  - ketu/complex.py
  - ketu/calculations.py
  - ketu/display.py
  - ketu/cycles/__init__.py
  - ketu/cycles/calculator.py
  - ketu/ephemeris/__init__.py
autonomous: true
requirements:
  - OPS-02
tags:
  - ops
  - docstrings
  - numpydoc
  - source
must_haves:
  truths:
    - "Le bloc `[tool.numpydoc_validation]` est présent dans pyproject.toml avec les checks `\"all\"` + suppressions `EX01 SA01 ES01 GL01` et l'`exclude` regex pour `lunar_calendar` + `_*`."
    - "Toutes les docstrings publiques de `ketu/complex.py`, `ketu/calculations.py`, `ketu/cycles/calculator.py`, `ketu/__init__.py`, `ketu/__main__.py`, `ketu/core.py`, `ketu/display.py`, `ketu/cycles/__init__.py`, `ketu/ephemeris/__init__.py` passent `python -m numpydoc lint` sous la config above."
    - "Les sections non-standard de `__init__.py` et `core.py` (Submodules, Precision Guarantees, Coordinate Transformations, Body IDs, Data Structures) sont repliées en sous-paragraphes en gras sous une seule section `Notes`."
    - "Les 250 tests existants restent verts après le fix-pass (zéro changement de signature, zéro changement de comportement)."
    - "`mypy --strict` reste clean après le fix-pass."
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.numpydoc_validation] config block"
      contains: "[tool.numpydoc_validation]"
    - path: "ketu/complex.py"
      provides: "numpydoc-clean docstrings (124 issues fixed)"
      contains: "Parameters"
    - path: "ketu/calculations.py"
      provides: "numpydoc-clean docstrings (68 issues fixed)"
      contains: "Parameters"
    - path: "ketu/cycles/calculator.py"
      provides: "numpydoc-clean docstrings (22 issues fixed)"
      contains: "Parameters"
    - path: "ketu/__init__.py"
      provides: "non-standard sections folded into Notes (GL06/GL07 fixes)"
      contains: "Notes"
    - path: "ketu/core.py"
      provides: "non-standard sections folded into Notes (GL06/GL07 fixes)"
      contains: "Notes"
  key_links:
    - from: "pyproject.toml [tool.numpydoc_validation].exclude"
      to: "ketu/lunar_calendar.py and _* private modules"
      via: "regex strings in exclude list"
      pattern: '\\.lunar_calendar\$|\\\\._'
    - from: "pyproject.toml [tool.numpydoc_validation].checks"
      to: "EX01 SA01 ES01 GL01 suppressions"
      via: "string literals in checks list"
      pattern: '"GL01"'
---

<objective>
Add the `[tool.numpydoc_validation]` config block to pyproject.toml with the SciPy-community-default suppressions (EX01, SA01, ES01) plus GL01 (suppressed during the warning phase per RESEARCH § "Note on GL01 suppression" Option A), then fix every remaining numpydoc issue across the 9 audited modules so that `python -m numpydoc lint <files>` produces ZERO output.

Purpose: closes OPS-02 baseline locally. Plan 04 wires the gate to CI (warning posture) only after this plan proves the gate is already clean. Per D-08 (audit-then-fix in this phase, not deferred), every gap identified by the audit is fixed here — not papered over with broader suppressions.

Output: pyproject.toml has new `[tool.numpydoc_validation]` block; 9 source files have numpydoc-clean docstrings; `python -m numpydoc lint $(find ketu -name "*.py" ! -name "lunar_calendar.py" ! -name "_*.py")` produces ZERO output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/13-doc-gates-and-ci-foundation/13-CONTEXT.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-VALIDATION.md

@.planning/phases/13-doc-gates-and-ci-foundation/13-01-SUMMARY.md

<interfaces>
<!-- Audit baselines (live audit 2026-05-08 from RESEARCH.md). -->
<!-- Per-module issue counts BEFORE the fix-pass (with EX01/SA01/ES01 suppressed; GL01 NOT YET suppressed in audit): -->

124 ketu/complex.py
 68 ketu/calculations.py
 22 ketu/cycles/calculator.py
  6 ketu/__init__.py
  5 ketu/display.py
  3 ketu/core.py
  1 ketu/__main__.py
  1 ketu/ephemeris/__init__.py
  1 ketu/cycles/__init__.py

<!-- Issue category breakdown (also from RESEARCH): -->
<!-- - GL01 (~59 hits): summary on same line as `"""`. SUPPRESSED during warning phase per Option A; fix mechanically in Phase 20. -->
<!-- - GL06/GL07 (~9 hits): non-standard section names in __init__.py + core.py. FOLD into Notes section. -->
<!-- - GL08 (~4 hits): truly missing docstrings on dunders in complex.py. ADD one-line summaries. -->
<!-- - PR01 (~10 hits): parameter documented but not in signature OR not documented. ADD missing Parameters or fix mismatch. -->
<!-- - RT01 (~4 hits): missing Returns section on non-None returners. ADD Returns. -->
<!-- - SS05 (~3 hits): summary not starting with infinitive verb. APPLIES to dataclass docstrings; use override_SS05. -->
<!-- - Misc residue (~138 hits): mix of small numpydoc-style violations resolved by targeted edits. -->

<!-- Canonical numpydoc shape used elsewhere in this codebase (PATTERNS.md § "Shared Pattern 4"): -->
<!-- ketu/houses/_ecliptic.py:ra_to_lambda (lines 15-47) and ketu/houses/placidus.py:_iterate_cusp_ra (lines 114-150). -->
```python
def function_name(arg1: T1, arg2: T2) -> ReturnT:
    """One-line imperative summary ending with a period.

    Optional extended description.

    Parameters
    ----------
    arg1 : T1
        Description of arg1.
    arg2 : T2
        Description of arg2.

    Returns
    -------
    name : ReturnT
        Description of the return value.

    Notes
    -----
    Optional. Use for non-trivial caveats or to fold non-standard
    sections that would otherwise trigger GL06/GL07.
    """
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `[tool.numpydoc_validation]` config block to pyproject.toml and confirm baseline counts</name>
  <files>pyproject.toml</files>

  <read_first>
    - pyproject.toml (full file — `[tool.coverage.run].omit` lines 78-84 for exclusion-list shape; `[tool.mypy]` block lines 96-120 for global-config + per-module-overrides shape)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md § "pyproject.toml — new `[tool.numpydoc_validation]` block" (concrete TOML provided)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md § "Recommended `[tool.numpydoc_validation]` block" + § "Note on GL01 suppression" (Option A vs B — Option A is the locked recommendation)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-CONTEXT.md § Decisions D-06, D-07, D-08, D-09, AND D-14 (GL01 deferral exception — formalizes the `"GL01"` suppression below)
  </read_first>

  <action>
Append the new `[tool.numpydoc_validation]` block to pyproject.toml. Place it AFTER `[tool.interrogate]` (landed by Plan 01) and BEFORE `[tool.mypy]` for narrative grouping (all quality-tooling configs together).

Use this EXACT block (verbatim from PATTERNS.md § "pyproject.toml — new `[tool.numpydoc_validation]` block" and RESEARCH § "Recommended `[tool.numpydoc_validation]` block"):

```toml
[tool.numpydoc_validation]
checks = [
    "all",   # report every check
    "EX01",  # ignore: no examples section required (internal helpers)
    "SA01",  # ignore: no See Also required
    "ES01",  # ignore: no extended summary required
    "GL01",  # ignore during warning phase (Phase 13–19); fix and remove in Phase 20
]
exclude = [
    '\.lunar_calendar$',
    '\._',
]
override_SS05 = [
    '^Aspect$',
    '^ZodiacPoint$',
    '^CycleRatio$',
]
```

Notes:
- The `checks = ["all", ...]` syntax in numpydoc means "run all checks EXCEPT these" — counter-intuitive, but verified live in the audit. (See RESEARCH § Common Pitfall 1 + 2.)
- `GL01` suppression IS load-bearing for this phase: there are ~59 GL01 hits (summary on same line as `"""`) clustered in `complex.py` + `calculations.py`. Option A (suppress during warning phase, fix in Phase 20 as one mechanical sed pass) is the locked recommendation, formalized as **D-14** in CONTEXT.md (the SOLE permitted exception to D-08; does NOT extend to GL08, PR01, RT01, or any substantive content code). Do NOT remove the `"GL01"` line.
- `exclude` uses regex strings (NOT module paths) — this is the numpydoc API. `\.lunar_calendar$` matches the suffix; `\._` matches anything starting with underscore (D-06).
- `override_SS05` for the 3 dataclass-style names (Aspect, ZodiacPoint, CycleRatio) is the structural analog of `[[tool.mypy.overrides]]` (PATTERNS.md § "pyproject.toml — new `[tool.numpydoc_validation]` block" notes).

After the edit, CONFIRM the audit baseline holds:

```bash
# Pre-fix baseline (before Tasks 2–4 land):
python -m numpydoc lint $(find ketu -name "*.py" \
    ! -path "*/__pycache__/*" \
    ! -name "lunar_calendar.py" \
    ! -name "_*.py") 2>&1 | grep -oE "ketu/[^ :]+\.py" | sort | uniq -c | sort -rn
```

Expected output (matches RESEARCH.md audit, but reduced by ~59 lines because GL01 is now suppressed by config):
```
   ~65 ketu/complex.py
   ~30 ketu/calculations.py
   ~10 ketu/cycles/calculator.py
    ~6 ketu/__init__.py
    ~5 ketu/display.py
    ~3 ketu/core.py
    ~1 ketu/__main__.py
    ~1 ketu/ephemeris/__init__.py
    ~1 ketu/cycles/__init__.py
```

(Approximate because GL01 was a substantial fraction of complex.py + calculations.py issues. Tasks 2–4 will drive the count to 0.)
  </action>

  <verify>
    <automated>cd /home/loc/workspace/ketu &amp;&amp; source venv/bin/activate &amp;&amp; grep -c '^\[tool.numpydoc_validation\]' pyproject.toml &amp;&amp; python -c "import tomllib; d = tomllib.loads(open('pyproject.toml').read()); cfg = d['tool']['numpydoc_validation']; assert 'GL01' in cfg['checks'], cfg; assert any('lunar_calendar' in e for e in cfg['exclude']), cfg; print('OK', cfg)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^\[tool.numpydoc_validation\]' pyproject.toml` returns exactly `1`.
    - `python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exits 0 (TOML still parses).
    - `python -c "import tomllib; cfg = tomllib.loads(open('pyproject.toml').read())['tool']['numpydoc_validation']; print(cfg['checks'])"` includes `'GL01'`, `'EX01'`, `'SA01'`, `'ES01'`, `'all'`.
    - The `exclude` list contains regex strings matching `lunar_calendar` and `_` (e.g. `\.lunar_calendar$` and `\._`).
    - `override_SS05` contains all three of `^Aspect$`, `^ZodiacPoint$`, `^CycleRatio$`.
    - `python -m interrogate ketu/` still passes (Plan 01 baseline holds — adding numpydoc config does not affect interrogate).
    - Tests still green: `python -m pytest tests/ -q --no-cov` exits 0.
  </acceptance_criteria>

  <done>
The `[tool.numpydoc_validation]` block matches PATTERNS.md verbatim, TOML parses, the per-file numpydoc baseline is computed and recorded for Tasks 2–4 to chip away at.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix numpydoc issues in complex.py + calculations.py + cycles/calculator.py (the bulk fix-pass — ~105 issues post-GL01-suppression)</name>
  <files>ketu/complex.py, ketu/calculations.py, ketu/cycles/calculator.py</files>

  <read_first>
    - ketu/complex.py (full file — current state of all docstrings; ~65 issues post-GL01-suppression)
    - ketu/calculations.py (full file — current state of all docstrings; ~30 issues)
    - ketu/cycles/calculator.py (full file — current state of all docstrings; ~10 issues)
    - ketu/houses/_ecliptic.py (full file — canonical numpydoc shape per PATTERNS.md § "Shared Pattern 4")
    - ketu/houses/placidus.py lines 114-150 (`_iterate_cusp_ra` — second canonical reference)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md § "ketu/complex.py, ketu/calculations.py, ketu/cycles/calculator.py — numpydoc fixes (bulk)" (4 pattern categories described)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md § "Common Pitfalls" (Pitfall 2 RT01, Pitfall 3 GL06)
  </read_first>

  <action>
Work file-by-file (RESEARCH § "Plan 3 vs Plan 4 scope split" — single Plan 3 covering all files; chunk by file inside the plan to keep merge surface small).

**Step 1 — Snapshot the baseline (record this in the SUMMARY):**
```bash
python -m numpydoc lint ketu/complex.py 2>&1 | tee /tmp/13-03-complex-before.log
python -m numpydoc lint ketu/calculations.py 2>&1 | tee /tmp/13-03-calc-before.log
python -m numpydoc lint ketu/cycles/calculator.py 2>&1 | tee /tmp/13-03-cycles-before.log
```

**Step 2 — Fix `ketu/complex.py` (~65 issues):**
Apply the 4 pattern categories from PATTERNS.md:
- **GL08 (truly missing docstrings, ~4 hits on dunders like `__post_init__`, `__repr__`):** Add one-line summaries. Example: `"""Validate aspect orb is positive after dataclass construction."""`. These overlap with interrogate's `ignore-magic = true` so interrogate doesn't see them — but numpydoc does. 30 seconds per dunder.
- **PR01 (parameter documented but not in signature, OR not documented):** For each hit, either (a) add the missing `Parameters` block following the canonical shape (see `_ecliptic.py:ra_to_lambda` lines 22-34), or (b) remove the stale parameter doc that no longer matches the signature.
- **RT01 (missing Returns section on non-None returners):** Add a `Returns` section. Use named returns (`name : ReturnT\n    description.`) — Sophie-flavored. Per RESEARCH § Pitfall 2: do NOT blanket-suppress RT01 globally.
- **SS05 (summary not infinitive):** For dataclasses (`Aspect`, `ZodiacPoint`, `CycleRatio`) the override in `[tool.numpydoc_validation].override_SS05` already handles it. For other functions, rewrite the summary to start with an infinitive verb (e.g. "Compute…", "Calculate…", "Return…", "Validate…").

**Step 3 — Fix `ketu/calculations.py` (~30 issues):**
Same pattern categories. The audit showed `calculations.py` is the second-largest issue cluster — most are PR01 (incomplete Parameters) and RT01 (missing Returns) on the high-level calculation API. The functions in this module are public-facing (per ketu/__init__.py re-exports) so docstring quality matters more here than in helpers.

**Step 4 — Fix `ketu/cycles/calculator.py` (~10 issues):**
Same pattern. Smaller module; expect most issues to be PR01 + RT01 with a couple of SS05.

**Step 5 — Verify each file CLEAN:**
```bash
python -m numpydoc lint ketu/complex.py        # MUST be empty
python -m numpydoc lint ketu/calculations.py    # MUST be empty
python -m numpydoc lint ketu/cycles/calculator.py  # MUST be empty
```

**Constraints (enforced — DO NOT violate):**
- ZERO signature changes (no parameter rename, no parameter add/remove, no return-type change). This is a non-breaking minor (per ROADMAP cross-cutting constraints).
- ZERO behavior changes (no logic edits, no constant changes). Docstrings are runtime no-ops outside `__doc__` access.
- All 250 existing tests stay green throughout. Run `pytest tests/ -q --no-cov` after EACH file's fix-pass.
- Mypy strict stays clean: `python -m mypy ketu/ --strict` after each file.
- Use the canonical numpydoc shape from `ketu/houses/_ecliptic.py` and `ketu/houses/placidus.py:_iterate_cusp_ra` as the reference dialect — same indentation (4 spaces), same section underline length (matches the section name length), same parameter-line format (`name : Type` with single space around the colon).
- Do NOT suppress new numpydoc codes (no edits to `[tool.numpydoc_validation].checks` beyond what Task 1 set). If a code is hard to fix, REWRITE the docstring rather than suppress.

**Anti-patterns to avoid (per PATTERNS.md):**
- Do NOT suppress RT01 globally. Per-function rewrites only.
- Do NOT add custom section names. Use the numpydoc allow-list (Parameters, Returns, Yields, Raises, Notes, Examples, See Also). Custom-section content goes inside `Notes` with bold sub-paragraphs.
- Do NOT shrink docstrings to one-liners just to pass the gate. The substantive checks (PR01, RT01) catch real gaps; fix the gap, don't hide it.
  </action>

  <verify>
    <automated>cd /home/loc/workspace/ketu &amp;&amp; source venv/bin/activate &amp;&amp; python -m numpydoc lint ketu/complex.py ketu/calculations.py ketu/cycles/calculator.py 2&gt;&amp;1 | tee /tmp/13-03-task2-after.log &amp;&amp; test ! -s /tmp/13-03-task2-after.log &amp;&amp; python -m pytest tests/ -q --no-cov &amp;&amp; python -m mypy ketu/ --strict</automated>
  </verify>

  <acceptance_criteria>
    - `python -m numpydoc lint ketu/complex.py` produces ZERO output (exit 0 with empty stdout).
    - `python -m numpydoc lint ketu/calculations.py` produces ZERO output.
    - `python -m numpydoc lint ketu/cycles/calculator.py` produces ZERO output.
    - All 250 existing tests pass: `python -m pytest tests/ -q --no-cov` exit 0.
    - `python -m mypy ketu/ --strict` exit 0.
    - `python -m interrogate ketu/` still passes (no docstring DELETIONS).
    - `git diff ketu/complex.py ketu/calculations.py ketu/cycles/calculator.py | grep '^[+-]' | grep -v '^[+-]\s*"""' | grep -v '^[+-]\s*#' | grep -E '^[+-]\s*(def |class |return |[A-Z_]+\s*=)' | wc -l` is `0` — i.e. NO non-docstring lines were added/removed (signatures + bodies untouched).
    - The number of issues fixed in this task ≈ 105 (matches the per-file audit baseline minus GL01 which is now suppressed). Record the actual count in the SUMMARY.
  </acceptance_criteria>

  <done>
The 3 highest-issue source files are numpydoc-clean under the locked config. Tests and mypy still pass. No signature or behavior changes anywhere. Diff is docstring-only.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix numpydoc issues in __init__.py + core.py + display.py + __main__.py + cycles/__init__.py + ephemeris/__init__.py (the GL06/GL07 + tail fix-pass)</name>
  <files>ketu/__init__.py, ketu/__main__.py, ketu/core.py, ketu/display.py, ketu/cycles/__init__.py, ketu/ephemeris/__init__.py</files>

  <read_first>
    - ketu/__init__.py (full file — non-standard sections "Submodules", "Precision Guarantees", "Coordinate Transformations", "Body IDs", "Data Structures" trigger GL06/GL07)
    - ketu/core.py (full file — non-standard "Data Structures" section, lines 7-34 sample)
    - ketu/display.py (full file — ~5 issues)
    - ketu/__main__.py (full file — 1 issue)
    - ketu/cycles/__init__.py (full file — 1 issue)
    - ketu/ephemeris/__init__.py (full file — 1 issue)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md § "ketu/complex.py, ketu/calculations.py, ketu/cycles/calculator.py — numpydoc fixes (bulk)" Pattern category 2 (GL06/GL07 — fold into Notes) and Pattern category 4 (GL08)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md § "Common Pitfalls" Pitfall 3 (GL06 on legacy section names)
  </read_first>

  <action>
Six smaller files; the work is concentrated in `__init__.py` (6 issues) and `core.py` (3 issues), all of category GL06/GL07 (non-standard section names). The other 4 files have 1–5 issues each, a mix of PR01/RT01/SS05.

**Step 1 — Snapshot the baseline:**
```bash
python -m numpydoc lint ketu/__init__.py ketu/__main__.py ketu/core.py \
    ketu/display.py ketu/cycles/__init__.py ketu/ephemeris/__init__.py \
    2>&1 | tee /tmp/13-03-task3-before.log
```

**Step 2 — Fix `ketu/__init__.py` and `ketu/core.py` (GL06/GL07 — fold non-standard sections into Notes):**

Current shape in `ketu/__init__.py` (PATTERNS.md confirms this is the issue):
```
Submodules
----------
ketu.core
    Astronomical constants...

Precision Guarantees
--------------------
- Angular separation: ±1e-6° ...
```

Numpydoc-compliant shape (PATTERNS.md § Pattern category 2 + RESEARCH § Pitfall 3 — the project's accepted dialect):
```
Notes
-----
**Submodules**

ketu.core
    Astronomical constants...

**Precision Guarantees**

- Angular separation: ±1e-6° ...
```

Apply the same transform for every non-standard section in `__init__.py` ("Submodules", "Precision Guarantees", "Coordinate Transformations", "Body IDs", "Data Structures") — fold them all into a SINGLE `Notes` section with bold sub-paragraphs. The existing `Examples` section (numpydoc-allow-listed) stays untouched.

For `core.py`, do the same transform on its "Data Structures" section.

**Step 3 — Fix `ketu/display.py` (~5 issues):**
Mixed PR01/RT01/SS05 — apply the same patterns as Task 2 (canonical numpydoc shape from `_ecliptic.py:ra_to_lambda`). Display module functions are public-facing (called from CLI) so docstrings should be polished.

**Step 4 — Fix `ketu/__main__.py`, `ketu/cycles/__init__.py`, `ketu/ephemeris/__init__.py` (1 issue each):**
Single issue per file — likely PR01 or RT01 on the module docstring or a single function. Apply targeted fix per the canonical shape.

**Step 5 — Verify each file CLEAN:**
```bash
python -m numpydoc lint ketu/__init__.py ketu/__main__.py ketu/core.py \
    ketu/display.py ketu/cycles/__init__.py ketu/ephemeris/__init__.py
# MUST produce ZERO output
```

**Constraints (enforced — same as Task 2):**
- ZERO signature changes, ZERO behavior changes — docstring edits ONLY.
- Re-exports in `__init__.py` MUST stay untouched (don't accidentally delete a re-export when restructuring the docstring).
- All 250 existing tests stay green throughout.
- Mypy strict stays clean.
- Use `Notes` section with bold sub-paragraphs (`**Submodules**`, `**Precision Guarantees**`, etc.) — NOT custom section underlines (which would re-trigger GL06/GL07).
  </action>

  <verify>
    <automated>cd /home/loc/workspace/ketu &amp;&amp; source venv/bin/activate &amp;&amp; python -m numpydoc lint ketu/__init__.py ketu/__main__.py ketu/core.py ketu/display.py ketu/cycles/__init__.py ketu/ephemeris/__init__.py 2&gt;&amp;1 | tee /tmp/13-03-task3-after.log &amp;&amp; test ! -s /tmp/13-03-task3-after.log &amp;&amp; python -m pytest tests/ -q --no-cov &amp;&amp; python -m mypy ketu/ --strict</automated>
  </verify>

  <acceptance_criteria>
    - `python -m numpydoc lint ketu/__init__.py ketu/__main__.py ketu/core.py ketu/display.py ketu/cycles/__init__.py ketu/ephemeris/__init__.py` produces ZERO output.
    - `grep -E "^(Submodules|Precision Guarantees|Coordinate Transformations|Body IDs|Data Structures)$" ketu/__init__.py ketu/core.py` returns NO match — the non-standard section headings are gone (replaced by `**Submodules**` etc. inside `Notes`).
    - `grep -c "^Notes$" ketu/__init__.py` returns at least `1` (the new `Notes` section exists).
    - `grep -c "^Notes$" ketu/core.py` returns at least `1`.
    - `grep -E "\*\*(Submodules|Precision Guarantees|Coordinate Transformations|Body IDs|Data Structures)\*\*" ketu/__init__.py` returns at least one match per renamed section (bold sub-paragraphs in place).
    - `python -c "import ketu; ketu.__doc__"` exits 0 — the docstring is still importable and parses cleanly.
    - All 250 tests pass.
    - mypy strict still clean.
    - `python -m interrogate ketu/` still passes.
    - `from ketu import *` does not error (re-exports preserved).
    - `git diff ketu/__init__.py | grep -E "^-(from|import|__all__)" | wc -l` returns `0` — no import or `__all__` line was deleted.
  </acceptance_criteria>

  <done>
All 9 audited files (the 3 from Task 2 plus the 6 here) are numpydoc-clean under the locked config. The `__init__.py` and `core.py` non-standard sections are folded into Notes with bold sub-paragraphs (preserves all the structured information per RESEARCH § Pitfall 3). Tests, mypy, interrogate, and re-exports all still work.
  </done>
</task>

</tasks>

<verification>
**Plan 03 phase check (run after all tasks):**
```bash
cd /home/loc/workspace/ketu && source venv/bin/activate

# 1. Full numpydoc baseline is now CLEAN
FILES=$(find ketu -name "*.py" \
    ! -path "*/__pycache__/*" \
    ! -name "lunar_calendar.py" \
    ! -name "_*.py")
python -m numpydoc lint $FILES                  # MUST produce zero output

# 2. interrogate still passes (no docstring deletions)
python -m interrogate ketu/

# 3. No regressions
python -m pytest tests/ -q --no-cov             # 250 tests pass
python -m mypy ketu/ --strict                   # clean

# 4. Sanity: TOML parses, dev install still works
python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"
pip install -e ".[dev]" >/dev/null && python -c "import interrogate, numpydoc"
```

**Per-task incremental check (during execution, NOT after):** after each file's fix-pass within Task 2 / Task 3, run `python -m numpydoc lint <that-file>` and confirm empty output before moving to the next file. This is the per-task feedback loop from VALIDATION.md (sub-second per file).

**Diff scope sanity check:** `git diff --stat <starting-commit>..HEAD -- ketu/` should show ONLY docstring edits — i.e. lines with `"""` in the diff far outnumber any other line type. No line matching `^[+-]\s*(def |class |return |import |from )` should appear (signature and import preservation).
</verification>

<success_criteria>
- pyproject.toml has the new `[tool.numpydoc_validation]` block matching PATTERNS.md verbatim (with GL01 suppressed for the warning phase).
- All 9 audited source files (`complex.py`, `calculations.py`, `cycles/calculator.py`, `__init__.py`, `__main__.py`, `core.py`, `display.py`, `cycles/__init__.py`, `ephemeris/__init__.py`) produce ZERO output under `python -m numpydoc lint <files>`.
- `python -m interrogate ketu/` still passes (no docstring deletions).
- 250 existing tests still pass; mypy --strict still clean.
- ZERO signature changes, ZERO behavior changes, ZERO removed re-exports.
- No edits to `.github/workflows/`, no edits to `Makefile`, no edits to docs (those are Plans 04 + 05).
</success_criteria>

<output>
After completion, create `.planning/phases/13-doc-gates-and-ci-foundation/13-03-SUMMARY.md` summarizing:
- Per-file numpydoc issue count BEFORE the fix-pass (post-config-suppression baseline) vs AFTER (zero)
- Total issues fixed (expected ~115 across the 9 files)
- Category breakdown (GL08, GL06/07, PR01, RT01, SS05) — counts per category
- Confirmation that NO signature/behavior changes happened (diff scope check)
- Note for Phase 20: the `"GL01"` line in `[tool.numpydoc_validation].checks` MUST be removed as part of the "flip numpydoc to blocking" task (per D-05) and the ~59 GL01 hits fixed in a single mechanical sed pass at that time. (Forward note for Phase 20 plan author.)
- Path forward for Plan 04 (wire numpydoc into tests.yml as warning, depends on this plan's clean baseline).
</output>
