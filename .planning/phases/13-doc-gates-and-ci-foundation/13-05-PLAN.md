---
phase: 13-doc-gates-and-ci-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 02
  - 04
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
tags:
  - ops
  - docs
  - changelog
  - readme
must_haves:
  truths:
    - "Le sweep `grep -rni 'interrog\\|numpydoc\\|≥95\\|95%\\|aspirat' CHANGELOG.md README.md docs/source/ CONTRIBUTING.md UPGRADING.md` n'a AUCUNE occurrence aspirational (toutes les mentions sont qualifiées 'enforced by CI' ou contextuelles dans `### Added`)."
    - "Le README contient une section `### Documentation Quality Gates` (sous `## Documentation` ou avant `## Roadmap`) décrivant les gates `interrogate ≥95%` (blocking) et `numpydoc validate` (warning, blocking from v1.2.0) et mentionnant `make doc-gates` pour exécution locale."
    - "Le CHANGELOG contient une entrée sous `## [Unreleased]` ou équivalent v1.2.0-dev avec un sous-bloc `### Added` mentionnant les CI doc-quality gates (OPS-01, OPS-02), le nouveau groupe `dev`, et la cible `make doc-gates`."
    - "AUCUN fichier sous `.planning/` n'est modifié par ce plan (`git diff --stat HEAD~1..HEAD -- '.planning/'` retourne vide) — D-12 enforcé."
    - "Sophie persona respectée dans le texte ajouté (concis, factuel, voix narrative française uniquement si le surrounding text est français — README est anglais, CHANGELOG est anglais avec un préambule français)."
  artifacts:
    - path: "README.md"
      provides: "Documentation Quality Gates section describing the live CI gates"
      contains: "Documentation Quality Gates"
    - path: "CHANGELOG.md"
      provides: "Unreleased/1.2.0-dev entry under ### Added citing OPS-01 and OPS-02"
      contains: "(OPS-01"
  key_links:
    - from: "README.md § Documentation Quality Gates"
      to: "Makefile doc-gates target (Plan 02)"
      via: "explicit instruction to run `make doc-gates` locally"
      pattern: "make doc-gates"
    - from: "CHANGELOG.md § Unreleased ### Added"
      to: "OPS-01 + OPS-02 traceability"
      via: "(OPS-01, OPS-02) parenthetical at end of entry"
      pattern: '\(OPS-01, OPS-02\)'
---

<objective>
Close OPS-01 / OPS-02 traceability with a positive-add to README.md and CHANGELOG.md. The aspirational-references audit (RESEARCH § "Aspirational References Audit (D-11 verification)" — verified live 2026-05-08) confirmed ZERO existing aspirational claims in CHANGELOG / README / CONTRIBUTING / UPGRADING / docs/source/. Per RESEARCH § Aspirational Refs implication, this is therefore a verify-only audit + positive-add of a one-paragraph README section and a `### Added` CHANGELOG entry — NOT a rewrite of stale claims.

Per D-12, `.planning/` files are NOT modified by this plan. They update through the normal `update_state` flow.

Per D-13, NO new aspirational claims are added. Every claim landed by Plan 05 is backed by code that already shipped in Plans 01–04.

Purpose: every public-doc mention of the gates is now factually accurate ("enforced by CI") and traceable back to the requirement IDs. OPS-01 / OPS-02 traceability is closed.
Output: README has new "Documentation Quality Gates" section; CHANGELOG has new entry under `## [Unreleased]` (creating that heading if absent — current CHANGELOG starts with `## [1.1.0]` per live read 2026-05-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/13-doc-gates-and-ci-foundation/13-CONTEXT.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-VALIDATION.md

@.planning/phases/13-doc-gates-and-ci-foundation/13-01-SUMMARY.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-02-SUMMARY.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-03-SUMMARY.md
@.planning/phases/13-doc-gates-and-ci-foundation/13-04-SUMMARY.md

<interfaces>
<!-- Existing README narrative voice — see lines 226-237 (Documentation section) and 316 onward (Roadmap). -->
<!-- README is in English; the project's narrative copy follows a "concise + factual + bullet-listy" style (see lines 226-237 for the canonical voice). -->

<!-- Existing CHANGELOG entry shape — `### Added` under `## [1.1.0]` (line 59 onward) — copy this convention. -->
<!-- Current CHANGELOG starts directly with `## [1.1.0] - 2026-05-08`. NO `## [Unreleased]` heading exists yet — Plan 05 creates it. -->
<!-- Each `### Added` entry follows: `- **<thing>** — <description>. (<requirement-IDs>)` -->

<!-- Aspirational-refs audit (live 2026-05-08, RESEARCH § Aspirational References Audit): -->
<!-- grep -rni "interrog|numpydoc|≥95|95%|aspirat" CHANGELOG.md README.md CONTRIBUTING.md UPGRADING.md docs/source/ -->
<!-- → ZERO HITS. So this plan is positive-add only, not rewrite. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify zero aspirational refs remain in public docs (audit), then positive-add the `Documentation Quality Gates` section to README.md</name>
  <files>README.md</files>

  <read_first>
    - README.md (full file — current section structure: ## Documentation lines 226-237, ## Requirements line 239, ## Roadmap line 316; identify the right anchor for the new section)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md § "CHANGELOG.md and README.md — positive-add of \"enforced by CI\" wording" (concrete README paragraph provided)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-RESEARCH.md § "Aspirational References Audit (D-11 verification)" (live audit confirmed zero hits)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-CONTEXT.md § Decisions D-11 (canonical replacement = "enforced by CI"), D-12 (.planning/ excluded), D-13 (no new aspirational claims)
  </read_first>

  <action>
**Step 1 — Re-run the aspirational-refs sweep (verify-only audit, must still be empty):**
```bash
grep -rni "interrog\|numpydoc\|≥95\|>= *95\|95 *%\|95%\|aspirat" \
    /home/loc/workspace/ketu/CHANGELOG.md \
    /home/loc/workspace/ketu/README.md \
    /home/loc/workspace/ketu/CONTRIBUTING.md \
    /home/loc/workspace/ketu/UPGRADING.md \
    /home/loc/workspace/ketu/docs/source/ 2>&1 | tee /tmp/13-05-aspirational-sweep.log
```

Expected: file paths may match (e.g. `README.md:295: …95% …`) BUT none of the matches should be aspirational claims about doc gates. If a hit exists that IS a stale aspirational claim about interrogate/numpydoc, STOP and surface to user — Plan 05 was scoped as positive-add only, and a stale claim found here would force a rewrite scope expansion. (Per RESEARCH live 2026-05-08, no such hits exist.)

If `/tmp/13-05-aspirational-sweep.log` is empty OR contains only non-doc-gate matches, proceed to Step 2.

**Step 2 — Insert the new `Documentation Quality Gates` section into README.md.**

PATTERNS.md § "CHANGELOG.md and README.md — positive-add of \"enforced by CI\" wording" provides the canonical paragraph. Use this EXACT block (verbatim, 7 lines of markdown including the heading):

```markdown
### Documentation Quality Gates

Documentation quality is enforced by CI on every push:

- **`interrogate ≥95%`** (blocking) — every public function, class, and module has a docstring.
- **`numpydoc validate`** (warning, blocking from v1.2.0) — docstrings follow the NumPy convention.

Run both locally before pushing: `make doc-gates`.
```

Placement: insert as a NEW `### Documentation Quality Gates` sub-section UNDER the existing `## Documentation` section (currently lines 226-237). Place it at the END of the Documentation section, AFTER the existing "Included sections:" bullet list (currently ends around line 237) and BEFORE the next top-level `## Requirements` section (line 239).

Why placement: `### Documentation Quality Gates` is conceptually about Ketu's documentation, so it belongs under the existing `## Documentation` heading. Using `###` (sub-section) instead of `##` (top-level) preserves the README's existing top-level structure (Documentation, Requirements, Supported bodies, …, Roadmap, …).

**Constraints:**
- DO NOT remove or modify the existing `## Documentation` lead paragraph or the "Included sections:" bullet list.
- DO NOT add aspirational language (D-13). The text must be backed by what shipped in Plans 01–04 — and IT IS: interrogate is wired blocking (Plan 02), numpydoc is wired warning (Plan 04), `make doc-gates` exists (Plan 02 Task 2).
- The text mentions "blocking from v1.2.0" — this is FACTUAL (per D-04 + D-05, Phase 20 flips to blocking). It is NOT aspirational because the work item exists in the v1.2 ROADMAP and Plan 04's YAML comment explicitly tags itself as the Phase 20 flip-target.
- The README is in English (per the project's existing voice — narrative copy is English; user-facing CLI/docstrings are English; Sophie's persona text — `persona-sophie.md`, CLAUDE.md, `.planning/` — is in French). DO NOT switch to French in the README.
- Sophie's voice is preserved through "concise + factual" tone — the paragraph above already matches.
  </action>

  <verify>
    <automated>cd /home/loc/workspace/ketu &amp;&amp; grep -c "^### Documentation Quality Gates$" README.md &amp;&amp; grep -A6 "^### Documentation Quality Gates$" README.md | grep -q "interrogate" &amp;&amp; grep -A6 "^### Documentation Quality Gates$" README.md | grep -q "numpydoc" &amp;&amp; grep -A6 "^### Documentation Quality Gates$" README.md | grep -q "make doc-gates"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^### Documentation Quality Gates$" README.md` returns exactly `1`.
    - `grep -A8 "^### Documentation Quality Gates$" README.md` contains all four substrings: `interrogate ≥95%`, `(blocking)`, `numpydoc validate`, `(warning, blocking from v1.2.0)`, AND `make doc-gates`.
    - Section placement: `grep -n "^## " README.md` shows the order is unchanged at the top level. The new `### Documentation Quality Gates` (subsection) appears AFTER `## Documentation` (line ~226 originally) and BEFORE `## Requirements` (line ~239 originally — line numbers shift but ordering preserved).
    - `git diff README.md` shows ONLY additions (new heading + 6 lines of body); the existing `## Documentation` paragraph and bullet list are unchanged.
    - Aspirational-refs sweep AFTER the edit shows the new "interrogate ≥95%" + "numpydoc validate" matches in README — but each of these is now in the `### Documentation Quality Gates` block context, NOT a bare aspirational claim. Specifically: `grep -B2 -A6 "interrogate" README.md` should always show "(blocking) — every public function" or "enforced by CI" wording in proximity, NEVER a bare claim.
    - No edits to `.planning/`: `git diff --stat HEAD~1..HEAD -- '.planning/'` returns no entries (D-12).
    - No edits to French translations: `git diff --stat HEAD~1..HEAD -- 'fr/' 'docs/source/locale/'` returns no entries (the `fr/CHANGELOG.md` decision is OPS-04 / Phase 20 territory per CONTEXT § Deferred Ideas).
    - 250 tests still pass; mypy still clean; both gates still green (sanity — README edits are runtime no-ops).
  </acceptance_criteria>

  <done>
The aspirational-refs sweep is verified empty (no stale claims). README has a new `### Documentation Quality Gates` section under `## Documentation` matching PATTERNS.md verbatim. The new claims are factually backed by Plans 01–04. No `.planning/` files modified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `## [Unreleased]` heading + `### Added` entry to CHANGELOG.md citing OPS-01 + OPS-02</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - CHANGELOG.md (full file — current state: starts with `## [1.1.0] - 2026-05-08`; NO `## [Unreleased]` heading yet; existing `### Added` shape under `## [1.1.0]` line 59 onward provides the convention)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-PATTERNS.md § "CHANGELOG.md and README.md — positive-add of \"enforced by CI\" wording" (concrete CHANGELOG entry provided)
    - .planning/phases/13-doc-gates-and-ci-foundation/13-CONTEXT.md § Decisions D-11, D-12, D-13
  </read_first>

  <action>
The current CHANGELOG (live read 2026-05-08) starts:
```
# Changelog

> Consultez la version française dans `fr/CHANGELOG.md`.

All notable changes to Ketu are documented here.

This project follows the [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)
format and adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.1.0] - 2026-05-08
```

NO `## [Unreleased]` heading exists. Per Keep-a-Changelog convention (which the project explicitly follows per the preamble), in-flight v1.2 work goes under `## [Unreleased]` until release. Phase 20 will rename it to `## [1.2.0] - YYYY-MM-DD`.

**Step 1 — Insert `## [Unreleased]` heading + `### Added` entry BEFORE `## [1.1.0] - 2026-05-08`.**

Use this EXACT block (verbatim from PATTERNS.md § "CHANGELOG.md and README.md — positive-add of \"enforced by CI\" wording"):

```markdown
## [Unreleased]

### Added

- **CI doc-quality gates** — `interrogate ≥95%` (blocking) and
  `numpydoc validate` (warnings, blocking from v1.2.0) are now wired
  into `tests.yml`. New `[project.optional-dependencies].dev` group
  installs both tools (`pip install -e .[dev]`); `make doc-gates`
  runs the full suite locally. (OPS-01, OPS-02)

## [1.1.0] - 2026-05-08
```

(The blank line BEFORE `## [1.1.0]` was already there in the original — the new block ends with a blank line above it for visual separation, matching the existing convention.)

Why this exact shape (per PATTERNS.md):
- `## [Unreleased]` follows Keep-a-Changelog. Existing project preamble explicitly cites Keep-a-Changelog.
- `### Added` matches the existing convention used 4× in `## [1.1.0]` (line 59) and earlier releases.
- `- **<thing>** — <description>. (<requirement-IDs>)` matches the existing entry shape used throughout `## [1.1.0]` (lines 21, 30, 43, 57, 69 — citing `(HOU-02 .. HOU-10)`, `(Phase 9 / ASP-04)`, etc.). The Phase 13 entry cites `(OPS-01, OPS-02)`.
- The text mentions `make doc-gates` and the `dev` extras group — both LANDED in earlier plans, so this is factual (D-13).
- The text mentions "blocking from v1.2.0" for numpydoc — factual per D-04/D-05 + Plan 04's YAML forward-note.

**Constraints:**
- DO NOT modify the existing `## [1.1.0]` content or anything below it.
- DO NOT modify the preamble (lines 1-8 — `# Changelog` + `> Consultez la version française...` + the Keep-a-Changelog reference). The blockquote `> Consultez la version française dans fr/CHANGELOG.md` is OPS-04 territory (Phase 20) — leave it.
- DO NOT touch `fr/CHANGELOG.md` (per CONTEXT § Deferred Ideas — `fr/CHANGELOG.md` reformulation pass is Phase 20 / OPS-04).
- DO NOT add aspirational claims (D-13) — every claim above is backed by Plans 01–04.
- DO NOT modify any `.planning/` file (D-12).
  </action>

  <verify>
    <automated>cd /home/loc/workspace/ketu &amp;&amp; grep -c "^## \[Unreleased\]$" CHANGELOG.md &amp;&amp; grep -A8 "^## \[Unreleased\]$" CHANGELOG.md | grep -q "CI doc-quality gates" &amp;&amp; grep -A8 "^## \[Unreleased\]$" CHANGELOG.md | grep -q "(OPS-01, OPS-02)" &amp;&amp; grep -B1 "^## \[1.1.0\] - 2026-05-08$" CHANGELOG.md | head -5 &amp;&amp; head -1 CHANGELOG.md | grep -q "^# Changelog$"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## \[Unreleased\]$" CHANGELOG.md` returns exactly `1`.
    - `grep -A8 "^## \[Unreleased\]$" CHANGELOG.md` contains all four substrings: `CI doc-quality gates`, `interrogate ≥95%`, `numpydoc validate`, AND `(OPS-01, OPS-02)`.
    - The new `## [Unreleased]` heading appears BEFORE the existing `## [1.1.0] - 2026-05-08` heading: `awk '/^## \[/{print NR": "$0}' CHANGELOG.md | head -3` shows `Unreleased` first, then `[1.1.0]`, then `[1.0.0]` etc.
    - The preamble is unchanged: `head -8 CHANGELOG.md` is identical to the pre-edit version (`# Changelog`, `> Consultez la version française dans fr/CHANGELOG.md`, the "All notable changes…" paragraph, and the Keep-a-Changelog reference paragraph).
    - The existing `## [1.1.0]` content is unchanged: `git diff CHANGELOG.md | grep -E "^[+-].*\[1\.1\.0\]"` shows NO removals or modifications inside the [1.1.0] block.
    - `git diff CHANGELOG.md` shows ONLY additions: the new `## [Unreleased]` block + the surrounding blank line.
    - No edits to `fr/CHANGELOG.md`: `git diff --stat HEAD~1..HEAD -- 'fr/'` returns no entries.
    - No edits to `.planning/`: `git diff --stat HEAD~1..HEAD -- '.planning/'` returns no entries.
    - 250 tests still pass; mypy still clean; both gates still green (sanity).
    - Final aspirational sweep across ALL public docs:
      `grep -rni "interrog\|numpydoc\|≥95\|aspirat" CHANGELOG.md README.md CONTRIBUTING.md UPGRADING.md docs/source/`
      EVERY match must occur in proximity to "(blocking)" / "(enforced by CI)" / "(warning…)" / "make doc-gates" / "(OPS-01, OPS-02)" — NOT a bare aspirational claim. (Confirmed manually as the planner's gate; record the verdict in the SUMMARY.)
  </acceptance_criteria>

  <done>
CHANGELOG has a new `## [Unreleased]` heading with a `### Added` entry citing both OPS-01 and OPS-02, matching the project's existing `### Added` entry shape. The preamble and existing entries are untouched. The Phase 13 narrative is closed: every aspirational claim is now factually backed by code that has shipped.
  </done>
</task>

</tasks>

<verification>
**Plan 05 phase check (after both tasks):**
```bash
cd /home/loc/workspace/ketu && source venv/bin/activate

# 1. Final aspirational sweep — every match must be qualified, never bare
grep -rni "interrog\|numpydoc\|≥95\|>= *95\|95 *%\|95%\|aspirat" \
    CHANGELOG.md README.md CONTRIBUTING.md UPGRADING.md docs/source/ 2>&1 \
    | tee /tmp/13-05-final-sweep.log
# Manual review: every line must be in the new Documentation Quality Gates / Unreleased blocks (qualified).

# 2. Both new sections grep clean
grep -A8 "^### Documentation Quality Gates$" README.md
grep -A8 "^## \[Unreleased\]$" CHANGELOG.md

# 3. .planning untouched (D-12 enforcement)
git diff --stat HEAD~2..HEAD -- '.planning/'   # MUST be empty
git diff --stat HEAD~2..HEAD -- 'fr/'           # MUST be empty (OPS-04 / Phase 20)

# 4. Both gates still green (Phase 13 acceptance)
python -m interrogate ketu/                     # exit 0, score >= 95
FILES=$(find ketu -name "*.py" ! -path "*/__pycache__/*" ! -name "lunar_calendar.py" ! -name "_*.py")
python -m numpydoc lint $FILES                  # ZERO output

# 5. No regressions
python -m pytest tests/ -q --no-cov             # 250 tests pass
python -m mypy ketu/ --strict                   # clean
make doc-gates                                  # both invocations work locally
```

**Phase 13 acceptance gate (run before `/gsd-verify-work`):**
- All four ROADMAP success criteria (13.1–13.4) PASS:
  - 13.1: `interrogate ≥95%` is in `[project.optional-dependencies].dev` AND CI fails the build below 95% (Plan 01 + Plan 02). ✓
  - 13.2: `numpydoc validate` runs in CI with warnings surfacing in the build log (gate flips blocking at end of milestone via Plan 04's YAML forward-note). ✓
  - 13.3: Aspirational refs are reformulated — verified empty + positive-add of qualified claims (Plan 05). ✓
  - 13.4: Re-running gates on v1.1 codebase produces a clean baseline (Plan 01 fixed 4 interrogate gaps; Plan 03 fixed ~115 numpydoc gaps). ✓
</verification>

<success_criteria>
- README.md has the new `### Documentation Quality Gates` section under `## Documentation` matching PATTERNS.md verbatim.
- CHANGELOG.md has a new `## [Unreleased]` heading with a `### Added` entry citing `(OPS-01, OPS-02)` matching PATTERNS.md verbatim.
- Final aspirational-refs sweep: every match is qualified (in proximity to "(blocking)", "(enforced by CI)", "(warning…)", or `make doc-gates`) — no bare aspirational claims remain.
- ZERO edits to `.planning/` (D-12 enforcement).
- ZERO edits to `fr/CHANGELOG.md` (Phase 20 / OPS-04 territory).
- ZERO edits to source `ketu/` or workflows (those are Plans 01–04).
- 250 tests still pass; mypy still clean; both gates (interrogate blocking, numpydoc warning) still green.
- Phase 13 acceptance gate: all four ROADMAP success criteria 13.1–13.4 pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13-doc-gates-and-ci-foundation/13-05-SUMMARY.md` summarizing:
- Final aspirational-refs sweep result (verified empty pre-edit; verified all-qualified post-edit)
- README section + CHANGELOG entry — exact diffs landed
- Confirmation that `.planning/` and `fr/CHANGELOG.md` are untouched (D-12 + Phase 20 boundaries enforced)
- Phase 13 closure summary: OPS-01 + OPS-02 traceability closed; ROADMAP success criteria 13.1–13.4 all green; ready for `/gsd-verify-work 13`.
- Forward note for Phase 20 plan author (echoing Plan 04 SUMMARY): when flipping numpydoc to blocking, also (a) rename `## [Unreleased]` to `## [1.2.0] - YYYY-MM-DD` in CHANGELOG, (b) bump version in pyproject.toml + ketu/__init__.py to 1.2.0, (c) remove `continue-on-error: true` from tests.yml numpydoc step, (d) remove `"GL01"` suppression from `[tool.numpydoc_validation].checks`, (e) fix the deferred ~59 GL01 hits.
</output>
