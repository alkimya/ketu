---
phase: 23-spike-chiron
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - .planning/phases/23-spike-chiron/23-DECISION.md
autonomous: true

must_haves:
  truths:
    - "A written go/no-go decision for the Chiron phase exists, stating GO or NO-GO explicitly"
    - "The decision records the chosen Phase 24 parameters: segment length, polynomial degree, and number of quantities fitted (lon, lat, dist)"
    - "The decision records achieved accuracy as an absolute max |Δλ| AND as a ratio to the 0.01° target, sourced from 23-MEASUREMENTS.md"
    - "The < 0.01° comparison is recorded explicitly (met or not met) over the supported 1950-2050 range; if not met, the real boundary + tuning to reach it is documented"
    - "The .npz layout (arrays, shapes, dtypes) for Phase 24 is documented"
    - "The ephemeris-file setup requirement (seas_18.se1 + set_ephe_path) for the Phase 24 build/test harness is documented"
    - "The Phase 24 insertion points are referenced (NOT modified): BODY_INDICES, SWE_IDS, BODY_STRATEGIES, calc_planet_position range 0-13"
    - "The decision states the deliverable is data + decision, with no production code under ketu/, no new pytest test, and no runtime dependency added"
  artifacts:
    - path: ".planning/phases/23-spike-chiron/23-DECISION.md"
      provides: "Go/no-go decision record (SPK-02) consuming 23-MEASUREMENTS.md numbers"
      contains: "GO"
      min_lines: 40
  key_links:
    - from: ".planning/phases/23-spike-chiron/23-DECISION.md"
      to: ".planning/phases/23-spike-chiron/23-MEASUREMENTS.md"
      via: "accuracy numbers + chosen config transcribed from the measurement table"
      pattern: "0\\.0[01]|max"
    - from: ".planning/phases/23-spike-chiron/23-DECISION.md"
      to: "ketu/ephemeris/planets.py (Phase 24 insertion points)"
      via: "references BODY_INDICES / SWE_IDS / BODY_STRATEGIES / calc_planet_position range"
      pattern: "BODY_INDICES|BODY_STRATEGIES|SWE_IDS"
---

<objective>
Write the go/no-go decision record for the Chiron phase (SPK-02), consuming the
measured numbers produced in plan 23-01. State GO or NO-GO explicitly, lock the
Phase 24 parameters (seg_len, degree, n_quantities), record achieved accuracy vs the
0.01° target, document the .npz layout and the ephemeris-file setup requirement, and
reference (without touching) the Phase 24 insertion points.

Purpose: This is the phase goal — a DOCUMENTED measured go/no-go. It turns 23-01's
data into an actionable decision Phase 24 can plan against. Deliverable is a decision
record, NOT production code.

Output: .planning/phases/23-spike-chiron/23-DECISION.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-spike-chiron/23-RESEARCH.md
@.planning/phases/23-spike-chiron/23-MEASUREMENTS.md
@.planning/phases/23-spike-chiron/23-01-SUMMARY.md

# This plan depends on 23-01: it consumes the MEASURED numbers in 23-MEASUREMENTS.md.
# Use those measured values, not the research preview, wherever they differ.
# Persona Sophie Chen: French + tutoiement throughout.
# Binding: deliverable is a DECISION, not code. Do NOT touch ketu/, tests/, pyproject.toml.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the go/no-go decision record (23-DECISION.md)</name>
  <files>.planning/phases/23-spike-chiron/23-DECISION.md</files>
  <action>
Write the SPK-02 decision record in French (Sophie Chen, tutoiement), consuming the
measured numbers from 23-MEASUREMENTS.md (produced by plan 23-01). Structure:

1. VERDICT (top, unmissable): an explicit "GO" or "NO-GO" for Phase 24 (Chiron).
   Base it on the measured (32,10) max |Δλ| vs the 0.01° threshold. Per research the
   expected outcome is GO (~0.0008°, ~12x margin) — but state the verdict from the
   ACTUAL measured number in 23-MEASUREMENTS.md. If the measured number unexpectedly
   exceeds 0.01°, write NO-GO and document the real boundary + the tuning needed
   (e.g. shorter segments / higher degree, citing the bracketing rows) to reach <0.01°.

2. CHOSEN PARAMETERS for Phase 24 (locked recommendation):
   - segment length (days)
   - polynomial degree
   - number of quantities fitted: 3 (lon, lat, dist); speeds via finite difference
     (jd_delta=0.01, matching the existing _make_planet_scalar pattern) — NOT stored.
   - n_segments and total coefficients for the 3-quantity config.

3. ACHIEVED ACCURACY (from measurements):
   - max |Δλ| (absolute °) and the ratio to 0.01° (safety margin).
   - max |Δlat| (°) and max |Δdist| (AU) for the chosen config.
   - the worst-case segment date (perihelion region) and its error.
   - the < 0.01° comparison stated explicitly as MET (or NOT MET).

4. ORACLE + EPHEMERIS provenance:
   - oracle call: swe.calc_ut(jd, swe.CHIRON, FLG_SWIEPH|FLG_SPEED), swe.CHIRON==15.
   - the observed retflag and whether full SE files or Moshier-fallback was the actual
     path; record the measured Moshier-vs-SE difference if available (research: ~0.000067°).
   - ephemeris-file setup requirement for Phase 24's build/test harness: seas_18.se1
     MUST be in the set_ephe_path() directory or swe.calc_ut(swe.CHIRON) raises
     swisseph.Error; for the spike it lived at
     /home/loc/workspace/rahu/kerykeion/kerykeion/sweph/seas_18.se1. Note the three CI
     options from research (bundle as fixture / importorskip+skip-if-absent / add to
     res/) as options for Phase 24 to decide — do NOT decide CI here.

5. .NPZ LAYOUT for Phase 24 (arrays, shapes, dtypes), e.g.:
   lon_coeffs (n_segs, degree+1) f8, lat_coeffs (n_segs, degree+1) f8,
   dist_coeffs (n_segs, degree+1) f8, seg_starts (n_segs,) f8, seg_len f8,
   degree i4, jd_start f8, jd_end f8. Use the MEASURED n_segs/degree.

6. RUNTIME EVALUATOR (confirmed, NOT shipped this phase): pure-NumPy
   np.polynomial.chebyshev.chebval with t = 2*(jd - seg_start)/seg_len - 1, clipped to
   [-1,1], lon re-wrapped via % 360 at eval; vectorizable; NO scipy, NO pyswisseph at
   runtime. Note the spike confirmed chebval matches the Chebyshev object (np.allclose).

7. PHASE 24 INSERTION POINTS (reference only — Phase 24 will modify these, NOT this phase):
   ketu/ephemeris/planets.py — BODY_INDICES (~L35-49) add "Chiron":13; SWE_IDS (~L52-66)
   add 13:"Chiron"; BODY_STRATEGIES (~L310-324) add "Chiron" strategy; calc_planet_position
   valid range 0-13. Also note (from research/roadmap) the 13->14 body-count ratchet:
   test_body_count_frozen_at_thirteen at tests/test_ketu.py:110 + synastry/transits/charts
   body-count assertions will need updating in Phase 24.

8. SCOPE GUARDRAIL (explicit): this spike's deliverable is DATA + this DECISION. No
   production code was added under ketu/, no test was registered in pytest, the
   1351-test suite and 100% coverage gate are untouched, and no runtime dependency was
   added (pyswisseph remains test/build-only and was used only by the spike script).

End with a short "Prochaine étape" line pointing to Phase 24 planning.
  </action>
  <verify>
  Read .planning/phases/23-spike-chiron/23-DECISION.md and confirm it contains:
  an explicit GO (or NO-GO) verdict at the top; chosen seg_len/degree/n_quantities;
  achieved max |Δλ| absolute + ratio to 0.01° + explicit MET/NOT-MET; lat/dist accuracy;
  worst-segment date; .npz layout with shapes/dtypes; ephemeris-file (seas_18.se1 +
  set_ephe_path) requirement; references to BODY_INDICES/SWE_IDS/BODY_STRATEGIES +
  calc_planet_position range 0-13 and the 13->14 ratchet; the no-code/no-pytest/no-runtime-dep
  scope guardrail. Confirm numbers match 23-MEASUREMENTS.md.
  Confirm nothing under ketu/, tests/, pyproject.toml changed:
    git status --porcelain ketu tests pyproject.toml   (must be EMPTY)
  </verify>
  <done>
  23-DECISION.md exists with an explicit GO/NO-GO verdict grounded in the measured
  accuracy, the locked Phase 24 parameters (seg_len, degree, 3 quantities), the
  accuracy-vs-0.01° comparison, the .npz layout, the seas_18.se1/set_ephe_path setup
  requirement, the referenced (unmodified) Phase 24 insertion points, and the explicit
  scope guardrail (no runtime code, no new pytest test, no runtime dep) — SPK-02 satisfied.
  </done>
</task>

</tasks>

<verification>
- 23-DECISION.md states an explicit go/no-go verdict derived from the MEASURED max |Δλ|
  vs the 0.01° target (numbers consistent with 23-MEASUREMENTS.md).
- Chosen Phase 24 parameters (seg_len, degree, 3 quantities lon/lat/dist; speeds via
  finite difference) are recorded.
- Achieved accuracy recorded as absolute + ratio + explicit MET/NOT-MET; lat/dist
  accuracy + worst-segment date recorded.
- .npz layout (arrays/shapes/dtypes) documented; ephemeris-file setup (seas_18.se1 +
  set_ephe_path) documented; Phase 24 insertion points referenced (not modified).
- Explicit scope guardrail: no production code under ketu/, no new pytest test, no
  runtime dependency added; suite + coverage gate untouched.
</verification>

<success_criteria>
- A written, measured go/no-go decision exists (SPK-02) consuming 23-01's numbers.
- The decision is actionable for Phase 24 planning: parameters, .npz layout, ephemeris
  setup, and insertion points are all present.
- Deliverable is data + decision only; zero impact on ketu/, tests/, pyproject.toml,
  the 1351-test suite, and the 100% coverage gate.
</success_criteria>

<output>
After completion, create `.planning/phases/23-spike-chiron/23-02-SUMMARY.md`
</output>
