---
phase: 31-documentation-en-fr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/relational_charts.md
autonomous: true

must_haves:
  truths:
    - "The compute_chart `aspects` parameter description (line ~18) no longer says 'default classical set' — it names the library default as TRADITIONAL (v1.3+)"
    - "The synastry `aspects` parameter description (line ~81) clarifies that aspects=\"classical\" is calculate_synastry's OWN default (backward-compat byte stability), distinct from the library-wide TRADITIONAL default"
    - "calculate_synastry's signature/default aspects=\"classical\" is NOT changed to TRADITIONAL — it is documented as an intentional function-level default"
    - "No remaining 'default classical set' / 'classical (default)' string implying the LIBRARY default is classical"
  artifacts:
    - path: docs/source/relational_charts.md
      provides: "Corrected aspects-default descriptions distinguishing library default (TRADITIONAL) from calculate_synastry's pinned classical default"
      contains: "TRADITIONAL"
  key_links:
    - from: "relational_charts.md aspects parameter notes"
      to: "calculate_aspects library default"
      via: "explicit distinction between function-level and library-wide defaults"
      pattern: "TRADITIONAL"
---

<objective>
Correct the stale "classical default" claims in `docs/source/relational_charts.md` (DOC-15). Two targets: (1) line ~18, the `compute_chart` `aspects` parameter says "`None` uses the default classical set" — wrong, the library default is TRADITIONAL; (2) line ~81, the `calculate_synastry` `aspects` parameter says `"classical"` (default) without distinguishing the synastry function's OWN default from the library-wide default.

Purpose: relational_charts.md must not imply the library default is classical, while correctly preserving that `calculate_synastry`'s own default IS `"classical"` (pinned for backward-compat byte stability — an intentional function-level choice, NOT to be "corrected" to TRADITIONAL).

Output: An edited `docs/source/relational_charts.md` that names TRADITIONAL as the library default and clarifies calculate_synastry's classical default as a deliberate function-level default.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/relational_charts.md
@ketu/synastry/api.py
</context>

<notes>
- CRITICAL GUARD: `calculate_synastry`'s actual default IS `aspects="classical"` (verified in `ketu/synastry/api.py`) and this is INTENTIONAL (backward-compat byte stability). Do NOT change the function signature shown at line ~75 from `aspects="classical"`. Do NOT say "the synastry default should be TRADITIONAL". The fix is to CLARIFY the distinction between this function-level default and the library-wide `calculate_aspects` TRADITIONAL default.
- Line ~18 is the `compute_chart` parameter (`None` → library default). For `compute_chart`/`calculate_aspects`, `None` resolves to TRADITIONAL — that line IS wrong and must say TRADITIONAL.
- This plan touches only relational_charts.md — two surgical edits + one clarifying note. No table restructuring.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Fix the compute_chart aspects parameter (line ~18) to name TRADITIONAL as the library default</name>
  <files>docs/source/relational_charts.md</files>
  <action>
At line ~18, replace:

`- \`aspects\` — aspect set spec (\`None\` uses the default classical set)`

with:

`- \`aspects\` — aspect set spec (\`None\` uses the library default — **TRADITIONAL**, the 7 half-circle aspects, v1.3+)`

Self-check: `grep -n "default classical set" docs/source/relational_charts.md` → 0 hits.
  </action>
  <verify>`grep -n "default classical set" docs/source/relational_charts.md` → 0 hits. Line ~18 now names TRADITIONAL as the library default.</verify>
  <done>The compute_chart `aspects` parameter description names TRADITIONAL (7 half-circle aspects, v1.3+) as the library default.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify the calculate_synastry aspects default (line ~81) without changing the pinned "classical" default</name>
  <files>docs/source/relational_charts.md</files>
  <action>
At line ~81, replace:

`- \`aspects\` — aspect set: \`"classical"\` (default), \`"traditional"\`, \`"extended"\`, or a list/mask`

with:

`- \`aspects\` — aspect set: \`"classical"\`, \`"traditional"\`, \`"extended"\`, or a list/mask. \`calculate_synastry\`'s own default is \`"classical"\` (5 major aspects), **pinned for backward-compatible byte stability** — this is distinct from the library-wide default of \`calculate_aspects\`, which is **TRADITIONAL** (7 half-circle aspects, v1.3+).`

Leave the function signature at line ~75 (`calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered")`) UNCHANGED — `aspects="classical"` is the correct, intentional default for this function.

Self-check: `grep -n 'aspects="classical"' docs/source/relational_charts.md` → still returns the signature line (unchanged). `grep -ni "classical.*default\|default.*classical" docs/source/relational_charts.md` → the only remaining hits, if any, are inside the clarifying note that explicitly frames classical as the FUNCTION default (acceptable), with NO claim that classical is the LIBRARY default.
  </action>
  <verify>`grep -n 'calculate_synastry(chart_a, chart_b, aspects="classical"' docs/source/relational_charts.md` → still present (signature unchanged). The line ~81 note explicitly distinguishes the function-level "classical" default from the library-wide TRADITIONAL default. No text states the LIBRARY default is classical.</verify>
  <done>The synastry `aspects` default is documented as `calculate_synastry`'s intentional, backward-compat-pinned function default, clearly distinguished from the library-wide TRADITIONAL default; the signature is unchanged.</done>
</task>

</tasks>

<verification>
- `grep -n "default classical set" docs/source/relational_charts.md` → 0 hits.
- `grep -n 'aspects="classical"' docs/source/relational_charts.md` → still present in the synastry signature (intentional default preserved).
- `grep -n "TRADITIONAL" docs/source/relational_charts.md` → ≥2 hits (compute_chart note + synastry clarification).
- No text claims the library-wide default aspect set is classical.
</verification>

<success_criteria>
DOC-15 satisfied for relational_charts.md: no claim that the library default is classical; TRADITIONAL is named as the library default for `compute_chart`/`calculate_aspects`; `calculate_synastry`'s own `"classical"` default is preserved and correctly documented as an intentional, byte-stable function-level default distinct from the library default.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-03-SUMMARY.md` recording both edits, explicit confirmation the `aspects="classical"` synastry signature was NOT changed, and the grep results.
</output>
