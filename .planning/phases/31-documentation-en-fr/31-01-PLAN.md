---
phase: 31-documentation-en-fr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/concepts.md
autonomous: true

must_haves:
  truths:
    - "concepts.md Summary Table contains ONLY harmonics 1, 2, 3, 6 (180°/n rows); no 360°/5, 360°/9, 360°/10 rows remain in any table"
    - "EXTENDED harmonics (H5/H9/H10) are framed as 'available in code', not shown in a table — the H5/H9/H10 #### subsections are collapsed into a prose note"
    - "The 14-row 'Aspect Types and Harmonic Coefficients' table is restructured so the 7 full-circle minor aspects (Decile/Novile/Quintile/Binovile/Tredecile/Biquintile/Quadrinovile) are no longer listed in the main table; only the 7 half-circle aspects remain in the table"
    - "The EXTENDED code example (calculate_aspects(jday, aspects=EXTENDED)) STILL EXISTS in the Configurable Aspect Sets section — EXTENDED is removed from TABLES only, not from code docs"
    - "A new generate_harmonic_aspects(h) subsection exists with a runnable example and the ~2× smaller full-circle orb note"
    - "The Chiron range note at line ~66 says 1900–2100 (New in v1.4), not 1950–2050"
    - "180°-division default (H1/H2/H3/H6 = TRADITIONAL) is the framing baseline of the Aspects section"
  artifacts:
    - path: docs/source/concepts.md
      provides: "Aspects section recentred on 180°-division default; EXTENDED out of tables but in code docs; generate_harmonic_aspects documented; Chiron range 1900–2100"
      contains: "generate_harmonic_aspects"
  key_links:
    - from: "concepts.md Configurable Aspect Sets section"
      to: "new generate_harmonic_aspects subsection"
      via: "subsection placed immediately after Configurable Aspect Sets, before ## Orbs"
      pattern: "generate_harmonic_aspects"
---

<objective>
Recentre `concepts.md` on the 180°-division default (DOC-14) and add the dynamic-harmonics generator documentation (part of DOC-16). Remove the EXTENDED harmonics (H5/H9/H10) from the doc TABLES, collapse their dedicated `####` subsections into a prose "available in code" note, restructure the 14-row coefficient table to show only the 7 half-circle aspects, add a `generate_harmonic_aspects(h)` subsection with a runnable example + the ~2× smaller full-circle orb note, and update the Chiron range note to 1900–2100.

Purpose: This is the largest Wave 1 edit. It makes the default aspect framing (TRADITIONAL = 7 half-circle aspects, harmonics 1/2/3/6) the baseline of the Aspects narrative, while keeping EXTENDED fully documented as code (not deleted). It also surfaces the v1.4 dynamic-harmonics public API and the expanded Chiron range.

Output: An edited `docs/source/concepts.md` whose aspect tables show CLASSICAL (5) and TRADITIONAL (7) only, EXTENDED framed as "available in code", a new `generate_harmonic_aspects` subsection, and the corrected 1900–2100 Chiron range note.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/concepts.md
@ketu/aspects/harmonics.py
</context>

<notes>
- CRITICAL GUARD: EXTENDED is NOT being deleted from code. The constant `ketu.aspects.EXTENDED` stays valid. ONLY remove H5/H9/H10 from doc TABLES; the `calculate_aspects(jday, aspects=EXTENDED)` code example in the "Configurable Aspect Sets" section MUST remain verbatim.
- Already CORRECT — do NOT change: lines ~128–132 ("Default aspect set (v1.3+): TRADITIONAL"), lines ~133–178 (Configurable Aspect Sets + aspects_for_harmonics), line ~199 ("Pluto, Chiron | 4°").
- v1.4 confirmed numbers: Chiron range 1900–2100; Chiron max error 0.001214°.
- generate_harmonic_aspects signature (verified): `def generate_harmonic_aspects(h: int)` — int, 2 <= h <= 64; returns structured array, shape `(h // 2,)`. Export: `from ketu.aspects import generate_harmonic_aspects`.
- The exact runnable example to embed is in RESEARCH §5 (h=7 → 3 rows, names b'H7-1'..b'H7-3', angles [51.43, 102.86, 154.29], coefs [0.1429, 0.2857, 0.4286]).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Remove H5/H9/H10 from tables, collapse their subsections to an "available in code" note, restructure the 14-row coefficient table</name>
  <files>docs/source/concepts.md</files>
  <action>
**1a — Collapse H5/H9/H10 subsections (current lines 98–112).** Delete the three `#### Harmonic 5`, `#### Harmonic 9`, `#### Harmonic 10` subsections (their bullet lists for Quintile/Biquintile, Novile/Binovile/Quadrinovile, Decile/Tredecile). Replace them with a single short prose note directly after the `#### Harmonic 6` block, e.g.:

> The full-circle minor harmonics — **H5** (quintiles), **H9** (noviles), **H10** (deciles) — are **available in code** but are not part of the default framing. They are opt-in via the `EXTENDED` preset or `aspects_for_harmonics([5, 9, 10])` (see [Configurable Aspect Sets](#configurable-aspect-sets-new-in-v11-updated-in-v13) below).

Keep H1/H2/H3/H6 `####` subsections (lines 79–96) unchanged.

**1b — Trim the Summary Table (current lines 116–124).** Remove the three rows `5 | 360°/5 | Quintile…`, `9 | 360°/9 | Novile…`, `10 | 360°/10 | Decile…`. Keep only rows for harmonics 1, 2, 3, 6 (all `180°/n`). Update the lead-in sentence at line 126 ("Ketu supports all 14 aspects across harmonics 1, 2, 3, 5, 6, 9, and 10.") — replace with a sentence that frames the table as the 7 default half-circle aspects and points to the prose note for the full-circle minors, e.g. "The table above lists the 7 default half-circle aspects (the TRADITIONAL preset). The full-circle minor aspects (H5/H9/H10) are available in code; see the note above and [Configurable Aspect Sets](#configurable-aspect-sets-new-in-v11-updated-in-v13)."

**1c — Restructure the 14-row "Aspect Types and Harmonic Coefficients" table (current lines 202–221).** Reduce the main table to ONLY the 7 half-circle aspects (Conjunction, Semi-sextile, Sextile, Square, Trine, Quincunx, Opposition — harmonics 1, 2, 3, 6). Remove the 7 full-circle minor rows (Decile, Novile, Quintile, Binovile, Tredecile, Biquintile, Quadrinovile) from this table. Update the lead-in at line 204 ("Ketu calculates 14 aspects across harmonics 1, 2, 3, 5, 6, 9, and 10:") to describe the 7 default aspects, and add a one-line prose note below the trimmed table that the 7 full-circle minor aspects (H5/H9/H10) remain available via `EXTENDED` / `aspects_for_harmonics` and are listed in the full 14-aspect table in [the API reference](api.md). Do NOT add a second 14-row table here — the API page is the home of the full enumeration.

Self-check after edits: `grep -n "360°/5\|360°/9\|360°/10" docs/source/concepts.md` returns nothing; `grep -nc "Quintile\|Novile\|Decile\|Binovile\|Tredecile\|Biquintile\|Quadrinovile" docs/source/concepts.md` shows these names now appear ONLY in the prose note (not in any table row). Do NOT remove the EXTENDED code example at lines ~143–154.
  </action>
  <verify>`grep -n "360°/5\|360°/9\|360°/10" docs/source/concepts.md` → empty. The Summary Table has 4 data rows (H1/H2/H3/H6). The "Aspect Types and Harmonic Coefficients" table has 7 data rows (the half-circle aspects). `grep -n "aspects=EXTENDED" docs/source/concepts.md` still returns the preserved code example.</verify>
  <done>EXTENDED harmonics removed from BOTH tables; their `####` subsections collapsed into a single "available in code" prose note; the 14-row coefficient table trimmed to the 7 half-circle aspects with a prose pointer to the API page; the EXTENDED code example intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add generate_harmonic_aspects subsection + ~2× orb note, update Chiron range note to 1900–2100</name>
  <files>docs/source/concepts.md</files>
  <action>
**2a — Add the dynamic-harmonics generator subsection.** Insert a new `### Dynamic Harmonic Generator (generate_harmonic_aspects)` subsection immediately AFTER the `### Configurable Aspect Sets` block (i.e. after the `aspects_for_harmonics` content ending at line ~178) and BEFORE the `## Orbs` heading (line 180). Content:

- One sentence: `generate_harmonic_aspects(h)` builds aspect specs on the fly for ANY integer harmonic `h` (2 ≤ h ≤ 64), not just the named-preset harmonics. It uses the full-circle 360° convention folded to 0–180°.
- The runnable example (copy verbatim from RESEARCH §5):

```python
from ketu.aspects import generate_harmonic_aspects

# Harmonic 7: 3 unique folded angles (septile family)
specs = generate_harmonic_aspects(7)
print(len(specs))          # 3
print(specs['name'])       # [b'H7-1', b'H7-2', b'H7-3']
print(specs['angle'])      # [51.43, 102.86, 154.29]
print(specs['coef'])       # [0.1429, 0.2857, 0.4286]

# Pass to calculate_aspects via dynamic_specs:
from ketu.aspects import calculate_aspects
result = calculate_aspects(jd, dynamic_specs=generate_harmonic_aspects(7))
```

- Note that the returned array is a drop-in for `core.aspects` (same dtype) and is passed as the `dynamic_specs=` argument to `calculate_aspects`, `find_aspects_between_dates`, and `calculate_synastry`.
- The MANDATORY orb note (DOC-16), as a MyST `{note}` admonition or a `> **Note**` blockquote:

> **Note on orbs:** Dynamic harmonic orbs use `coef = k / h` (full-circle convention). For high harmonics this yields orbs roughly half the size of the equivalent half-circle aspect — e.g. a septile (H7-1, coef ≈ 0.143) gives a smaller orb than a sextile (coef ≈ 0.333) for the same body pair. This is **accepted behaviour**: the two conventions coexist without unification (see the v1.4 release notes).

**2b — Update the Chiron range note (line 66).** Change "Computed via embedded Chebyshev polynomial coefficients; valid range 1950–2050." to "Computed via embedded Chebyshev polynomial coefficients; valid range **1900–2100** (expanded in v1.4). Out-of-range input is silently clamped to the nearest segment boundary." Keep the "See [Chiron](chiron.md) for details." pointer. Do NOT mention ValueError (Phase 30 changed to clamping).

Self-check: `grep -n "1950\|2050" docs/source/concepts.md` → empty; `grep -n "generate_harmonic_aspects" docs/source/concepts.md` → ≥1 hit; `grep -n "dynamic_specs" docs/source/concepts.md` → ≥1 hit.
  </action>
  <verify>`grep -n "generate_harmonic_aspects" docs/source/concepts.md` returns the new subsection. `grep -n "1950\|2050" docs/source/concepts.md` → empty. The new subsection sits between "Configurable Aspect Sets" and "## Orbs". The orb note containing "accepted behaviour" / "coexist" is present.</verify>
  <done>`generate_harmonic_aspects(h)` documented with runnable example + the ~2× smaller full-circle orb note; Chiron range note updated to 1900–2100 with clamping (no ValueError).</done>
</task>

</tasks>

<verification>
- `grep -n "360°/5\|360°/9\|360°/10" docs/source/concepts.md` → 0 hits (EXTENDED out of tables).
- `grep -n "aspects=EXTENDED" docs/source/concepts.md` → ≥1 hit (EXTENDED code example preserved).
- `grep -n "generate_harmonic_aspects" docs/source/concepts.md` → ≥1 hit.
- `grep -n "1950\|2050" docs/source/concepts.md` → 0 hits (Chiron range corrected).
- Both tables (Summary + coefficient) now show only H1/H2/H3/H6 aspects.
</verification>

<success_criteria>
DOC-14 satisfied for concepts.md: aspect tables show CLASSICAL(5)/TRADITIONAL(7) only with default vs opt-in marked; EXTENDED (H5/H9/H10) removed from tables but framed as "available in code"; the 180°-division default is the framing baseline. Part of DOC-16 satisfied: `generate_harmonic_aspects(h)` documented with a runnable example + the ~2× orb note; Chiron range note is 1900–2100 with clamping.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-01-SUMMARY.md` recording: which subsections/tables were edited (with before/after row counts), confirmation the EXTENDED code example survived, the exact placement of the new generate_harmonic_aspects subsection, and the grep results proving 0 stale range/360° hits.
</output>
