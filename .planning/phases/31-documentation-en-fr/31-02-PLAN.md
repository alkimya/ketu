---
phase: 31-documentation-en-fr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/migration.md
autonomous: true

must_haves:
  truths:
    - "migration.md no longer claims the library default is EXTENDED — the v1.0→v1.1 section states the v1.1 default WAS EXTENDED but the current (v1.3+) default is TRADITIONAL"
    - "The Chiron date-range line (~58) says 1900–2100 and describes silent clamping, NOT a ValueError"
    - "No remaining 'unchanged (EXTENDED' or 'EXTENDED = all 14 aspects' default-claim string in migration.md"
    - "The v1.0→v1.1 code comment '# v1.0 behavior (unchanged default)' is corrected so it does not imply EXTENDED is the current default"
  artifacts:
    - path: docs/source/migration.md
      provides: "Corrected stale EXTENDED-default claim + Chiron range/clamping fix"
      contains: "TRADITIONAL"
  key_links:
    - from: "migration.md v1.0→v1.1 section"
      to: "concepts.md preset table"
      via: "pointer to current TRADITIONAL default"
      pattern: "TRADITIONAL"
---

<objective>
Correct the two stale claims in `docs/source/migration.md` (DOC-15 + the Chiron behavior change from DOC-16): (1) the v1.0→v1.1 section says "behavior is unchanged (EXTENDED = all 14 aspects)" — must clarify EXTENDED was the v1.1 default but TRADITIONAL is the current (v1.3+) default; (2) the Chiron date-range line says "1950–2050 … raises a ValueError" — Phase 30 changed the range to 1900–2100 and out-of-range to silent clamping.

Purpose: migration.md must not state the wrong library default, and must reflect the Phase 30 runtime behavior change (clamping, not ValueError) and the expanded Chiron range.

Output: An edited `docs/source/migration.md` with no stale EXTENDED-default claim, the corrected Chiron range (1900–2100), and clamping behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/migration.md
</context>

<notes>
- The v1.1 changelog/migration entries are HISTORICAL: at v1.1, EXTENDED genuinely WAS the default. The fix is to make clear that the CURRENT default is TRADITIONAL — not to erase the history. Annotate, don't rewrite history.
- Phase 30 changed out-of-range Chiron from raising ValueError to SILENT CLAMPING. The migration.md range line must reflect both the new range (1900–2100) and the new behavior (clamp, no ValueError).
- Do NOT touch the v1.2/v1.3 sections except the Chiron line at ~58 if it lives in the v1.2→v1.3 section (it documents `long(jd, 13)` Chiron — that is the line to fix).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Fix the Chiron date-range line (1900–2100 + clamping, not ValueError)</name>
  <files>docs/source/migration.md</files>
  <action>
At line ~58, replace:

`Valid date range: 1950–2050. Attempting to compute Chiron outside this range raises a \`ValueError\`.`

with:

`Valid date range: 1900–2100 (expanded in v1.4). Out-of-range input is silently clamped to the nearest segment boundary — no \`ValueError\` is raised.`

Self-check: `grep -n "1950\|2050\|ValueError" docs/source/migration.md` returns no hit for the Chiron line (the only remaining ValueError mentions, if any, must be unrelated to Chiron range — verify each).
  </action>
  <verify>`grep -n "1900–2100\|clamped" docs/source/migration.md` → ≥1 hit on the Chiron line. `grep -n "1950–2050" docs/source/migration.md` → 0 hits. The word "ValueError" no longer appears in the Chiron range context.</verify>
  <done>The Chiron range line states 1900–2100 (expanded in v1.4) and silent clamping; the ValueError claim is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale EXTENDED-default claim in the v1.0→v1.1 section</name>
  <files>docs/source/migration.md</files>
  <action>
**2a — Fix the prose claim at line ~131.** Replace:

`\`calculate_aspects\` now accepts an optional \`aspects\` parameter. Without it, behavior is unchanged (EXTENDED = all 14 aspects).`

with:

`\`calculate_aspects\` now accepts an optional \`aspects\` parameter. When introduced in v1.1 the default was \`EXTENDED\` (all 14 aspects). **As of v1.3 the library default is \`TRADITIONAL\`** (7 half-circle aspects); see [Aspects](concepts.md) for the preset table.`

**2b — Fix the code comment at line ~136.** Change:

`# v1.0 behavior (unchanged default)`

to:

`# v1.1 default at the time (EXTENDED — all 14 aspects; the current v1.3+ default is TRADITIONAL)`

Leave the surrounding code (`all_aspects = calculate_aspects(jd)` and the CLASSICAL example) intact — only the comment text changes.

Self-check: `grep -ni "unchanged (EXTENDED\|EXTENDED = all 14 aspects" docs/source/migration.md` → 0 hits. `grep -n "TRADITIONAL" docs/source/migration.md` → ≥2 hits (prose + comment).
  </action>
  <verify>`grep -ni "unchanged (EXTENDED\|EXTENDED = all 14 aspects" docs/source/migration.md` → 0 hits. The v1.0→v1.1 section now names TRADITIONAL as the current default and points to concepts.md.</verify>
  <done>No stale "EXTENDED = default / unchanged" claim remains; the section makes clear the current library default is TRADITIONAL while preserving the v1.1 history.</done>
</task>

</tasks>

<verification>
- `grep -ni "unchanged (EXTENDED\|EXTENDED = all 14 aspects\|classical.*default\|default.*classical" docs/source/migration.md` → 0 hits.
- `grep -n "1950–2050" docs/source/migration.md` → 0 hits.
- `grep -n "1900–2100\|clamped" docs/source/migration.md` → ≥1 hit.
- `grep -n "TRADITIONAL" docs/source/migration.md` → ≥1 hit.
</verification>

<success_criteria>
DOC-15 satisfied for migration.md: no claim that the default aspect set is EXTENDED; the current v1.3+ TRADITIONAL default is the only stated current default (v1.1 history preserved, annotated). DOC-16 Chiron-behavior part satisfied: range is 1900–2100 with silent clamping (no ValueError).
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-02-SUMMARY.md` recording the exact before/after of both edits and the grep results proving 0 stale default/range hits.
</output>
