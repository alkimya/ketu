---
phase: 31-documentation-en-fr
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/changelog.md
  - docs/source/conf.py
autonomous: true

must_haves:
  truths:
    - "changelog.md has a new `## [1.4.0]` section listing Added (generate_harmonic_aspects dynamic generator, Chiron range 1900–2100) and Changed (Chiron orb 0°→4°, Chiron out-of-range now clamps instead of ValueError, documentation recentring on 180°-division default)"
    - "The stale v1.1 `EXTENDED (14 — default)` entry is annotated inline with '(changed to TRADITIONAL in v1.3)' — history preserved, not rewritten"
    - "The v1.3 Chiron changelog entries (1142 segments / 1950–2050 / 0.005695°) are LEFT as historical v1.3 records — the v1.4 range expansion is documented only in the new [1.4.0] section"
    - "conf.py `version` and `release` are bumped to 1.4.0 (docs-build metadata only; NOT the package version in pyproject.toml/ketu/__init__.py)"
    - "No reference to Kala appears in changelog.md"
  artifacts:
    - path: docs/source/changelog.md
      provides: "[1.4.0] section + annotated v1.1 default note"
      contains: "[1.4.0]"
    - path: docs/source/conf.py
      provides: "Docs-build version bumped to 1.4.0"
      contains: "1.4.0"
  key_links:
    - from: "changelog.md [1.4.0] section"
      to: "v1.4 feature set"
      via: "Added/Changed entries for harmonics generator, Chiron range, Chiron orb, clamping"
      pattern: "1.4.0"
---

<objective>
Add the `## [1.4.0]` changelog section to `docs/source/changelog.md` documenting the v1.4 changes (DOC-16), annotate the stale v1.1 `EXTENDED (14 — default)` entry to preserve history accurately (DOC-15), and bump the docs-build version strings in `docs/source/conf.py` to 1.4.0 so the rendered docs don't display the wrong version while documenting v1.4.

Purpose: The changelog page (a docs page, in scope here — distinct from the repo-root release CHANGELOG.md which is Phase 32) must record the v1.4 features. The conf.py version bump is a docs-build concern (the RTD theme shows the version prominently); leaving it at 1.3.0 would be visible doc/code drift. NOTE: this bumps ONLY the Sphinx docs-build metadata — the package version in `pyproject.toml` and `ketu/__init__.py` remains a Phase 32 (Release) task.

Output: An edited `docs/source/changelog.md` with a `[1.4.0]` section and an annotated v1.1 entry, plus `conf.py` showing version/release = 1.4.0.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/changelog.md
@docs/source/conf.py
</context>

<notes>
- HISTORY-PRESERVATION: the v1.1 `EXTENDED (14 — default)` entry (line ~41) and the v1.3 Chiron entries (lines ~12–13, 1142 segments / 1950–2050 / 0.005695°) are HISTORICALLY ACCURATE for those releases. Do NOT rewrite them. For v1.1, append an inline annotation "(changed to TRADITIONAL in v1.3)". For the v1.3 Chiron entries, leave them as-is — the v1.4 range expansion belongs ONLY in the new [1.4.0] section.
- conf.py version decision (planner judgment, stated in plan): bump `version`/`release` to "1.4.0" HERE because the rendered docs display the version prominently and this is the documentation phase. This is docs-build metadata, NOT the package version (pyproject.toml + ketu/__init__.py stay at 1.3.0 until Phase 32 Release). This does NOT touch any `ketu/` runtime source.
- v1.4 confirmed facts for the [1.4.0] Added/Changed entries: generate_harmonic_aspects(h) dynamic generator (any integer harmonic 2–64, dynamic_specs= param); Chiron range expanded 1950–2050 → 1900–2100 (2283 segments, max error 0.001214°); Chiron orb 0° → 4° (Pluto parity); Chiron out-of-range now silently clamps (was ValueError); documentation recentred on the 180°-division default (EXTENDED out of doc tables).
- Use the existing changelog date placeholder style ("2026-06-XX") to match neighbouring entries; the final release date is set in Phase 32.
- Kala: changelog.md currently has 0 hits — verify it STAYS 0.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Add the [1.4.0] changelog section</name>
  <files>docs/source/changelog.md</files>
  <action>
Insert a new `## [1.4.0] - 2026-06-XX` section ABOVE the existing `## [1.3.0]` section (line ~8), right after the intro paragraph (after line ~6, the SemVer line). Use the project's existing structure (`### Added 1.4.0`, `### Changed 1.4.0`). Content:

`## [1.4.0] - 2026-06-XX`

`### Added 1.4.0`
- **`generate_harmonic_aspects(h)` — dynamic harmonic generator**: builds aspect specs on the fly for ANY integer harmonic `h` (2 ≤ h ≤ 64), using the full-circle 360° convention folded to 0–180° (`coef = k/h`). Returns a structured array that is a drop-in for `core.aspects`; pass it as the `dynamic_specs=` argument to `calculate_aspects`, `find_aspects_between_dates`, and `calculate_synastry`. The frozen 14-row `core.aspects` table and preset fingerprints are byte-identical (parallel code path).
- **Chiron range expanded to 1900–2100**: `ketu/data/chiron_coeffs.npz` regenerated (2283 Chebyshev segments, `jd_start=2415020.5`, `jd_end=2488069.5`), max error 0.001214° over the new range. Pure-NumPy runtime preserved.

`### Changed 1.4.0`
- **Chiron orb 0° → 4°** (Pluto parity): `core.bodies['orb']` for Chiron is now 4°, so Chiron now forms scored aspects.
- **Chiron out-of-range behaviour**: input outside 1900–2100 is now **silently clamped** to the nearest segment boundary (previously raised `ValueError`).
- **Documentation recentred on the 180°-division default**: `concepts.md` aspect tables now show CLASSICAL (5) and TRADITIONAL (7) only; the full-circle minor harmonics (H5/H9/H10 / EXTENDED) remain available in code but are out of the summary tables.

Self-check: `grep -n "\[1.4.0\]" docs/source/changelog.md` → ≥1 hit; `grep -n "generate_harmonic_aspects" docs/source/changelog.md` → ≥1 hit; `grep -ni "kala" docs/source/changelog.md` → 0 hits.
  </action>
  <verify>`grep -n "\[1.4.0\]" docs/source/changelog.md` → ≥1 hit. The new section sits ABOVE `[1.3.0]` and lists Added (generate_harmonic_aspects, Chiron 1900–2100) and Changed (Chiron orb 4°, clamping, doc recentring). `grep -ni "kala" docs/source/changelog.md` → 0 hits.</verify>
  <done>A `[1.4.0]` changelog section documents the v1.4 dynamic-harmonics generator, Chiron 1900–2100 range, Chiron orb 4°, clamping behavior, and the documentation recentring.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the stale v1.1 EXTENDED-default entry + bump conf.py docs-build version to 1.4.0</name>
  <files>docs/source/changelog.md, docs/source/conf.py</files>
  <action>
**2a — Annotate the v1.1 entry (changelog.md line ~41).** In the `[1.1.0]` `Added` entry, change:

`\`CLASSICAL\` (5 aspects), \`TRADITIONAL\` (7), \`EXTENDED\` (14 — default).`

to:

`\`CLASSICAL\` (5 aspects), \`TRADITIONAL\` (7), \`EXTENDED\` (14 — default at v1.1, changed to \`TRADITIONAL\` in v1.3).`

Leave the rest of the v1.1 entry (and ALL v1.2/v1.3 entries, including the 1142-segments / 1950–2050 / 0.005695° Chiron records) UNCHANGED — they are accurate history for those releases.

Self-check: `grep -ni "EXTENDED.*default" docs/source/changelog.md` → the only remaining hit is the annotated v1.1 line that explicitly says "default at v1.1, changed to TRADITIONAL in v1.3" (acceptable historical annotation), with NO claim that EXTENDED is the CURRENT default.

**2b — Bump conf.py docs-build version (conf.py lines 14–15).** Change:

`release = "1.3.0"` → `release = "1.4.0"`
`version = "1.3.0"` → `version = "1.4.0"`

These two lines ONLY. Do NOT touch `pyproject.toml` or `ketu/__init__.py` (package version is Phase 32). Do NOT change any other conf.py setting (notably leave `html_theme_options` `display_version` untouched — it is the 1-warning baseline).

Self-check: `grep -n '1.3.0' docs/source/conf.py` → 0 hits; `grep -n '1.4.0' docs/source/conf.py` → 2 hits (version + release).
  </action>
  <verify>`grep -ni "EXTENDED.*default" docs/source/changelog.md` → only the annotated historical v1.1 line (states "changed to TRADITIONAL in v1.3"), no current-default claim. `grep -n '1.4.0' docs/source/conf.py` → exactly 2 hits (version + release). `grep -n '1.3.0' docs/source/conf.py` → 0 hits. pyproject.toml and ketu/__init__.py untouched.</verify>
  <done>The v1.1 EXTENDED-default entry is annotated to record the v1.3 change (history preserved); conf.py docs-build version/release are 1.4.0 (package version untouched).</done>
</task>

</tasks>

<verification>
- `grep -n "\[1.4.0\]" docs/source/changelog.md` → ≥1 hit; section lists harmonics generator + Chiron 1900–2100 (Added) and Chiron orb 4° + clamping + doc recentring (Changed).
- `grep -ni "EXTENDED.*default" docs/source/changelog.md` → only the annotated v1.1 historical line (no current-default claim).
- `grep -n '1.4.0' docs/source/conf.py` → 2 hits; `grep -n '1.3.0' docs/source/conf.py` → 0 hits.
- `grep -ni "kala" docs/source/changelog.md` → 0 hits.
- `git status` shows pyproject.toml and ketu/__init__.py UNCHANGED (package version is Phase 32).
</verification>

<success_criteria>
DOC-16 changelog part satisfied: `[1.4.0]` section documents the dynamic-harmonics generator, Chiron 1900–2100 range, Chiron orb 4°, and clamping. DOC-15 changelog part satisfied: the v1.1 EXTENDED-default entry is annotated (history preserved, no current-default claim). conf.py docs-build version is 1.4.0 (judgment call: bump docs metadata only; package version deferred to Phase 32). No Kala reference; no `ketu/` runtime source touched.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-06-SUMMARY.md` recording the [1.4.0] section content, the v1.1 annotation, the conf.py version bump (and explicit confirmation pyproject.toml/ketu/__init__.py were NOT touched), and the grep results.
</output>
