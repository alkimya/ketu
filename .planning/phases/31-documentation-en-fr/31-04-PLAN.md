---
phase: 31-documentation-en-fr
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/api.md
autonomous: true

must_haves:
  truths:
    - "api.md has a new `generate_harmonic_aspects(h)` subsection in the Aspects section, between `aspects_for_harmonics` and the `core.aspects columns` subsection"
    - "The generate_harmonic_aspects subsection has a runnable example and the ~2× smaller full-circle orb note"
    - "The Chiron section (lines ~725–726) states valid range 1900–2100 (2283 segments) and max error 0.001214° — not 1950–2050 / 0.005695°"
    - "No reference to Kala appears anywhere in api.md"
  artifacts:
    - path: docs/source/api.md
      provides: "generate_harmonic_aspects API subsection + corrected Chiron range/accuracy"
      contains: "generate_harmonic_aspects"
  key_links:
    - from: "api.md Aspects section"
      to: "generate_harmonic_aspects subsection"
      via: "placed between aspects_for_harmonics and core.aspects columns"
      pattern: "generate_harmonic_aspects"
---

<objective>
Add the `generate_harmonic_aspects(h)` API subsection to `docs/source/api.md` (DOC-16) and correct the Chiron section's range/accuracy numbers (DOC-15/DOC-16). The Aspects section already documents `aspects_for_harmonics` in detail; `generate_harmonic_aspects` is the dynamic (any-integer-harmonic) sibling and must be documented alongside it. The Chiron section still says 1950–2050 / 0.005695° — must become 1900–2100 (2283 segments) / 0.001214°.

Purpose: The api.md page is the canonical full API reference. It must surface the v1.4 dynamic-harmonics generator with a runnable example + the accepted orb note, and reflect the expanded Chiron range and improved accuracy figure.

Output: An edited `docs/source/api.md` with a new `generate_harmonic_aspects(h)` subsection and corrected Chiron range/accuracy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/api.md
@ketu/aspects/harmonics.py
</context>

<notes>
- The Preset Aspect Sets table (lines 179–183) is already CORRECT (TRADITIONAL = Library default v1.3+). Do NOT change it. EXTENDED stays in this table — api.md is the home of the full 14-aspect enumeration (concepts.md points here for the full table).
- Insert the new subsection AFTER the `### \`aspects_for_harmonics(harmonics)\`` block (ends ~line 219) and BEFORE `### \`core.aspects\` columns: \`harmonic\` and \`symbol\`` (line 221).
- generate_harmonic_aspects signature (verified `ketu/aspects/harmonics.py:118`): `def generate_harmonic_aspects(h: int) -> npt.NDArray[np.void]`. h must be int, 2 ≤ h ≤ 64. Returns structured array with the same dtype as `core.aspects`, shape `(h // 2,)`. Export: `from ketu.aspects import generate_harmonic_aspects`.
- v1.4 confirmed numbers: Chiron range 1900–2100, 2283 segments, max error 0.001214°.
- Kala: api.md currently has 0 hits — verify it STAYS 0 (do not introduce any).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Add the generate_harmonic_aspects(h) subsection</name>
  <files>docs/source/api.md</files>
  <action>
Insert a new subsection between the `aspects_for_harmonics` block (ends ~line 219) and the `core.aspects columns` subsection (line 221):

`### \`generate_harmonic_aspects(h)\``

Content:
- One/two sentences: Generate aspect specs on the fly for ANY integer harmonic `h` (not just the named preset harmonics `{1,2,3,5,6,9,10}`). Uses the full-circle 360° convention folded to 0–180° (`angle = fold_to_0_180(k · 360 / h)` for `k = 1 … h//2`; `coef = k / h`).
- Parameters: `h` (`int`): the harmonic, `2 ≤ h ≤ 64`. `h = 1` produces 0 rows; `h > 64` is rejected.
- Returns: structured array with the SAME dtype as `core.aspects` (drop-in), shape `(h // 2,)`. Names are auto-generated (`b'H7-1'`, …). Pass it as the `dynamic_specs=` argument to `calculate_aspects`, `find_aspects_between_dates`, and `calculate_synastry`.
- Raises: `ValueError` if `h` is not an int or is outside `[2, 64]`.
- Runnable example (copy verbatim from RESEARCH §5):

```python
from ketu.aspects import generate_harmonic_aspects

# Harmonic 7: 3 unique folded angles (septile family)
specs = generate_harmonic_aspects(7)
print(len(specs))          # 3
print(specs['name'])       # [b'H7-1', b'H7-2', b'H7-3']
print(specs['angle'])      # [51.43, 102.86, 154.29]
print(specs['coef'])       # [0.1429, 0.2857, 0.4286]

# Pass to calculate_aspects via dynamic_specs:
from ketu.aspects import calculate_aspects
result = calculate_aspects(jd, dynamic_specs=generate_harmonic_aspects(7))
```

- The MANDATORY orb note (DOC-16), as a `> **Note on orbs:**` blockquote:

> **Note on orbs:** Dynamic harmonic orbs use `coef = k / h` (full-circle convention). For high harmonics this yields orbs roughly half the size of the equivalent half-circle aspect — accepted behaviour; the two conventions coexist without unification (see the v1.4 release notes).

Self-check: `grep -n "generate_harmonic_aspects" docs/source/api.md` → ≥1 hit; `grep -n "dynamic_specs" docs/source/api.md` → ≥1 hit; the subsection sits before the `core.aspects columns` heading.
  </action>
  <verify>`grep -n "generate_harmonic_aspects" docs/source/api.md` → ≥1 hit. The subsection is between `aspects_for_harmonics` and `core.aspects columns`. The orb note ("accepted behaviour" / "coexist") is present.</verify>
  <done>`generate_harmonic_aspects(h)` documented in api.md with parameters/returns/raises, a runnable example, and the ~2× smaller full-circle orb note.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the Chiron section range and accuracy numbers</name>
  <files>docs/source/api.md</files>
  <action>
In the "Chiron (body_id=13)" section, at lines ~725–726, replace:

`- Valid date range: 1950–2050 (Chebyshev polynomial coefficients embedded in \`ketu/data/chiron_coeffs.npz\`)`
`- Accuracy: max error 0.005695° (sub-arcminute) over the 1950–2050 range`

with:

`- Valid date range: 1900–2100 (expanded in v1.4; 2283 Chebyshev segments embedded in \`ketu/data/chiron_coeffs.npz\`). Out-of-range input is silently clamped to the nearest segment boundary.`
`- Accuracy: max error 0.001214° (sub-arcminute) over the 1900–2100 range`

Leave the rest of the Chiron section (the body_id facts, the breaking-change note, the code examples) unchanged. Note: the heading still reads "New in v1.3" because Chiron itself shipped in v1.3 — only the range/accuracy numbers (a v1.4 change) are updated. Do NOT change the heading.

Self-check: `grep -n "1950\|2050\|0.005695" docs/source/api.md` → 0 hits. `grep -ni "kala" docs/source/api.md` → 0 hits.
  </action>
  <verify>`grep -n "1900–2100\|2283\|0.001214" docs/source/api.md` → ≥1 hit. `grep -n "1950\|2050\|0.005695" docs/source/api.md` → 0 hits. `grep -ni "kala" docs/source/api.md` → 0 hits.</verify>
  <done>The Chiron section states range 1900–2100 (2283 segments, clamping) and max error 0.001214°; no stale 1950–2050 / 0.005695° remains; no Kala reference introduced.</done>
</task>

</tasks>

<verification>
- `grep -n "generate_harmonic_aspects" docs/source/api.md` → ≥1 hit.
- `grep -n "1950\|2050\|0.005695" docs/source/api.md` → 0 hits.
- `grep -n "1900–2100\|0.001214" docs/source/api.md` → ≥1 hit.
- `grep -ni "kala" docs/source/api.md` → 0 hits.
- The Preset Aspect Sets table (TRADITIONAL = Library default) is unchanged.
</verification>

<success_criteria>
DOC-16 satisfied for api.md: `generate_harmonic_aspects(h)` documented with a runnable example + the ~2× orb note; Chiron range 1900–2100 and orb facts correct; max error 0.001214°; no Kala reference. DOC-15 supported: no stale default/range claim introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-04-SUMMARY.md` recording the placement of the new subsection, the exact Chiron line before/after, and the grep results (generate_harmonic_aspects ≥1, 1950/2050/0.005695 = 0, kala = 0).
</output>
