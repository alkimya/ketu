---
phase: 31-documentation-en-fr
plan: 07
type: execute
wave: 2
depends_on: ["31-01", "31-02", "31-03", "31-04", "31-05", "31-06"]
files_modified:
  - docs/locale/fr/LC_MESSAGES/concepts.po
  - docs/locale/fr/LC_MESSAGES/migration.po
  - docs/locale/fr/LC_MESSAGES/relational_charts.po
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/chiron.po
  - docs/locale/fr/LC_MESSAGES/changelog.po
  - docs/locale/fr/LC_MESSAGES/concepts.mo
  - docs/locale/fr/LC_MESSAGES/migration.mo
  - docs/locale/fr/LC_MESSAGES/relational_charts.mo
  - docs/locale/fr/LC_MESSAGES/api.mo
  - docs/locale/fr/LC_MESSAGES/chiron.mo
  - docs/locale/fr/LC_MESSAGES/changelog.mo
autonomous: true

must_haves:
  truths:
    - "make gettext + make update-po run AFTER all 6 Wave-1 English edits, re-extracting POTs and merging into the 6 touched .po (fuzzies + new empties marked)"
    - "Every fuzzy and every empty msgstr in the 6 touched .po (concepts, migration, relational_charts, api, chiron, changelog) is translated to French — zero English-fallback, zero fuzzy remaining"
    - "MyST roles ({ref}/{doc}/all {role}`...`) are preserved verbatim in every translated msgstr; no heading markers added that are absent from the msgid; FR space before ':'"
    - "The 6 .mo are recompiled via make build-mo (sphinx-intl)"
    - "make html (en) builds at exactly 1 WARNING (the display_version baseline)"
    - "make html-fr builds at exactly 1 WARNING (the display_version baseline) — no NEW warning (myst.header, broken xref) introduced"
    - "The other 11 .po (untouched pages) accumulated NO fuzzies/empties — if any did, that page's source wasn't supposed to change; investigate"
  artifacts:
    - path: docs/locale/fr/LC_MESSAGES/concepts.po
      provides: "Re-translated concepts catalog (0 empty, 0 fuzzy)"
    - path: docs/locale/fr/LC_MESSAGES/chiron.po
      provides: "Re-translated chiron catalog (clamping/range strings)"
    - path: docs/locale/fr/LC_MESSAGES/concepts.mo
      provides: "Recompiled concepts catalog"
  key_links:
    - from: "6 modified English .md (Wave 1)"
      to: "6 re-extracted .po"
      via: "make gettext && make update-po"
      pattern: "update-po"
    - from: "6 translated .po"
      to: "6 recompiled .mo"
      via: "make build-mo (sphinx-intl)"
      pattern: "build-mo"
    - from: "6 .mo + unchanged sources"
      to: "clean en+fr HTML at 1 warning each"
      via: "make html + make html-fr"
      pattern: "html-fr"
---

<objective>
Run the full French gettext cycle on the 6 catalogs whose English source was edited in Wave 1 (DOC-17), then verify both en + fr Sphinx builds at the 1-warning baseline. Re-extract POTs, merge into the `.po` (marking changed strings fuzzy and new strings empty), translate every fuzzy/empty msgstr in `concepts.po`, `migration.po`, `relational_charts.po`, `api.po`, `chiron.po`, `changelog.po` to zero-English-fallback French, recompile the `.mo`, and assert each build (en + fr) produces exactly 1 warning (the known `display_version` theme warning).

Purpose: This is the i18n half of Phase 31 and the final phase gate. It MUST run after ALL Wave-1 English edits (gettext extraction reads the modified source — running early would miss strings). It enforces DOC-17 as command-checkable facts: 0 empty/0 fuzzy across the 6 touched catalogs, no doc/code drift, and a clean en+fr build at 1 warning each.

Output: 6 re-translated `.po` (0 empty / 0 fuzzy) + 6 recompiled `.mo`; clean en + fr builds at exactly 1 warning each.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/Makefile
</context>

<notes>
- HARD ORDERING: this plan depends on all 6 Wave-1 plans. `make gettext` reads the CURRENT source state — if any Wave-1 edit is missing, extraction misses strings (RESEARCH Pitfall 5).
- `cd` does NOT persist between bash calls — every command is a single compound `cd /home/loc/workspace/ketu/docs && <cmd>` or uses absolute paths.
- The .po AND .mo files are git-tracked (Phase 26.1 committed them). Both must be updated.
- Marker-preservation rules (Phase 26.1, carry forward): preserve `{ref}\`...\``, `{doc}\`...\``, and all `{role}\`...\`` verbatim; NEVER add `#`/`##`/`###` heading markers absent from the msgid; code-only msgid (entirely backtick-wrapped) copied verbatim as msgstr; keep `**bold**`; translate text; FR space before `:` (e.g. `Paramètres :`).
- Baseline (confirmed 2026-06-03): exactly 1 WARNING per build — "display_version n'est pas supportée pour ce thème". Any OTHER warning (myst.header, broken xref) is a regression and BLOCKS the phase: trace it to the offending .po msgstr, fix, recompile (Task 2), re-build.
- Do NOT translate the new English technical TERMS that are code identifiers (generate_harmonic_aspects, dynamic_specs, TRADITIONAL, EXTENDED, CLASSICAL, ValueError-free clamping note's code) — keep code/identifiers verbatim; translate ONLY surrounding prose.
- Expected newly-fuzzy/empty content per file (from Wave 1): concepts (collapsed H5/H9/H10 note, trimmed table lead-ins, generate_harmonic_aspects subsection + orb note, Chiron range note), migration (EXTENDED-default fix + Chiron clamp line), relational_charts (2 aspects-default notes), api (generate_harmonic_aspects subsection + Chiron range/accuracy), chiron (table rows + clamping section), changelog ([1.4.0] section + v1.1 annotation).
- DO NOT run gettext/update-po for the other 11 pages' translations — only translate the 6 touched .po. (update-po runs over all, but only the 6 touched should have new fuzzies/empties; verify the other 11 stayed clean.)
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Re-extract POTs, merge into .po, translate all fuzzies/empties in the 6 touched catalogs</name>
  <files>docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/migration.po, docs/locale/fr/LC_MESSAGES/relational_charts.po, docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/chiron.po, docs/locale/fr/LC_MESSAGES/changelog.po</files>
  <action>
**1a — Re-extract and merge (run from docs/):**

```
cd /home/loc/workspace/ketu/docs && make gettext
cd /home/loc/workspace/ketu/docs && make update-po
```

`make gettext` writes `docs/build/gettext/*.pot`; `make update-po` merges into `docs/locale/fr/LC_MESSAGES/*.po`, marking changed strings `#, fuzzy` and adding new strings as empty `msgstr ""`.

**1b — Identify what changed.** For each of the 6 touched .po, list the fuzzy + empty entries (use the babel check from RESEARCH §7, or a grep for `#, fuzzy` and empty `msgstr ""`):

```
for po in concepts migration relational_charts api chiron changelog; do
  f="/home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/$po.po"
  echo "=== $po ==="; grep -c "#, fuzzy" "$f"; grep -cE '^msgstr ""$' "$f"
done
```

**1c — Translate.** Edit each of the 6 .po files. For EVERY fuzzy entry: re-translate the msgstr to match the changed msgid, then REMOVE the `#, fuzzy` flag line. For EVERY empty `msgstr ""`: write the French translation. Apply the marker-preservation rules (see notes): MyST roles verbatim; no spurious heading markers; code-only msgid copied verbatim; FR space before `:`; keep code identifiers (generate_harmonic_aspects, dynamic_specs, TRADITIONAL, etc.) untranslated. For code-block msgids (entirely a fenced ```python block) copy verbatim.

**1d — Verify the 6 are clean (0 empty, 0 fuzzy)** using the babel script from RESEARCH §7 (the 6-file loop printing OK/FAIL per file and a final PASS/FAIL). Also confirm the OTHER 11 .po stayed clean:

```
for po in /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/*.po; do
  base=$(basename "$po")
  fz=$(grep -c "#, fuzzy" "$po"); mt=$(grep -cE '^msgstr ""$' "$po")
  [ "$fz" -ne 0 -o "$mt" -ne 1 ] && echo "$base: fuzzy=$fz empty(approx)=$mt"
done
```

(The header msgstr "" is line 1 of every .po — so a clean file shows exactly 1 `^msgstr ""$`; the babel script is the authoritative check that distinguishes header from real empties.)
  </action>
  <verify>The RESEARCH §7 babel script prints OK for all 6 touched catalogs and final `PASS`. The other 11 .po report 0 fuzzy. No msgstr contains an English-fallback for a changed string. MyST roles preserved (spot-check `{ref}`/`{doc}` lines unchanged between msgid and msgstr structure).</verify>
  <done>POTs re-extracted and merged; all fuzzy/empty entries in the 6 touched catalogs translated to French (0 empty, 0 fuzzy); markers preserved; the other 11 catalogs untouched/clean.</done>
</task>

<task type="auto">
  <name>Task 2: Recompile .mo and verify en + fr builds at exactly 1 warning each</name>
  <files>docs/locale/fr/LC_MESSAGES/concepts.mo, docs/locale/fr/LC_MESSAGES/migration.mo, docs/locale/fr/LC_MESSAGES/relational_charts.mo, docs/locale/fr/LC_MESSAGES/api.mo, docs/locale/fr/LC_MESSAGES/chiron.mo, docs/locale/fr/LC_MESSAGES/changelog.mo</files>
  <action>
**2a — Recompile .mo via the gettext infra (NOT manual per-file msgfmt):**

```
cd /home/loc/workspace/ketu/docs && make build-mo
```

This runs `sphinx-intl build -d locale`, recompiling all catalogs (the 6 touched ones get fresh .mo). Confirm the 6 touched .mo exist and are non-empty:

```
ls -la /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/{concepts,migration,relational_charts,api,chiron,changelog}.mo
```

**2b — Build en + fr, assert exactly 1 warning each:**

```
cd /home/loc/workspace/ketu/docs && make html 2>&1 | tee /tmp/31-en.log
cd /home/loc/workspace/ketu/docs && make html-fr 2>&1 | tee /tmp/31-fr.log
grep -c "WARNING" /tmp/31-en.log    # MUST be 1
grep -c "WARNING" /tmp/31-fr.log    # MUST be 1
grep "WARNING" /tmp/31-en.log /tmp/31-fr.log   # MUST be the display_version theme warning only
```

If either build shows a SECOND warning (myst.header from a stray heading marker, broken xref from a mangled MyST role, etc.), STOP — the offending .po msgstr must be fixed (re-open Task 1 for that file), `make build-mo` re-run, and the build re-checked. Do NOT proceed with a new warning.

**2c — Run the requirement-level greps over the final English source (regression guard that Wave 1 stuck):**

```
# Stale Chiron range — EXCLUDE changelog.md (Plan 06 intentionally keeps the v1.3
# historical entry "1950–2050 / 1142 segments / 0.005695°" as accurate release history):
grep -rn "1950\|2050" /home/loc/workspace/ketu/docs/source/ --include="*.md" | grep -v "changelog.md"   # MUST be 0
grep -rni "kala" /home/loc/workspace/ketu/docs/source/ --include="*.md"               # MUST be 0
grep -rni "unchanged (EXTENDED\|EXTENDED = all 14 aspects\|default classical set" /home/loc/workspace/ketu/docs/source/ --include="*.md"   # MUST be 0
grep -rn "generate_harmonic_aspects" /home/loc/workspace/ketu/docs/source/api.md      # MUST be ≥1
```

(Why the changelog exclusion: `0.005695°` and `1950–2050` legitimately live in the historical v1.3 changelog entries, which Wave-1 Plan 06 deliberately preserved as accurate release history — do NOT assert these == 0 globally. The v1.4 range expansion is documented only in the new `[1.4.0]` section. A naive global `grep "1950\|2050"` over all `.md` WOULD hit changelog.md's historical v1.3 entry — that hit is allowed, which is exactly why the grep above pipes through `grep -v "changelog.md"`.)
  </action>
  <verify>`grep -c "WARNING" /tmp/31-en.log` == 1 AND `grep -c "WARNING" /tmp/31-fr.log` == 1, both the display_version warning. The 6 touched .mo are recompiled and non-empty. Requirement greps: 1950/2050 (excluding changelog historical) = 0; kala = 0; stale-default strings = 0; generate_harmonic_aspects in api.md ≥1.</verify>
  <done>6 .mo recompiled via the gettext infra; en + fr builds both at exactly 1 (known display_version) warning; final requirement greps pass (no stale range outside the historical changelog entry, no Kala, no stale default, generator documented).</done>
</task>

</tasks>

<verification>
- RESEARCH §7 babel script: 6 touched .po all OK, final PASS (0 empty, 0 fuzzy).
- Other 11 .po: 0 fuzzy (untouched pages stayed clean).
- `make build-mo` recompiled the 6 .mo (non-empty).
- `grep -c "WARNING" /tmp/31-en.log` == 1; `grep -c "WARNING" /tmp/31-fr.log` == 1; both display_version.
- `grep -rn "1950\|2050" docs/source/ --include="*.md" | grep -v changelog.md` → 0; `grep -rni "kala" docs/source/ --include="*.md"` → 0; stale-default greps → 0; `generate_harmonic_aspects` in api.md → ≥1.
</verification>

<success_criteria>
DOC-17 satisfied: every French gettext catalog whose source changed (the 6) is re-extracted, translated (zero English-fallback msgstr, zero fuzzy), and recompiled to `.mo`; `make html` (en) and `make html-fr` build clean at the established 1-warning baseline (no new warnings). No doc/code drift: final requirement greps confirm no stale range (outside the historical changelog), no Kala, no stale default, and the dynamic generator is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-07-SUMMARY.md` recording: per-file fuzzy/empty counts before translation, confirmation all 6 are 0/0 after, the en + fr warning counts (1/1) with the warning text, the recompiled .mo sizes, and the final requirement-grep results.
</output>
