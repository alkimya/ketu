---
phase: 31-documentation-en-fr
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/chiron.md
autonomous: true

must_haves:
  truths:
    - "The Implementation Details table valid-date-range row says 1900-01-01 to 2100-12-31 (with the correct Julian Days) — not 1950–2050"
    - "The Position accuracy row says max error 0.001214° — not 0.005695°"
    - "The 'Date Range and Error Behaviour' section says out-of-range input is silently clamped to the nearest segment boundary; it does NOT say a ValueError is raised; the ValueError code example is removed or rewritten as a clamping note"
    - "Changed v1.4 values are marked (New in v1.4) mirroring the existing (New in v1.3) pattern"
    - "No reference to Kala appears in chiron.md"
  artifacts:
    - path: docs/source/chiron.md
      provides: "Chiron page updated to 1900–2100 range, clamping behavior, 0.001214° accuracy"
      contains: "1900"
  key_links:
    - from: "chiron.md Date Range and Error Behaviour section"
      to: "Phase 30 clamping behavior"
      via: "clamping note replacing the ValueError example"
      pattern: "clamp"
---

<objective>
Update `docs/source/chiron.md` to reflect the v1.4 Chiron changes (DOC-16): the valid date range 1950–2050 → 1900–2100, the accuracy 0.005695° → 0.001214°, and — most importantly — the Phase 30 BEHAVIOR CHANGE from raising `ValueError` to silent clamping. The "Date Range and Error Behaviour" section currently shows a code example that raises ValueError for out-of-range input; that example must be removed/rewritten as a clamping note.

Purpose: chiron.md is the dedicated Chiron page; it currently documents stale range, stale accuracy, and the OLD (pre-Phase-30) error semantics. Out-of-range Chiron no longer raises — it clamps. The doc must not tell users to expect a ValueError that no longer happens.

Output: An edited `docs/source/chiron.md` with the 1900–2100 range, 0.001214° accuracy, clamping behavior (no ValueError), and (New in v1.4) badges on changed values.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-en-fr/31-RESEARCH.md
@docs/source/chiron.md
@ketu/ephemeris/chiron.py
</context>

<notes>
- BEHAVIOR CHANGE (Phase 30, confirmed in source `ketu/ephemeris/chiron.py`): out-of-range Chiron input is now silently CLAMPED to the nearest segment boundary; it no longer raises ValueError. The chiron.md:64–71 ValueError example is now WRONG and must be removed/rewritten.
- v1.4 confirmed numbers: range 1900-01-01 to 2100-12-31; Julian Days 2415020.5 to 2488069.5 (jd_start/jd_end from the regenerated .npz, per STATE.md / Phase 30); max error 0.001214°.
- The page heading "Chiron — the 14th Body" and the "Ketu v1.3 adds Chiron" framing stay (Chiron shipped in v1.3); only the v1.4-changed values get (New in v1.4) badges, mirroring the existing (New in v1.3) pattern elsewhere in the docs.
- Kala: chiron.md currently has 0 hits — verify it STAYS 0.
- The Phase 23 reference at line ~73 ("0.01° target set during the Chebyshev spike (Phase 23)") may stay as context, but update the guaranteed-error number to 0.001214°.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Update the Implementation Details table (range row + accuracy row)</name>
  <files>docs/source/chiron.md</files>
  <action>
**1a — Valid date range row (line ~10).** Replace:

`| Valid date range | 1950-01-01 to 2050-12-31 (Julian Days ~2433282 to ~2469807) |`

with:

`| Valid date range | 1900-01-01 to 2100-12-31 (Julian Days 2415020.5 to 2488069.5) — expanded in v1.4 |`

**1b — Position accuracy row (line ~11).** Replace:

`| Position accuracy | max error 0.005695° (sub-arcminute, ~20 arcseconds) |`

with:

`| Position accuracy | max error 0.001214° (sub-arcminute, ~4 arcseconds) — improved in v1.4 |`

(0.001214° ≈ 4.37 arcseconds; "~4 arcseconds" is accurate.)

Self-check: `grep -n "1950\|2050\|0.005695" docs/source/chiron.md` → these two rows no longer match (any remaining hits, if present, are in Task 2's section and the historical context line — handle in Task 2).
  </action>
  <verify>The Implementation Details table valid-date-range row reads 1900-01-01 to 2100-12-31 (2415020.5 to 2488069.5); the accuracy row reads 0.001214°. Both carry an "in v1.4" note.</verify>
  <done>The Implementation Details table shows the v1.4 range (1900–2100) and accuracy (0.001214°).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Date Range and Error Behaviour section (clamping, not ValueError) + fix accuracy number</name>
  <files>docs/source/chiron.md</files>
  <action>
**2a — Rewrite the section header content (lines ~62–73).** Replace:

```
Requesting Chiron outside 1950-2050 raises a `ValueError`:

\`\`\`python
from ketu.calculations import long

# This raises ValueError: JD outside Chiron's supported range
chiron_lon = long(2300000.0, 13)
\`\`\`

For dates inside the range, the positional error is guaranteed to be at most 0.005695° (about 20 arcseconds), well below the 0.01° target set during the Chebyshev spike (Phase 23).
```

with:

```
Chiron's embedded coefficients cover **1900-01-01 to 2100-12-31** (expanded in v1.4). Input outside this range is **silently clamped** to the nearest segment boundary — no `ValueError` is raised:

\`\`\`python
from ketu.calculations import long

# Out-of-range JD: result is clamped to the nearest boundary (no exception)
chiron_lon = long(2300000.0, 13)   # clamped to the 1900/2100 edge
\`\`\`

For dates inside the range, the positional error is guaranteed to be at most 0.001214° (about 4 arcseconds), well below the 0.01° target set during the Chebyshev spike (Phase 23).
```

Self-check: `grep -n "ValueError" docs/source/chiron.md` → 0 hits (the only ValueError mention is removed). `grep -n "1950\|2050\|0.005695" docs/source/chiron.md` → 0 hits across the whole file. `grep -ni "kala" docs/source/chiron.md` → 0 hits.
  </action>
  <verify>`grep -n "ValueError" docs/source/chiron.md` → 0 hits. `grep -n "1950\|2050\|0.005695" docs/source/chiron.md` → 0 hits. `grep -n "clamp" docs/source/chiron.md` → ≥1 hit. `grep -ni "kala" docs/source/chiron.md` → 0 hits.</verify>
  <done>The Date Range and Error Behaviour section documents silent clamping (no ValueError), the 1900–2100 range, and the 0.001214° accuracy; the ValueError code example is rewritten as a clamping example.</done>
</task>

</tasks>

<verification>
- `grep -n "1950\|2050\|0.005695" docs/source/chiron.md` → 0 hits.
- `grep -n "1900\|2100\|0.001214" docs/source/chiron.md` → ≥1 hit each.
- `grep -n "ValueError" docs/source/chiron.md` → 0 hits.
- `grep -n "clamp" docs/source/chiron.md` → ≥1 hit.
- `grep -ni "kala" docs/source/chiron.md` → 0 hits.
</verification>

<success_criteria>
DOC-16 satisfied for chiron.md: range 1900–2100, accuracy 0.001214°, out-of-range = silent clamping (not ValueError), changed values badged (New in v1.4); no stale 1950–2050 / 0.005695° / ValueError remains; no Kala reference.
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-en-fr/31-05-SUMMARY.md` recording each edit (table rows + the rewritten section), explicit confirmation the ValueError example was replaced with a clamping example, and the grep results (ValueError=0, 1950/2050/0.005695=0, kala=0).
</output>
