---
phase: 39-documentation-release-v1-7-0
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/index.md
  - docs/source/concepts.md
  - docs/source/api.md
  - README.md
  - docs/locale/fr/LC_MESSAGES/index.po
  - docs/locale/fr/LC_MESSAGES/index.mo
  - docs/locale/fr/LC_MESSAGES/concepts.po
  - docs/locale/fr/LC_MESSAGES/concepts.mo
  - docs/locale/fr/LC_MESSAGES/api.po
  - docs/locale/fr/LC_MESSAGES/api.mo
autonomous: true
requirements: [ORB-04]
user_setup: []

must_haves:
  truths:
    - "English docs state Rahu/Ketu/Lilith carry a 2 deg longitude orb (was 0)"
    - "English docs explain the Rahu-Ketu Opposition filter and its tautological rationale"
    - "English docs explain MINOR-not-patch (results change so Kala must opt in deliberately)"
    - "French .po carry translated msgstr for every changed msgid (no fallback); .mo recompiled"
  artifacts:
    - path: "docs/source/concepts.md"
      provides: "Fictitious-point orb subsection (2 deg, filter, MINOR rationale)"
      contains: "tautolog"
    - path: "docs/source/index.md"
      provides: "Bodies orb table with 2 for Rahu/Ketu/Lilith"
    - path: "README.md"
      provides: "Bodies orb column matching core.bodies"
    - path: "docs/locale/fr/LC_MESSAGES/concepts.mo"
      provides: "Recompiled French concepts translation"
  key_links:
    - from: "docs/source/index.md orb table"
      to: "ketu/core.py bodies orb id 10/11/12 = 2"
      via: "documented value matches source-of-truth"
      pattern: "2"
---

<objective>
Document the v1.7 fictitious-point orb change in English AND French (ORB-04): Rahu/Ketu/Lilith now carry a 2 deg longitude orb (was 0) so nodes and Lilith enter ecliptic aspect; the targeted Rahu-Ketu Opposition filter and WHY (Ketu = South Node = exact 180 deg opposite of Rahu by construction, so a non-zero orb makes that opposition a permanent tautological detection); MINOR-not-patch reasoning (aspect RESULTS change for every consumer including Kala, so the upgrade is deliberate not a neutral pip install -U). Then refresh the French .po, translate every changed string (no English fallback), recompile the .mo.

Output: updated index.md, concepts.md, api.md, README.md; refreshed/translated French .po; recompiled French .mo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-fictitious-point-orbs-engine/38-01-SUMMARY.md
@.planning/phases/38-fictitious-point-orbs-engine/38-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- English doc prose (index.md, concepts.md, api.md): the 2 deg orb; the Rahu-Ketu Opposition filter + tautological rationale; the MINOR-not-patch / Kala-opt-in note.
- Corrected README.md bodies orb column (Rahu/Ketu/Lilith to 2; id 11 relabelled Ketu/Mean South Node; Chiron id 13 to 4).
- Translated French .po (no fallback) + recompiled .mo for index, concepts, api.
Does NOT include the version bump or CHANGELOG (39-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update English doc source and README</name>
  <files>docs/source/index.md, docs/source/concepts.md, docs/source/api.md, README.md</files>
  <read_first>
    - docs/source/index.md lines 52-68 (bodies table; Rahu/Lilith show 0, Ketu absent)
    - docs/source/concepts.md lines 266-325 (## Orbs + worked examples) and 484-505 (declination orb formula calls them zero-orb bodies)
    - docs/source/api.md lines 1052-1056 (MIN_DECL_ORB note calls them zero-orb bodies)
    - README.md lines 229-244 (id 11 mislabelled True North Node orb 0; Chiron id 13 orb 0)
    - .planning/REQUIREMENTS.md lines 10-17 + line 27 (authoritative rationale)
    - 38-01-SUMMARY.md (facts: orb 0 to 2 on ids 10/11/12; point-point=2; Rahu-Sun=7; helper suppresses ONLY Rahu+Ketu+Opposition)
  </read_first>
  <action>
    index.md table lines 66-67: Rahu orb 0 to 2; Lilith orb 0 to 2; ADD a Ketu (Mean South Node, id 11) row orb 2 speed -0.052954 if absent. Keep Chiron 4 and planet orbs unchanged; reuse the existing degree glyph.

    concepts.md ## Orbs around line 277: list the three points with orb 2 and add a subsection Fictitious points (Rahu, Ketu, Lilith): (a) as of v1.7 they carry a 2 deg longitude orb (previously 0, an Abu Mashar / Al-Biruni choice that left point-point at (0+0)/2*coef = 0, unable to aspect); (b) point-point conjunction orb = 2, point-planet orb = the mean (Rahu-Sun = (2+12)/2 = 7); (c) the engine suppresses ONLY the simultaneous Rahu-Ketu Opposition because Ketu is the South Node, the exact 180 deg opposite of Rahu by construction, so a non-zero orb would otherwise emit a permanent tautological 180 deg detection; every OTHER Rahu/Ketu aspect is detected normally. Use the word tautological. At lines 500-505 fix the stale zero-orb-bodies wording: the LONGITUDE orb is now 2 (v1.7) but the DECLINATION axis still floors via MIN_DECL_ORB (delta formula and 0.5 floor unchanged); the two axes are independent. Add a callout labelled "v1.7 - fictitious-point orbs (MINOR, not patch)": aspect RESULTS change (new node/Lilith aspects appear; Rahu-Ketu Opposition no longer appears) so Kala must treat 1.6.x to 1.7.0 as a deliberate reviewed upgrade, NOT a neutral pip install -U. Use the literal token MINOR.

    api.md lines 1052-1056: update the MIN_DECL_ORB note so it no longer calls Rahu/Lilith flatly zero-orb bodies; note their longitude orb is 2 (v1.7) while the declination floor stays 0.5.

    README.md table lines 229-244: set orb 2 for Rahu (id 10), the id-11 node, Lilith (id 12); relabel id 11 True North Node to Ketu (Mean South Node); correct Chiron (id 13) orb 0 to 4. Do NOT change planet orbs or the aspects table.

    Do NOT touch CHANGELOG.md, UPGRADING.md, version strings, or conf.py (those are 39-02).
  </action>
  <verify>
    <automated>grep -qi tautolog docs/source/concepts.md; grep -q MINOR docs/source/concepts.md; grep -q "Ketu (Mean South Node)" README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/source/index.md lists Rahu orb 2 and Lilith orb 2 (no longer 0); a Ketu row with orb 2 is present.
    - docs/source/concepts.md has a fictitious-points subsection containing tautological and a MINOR callout explaining results change for Kala.
    - docs/source/concepts.md declination section no longer states Rahu/Ketu/Lilith are orb 0 without the v1.7 2-deg-longitude qualification.
    - docs/source/api.md MIN_DECL_ORB note qualifies longitude orb 2 (v1.7) vs the 0.5 declination floor.
    - README.md table: Rahu (id 10) orb 2, id-11 relabelled Ketu (Mean South Node) orb 2, Lilith (id 12) orb 2, Chiron (id 13) orb 4.
    - git diff --name-only shows NO change to CHANGELOG.md, UPGRADING.md, pyproject.toml, ketu/__init__.py, docs/source/conf.py in this task.
  </acceptance_criteria>
  <done>English docs and README accurately describe the 2 deg orb, the Rahu-Ketu Opposition filter with its tautological rationale, and the MINOR-not-patch / Kala-opt-in reasoning; orb tables match core.bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh and translate French .po, recompile .mo</name>
  <files>docs/locale/fr/LC_MESSAGES/index.po, docs/locale/fr/LC_MESSAGES/index.mo, docs/locale/fr/LC_MESSAGES/concepts.po, docs/locale/fr/LC_MESSAGES/concepts.mo, docs/locale/fr/LC_MESSAGES/api.po, docs/locale/fr/LC_MESSAGES/api.mo</files>
  <read_first>
    - docs/Makefile (i18n targets gettext, update-po, build-mo; VENVBIN defaults to ../venv/bin so make runs from docs/ without activating)
    - docs/locale/fr/LC_MESSAGES/index.po (existing FR; lines ~285-346 hold Rahu/Lilith names + the orb cell msgid "0" / msgstr "0" that must become 2)
    - docs/locale/fr/LC_MESSAGES/concepts.po and api.po (existing FR orb/API translations)
    - fr/CHANGELOG.md (FR vocabulary: tautologique, Noeud Sud, orbe, version mineure)
  </read_first>
  <action>
    From docs/ run: make gettext then make update-po. This regenerates POT templates and merges new/changed msgids into the FR .po for index, concepts, api.

    Open the affected .po files and TRANSLATE every new or fuzzy msgstr into French - no English fallback, no empty msgstr, and remove the fuzzy flag once each is translated. The orb-cell msgid "0" in index.po (currently msgstr "0") becomes msgstr "2" (no planet has orb 0, so this cell applied only to the fictitious points; if sphinx-intl produced one entry per cell, set each Rahu/Lilith/Ketu cell to 2). Translate the new fictitious-points subsection, the tautological rationale, and the MINOR / Kala opt-in callout (use: tautologique; Noeud Sud, oppose a Rahu par construction; version mineure et non un correctif; Kala doit traiter la mise a jour comme deliberee).

    Then from docs/ run make build-mo to recompile the .mo binaries from the updated .po. Stage the regenerated .po AND .mo for commit. Confirm 0 untranslated / 0 fuzzy for index/concepts/api via sphinx-intl stat.
  </action>
  <verify>
    <automated>cd docs; ../venv/bin/sphinx-intl stat 2>/dev/null | grep -E "(index|concepts|api).po"</automated>
  </verify>
  <acceptance_criteria>
    - sphinx-intl stat reports 0 fuzzy, 0 untranslated for index.po, concepts.po, api.po.
    - index.po orb cell for the fictitious points reads msgstr "2" (not 0); no empty/fuzzy msgstr remains on changed entries.
    - docs/locale/fr/LC_MESSAGES/index.mo, concepts.mo, api.mo exist and are newer than their .po (recompiled).
    - The translated msgstr include French renderings of tautologique and version mineure (no English fallback).
  </acceptance_criteria>
  <done>French .po fully translated for the changed pages with no English fallback; .mo recompiled and staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build toolchain -> committed artifacts | make gettext/update-po/build-mo regenerate .po/.mo committed into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information disclosure | English vs French doc drift | mitigate | sphinx-intl stat gate forces 0 untranslated; EN is authoritative and FR mirrors it |
| T-39-02 | Tampering | stale .mo shipped (English fallback in FR build) | mitigate | make build-mo recompiles .mo from .po; acceptance asserts .mo newer than .po |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; docs toolchain already pinned in requirements-docs.txt from prior phases |
</threat_model>

<verification>
- docs/source/index.md, concepts.md, api.md, README.md reflect the 2 deg orb, the filter rationale, and the MINOR note.
- sphinx-intl stat shows 0 fuzzy / 0 untranslated for index, concepts, api.
- FR .mo recompiled and staged alongside the .po.
</verification>

<success_criteria>
- English + French docs explain the 2 deg fictitious-point orb, the Rahu-Ketu Opposition filter and its tautological rationale, and the MINOR-not-patch reasoning.
- FR .po translated (no English fallback) and FR .mo recompiled.
- index.md and README orb tables match ketu/core.py (Rahu/Ketu/Lilith = 2, Chiron = 4).
</success_criteria>

<output>
Create .planning/phases/39-documentation-release-v1-7-0/39-01-SUMMARY.md when done.
</output>
