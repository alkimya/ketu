---
phase: 39-documentation-release-v1-7-0
plan: "03"
type: execute
wave: 2
depends_on: ["39-01", "39-02"]
files_modified:
  - dist/
autonomous: false
requirements: [REL-01]
user_setup: []

must_haves:
  truths:
    - "Local pre-flight is green: full pytest (1666+), 100% coverage, mypy --strict ketu/, doctest, doc-gates, python -m build, local-wheel fresh-venv smoke"
    - "A blocking human go/no-go relecture-validation is honoured before any tag/push/publish"
    - "Tag v1.7.0 created; both origin/main and the tag pushed; OIDC publish.yml runs on the tag"
    - "ketu==1.7.0 is live on PyPI; post-publish fresh-venv smoke FROM PyPI passes"
  artifacts:
    - path: "dist/"
      provides: "Built sdist + wheel for 1.7.0 (twine check clean)"
    - path: ".git (tag v1.7.0)"
      provides: "Annotated release tag triggering publish.yml"
  key_links:
    - from: "git tag v1.7.0 push"
      to: ".github/workflows/publish.yml (on push tags v*.*.*)"
      via: "OIDC trusted publishing to PyPI"
      pattern: "v1.7.0"
    - from: "origin/main push"
      to: "Read the Docs build"
      via: "RTD follows main (push main, not just the tag)"
      pattern: "main"
---

<objective>
Ship ketu==1.7.0 to PyPI via OIDC trusted publishing (REL-01) AFTER a blocking human go/no-go relecture-validation. Run the full local pre-flight, pause for the user to review the whole milestone, then tag v1.7.0, push BOTH origin/main and the tag (RTD follows main, PyPI follows the tag), let publish.yml OIDC-publish, create the GitHub release with sdist+wheel, and run a post-publish fresh-venv smoke FROM PyPI.

Output: ketu==1.7.0 live on PyPI; GitHub release v1.7.0; passing post-publish smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.github/workflows/publish.yml
</context>

<artifacts_this_phase_produces>
- Built distributions in dist/ (sdist + wheel) for 1.7.0, twine-checked.
- Annotated git tag v1.7.0 (pushed) and pushed origin/main.
- PyPI release ketu==1.7.0 (OIDC via publish.yml).
- GitHub release v1.7.0 with sdist+wheel attached.
- Passing post-publish fresh-venv smoke proving node/Lilith aspect present, Rahu-Ketu Opposition absent, no swisseph at runtime.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Local pre-flight - gates + build + local-wheel smoke</name>
  <files>dist/</files>
  <read_first>
    - Makefile (recipes: test = pytest with coverage; doctest; doc-gates = interrogate + numpydoc; mypy = mypy --strict ketu/)
    - .github/workflows/publish.yml (build = python -m build --sdist --wheel; twine check dist/*; OIDC publish on tag)
    - 38-VERIFICATION.md (baseline: 1666 passed, 2 skipped, 100% coverage, mypy --strict ketu/ clean)
  </read_first>
  <action>
    From the repo root with venv active run the full gate set and STOP if any fails: make test (full pytest + coverage, expect 1666+ passed / 100%); make mypy (mypy --strict ketu/ clean); make doctest; make doc-gates (interrogate + numpydoc lint). Confirm version is 1.7.0 (python -c "import ketu; print(ketu.__version__)").

    Build the distributions: python -m build --sdist --wheel into dist/, then twine check dist/* (must pass). Then local-wheel fresh-venv smoke: create a throwaway venv, pip install the freshly built wheel from dist/, and in that venv assert ketu.__version__ == 1.7.0, detect at least one Rahu OR Lilith longitude aspect at some date via the public ketu.aspects API, confirm no Rahu-Ketu Opposition is emitted, and confirm importlib.util.find_spec("swisseph") is None. Tear down the throwaway venv. Do NOT tag, push, or publish in this task.
  </action>
  <verify>
    <automated>ls dist/*.whl dist/*.tar.gz</automated>
  </verify>
  <acceptance_criteria>
    - make test passes (1666+ passed, 100% coverage); make mypy clean; make doctest passes; make doc-gates passes.
    - dist/ contains a 1.7.0 wheel and sdist; twine check dist/* reports PASSED.
    - Local-wheel fresh-venv smoke: ketu.__version__ == 1.7.0; >=1 Rahu/Lilith longitude aspect detected; no Rahu-Ketu Opposition; find_spec(swisseph) is None.
    - No git tag, push, or PyPI upload performed in this task.
  </acceptance_criteria>
  <done>All local gates green, distributions built + twine-checked, local-wheel smoke confirms 1.7.0 behaviour; nothing irreversible done yet.</done>
</task>


<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human go/no-go relecture-validation (BLOCKING - before any irreversible action)</name>
  <files>(none - review gate, no files modified)</files>
  <what-built>
    v1.7 docs (en + fr), the [1.7.0] CHANGELOG/UPGRADING entries, the 1.7.0 version bump across the three source-of-truth files, and the locally built + twine-checked distributions in dist/ with a passing local-wheel smoke. Nothing has been tagged, pushed, or published.
  </what-built>
  <action>
    PAUSE and present the milestone for relecture-validation. Do NOT tag, push, build-publish, or create a release until the user replies go. This is the user standing rule: the human relecture-validation gate MUST be passed before any irreversible action. Surface the pre-flight results (pytest/coverage/mypy/doctest/doc-gates, build + twine check, local-wheel smoke) and the doc/changelog/UPGRADING diffs for review, then wait.
  </action>
  <how-to-verify>
    1. Relis tout le milestone v1.7 (Phase 38 moteur + Phase 39 docs/release) - cette relecture-validation est ta regle: aucun tag/push/publish avant ton feu vert.
    2. Verifie le CHANGELOG [1.7.0] (EN racine + copie RTD + FR) et la note UPGRADING v1.6 -> v1.7 + Kala guidance (les resultats daspects changent, mise a jour deliberee).
    3. Verifie les docs: index.md / concepts.md / api.md (orbe 2 deg, filtre Rahu-Ketu Opposition, raison tautologique, MINOR) et la traduction FR (.po/.mo, pas de fallback anglais).
    4. Confirme le pre-flight local: pytest 1666+ vert, couverture 100%, mypy --strict propre, build + twine check OK, smoke local-wheel OK.
    5. Donne ton go/no-go.
  </how-to-verify>
  <verify>
    <human-check>User replies go after relecture-validation of docs, changelog, UPGRADING, and pre-flight results.</human-check>
  </verify>
  <done>User has given an explicit go; no tag/push/publish occurred before this signal.</done>
  <resume-signal>Reponds "go" pour autoriser tag + push + publish PyPI + release GitHub, ou decris les corrections a faire (no-go).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tag, push main + tag, OIDC publish, GitHub release</name>
  <files>dist/</files>
  <read_first>
    - .github/workflows/publish.yml (triggers on push tags v*.*.*; build job runs python -m build + twine check; publish job uses OIDC environment pypi, no long-lived token)
    - CHANGELOG.md [1.7.0] section (source text for the GitHub release notes)
  </read_first>
  <action>
    ONLY after the Task 2 go. Ensure the working tree is committed. Create an annotated tag: git tag -a v1.7.0 -m "Ketu 1.7.0 - fictitious-point orbs (Rahu/Ketu/Lilith 2 deg)". Push BOTH refs: git push origin main AND git push origin v1.7.0 (RTD follows main - pushing only the tag leaves docs stale; PyPI follows the tag). The tag push triggers .github/workflows/publish.yml which builds, twine-checks, and OIDC-publishes to PyPI.

    Monitor the publish run to success (gh run watch / gh run list for publish.yml). Then create the GitHub release: gh release create v1.7.0 with notes drawn from the CHANGELOG [1.7.0] section and attach the dist/ sdist + wheel built in Task 1. If any auth step fails (gh / git push), surface it as an auth gate for the user rather than retrying blindly.
  </action>
  <verify>
    <automated>git tag -l v1.7.0</automated>
  </verify>
  <acceptance_criteria>
    - git tag -l v1.7.0 shows the tag exists (annotated).
    - Both origin/main and refs/tags/v1.7.0 are pushed to origin (git ls-remote origin shows v1.7.0).
    - The publish.yml run for the tag completed successfully (gh run list shows the publish workflow SUCCESS).
    - gh release view v1.7.0 exists with sdist + wheel attached.
  </acceptance_criteria>
  <done>v1.7.0 tagged, main + tag pushed, OIDC publish succeeded, GitHub release created with artifacts.</done>
</task>

<task type="auto">
  <name>Task 4: Post-publish fresh-venv smoke FROM PyPI</name>
  <files>dist/</files>
  <read_first>
    - ketu/aspects/calculator.py (public get_aspect / calculate_aspects API used by the smoke; _is_tautological_node_opposition suppresses Rahu-Ketu Opposition)
    - 38-VERIFICATION.md behavioral spot-checks (Rahu-Sun opposition detected ~2000-01-18; no Rahu-Ketu Opposition emitted)
  </read_first>
  <action>
    After PyPI shows 1.7.0 (allow propagation), create a throwaway fresh venv, pip install ketu==1.7.0 FROM PyPI (no local path, no editable). In that venv assert: ketu.__version__ == "1.7.0"; at least one Rahu OR Lilith longitude aspect is detected at some date via the public ketu.aspects API (the 2 deg orb now produces node/Lilith aspects); the Rahu-Ketu Opposition is ABSENT from results at a date where Rahu and Ketu are ~180 deg apart (always, by construction); and importlib.util.find_spec("swisseph") is None (no swisseph at runtime). Tear down the throwaway venv. If any assertion fails, the release is bad - flag a no-go / yank decision to the user.
  </action>
  <verify>
    <automated>pip index versions ketu 2>/dev/null | grep -q 1.7.0</automated>
  </verify>
  <acceptance_criteria>
    - pip install ketu==1.7.0 from PyPI succeeds in a clean venv.
    - ketu.__version__ == "1.7.0" in that venv.
    - >=1 Rahu or Lilith longitude aspect detected via the public API.
    - The Rahu-Ketu Opposition is absent from emitted results.
    - importlib.util.find_spec("swisseph") is None at runtime.
  </acceptance_criteria>
  <done>ketu==1.7.0 from PyPI installs clean and exhibits the 2 deg node/Lilith aspects, the suppressed Rahu-Ketu Opposition, and no runtime swisseph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> GitHub Actions -> PyPI | a pushed tag triggers an automated build that publishes to PyPI via OIDC |
| human -> irreversible actions | tag/push/publish are irreversible; the go/no-go gate is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | supply-chain / publish integrity | mitigate | OIDC trusted publishing (no long-lived PyPI token); tag-triggered build; twine check in publish.yml |
| T-39-06 | Elevation of privilege | accidental publish of wrong version / pre-flight bypass | mitigate | blocking human go/no-go gate (Task 2) before any tag/push; version asserted 1.7.0 in pre-flight |
| T-39-07 | Information disclosure | stale RTD docs (tag pushed but not main) | mitigate | push BOTH origin/main and the tag; key_link documents RTD-follows-main |
| T-39-08 | Tampering | corrupt/incorrect release artifacts | mitigate | twine check dist/*; post-publish fresh-venv smoke FROM PyPI |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | only build/twine/ketu installed in ephemeral venvs from pinned/official sources; no new deps added |
</threat_model>

<verification>
- Local gates green + dist built + twine check + local-wheel smoke (Task 1).
- Human go/no-go honoured before any irreversible action (Task 2).
- Tag v1.7.0 + main pushed; publish.yml OIDC run SUCCESS; GitHub release created (Task 3).
- Post-publish fresh-venv smoke FROM PyPI passes (Task 4).
</verification>

<success_criteria>
- ketu==1.7.0 live on PyPI via OIDC; both origin/main and v1.7.0 pushed.
- The blocking human relecture-validation gate was passed before tag/push/publish.
- Post-publish smoke FROM PyPI confirms >=1 node/Lilith aspect, Rahu-Ketu Opposition absent, no runtime swisseph.
</success_criteria>

<output>
Create .planning/phases/39-documentation-release-v1-7-0/39-03-SUMMARY.md when done.
</output>
