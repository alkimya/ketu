---
phase: 01-api-surface-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - UPGRADING.md
autonomous: false
must_haves:
  truths:
    - "UPGRADING.md explains every breaking change from 0.4.0 to 1.0"
    - "UPGRADING.md provides before/after code examples for import changes"
    - "UPGRADING.md documents removal of chart, icalendar, and optional dependency extras"
    - "UPGRADING.md has a quick migration checklist"
    - "Fresh wheel install in clean venv pulls only numpy (no matplotlib, icalendar)"
  artifacts:
    - path: "UPGRADING.md"
      provides: "Migration guide from 0.4.0 to 1.0"
      min_lines: 40
      contains: "ketu.cycles import"
  key_links:
    - from: "UPGRADING.md"
      to: "actual ketu API"
      via: "import examples match reality"
      pattern: "from ketu\\."
---

<objective>
Write UPGRADING.md migration guide and verify clean package installation.

Purpose: Users upgrading from 0.4.0 need clear guidance on import changes and removed features. Final verification ensures the package installs cleanly with only numpy as dependency.

Output: UPGRADING.md at project root, verified clean install
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-surface-cleanup/01-CONTEXT.md
@.planning/phases/01-api-surface-cleanup/01-RESEARCH.md
@.planning/phases/01-api-surface-cleanup/01-01-SUMMARY.md
@ketu/__init__.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UPGRADING.md migration guide</name>
  <files>UPGRADING.md</files>
  <action>
    Create `UPGRADING.md` at project root following the pandas 3.0 migration guide structure from research.
    Audience: external developers (public-quality for PyPI release). Tone: concise but professional.
    No Kala-specific references per user decision.

    Structure:
    1. **Header**: "Upgrading from Ketu 0.4.x to 1.0.0"
    2. **Overview**: One paragraph explaining this is a breaking release focused on API cleanup
    3. **Removed Features** section:
       - Chart Visualization (`ketu.export.chart`) — removed, matplotlib dependency gone
       - iCalendar Export (`ketu.export.icalendar`) — removed, icalendar dependency gone
       - Optional dependency extras (`pip install ketu[chart]`, `ketu[icalendar]`, `ketu[all]`) — removed
       - For each: brief explanation, before/after code showing old import vs. "no replacement in ketu"
       - Migration tip: "Copy chart.py from v0.4.0 into your project if needed"
    4. **Import Changes** section:
       - Explain: all functions now via submodule imports
       - Before/after table or code blocks:
         ```python
         # v0.4.x (REMOVED)
         from ketu import utc_to_julian, long, positions
         from ketu import calculate_aspects, find_aspect_timing
         from ketu import generate_cycle_series

         # v1.0.0 (REQUIRED)
         from ketu.calculations import utc_to_julian, long, positions
         from ketu.aspects import calculate_aspects, find_aspect_timing
         from ketu.cycles import generate_cycle_series
         ```
       - Note what still works: `from ketu import bodies, aspects, signs, __version__`
    5. **Submodule Reference** — quick table mapping old top-level imports to new submodule paths:
       | Old import | New import |
       |------------|------------|
       | `from ketu import utc_to_julian` | `from ketu.calculations import utc_to_julian` |
       | `from ketu import calculate_aspects` | `from ketu.aspects import calculate_aspects` |
       | `from ketu import generate_cycle_series` | `from ketu.cycles import generate_cycle_series` |
       | etc. |
    6. **Installation** section:
       ```bash
       pip install ketu==1.0.0  # Only numpy required
       ```
    7. **Quick Migration Checklist** — bullet list with checkboxes:
       - Search codebase for `from ketu import`
       - Update to submodule imports
       - Remove `ketu[chart]` or `ketu[all]` from requirements
       - Test in clean virtual environment

    Keep under 150 lines. No emojis. Professional, factual tone.
  </action>
  <verify>
    Run: `wc -l UPGRADING.md` — between 40 and 150 lines.
    Run: `grep "from ketu.calculations import" UPGRADING.md` — at least one match.
    Run: `grep "from ketu.aspects import" UPGRADING.md` — at least one match.
    Run: `grep "from ketu.cycles import" UPGRADING.md` — at least one match.
    Run: `grep "matplotlib\|icalendar\|chart" UPGRADING.md` — mentioned in removed features.
    Run: `grep -i "kala" UPGRADING.md` — must return nothing (no Kala references per user decision).
  </verify>
  <done>
    UPGRADING.md exists at project root with complete migration guidance: removed features, import changes with before/after examples, submodule reference table, installation instructions, and migration checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 cleanup</name>
  <files>UPGRADING.md, ketu/__init__.py, pyproject.toml</files>
  <action>
    Present the complete Phase 1 results for human verification. All automated work is done in Plan 01 and Task 1 above.
    This checkpoint confirms the full API surface cleanup is correct before proceeding to Phase 2.
  </action>
  <verify>
    Human runs verification steps listed in how-to-verify below.
  </verify>
  <done>Human confirms all Phase 1 success criteria are met.</done>
  <what-built>
    Complete Phase 1 API surface cleanup:
    - Deleted ketu/export/ directory (chart.py, icalendar.py, constants.py)
    - Deleted fr/ directory from git
    - Rewrote ketu/__init__.py to minimal form (only version + core constants)
    - Cleaned pyproject.toml (no optional deps, no export package)
    - Updated all test imports to submodule pattern
    - Fixed ketu/lunar_calendar.py to not depend on export module
    - Created UPGRADING.md migration guide
  </what-built>
  <how-to-verify>
    1. Activate venv: `cd /home/loc/workspace/solaris/ketu && source venv/bin/activate`

    2. Run full test suite:
       `python -m pytest tests/ -v`
       Expected: all 183+ tests pass

    3. Verify clean API:
       `python -c "from ketu import bodies, aspects, signs; print('Top-level constants: OK')"`
       `python -c "from ketu.calculations import utc_to_julian; print('Submodule import: OK')"`
       `python -c "from ketu import utc_to_julian"` — should raise ImportError

    4. Verify export removal:
       `python -c "from ketu.export import chart"` — should raise ImportError
       `ls ketu/export/` — should not exist

    5. Verify clean install:
       `pip install -e . 2>&1 | grep -i "matplotlib\|icalendar"`
       Expected: no output (these deps are not installed)

    6. Review UPGRADING.md: `cat UPGRADING.md` — check it reads well

    7. Verify pyproject.toml has no optional deps:
       `grep "optional" pyproject.toml` — should return nothing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Full test suite passes: `python -m pytest tests/ -v`
2. `from ketu.export import chart` raises ImportError
3. `from ketu import utc_to_julian` raises ImportError
4. `from ketu.cycles import generate_cycle_series` succeeds
5. `from ketu import bodies, aspects, signs` succeeds
6. `pip install -e .` in clean venv installs only numpy
7. UPGRADING.md exists with migration guidance
8. No references to export/chart/ical in ketu/ source (excluding aspects/timelines.py "export methods" comments which refer to data export, not the removed module)
</verification>

<success_criteria>
- UPGRADING.md exists with before/after examples, removed features list, and migration checklist
- Clean wheel builds without matplotlib or icalendar as dependencies
- Human has verified the full cleanup works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-surface-cleanup/01-02-SUMMARY.md`
</output>
