---
phase: 01-api-surface-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/__init__.py
  - ketu/lunar_calendar.py
  - pyproject.toml
  - tests/test_ketu.py
  - tests/test_coverage_improvements.py
  - tests/test_aspects_vectorization.py
autonomous: true
must_haves:
  truths:
    - "Importing ketu.export raises ImportError (module does not exist)"
    - "ketu.__init__.py does NOT re-export submodule functions"
    - "ketu.__all__ contains only metadata (version, author, license) and core constants (bodies, aspects, signs)"
    - "pyproject.toml has no optional-dependencies section and no ketu.export in packages list"
    - "All 183+ existing tests pass with updated imports"
    - "ketu/lunar_calendar.py works without depending on ketu.export"
    - "swisseph is not referenced in pyproject.toml and no swisseph imports exist in source code"
    - "matplotlib, svgwrite, and icalendar are absent from all dependency sections in pyproject.toml"
    - "fr/ directory is removed from git tracking"
  artifacts:
    - path: "ketu/__init__.py"
      provides: "Minimal package root with submodule docstring"
      contains: "__version__"
    - path: "pyproject.toml"
      provides: "Clean dependency specification with only numpy"
      contains: "numpy"
  key_links:
    - from: "ketu/lunar_calendar.py"
      to: "BIG_FIVE constant"
      via: "inline definition or moved constant"
      pattern: "BIG_FIVE"
    - from: "tests/test_ketu.py"
      to: "ketu.calculations"
      via: "submodule import"
      pattern: "from ketu\\.calculations import"
---

<objective>
Remove export modules, rewrite ketu's __init__.py to minimal submodule pattern, clean pyproject.toml, remove fr/ from git, and update all test imports.

Purpose: Establish clean public API boundary. This is the core surgical change that defines ketu 1.0's API surface -- submodule imports only, no flat namespace, no optional dependencies. Also finalizes the dependency cleanup (swisseph, matplotlib, svgwrite, icalendar all confirmed gone).

Output: Working package with all tests passing, no export modules, minimal __init__.py, clean dependencies
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-surface-cleanup/01-CONTEXT.md
@.planning/phases/01-api-surface-cleanup/01-RESEARCH.md
@ketu/__init__.py
@ketu/lunar_calendar.py
@ketu/export/__init__.py
@ketu/export/constants.py
@pyproject.toml
@tests/test_ketu.py
@tests/test_coverage_improvements.py
@tests/test_aspects_vectorization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete export modules, fix internal references, remove fr/ from git</name>
  <files>
    ketu/lunar_calendar.py
  </files>
  <action>
    1. Delete the entire `ketu/export/` directory: `rm -rf ketu/export/`

    2. Fix `ketu/lunar_calendar.py` line 14 which imports `from .export.constants import BIG_FIVE`.
       Replace with an inline constant definition at module level:
       ```python
       # Major aspect angles (conjunction, sextile, square, trine, opposition)
       BIG_FIVE = [0, 60, 90, 120, 180]
       ```
       Remove the `from .export.constants import BIG_FIVE` import line entirely.
       The BIG_FIVE value is `[0, 60, 90, 120, 180]` (verified from export/constants.py).

    3. Stage the fr/ directory deletion with git: `git rm fr/CHANGELOG.md fr/CONTRIBUTING.md fr/README.md`
       (Files are already deleted from disk but still tracked by git -- git rm stages the removal.)

    4. Verify no other files import from ketu.export by running:
       `grep -r "from.*export" ketu/ --include="*.py"` -- should return nothing after deletion.
  </action>
  <verify>
    Run: `python -c "from ketu.lunar_calendar import generate_lunar_calendar; print('OK')"` -- must succeed.
    Run: `python -c "from ketu.export import chart"` -- must raise ImportError.
    Run: `ls ketu/export/ 2>&1` -- must show "No such file or directory".
    Run: `git status fr/` -- must show fr/ files staged for deletion (or already removed from index).
  </verify>
  <done>
    ketu/export/ directory fully deleted. lunar_calendar.py has inline BIG_FIVE constant and works independently.
    fr/ directory removed from git tracking. No remaining imports from ketu.export in the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite __init__.py and clean pyproject.toml</name>
  <files>
    ketu/__init__.py
    pyproject.toml
  </files>
  <action>
    1. **Rewrite `ketu/__init__.py`** to minimal form per user decision (NO re-exports of submodule functions):
       ```python
       """Ketu - Astronomical cycle calculations.

       Submodules:
       - ketu.core: Astronomical constants (bodies, aspects, signs)
       - ketu.calculations: Position and velocity calculations
       - ketu.aspects: Aspect calculations, windows, timelines, transits
       - ketu.cycles: Planetary cycle time series generation
       - ketu.ephemeris: Low-level ephemeris computations
       - ketu.cache: Ephemeris caching for fast lookups
       - ketu.complex: Complex number representations for ML
       - ketu.resonance: Resonance field calculations
       - ketu.lunar_calendar: Lunar calendar generation
       - ketu.display: CLI display functions
       """

       __version__ = "0.4.0"
       __author__ = "Loc Cosnier"
       __license__ = "MIT"

       from ketu.core import bodies, aspects, signs

       __all__ = [
           "__version__",
           "__author__",
           "__license__",
           "bodies",
           "aspects",
           "signs",
       ]
       ```
       Keep version at 0.4.0 (version bump is Phase 7).
       Per user decision: NO re-exports of functions from calculations, aspects, cycles, etc.
       Core constants (bodies, aspects, signs) are OK at top level per research recommendation (zero import cost, used everywhere).

    2. **Clean `pyproject.toml`**:
       - Remove entire `[project.optional-dependencies]` section (lines 46-49: chart, icalendar, all groups)
       - Remove `"ketu.export"` from `[tool.setuptools] packages` list
       - Keep everything else unchanged (version stays 0.4.0, scripts entry for CLI stays)
       - Final packages list: `["ketu", "ketu.ephemeris", "ketu.aspects", "ketu.cycles", "ketu.cache"]`

    3. Verify dependency cleanup:
       - Confirm swisseph removal (per locked decision): `grep -E 'swisseph|pyswisseph' pyproject.toml` -- must return nothing.
       - Confirm all removed deps gone: `grep -E 'matplotlib|svgwrite|icalendar' pyproject.toml` -- must return nothing.
       - Confirm no optional-dependencies section: `grep 'optional-dependencies' pyproject.toml` -- must return nothing.
       - Confirm no export package reference: `grep 'export' pyproject.toml` -- must return nothing.
  </action>
  <verify>
    Run: `python -c "import ketu; print(ketu.__all__)"` -- shows only metadata + core constants.
    Run: `python -c "from ketu import utc_to_julian"` -- must raise ImportError.
    Run: `python -c "from ketu import bodies, aspects, signs; print('OK')"` -- must succeed.
    Run: `grep -c 'optional-dependencies' pyproject.toml` -- must return 0.
    Run: `grep 'export' pyproject.toml` -- must return nothing.
    Run: `grep -E 'swisseph|pyswisseph' pyproject.toml` -- must return nothing.
    Run: `grep -E 'matplotlib|svgwrite|icalendar' pyproject.toml` -- must return nothing.
  </verify>
  <done>
    ketu.__init__.py is minimal (version + core constants only). pyproject.toml has no optional dependencies and no export package reference.
    `from ketu import utc_to_julian` raises ImportError (functions only accessible via submodules).
    swisseph, matplotlib, svgwrite, and icalendar confirmed absent from all dependency sections in pyproject.toml.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test imports to submodule pattern and verify full suite</name>
  <files>
    tests/test_ketu.py
    tests/test_coverage_improvements.py
    tests/test_aspects_vectorization.py
  </files>
  <action>
    1. **Update `tests/test_ketu.py`** imports:
       - Line 8: `import ketu` stays (used for `ketu.__version__` and accessing top-level constants)
       - All `ketu.utc_to_julian()` calls -> `from ketu.calculations import utc_to_julian` then use `utc_to_julian()`
       - All `ketu.long()` calls -> `from ketu.calculations import long` then use `long()`
       - All `ketu.longitude()` calls -> `from ketu.calculations import longitude`
       - All `ketu.vlong()` calls -> `from ketu.calculations import vlong`
       - All `ketu.longitude_velocity()` calls -> `from ketu.calculations import longitude_velocity`
       - All `ketu.body_name()` calls -> `from ketu.calculations import body_name`
       - All `ketu.positions()` calls -> `from ketu.calculations import positions`
       - All `ketu.calculate_aspects()` calls -> `from ketu.aspects import calculate_aspects`
       - Keep `import ketu` only if tests check `ketu.__version__` or `ketu.bodies` (top-level constants)
       - Add specific submodule imports at the top of the file

    2. **Update `tests/test_coverage_improvements.py`** imports:
       - Line 13: `import ketu` -- replace with specific imports
       - `ketu.utc_to_julian` -> `from ketu.calculations import utc_to_julian`
       - `ketu.vdist_au` -> `from ketu.calculations import vdist_au`
       - `ketu.calculate_aspects_vectorized` -> `from ketu.aspects import calculate_aspects_vectorized`
       - `ketu.calculate_aspects_batch` -> `from ketu.aspects import calculate_aspects_batch`
       - `ketu.calculate_aspects` -> `from ketu.aspects import calculate_aspects`

    3. **Update `tests/test_aspects_vectorization.py`** imports:
       - Line 8: `from ketu import aspects, calculations, core` -- this is already submodule-style and WORKS because we still expose `ketu.aspects` etc. as submodules. However, the local `class ketu:` shim on lines 12-17 should be removed.
       - Replace the shim with direct function calls: `aspects.calculate_aspects(TEST_JD)` instead of `ketu.calculate_aspects(TEST_JD)`.
       - Update all references from `ketu.calculate_aspects` to `aspects.calculate_aspects`, `ketu.calculate_aspects_vectorized` to `aspects.calculate_aspects_vectorized`, etc.
       - `ketu.positions` -> `calculations.positions`, `ketu.bodies` -> `core.bodies`

    4. Run full test suite: `source venv/bin/activate && python -m pytest tests/ -v`
       All 183+ tests must pass. Fix any remaining import issues.
  </action>
  <verify>
    Run: `source venv/bin/activate && python -m pytest tests/ -v` -- all tests pass (183+).
    Run: `python -c "from ketu.calculations import utc_to_julian; print('OK')"` -- must succeed.
    Run: `grep -rn "import ketu$" tests/` -- only in files that test `ketu.__version__` or top-level constants.
    Run: `grep -rn "ketu\\.utc_to_julian\|ketu\\.long(\|ketu\\.positions(" tests/` -- must return nothing (all migrated to submodule calls).
    Confirm swisseph not imported in source: `grep -rn 'import swisseph\|import swe\b\|from swisseph\|from swe ' ketu/ --include="*.py"` -- must return nothing (docstring mentions OK, actual imports not).
  </verify>
  <done>
    All test files use submodule imports. Full test suite passes (183+ tests).
    No test file calls functions via `ketu.<function>` -- all use direct submodule imports.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` -- all 183+ tests pass
2. `python -c "import ketu; print(ketu.__all__)"` -- only `['__version__', '__author__', '__license__', 'bodies', 'aspects', 'signs']`
3. `python -c "from ketu.export import chart"` -- raises ImportError
4. `python -c "from ketu import draw_zodiacal_chart"` -- raises ImportError
5. `python -c "from ketu.cycles import generate_cycle_series"` -- succeeds
6. `python -c "from ketu.aspects import calculate_aspects"` -- succeeds
7. `grep -r "from.*export" ketu/ --include="*.py"` -- returns nothing
8. `pip install -e .` in active venv -- installs without matplotlib/icalendar
9. `grep -E 'swisseph|pyswisseph' pyproject.toml` -- returns nothing
10. `grep -E 'matplotlib|svgwrite|icalendar' pyproject.toml` -- returns nothing
</verification>

<success_criteria>
- Zero references to ketu.export in the codebase
- ketu.__init__.py has fewer than 20 lines of actual code
- pyproject.toml has no [project.optional-dependencies] section
- pyproject.toml has no references to swisseph, matplotlib, svgwrite, or icalendar
- All tests pass with submodule imports
- `from ketu import <function>` raises ImportError for any function (only constants and metadata accessible at top level)
- fr/ directory removed from git tracking
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-surface-cleanup/01-01-SUMMARY.md`
</output>
