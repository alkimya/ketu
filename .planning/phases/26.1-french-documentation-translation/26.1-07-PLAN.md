---
phase: 26.1-french-documentation-translation
plan: 07
type: execute
wave: 2
depends_on: [26.1-01]
files_modified:
  - docs/locale/fr/LC_MESSAGES/api.po
autonomous: true

must_haves:
  truths:
    - "api.po reports 0 untranslated via msgfmt --statistics"
    - "Code-only function-signature msgids are copied verbatim as their msgstr"
    - "Parameter descriptions are translated while code spans are preserved verbatim"
    - "No msgstr introduces a #/##/### heading marker absent from its msgid"
  artifacts:
    - path: "docs/locale/fr/LC_MESSAGES/api.po"
      provides: "fully translated API reference (185 entries)"
  key_links:
    - from: "api.po parameter descriptions"
      to: "preserved code spans / signatures"
      via: "verbatim backtick code, translated prose"
      pattern: "`[a-z_]+`"
---

<objective>
Translate the API reference (185 entries) — the single largest file. About 39 entries are
code-only function signatures (identity copy), ~90 are text-with-code (translate prose,
preserve code spans), ~50 are pure text, and ~5 are multiline parameter descriptions.

Purpose: api.po is the largest catalog and the last per-file translation. The final
compile/build/verify GATE is a SEPARATE plan (26.1-08) that runs after this one — this plan
ONLY translates api.po.
Output: api.po at 0 untranslated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-french-documentation-translation/26.1-RESEARCH.md
</context>

<reference_glossary_and_rules>

## EN→FR Glossary (single source of truth — use these exact terms)

### Technical / API
API Reference→Référence API | User Guide→Guide utilisateur | Developer Guide→Guide développeur |
Parameters→Paramètres | Returns→Retourne / Valeur retournée | Raises→Lève (exceptions) |
Examples→Exemples | Notes→Notes | See Also→Voir aussi |
Julian Day→Jour Julien | Ecliptic longitude→Longitude écliptique |
Ecliptic latitude→Latitude écliptique | Distance in AU→Distance en UA |
Coordinate system→Système de coordonnées | Structured arrays→Tableaux structurés |
Batch calculations→Calculs batch | Vectorized→Vectorisé | LRU cache→Cache LRU |
Type hints→Annotations de type | Pure NumPy→Pure NumPy (keep English) |
Array→Tableau | Float→Float (keep) | Integer→Entier | Boolean→Booléen | String→Chaîne |
Returns a structured array→Retourne un tableau structuré

### Core Astrology / Bodies (when referenced)
Aspect→Aspect | Conjunction→Conjonction | Orb→Orbe | House system→Système de maisons |
Natal chart→Thème natal | Synastry→Synastrie | Composite chart→Thème composite |
Solar return→Retour solaire | Lunar return→Retour lunaire | Transit→Transit | Chiron→Chiron |
Sun→Soleil | Moon→Lune | Mercury→Mercure | Venus→Vénus | Celestial bodies→Corps célestes

### Aspect Sets / Presets (keep preset NAMES in English in code spans)
TRADITIONAL (7 half-circle aspects)→TRADITIONAL (7 aspects demi-cercle) |
CLASSICAL (5 aspects)→CLASSICAL (5 aspects) | EXTENDED (all 14 aspects)→EXTENDED (14 aspects)

## Marker-Preservation DO/DON'T (CRITICAL — breaks the build if violated)

DON'T:
- NEVER add `#`, `##`, `###` heading markers to a msgstr when the msgid has none (the
  Phase 25-03 `myst.header` bug).
- NEVER translate `{ref}`...``, `{doc}`...``, or any `{role}`...`` MyST role — copy verbatim.
- NEVER translate a msgid that is ENTIRELY a code span (backtick to backtick) — copy verbatim.
  This includes function signatures like `` `positions(jday, l_bodies=bodies)` `` and class
  names like `` `CHART_DTYPE` ``. ~39 api.po entries are this kind — identity copy.
- NEVER translate code identifiers inside backticks: `` `jday` ``, `` `float` ``, `` `body` ``
  stay verbatim — translate ONLY the surrounding description.
- NEVER drop `` `` `` backticks or `**bold**`, and NEVER alter URLs.

DO:
- Translate prose headings: `msgid "API Reference"` → `msgstr "Référence API"`.
- Translate parameter descriptions while preserving code spans:
  `` `jday` (`float`): Julian Day number `` → `` `jday` (`float`) : Jour Julien ``.
- Keep `**bold**`, translate inside, add FR colon space: `**Parameters:**` → `**Paramètres :**`.
- FR colon spacing: a space before `:` `;` `!` `?`.
- Multiline: single-line msgstr valid for short entries; concatenated quoted strings for long.

Skip `#~`-prefixed obsolete entries. Only fill empty `msgstr ""` entries.
</reference_glossary_and_rules>

<tasks>

<task type="auto">
  <name>Task 1: api.po — code-only signatures + first prose batch (~first half)</name>
  <files>docs/locale/fr/LC_MESSAGES/api.po</files>
  <action>
    Read api.po. First pass: for every msgid that is ENTIRELY a code span (function signature
    or identifier, ~39 entries), copy the msgid verbatim into its empty msgstr (identity).
    Then translate the first batch of mixed/prose entries (work top-to-bottom through roughly
    the first half of the 185 empties): translate parameter descriptions and prose while
    preserving every backtick code span verbatim. Apply FR colon spacing, preserve MyST roles,
    do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/api.po -o /dev/null 2>&1 | tail -1 — untranslated count strictly decreasing from 185 toward ~90.</verify>
  <done>All code-only signatures copied verbatim; roughly half of api.po empties filled.</done>
</task>

<task type="auto">
  <name>Task 2: api.po — remaining prose + parameter descriptions + final check</name>
  <files>docs/locale/fr/LC_MESSAGES/api.po</files>
  <action>
    Translate all REMAINING empty `msgstr ""` entries in api.po using the same glossary and
    rules. Focus on the text-with-code parameter descriptions and the ~5 multiline parameter
    blocks (use concatenated quoted strings matching the msgid wrapping, or a single-line
    msgstr if short). Preserve all code spans, MyST roles, and bold markers verbatim; apply FR
    colon spacing; do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/api.po -o /dev/null 2>&1 | tail -1 — reports "N messages traduits." with NO "messages non traduits".</verify>
  <done>api.po reports 0 untranslated; code spans and signatures preserved verbatim.</done>
</task>

</tasks>

<verification>
- api.po reports 0 untranslated via msgfmt --statistics.
- Code-only function-signature msgids equal their msgstr (identity).
- No msgstr starts with `#`/`##`/`###` unless the msgid does.
</verification>

<success_criteria>
The API reference is 100% translated: signatures identity-copied, descriptions translated,
code spans intact.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-french-documentation-translation/26.1-07-SUMMARY.md`.
Commit (force gpgsign off — repo has commit.gpgsign=true but signing has no TTY in agent context; all recent commits are unsigned, so a bare `git commit` silently fails):
`git -c commit.gpgsign=false commit -m "docs(26.1-07): translate API reference to French"`
</output>
