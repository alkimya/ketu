---
phase: 26.1-french-documentation-translation
plan: 05
type: execute
wave: 2
depends_on: [26.1-01]
files_modified:
  - docs/locale/fr/LC_MESSAGES/migration.po
autonomous: true

must_haves:
  truths:
    - "migration.po reports 0 untranslated via msgfmt --statistics"
    - "Shape notation like (13, 13) and (14, 14) is preserved verbatim in every msgstr"
    - "No msgstr introduces a #/##/### heading marker absent from its msgid"
  artifacts:
    - path: "docs/locale/fr/LC_MESSAGES/migration.po"
      provides: "fully translated migration / upgrade guide"
  key_links:
    - from: "migration.po breaking-change explanations"
      to: "preserved shape/version notation"
      via: "verbatim code spans and shape tuples"
      pattern: "\\(14, 14\\)|body_id"
---

<objective>
Translate the migration guide (54 entries). This page is dense with breaking-change
explanations, shape notation (e.g. `(13, 13)` → `(14, 14)` for the 13→14 body change),
version numbers, and code spans — translate the surrounding explanation in clear technical
French while preserving all notation verbatim.

Purpose: the migration guide is the user-facing upgrade contract for the 1.3.0 release.
Getting shape/version notation wrong would mislead readers. Standalone plan for focus.
Output: migration.po at 0 untranslated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-french-documentation-translation/26.1-RESEARCH.md
</context>

<reference_glossary_and_rules>

## EN→FR Glossary (single source of truth — use these exact terms)

### Core Astrology / Bodies
Aspect→Aspect | Conjunction→Conjonction | Orb→Orbe | House system→Système de maisons |
Natal chart→Thème natal | Synastry→Synastrie | Composite chart→Thème composite |
Solar return→Retour solaire | Lunar return→Retour lunaire | Transit→Transit |
Arabic Parts→Parties arabes | Chiron→Chiron | Centaur body→Corps centaure |
Sun→Soleil | Moon→Lune | Celestial bodies→Corps célestes |
Classical planets→Planètes classiques | Modern planets→Planètes modernes

### Technical / Migration
Breaking change→Changement incompatible | Backward-compatible→Rétrocompatible |
Migration→Migration | Upgrade→Mise à niveau | Deprecated→Déprécié | Removed→Supprimé |
Parameters→Paramètres | Returns→Retourne | Structured arrays→Tableaux structurés |
Vectorized→Vectorisé | Type hints→Annotations de type | Pure NumPy→Pure NumPy (keep English) |
Positional contract→Contrat positionnel | Shape→Forme (of an array) |
Index→Indice (array position) | Consumer→Consommateur

## Marker-Preservation DO/DON'T (CRITICAL — breaks the build if violated)

DON'T:
- NEVER add `#`, `##`, `###` heading markers to a msgstr when the msgid has none (the
  Phase 25-03 `myst.header` bug).
- NEVER translate `{ref}`...``, `{doc}`...``, or any `{role}`...`` MyST role — copy verbatim.
- NEVER translate a msgid that is ENTIRELY a code span (backtick to backtick) — copy verbatim.
- NEVER alter shape tuples like `(13, 13)`, `(14, 14)`, array indices `body_id=13`, version
  numbers `1.2.0`/`1.3.0`, code blocks, or URLs — these MUST be byte-identical.
- NEVER drop `` `` `` backticks or `**bold**` markers.

DO:
- Translate prose headings and breaking-change explanations into clear technical French.
- Keep `**bold**`, translate inside, add FR colon space: `**Parameters:**` → `**Paramètres :**`.
- Keep all code spans, shape tuples, and version numbers verbatim; translate ONLY the prose
  around them.
- FR colon spacing: a space before `:` `;` `!` `?`.
- Multiline: single-line msgstr valid for short entries; concatenated quoted strings for long.

Skip `#~`-prefixed obsolete entries. Only fill empty `msgstr ""` entries.
</reference_glossary_and_rules>

<tasks>

<task type="auto">
  <name>Task 1: Translate migration.po prose entries (~first half)</name>
  <files>docs/locale/fr/LC_MESSAGES/migration.po</files>
  <action>
    Read migration.po. Scan the 35 already-translated entries for the established upgrade-guide
    voice. Translate the first batch of empty `msgstr ""` entries (work top-to-bottom through
    roughly the first half of the 54 empties) into clear technical French using the glossary.
    CRITICAL: preserve every shape tuple `(13, 13)`/`(14, 14)`, version number, code span, and
    array index VERBATIM — translate only the surrounding explanation. Apply FR colon spacing,
    preserve MyST roles, do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/migration.po -o /dev/null 2>&1 | tail -1 — untranslated count strictly decreasing from 54.</verify>
  <done>Roughly half of migration.po empties filled with notation preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Translate remaining migration.po entries + final check</name>
  <files>docs/locale/fr/LC_MESSAGES/migration.po</files>
  <action>
    Translate all REMAINING empty `msgstr ""` entries in migration.po using the same glossary
    and rules. Pay special attention to the 13→14 breaking-change section: the shape change
    `(13, 13)` → `(14, 14)` and the Kala-adapts positional-contract note must be explained
    clearly in French with the notation verbatim. Apply FR colon spacing, preserve all code
    spans and MyST roles, do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/migration.po -o /dev/null 2>&1 | tail -1 — reports "N messages traduits." with NO "messages non traduits". Also: grep -E "\(14, 14\)" returns the tuple intact in at least one msgstr where the msgid had it.</verify>
  <done>migration.po reports 0 untranslated; shape/version notation preserved verbatim.</done>
</task>

</tasks>

<verification>
- migration.po reports 0 untranslated via msgfmt --statistics.
- Shape tuples (13, 13)/(14, 14) and version numbers preserved verbatim where present.
- No msgstr starts with `#`/`##`/`###` unless the msgid does.
</verification>

<success_criteria>
The migration / upgrade guide is 100% translated with all technical notation intact.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-french-documentation-translation/26.1-05-SUMMARY.md`.
Commit (force gpgsign off — repo has commit.gpgsign=true but signing has no TTY in agent context; all recent commits are unsigned, so a bare `git commit` silently fails):
`git -c commit.gpgsign=false commit -m "docs(26.1-05): translate migration guide to French"`
</output>
