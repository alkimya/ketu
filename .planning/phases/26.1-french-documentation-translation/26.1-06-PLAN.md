---
phase: 26.1-french-documentation-translation
plan: 06
type: execute
wave: 2
depends_on: [26.1-01]
files_modified:
  - docs/locale/fr/LC_MESSAGES/architecture.po
autonomous: true

must_haves:
  truths:
    - "architecture.po reports 0 untranslated via msgfmt --statistics"
    - "Module/code names (ketu.core, planets.py, CHART_DTYPE, etc.) are preserved verbatim in every msgstr"
    - "No msgstr introduces a #/##/### heading marker absent from its msgid"
  artifacts:
    - path: "docs/locale/fr/LC_MESSAGES/architecture.po"
      provides: "fully translated internal architecture page"
  key_links:
    - from: "architecture.po prose"
      to: "preserved module/code identifiers"
      via: "verbatim backtick code spans"
      pattern: "ketu\\.|\\.py|DTYPE"
---

<objective>
Translate the architecture page (100 entries) — internal design, the pure-NumPy runtime
contract, the Chebyshev approach for Chiron, and the module dependency graph. Many entries
contain code-module names that must be preserved verbatim. Standalone plan for focus.

Purpose: this is the densest technical-reference page (100 of 119 entries empty). Module
names and the pure-NumPy contract phrasing must be accurate.
Output: architecture.po at 0 untranslated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-french-documentation-translation/26.1-RESEARCH.md
</context>

<reference_glossary_and_rules>

## EN→FR Glossary (single source of truth — use these exact terms)

### Architecture Terms
Entry point→Point d'entrée | Module→Module | Subpackage→Sous-paquet | Registry→Registre |
Strategy→Stratégie | Chebyshev coefficients→Coefficients de Chebyshev | Segment→Segment |
Epoch→Époque | Offline build tool→Outil de build hors-ligne |
Dependency graph→Graphe de dépendances | Runtime→Runtime (keep English) |
Pure NumPy→Pure NumPy (keep English) | Structured arrays→Tableaux structurés |
Vectorized→Vectorisé | Coordinate system→Système de coordonnées |
Orbital mechanics→Mécanique orbitale | Ephemeris→Éphéméride |
Polynomial evaluation→Évaluation polynomiale | Centaur body→Corps centaure |
LRU cache→Cache LRU | Build artifact→Artefact de build

### Core Astrology / Bodies (when referenced)
Aspect→Aspect | Celestial bodies→Corps célestes | Chiron→Chiron |
Julian Day→Jour Julien | Ecliptic longitude→Longitude écliptique |
Classical planets→Planètes classiques | Modern planets→Planètes modernes

### Technical / API
API Reference→Référence API | Parameters→Paramètres | Returns→Retourne |
Type hints→Annotations de type | Breaking change→Changement incompatible |
Backward-compatible→Rétrocompatible | Test suite→Suite de tests | Coverage→Couverture

## Marker-Preservation DO/DON'T (CRITICAL — breaks the build if violated)

DON'T:
- NEVER add `#`, `##`, `###` heading markers to a msgstr when the msgid has none (the
  Phase 25-03 `myst.header` bug).
- NEVER translate `{ref}`...``, `{doc}`...``, or any `{role}`...`` MyST role — copy verbatim.
- NEVER translate a msgid that is ENTIRELY a code span (backtick to backtick), e.g.
  `` `ketu.core` `` or `` `planets.py` `` or `` `CHART_DTYPE` `` — copy verbatim.
- NEVER alter module names, file names (`*.py`), DTYPE names, or code blocks — verbatim.
- NEVER drop `` `` `` backticks or `**bold**`, and NEVER alter URLs.

DO:
- Translate prose headings and architecture explanations into clear technical French.
- Keep `**bold**`, translate inside, add FR colon space: `**Parameters:**` → `**Paramètres :**`.
- Keep ALL code-module names and identifiers verbatim; translate ONLY the prose around them.
- FR colon spacing: a space before `:` `;` `!` `?`.
- Multiline: single-line msgstr valid for short entries; concatenated quoted strings for long.

Skip `#~`-prefixed obsolete entries. Only fill empty `msgstr ""` entries.
</reference_glossary_and_rules>

<tasks>

<task type="auto">
  <name>Task 1: Translate architecture.po (~first half, ~50 entries)</name>
  <files>docs/locale/fr/LC_MESSAGES/architecture.po</files>
  <action>
    Read architecture.po. Scan the 19 already-translated entries for the established voice.
    Translate the first ~50 empty `msgstr ""` entries (top-to-bottom) into clear technical
    French using the glossary. Code-only msgids (e.g. `` `ketu.core` ``, `` `planets.py` ``)
    are identity copies — paste the backtick span verbatim as the msgstr. For mixed entries,
    translate the prose and keep code spans verbatim. Apply FR colon spacing, preserve MyST
    roles, do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/architecture.po -o /dev/null 2>&1 | tail -1 — untranslated count strictly decreasing from 100.</verify>
  <done>Roughly half of architecture.po empties filled with module names preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Translate remaining architecture.po entries + final check</name>
  <files>docs/locale/fr/LC_MESSAGES/architecture.po</files>
  <action>
    Translate all REMAINING empty `msgstr ""` entries in architecture.po using the same
    glossary and rules. Cover the pure-NumPy runtime contract, the Chebyshev/Chiron section
    (Chebyshev coefficients→Coefficients de Chebyshev, Segment→Segment, Epoch→Époque), and
    the module dependency graph. Keep all module/code identifiers verbatim. Apply FR colon
    spacing, preserve MyST roles, do NOT add heading markers.
  </action>
  <verify>msgfmt --statistics /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/architecture.po -o /dev/null 2>&1 | tail -1 — reports "N messages traduits." with NO "messages non traduits".</verify>
  <done>architecture.po reports 0 untranslated; all module/code names preserved verbatim.</done>
</task>

</tasks>

<verification>
- architecture.po reports 0 untranslated via msgfmt --statistics.
- Code-module names (ketu.*, *.py, *_DTYPE) preserved verbatim in msgstr.
- No msgstr starts with `#`/`##`/`###` unless the msgid does.
</verification>

<success_criteria>
The internal architecture page is 100% translated with all module identifiers intact.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-french-documentation-translation/26.1-06-SUMMARY.md`.
Commit (force gpgsign off — repo has commit.gpgsign=true but signing has no TTY in agent context; all recent commits are unsigned, so a bare `git commit` silently fails):
`git -c commit.gpgsign=false commit -m "docs(26.1-06): translate architecture page to French"`
</output>
