---
phase: 26.1-french-documentation-translation
plan: 08
type: execute
wave: 3
depends_on: [26.1-01, 26.1-02, 26.1-03, 26.1-04, 26.1-05, 26.1-06, 26.1-07]
files_modified:
  - docs/locale/fr/LC_MESSAGES/api.mo
  - docs/locale/fr/LC_MESSAGES/arabic_parts.mo
  - docs/locale/fr/LC_MESSAGES/architecture.mo
  - docs/locale/fr/LC_MESSAGES/changelog.mo
  - docs/locale/fr/LC_MESSAGES/chiron.mo
  - docs/locale/fr/LC_MESSAGES/concepts.mo
  - docs/locale/fr/LC_MESSAGES/contributing.mo
  - docs/locale/fr/LC_MESSAGES/examples.mo
  - docs/locale/fr/LC_MESSAGES/houses.mo
  - docs/locale/fr/LC_MESSAGES/index.mo
  - docs/locale/fr/LC_MESSAGES/installation.mo
  - docs/locale/fr/LC_MESSAGES/migration.mo
  - docs/locale/fr/LC_MESSAGES/performance.mo
  - docs/locale/fr/LC_MESSAGES/predictive_charts.mo
  - docs/locale/fr/LC_MESSAGES/quickstart.mo
  - docs/locale/fr/LC_MESSAGES/relational_charts.mo
  - docs/locale/fr/LC_MESSAGES/acknowledgments.mo
autonomous: true

must_haves:
  truths:
    - "All 17 .po catalogs report 0 untranslated (zero 'messages non traduits') via msgfmt --statistics"
    - "No .po has a msgstr with a #/##/### heading marker absent from its msgid (no myst.header regression)"
    - "make html builds with exactly 1 warning (the display_version theme warning)"
    - "make html-fr builds with exactly 1 warning (the display_version theme warning)"
    - "All 17 .mo files are recompiled, non-empty (>200 bytes), and committed"
  artifacts:
    - path: "docs/locale/fr/LC_MESSAGES/api.mo"
      provides: "compiled api catalog (>200 bytes)"
    - path: "docs/locale/fr/LC_MESSAGES/chiron.mo"
      provides: "compiled chiron catalog (was a 395-byte stub; now full)"
  key_links:
    - from: "all 17 translated .po"
      to: "compiled .mo catalogs"
      via: "sphinx-intl build (make build-mo)"
      pattern: "build-mo"
    - from: ".mo catalogs"
      to: "French HTML output"
      via: "make html-fr -D language=fr"
      pattern: "html-fr"
---

<objective>
The FINAL GATE. After all 7 translation plans complete (01-07), this plan: (1) asserts every
one of the 17 .po catalogs has 0 untranslated entries, (2) checks no heading-marker
regression slipped into any msgstr, (3) recompiles ALL 17 .mo via the existing gettext infra,
(4) builds BOTH en and fr Sphinx HTML asserting exactly 1 warning each (the known
display_version theme warning), and (5) commits the recompiled .mo catalogs.

Purpose: this single gate enforces the phase success criteria as command-checkable
assertions, decoupled from any individual translation. If any check fails, the phase is not
done — the failure points at the offending file for a follow-up fix.
Output: 17 recompiled+committed .mo catalogs; clean en+fr build at 1 warning each.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-french-documentation-translation/26.1-RESEARCH.md
@docs/Makefile
</context>

<notes>
- .mo files ARE tracked in git (verified: not gitignored; Phase 26-03 committed them). They
  MUST be recompiled and committed in this plan.
- `cd` does NOT persist between bash calls — every command uses absolute paths OR is a single
  compound `cd /home/loc/workspace/ketu/docs && <cmd>`.
- Do NOT run `make gettext` / `make update-po` — the POTs are current (Phase 26). Re-running
  gettext would re-extract and risk reintroducing empty entries. Only build-mo + html + html-fr.
- The baseline (confirmed 2026-06-01) is exactly 1 WARNING per build:
  "display_version n'est pas supportée pour ce thème". Any OTHER warning (esp. myst.header) is
  a regression and BLOCKS the phase — trace it to the offending .po, fix that msgstr, recompile.
- venv python: /home/loc/workspace/ketu/venv/bin/python. The Makefile uses VENVBIN=../venv/bin.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Assert 0 untranslated across all 17 .po + no marker regressions</name>
  <files>docs/locale/fr/LC_MESSAGES/*.po (read-only assertions)</files>
  <action>
    Run the per-file statistics loop and fail loudly if any file still has untranslated entries:

      for po in /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/*.po; do
        line=$(msgfmt --statistics "$po" -o /dev/null 2>&1 | tail -1)
        echo "$(basename $po): $line"
      done

    Every line MUST read "N messages traduits." with NO "messages non traduits". If any file
    reports untranslated entries, STOP — report which file(s) and how many; that file's
    translation plan must be revisited (do NOT paper over it by stubbing msgstr).

    Then check for heading-marker regressions across all .po: for each catalog, confirm no
    msgstr line begins with `#`, `##`, or `###` unless the paired msgid does. A practical
    scan: grep for msgstr lines starting with a literal `#` and manually confirm the paired
    msgid also starts with `#`. Report any mismatch as a BLOCKER (myst.header bug).
  </action>
  <verify>The statistics loop prints 17 lines, none containing "non traduits". The marker scan finds no msgstr-only `#`/`##`/`###` prefix.</verify>
  <done>All 17 .po are 100% translated with no heading-marker regression.</done>
</task>

<task type="auto">
  <name>Task 2: Recompile all 17 .mo via gettext infra</name>
  <files>docs/locale/fr/LC_MESSAGES/*.mo</files>
  <action>
    Recompile ALL catalogs through the existing infra (NOT manual msgfmt per file, NOT
    duplicated files):

      cd /home/loc/workspace/ketu/docs && make build-mo 2>&1

    (If `make build-mo` misbehaves, the documented fallback is sphinx-intl build:
      /home/loc/workspace/ketu/venv/bin/python -c "from sphinx_intl.commands import main; import sys; sys.argv=['sphinx-intl','build','-d','docs/locale']; main()"
    run from repo root.)

    Then confirm all 17 .mo exist and are non-empty (the 5 previously-stub .mo —
    arabic_parts, chiron, houses, predictive_charts, relational_charts — were ~395 bytes and
    must now be substantially larger):

      ls -la /home/loc/workspace/ketu/docs/locale/fr/LC_MESSAGES/*.mo
  </action>
  <verify>17 .mo files present; every file > 200 bytes; the 5 formerly-stub catalogs are now clearly larger than 395 bytes.</verify>
  <done>All 17 .mo recompiled via the gettext infra; none is an empty stub.</done>
</task>

<task type="auto">
  <name>Task 3: Build en + fr (assert 1 warning each) and commit .mo</name>
  <files>docs/locale/fr/LC_MESSAGES/*.mo</files>
  <action>
    Build both languages from docs/ and capture warnings:

      cd /home/loc/workspace/ketu/docs && make html 2>&1 | tee /tmp/26.1-en.log
      cd /home/loc/workspace/ketu/docs && make html-fr 2>&1 | tee /tmp/26.1-fr.log

    Assert each log has exactly 1 WARNING and it is the display_version theme warning:

      grep -c "WARNING" /tmp/26.1-en.log   # must be 1
      grep -c "WARNING" /tmp/26.1-fr.log   # must be 1
      grep "WARNING" /tmp/26.1-en.log /tmp/26.1-fr.log   # must be "display_version ... pour ce thème"

    If either build shows a NEW warning (myst.header, broken xref, etc.), STOP — the offending
    .po msgstr must be fixed and Task 2 re-run before committing. Do NOT commit with new warnings.

    Once both builds are clean at 1 warning, stage and commit the 17 recompiled .mo catalogs
    (the .mo are git-tracked). Do NOT commit docs/build/ (gitignored). Use this exact command
    (force gpgsign off — repo has commit.gpgsign=true but signing has no TTY in agent context;
    all recent commits are unsigned, so a bare `git commit` silently fails and would leave the
    .mo UNCOMMITTED):

      git -c commit.gpgsign=false commit -m "docs(26.1-08): recompile fr .mo catalogs + verify clean en+fr build (100% translated)"
  </action>
  <verify>grep -c "WARNING" /tmp/26.1-en.log == 1 AND grep -c "WARNING" /tmp/26.1-fr.log == 1, both the display_version warning. `git status` shows the 17 .mo committed (clean working tree for docs/locale/*.mo). `git log -1 --stat` lists the .mo files.</verify>
  <done>Both en and fr build at exactly 1 (known) warning; all 17 .mo recompiled and committed.</done>
</task>

</tasks>

<verification>
- 17-file msgfmt loop: zero "messages non traduits".
- No heading-marker regression in any msgstr.
- make html → exactly 1 warning (display_version); make html-fr → exactly 1 warning (display_version).
- 17 .mo recompiled, all >200 bytes, committed (git log shows them).
</verification>

<success_criteria>
Phase success criteria met as command-checkable facts: 100% translated (0 empty msgstr
across 17 catalogs), no broken markdown markers, clean en+fr build at the 1-warning baseline,
.mo compiled and committed. DOC-13 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-french-documentation-translation/26.1-08-SUMMARY.md`.
Commit (force gpgsign off — repo has commit.gpgsign=true but signing has no TTY in agent context; all recent commits are unsigned, so a bare `git commit` silently fails and would leave the .mo UNCOMMITTED):
`git -c commit.gpgsign=false commit -m "docs(26.1-08): recompile fr .mo catalogs + verify clean en+fr build (100% translated)"`
</output>
