---
phase: 16-synastry
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - ketu/synastry/api.py
  - ketu/synastry/__init__.py
  - tests/synastry/conftest.py
  - tests/synastry/test_calculate_synastry.py
  - tests/synastry/test_applying.py
  - tests/synastry/test_modes_idempotent.py
autonomous: true

must_haves:
  truths:
    - "calculate_synastry(chart_a, chart_b) returns a SYNASTRY_DTYPE structured array (filtered mode default)"
    - "mode='dense' returns shape (225,) with all 15×15 cross-pairs; mode='filtered' returns only aspected rows"
    - "Self-pairs (Sun_A↔Sun_B, Moon_A↔Moon_B) are present in dense output"
    - "applying field is computed from natal speeds: sign(delta) * (speed_a - speed_b) > 0"
    - "Default orbs='synastry' tightens natal orbs by factor 0.5; orbs='classical' uses full natal orbs"
    - "Idempotency: calculate_synastry(a, b, mode='dense')[result['aspect_type'] >= 0] == calculate_synastry(a, b, mode='filtered') modulo row order"
    - "Filtered rows ordered by canonical (body_a, body_b) index ascending — predictable for ML/tests"
    - "ASC and MC participate in synastry as bodies 13 and 14; chart['cusps'] 1-12 are NOT consulted"
    - "UTC-only contract restated LOUDLY in the docstring (Notes section)"
    - "synastry_orb_limit returns f4 bit-exact across all 225 pairs (no silent f8 upcast — Pitfall 6 ratchet)"
    - "Doc gates green: interrogate ≥95%, numpydoc validate clean"
  artifacts:
    - path: "ketu/synastry/api.py"
      provides: "calculate_synastry public function + private _extend_body_data helper"
      contains: "def calculate_synastry"
    - path: "ketu/synastry/__init__.py"
      provides: "Updated public surface — adds calculate_synastry export"
      contains: "calculate_synastry"
    - path: "tests/synastry/conftest.py"
      provides: "Phase-16 chart fixtures (chart_a_paris, chart_b_reykjavik, chart_b_nyc, etc.) built inline via compute_chart"
      contains: "chart_a_paris"
    - path: "tests/synastry/test_calculate_synastry.py"
      provides: "Filter mode + dense mode + self-pairs + cross-product + dtype roundtrip tests"
    - path: "tests/synastry/test_applying.py"
      provides: "Applying/separating sign tests including retrograde + edge cases"
    - path: "tests/synastry/test_modes_idempotent.py"
      provides: "Property test: dense filtered == filtered mode"
  key_links:
    - from: "ketu/synastry/api.py"
      to: "ketu.charts.CHART_DTYPE (chart_a, chart_b inputs)"
      via: "chart['body_lons'], chart['body_speeds'], chart['asc'], chart['mc']"
      pattern: "chart\\[.body_lons.\\]|chart\\[.asc.\\]|chart\\[.mc.\\]"
    - from: "ketu/synastry/api.py"
      to: "ketu.aspects.presets.resolve_aspect_set"
      via: "aspects= parameter pass-through"
      pattern: "resolve_aspect_set"
    - from: "ketu/synastry/api.py"
      to: "ketu.synastry.orbs.resolve_orb_set + _BODY_ORBS_15"
      via: "orbs= parameter resolution + per-pair orb_limit computation"
      pattern: "resolve_orb_set|_BODY_ORBS_15"
    - from: "ketu/synastry/api.py"
      to: "ketu.calculations.distance"
      via: "cross-product angular separation"
      pattern: "from ketu\\.calculations import .*distance|distance\\("
---

<objective>
Implement `calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered")` — the cross-chart aspect compute engine. Consumes two `CHART_DTYPE` scalar records (Phase 14), enumerates the full Cartesian product of 15×15 = 225 body pairs (13 canonical bodies + ASC + MC on each side, self-pairs INCLUDED per locked decision), applies first-aspect-wins matching against the synastry-tightened orb table, computes the velocity-based `applying` field from natal speeds, and returns a `SYNASTRY_DTYPE` structured array in either dense (225 rows with `-1`/`NaN` sentinels) or filtered (only aspected rows) mode.

Purpose: Deliver SYN-01, SYN-02, SYN-03, SYN-04 in a single composition layer that reuses every existing v1.2 primitive (CHART_DTYPE, resolve_aspect_set, distance, _BODY_ORBS_15) and adds zero astronomical state. The filtered-vs-dense duality, the velocity-based `applying` semantics, and the cross-product-not-triu_indices loop are the three novelties this plan ships.

Output: `ketu/synastry/api.py` + updated `__init__.py` + `tests/synastry/conftest.py` (Phase-16 fixtures) + 3 test files (unit + applying + idempotency property). Public surface: `from ketu.synastry import calculate_synastry` resolves and computes correctly on a Phase 14 `CHART_DTYPE` pair.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-synastry/16-CONTEXT.md
@.planning/phases/16-synastry/16-RESEARCH.md
@.planning/phases/16-synastry/16-01-SUMMARY.md

# Codebase patterns to compose
@ketu/synastry/core.py
@ketu/synastry/orbs.py
@ketu/charts/api.py
@ketu/charts/core.py
@ketu/aspects/calculator.py
@ketu/aspects/presets.py
@ketu/calculations.py
@ketu/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement calculate_synastry in ketu/synastry/api.py</name>
  <files>ketu/synastry/api.py,ketu/synastry/__init__.py</files>
  <action>
Create `ketu/synastry/api.py` implementing the public composition function. Follow 16-RESEARCH.md §"Example 3: calculate_synastry — composition core" (lines 532-682) closely; deviations require justification in the SUMMARY.

1. **Module docstring**: Mirror `ketu/charts/api.py:1-29` shape — describe the public surface, the composition-only nature (no new astronomical math), and pin the locked decisions:
   - "Cross-product enumeration over 15×15 = 225 ordered pairs (NOT `triu_indices`); self-pairs INCLUDED per locked decision (Sun_A↔Sun_B, Moon_A↔Moon_B are canonical synastry aspects)."
   - "Default orbs='synastry' tightens natal orbs by factor 0.5 (Astrodienst convention; cited in `ketu.synastry.orbs.SYNASTRY_FACTOR`)."
   - "applying field is velocity-based using NATAL speeds (`CHART_DTYPE.body_speeds`); both partner charts are static, the relative motion is `speed_a - speed_b`."
   - "Filtered-mode rows are canonically ordered by `(body_a, body_b)` index ascending (predictable for ML/tests; callers may sort by `|orb|` post-hoc)."
   - "Dense mode uses `aspect_type=-1` and `orb=NaN` sentinels for non-aspected pairs (mirrors Phase 14 `CHART_DTYPE.aspect_matrix` convention)."
   - **UTC-ONLY CONTRACT — LOUD**: "Both `chart_a` and `chart_b` MUST have been computed with UTC Julian Dates. Time-zone conversion is the caller's responsibility; mixing local-time charts will produce incorrect aspects with no error signal." This restated-loudly clause is REQUIRED by ROADMAP success criterion #5.

2. **Private helper** `_extend_body_data(chart: np.ndarray) -> tuple[np.ndarray, np.ndarray]`:
   - Body of helper EXACTLY as 16-RESEARCH.md §Example 3 lines 550-571.
   - Returns `(lons, speeds)` each shape `(15,)` — concatenates 13-canonical body data with `[asc, mc]` longitudes and `[0.0, 0.0]` speeds for ASC/MC.
   - Docstring documents the ASC/MC speed=0 implication: "ASC and MC have no per-day speed in the static natal sense; assigning 0.0 means contacts involving ASC/MC are always classified as non-applying — consistent with the static-chart convention."
   - Pure-NumPy. No swisseph runtime import.

3. **Public function** `calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered") -> np.ndarray`:
   - Type hints exactly as 16-RESEARCH.md §Example 3 line 574-580.
   - Algorithm: follow 16-RESEARCH.md §Example 3 lines 616-681 verbatim, with these mandatory invariants:
     - Resolve aspect mask via `resolve_aspect_set(aspects)` (length-14 bool mask).
     - Resolve orb factor via `resolve_orb_set(orbs)` (float scalar).
     - Extend both charts via `_extend_body_data` → 15-body lons + speeds.
     - Cross-product enumeration via `np.indices((15, 15))` then `.ravel()` → flat shape `(225,)` for `i_flat`, `j_flat`. **CRITICAL**: do NOT use `triu_indices` — this is the locked Pitfall 1 from 16-RESEARCH.md (self-pairs and ordering both matter).
     - Initialize `out = np.empty(225, dtype=SYNASTRY_DTYPE)` with sentinel defaults (`aspect_type=-1`, `orb=np.nan`, `applying=False`, `orb_limit=np.nan`). `body_a/body_b/lon_a/lon_b` always populated (even for non-aspected pairs in dense mode).
     - Loop over `selected_indices = np.where(mask)[0]` (canonical aspect indices). For each aspect:
       - Compute per-pair `orbs_pair = (_BODY_ORBS_15[i_flat] + _BODY_ORBS_15[j_flat]) / 2.0 * coef * factor` (shape (225,)).
       - Conjunction (i_asp == 0): `in_orb = (dist <= orbs_pair) & (~matched)`; `delta = -dist`.
       - Other aspects: `in_orb = (np.abs(dist - ang) <= orbs_pair) & (~matched)`; `delta = ang - dist` (signed orb, Phase 14 D-05 convention; positive when distance < aspect_angle).
       - Compute `applying = (np.sign(delta) * (speed_a - speed_b)) > 0` (Pitfall 4: signed convention, do NOT call `np.abs` on speeds).
       - Write to `out[in_orb]` for the four mutable fields and update `matched |= in_orb` (first-aspect-wins, mirrors `ketu/aspects/calculator.py:204` convention).
     - Mode dispatch:
       - `mode == "dense"` → return `out` (shape (225,), full table).
       - `mode == "filtered"` → return `out[out["aspect_type"] >= 0]` (canonical body-pair order preserved; Pattern 5 from 16-RESEARCH.md).
       - Other → `ValueError(f"unknown mode {mode!r}; expected 'dense' or 'filtered'")`.

4. **Numpydoc docstring** for `calculate_synastry`:
   - Parameters: `chart_a, chart_b` (both `np.ndarray`, scalar `CHART_DTYPE`), `aspects` (default `"classical"`), `orbs` (default `"synastry"`, cite the 0.5 factor + astro.com), `mode` (default `"filtered"`).
   - Returns: structured array of `SYNASTRY_DTYPE` rows.
   - Raises: `ValueError` for unknown mode / unknown orbs preset / unknown aspects spec.
   - Notes:
     - **UTC-ONLY** restated.
     - "Self-pairs INCLUDED" restated.
     - "Filtered rows are ordered by `(body_a, body_b)` index ascending; sort by `|orb|` with `result[np.argsort(np.abs(result['orb']))]` if needed."
     - "Dense mode shape is always `(225,)`; non-aspected pairs have `aspect_type=-1` and `orb=NaN`."
     - "applying convention: velocity-based using `CHART_DTYPE.body_speeds`; ASC/MC contacts are always classified as `applying=False` (speed=0 by convention)."
     - "Body axis: 0..12 from `ketu.core.bodies` + 13=ASC + 14=MC. `chart['cusps']` 1-12 are NOT consulted (deferred to v1.3 if Phase 17/18 demand it)."
   - Examples: at least one usable doctest skipped if it would touch ephemeris (mark `# doctest: +SKIP`); show the import + a one-line call.
   - See Also: `ketu.charts.compute_chart`, `ketu.synastry.orbs.resolve_orb_set`, `ketu.aspects.presets.resolve_aspect_set`.

5. **Update `ketu/synastry/__init__.py`**:
   - Add `from .api import calculate_synastry` after the existing imports.
   - Add `"calculate_synastry"` to `__all__` (insert in alphabetical position).
   - Update the module docstring's "Public API surface" enumeration to include `calculate_synastry`.

Constraints:
- Pure-NumPy. Re-use `ketu.calculations.distance` (vectorized), `ketu.aspects.presets.resolve_aspect_set`, `ketu.synastry.orbs.{_BODY_ORBS_15, resolve_orb_set, OrbSetSpec}`, and `ketu.core.aspects` (private alias `_ASPECTS`).
- Single-pair MVP: signature accepts ONE `chart_a` + ONE `chart_b` (scalar CHART_DTYPE records). Vectorization over chart-pair arrays is OUT OF SCOPE per CONTEXT.md locked decision.
- Pitfall 6 (dtype precision): explicitly cast `delta[in_orb]` and `orbs_pair[in_orb]` to `np.float32` before assigning to `out["orb"]` and `out["orb_limit"]` (both are `f4` per SYNASTRY_DTYPE).
- Pitfall 8 (--list-* collision) is a Plan 04 concern, not this plan.
- Period-terminated summaries (Phase 13 numpydoc convention).
- All public/private functions documented (interrogate ≥95% gate).
- Keep `_extend_body_data` private (underscore-prefixed); it's an implementation detail.
  </action>
  <verify>
- `python -c "from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE; print('OK')"` prints `OK`.
- `python -c "
import numpy as np
from ketu.charts import compute_chart
from ketu.synastry import calculate_synastry
ca = compute_chart(2451545.0, 48.86, 2.35)
cb = compute_chart(2470204.0, 64.15, -21.94, polar_fallback='porphyry')
result = calculate_synastry(ca, cb)
assert result.dtype.names == ('body_a','body_b','lon_a','lon_b','aspect_type','orb','applying','orb_limit')
assert result.ndim == 1
assert (result['aspect_type'] >= 0).all()
print('filtered:', len(result), 'aspects')
dense = calculate_synastry(ca, cb, mode='dense')
assert dense.shape == (225,), f'dense shape {dense.shape}'
print('dense:', len(dense), 'rows')
"` runs without assertion failure and prints two reasonable numbers (filtered <= 225, dense == 225).
- `python -m interrogate ketu/synastry/api.py -f 95` passes.
- `python -m numpydoc lint ketu/synastry/api.py ketu/synastry/__init__.py` returns zero issues.
- `mypy --strict ketu/synastry/api.py` clean.
  </verify>
  <done>
`calculate_synastry` is callable from a Phase 14 `CHART_DTYPE` pair, returns a structured `SYNASTRY_DTYPE` array, and supports both `mode="filtered"` (default) and `mode="dense"`. Public surface `from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE, ...` resolves cleanly. Cross-product enumeration uses `np.indices`, NOT `triu_indices`. UTC-only contract loud in docstring. Doc gates green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build tests/synastry/conftest.py + unit tests in test_calculate_synastry.py</name>
  <files>tests/synastry/conftest.py,tests/synastry/test_calculate_synastry.py</files>
  <action>
**STEP A — Build `tests/synastry/conftest.py`** (Phase-16 dedicated fixtures; do NOT re-import from `tests/charts/conftest.py` because that conftest exposes `reference_charts`, `loaded_reference_snapshot`, `swe_oracle`, `SYSTEM_BYTES` — there is NO `sample_chart` or `polar_chart` fixture in that file).

Create the conftest with explicit pytest fixtures built INLINE via `compute_chart()` calls. Required fixtures:

```python
"""Phase-16 synastry test fixtures.

Builds chart pairs inline via :func:`ketu.charts.compute_chart` so tests
can exercise :func:`ketu.synastry.calculate_synastry` against deterministic,
hand-picked chart inputs. Phase-14's ``tests/charts/conftest.py`` exposes
oracle-validation fixtures (``reference_charts``, ``swe_oracle``); those are
not what synastry needs, so we build our own here.
"""
from __future__ import annotations

import numpy as np
import pytest

from ketu.charts import compute_chart


@pytest.fixture(scope="session")
def chart_a_paris() -> np.ndarray:
    """J2000 noon UTC chart for Paris (lat 48.86, lon 2.35) — chart A baseline."""
    return compute_chart(2451545.0, 48.86, 2.35)


@pytest.fixture(scope="session")
def chart_b_reykjavik() -> np.ndarray:
    """JD 2470204 noon UTC chart for Reykjavik (lat 64.15, lon -21.94).

    MUST pass ``polar_fallback='porphyry'`` — at lat 64.15 placidus may
    raise :class:`HighLatitudeError` and the test would fail because
    ``compute_chart`` itself raises, not because synastry is wrong.
    """
    return compute_chart(2470204.0, 64.15, -21.94, polar_fallback="porphyry")


@pytest.fixture(scope="session")
def chart_b_nyc() -> np.ndarray:
    """JD 2451900 noon UTC chart for New York (lat 40.71, lon -74.01)."""
    return compute_chart(2451900.0, 40.71, -74.01)


@pytest.fixture(scope="session")
def chart_b_tokyo() -> np.ndarray:
    """JD 2451545 noon UTC chart for Tokyo (lat 35.69, lon 139.69)."""
    return compute_chart(2451545.0, 35.69, 139.69)


@pytest.fixture(scope="session")
def chart_b_sydney() -> np.ndarray:
    """JD 2451545 noon UTC chart for Sydney (lat -33.87, lon 151.21)."""
    return compute_chart(2451545.0, -33.87, 151.21)
```

Constraints on the conftest:
- All fixtures are `scope="session"` — chart computation is non-trivial; reuse across tests.
- No re-export from `tests/charts/conftest.py` (those fixtures don't exist with the names previous drafts assumed). If a test genuinely needs `reference_charts`, import via `from tests.charts.conftest import reference_charts` explicitly with `noqa: F401`.
- Each fixture has a one-line docstring documenting JD + city + lat/lon.
- The polar fixture (`chart_b_reykjavik`) MUST pass `polar_fallback='porphyry'` — this is a Pitfall 3 ratchet from 16-RESEARCH.md.
- Add additional fixtures if Tasks 2/3 below need more variety, but keep total ≤ 6 to limit fixture-startup cost.

**STEP B — Build `tests/synastry/test_calculate_synastry.py`** (~20-25 tests).

Use `from tests.synastry.conftest import ...` is NOT needed — pytest auto-discovers conftest fixtures in the same directory. Just declare the fixture names as test parameters.

Required test groups:

**A. Public API surface (~3 tests)**
- `test_calculate_synastry_signature_accepts_default_args(chart_a_paris, chart_b_nyc)` — calling with only `(chart_a, chart_b)` works (defaults: classical, synastry, filtered).
- `test_calculate_synastry_returns_synastry_dtype(chart_a_paris, chart_b_nyc)` — `result.dtype == SYNASTRY_DTYPE`.
- `test_calculate_synastry_filtered_default_mode(chart_a_paris, chart_b_nyc)` — calling without `mode=` keyword returns same length as explicit `mode="filtered"`.

**B. Mode dispatch (~4 tests)**
- `test_dense_mode_shape_is_225(chart_a_paris, chart_b_nyc)` — `mode="dense"` returns shape `(225,)` for any chart pair (15 × 15 cross-product).
- `test_filtered_mode_returns_only_aspected_rows(chart_a_paris, chart_b_nyc)` — `(result["aspect_type"] >= 0).all()` for `mode="filtered"`.
- `test_filtered_count_le_dense_count(chart_a_paris, chart_b_nyc)` — `len(filtered) <= len(dense)`.
- `test_invalid_mode_raises_value_error(chart_a_paris, chart_b_nyc)` — `mode="matrix"` raises `ValueError` with message naming `'dense'` and `'filtered'`.

**C. Cross-product enumeration / self-pairs (~4 tests)**
- `test_dense_includes_self_pairs_sun_sun(chart_a_paris, chart_b_nyc)` — dense output contains a row where `body_a == 0 and body_b == 0` (Sun_A↔Sun_B). This is the locked CONTEXT.md decision; if this test fails, the implementation reverted to `triu_indices`.
- `test_dense_includes_self_pairs_moon_moon(chart_a_paris, chart_b_nyc)` — similar for `(1, 1)` (Moon_A↔Moon_B).
- `test_dense_distinguishes_ordered_pairs(chart_a_paris, chart_b_nyc)` — for any non-identity chart pair, the row `(Sun_A, Mars_B)` exists distinct from `(Mars_A, Sun_B)` (different `lon_a`, `lon_b`); both are in the dense output.
- `test_dense_includes_asc_mc_contacts(chart_a_paris, chart_b_nyc)` — dense rows where `body_a == 13` (ASC_A) and `body_a == 14` (MC_A) both exist; total dense rows = 225 = 15².

**D. Orb tightening (~4 tests)**
- `test_default_orbs_synastry_factor_05(chart_a_paris, chart_b_nyc)` — for the chart pair, the largest `orb_limit` in the result is half of the natal-orb equivalent (compute natal via `synastry_orb_limit(b1, b2, asp, factor=1.0)` and compare).
- `test_orbs_classical_uses_full_natal(chart_a_paris, chart_b_nyc)` — `calculate_synastry(a, b, orbs="classical")` returns more aspects than default (factor 1.0 vs 0.5 → wider orbs → more matches).
- `test_orbs_unknown_string_raises(chart_a_paris, chart_b_nyc)` — `orbs="random"` raises `ValueError`.
- `test_aspects_classical_5_aspect_types_only(chart_a_paris, chart_b_nyc)` — for `aspects="classical"` (default), `set(result["aspect_type"])` is a subset of `{0, 4, 7, 9, 13}` (Conjunction, Sextile, Square, Trine, Opposition).

**E. CHART_DTYPE consumption (~3 tests)**
- `test_consumes_chart_dtype_scalar(chart_a_paris, chart_b_nyc)` — both inputs are scalar (0-d) `CHART_DTYPE` from `compute_chart(scalar_jd, lat, lon)`; result is a 1-D structured array.
- `test_chart_lons_propagate_correctly(chart_a_paris, chart_b_nyc)` — for the two charts, the `lon_a` field of dense row `(0, k)` for any `k` equals `chart_a["body_lons"][0]` (Sun_A's longitude). Sanity check on `_extend_body_data`.
- `test_chart_asc_propagates_to_body_13(chart_a_paris, chart_b_nyc)` — `lon_a` for any dense row where `body_a == 13` equals `chart_a["asc"]`.

**F. Sentinel convention (~3 tests)**
- `test_dense_non_aspected_rows_have_aspect_type_minus_one(chart_a_paris, chart_b_nyc)` — dense rows where the orb test fails have `aspect_type == -1`.
- `test_dense_non_aspected_rows_have_orb_nan(chart_a_paris, chart_b_nyc)` — same rows have `np.isnan(row["orb"])` True.
- `test_dense_non_aspected_rows_have_orb_limit_nan(chart_a_paris, chart_b_nyc)` — same rows have `np.isnan(row["orb_limit"])` True.
- `test_filtered_rows_never_have_negative_aspect_type(chart_a_paris, chart_b_nyc)` — `(filtered["aspect_type"] >= 0).all()`.

**G. Filtered row order (~2 tests)**
- `test_filtered_canonical_body_pair_order(chart_a_paris, chart_b_nyc)` — pairs are sorted by `(body_a * 15 + body_b)` ascending. Construct linearized indices and assert monotonic.
- `test_filtered_NOT_orb_ascending(chart_a_paris, chart_b_nyc)` — explicitly verify the result is NOT pre-sorted by `|orb|` (regression guard against future "helpful" reorderings; document the rationale in test docstring).

**H. Dtype precision (Pitfall 6 — STRENGTHENED ratchet, ~2 tests)**
- `test_orb_field_is_float32(chart_a_paris, chart_b_nyc)` — `result.dtype["orb"] == np.float32`.
- `test_synastry_orb_limit_f4_bit_exact_all_225_pairs` — for ALL 225 cross-pairs and a fixed conjunction (asp=0), assert `synastry_orb_limit(b1, b2, asp)` returns a value bit-exact equal (`==`, NOT `np.isclose`) to the manually-computed f4 expression `np.float32((_BODY_ORBS_15[b1] + _BODY_ORBS_15[b2]) / 2.0 * np.float32(_ASPECTS["coef"][0]) * np.float32(SYNASTRY_FACTOR))`. This proves NO silent f8 upcast happens in the intermediate computation. Loop over all 225 pairs (`for b1 in range(15): for b2 in range(15): assert ...`).

**I. Polar input ratchet (~1 test)**
- `test_calculate_synastry_with_polar_chart(chart_a_paris, chart_b_reykjavik)` — calling `calculate_synastry(chart_a_paris, chart_b_reykjavik)` with the Reykjavik fixture (lat 64.15, built with `polar_fallback='porphyry'`) returns a non-empty SYNASTRY_DTYPE array; ASC and MC longitudes propagate as finite (not NaN) values into `lon_a`/`lon_b`. **The assertion is on the SYNASTRY result, not on chart computation** — the polar_fallback responsibility lives in the fixture, not in calculate_synastry.

Constraints:
- Use the dedicated `tests/synastry/conftest.py` fixtures defined in STEP A above. Do NOT import nonexistent `sample_chart` / `polar_chart` from `tests/charts/conftest.py`.
- Each test has a one-line docstring.
- No swisseph runtime imports in production paths — fixture chart computation goes through `ketu.charts.compute_chart` which already wraps the swisseph dependency.
- Polar test (`test_calculate_synastry_with_polar_chart`): the assertion is on synastry output, NOT on chart computation. The Reykjavik fixture handles the polar_fallback contract upstream.
  </action>
  <verify>
- `pytest tests/synastry/test_calculate_synastry.py -v` — all tests PASS.
- `pytest tests/synastry/test_calculate_synastry.py --co -q | wc -l` — at least 22 collected tests (including new f4 bit-exact + polar tests).
- `coverage run --source=ketu/synastry -m pytest tests/synastry/test_calculate_synastry.py && coverage report --include="ketu/synastry/api.py"` — coverage ≥90% on api.py (the remaining 10% will be covered by tests in Tasks 3 + Plan 03 oracles).
- `python -m interrogate ketu/synastry/api.py -f 95` passes.
- `python -c "from tests.synastry.conftest import chart_a_paris, chart_b_reykjavik" 2>&1 | head` — names resolvable (sanity check).
  </verify>
  <done>
`tests/synastry/conftest.py` exposes 5 chart fixtures built inline via `compute_chart` (Paris, Reykjavik with polar_fallback, NYC, Tokyo, Sydney). `tests/synastry/test_calculate_synastry.py` exists with ~22-25 passing tests covering: API surface, mode dispatch, cross-product/self-pairs, orb tightening, CHART_DTYPE consumption, sentinel convention, filtered row order, **f4 bit-exact precision across all 225 pairs (Pitfall 6 ratchet)**, polar-chart input. Coverage on `ketu/synastry/api.py` is ≥90%.
  </done>
</task>

<task type="auto">
  <name>Task 3: Applying field tests + dense/filtered idempotency property</name>
  <files>tests/synastry/test_applying.py,tests/synastry/test_modes_idempotent.py</files>
  <action>
Create two focused test files documenting the applying-field semantics and the dense-vs-filtered idempotency invariant.

**1. `tests/synastry/test_applying.py`** (~8-10 tests):

Test the velocity-based `applying` field convention. Each test consumes fixtures from `tests/synastry/conftest.py` (built in Plan 02 Task 2) — declare them as parameters; pytest auto-discovers.

- `test_applying_field_is_bool_dtype(chart_a_paris, chart_b_nyc)` — `result.dtype["applying"] == np.bool_`.
- `test_applying_for_perfect_aspect_is_false(chart_a_paris)` — when `delta == 0` (exact aspect), `applying` should be False (neither applying nor separating; documented edge case). Use `chart_a == chart_a` self-synastry — Sun_A↔Sun_A is conjunction at delta=0.
- `test_applying_self_synastry_diagonal_all_false(chart_a_paris)` — for `chart_a == chart_a`, all self-pair conjunctions (rows where `body_a == body_b`) have `applying == False` (delta=0 edge case applied to all 15 self-pairs).
- `test_applying_sign_convention_signed_delta_times_relative_speed(chart_a_paris, chart_b_nyc)` — verify the implementation matches `(np.sign(delta) * (speed_a - speed_b)) > 0` for at least 3 hand-derived rows. Use the conftest chart fixtures.
- `test_applying_with_retrograde_body(chart_a_paris, chart_b_tokyo)` — pick a fixture date where Mercury is retrograde (negative speed) in chart_a; verify the applying flag for at least one Mercury_A involvement is consistent with the signed convention. Document the hand-derived expected result in the test docstring (Pitfall 4 ratchet). If the J2000 fixtures don't have a retrograde Mercury, add a dedicated `chart_a_retrograde_mercury` fixture in `tests/synastry/conftest.py`.
- `test_applying_asc_contact_is_false(chart_a_paris, chart_b_nyc)` — any row where `body_a == 13` (ASC_A) or `body_b == 13` (ASC_B) has `applying == False` (ASC speed is 0 by `_extend_body_data` convention).
- `test_applying_mc_contact_is_false(chart_a_paris, chart_b_nyc)` — same for `body_a == 14` or `body_b == 14`.
- `test_applying_non_aspected_dense_rows_are_false(chart_a_paris, chart_b_nyc)` — dense rows with `aspect_type == -1` have `applying == False` (initialized sentinel).
- `test_applying_consistent_dense_filtered(chart_a_paris, chart_b_nyc)` — for the chart pair, `applying` field for an aspected pair is identical between dense (post-filter) and filtered modes (idempotency on `applying` specifically).

**2. `tests/synastry/test_modes_idempotent.py`** (~5-8 tests):

Property tests on the dense-vs-filtered identity. Use `pytest.mark.parametrize` with at least 4-5 chart-pair combinations drawn from the conftest fixtures (Paris × NYC, Paris × Tokyo, Paris × Sydney, Paris × Reykjavik, NYC × Tokyo).

- `test_dense_mask_filtered_equals_filtered_mode(chart_a, chart_b)` — for each chart pair: `dense = calculate_synastry(a, b, mode='dense')`; `filtered = calculate_synastry(a, b, mode='filtered')`; assert `len(dense[dense['aspect_type'] >= 0]) == len(filtered)`.
- `test_dense_filtered_same_aspect_type_set(chart_a, chart_b)` — sorted set of aspect types in `dense[mask]` equals sorted set in `filtered`.
- `test_dense_filtered_same_orb_values(chart_a, chart_b)` — for each (body_a, body_b, aspect_type) triple in filtered, the corresponding orb in dense equals the orb in filtered (within `np.float32` precision).
- `test_dense_filtered_no_hidden_state(chart_a_paris, chart_b_nyc)` — calling `calculate_synastry(a, b)` twice returns equal arrays (`np.array_equal` on every field). Idempotency / no caching state.
- `test_self_synastry_dense_diagonal_is_conjunction(chart_a_paris)` — `calculate_synastry(a, a, mode='dense')` produces a conjunction (aspect_type=0, orb≈0) on every self-pair (body_a == body_b) for the 13 canonical bodies AND for ASC, MC. Sanity: a chart synastry'd with itself shows all 15 self-pair conjunctions at exact orb. (For Rahu/Ketu/Lilith with zero-orb, the conjunction will only register if `dist <= 0` strictly — verify this edge case explicitly: at delta=0 exactly, `dist == 0 <= 0` is true, so the aspect IS detected. Document.)
- `test_dense_count_always_225(chart_a, chart_b)` — `len(calculate_synastry(a, b, mode='dense')) == 225` for any chart pair (parametrized over 4-5 pairs).
- `test_filtered_count_le_225(chart_a, chart_b)` — `len(filtered) <= 225` invariant.

For the parametrized `chart_a, chart_b` tests, build a parameter list at module level:
```python
CHART_PAIRS = [
    ("paris_nyc", "chart_a_paris", "chart_b_nyc"),
    ("paris_tokyo", "chart_a_paris", "chart_b_tokyo"),
    ("paris_sydney", "chart_a_paris", "chart_b_sydney"),
    ("paris_reykjavik", "chart_a_paris", "chart_b_reykjavik"),
]
```
Then use `pytest.mark.parametrize` with `indirect=True` or use `request.getfixturevalue()` inside the test body to resolve fixture names dynamically.

Constraints:
- No `hypothesis` dependency — use `pytest.mark.parametrize` over known fixtures.
- Each test has a one-line docstring.
- Reference fixtures come from `tests/synastry/conftest.py` (built in Task 2; do NOT re-import from `tests/charts/conftest.py`).
- Document the Rahu-Rahu zero-orb-conjunction edge case in `test_self_synastry_dense_diagonal_is_conjunction` — this pre-empts the surprise documented in 16-RESEARCH.md Pitfall 2.
  </action>
  <verify>
- `pytest tests/synastry/test_applying.py tests/synastry/test_modes_idempotent.py -v` — all tests PASS.
- `pytest tests/synastry/ -v` — full synastry test suite (Plans 01 + 02) all PASS.
- `coverage run --source=ketu/synastry -m pytest tests/synastry/ && coverage report --include="ketu/synastry/*" --fail-under=95` — ≥95% on `ketu/synastry/`.
- `python -m interrogate ketu/synastry/ -f 95 -v` passes.
- `python -m numpydoc lint ketu/synastry/*.py` zero issues.
- `mypy --strict ketu/synastry/` clean.
- `pytest tests/ -v` — full project suite still green (no regressions; expected new total ~960 tests).
  </verify>
  <done>
Applying field semantics fully covered: bool dtype, exact-aspect edge case, self-synastry diagonal, signed convention, retrograde edge case, ASC/MC speed=0 implications, dense/filtered consistency. Idempotency invariant pinned: `dense[mask] == filtered` modulo ordering for any chart pair. Coverage on `ketu/synastry/` is ≥95% (the locked phase gate). Project test suite green.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- `pytest tests/synastry/ -v` — all Phase 16 synastry tests pass (Plans 01 + 02 combined: ~50+ tests).
- `pytest tests/ -v` — full suite green; no regressions.
- `coverage run --source=ketu -m pytest tests/synastry/ && coverage report --include="ketu/synastry/*" --fail-under=95` — passes.
- `python -m interrogate ketu/ -f 95 -v` — passes.
- `python -m numpydoc lint ketu/synastry/*.py` — zero issues.
- `mypy --strict ketu/synastry/` — clean.
- `python -c "
from ketu.charts import compute_chart
from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE
ca = compute_chart(2451545.0, 48.86, 2.35)
cb = compute_chart(2451900.0, 40.71, -74.01)
filtered = calculate_synastry(ca, cb)
dense = calculate_synastry(ca, cb, mode='dense')
assert dense.shape == (225,)
assert len(filtered) <= len(dense)
assert (filtered['aspect_type'] >= 0).all()
print(f'filtered: {len(filtered)} aspects; dense: {len(dense)} rows')
"` — succeeds.
</verification>

<success_criteria>
- `calculate_synastry(chart_a, chart_b, aspects="classical", orbs="synastry", mode="filtered")` returns SYNASTRY_DTYPE array; `mode="dense"` returns shape (225,) (SYN-01, SYN-04 satisfied).
- SYNASTRY_DTYPE preserves chart-of-origin via `body_a` / `body_b` indices and `lon_a` / `lon_b` longitudes; `aspect_type` + `orb` + `applying` populated correctly (SYN-02).
- Default `orbs="synastry"` applies factor 0.5; orb formula derives from `(orb_a + orb_b)/2 * coef * 0.5`; UTC + factor source cited in docstring (SYN-03).
- Self-pairs (Sun_A↔Sun_B, Moon_A↔Moon_B) appear in dense output (locked CONTEXT.md decision).
- Filtered rows ordered by canonical (body_a, body_b) index ascending (research recommendation).
- applying field velocity-based via natal speeds (research recommendation).
- ASC and MC participate as bodies 13, 14; chart['cusps'] not consulted.
- UTC-only contract restated LOUDLY in docstring Notes section.
- `synastry_orb_limit` returns f4 bit-exact across all 225 pairs (Pitfall 6 strengthened ratchet).
- `tests/synastry/conftest.py` provides explicit Phase-16 fixtures (no reliance on nonexistent `sample_chart`/`polar_chart` in `tests/charts/conftest.py`).
- Coverage on `ketu/synastry/` ≥95% (phase gate; ROADMAP success criterion #5).
- Doc gates green; mypy strict clean.
</success_criteria>

<output>
After completion, create `.planning/phases/16-synastry/16-02-SUMMARY.md` documenting:
- `calculate_synastry` signature, default values, return shape semantics for both modes
- Coverage delta and the new total on `ketu/synastry/`
- Doc-gate status (interrogate score, numpydoc result)
- Test count delta (synastry tests: Plan 01 ~25 → Plan 02 ~50+)
- Any deviations from research recommendations and the rationale
- Hand-off note for Plan 03 (oracle tests) and Plan 04 (CLI) — both can proceed in parallel; both consume `calculate_synastry` from `ketu.synastry`. Plan 03 may extend `tests/synastry/conftest.py` with the `load_oracle_fixture` helper.
- Note any specific edge cases that surprised execution (e.g., Rahu-Rahu conjunction detection at exact distance=0)
</output>
