---
phase: 16-synastry
plan: 05
type: execute
wave: 4
depends_on: ["16-02", "16-03", "16-04"]
files_modified:
  - Makefile
  - pyproject.toml
  - tests/synastry/test_synastry_coverage_gate.py
  - ketu/synastry/__init__.py
  - ketu/synastry/api.py
  - ketu/synastry/orbs.py
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "make synastry-coverage Makefile target exists; runs the synastry test suite + asserts coverage ≥95% on ketu/synastry/ (CI-mirrored)"
    - "synastry_coverage_gate pytest marker is registered in pyproject.toml [tool.pytest.ini_options].markers (mirroring houses_coverage_gate, charts_coverage_gate); tests/synastry/test_synastry_coverage_gate.py asserts the marker is recognized and the synastry module imports cleanly"
    - "All synastry public functions have See Also cross-references pointing to ketu.charts.compute_chart, ketu.aspects.calculate_aspects_vectorized, ketu.synastry.orbs (cross-module discoverability)"
    - "CHANGELOG.md ## [Unreleased] entry documents the additive synastry surface (calculate_synastry, SYNASTRY_DTYPE, ketu synastry CLI, --list-orbs, orbs preset registry)"
    - "All 5 ROADMAP success criteria for Phase 16 are end-to-end satisfied (smoke-tested via integration script in verify step)"
    - "Doc gates green project-wide: interrogate ≥95%, numpydoc validate clean across all v1.2 modules including ketu/synastry/"
  artifacts:
    - path: "Makefile"
      provides: "make synastry-coverage target (mirror make charts-coverage from Phase 14)"
      contains: "synastry-coverage:"
    - path: "pyproject.toml"
      provides: "synastry_coverage_gate pytest marker registered in [tool.pytest.ini_options].markers (mirror houses_coverage_gate, charts_coverage_gate entries)"
      contains: "synastry_coverage_gate"
    - path: "tests/synastry/test_synastry_coverage_gate.py"
      provides: "Sentinel test ratcheting marker recognition + synastry module import"
      contains: "synastry_coverage_gate"
    - path: "ketu/synastry/__init__.py"
      provides: "Updated module docstring with full See Also block"
    - path: "ketu/synastry/api.py"
      provides: "calculate_synastry docstring extended with See Also cross-references"
    - path: "ketu/synastry/orbs.py"
      provides: "resolve_orb_set + synastry_orb_limit docstrings extended with See Also"
    - path: "CHANGELOG.md"
      provides: "## [Unreleased] ### Added entry citing SYN-01..05"
      contains: "synastry"
  key_links:
    - from: "Makefile synastry-coverage"
      to: "tests/synastry/ + ketu/synastry/"
      via: "pytest --cov=ketu/synastry --cov-fail-under=95"
      pattern: "ketu/synastry|cov-fail-under=95"
    - from: "pyproject.toml [tool.pytest.ini_options].markers"
      to: "tests/synastry/test_synastry_coverage_gate.py"
      via: "marker recognition (no PytestUnknownMarkWarning)"
      pattern: "synastry_coverage_gate"
    - from: "ketu/synastry/api.py See Also"
      to: "ketu.charts.compute_chart and ketu.aspects.calculate_aspects_vectorized"
      via: "numpydoc See Also block"
      pattern: "See Also"
---

<objective>
Phase 16 close-out: ratchet the coverage gate via a CI-mirrored `make synastry-coverage` Makefile target, register the `synastry_coverage_gate` pytest marker in `pyproject.toml` (the project's actual marker registration mechanism — there is NO project-wide `tests/conftest.py`), complete the See Also cross-reference graph across `ketu/synastry/*.py` (so `numpydoc validate` shows full discoverability between charts ↔ synastry ↔ orbs ↔ aspects), update `CHANGELOG.md` with the additive `## [Unreleased]` entry citing SYN-01..05, and run the end-to-end integration smoke proving all 5 ROADMAP success criteria are satisfied.

Purpose: This is the doc-gate + traceability ratchet plan. Mirrors Phase 14 Plan 05's close-out shape (RESEARCH.md history-digest entry: "Plan-05 doc-gate ratchet template: any future v1.X subpackage can run a 4-task close-out (audit docstrings -> add See Also -> add coverage Makefile target -> add pytest marker) without touching production code"). No new functionality — pure ratchets and discoverability.

Output: Makefile target, pyproject.toml marker registration, sentinel test, docstring See Also additions, CHANGELOG entry, integration smoke verdict.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-synastry/16-CONTEXT.md
@.planning/phases/16-synastry/16-02-SUMMARY.md
@.planning/phases/16-synastry/16-03-SUMMARY.md
@.planning/phases/16-synastry/16-04-SUMMARY.md

# Codebase patterns to mirror (Phase 14 close-out precedent)
@Makefile
@pyproject.toml
@ketu/charts/__init__.py
@ketu/charts/api.py
@ketu/synastry/__init__.py
@ketu/synastry/api.py
@ketu/synastry/orbs.py
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add make synastry-coverage target + register synastry_coverage_gate pytest marker in pyproject.toml</name>
  <files>Makefile,pyproject.toml,tests/synastry/test_synastry_coverage_gate.py</files>
  <action>
**1. `Makefile`** — add a `synastry-coverage` target. Locate the existing `charts-coverage` target (from Phase 14 Plan 05) via `grep -n "charts-coverage:" Makefile`. Mirror its shape exactly. Add the new target adjacent to it (alphabetical or logical order):

```makefile
.PHONY: synastry-coverage
synastry-coverage:  ## Run synastry tests and assert ≥95% coverage on ketu/synastry/ (Phase 16 SYN-05 gate)
	$(VENV_PYTHON) -m pytest tests/synastry/ \
		--cov=ketu/synastry \
		--cov-report=term-missing \
		--cov-fail-under=95 \
		-v
```

(Adapt the variable names — `VENV_PYTHON`, indentation with hard tabs — to match the actual existing Makefile conventions. Use `cat -A Makefile | head -50` to inspect tab/space conventions before editing.)

If `Makefile` doesn't exist or doesn't have a `charts-coverage` precedent, create the target with `python3` directly (no `$(VENV_PYTHON)` variable) using the actual command sequence run in CI for Phase 14's coverage gate. Investigate `.github/workflows/tests.yml` for the canonical command form.

**2. `pyproject.toml`** — register the `synastry_coverage_gate` pytest marker.

**Reality check (verified by Plan 16 checker):** There is NO project-wide `tests/conftest.py`. Markers are registered in `pyproject.toml` under `[tool.pytest.ini_options].markers` as a list of `"name: description"` strings. The current entries (verify with `grep -A 5 "markers" pyproject.toml` before editing) are:

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = "-v --cov=ketu --cov-report=term-missing"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "houses_coverage_gate: HOU-09 95% coverage gate for ketu.houses (run via Makefile target `make houses-coverage`)",
    "charts_coverage_gate: CHART-05 95% coverage gate for ketu.charts (run via Makefile target `make charts-coverage`)",
]
```

ADD a new entry to the `markers` list mirroring the houses/charts entries exactly:

```toml
    "synastry_coverage_gate: SYN-05 95% coverage gate for ketu.synastry (run via Makefile target `make synastry-coverage`)",
```

The result must be a well-formed TOML list (preserve trailing commas / formatting style of existing entries). Do NOT add a `tests/conftest.py` — that file does NOT exist project-wide and creating it would diverge from established convention.

**3. `tests/synastry/test_synastry_coverage_gate.py`** — sentinel test ratcheting marker recognition (this file STAYS per the original plan):

```python
"""Sentinel test for the synastry_coverage_gate marker (Phase 16 SYN-05).

Ensures the marker registered in ``pyproject.toml`` ``[tool.pytest.ini_options].markers``
is recognized by pytest (no ``PytestUnknownMarkWarning``) and that the
``ketu.synastry`` module imports cleanly.
"""
import pytest


@pytest.mark.synastry_coverage_gate
def test_synastry_module_loads_and_marker_recognized():
    """Sentinel: the marker is registered and the synastry module imports cleanly."""
    import ketu.synastry  # noqa: F401
    # If this test runs without warnings about the marker, registration is OK.
```

This file does NOT enforce the 95% threshold — that's the Makefile + CI's job. The marker is for filtering in `pytest -m synastry_coverage_gate` runs.

Constraints:
- Do not break existing Makefile targets — additive only.
- Do not break existing `pyproject.toml` `[tool.pytest.ini_options].markers` list — additive only (new entry appended).
- Do NOT create `tests/conftest.py` — it does not exist in this project; markers go in `pyproject.toml`.
- Do NOT use `pytest_configure(config)` — that mechanism is not used by this project.
- Use the same indentation style (hard tabs in Makefile) as the existing rules.
- Verify the existing `charts-coverage` invocation actually runs in `.github/workflows/tests.yml`; if there's a CI step like `make charts-coverage`, add a parallel `make synastry-coverage` step or document in the SUMMARY that CI wiring is deferred to Plan 20 (release prep).
  </action>
  <verify>
- `make synastry-coverage` runs and exits 0; output contains "Required test coverage of 95%" and a coverage report listing `ketu/synastry/api.py`, `core.py`, `orbs.py`.
- `grep -c "synastry_coverage_gate" pyproject.toml` ≥ 1 (marker entry appended to the markers list).
- `python -c "import tomllib; d = tomllib.loads(open('pyproject.toml').read()); names = [m.split(':')[0].strip() for m in d['tool']['pytest']['ini_options']['markers']]; assert 'synastry_coverage_gate' in names; print(names)"` prints a list including `synastry_coverage_gate` (proves valid TOML + marker registered).
- `pytest tests/synastry/ -m synastry_coverage_gate -v 2>&1 | grep -i "PytestUnknownMark"` returns NOTHING (no warnings about the marker).
- `pytest tests/synastry/ -m synastry_coverage_gate -v` runs and reports at least 1 test (the sentinel from `test_synastry_coverage_gate.py`).
- `pytest tests/synastry/ -v` — full synastry suite still green (the new marker does not break anything).
- `make help` (if it exists) shows the new target.
- `ls tests/conftest.py 2>&1 | grep "No such"` — confirms no project-wide conftest.py was created (we use `pyproject.toml` markers instead).
  </verify>
  <done>
`make synastry-coverage` is a working Makefile target asserting ≥95% line coverage on `ketu/synastry/`. `synastry_coverage_gate` pytest marker is registered in `pyproject.toml` `[tool.pytest.ini_options].markers` (mirroring `houses_coverage_gate` and `charts_coverage_gate`). Sentinel test in `tests/synastry/test_synastry_coverage_gate.py` ratchets marker recognition — runs without `PytestUnknownMarkWarning`. Existing Makefile + pyproject.toml markers list is preserved (additive only). NO `tests/conftest.py` was created (correctly — that file does not exist in this project).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add See Also cross-references in synastry docstrings + audit numpydoc clean</name>
  <files>ketu/synastry/__init__.py,ketu/synastry/api.py,ketu/synastry/orbs.py</files>
  <action>
Audit the docstrings of every public symbol in `ketu/synastry/` and add See Also cross-references for cross-module discoverability. Mirror the pattern used in `ketu/charts/__init__.py` (See Also block at module level) and `ketu/charts/api.py` (See Also section in each public function docstring).

**1. `ketu/synastry/__init__.py`** — module docstring:
- Ensure the existing module docstring includes a `See Also` section with these references:
  ```
  See Also
  --------
  ketu.charts.compute_chart : Compute a single CHART_DTYPE — input to calculate_synastry.
  ketu.charts.CHART_DTYPE : Frozen layout consumed by calculate_synastry.
  ketu.aspects.calculate_aspects_vectorized : Intra-chart aspect engine (natal, single chart).
  ketu.aspects.presets.resolve_aspect_set : Aspect-set spec resolver (reused for synastry's aspects= param).
  ```
- Numpydoc validate: See Also entries must reference importable Python paths (no `.py` paths or `/` slashes — Phase 13 BLOCKER documented in `ketu/ephemeris/orbital.py` aftermath).

**2. `ketu/synastry/api.py`** — `calculate_synastry` docstring:
- Add (or extend) the existing See Also block:
  ```
  See Also
  --------
  ketu.charts.compute_chart : Build CHART_DTYPE inputs.
  ketu.synastry.orbs.resolve_orb_set : Resolve the orbs= parameter.
  ketu.synastry.orbs.synastry_orb_limit : Per-pair orb formula (scalar form).
  ketu.aspects.presets.resolve_aspect_set : Resolve the aspects= parameter.
  ```

**3. `ketu/synastry/orbs.py`** — public functions:
- `synastry_orb_limit` See Also:
  ```
  See Also
  --------
  ketu.aspects.calculator.get_orb : Natal-orb formula (factor=1.0 equivalent).
  ketu.synastry.orbs.resolve_orb_set : Preset name → factor resolver.
  ketu.synastry.calculate_synastry : Public entry point that uses this formula in vectorised form.
  ```
- `resolve_orb_set` See Also:
  ```
  See Also
  --------
  ketu.aspects.presets.resolve_aspect_set : Sibling resolver for aspects= specs.
  ketu.synastry.orbs.synastry_orb_limit : Scalar orb computation using the resolved factor.
  ```

**Constraints**:
- See Also entries must be importable Python references in `module.symbol` form. Test each one with `python -c "from <module> import <symbol>"` before committing.
- Do not introduce any See Also entries pointing to non-existent symbols.
- Period-terminated sentences in numpydoc descriptions.
- Run `python -m numpydoc lint ketu/synastry/*.py` after each file edit to catch parse errors immediately (Phase 13 lesson: numpydoc's See Also parser rejects file paths and crashes the lint).
- Do NOT modify any function bodies — this task is docstring-only.
  </action>
  <verify>
- `python -m numpydoc lint ketu/synastry/__init__.py ketu/synastry/api.py ketu/synastry/orbs.py` — zero issues, no parser crashes.
- For each See Also entry, `python -c "from ketu.charts import compute_chart, CHART_DTYPE; from ketu.aspects.calculator import calculate_aspects_vectorized, get_orb; from ketu.aspects.presets import resolve_aspect_set; from ketu.synastry import calculate_synastry; from ketu.synastry.orbs import resolve_orb_set, synastry_orb_limit; print('all references resolvable')"` succeeds.
- `python -m interrogate ketu/synastry/ -f 95` still passes.
- `pytest tests/synastry/ -v` still all green (no regressions; docstrings have no runtime impact).
- `python -c "import ketu.synastry; help(ketu.synastry.calculate_synastry)" | grep -A 4 "See Also"` shows the cross-references in the rendered help.
  </verify>
  <done>
Cross-module See Also blocks exist in `ketu/synastry/__init__.py`, `ketu/synastry/api.py`, `ketu/synastry/orbs.py`. All See Also references resolve to importable Python paths (no parser crashes; Phase 13 lesson honored). Numpydoc validate clean across all 4 synastry source files. Project test suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG.md [Unreleased] entry + end-to-end integration smoke for Phase 16 ROADMAP success criteria</name>
  <files>CHANGELOG.md</files>
  <action>
**1. `CHANGELOG.md`** — add an `## [Unreleased]` `### Added` entry citing the synastry surface. Locate the existing `## [Unreleased]` section (Phase 13 added one for OPS-01/OPS-02; subsequent phases extend it). If the section exists, ADD a `### Added` sub-entry for synastry. If `## [Unreleased]` is absent, create it.

The entry should be terse and additive-flagged (the project's v1.2 framing is "non-breaking minor strict — purely additive"):

```markdown
## [Unreleased]

### Added

- `ketu/synastry/` subpackage with `calculate_synastry(chart_a, chart_b)` returning a `SYNASTRY_DTYPE`-formatted structured array (8 fields, record-style; cross-product enumeration of 15 bodies including ASC/MC; self-pairs included per locked decision). Supports `mode="filtered"` (default) and `mode="dense"` outputs. Default `orbs="synastry"` applies a 0.5 multiplicative tightening to Ketu's natal orb formula (Astrodienst convention; cited in `ketu.synastry.orbs.SYNASTRY_FACTOR`). `applying` field is computed from natal speeds (`CHART_DTYPE.body_speeds`) per the static-chart convention. Closes SYN-01, SYN-02, SYN-03, SYN-04 (Phase 16 v1.2). [#PR_NUMBER]
- `ketu/synastry/orbs.py` — `_PRESET_BY_NAME` registry (`synastry`, `classical`), `resolve_orb_set(spec)` resolver, `synastry_orb_limit(b1, b2, asp)` scalar formula, `SYNASTRY_FACTOR=0.5`, `ASC_MC_NATAL_ORB_DEG=8.0` constants. Preset surface is name-only string (rich override deferred to v1.3 per RESEARCH §Open Question 2).
- `ketu synastry --date-a ISO --lat-a F --lon-a F --date-b ISO --lat-b F --lon-b F [--mode filtered|dense] [--system NAME] [--polar-fallback raise|porphyry] [--json]` CLI sub-command — aligned ASCII table by default, JSON list-of-dicts opt-in. Mirrors `ketu houses` CLI conventions.
- `ketu --list-orbs` top-level introspection flag — prints synastry orb preset table with formula derivation. Sibling of `--list-aspect-sets` and `--list-house-systems`.
- 3 hand-validated synastry oracle tests pinned (Marie + Pierre Curie, Princess Diana + Prince Charles, John Lennon + Yoko Ono) — JSON fixtures cite Rodden ratings + AstroDatabank URLs; max orb delta per couple reported in `pytest -v -s` output. Closes SYN-05.
- `make synastry-coverage` Makefile target asserting ≥95% line coverage on `ketu/synastry/` (matches Phase 13 doc-gate contract).
- `synastry_coverage_gate` pytest marker registered in `pyproject.toml` `[tool.pytest.ini_options].markers`.
```

(Adapt header levels and exact wording to match the existing CHANGELOG.md tone — read the existing entries for OPS-01, CHART-01..05, and HOU2-01..05 to mirror the register.)

**2. End-to-end integration smoke** (no file write — verification only; document outcomes in SUMMARY):

Manually run each ROADMAP Phase 16 success criterion:

1. **SC#1**: `python -c "
from ketu.charts import compute_chart
from ketu.synastry import calculate_synastry, SYNASTRY_DTYPE
ca = compute_chart(2451545.0, 48.86, 2.35)
cb = compute_chart(2470204.0, 40.71, -74.01)
result = calculate_synastry(ca, cb, aspects='classical', orbs='synastry')
assert result.dtype == SYNASTRY_DTYPE
required = {'body_a','body_b','aspect_type','orb','applying'}
assert required.issubset(set(result.dtype.names))
print('SC#1 OK:', len(result), 'aspects')
"`

2. **SC#2**: same command but verify both `mode='dense'` (shape (225,)) and `mode='filtered'` (shape ≤ 225) share the SYNASTRY_DTYPE schema.

3. **SC#3**: `python -c "
from ketu.synastry.orbs import synastry_orb_limit
sun_moon_synastry = synastry_orb_limit(0, 1, 0)  # synastry conjunction
sun_moon_classical = synastry_orb_limit(0, 1, 0, factor=1.0)  # natal-equivalent
assert sun_moon_synastry < sun_moon_classical
assert 3 <= sun_moon_synastry <= 8  # in the documented 3-8° band
print(f'SC#3 OK: synastry={sun_moon_synastry}°, classical={sun_moon_classical}°')
"` AND verify `calculate_synastry` docstring contains a citation to "Astrodienst" or "astro.com".

4. **SC#4**: `pytest tests/synastry/test_oracle.py -v -s | grep "max |orb|"` shows 3 max-orb-delta lines (one per oracle couple).

5. **SC#5**: `coverage run --source=ketu/synastry -m pytest tests/synastry/ && coverage report --include="ketu/synastry/*" --fail-under=95` passes AND `python -c "import ketu.synastry; help(ketu.synastry.calculate_synastry)" | grep -i "UTC"` shows the UTC-only restated-loud line.

Document each success criterion with a PASS/FAIL verdict + the actual command output excerpt in `16-05-SUMMARY.md`. If ANY criterion fails, STOP and either fix the gap (preferred) or document the deviation explicitly with a justification.

**3. (Optional follow-up — manual)** Inherit from Plan 03's deferred Astro.com cross-validation:
- For each oracle (Curie, Diana/Charles, Lennon/Ono), open the synastry chart on Astro.com manually (or another reference tool).
- Capture any discrepancies between the self-consistency oracle in `tests/synastry/fixtures/oracle_*.json` and Astro.com's rendered aspects.
- Document discrepancies (if any) in `16-05-SUMMARY.md` under a "## Astro.com cross-validation follow-up" section.
- This is OPTIONAL — Plan 03's self-consistency oracle is the primary regression contract; Astro.com agreement is a quality bar for future v1.3 work.

Constraints:
- CHANGELOG entry uses past-tense additive verbs ("Added", "Pinned", "Mirrors") consistent with existing entries.
- Do NOT add a `### Changed` or `### Deprecated` entry for this phase — v1.2 framing is "purely additive" per CONTEXT.md.
- Do NOT add a `[1.2.0]` heading — Phase 20 (release prep) owns that.
- The smoke commands MUST be runnable as-is (copy-pasteable into a fresh terminal in the repo root with `venv` activated).
- If any smoke command exits non-zero, do not proceed to commit; investigate and fix the gap (which usually means a Plan 02 or Plan 03 regression — re-run the prior plan's verify steps).
  </action>
  <verify>
- `git diff CHANGELOG.md` shows additions to `## [Unreleased]` `### Added` only — no other section changed.
- All 5 ROADMAP smoke commands exit 0 with the documented output patterns.
- `pytest tests/ -v` — full project suite green (final regression check).
- `coverage run --source=ketu -m pytest tests/ && coverage report --include="ketu/synastry/*" --fail-under=95` passes.
- `coverage report --include="ketu/cli/synastry_cmd.py" --fail-under=85` passes (CLI dispatcher coverage; less strict than synastry module proper).
- `python -m interrogate ketu/ -f 95 -v` — project-wide ≥95%.
- `python -m numpydoc lint $(find ketu -name "*.py" -not -name "_*.py" -not -name "lunar_calendar.py")` zero issues.
- `mypy --strict ketu/` clean.
- `make synastry-coverage` exits 0.
  </verify>
  <done>
CHANGELOG.md `## [Unreleased]` `### Added` includes Phase 16 entries citing SYN-01..05 (with marker registered in `pyproject.toml`, NOT in `tests/conftest.py`). All 5 ROADMAP success criteria for Phase 16 are end-to-end satisfied (smoke commands documented in SUMMARY with PASS verdicts and output excerpts). Project test suite green; coverage on `ketu/synastry/` ≥95%; doc gates green; mypy strict clean. Phase 16 is shippable.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- `make synastry-coverage` exits 0.
- `pytest tests/ -v` — full suite green; new total ~970+ tests.
- `coverage run --source=ketu -m pytest tests/ && coverage report --fail-under=90` passes (project-wide ≥90%; new module ≥95%).
- `python -m interrogate ketu/ -f 95 -v` passes.
- `python -m numpydoc lint $(find ketu -name "*.py" -not -name "_*.py" -not -name "lunar_calendar.py")` zero issues.
- `mypy --strict ketu/` clean.
- `python -c "import ketu.synastry; help(ketu.synastry.calculate_synastry)" | grep -i "UTC"` shows the UTC restated-loud line.
- `python -c "import tomllib; d = tomllib.loads(open('pyproject.toml').read()); names = [m.split(':')[0].strip() for m in d['tool']['pytest']['ini_options']['markers']]; assert 'synastry_coverage_gate' in names; print('marker OK')"` succeeds.
- `ls tests/conftest.py 2>&1 | grep "No such"` confirms no project-wide conftest.py (intentional — markers live in pyproject.toml).
- All 5 ROADMAP Phase 16 success criteria documented as PASS in 16-05-SUMMARY.md.
</verification>

<success_criteria>
- All 5 ROADMAP Phase 16 success criteria end-to-end satisfied (SC#1-5; documented in 16-05-SUMMARY.md).
- `make synastry-coverage` is the CI-mirrored gate enforcing ≥95% line coverage on `ketu/synastry/`.
- `synastry_coverage_gate` pytest marker registered in `pyproject.toml` `[tool.pytest.ini_options].markers` (mirroring the project's actual mechanism — NOT in a nonexistent `tests/conftest.py`).
- See Also cross-references span charts ↔ synastry ↔ orbs ↔ aspects (numpydoc validate clean).
- CHANGELOG.md `## [Unreleased]` `### Added` documents the additive synastry surface (SYN-01..05).
- Project test suite green; doc gates green; mypy strict clean.
- Phase 16 is ready for `/gsd:verify-phase` then `/gsd:close-phase`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-synastry/16-05-SUMMARY.md` documenting:
- Files created/modified
- The 5 ROADMAP success criteria with PASS verdicts and output excerpts
- Final coverage on `ketu/synastry/` and on `ketu/cli/synastry_cmd.py`
- Project-wide test count delta (Phase 16 added ~75-90 tests across all 5 plans)
- Doc-gate status (interrogate score, numpydoc result)
- Confirmation that the marker was registered in `pyproject.toml` (not in a `tests/conftest.py`) — H-1 ratchet
- Optional "## Astro.com cross-validation follow-up" section if performed
- Any deferred items handed off to Phase 17 / 18 / 20
- Sign-off: "Phase 16 ready for /gsd:verify-phase 16" — confirms close-out is complete and the phase can move to verification
</output>
