---
phase: 16-synastry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/synastry/__init__.py
  - ketu/synastry/core.py
  - ketu/synastry/orbs.py
  - tests/synastry/__init__.py
  - tests/synastry/test_dtype.py
  - tests/synastry/test_orbs.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "ketu.synastry imports cleanly: SYNASTRY_DTYPE, SYNASTRY_BODY_COUNT, SYNASTRY_FACTOR, ASC_MC_NATAL_ORB_DEG, resolve_orb_set are public"
    - "SYNASTRY_DTYPE has exactly 8 fields in the documented order; field dtypes pinned"
    - "SYNASTRY_BODY_COUNT == 15 (13 canonical + ASC + MC)"
    - "resolve_orb_set('synastry') returns 0.5; resolve_orb_set('classical') returns 1.0; resolve_orb_set(None) returns 0.5"
    - "synastry_orb_limit reproduces Sun-Moon conjunction natal=12° → synastry=6° and Venus-Mars trine derivation"
    - "Rahu/Ketu/Lilith self-pair orb is 0.0 (zero-orb pinned by test)"
    - "Doc gates green: interrogate ≥95% on ketu/synastry/, numpydoc validate clean"
  artifacts:
    - path: "ketu/synastry/__init__.py"
      provides: "Public surface exports (4 names)"
      contains: "SYNASTRY_DTYPE"
    - path: "ketu/synastry/core.py"
      provides: "SYNASTRY_DTYPE definition + SYNASTRY_BODY_COUNT constant + module 'Why structured array' docstring"
      contains: "SYNASTRY_DTYPE"
    - path: "ketu/synastry/orbs.py"
      provides: "Orb formula, factor presets, ASC_MC_NATAL_ORB_DEG, resolve_orb_set resolver"
      contains: "_PRESET_BY_NAME"
    - path: "tests/synastry/test_dtype.py"
      provides: "Structural ratchets on SYNASTRY_DTYPE shape and field names"
    - path: "tests/synastry/test_orbs.py"
      provides: "Formula values, preset resolver, zero-orb edge case"
    - path: "pyproject.toml"
      provides: "ketu.synastry registered in [tool.setuptools].packages list"
      contains: "ketu.synastry"
  key_links:
    - from: "ketu/synastry/orbs.py"
      to: "ketu.core.bodies['orb'] and ketu.core.aspects['coef']"
      via: "_BODY_ORBS_15 concatenation + formula reuse"
      pattern: "_BODIES\\[.orb.\\]"
    - from: "ketu/synastry/__init__.py"
      to: "ketu/synastry/core.py and ketu/synastry/orbs.py"
      via: "explicit re-export, __all__ defined"
      pattern: "from \\.core import|from \\.orbs import"
---

<objective>
Lay the data-contract foundation for Phase 16 Synastry: the `ketu/synastry/` subpackage skeleton, the frozen `SYNASTRY_DTYPE` (8 fields, record-style), the body-axis constant (15 = 13 canonical + ASC + MC), the orb formula module (`orbs.py`) with `_PRESET_BY_NAME` + `resolve_orb_set` resolver mirroring `ketu/aspects/presets.py`, and the corresponding test ratchets.

Purpose: Lock the synastry data contract BEFORE any compute logic touches it, so Plan 02 (calculate_synastry) and Plan 04 (CLI) consume a frozen dtype + frozen orb-resolution surface. Mirrors the Phase 14 Plan 01 pattern (skeleton + frozen dtype + structural tests first; compute later).

Output: `ketu/synastry/{__init__.py, core.py, orbs.py}` + test files + pyproject.toml package registration. Importable surface: `from ketu.synastry import SYNASTRY_DTYPE, SYNASTRY_BODY_COUNT, SYNASTRY_FACTOR, ASC_MC_NATAL_ORB_DEG, resolve_orb_set`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-synastry/16-CONTEXT.md
@.planning/phases/16-synastry/16-RESEARCH.md

# Codebase patterns to mirror
@ketu/charts/__init__.py
@ketu/charts/core.py
@ketu/aspects/presets.py
@ketu/core.py
@ketu/aspects/calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ketu/synastry/ skeleton + SYNASTRY_DTYPE + SYNASTRY_BODY_COUNT</name>
  <files>ketu/synastry/__init__.py,ketu/synastry/core.py,pyproject.toml</files>
  <action>
Create the `ketu/synastry/` subpackage with these files:

1. `ketu/synastry/core.py`:
   - Module docstring: "Core types for the synastry subpackage" + "Why a structured array?" section mirroring `ketu/charts/core.py:1-42` (cite Kala ML-interop, batchability, self-describing — point to D-08 frozen-axis lineage). Include a "Why 8 fields, not 5 or 12?" sub-section explaining the auto-sufficiency rationale (5 mandatory from ROADMAP + 3 metadata: lon_a, lon_b, orb_limit) and the explicit rejection of axis-style ((15, 15) matrix) layout — record-style aligns with both dense and filtered modes under one schema.
   - Define `SYNASTRY_BODY_COUNT: int = 15` with docstring noting "13 canonical (per CHART_DTYPE) + ASC + MC; frozen for v1.2; v1.3 may add Vertex if Phase 17/18 demand it."
   - Define `SYNASTRY_DTYPE: np.dtype` exactly as specified in 16-RESEARCH.md §"Example 1: SYNASTRY_DTYPE definition" (lines 427-436):
     ```
     ("body_a",      "i1"),
     ("body_b",      "i1"),
     ("lon_a",       "f8"),
     ("lon_b",       "f8"),
     ("aspect_type", "i1"),
     ("orb",         "f4"),
     ("applying",    "?"),
     ("orb_limit",   "f4"),
     ```
   - Numpydoc `Notes` section enumerating each field's semantics, valid range, and the `aspect_type=-1` / `orb=NaN` sentinel convention (match Phase 14 D-06 sentinel pattern from `ketu/charts/core.py:63-77`).

2. `ketu/synastry/__init__.py`:
   - Module docstring mirroring `ketu/charts/__init__.py:1-38` (public API surface section, See Also block pointing to `ketu.charts.compute_chart` and `ketu.aspects.calculate_aspects_vectorized`, Notes section restating UTC-only contract LOUDLY).
   - Imports: `from .core import SYNASTRY_DTYPE, SYNASTRY_BODY_COUNT` and `from .orbs import SYNASTRY_FACTOR, ASC_MC_NATAL_ORB_DEG, resolve_orb_set, OrbSetSpec`.
   - `__all__ = ["SYNASTRY_DTYPE", "SYNASTRY_BODY_COUNT", "SYNASTRY_FACTOR", "ASC_MC_NATAL_ORB_DEG", "resolve_orb_set", "OrbSetSpec"]`. Note: `calculate_synastry` is NOT in this plan — it lands in Plan 02. The `__init__.py` will be re-edited in Plan 02 to add it. Make the Plan 01 `__init__.py` self-consistent (no broken imports).

3. `pyproject.toml`:
   - Locate the `[tool.setuptools] packages = [...]` (or equivalent `packages` list) and add `"ketu.synastry"` in alphabetical order. If using `setuptools.find` automatic discovery, just confirm the new package will be picked up (no edit needed) and document this in the SUMMARY. Mirror exactly how `ketu.charts` was added in Phase 14 Plan 01 (run `git log --oneline -- pyproject.toml | head` to find the precedent commit).

Constraints:
- Pure-NumPy. No new runtime dependency.
- All public names get full numpydoc docstrings (interrogate ≥95% gate is BLOCKING from Phase 13).
- Period-terminated summaries (numpydoc convention pinned by Phase 13).
- Do NOT define `calculate_synastry` here — Plan 02 owns the compute logic.
- Do NOT touch `ketu/__init__.py` — `ketu.synastry` is a subpackage with its own surface, like `ketu.charts` and `ketu.houses`.
  </action>
  <verify>
- `python -c "from ketu.synastry import SYNASTRY_DTYPE, SYNASTRY_BODY_COUNT; print(SYNASTRY_BODY_COUNT, len(SYNASTRY_DTYPE.names))"` prints `15 8`.
- `python -c "from ketu.synastry import SYNASTRY_DTYPE; assert SYNASTRY_DTYPE.names == ('body_a','body_b','lon_a','lon_b','aspect_type','orb','applying','orb_limit')"` exits 0.
- `python -m interrogate ketu/synastry/ -f 95` passes.
- `python -m numpydoc lint ketu/synastry/__init__.py ketu/synastry/core.py` returns zero issues.
- `pip install -e . && python -c "import ketu.synastry"` succeeds (proves pyproject.toml package registration is correct).
  </verify>
  <done>
Subpackage `ketu.synastry` is importable from a fresh editable install. `SYNASTRY_DTYPE` is frozen with 8 fields in the documented order. `SYNASTRY_BODY_COUNT` constant exposed at the package surface. Module docstrings are numpydoc-clean and interrogate-clean. `__init__.py` does NOT yet expose `calculate_synastry` (deferred to Plan 02).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ketu/synastry/orbs.py — formula + presets + resolver</name>
  <files>ketu/synastry/orbs.py</files>
  <action>
Create `ketu/synastry/orbs.py` implementing the synastry orb-formula module, mirroring `ketu/aspects/presets.py:91-156` and matching 16-RESEARCH.md §"Example 2: Orb formula + preset resolver" (lines 441-530).

Required public surface (must match the example exactly):
1. **Constants** (with full numpydoc docstrings):
   - `SYNASTRY_FACTOR: float = 0.5` — cite source: "Astrodienst (astro.com) FAQ 'Partner horoscopes': 'for drawing aspects in a synastry chart, half the orb of the natal chart is used.' Single-source-of-truth multiplicative factor; no parallel hardcoded orb table per CONTEXT.md locked decision."
   - `ASC_MC_NATAL_ORB_DEG: float = 8.0` — cite: "Mid-tier convention (matches Mercury/Mars/Uranus/Neptune in `ketu.core.bodies['orb']`); halved by SYNASTRY_FACTOR yields 4° on ASC↔planet conjunctions, matching astro.com practice. ASC and MC are not in `ketu.core.bodies` (they are houses fields), hence this dedicated default."

2. **Internal table** `_BODY_ORBS_15: np.ndarray` (shape `(15,)`, `np.float32`):
   - Concatenate `ketu.core.bodies["orb"].astype(np.float32)` (13 entries) with `np.array([ASC_MC_NATAL_ORB_DEG] * 2, dtype=np.float32)` (2 entries for ASC, MC).
   - Frozen via `_BODY_ORBS_15.flags.writeable = False`.
   - Module-level constant; computed once at import time.

3. **Type alias** `OrbSetSpec = Union[None, str]` — mirror the `AspectSetSpec` shape in `ketu/aspects/presets.py:97-98`. NOTE: MVP scope is name-only string preset (per 16-RESEARCH.md §"Open Questions" Q2). Do NOT accept dict / callable / Sequence in v1.2.

4. **Function** `synastry_orb_limit(b1: int, b2: int, asp: int, factor: float = SYNASTRY_FACTOR) -> float`:
   - Body of formula EXACTLY: `float((_BODY_ORBS_15[b1] + _BODY_ORBS_15[b2]) / 2.0 * float(_ASPECTS["coef"][asp]) * factor)`.
   - Docstring cites the natal formula source: the function `get_orb` in `ketu/aspects/calculator.py` (currently at line 50 — verify with `grep -n "orbs\[body1\] + orbs\[body2\]" ketu/aspects/calculator.py` before writing the docstring; cite by function name AND verified line number).
   - Examples block in docstring with Sun-Moon conjunction → 6.0 and the Rahu-Rahu zero-orb edge case → 0.0.

5. **Registry** `_PRESET_BY_NAME: dict[str, float]`:
   - `{"synastry": SYNASTRY_FACTOR, "classical": 1.0}`.
   - **Naming**: use `_PRESET_BY_NAME` (singular) to match the existing project convention in `ketu/aspects/presets.py:91` (`_PRESET_BY_NAME: dict[str, npt.NDArray[np.bool_]]`). Do NOT use `_PRESETS_BY_NAME` (plural) — that drifts from the established sibling module.
   - Module-level docstring comment: "v1.2 ships ONE preset (`synastry`); `classical` accepts the natal formula unchanged for expert comparison views (CONTEXT.md gives discretion). v1.3 may add `liz_greene` per-aspect coefficients via in-place dict extension."

6. **Function** `resolve_orb_set(spec: OrbSetSpec) -> float`:
   - `None` or `"synastry"` → `SYNASTRY_FACTOR` (0.5).
   - `"classical"` → 1.0.
   - Lower-case string lookup against `_PRESET_BY_NAME`.
   - Unknown string → `ValueError` with message listing valid presets (mirror `resolve_aspect_set` error message pattern at `ketu/aspects/presets.py:194-198`).
   - Non-string non-None → `ValueError` with message naming the offending type.
   - Full numpydoc docstring with Parameters / Returns / Raises / Examples sections.

Constraints:
- Pure-NumPy. Re-use `ketu.core.bodies` and `ketu.core.aspects` imports (rename to `_BODIES` / `_ASPECTS` private aliases per the same convention used in `ketu/aspects/calculator.py:19`).
- Float dtype care (Pitfall 6 from 16-RESEARCH.md): the formula uses `np.float32` arithmetic via `_BODY_ORBS_15` and `_ASPECTS["coef"]` (also f4); the final cast to Python `float` is explicit. Do NOT introduce silent f8 upcasts inside the per-pair vectorized code path (Plan 02 owns the vectorized version, but the scalar `synastry_orb_limit` here must agree with it bit-for-bit on equivalent inputs).
- Period-terminated summaries (Phase 13 numpydoc convention).
- All public names documented for interrogate ≥95% gate.
  </action>
  <verify>
- `python -c "from ketu.synastry.orbs import SYNASTRY_FACTOR, ASC_MC_NATAL_ORB_DEG, resolve_orb_set, synastry_orb_limit; assert SYNASTRY_FACTOR == 0.5 and ASC_MC_NATAL_ORB_DEG == 8.0 and resolve_orb_set('synastry') == 0.5 and resolve_orb_set('classical') == 1.0 and resolve_orb_set(None) == 0.5; print('OK')"` prints `OK`.
- `python -c "from ketu.synastry.orbs import synastry_orb_limit; print(round(synastry_orb_limit(0, 1, 0), 4), round(synastry_orb_limit(10, 10, 0), 4))"` prints `6.0 0.0` (Sun-Moon conjunction = 6°; Rahu-Rahu conjunction = 0°).
- `python -c "from ketu.synastry.orbs import resolve_orb_set; resolve_orb_set('xyzzy')"` raises `ValueError` with message listing `classical, synastry`.
- `python -c "from ketu.synastry.orbs import _PRESET_BY_NAME; print(sorted(_PRESET_BY_NAME))"` prints `['classical', 'synastry']` (singular `_PRESET_BY_NAME` matches `ketu/aspects/presets.py` convention).
- `python -m interrogate ketu/synastry/orbs.py -f 95` passes.
- `python -m numpydoc lint ketu/synastry/orbs.py` returns zero issues.
  </verify>
  <done>
`ketu/synastry/orbs.py` implements `SYNASTRY_FACTOR`, `ASC_MC_NATAL_ORB_DEG`, `_BODY_ORBS_15` (frozen), `OrbSetSpec` type alias, `synastry_orb_limit()` scalar function, `_PRESET_BY_NAME` registry (singular, matching `ketu/aspects/presets.py`), and `resolve_orb_set()` resolver. Sun-Moon conjunction synastry orb is 6.0°; Rahu-Rahu conjunction is 0.0° (documented edge case). Unknown preset raises a helpful `ValueError`. Doc gates green on the file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add structural + formula tests in tests/synastry/{test_dtype.py, test_orbs.py}</name>
  <files>tests/synastry/__init__.py,tests/synastry/test_dtype.py,tests/synastry/test_orbs.py</files>
  <action>
Create the `tests/synastry/` test package mirroring `tests/charts/` layout (Phase 14 precedent).

1. `tests/synastry/__init__.py` — empty file (test package marker).

2. `tests/synastry/test_dtype.py` — structural ratchets on `SYNASTRY_DTYPE` (~12-15 tests). Mirror `tests/charts/test_dtype.py` shape:
   - `test_dtype_field_count_eight` — `len(SYNASTRY_DTYPE.names) == 8`.
   - `test_dtype_field_names_canonical_order` — assert `SYNASTRY_DTYPE.names == ("body_a","body_b","lon_a","lon_b","aspect_type","orb","applying","orb_limit")` (this is the FROZEN contract — any reorder breaks Kala consumers).
   - `test_dtype_body_a_dtype_int8` and `test_dtype_body_b_dtype_int8`.
   - `test_dtype_lon_a_dtype_float64` and `test_dtype_lon_b_dtype_float64`.
   - `test_dtype_aspect_type_dtype_int8` (sentinel -1 must fit; i1 range [-128, 127] OK).
   - `test_dtype_orb_dtype_float32` (memory budget — orb is f4 not f8).
   - `test_dtype_applying_dtype_bool`.
   - `test_dtype_orb_limit_dtype_float32`.
   - `test_synastry_body_count_frozen_at_fifteen` — `SYNASTRY_BODY_COUNT == 15` (ratchet against accidental Vertex addition before v1.3).
   - `test_dtype_itemsize_pinned` — assert `SYNASTRY_DTYPE.itemsize` equals the computed sum (1+1+8+8+1+4+1+4 = 28 bytes; pin to catch struct-padding surprises).
   - `test_can_allocate_empty_array_with_dtype` — `np.empty(225, dtype=SYNASTRY_DTYPE).shape == (225,)` (positive sanity).
   - `test_anti_axis_style_no_two_d_subarray` — assert no field has a 2-D subshape (ratchet against accidental N×N matrix layout — record-style is the locked convention).

3. `tests/synastry/test_orbs.py` — formula + resolver tests (~12-15 tests):
   - `test_synastry_factor_value` — `SYNASTRY_FACTOR == 0.5`.
   - `test_asc_mc_natal_orb_deg_value` — `ASC_MC_NATAL_ORB_DEG == 8.0`.
   - `test_body_orbs_15_shape_and_dtype` — `_BODY_ORBS_15.shape == (15,)` and `dtype == np.float32`; entries 0-12 equal `bodies["orb"]`; entries 13-14 equal 8.0.
   - `test_body_orbs_15_frozen` — `_BODY_ORBS_15.flags.writeable is False` (mutation guard).
   - `test_synastry_orb_limit_sun_moon_conjunction` — `synastry_orb_limit(0, 1, 0) == 6.0` (12+12)/2 * 1 * 0.5.
   - `test_synastry_orb_limit_venus_mars_trine` — `synastry_orb_limit(3, 4, 9) ≈ (10+8)/2 * (2/3) * 0.5 = 3.0` (Trine is index 9 in core.aspects).
   - `test_synastry_orb_limit_sun_sun_conjunction` — self-pair = 6.0 (matches astro.com headline).
   - `test_synastry_orb_limit_rahu_rahu_zero_orb` — `synastry_orb_limit(10, 10, 0) == 0.0` (documented zero-orb behavior; pre-empts user surprise per Pitfall 2 in 16-RESEARCH.md).
   - `test_synastry_orb_limit_asc_planet_conjunction` — `synastry_orb_limit(13, 0, 0) == 5.0` ((8+12)/2 * 1 * 0.5).
   - `test_resolve_orb_set_synastry` — returns 0.5.
   - `test_resolve_orb_set_classical` — returns 1.0.
   - `test_resolve_orb_set_none_defaults_to_synastry` — `resolve_orb_set(None) == 0.5`.
   - `test_resolve_orb_set_case_insensitive` — `resolve_orb_set("SYNASTRY") == 0.5`.
   - `test_resolve_orb_set_unknown_string_raises_with_valid_list` — `pytest.raises(ValueError, match="synastry")` (the error message must enumerate valid presets).
   - `test_resolve_orb_set_invalid_type_raises` — `resolve_orb_set(42)` raises `ValueError`.
   - `test_orb_limit_dtype_pure_python_float` — return value is `isinstance(result, float)` (not numpy scalar, ratchets against silent f4/f8 confusion).

Constraints:
- Use plain pytest (no hypothesis in this plan).
- All test functions have one-line docstrings (interrogate gate applies to tests too — verify against the actual `[tool.interrogate]` exclusion list; if tests are excluded, docstrings still recommended for readability).
- Tests are pure data assertions — no chart computation, no I/O.
  </action>
  <verify>
- `pytest tests/synastry/test_dtype.py tests/synastry/test_orbs.py -v` — all tests PASS.
- `pytest tests/synastry/ --co -q | wc -l` — collected test count is at least 25 (12 dtype + 13+ orbs).
- `coverage run --source=ketu/synastry -m pytest tests/synastry/test_dtype.py tests/synastry/test_orbs.py && coverage report --include="ketu/synastry/*"` — coverage on `ketu/synastry/core.py` and `ketu/synastry/orbs.py` is ≥95% (the only uncovered branches should be the type-error path in `resolve_orb_set` if not exercised by `test_resolve_orb_set_invalid_type_raises`; ensure that test is present).
- `python -m interrogate ketu/synastry/ -f 95 -v` passes with score 100% (every public function/class has a docstring).
- `python -m numpydoc lint ketu/synastry/__init__.py ketu/synastry/core.py ketu/synastry/orbs.py` — zero issues.
  </verify>
  <done>
`tests/synastry/` test package exists with `test_dtype.py` (~12 tests) and `test_orbs.py` (~15 tests) all passing. Coverage on `ketu/synastry/core.py` and `ketu/synastry/orbs.py` is ≥95%. SYNASTRY_DTYPE field order is FROZEN by ratchet test. SYNASTRY_BODY_COUNT == 15 ratcheted. Sun-Moon synastry conjunction orb pinned at 6°. Rahu-Rahu zero-orb edge case documented in test name. Doc gates green.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- `pytest tests/synastry/ -v` — all tests pass (≥25 tests).
- `pytest tests/ -v` — full suite still green (no regressions; v1.1.0 baseline = 909 tests; expected new total ≈ 935+).
- `coverage run --source=ketu -m pytest tests/synastry/ && coverage report --include="ketu/synastry/*" --fail-under=95` — passes.
- `python -m interrogate ketu/ -f 95 -v` — passes (project-wide docstring coverage maintained).
- `python -m numpydoc lint ketu/synastry/*.py` — zero issues.
- `python -c "from ketu.synastry import SYNASTRY_DTYPE, SYNASTRY_BODY_COUNT, SYNASTRY_FACTOR, ASC_MC_NATAL_ORB_DEG, resolve_orb_set; print('public surface OK')"` succeeds.
- `mypy --strict ketu/synastry/` clean.
</verification>

<success_criteria>
- `ketu.synastry` is importable; SYNASTRY_DTYPE has 8 fields in canonical order with documented dtypes.
- `SYNASTRY_BODY_COUNT == 15` is the frozen body-axis size for v1.2.
- `synastry_orb_limit` reproduces the locked formula (`(orb_a + orb_b) / 2 * coef * 0.5`) with documented Sun-Moon = 6° and Rahu-Rahu = 0° edge cases.
- `resolve_orb_set("synastry") == 0.5`, `resolve_orb_set("classical") == 1.0`, `resolve_orb_set(None) == 0.5`; unknown preset raises `ValueError` listing valid names.
- `_PRESET_BY_NAME` (singular) matches the existing `ketu/aspects/presets.py` naming convention.
- All public names have full numpydoc docstrings; interrogate ≥95% green; numpydoc validate clean.
- Coverage on `ketu/synastry/core.py` and `ketu/synastry/orbs.py` is ≥95%.
- pyproject.toml registers `ketu.synastry` for distribution.
</success_criteria>

<output>
After completion, create `.planning/phases/16-synastry/16-01-SUMMARY.md` documenting:
- Files created (paths + LoC)
- Public symbols exposed at `ketu.synastry` surface
- Coverage delta (% on ketu/synastry/)
- Doc-gate status (interrogate score, numpydoc lint result)
- Any deviations from research recommendations and the rationale
- Hand-off note for Plan 02: "calculate_synastry is the next public symbol to add to __init__.py; the dtype + orbs surface is now FROZEN."
</output>
