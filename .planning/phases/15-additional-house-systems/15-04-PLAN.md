---
phase: 15-additional-house-systems
plan: 04
plan_name: cli-integration-and-phase-gates
type: execute
wave: 2
depends_on: [15-02, 15-03]
files_modified:
  - ketu/cli/parser.py
  - ketu/cli/introspection.py
  - tests/cli/test_introspection.py
  - tests/cli/test_houses_cmd.py
autonomous: true
requirements:
  - HOU2-04
  - HOU2-05
parallelizable_with: []

must_haves:
  truths:
    - "`ketu houses --list-house-systems` retourne `equal, koch, placidus, porphyry, regiomontanus, whole_sign` (6 systèmes triés alphabétiquement, déterministe)"
    - "Le parser argparse `--system` accepte dynamiquement tous les systèmes enregistrés (`choices=sorted(SYSTEMS.keys())`), pas une liste hardcodée"
    - "`ketu houses --date ... --lat ... --lon ... --system whole_sign` (et pour equal et regiomontanus) retourne les cusps formatés sans erreur"
    - "Le test legacy `test_invalid_system_rejected` est inversé : `regiomontanus` est désormais ACCEPTÉ ; un nouveau nom impossible (`nonexistent_xyz`) ratchet le rejet"
    - "Chaque système dans `SYSTEMS` a une entrée `_SYSTEM_DESCRIPTIONS` (no `(no description available)` fallback affiché)"
    - "Les CLI cusps matchent l'API Python pour les 6 systèmes sur 3 locations (Paris/Sydney/Greenwich)"
    - "Doc gates Phase 13 verts sur l'ensemble `ketu/houses/` : `numpydoc validate` clean ; `interrogate ≥95%`"
    - "Coverage `ketu/houses/` ≥95% (gate v1.2 strict pour les nouveaux modules)"
  artifacts:
    - path: "ketu/cli/parser.py"
      provides: "Dynamic --system choices via sorted(SYSTEMS.keys()) — auto-extends with future v1.3 systems"
      contains: "sorted(SYSTEMS.keys())"
    - path: "ketu/cli/introspection.py"
      provides: "_SYSTEM_DESCRIPTIONS extended with 3 v1.2 entries (whole_sign, equal, regiomontanus)"
      contains: '"whole_sign"'
    - path: "tests/cli/test_introspection.py"
      provides: "TestListHouseSystems::test_lists_registered_systems extended to 6 names + new every-system-has-description ratchet"
    - path: "tests/cli/test_houses_cmd.py"
      provides: "test_invalid_system_rejected inverted; TestHousesCmdMatchesPythonAPI parametrized on 6 systems"
  key_links:
    - from: "ketu/cli/parser.py:135"
      to: "ketu.houses.SYSTEMS"
      via: "choices=sorted(SYSTEMS.keys()) — dynamic, evaluates at import time"
      pattern: "choices=sorted\\(SYSTEMS"
    - from: "ketu/cli/introspection.py:_SYSTEM_DESCRIPTIONS"
      to: "test_every_registered_system_has_description"
      via: "ratchet that no system in SYSTEMS lacks an entry"
      pattern: "no description available"
    - from: "tests/cli/test_houses_cmd.py:test_invalid_system_rejected"
      to: "argparse choices error"
      via: "use 'nonexistent_xyz' as the invalid name (regiomontanus now valid)"
      pattern: "nonexistent_xyz"
---

# Plan 15-04 — CLI integration et clôture des success criteria de Phase 15

## Goal

Plan **finition** : intégrer les 3 nouveaux house systems au CLI Ketu, inverser le test legacy v1.1 qui rejetait `regiomontanus`, étendre les descriptions introspection, et valider tous les **success criteria de Phase 15** (lignes 119-126 de ROADMAP.md). Ce plan ferme HOU2-04 (CLI listing) et HOU2-05 (validation finale par les gates phase). Aucune logique mathématique de house system n'est touchée — seul le surface CLI bouge.

**Quatre actions concrètes :**

1. **Dispatcher dynamique** (D-07 verrouillé) : remplacer `choices=["placidus", "koch", "porphyry"]` ligne 135 de `parser.py` par `choices=sorted(SYSTEMS.keys())`. Le parser apprend automatiquement les systèmes enregistrés au moment de `import ketu.cli` (qui chaîne `import ketu.houses` qui exécute les `@register` decorators). Future-proof : Campanus / Topocentric / Alcabitius en v1.3 ne nécessiteront aucune modification du parser.
2. **Descriptions introspection** : étendre `_SYSTEM_DESCRIPTIONS` dans `ketu/cli/introspection.py:22-26` avec 3 entrées (whole_sign, equal, regiomontanus). Le formatage `cmd_list_house_systems` (`introspection.py:48-57`) consomme le dict via `sorted(_HOUSE_SYSTEMS.keys())` — le listing s'étend automatiquement à 6 systèmes (HOU2-04).
3. **Inverser le test legacy** (D-08 verrouillé) : `tests/cli/test_houses_cmd.py:53-59` (`test_invalid_system_rejected`) asserte actuellement que `--system regiomontanus` retourne `SystemExit(2)`. C'est obsolète depuis ce plan — `regiomontanus` est désormais un choice valide. Le test devient :
   - **Garde la sémantique** : un système inconnu doit toujours être rejeté (CLI-04).
   - **Substitue le nom** : utiliser un nom impossible (`nonexistent_xyz`) qui ne sera jamais enregistré.
   - **Ajoute un test positif** : `regiomontanus` (et whole_sign + equal) sont désormais ACCEPTÉS — `rc == 0`.
4. **Coverage + doc gates** : valider que `ketu/houses/` reste ≥95% coverage (avec les 3 nouveaux modules) ; `numpydoc validate` clean ; `interrogate ≥95%`. Ces gates sont déjà actifs depuis Phase 13 (OPS-01, OPS-02) — ce plan vérifie qu'ils restent verts après l'addition des 3 fichiers.

**Plan de clôture des success criteria Phase 15** (ROADMAP §119-126) :

| Success criterion | Couvert par | Test ratchet |
|-------------------|-------------|--------------|
| 1. Each system returns valid HOUSES_DTYPE; W/E polar-safe | Plans 15-02, 15-03 | `test_calculate_houses_routes_*`, `test_*_no_nan_at_polar_latitudes` |
| 2. CLI lists 6 systems sorted | **Ce plan** | `test_lists_registered_systems` étendu |
| 3. Each system passes 10-charts oracle gate | Plans 15-01, 15-02, 15-03 + ce plan (régression finale) | `test_loaded_reference_snapshot_matches_oracle` (étendu Plan 15-01), `test_*_algorithm_matches_oracle_armc` |
| 4. CLI prints 12 cusps for `--system whole_sign` | **Ce plan** | `TestHousesCmdMatchesPythonAPI::test_cli_cusps_match_python_api` parametré 6 systèmes |

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-CONTEXT.md` — D-03 (CLI alphabétique), D-07 (parser dispatcher dynamique), D-08 (inverser test legacy).
2. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-RESEARCH.md` § 7 (CLI integration), § 8 (parser changes), § 11 Pitfall 7 (test legacy).
3. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-PATTERNS.md` § 10 (`_SYSTEM_DESCRIPTIONS` extend), § 11 (`test_introspection.py` extend), § 14.5 (Sophie hint : test ratchet `every-system-has-description`).
4. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-VALIDATION.md` lignes 52-55 (tasks 15-04-01..04 verification map).
5. `/home/loc/workspace/ketu/.planning/ROADMAP.md` lignes 108-126 — success criteria Phase 15 à clôturer.
6. `/home/loc/workspace/ketu/ketu/cli/parser.py` (intégral 150+ lignes) — particulièrement lignes 133-138 (`--system` actuel hardcodé) et l'import `SYSTEMS` à ajouter.
7. `/home/loc/workspace/ketu/ketu/cli/introspection.py` (intégral 58 lignes) — `_SYSTEM_DESCRIPTIONS` à étendre + `cmd_list_house_systems` qui consomme déjà `sorted(_HOUSE_SYSTEMS.keys())`.
8. `/home/loc/workspace/ketu/tests/cli/test_introspection.py` (intégral 44 lignes) — `TestListHouseSystems` à étendre (lignes 20-31).
9. `/home/loc/workspace/ketu/tests/cli/test_houses_cmd.py` lignes 1-90 — `TestHousesCmdMatchesPythonAPI` à parametrer sur 6 systèmes ; `test_invalid_system_rejected` à inverser.

## Files

**Modifie :**
- `/home/loc/workspace/ketu/ketu/cli/parser.py` — ligne 133-138 : `choices` dynamique via `sorted(SYSTEMS.keys())` ; ajouter import `from ketu.houses import SYSTEMS`.
- `/home/loc/workspace/ketu/ketu/cli/introspection.py` — étendre `_SYSTEM_DESCRIPTIONS` lignes 22-26 avec 3 entrées.
- `/home/loc/workspace/ketu/tests/cli/test_introspection.py` — étendre `TestListHouseSystems::test_lists_registered_systems` à 6 noms + ajouter `test_every_registered_system_has_description` (Sophie hint, PATTERNS §14.5).
- `/home/loc/workspace/ketu/tests/cli/test_houses_cmd.py` — inverser `test_invalid_system_rejected` (utiliser `nonexistent_xyz`) ; ajouter `test_new_systems_accepted` ; étendre `TestHousesCmdMatchesPythonAPI` à 6 systèmes.

**Lit (référence) :**
- `/home/loc/workspace/ketu/ketu/houses/__init__.py` — vérifier que les 6 systèmes sont enregistrés (Plans 15-02 et 15-03 mergés).
- `/home/loc/workspace/ketu/ketu/cli/__init__.py` — vérifier l'ordre d'import (le top-level `ketu.cli` doit avoir importé `ketu.houses` avant que le parser construise ses choices).

## Tasks

### Task 1 — Dispatcher dynamique dans `ketu/cli/parser.py`

Édite `/home/loc/workspace/ketu/ketu/cli/parser.py` :

**Ajouter l'import** (en haut du fichier, dans la zone des imports `from ketu...`) :
```python
from ketu.houses import SYSTEMS as _HOUSE_SYSTEMS
```
Le préfixe underscore signale "internal" et évite tout shadowing. Si l'import existe déjà sous un autre nom (e.g. `from ketu.houses import SYSTEMS`), le réutiliser sans dupliquer.

**Modifier ligne 133-138** (subcommand `houses --system`) :

**Avant** :
```python
    p_houses.add_argument(
        "--system",
        choices=["placidus", "koch", "porphyry"],
        default="placidus",
        help="House system (default: placidus).",
    )
```

**Après** :
```python
    p_houses.add_argument(
        "--system",
        choices=sorted(_HOUSE_SYSTEMS.keys()),
        default="placidus",
        help=(
            "House system (default: placidus). Available: "
            f"{', '.join(sorted(_HOUSE_SYSTEMS.keys()))}. "
            "Use --list-house-systems for descriptions."
        ),
    )
```

**Modifier également ligne 53-59** (top-level `--list-house-systems` help) :

**Avant** :
```python
    parser.add_argument(
        "--list-house-systems",
        action="store_true",
        help=(
            "List available house systems (placidus, koch, porphyry) and exit."
        ),
    )
```

**Après** :
```python
    parser.add_argument(
        "--list-house-systems",
        action="store_true",
        help="List all registered house systems and exit.",
    )
```

Pourquoi rendre le help statique générique plutôt que dynamique ici : la ligne 57 est lue par `argparse` au moment du `--help` global, avant tout dispatch. La forme "List all registered house systems and exit." reste vraie quel que soit le nombre de systèmes (6 en v1.2, 8 ou 10 en v1.3+). Pas de mensonge, pas de friction de maintenance.

**Notes :**
- `sorted(_HOUSE_SYSTEMS.keys())` est évalué **à la construction du parser**, pas à chaque appel CLI. Tant que `import ketu.cli` chaîne `import ketu.houses` (qui run les `@register` decorators), les 6 systèmes sont disponibles au moment où `make_parser()` est appelé.
- **Vérifier l'ordre d'import** : si `ketu/cli/__init__.py` importe `ketu.houses` avant `ketu.cli.parser`, c'est OK. Si l'import est lazy ailleurs, ajouter `import ketu.houses  # noqa: F401  ensure registry populated` au top de `parser.py` avant le `from ketu.houses import SYSTEMS`.
- **Help text** : la ligne 137 (`--system`) mentionne explicitement les systèmes disponibles ; la ligne 57 (`--list-house-systems`) reste générique pour éviter une dette de maintenance à chaque ajout futur. C'est UX-relevant pour `ketu houses --help` qui imprimera la liste.

**Done :**
```bash
# 1. ketu houses --help cite les 6 systèmes dans le --system help text
ketu houses --help 2>&1 | grep -E "system|placidus|whole_sign|regiomontanus"
# doit afficher la liste des 6 systèmes

# 2. ketu --help (top-level) ne mentionne plus la liste hardcodée v1.1
ketu --help 2>&1 | grep -- "--list-house-systems"
# doit afficher "List all registered house systems and exit."
# NE doit PAS afficher "(placidus, koch, porphyry)"
```

### Task 2 — Étendre `_SYSTEM_DESCRIPTIONS` dans `ketu/cli/introspection.py`

Édite `/home/loc/workspace/ketu/ketu/cli/introspection.py` lignes 22-26 :

**Avant** :
```python
_SYSTEM_DESCRIPTIONS = {
    "placidus": "Time-based; iterative trisection of the diurnal/nocturnal arcs (v1.1)",
    "koch": "Birthplace-based; closed-form trisection of the oblique-ascension arc (v1.1)",
    "porphyry": "Space-based; equal trisection of the ARMC quadrants — works at all latitudes (v1.1, also the polar fallback)",
}
```

**Après** :
```python
_SYSTEM_DESCRIPTIONS = {
    "placidus": "Time-based; iterative trisection of the diurnal/nocturnal arcs (v1.1)",
    "koch": "Birthplace-based; closed-form trisection of the oblique-ascension arc (v1.1)",
    "porphyry": "Space-based; equal trisection of the ARMC quadrants — works at all latitudes (v1.1, also the polar fallback)",
    "whole_sign": "Sign-based; cusp 1 = start of rising sign, then 30° spacing — oldest historical system; polar-safe (v1.2)",
    "equal": "Equal-house; cusp 1 = ASC, then 30° spacing — note cusp 10 ≠ astronomical MC; polar-safe (v1.2)",
    "regiomontanus": "Space-based; equal 30° divisions of celestial equator projected through prime vertical; NaN at polar (v1.2)",
}
```

**Conventions de style** (cohérence avec existant) :
- Format `<Catégorie>; <méthode courte> (<version>)`.
- `(v1.1)` ou `(v1.2)` à la fin pour distinguer les générations.
- Mentionner explicitement les divergences UX-relevantes : `cusp 10 ≠ astronomical MC` pour Equal (PATTERNS §14.2), `NaN at polar` pour Regiomontanus.

**Done :** `ketu --list-house-systems` affiche les 6 entrées avec descriptions sans `(no description available)` fallback.

### Task 3 — Étendre les tests introspection

Édite `/home/loc/workspace/ketu/tests/cli/test_introspection.py`. **Modifier** `TestListHouseSystems::test_lists_registered_systems` (lignes 21-26) :

**Avant** :
```python
    def test_lists_registered_systems(self, invoke_main, capsys):
        rc = invoke_main(["--list-house-systems"])
        assert rc == 0
        out = capsys.readouterr().out
        for name in ("placidus", "koch", "porphyry"):
            assert name in out
```

**Après** :
```python
    def test_lists_registered_systems(self, invoke_main, capsys):
        """HOU2-04: --list-house-systems retourne 6 systèmes triés alphabétiquement."""
        rc = invoke_main(["--list-house-systems"])
        assert rc == 0
        out = capsys.readouterr().out
        # All 6 v1.2 systems must appear.
        for name in ("placidus", "koch", "porphyry",
                     "whole_sign", "equal", "regiomontanus"):
            assert name in out, f"system {name!r} missing from --list-house-systems output"

    def test_systems_listed_in_alphabetical_order(self, invoke_main, capsys):
        """D-03 verrouillé: ordre alphabétique déterministe via sorted(SYSTEMS.keys())."""
        invoke_main(["--list-house-systems"])
        out = capsys.readouterr().out
        # Find the line index of each system name; they should be in
        # alphabetical order: equal, koch, placidus, porphyry, regiomontanus, whole_sign.
        expected_order = ["equal", "koch", "placidus", "porphyry", "regiomontanus", "whole_sign"]
        positions = []
        for name in expected_order:
            idx = out.find(f"  {name:")  # match the formatted line "  NAME       : ..."
            assert idx >= 0, f"system {name!r} not found in CLI output"
            positions.append(idx)
        # Verify monotonically increasing.
        assert positions == sorted(positions), (
            f"systems not in alphabetical order; positions: {positions}"
        )

    def test_every_registered_system_has_description(self, invoke_main, capsys):
        """Sophie hint (PATTERNS §14.5): no system shall fall through to the
        '(no description available)' default — _SYSTEM_DESCRIPTIONS must
        cover every entry in SYSTEMS."""
        invoke_main(["--list-house-systems"])
        out = capsys.readouterr().out
        assert "(no description available)" not in out, (
            "Some registered system lacks a _SYSTEM_DESCRIPTIONS entry; "
            "extend ketu/cli/introspection.py:_SYSTEM_DESCRIPTIONS."
        )
```

**Done :** `pytest tests/cli/test_introspection.py::TestListHouseSystems -x -v` passe (4 tests verts).

### Task 4 — Inverser le test legacy + ajouter test des nouveaux systèmes

Édite `/home/loc/workspace/ketu/tests/cli/test_houses_cmd.py`. **Modifier** `test_invalid_system_rejected` (lignes 52-59) :

**Avant** :
```python
    def test_invalid_system_rejected(self, invoke_main, capsys):
        with pytest.raises(SystemExit) as exc:
            invoke_main([
                "houses", "--date", "2000-01-01T12:00:00Z",
                "--lat", "48.85", "--lon", "2.35",
                "--system", "regiomontanus",
            ])
        assert exc.value.code == 2
```

**Après** :
```python
    def test_invalid_system_rejected(self, invoke_main, capsys):
        """argparse rejects unregistered system names with exit code 2.

        Pitfall 7 (15-RESEARCH §11): in v1.1 this test pinned ``regiomontanus``
        as invalid. Phase 15 makes it valid (HOU2-03) — the test now uses an
        impossible name to ratchet the rejection path without depending on
        a specific blacklist.
        """
        with pytest.raises(SystemExit) as exc:
            invoke_main([
                "houses", "--date", "2000-01-01T12:00:00Z",
                "--lat", "48.85", "--lon", "2.35",
                "--system", "nonexistent_xyz",
            ])
        assert exc.value.code == 2

    def test_v12_systems_accepted(self, invoke_main, capsys):
        """HOU2-01..03: v1.2 systems are accepted by the parser (no SystemExit)."""
        for system in ("whole_sign", "equal", "regiomontanus"):
            rc = invoke_main([
                "houses", "--date", "2000-01-01T12:00:00Z",
                "--lat", "48.85", "--lon", "2.35",
                "--system", system,
                "--polar-fallback", "porphyry",  # safe at all latitudes
            ])
            assert rc == 0, f"system {system!r} unexpectedly rejected"
```

**Étendre `TestHousesCmdMatchesPythonAPI::test_cli_cusps_match_python_api`** (lignes 20-41) — l'élargir aux 6 systèmes :

**Avant** :
```python
    @pytest.mark.parametrize("system", ["placidus", "koch", "porphyry"])
    @pytest.mark.parametrize("loc", [PARIS, SYDNEY, GREENWICH])
    def test_cli_cusps_match_python_api(self, invoke_main, capsys, system, loc):
        ...
```

**Après** :
```python
    @pytest.mark.parametrize("system", [
        "placidus", "koch", "porphyry",
        "whole_sign", "equal", "regiomontanus",
    ])
    @pytest.mark.parametrize("loc", [PARIS, SYDNEY, GREENWICH])
    def test_cli_cusps_match_python_api(self, invoke_main, capsys, system, loc):
        date_iso, lat, lon = loc
        rc = invoke_main([
            "houses",
            "--date", date_iso, "--lat", str(lat), "--lon", str(lon),
            "--system", system,
        ])
        assert rc == 0
        out = capsys.readouterr().out
        assert "House Cusps" in out
        # Cross-check: parse the printed degrees and compare to API.
        jd = parse_iso_utc(date_iso)
        rec = calculate_houses(jd=jd, lat=lat, lon=lon, system=system, polar_fallback="raise")
        cusps_api = np.asarray(rec["cusps"]).reshape(-1)
        printed = [float(m.group(1)) for m in re.finditer(r"\(\s*([\d.\-]+)°\)", out)]
        assert len(printed) == 14
        for i in range(12):
            assert printed[i] == pytest.approx(cusps_api[i], abs=1e-3)
```

**Note :** les 3 locations (Paris/Sydney/Greenwich) sont toutes non-polaires, donc `polar_fallback="raise"` (default) ne lève rien pour aucun système, y compris regiomontanus. **Total parametré** : 6 systèmes × 3 locations = 18 cas à valider.

**Done :** `pytest tests/cli/test_houses_cmd.py -x -v` passe (18 cas matchent l'API + 1 invalid system rejected + 1 v12 systems accepted + tests existants polar/missing/default).

### Task 5 — Doc gates et coverage gate Phase 13/15 finaux

Lance les gates Phase 13 (OPS-01, OPS-02) et Phase 15 (HOU2-05) :

```bash
cd /home/loc/workspace/ketu

# Doc gate OPS-01 : interrogate ≥95% sur ketu/houses (incluant les 3 nouveaux modules)
python -m interrogate ketu/houses/ -f 95

# Doc gate OPS-02 : numpydoc validate clean sur tous les fichiers ketu/houses
python -m numpydoc lint ketu/houses/__init__.py ketu/houses/core.py \
    ketu/houses/api.py ketu/houses/registry.py ketu/houses/_ecliptic.py \
    ketu/houses/ascmc.py ketu/houses/placidus.py ketu/houses/koch.py \
    ketu/houses/porphyry.py ketu/houses/whole_sign.py ketu/houses/equal.py \
    ketu/houses/regiomontanus.py

# Coverage gate v1.2 : ≥95% sur ketu/houses dans son ensemble
pytest tests/houses/ tests/cli/ --cov=ketu.houses --cov-report=term-missing --cov-fail-under=95
```

**Si une de ces gates échoue :**
- `interrogate < 95%` : un docstring manque sur une fonction publique récente — réviser whole_sign/equal/regiomontanus pour exhaustivité (les modules ont déjà des docstrings dans Plans 15-02/03 ; la cause typique est un test/fixture non documenté).
- `numpydoc lint` : section manquante ou mal formée — corriger en suivant le pattern de `koch.py` / `porphyry.py`.
- `coverage < 95%` : une branche non couverte par les tests des Plans 15-02/03 (typiquement la branche polar mask de `regiomontanus.py` ou la branche polar swap de `whole_sign.py`/`equal.py`). Ajouter un test ciblé dans le fichier test approprié.

**Done :** les 3 commandes retournent exit 0.

### Task 6 — Régression complète, multi-vagues

```bash
pytest tests/ -x --no-cov
mypy --strict ketu/houses/ ketu/cli/
```

**Doit rester :** 100 % vert ; aucune régression sur les Plans antérieurs (Phase 10 + Phase 13 + Phase 14 + Plans 15-01/02/03).

**Done :** sortie pytest et mypy verte.

### Task 7 — Vérification finale des Phase 15 success criteria

Vérifier explicitement les 4 success criteria de ROADMAP.md lignes 119-126 :

```bash
# Success criterion 1: each system returns valid HOUSES_DTYPE; W/E polar-safe
python -c "
from ketu.houses import calculate_houses
import numpy as np
for system in ('whole_sign', 'equal', 'regiomontanus'):
    pf = 'porphyry' if system == 'regiomontanus' else 'raise'
    r = calculate_houses(2451545.0, 48.85, 2.35, system=system, polar_fallback=pf)
    assert r['cusps'].shape == (12,), f'{system}: bad cusps shape {r[\"cusps\"].shape}'
    assert not np.isnan(r['cusps']).any(), f'{system}: cusps contain NaN at non-polar'
    assert r['system'] == system, f'{system}: out[\"system\"] mismatch {r[\"system\"]!r}'
# W/E polar-safe at lat=80°
for system in ('whole_sign', 'equal'):
    r = calculate_houses(2451545.0, 80.0, 0.0, system=system)  # default raise
    assert not np.isnan(r['cusps']).any(), f'{system}: NaN at lat=80° (should be polar-safe)'
print('Success criterion 1: PASS')
"

# Success criterion 2: CLI lists exactly 6 systems
ketu --list-house-systems > /tmp/list.txt
for s in placidus koch porphyry whole_sign equal regiomontanus; do
    grep -q "$s" /tmp/list.txt || { echo "Missing $s"; exit 1; }
done
echo 'Success criterion 2: PASS'

# Success criterion 3: 10-charts oracle gate per system
pytest tests/houses/test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle -x
echo 'Success criterion 3: PASS'

# Success criterion 4: CLI prints 12 cusps for whole_sign
ketu houses --date 2025-06-21T12:00:00Z --lat 48.85 --lon 2.35 --system whole_sign > /tmp/cli.txt
grep -c "°" /tmp/cli.txt | (read n; [ "$n" -ge 12 ] || { echo "Expected ≥12 degrees in output, got $n"; exit 1; })
echo 'Success criterion 4: PASS'
```

**Done :** les 4 messages `PASS` apparaissent.

## Verification gates

```bash
cd /home/loc/workspace/ketu

# 1. Plans 15-02 et 15-03 doivent être merged (6 systèmes enregistrés)
python -c "from ketu.houses import SYSTEMS; assert len(SYSTEMS) == 6, len(SYSTEMS); print('OK')"

# 2. Parser dispatcher dynamique
python -c "
from ketu.cli.parser import make_parser
p = make_parser()
sub = next(a for a in p._actions if hasattr(a, 'choices') and a.choices)  # find subparsers
houses = sub.choices['houses']
sys_action = next(a for a in houses._actions if a.dest == 'system')
assert sorted(sys_action.choices) == ['equal', 'koch', 'placidus', 'porphyry', 'regiomontanus', 'whole_sign'], sys_action.choices
print('OK dynamic choices:', sorted(sys_action.choices))
"

# 3. CLI listing alphabétique
ketu --list-house-systems

# 4. test_invalid_system_rejected inversé
pytest tests/cli/test_houses_cmd.py::TestHousesCmdFlags::test_invalid_system_rejected -x -v
pytest tests/cli/test_houses_cmd.py::TestHousesCmdFlags::test_v12_systems_accepted -x -v

# 5. CLI cusps match API for 6 systems × 3 locations = 18 cases
pytest "tests/cli/test_houses_cmd.py::TestHousesCmdMatchesPythonAPI::test_cli_cusps_match_python_api" -x -v

# 6. Introspection tests
pytest tests/cli/test_introspection.py::TestListHouseSystems -x -v

# 7. Sophie ratchet : every-system-has-description
pytest tests/cli/test_introspection.py::TestListHouseSystems::test_every_registered_system_has_description -x -v

# 8. Doc gates Phase 13 verts
python -m numpydoc lint ketu/houses/whole_sign.py ketu/houses/equal.py ketu/houses/regiomontanus.py
python -m interrogate ketu/houses/ -f 95

# 9. Coverage ≥95% project-wide sur ketu/houses
pytest tests/ --cov=ketu.houses --cov-report=term-missing --cov-fail-under=95

# 10. mypy --strict clean (cli + houses)
python -m mypy --strict ketu/cli/parser.py ketu/cli/introspection.py ketu/houses/

# 11. Régression complète project-wide
pytest tests/ -x --no-cov

# 12. Phase 15 success criteria — runnable check
ketu houses --date 2025-06-21T12:00:00Z --lat 48.85 --lon 2.35 --system whole_sign
ketu houses --date 2025-06-21T12:00:00Z --lat 48.85 --lon 2.35 --system equal
ketu houses --date 2025-06-21T12:00:00Z --lat 48.85 --lon 2.35 --system regiomontanus
# Les 3 commandes doivent imprimer 12 cusps formatés sans erreur
```

## Done criteria

- [ ] HOU2-04 satisfait : `ketu --list-house-systems` retourne `equal, koch, placidus, porphyry, regiomontanus, whole_sign` (6 noms triés alphabétiquement, déterministe).
- [ ] HOU2-05 satisfait : `test_loaded_reference_snapshot_matches_oracle` itère 6 systèmes × 10 charts à 1e-9° (Plan 15-01) ; algorithm-tier oracle bit-exact à 1e-6° pour whole_sign/equal/regiomontanus (Plans 15-02/03) ; max ASC delta documenté dans le snapshot script + commentaires de test.
- [ ] Parser dispatcher dynamique : `choices=sorted(SYSTEMS.keys())` ; le parser apprend automatiquement les nouveaux systèmes au moment de `import ketu.cli`. Plus aucune liste hardcodée.
- [ ] `_SYSTEM_DESCRIPTIONS` couvre les 6 systèmes ; `(no description available)` n'apparaît plus dans le CLI output (test ratchet `test_every_registered_system_has_description`).
- [ ] `test_invalid_system_rejected` inversé : utilise `nonexistent_xyz` (pas `regiomontanus`) ; un nouveau test `test_v12_systems_accepted` ratchet whole_sign/equal/regiomontanus comme inputs valides.
- [ ] `TestHousesCmdMatchesPythonAPI::test_cli_cusps_match_python_api` parametré sur 6 systèmes × 3 locations = 18 cas verts.
- [ ] **Phase 15 success criterion 4** explicitement vérifié : `ketu houses --date 2025-06-21T12:00:00Z --lat 48.85 --lon 2.35 --system whole_sign` imprime 12 cusps sans erreur (idem equal et regiomontanus).
- [ ] Doc gates Phase 13 verts sur `ketu/houses/` complet (12 fichiers : 9 v1.1 + 3 v1.2 + `_ecliptic.py` + `core.py`) : `numpydoc validate` clean, `interrogate ≥95%`.
- [ ] Coverage Phase 15 verte : `ketu/houses` ≥95% (gate strict v1.2 pour les 3 nouveaux modules), project ≥90%.
- [ ] mypy `--strict` clean sur `ketu/cli/parser.py`, `ketu/cli/introspection.py`, et `ketu/houses/` complet.
- [ ] Régression project-wide : `pytest tests/ -x` reste 100 % vert (Phases 10, 13, 14 et Plans 15-01/02/03 intacts).

## Atomic commit message

```
feat(15-04): wire 3 v1.2 systems into CLI; close Phase 15 success criteria

CLI integration (HOU2-04, D-03/D-07):
- ketu/cli/parser.py: --system choices now dynamic via
  sorted(SYSTEMS.keys()); future v1.3 systems auto-extend without
  parser changes. Help text lists available systems explicitly.
- ketu/cli/introspection.py: _SYSTEM_DESCRIPTIONS extended with 3 v1.2
  entries (whole_sign, equal, regiomontanus); cmd_list_house_systems
  iterates sorted(_HOUSE_SYSTEMS.keys()) — listing emerges
  automatically from registry.

Test legacy inversion (D-08, Pitfall 7):
- tests/cli/test_houses_cmd.py:test_invalid_system_rejected now uses
  'nonexistent_xyz' (regiomontanus is valid as of Plan 15-03).
- New test_v12_systems_accepted ratchets whole_sign/equal/regiomontanus
  as valid CLI choices.
- TestHousesCmdMatchesPythonAPI parametrised on 6 systems × 3
  locations = 18 cases (was 9). All match the Python API to 1e-3°.

Introspection ratchets:
- TestListHouseSystems::test_lists_registered_systems extended to all
  6 names.
- New test_systems_listed_in_alphabetical_order ratchets D-03.
- New test_every_registered_system_has_description (Sophie hint, PATTERNS
  §14.5) ratchets that no system falls through to '(no description
  available)'.

Phase 15 success criteria closed:
1. (Plans 15-02/03) systems return valid HOUSES_DTYPE; W/E polar-safe.
2. (this plan) CLI lists 6 systems alphabetically.
3. (Plan 15-01 + 02/03) 10-charts oracle gate green for each system.
4. (this plan) `ketu houses --system whole_sign|equal|regiomontanus`
   prints 12 cusps without error.

Doc gates (OPS-01, OPS-02) and coverage gate (≥95%) green on the full
ketu/houses/ subpackage including 3 new v1.2 modules.

Covers: HOU2-04 (CLI listing), HOU2-05 (validation finale)
Refs: D-03 (alphabetical CLI), D-07 (dynamic dispatcher), D-08 (test
      legacy inversion), Pitfall 7 from 15-RESEARCH §11.
Closes Phase 15.
```

## Rollback note

`git revert <commit_sha>` annule complètement ce plan : retour à `choices=["placidus", "koch", "porphyry"]` hardcodé dans `parser.py` ; `_SYSTEM_DESCRIPTIONS` à 3 entrées ; `test_invalid_system_rejected` revient à asserter `regiomontanus` rejeté ; les 3 tests CLI ajoutés disparaissent ; les 18 cases de `test_cli_cusps_match_python_api` redeviennent 9. Plans 15-02 et 15-03 (qui sont indépendants du CLI) restent intacts — `calculate_houses(system="whole_sign")` continue de fonctionner via l'API Python, mais le CLI rejette les 3 nouveaux systèmes (régression UX visible mais pas de perte de fonctionnalité Python).

**Cas particulier** : si on revert ce plan APRÈS avoir mergé un futur Plan v1.3 qui ajoute Campanus et qui dépend du dispatcher dynamique, le revert casserait Campanus aussi. À ce moment, ne pas faire un revert simple — réintroduire manuellement le dispatcher dynamique dans `parser.py` et garder uniquement la suppression des 3 entrées de descriptions et du test legacy.
