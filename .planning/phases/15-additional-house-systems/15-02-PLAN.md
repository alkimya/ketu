---
phase: 15-additional-house-systems
plan: 02
plan_name: whole-sign-and-equal
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - ketu/houses/whole_sign.py
  - ketu/houses/equal.py
  - ketu/houses/__init__.py
  - ketu/houses/api.py
  - tests/houses/test_whole_sign.py
  - tests/houses/test_equal.py
  - tests/houses/test_polar_safety.py
autonomous: true
requirements:
  - HOU2-01
  - HOU2-02
parallelizable_with: [15-03]

must_haves:
  truths:
    - "`calculate_houses(jd, lat, lon, system='whole_sign')` retourne un `HOUSES_DTYPE` valide avec cusps[0] = début du signe contenant l'ASC (PAS l'ASC lui-même)"
    - "`calculate_houses(jd, lat, lon, system='equal')` retourne un `HOUSES_DTYPE` valide avec cusps[0] = ASC et cusps espacés régulièrement de 30°"
    - "Les deux systèmes sont polar-safe : aucun NaN à lat=70°, 80°, 89° (par construction, pas via fallback)"
    - "Les algorithmes matchent `swe_oracle_armc(armc, lat, eps, system)` à 1e-6° près sur les 10 reference charts non polaires"
    - "Le snapshot end-to-end (`test_loaded_reference_snapshot_matches_oracle` Plan 15-01) reste vert (incluant whole_sign et equal)"
    - "Equal cusps[9] = (asc + 270) mod 360 ≠ MC astronomique réel ; cette divergence est testée et documentée"
    - "`SYSTEMS` registry contient 'whole_sign' et 'equal' au moment de `import ketu.houses`"
    - "La liste polar-safe dans `api.py:126` inclut désormais `'whole_sign'` et `'equal'` (le polar gate ne s'applique pas)"
  artifacts:
    - path: "ketu/houses/whole_sign.py"
      provides: "@register('whole_sign') def whole_sign_cusps(armc, lat, eps) -> ndarray[..., 12]"
      contains: '@register("whole_sign")'
    - path: "ketu/houses/equal.py"
      provides: "@register('equal') def equal_cusps(armc, lat, eps) -> ndarray[..., 12]"
      contains: '@register("equal")'
    - path: "tests/houses/test_whole_sign.py"
      provides: "Algorithm-tier oracle (1e-6°) + invariants + sign-floor test + registry test + ASC=0° boundary test"
      min_lines: 150
    - path: "tests/houses/test_equal.py"
      provides: "Algorithm-tier oracle (1e-6°) + 30°-spacing invariant + cusp10≠MC test + registry test"
      min_lines: 130
  key_links:
    - from: "ketu/houses/whole_sign.py"
      to: "ketu.houses.registry.SYSTEMS"
      via: "@register decorator at module load"
      pattern: '@register\\("whole_sign"\\)'
    - from: "ketu/houses/equal.py"
      to: "ketu.houses.registry.SYSTEMS"
      via: "@register decorator at module load"
      pattern: '@register\\("equal"\\)'
    - from: "ketu/houses/__init__.py"
      to: "submodules whole_sign + equal"
      via: "from . import whole_sign / equal trigger imports (Pitfall 2)"
      pattern: "from \\. import whole_sign"
    - from: "ketu/houses/api.py:126"
      to: "polar-safe set"
      via: "any_polar = ... and system_lower not in {polar-safe systems}"
      pattern: '"whole_sign"'
---

# Plan 15-02 — Whole Sign + Equal house systems

## Goal

Implémenter les deux house systems closed-form polar-safe **les plus simples** de Phase 15 : Whole Sign (HOU2-01) et Equal (HOU2-02). Les deux partagent profil mathématique et tests (closed-form arithmétique pure, jamais NaN, invariant 30°-spacing) — d'où le regroupement dans un seul plan, conforme à la recommandation §10 du RESEARCH.md.

**Whole Sign** : `cusp[0] = floor(asc/30)*30`, puis `cusp[k+1] = (cusp[k] + 30) mod 360`. La maison 1 démarre au **début du signe** contenant l'ASC, pas à l'ASC lui-même — divergence avec Placidus/Koch/Porphyry à documenter dans le docstring et à ratchet par un test dédié (PATTERNS §1, §14.1).

**Equal** : `cusp[k] = (asc + 30*k) mod 360`. La maison 1 démarre **à l'ASC**, et la maison 10 (`cusp[9]`) = `(asc + 270) mod 360` — qui n'est **pas** le MC astronomique réel. Cette divergence est intentionnelle (HOU2-02 = ASC-anchored Equal, **pas** MC-anchored Equal `'D'` swisseph) et doit être testée pour ratchet la convention (PATTERNS §14.2).

Les deux modules sont implémentés en parallèle dans le même plan parce que :
- Code production trivial (~50 lignes chacun, pattern Porphyry copié sans complexification).
- Tests partagent l'invariant 30°-spacing.
- Aucun risque de conflit (deux fichiers distincts ; même append au registry ; même append à `__init__.py`).

Le polar gate `api.py:126` est étendu à `{"porphyry", "whole_sign", "equal"}` pour autoriser `calculate_houses(jd, 80, 0, system="whole_sign")` à passer **directement** sans `polar_fallback="porphyry"` — ces systèmes n'ont aucune dépendance latitude-singulière.

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-CONTEXT.md` — D-04 (un fichier par système), D-09 (helper internal naming non-breaking).
2. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-RESEARCH.md` § 3 (formules canoniques Whole Sign + Equal), § 6 (polar safety), § 11 Pitfalls 1, 2, 3.
3. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-PATTERNS.md` § 1 (Whole Sign analogue Porphyry, **divergence cusp[0]**), § 2 (Equal analogue Porphyry, **divergence cusp[9]**), § 4 (`__init__.py` trigger imports), § 7 (test_whole_sign), § 8 (test_equal), § 12.2 (cusps ordering canonique), § 12.6 (tolérances oracle 1e-6°).
4. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-VALIDATION.md` lignes 43-47 (tasks 15-02-01..05 verification map).
5. `/home/loc/workspace/ketu/ketu/houses/porphyry.py` (intégral, 194 lignes) — gabarit littéral (closed-form, polar-safe, broadcast, stack ordre canonique).
6. `/home/loc/workspace/ketu/ketu/houses/registry.py` (intégral, 108 lignes) — `@register` mécanisme, `SYSTEMS` dict.
7. `/home/loc/workspace/ketu/ketu/houses/__init__.py` (intégral, 53 lignes) — pattern lignes 41-43 à étendre.
8. `/home/loc/workspace/ketu/ketu/houses/api.py` lignes 118-126 (polar gate à étendre ligne 126 où `system_lower != "porphyry"` devient `system_lower not in {"porphyry","whole_sign","equal"}`).
9. `/home/loc/workspace/ketu/tests/houses/test_porphyry.py` (intégral, 186 lignes) — gabarit `test_whole_sign.py` et `test_equal.py`.
10. `/home/loc/workspace/ketu/tests/houses/test_polar_safety.py` (intégral, 158 lignes) — où ajouter les assertions « no NaN » pour whole_sign/equal.
11. `/home/loc/workspace/ketu/tests/houses/conftest.py:swe_oracle_armc` (lignes 150-188) — utilisé par les tests algorithm-tier.

## Files

**Crée :**
- `/home/loc/workspace/ketu/ketu/houses/whole_sign.py`
- `/home/loc/workspace/ketu/ketu/houses/equal.py`
- `/home/loc/workspace/ketu/tests/houses/test_whole_sign.py`
- `/home/loc/workspace/ketu/tests/houses/test_equal.py`

**Modifie :**
- `/home/loc/workspace/ketu/ketu/houses/__init__.py` — ajouter 2 lignes `from . import whole_sign` et `from . import equal` (après ligne 43).
- `/home/loc/workspace/ketu/ketu/houses/api.py` — ligne 126 : étendre la liste polar-safe.
- `/home/loc/workspace/ketu/tests/houses/test_polar_safety.py` — ajouter 2 tests `test_whole_sign_does_not_yield_nan_above_polar_circle` et `test_equal_does_not_yield_nan_above_polar_circle`.

**Lit (référence) :**
- `/home/loc/workspace/ketu/ketu/houses/porphyry.py` (gabarit closed-form polar-safe).
- `/home/loc/workspace/ketu/tests/houses/test_porphyry.py` (gabarit tests).
- `/home/loc/workspace/ketu/ketu/houses/ascmc.py` (lignes 1-50 — `compute_ascmc` returns dict avec `armc`, `eps`, `asc`, `mc`, `vertex`).

## Tasks

### Task 1 — Créer `ketu/houses/whole_sign.py`

Crée `/home/loc/workspace/ketu/ketu/houses/whole_sign.py` en mirorant `ketu/houses/porphyry.py` :

```python
"""Whole Sign house system implementation.

In Whole Sign, each of the 12 houses corresponds to a complete zodiacal
sign of 30 degrees. House 1 begins at the **start of the sign containing
the Ascendant** — NOT at the Ascendant itself. This is the oldest
historical Western system (Hellenistic and Vedic traditions).

Per swisseph C source (``swehouse.c`` case ``'W'``):

    cusp_1 = floor(asc / 30) * 30        # start of rising sign
    cusp_k = (cusp_1 + 30 * (k - 1)) mod 360   # k = 2..12

The function is mathematically defined at all latitudes (it depends only
on the Ascendant, which is closed-form via :func:`compute_ascmc` for
``|lat| < 90°``). Polar-safe by construction — no NaN propagation, no
``HighLatitudeError`` path.

Caveat — ``cusps[0]`` divergence
--------------------------------
For Placidus / Koch / Porphyry / Equal / Regiomontanus, ``cusps[0] == asc``.
For Whole Sign, ``cusps[0]`` is the start of the sign (Aries 0° if asc in
[0°, 30°), Taurus 0° if asc in [30°, 60°), etc.). The actual Ascendant is
preserved in ``out["asc"]`` (set by :func:`calculate_houses` via
:func:`compute_ascmc`). Callers needing the rising longitude must read
``out["asc"]``, NOT ``out["cusps"][0]``.

This divergence is verified by ``tests/houses/test_whole_sign.py``
(``test_whole_sign_cusp_1_is_start_of_rising_sign``).
"""
from __future__ import annotations

import numpy as np

from .registry import register


@register("whole_sign")
def whole_sign_cusps(
    armc: np.ndarray,
    lat: np.ndarray,
    eps: np.ndarray,
) -> np.ndarray:
    """Compute the 12 Whole Sign house cusps.

    Closed-form per swisseph ``swehouse.c`` case ``'W'``. Polar-safe by
    construction (no latitude-dependent singularity).

    Parameters
    ----------
    armc : np.ndarray
        Right Ascension of Medium Coeli (degrees), broadcast-compatible
        with ``lat`` and ``eps``.
    lat : np.ndarray
        Geographic latitude (degrees). Used only to compute the ASC; the
        sign-floor itself does not depend on latitude.
    eps : np.ndarray
        Mean obliquity of the ecliptic (degrees). Used only to compute
        the ASC and MC.

    Returns
    -------
    np.ndarray
        Array of shape ``(..., 12)`` with cusp ordering
        ``[cusp_1, cusp_2, ..., cusp_12]`` where ``cusp_1`` is the start
        of the sign containing the ASC (NOT the ASC itself; see module
        docstring). Indices 3, 6, 9 hold cusps 4 (IC equivalent), 7
        (DESC equivalent), and 10 (MC equivalent) by sign-floor symmetry,
        which generally diverge from the astronomical IC/DESC/MC.

    Notes
    -----
    The standard cusp ordering convention used elsewhere in
    ``ketu.houses`` (``[asc, c2, c3, ic, c5, c6, desc, c8, c9, mc, c11,
    c12]``) is preserved by index but loses its "asc/ic/desc/mc"
    interpretation: for Whole Sign these slots hold the sign-floor
    cusps, not the astronomical angles. Use ``out["asc"]``, ``out["mc"]``
    for the true ASC/MC.
    """
    armc_b, lat_b, eps_b = np.broadcast_arrays(armc, lat, eps)
    armc_rad = np.deg2rad(armc_b)
    eps_rad = np.deg2rad(eps_b)
    lat_rad = np.deg2rad(lat_b)

    # ASC closed-form (mirror porphyry.py:147-151). We need the ASC to
    # determine the rising sign; we discard MC since Whole Sign cusps
    # 4/7/10 are sign-floor opposites, not the astronomical IC/DESC/MC.
    asc = np.rad2deg(np.arctan2(
        np.cos(armc_rad),
        -(np.sin(eps_rad) * np.tan(lat_rad)
          + np.cos(eps_rad) * np.sin(armc_rad)),
    )) % 360.0

    # Polar ASC swap (mirror porphyry.py:153-162) — at high latitudes the
    # closed-form ASC may emerge in the antipodal quadrant; swisseph case
    # 'W' applies the same swap as case 'O' (Porphyry) before the
    # sign-floor. Pitfall 1 from 15-RESEARCH §11: do the swap BEFORE the
    # floor, not after.
    mc = np.rad2deg(np.arctan2(
        np.sin(armc_rad),
        np.cos(armc_rad) * np.cos(eps_rad),
    )) % 360.0
    acmc_signed = ((asc - mc + 540.0) % 360.0) - 180.0
    swap_mask = acmc_signed < 0.0
    asc = np.where(swap_mask, (asc + 180.0) % 360.0, asc)

    # Sign-floor: cusp_1 = start of the 30° sign containing ASC.
    cusp_1 = np.floor(asc / 30.0) * 30.0

    # Cusps 2..12: each 30° east of the previous one.
    # Build (..., 12) by adding [0, 30, 60, ..., 330] to cusp_1.
    offsets = np.arange(12, dtype=np.float64) * 30.0
    cusps = (cusp_1[..., np.newaxis] + offsets) % 360.0

    result: np.ndarray = cusps
    return result
```

**Notes pour l'implémentateur :**
- **Garde le ratio docstring/code élevé** — le module fait ~50 lignes de code mais le docstring explique POURQUOI cusps[0] diverge ; c'est crucial pour interrogate ≥95% et pour la lisibilité.
- **NE PAS** importer `polar_circle`/`is_polar`/`POLAR_EPS_TOL` depuis porphyry — Whole Sign n'en a pas besoin (PATTERNS §1 Skip).
- **NE PAS** ajouter de masque NaN polaire — Pitfall 1 garde-fou : la formule est définie partout (`np.tan(lat_rad)` à `lat=89°` est ~57.3, fini), seul `lat=±90°` exact diverge mais c'est exclu par convention.
- **Pitfall 1 (15-RESEARCH §11) :** ordre ASC swap → THEN sign-floor. Le code ci-dessus respecte cet ordre.

**Done :** `python -c "from ketu.houses import whole_sign_cusps; import numpy as np; r = whole_sign_cusps(np.float64(45.0), np.float64(48.86), np.float64(23.4393)); print(r.shape, r[0])"` retourne `(12,) <multiple of 30°>`.

### Task 2 — Créer `ketu/houses/equal.py`

Crée `/home/loc/workspace/ketu/ketu/houses/equal.py` en mirorant `whole_sign.py` mais avec la formule Equal :

```python
"""Equal house system implementation (ASC-anchored).

In Equal house, each cusp is exactly 30° east of the previous, starting
from the Ascendant. This is the simplest possible house system — used as
a teaching system and occasionally for natal charts where the user wants
sign-aligned spacing without the sign-floor.

Per swisseph C source (``swehouse.c`` case ``'E'``):

    cusp_k = (asc + 30 * (k - 1)) mod 360   # k = 1..12

The function is mathematically defined at all latitudes (it depends only
on the Ascendant). Polar-safe by construction — no NaN propagation.

Caveat — ``cusps[9]`` is NOT the astronomical MC
-------------------------------------------------
For Placidus / Koch / Porphyry / Regiomontanus, ``cusps[9] == mc`` (the
astronomical Medium Coeli, computed via :func:`compute_ascmc`). For
Equal house, ``cusps[9] = (asc + 270) mod 360``, which only coincides
with the astronomical MC at the equator (where ASC and MC are
exactly 90° apart). At any other latitude, ``cusps[9]`` and ``out["mc"]``
diverge by up to ~10° (mid-latitudes) or more (near polar). This is
intentional — HOU2-02 specifies ASC-anchored Equal, not MC-anchored
Equal (swisseph case ``'D'``, deferred to v1.3).

Callers needing the astronomical MC must read ``out["mc"]``, NOT
``out["cusps"][9]``. This divergence is verified by
``tests/houses/test_equal.py``
(``test_equal_cusp_10_is_asc_plus_270_not_astronomical_mc``).
"""
from __future__ import annotations

import numpy as np

from .registry import register


@register("equal")
def equal_cusps(
    armc: np.ndarray,
    lat: np.ndarray,
    eps: np.ndarray,
) -> np.ndarray:
    """Compute the 12 Equal house cusps (ASC-anchored).

    Closed-form per swisseph ``swehouse.c`` case ``'E'``. Polar-safe.

    Parameters
    ----------
    armc : np.ndarray
        Right Ascension of Medium Coeli (degrees), broadcast-compatible
        with ``lat`` and ``eps``.
    lat : np.ndarray
        Geographic latitude (degrees). Used only to compute the ASC.
    eps : np.ndarray
        Mean obliquity of the ecliptic (degrees). Used only to compute
        the ASC.

    Returns
    -------
    np.ndarray
        Array of shape ``(..., 12)``: ``cusps[k] = (asc + 30k) mod 360``.
        ``cusps[0] == asc`` (consistent with Placidus/Koch/Porphyry);
        ``cusps[9]`` is ``(asc + 270) mod 360``, which generally diverges
        from the astronomical MC stored in ``out["mc"]`` (see module
        docstring).
    """
    armc_b, lat_b, eps_b = np.broadcast_arrays(armc, lat, eps)
    armc_rad = np.deg2rad(armc_b)
    eps_rad = np.deg2rad(eps_b)
    lat_rad = np.deg2rad(lat_b)

    # ASC closed-form (mirror porphyry.py:147-151).
    asc = np.rad2deg(np.arctan2(
        np.cos(armc_rad),
        -(np.sin(eps_rad) * np.tan(lat_rad)
          + np.cos(eps_rad) * np.sin(armc_rad)),
    )) % 360.0

    # Polar ASC swap (mirror porphyry.py:153-162).
    mc = np.rad2deg(np.arctan2(
        np.sin(armc_rad),
        np.cos(armc_rad) * np.cos(eps_rad),
    )) % 360.0
    acmc_signed = ((asc - mc + 540.0) % 360.0) - 180.0
    swap_mask = acmc_signed < 0.0
    asc = np.where(swap_mask, (asc + 180.0) % 360.0, asc)

    # Equal cusps: each 30° east of the ASC.
    offsets = np.arange(12, dtype=np.float64) * 30.0
    cusps = (asc[..., np.newaxis] + offsets) % 360.0

    result: np.ndarray = cusps
    return result
```

**Note :** Le calcul de `mc` ici sert uniquement au swap polaire (pour vérifier `acmc_signed`), pas au stack final. Equal cusps[9] sera `(asc + 270) % 360`, calculé naturellement par le `offsets` array.

**Done :** `python -c "from ketu.houses import equal_cusps; import numpy as np; r = equal_cusps(np.float64(45.0), np.float64(48.86), np.float64(23.4393)); diffs = np.diff(r) % 360; print(diffs)"` retourne un array de 11 valeurs toutes égales à 30.0 (à epsilon près).

### Task 3 — Étendre `ketu/houses/__init__.py` (trigger imports)

Édite `/home/loc/workspace/ketu/ketu/houses/__init__.py`. **Append** après ligne 43 (`from . import porphyry`) :

```python
from . import whole_sign  # noqa: F401  registers 'whole_sign' in SYSTEMS
from . import equal       # noqa: F401  registers 'equal' in SYSTEMS
```

**Pitfall 2 garde-fou (15-RESEARCH §11) :** sans ces 2 lignes, le `@register` decorator n'est jamais exécuté ; `calculate_houses(system="whole_sign")` lèverait `ValueError("unknown house system 'whole_sign'")`.

**Note merge avec Plan 15-03 :** ce plan ajoute 2 lignes ; le Plan 15-03 ajoutera 1 ligne supplémentaire `from . import regiomontanus`. Les 3 lignes sont append-only et disjointes ; git merge sera trivial. Si Plan 15-02 est merge avant 15-03, l'`__init__.py` aura 5 lignes de trigger imports (placidus, koch, porphyry, whole_sign, equal). Plan 15-03 ajoutera la 6e à la fin.

**Done :** `python -c "from ketu.houses import SYSTEMS; assert 'whole_sign' in SYSTEMS and 'equal' in SYSTEMS; print(sorted(SYSTEMS))"` affiche `['equal', 'koch', 'placidus', 'porphyry', 'whole_sign']` (5 entrées tant que 15-03 n'a pas mergé).

### Task 4 — Étendre la liste polar-safe dans `ketu/houses/api.py`

Édite `/home/loc/workspace/ketu/ketu/houses/api.py` ligne 126 :

**Avant** :
```python
    any_polar = bool(polar_mask.any()) and system_lower != "porphyry"
```

**Après** :
```python
    # Polar-safe systems do NOT trigger the polar gate — they are
    # mathematically defined at all latitudes. Phase 15: whole_sign
    # and equal join porphyry in this set (HOU2-01, HOU2-02).
    POLAR_SAFE_SYSTEMS = frozenset({"porphyry", "whole_sign", "equal"})
    any_polar = bool(polar_mask.any()) and system_lower not in POLAR_SAFE_SYSTEMS
```

**Note :** placer la définition de `POLAR_SAFE_SYSTEMS` au top du module (juste après les imports) est plus propre stylistiquement. Si le linter (mypy) impose l'extraction au top-level, faire `POLAR_SAFE_SYSTEMS: frozenset[str] = frozenset({"porphyry", "whole_sign", "equal"})` ligne ~25 (juste après `ArrayLike = Union[float, np.ndarray]`).

**Justifie dans le commentaire pourquoi `equal` est polar-safe :** Equal n'a aucune dépendance latitudinale dans le sign-floor (`asc + 30k` est définie partout où l'ASC l'est). Idem pour Whole Sign (sign-floor d'un ASC fini est toujours fini).

**Done :** `pytest tests/houses/test_integration.py -x` reste vert (pas de régression sur Placidus/Koch — le polar gate s'applique toujours pour eux).

### Task 5 — Créer `tests/houses/test_whole_sign.py`

Crée `/home/loc/workspace/ketu/tests/houses/test_whole_sign.py` en mirorant `tests/houses/test_porphyry.py` :

```python
"""Whole Sign tests — closed-form, polar-safe, sign-floor invariants.

Two-tier oracle strategy (mirrors v1.1 Phase 10 pattern):

- **Algorithm tier** (``test_algorithm_matches_oracle_armc``): bit-exact
  comparison vs ``swe.houses_armc``, tolerance ``1e-6°``. Whole Sign is
  pure arithmetic (floor + modulo + addition), so machine precision is
  achievable on all 10 reference charts including polar latitudes.
- **End-to-end tier**: covered automatically by Plan 15-01's
  ``test_loaded_reference_snapshot_matches_oracle`` once it iterates
  whole_sign.

Plus invariants specific to Whole Sign:

- 30° spacing between adjacent cusps (modulo 360).
- ``cusps[0]`` is the start of the rising sign, NOT the ASC.
- ASC = 0° boundary case (Pitfall 3 from 15-RESEARCH §11).
- Registry registration.
- Vectorisation matches scalar element-wise.
"""
from __future__ import annotations

from typing import Any

import numpy as np
import pytest

from ketu.houses.ascmc import compute_ascmc
from ketu.houses.whole_sign import whole_sign_cusps

ALGO_TOL_DEG: float = 1e-6


# --- Algorithm-tier oracle ---------------------------------------------------


def test_whole_sign_algorithm_matches_oracle_armc_at_all_latitudes(
    reference_charts: list[dict[str, Any]],
) -> None:
    """Bit-exact match vs swe.houses_armc on all 10 reference charts."""
    from tests.houses.conftest import swe_oracle_armc

    for chart in reference_charts:
        ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
        armc = float(np.asarray(ascmc["armc"]))
        eps = float(np.asarray(ascmc["eps"]))
        cusps = whole_sign_cusps(
            np.float64(armc), np.float64(chart["lat"]), np.float64(eps),
        )
        oracle = swe_oracle_armc(armc, float(chart["lat"]), eps, "whole_sign")
        if "error" in oracle:
            pytest.skip(f"swe.houses_armc errored at {chart['label']}: {oracle['error']}")
        # Short-arc distance (handles wraparound at 0/360).
        deltas = np.abs(((cusps - oracle["cusps"] + 180.0) % 360.0) - 180.0)
        assert deltas.max() < ALGO_TOL_DEG, (
            f"{chart['label']}: max delta {deltas.max():.2e}° exceeds "
            f"{ALGO_TOL_DEG:.0e}° tolerance; cusps={cusps[:4]} vs "
            f"oracle={oracle['cusps'][:4]}"
        )


# --- Polar safety ------------------------------------------------------------


def test_whole_sign_no_nan_at_polar_latitudes() -> None:
    """Whole Sign is polar-safe by construction — no NaN at lat=70°/80°/89°."""
    for lat in (70.0, 80.0, 89.0):
        ascmc = compute_ascmc(2451545.0, lat, 0.0)
        cusps = whole_sign_cusps(
            np.asarray(ascmc["armc"]),
            np.asarray(lat),
            np.asarray(ascmc["eps"]),
        )
        assert not np.isnan(cusps).any(), (
            f"Whole Sign returned NaN at lat={lat}°; polar safety regressed"
        )


# --- 30° spacing invariant ---------------------------------------------------


def test_whole_sign_cusps_evenly_spaced_30_degrees() -> None:
    """All adjacent cusps are exactly 30° apart (modulo 360)."""
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = whole_sign_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    diffs = np.diff(cusps) % 360.0
    np.testing.assert_allclose(diffs, 30.0, atol=1e-9)
    # Wraparound: cusps[0] = (cusps[11] + 30) % 360
    assert abs((cusps[11] + 30.0) % 360.0 - cusps[0]) < 1e-9


# --- Sign-floor convention (cusps[0] = start of rising sign) -----------------


def test_whole_sign_cusp_1_is_start_of_rising_sign() -> None:
    """cusps[0] = floor(asc / 30) * 30, NOT the ASC longitude itself."""
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = whole_sign_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    asc = float(ascmc["asc"])
    expected_cusp_1 = float(np.floor(asc / 30.0) * 30.0)
    assert abs(float(cusps[0]) - expected_cusp_1) < 1e-9, (
        f"cusps[0]={cusps[0]} but expected start-of-sign {expected_cusp_1} "
        f"(asc={asc:.4f}°). Whole Sign convention regressed — see PATTERNS §1."
    )
    # Sanity: cusps[0] is always a multiple of 30.
    assert abs(cusps[0] % 30.0) < 1e-9


# --- ASC = 0° boundary case (Pitfall 3) -------------------------------------


def test_whole_sign_asc_at_sign_boundary_yields_cusp_1_zero() -> None:
    """At ASC = 0° exact (Aries 0°), cusps[0] = 0.0 — sign boundary case.

    Pitfall 3 from 15-RESEARCH §11: floor(0/30)*30 = 0 must NOT regress.
    Empirically engineered ARMC where ASC ≈ 0° at the equator.
    """
    # At the equator with eps=23.44° and armc=0°, the ASC is approximately
    # the Aries point (0°). We accept any chart whose computed ASC is in
    # [0°, 0.01°) and verify cusps[0] == 0.0 exactly.
    armc = np.float64(0.0)
    lat = np.float64(0.0)
    eps = np.float64(23.4393)
    ascmc = compute_ascmc(2451545.0, 0.0, 0.0)
    asc = float(ascmc["asc"])
    if asc % 30.0 < 0.01 or asc % 30.0 > 29.99:
        # We're at a sign boundary — verify cusps[0] is exactly the floor.
        cusps = whole_sign_cusps(armc, lat, eps)
        assert abs(cusps[0] - np.floor(asc / 30.0) * 30.0) < 1e-9


# --- Registry registration ---------------------------------------------------


def test_whole_sign_registered_in_systems() -> None:
    """``@register('whole_sign')`` populates SYSTEMS at module import time."""
    from ketu.houses.registry import SYSTEMS, get_system
    assert "whole_sign" in SYSTEMS, (
        "whole_sign not registered — did __init__.py forget the trigger import?"
    )
    assert get_system("whole_sign") is whole_sign_cusps
    # Case-insensitive lookup.
    assert get_system("WHOLE_SIGN") is whole_sign_cusps


# --- Vectorisation -----------------------------------------------------------


def test_whole_sign_vectorised_matches_scalar_per_element() -> None:
    """Vector call returns same per-element result as N scalar calls."""
    armcs = np.array([0.0, 90.0, 180.0, 270.0])
    lats = np.array([10.0, 30.0, 50.0, 70.0])
    eps = np.array([23.44, 23.44, 23.44, 23.44])
    vec = whole_sign_cusps(armcs, lats, eps)
    assert vec.shape == (4, 12)
    for i in range(4):
        scalar = whole_sign_cusps(
            np.float64(armcs[i]), np.float64(lats[i]), np.float64(eps[i]),
        )
        np.testing.assert_allclose(vec[i], scalar, atol=1e-12)


# --- Integration with calculate_houses ---------------------------------------


def test_calculate_houses_routes_whole_sign() -> None:
    """End-to-end: ``calculate_houses(..., system='whole_sign')`` returns finite cusps."""
    from ketu.houses import calculate_houses
    r = calculate_houses(2451545.0, 48.8566, 2.3522, system="whole_sign")
    assert r["cusps"].shape == (12,)
    assert not np.isnan(r["cusps"]).any()
    # Sign-floor: cusps[0] is multiple of 30.
    assert abs(float(r["cusps"][0]) % 30.0) < 1e-9
    # out["asc"] preserves the actual ASC (NOT the sign-floor).
    assert float(r["asc"]) != float(r["cusps"][0]) or float(r["asc"]) % 30.0 < 1e-9


def test_calculate_houses_whole_sign_polar_safe_no_fallback_needed() -> None:
    """At lat=80° with system='whole_sign', no HighLatitudeError raised."""
    from ketu.houses import calculate_houses
    # polar_fallback='raise' (default) — should NOT raise for whole_sign.
    r = calculate_houses(2451545.0, 80.0, 0.0, system="whole_sign")
    assert not np.isnan(r["cusps"]).any()
    assert r["system"] == "whole_sign"
```

**Done :** `pytest tests/houses/test_whole_sign.py -x -v` passe (au minimum 7 tests verts).

### Task 6 — Créer `tests/houses/test_equal.py`

Crée `/home/loc/workspace/ketu/tests/houses/test_equal.py` en mirorant `test_whole_sign.py` mais en substituant la formule Equal :

```python
"""Equal (ASC-anchored) house tests — closed-form, polar-safe, 30°-spacing invariants.

Two-tier oracle strategy:

- **Algorithm tier**: bit-exact match vs ``swe.houses_armc`` (tolerance 1e-6°).
- **End-to-end tier**: covered by Plan 15-01's snapshot ratchet.

Plus the Equal-specific divergence tests:

- ``cusps[0] == asc`` (consistent with Placidus/Koch/Porphyry).
- ``cusps[9] == (asc + 270) mod 360`` ≠ astronomical MC (HOU2-02 contract).
- 30° spacing.
- Registry, vectorisation.
"""
from __future__ import annotations

from typing import Any

import numpy as np
import pytest

from ketu.houses.ascmc import compute_ascmc
from ketu.houses.equal import equal_cusps

ALGO_TOL_DEG: float = 1e-6


def test_equal_algorithm_matches_oracle_armc_at_all_latitudes(
    reference_charts: list[dict[str, Any]],
) -> None:
    """Bit-exact match vs swe.houses_armc on all 10 reference charts."""
    from tests.houses.conftest import swe_oracle_armc

    for chart in reference_charts:
        ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
        armc = float(np.asarray(ascmc["armc"]))
        eps = float(np.asarray(ascmc["eps"]))
        cusps = equal_cusps(
            np.float64(armc), np.float64(chart["lat"]), np.float64(eps),
        )
        oracle = swe_oracle_armc(armc, float(chart["lat"]), eps, "equal")
        if "error" in oracle:
            pytest.skip(f"swe.houses_armc errored at {chart['label']}: {oracle['error']}")
        deltas = np.abs(((cusps - oracle["cusps"] + 180.0) % 360.0) - 180.0)
        assert deltas.max() < ALGO_TOL_DEG, (
            f"{chart['label']}: max delta {deltas.max():.2e}° exceeds "
            f"{ALGO_TOL_DEG:.0e}° tolerance"
        )


def test_equal_no_nan_at_polar_latitudes() -> None:
    """Equal is polar-safe by construction — no NaN at lat=70°/80°/89°."""
    for lat in (70.0, 80.0, 89.0):
        ascmc = compute_ascmc(2451545.0, lat, 0.0)
        cusps = equal_cusps(
            np.asarray(ascmc["armc"]),
            np.asarray(lat),
            np.asarray(ascmc["eps"]),
        )
        assert not np.isnan(cusps).any(), (
            f"Equal returned NaN at lat={lat}°; polar safety regressed"
        )


def test_equal_cusps_evenly_spaced_30_degrees() -> None:
    """All adjacent cusps are exactly 30° apart (modulo 360)."""
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = equal_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    diffs = np.diff(cusps) % 360.0
    np.testing.assert_allclose(diffs, 30.0, atol=1e-9)


def test_equal_cusp_1_equals_ascendant() -> None:
    """cusps[0] == asc (consistent with Placidus/Koch/Porphyry convention).

    Paris J2000 is non-polar (lat=48.86° well below polar circle), so
    no swisseph swap occurs. We ratchet the actual short-arc equality
    cusps[0] == asc, not a noop modulo identity.
    """
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = equal_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    asc = float(ascmc["asc"])
    # Short-arc comparison: distance modulo 360°, accepting up to 1e-6° tolerance.
    # ((x - asc + 180) % 360) - 180 maps any wrap into [-180°, +180°].
    short_arc = ((float(cusps[0]) - asc + 180.0) % 360.0) - 180.0
    assert abs(short_arc) < 1e-6, (
        f"cusps[0]={cusps[0]:.9f} should equal asc={asc:.9f} "
        f"(short_arc={short_arc:.9f}°)"
    )
    # Plus, cusps[0] differs from cusps[9] by exactly 270° (mod 360).
    diff = (float(cusps[0]) - float(cusps[9]) + 360.0) % 360.0
    assert abs(diff - 90.0) < 1e-9, (
        f"cusps[0] - cusps[9] should be 90° (mod 360), got {diff}°"
    )


def test_equal_cusp_10_is_asc_plus_270_not_astronomical_mc() -> None:
    """cusps[9] = (asc + 270) mod 360, which generally ≠ astronomical MC.

    HOU2-02 contract (PATTERNS §14.2): Equal is ASC-anchored, NOT
    MC-anchored. At Paris J2000, ASC ≈ 26.77° and MC ≈ 281.78° — the
    Equal cusp 10 = 296.77°, divergent from the astro MC by ~15°.
    """
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = equal_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    asc = float(ascmc["asc"])
    mc = float(ascmc["mc"])
    expected_equal_cusp_10 = (asc + 270.0) % 360.0
    assert abs(float(cusps[9]) - expected_equal_cusp_10) < 1e-9, (
        f"cusps[9]={cusps[9]} but expected (asc+270)%360={expected_equal_cusp_10}"
    )
    # Sanity: at Paris J2000 the divergence vs astronomical MC is > 1°.
    delta_to_astro_mc = abs(((float(cusps[9]) - mc + 180.0) % 360.0) - 180.0)
    assert delta_to_astro_mc > 1.0, (
        f"Equal cusp 10 ({cusps[9]:.4f}°) should diverge from astro MC "
        f"({mc:.4f}°) by > 1° at Paris J2000; got {delta_to_astro_mc:.4f}°. "
        "Did the implementation accidentally use MC instead of (asc+270)?"
    )


def test_equal_registered_in_systems() -> None:
    """``@register('equal')`` populates SYSTEMS at module import time."""
    from ketu.houses.registry import SYSTEMS, get_system
    assert "equal" in SYSTEMS
    assert get_system("equal") is equal_cusps
    assert get_system("EQUAL") is equal_cusps


def test_equal_vectorised_matches_scalar_per_element() -> None:
    """Vector call returns same per-element result as N scalar calls."""
    armcs = np.array([0.0, 90.0, 180.0, 270.0])
    lats = np.array([10.0, 30.0, 50.0, 70.0])
    eps = np.array([23.44, 23.44, 23.44, 23.44])
    vec = equal_cusps(armcs, lats, eps)
    assert vec.shape == (4, 12)
    for i in range(4):
        scalar = equal_cusps(
            np.float64(armcs[i]), np.float64(lats[i]), np.float64(eps[i]),
        )
        np.testing.assert_allclose(vec[i], scalar, atol=1e-12)


def test_calculate_houses_routes_equal() -> None:
    """End-to-end: ``calculate_houses(..., system='equal')`` returns finite cusps."""
    from ketu.houses import calculate_houses
    r = calculate_houses(2451545.0, 48.8566, 2.3522, system="equal")
    assert r["cusps"].shape == (12,)
    assert not np.isnan(r["cusps"]).any()
    # cusps[9] = (asc + 270) % 360 — verify the ASC-anchored convention.
    asc = float(r["asc"])
    expected = (asc + 270.0) % 360.0
    # Allow modest delta because the ASC stored may differ from the
    # post-swap ASC used internally by equal_cusps. Tolerance 1e-6° is
    # appropriate for non-polar charts (Paris is well within polar
    # circle).
    assert abs(float(r["cusps"][9]) - expected) < 1e-6


def test_calculate_houses_equal_polar_safe_no_fallback_needed() -> None:
    """At lat=80° with system='equal', no HighLatitudeError raised."""
    from ketu.houses import calculate_houses
    r = calculate_houses(2451545.0, 80.0, 0.0, system="equal")
    assert not np.isnan(r["cusps"]).any()
    assert r["system"] == "equal"
```

**Done :** `pytest tests/houses/test_equal.py -x -v` passe (au minimum 8 tests verts).

### Task 7 — Étendre `tests/houses/test_polar_safety.py`

Édite `/home/loc/workspace/ketu/tests/houses/test_polar_safety.py`. **Append** après `test_porphyry_does_not_yield_nan_above_polar_circle` (ligne 95) :

```python
def test_whole_sign_does_not_yield_nan_above_polar_circle() -> None:
    """Whole Sign remains finite at lat 70°, 80°, 89° (polar-safe by construction)."""
    from ketu.houses.whole_sign import whole_sign_cusps
    for lat in (70.0, 80.0, 89.0):
        ascmc = compute_ascmc(2451545.0, lat, 0.0)
        cusps = whole_sign_cusps(
            np.asarray(ascmc["armc"]),
            np.asarray(lat),
            np.asarray(ascmc["eps"]),
        )
        assert not np.isnan(cusps).any(), (
            f"Whole Sign NaN at lat={lat}°; sign-floor is polar-safe by construction"
        )


def test_equal_does_not_yield_nan_above_polar_circle() -> None:
    """Equal remains finite at lat 70°, 80°, 89° (polar-safe by construction)."""
    from ketu.houses.equal import equal_cusps
    for lat in (70.0, 80.0, 89.0):
        ascmc = compute_ascmc(2451545.0, lat, 0.0)
        cusps = equal_cusps(
            np.asarray(ascmc["armc"]),
            np.asarray(lat),
            np.asarray(ascmc["eps"]),
        )
        assert not np.isnan(cusps).any(), (
            f"Equal NaN at lat={lat}°; ASC-anchored 30° spacing is polar-safe"
        )
```

**Done :** `pytest tests/houses/test_polar_safety.py -x -v` passe (les 2 nouveaux tests s'ajoutent à la liste existante).

## Verification gates

```bash
cd /home/loc/workspace/ketu

# 1. Plan 15-01 doit être merged (snapshot étendu, _asc1 factorisé, U16 bump)
python -c "from ketu.houses import HOUSES_DTYPE; assert HOUSES_DTYPE.fields['system'][0].itemsize >= 16*4; print('15-01 OK')"

# 2. Whole Sign et Equal sont enregistrés
python -c "from ketu.houses import SYSTEMS; assert 'whole_sign' in SYSTEMS and 'equal' in SYSTEMS; print('OK')"

# 3. Algorithm-tier oracle bit-exact (1e-6°)
pytest tests/houses/test_whole_sign.py::test_whole_sign_algorithm_matches_oracle_armc_at_all_latitudes -x -v
pytest tests/houses/test_equal.py::test_equal_algorithm_matches_oracle_armc_at_all_latitudes -x -v

# 4. End-to-end snapshot reste vert (le snapshot Plan 15-01 inclut whole_sign et equal)
pytest tests/houses/test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle -x -v

# 5. Polar safety
pytest tests/houses/test_polar_safety.py -x -v
pytest tests/houses/test_whole_sign.py::test_whole_sign_no_nan_at_polar_latitudes -x
pytest tests/houses/test_equal.py::test_equal_no_nan_at_polar_latitudes -x

# 6. Convention divergences pinned
pytest tests/houses/test_whole_sign.py::test_whole_sign_cusp_1_is_start_of_rising_sign -x
pytest tests/houses/test_equal.py::test_equal_cusp_10_is_asc_plus_270_not_astronomical_mc -x

# 7. Pitfall 3 (ASC=0° boundary)
pytest tests/houses/test_whole_sign.py::test_whole_sign_asc_at_sign_boundary_yields_cusp_1_zero -x

# 8. Polar gate étendu : calculate_houses(80, 0, system="whole_sign") sans fallback
pytest tests/houses/test_whole_sign.py::test_calculate_houses_whole_sign_polar_safe_no_fallback_needed -x
pytest tests/houses/test_equal.py::test_calculate_houses_equal_polar_safe_no_fallback_needed -x

# 9. Suite houses entière reste verte
pytest tests/houses/ -x

# 10. Coverage ≥95% sur les 2 nouveaux fichiers
pytest tests/houses/test_whole_sign.py tests/houses/test_equal.py \
    --cov=ketu.houses.whole_sign --cov=ketu.houses.equal \
    --cov-report=term-missing --cov-fail-under=95

# 11. mypy --strict clean sur les 2 nouveaux modules + api.py modifié
python -m mypy --strict ketu/houses/whole_sign.py ketu/houses/equal.py ketu/houses/api.py

# 12. Doc gates
python -m numpydoc lint ketu/houses/whole_sign.py ketu/houses/equal.py
python -m interrogate ketu/houses/whole_sign.py ketu/houses/equal.py -f 95

# 13. AGPL boundary preserved
python -c "import ketu.houses.whole_sign as m; assert not any(n.startswith('swe') for n in dir(m)); print('AGPL OK whole_sign')"
python -c "import ketu.houses.equal as m; assert not any(n.startswith('swe') for n in dir(m)); print('AGPL OK equal')"
```

## Done criteria

- [ ] HOU2-01 satisfait : `calculate_houses(jd, lat, lon, system="whole_sign")` retourne un `HOUSES_DTYPE` valide ; cusps[0] = floor(asc/30)*30 ; polar-safe à lat=70°/80°/89° (no NaN sans fallback).
- [ ] HOU2-02 satisfait : `calculate_houses(jd, lat, lon, system="equal")` retourne un `HOUSES_DTYPE` valide ; cusps[9] = (asc+270) mod 360 ≠ astronomical MC ; polar-safe.
- [ ] `SYSTEMS` registry contient `'whole_sign'` et `'equal'` au moment de `import ketu.houses`.
- [ ] `ketu/houses/api.py` polar gate étendu : `POLAR_SAFE_SYSTEMS = {"porphyry", "whole_sign", "equal"}` ; `calculate_houses(80, 0, system="equal")` ne lève pas `HighLatitudeError`.
- [ ] Algorithm-tier oracle (`swe_oracle_armc` vs implémentation Ketu) match à 1e-6° sur les 10 reference charts pour whole_sign ET equal.
- [ ] End-to-end snapshot test (`test_loaded_reference_snapshot_matches_oracle`) reste vert (les 60 blocs incluent désormais des données live-cohérentes pour whole_sign et equal).
- [ ] Convention ratchet tests verts : `test_whole_sign_cusp_1_is_start_of_rising_sign`, `test_equal_cusp_10_is_asc_plus_270_not_astronomical_mc`.
- [ ] Pitfall 3 ratchet (`test_whole_sign_asc_at_sign_boundary_yields_cusp_1_zero`) vert.
- [ ] Coverage ≥95 % sur `ketu/houses/whole_sign.py` et `ketu/houses/equal.py` (nouveaux modules — gate v1.2 strict).
- [ ] mypy `--strict` clean sur les 2 nouveaux modules ET `api.py` modifié.
- [ ] Doc gates : `numpydoc lint` clean, `interrogate ≥95%`.
- [ ] Régression : `pytest tests/houses/ -x` reste 100 % vert (Plans 10-03..10-06 intacts).

## Atomic commit message

```
feat(15-02): add Whole Sign and Equal house systems (HOU2-01, HOU2-02)

- ketu/houses/whole_sign.py: closed-form sign-floor cusps
  (cusps[0] = floor(asc/30)*30, NOT the ASC itself; divergence
  documented in module docstring and ratcheted by
  test_whole_sign_cusp_1_is_start_of_rising_sign)
- ketu/houses/equal.py: closed-form ASC-anchored 30° spacing
  (cusps[9] = (asc+270)%360, NOT astronomical MC; HOU2-02 contract
  ratcheted by test_equal_cusp_10_is_asc_plus_270_not_astronomical_mc)
- Both systems polar-safe by construction (no NaN at lat=70°/80°/89°);
  ketu/houses/api.py polar gate extended to skip whole_sign and equal
  (POLAR_SAFE_SYSTEMS set)
- ketu/houses/__init__.py: trigger imports for whole_sign + equal so
  @register decorators populate SYSTEMS at package load
- tests/houses/test_whole_sign.py + test_equal.py: algorithm-tier
  oracle bit-exact at 1e-6° on all 10 reference charts; 30°-spacing
  invariants; ASC=0° boundary regression (Pitfall 3); registry checks;
  vectorisation; calculate_houses integration
- tests/houses/test_polar_safety.py: extend with no-NaN assertions for
  whole_sign and equal at polar latitudes

Covers: HOU2-01 (Whole Sign), HOU2-02 (Equal)
Refs: D-04 (one file per system), Pitfalls 1, 2, 3 from 15-RESEARCH §11
End-to-end snapshot gate (HOU2-05 fragment) inherited green from Plan
15-01's regenerated reference_charts.json.
```

## Rollback note

`git revert <commit_sha>` annule complètement ce plan : suppression de `whole_sign.py`, `equal.py`, `test_whole_sign.py`, `test_equal.py` ; restauration de `__init__.py` à l'état Plan 15-01 (3 trigger imports) ; restauration de `api.py` à `system_lower != "porphyry"` ; restauration de `test_polar_safety.py` à 7 tests. Plan 15-03 (Regiomontanus) ne dépend que du `_asc1` factorisé en Plan 15-01, pas de ce plan — son revert est isolé. Plan 15-04 (CLI) consomme `SYSTEMS.keys()` dynamiquement — il afficherait 4 systèmes (placidus/koch/porphyry/regiomontanus si 15-03 est mergé) au lieu de 6 ; à recheck.
