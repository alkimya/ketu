---
phase: 15-additional-house-systems
plan: 03
plan_name: regiomontanus
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - ketu/houses/regiomontanus.py
  - ketu/houses/__init__.py
  - tests/houses/test_regiomontanus.py
  - tests/houses/test_polar_safety.py
  - tests/houses/test_integration.py
autonomous: true
requirements:
  - HOU2-03
parallelizable_with: [15-02]

must_haves:
  truths:
    - "`calculate_houses(jd, lat, lon, system='regiomontanus')` retourne un `HOUSES_DTYPE` valide à toutes latitudes |lat| < (90 - eps_mean(jd))"
    - "Au-dessus du cercle polaire (|lat| ≥ 90 - eps), `regiomontanus_cusps` retourne NaN — Koch-style propagation, PAS de swisseph swap MC↔IC"
    - "`polar_fallback='porphyry'` route correctement les éléments polaires de regiomontanus vers porphyry_cusps via `calculate_houses` existant"
    - "`polar_fallback='raise'` (défaut) lève `HighLatitudeError` au-dessus du cercle polaire"
    - "L'algorithme matche `swe_oracle_armc(armc, lat, eps, 'regiomontanus')` à 1e-6° près sur les charts non polaires"
    - "Le snapshot end-to-end matche à 1 arcmin (60 charts × 12 cusps), avec exception Reykjavik mesurée empiriquement et pinned"
    - "`SYSTEMS` registry contient 'regiomontanus' au moment de `import ketu.houses`"
    - "Les 4 cusps non triviaux (11, 12, 2, 3) sont calculés via `_asc1` du Plan 15-01 avec pole heights `fh1`/`fh2`, PAS la latitude géographique (Pitfall 4)"
  artifacts:
    - path: "ketu/houses/regiomontanus.py"
      provides: "@register('regiomontanus') def regiomontanus_cusps(armc, lat, eps) -> ndarray[..., 12]"
      contains: '@register("regiomontanus")'
      min_lines: 100
    - path: "tests/houses/test_regiomontanus.py"
      provides: "Algorithm-tier oracle 1e-6°, end-to-end 1 arcmin, Reykjavik empirical pin, polar NaN ratchet, registry, vectorisation"
      min_lines: 200
  key_links:
    - from: "ketu/houses/regiomontanus.py"
      to: "ketu/houses/_ecliptic.py:_asc1"
      via: "from ._ecliptic import _asc1"
      pattern: "from \\._ecliptic import _asc1"
    - from: "ketu/houses/regiomontanus.py"
      to: "ketu.houses.registry.SYSTEMS"
      via: "@register decorator at module load"
      pattern: '@register\\("regiomontanus"\\)'
    - from: "ketu/houses/__init__.py"
      to: "ketu/houses/regiomontanus.py"
      via: "from . import regiomontanus trigger import (Pitfall 2)"
      pattern: "from \\. import regiomontanus"
    - from: "ketu/houses/api.py polar_fallback path"
      to: "regiomontanus NaN cusps"
      via: "is_polar(lat, jd) detects |lat| >= 90 - eps; np.where routes polar elements to porphyry_cusps"
      pattern: "polar_fallback"
---

# Plan 15-03 — Regiomontanus house system (closed-form, polar→NaN)

## Goal

Implémenter Regiomontanus (HOU2-03), le seul système Phase 15 avec singularité polaire. Plus complexe que Whole Sign/Equal (formule trigonométrique, dépendance latitudinale via `tan(lat)`) mais plus simple que Placidus (closed-form, **pas d'itération**). Profil mathématique isomorphe à Koch — d'où l'usage de `_asc1` factorisé en Plan 15-01.

**Algorithme** (per swisseph `swehouse.c` case `'R'`) :

```
fh1 = atan(tan(lat) / 2)            # pole height for cusps 11/3
fh2 = atan(tan(lat) * cos(30°))     # pole height for cusps 12/2

cusp_11 = _asc1(armc + 30°,  fh1, sin_eps, cos_eps)
cusp_12 = _asc1(armc + 60°,  fh2, sin_eps, cos_eps)
cusp_2  = _asc1(armc + 120°, fh2, sin_eps, cos_eps)
cusp_3  = _asc1(armc + 150°, fh1, sin_eps, cos_eps)

cusps 5/6/8/9 = opposites 180° de 11/12/2/3
cusps 1/4/7/10 = ASC, IC, DESC, MC (closed-form via arctan2)
```

**Polar safety** (D-02 verrouillé) :
- `|lat| ≥ 90° - eps_mean(jd)` → NaN propagation Koch-style. Pas de swisseph swap MC↔IC (qui serait une digression vs HOU-06).
- `calculate_houses(...)` existant détecte les NaN via `is_polar` et route via `polar_fallback="porphyry"` ou lève `HighLatitudeError` selon le paramètre.
- Le mécanisme de routing est déjà en place dans `api.py:124-152` ; aucune modification d'`api.py` requise pour Regiomontanus.

**Pitfall 4 critique (15-RESEARCH §11) :** dans `_asc1(x, lat, sin_eps, cos_eps)`, le paramètre `lat` représente une **pole height**, pas la latitude géographique. Pour Regiomontanus, on passe `fh1` ou `fh2` (calculés depuis `geo_lat`) — **JAMAIS** `geo_lat` directement. Variable de boucle nommée explicitement `pole_height` dans le code pour ratchet visuel.

**Tolérance Reykjavik** : le researcher recommande mesure empirique (15-RESEARCH §14.3, VALIDATION §Manual-Only). Procédure : implémenter Regio, lancer le test end-to-end sur Reykjavik, mesurer le delta max, pinner à `delta_max + 0.5 arcmin` de marge. Si `delta_max > 5 arcmin` → c'est un BUG, pas une tolérance à relâcher (probable Pitfall 4 ou erreur de signe).

Plan parallèle avec Plan 15-02 (Whole Sign + Equal). Aucun conflit fichier (chacun crée son propre fichier production + test). **Le seul fichier partagé est `ketu/houses/__init__.py` qui reçoit 1 ligne append** (`from . import regiomontanus`) — disjoint des 2 lignes du Plan 15-02 ; merge trivial.

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-CONTEXT.md` — D-02 (polar NaN style Koch), D-04 (un fichier par système), D-05 (`_asc1` extraction préliminaire).
2. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-RESEARCH.md` § 3.3 (formule canonique Regiomontanus, swehouse.c case 'R'), § 6 (polar safety boundary), § 11 Pitfalls 2, 4, 5, § 14.3 (tolérance Reykjavik à mesurer empiriquement).
3. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-PATTERNS.md` § 3 (analogue Koch — gabarit littéral), § 4 (`__init__.py`), § 9 (test_regiomontanus analogue test_koch), § 12.1-12.6 (cross-cutting patterns, tolérances).
4. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-VALIDATION.md` lignes 48-51 (tasks 15-03-01..04 verification map).
5. `/home/loc/workspace/ketu/ketu/houses/koch.py` (intégral, 181 lignes) — gabarit littéral. **Particulièrement** : lignes 92-181 (`koch_cusps` body), pour la structure (broadcast → ASC/MC closed-form → polar mask → 4 cusps via `_asc1` → opposites 180° → stack → mask).
6. `/home/loc/workspace/ketu/ketu/houses/_ecliptic.py` (après modif Plan 15-01) — vérifier que `_asc1` est accessible via `from ._ecliptic import _asc1`.
7. `/home/loc/workspace/ketu/ketu/houses/api.py` lignes 118-152 — comprendre que le polar_fallback Porphyry est déjà géré pour TOUT système retournant NaN au polar gate (aucun ajout requis).
8. `/home/loc/workspace/ketu/tests/houses/test_koch.py` (intégral, 256 lignes) — gabarit littéral pour `test_regiomontanus.py` (algorithm-tier 1e-6°, end-to-end 1 arcmin, Reykjavik tolerance pinned, polar mask, vectorisation).
9. `/home/loc/workspace/ketu/tests/houses/test_polar_safety.py` (lignes 50-80) — pattern `test_*_yields_nan_above_polar_circle` ; un test similaire à ajouter pour regiomontanus.
10. `/home/loc/workspace/ketu/tests/houses/test_integration.py` (intégral, 11 KB) — où ajouter un `test_polar_fallback_routes_regiomontanus_to_porphyry` (intégration polar_fallback="porphyry").

**Référence canonique externe (à grep dans le filesystem si dispo, ou à laisser au commentaire) :** `swehouse.c` case `'R'` lignes ~1200-1250 du source pyswisseph. Si non accessible, le code de Regiomontanus dans le RESEARCH.md §3.3 fait foi.

## Files

**Crée :**
- `/home/loc/workspace/ketu/ketu/houses/regiomontanus.py`
- `/home/loc/workspace/ketu/tests/houses/test_regiomontanus.py`

**Modifie :**
- `/home/loc/workspace/ketu/ketu/houses/__init__.py` — append 1 ligne `from . import regiomontanus` après les imports trigger existants.
- `/home/loc/workspace/ketu/tests/houses/test_polar_safety.py` — ajouter `test_regiomontanus_yields_nan_above_polar_circle`.
- `/home/loc/workspace/ketu/tests/houses/test_integration.py` — ajouter `test_polar_fallback_routes_regiomontanus_to_porphyry`.

**Lit (référence) :**
- `/home/loc/workspace/ketu/ketu/houses/koch.py` (gabarit production littéral).
- `/home/loc/workspace/ketu/tests/houses/test_koch.py` (gabarit test littéral).

## Tasks

### Task 1 — Créer `ketu/houses/regiomontanus.py`

Crée `/home/loc/workspace/ketu/ketu/houses/regiomontanus.py` en mirorant `ketu/houses/koch.py` :

```python
"""Regiomontanus house system implementation.

Regiomontanus divides the celestial equator into 12 equal 30° arcs, then
projects each arc into the ecliptic via great circles passing through the
north and south points of the local horizon (the prime vertical). The
result: each cusp is the ecliptic longitude where a specific great circle
crosses the ecliptic.

Per swisseph C source (``swehouse.c`` case ``'R'``):

    fh1 = atan(tan(lat) / 2)             # pole height for cusps 11/3
    fh2 = atan(tan(lat) * cos(30°))      # pole height for cusps 12/2

    cusp_11 = Asc1(ARMC + 30°,  fh1, sin_eps, cos_eps)
    cusp_12 = Asc1(ARMC + 60°,  fh2, sin_eps, cos_eps)
    cusp_2  = Asc1(ARMC + 120°, fh2, sin_eps, cos_eps)
    cusp_3  = Asc1(ARMC + 150°, fh1, sin_eps, cos_eps)

    cusps 5/6/8/9 = opposites of 11/12/2/3 (180°)
    cusps 1/4/7/10 = ASC, IC, DESC, MC (closed-form via arctan2)

Polar boundary at ``|lat| ≥ 90° − eps``: ``tan(lat)`` diverges,
``fh1``/``fh2`` lose precision, the formula breaks. We mirror Koch's
NaN propagation strategy: at polar latitudes, return ``NaN`` cusps so
:func:`ketu.houses.calculate_houses` can route via ``polar_fallback``
per HOU-06 (D-02 in 15-CONTEXT.md). NO swisseph-style MC↔IC swap (which
would be a digression from the v1.1 polar contract).

Pitfall — pole height vs geographic latitude
--------------------------------------------
The shared :func:`_asc1` helper takes a parameter named ``lat`` for
historical reasons (Koch passes the geographic latitude there). For
Regiomontanus, the parameter is the great-circle **pole height**:
``fh1 = atan(tan(geo_lat) / 2)`` for cusps 11 and 3, and
``fh2 = atan(tan(geo_lat) * cos(30°))`` for cusps 12 and 2. Passing
``geo_lat`` instead would produce a uniform ~10° drift on all four
non-trivial cusps (Pitfall 4 from 15-RESEARCH §11). We name the
intermediate variables ``pole_height_outer`` and ``pole_height_inner``
to make the distinction visually obvious.
"""
from __future__ import annotations

import numpy as np

from ._ecliptic import _asc1
from .registry import register

#: Iteration cap kept for API parity with Placidus tests; Regiomontanus
#: is closed-form (no fixed-point solve), so the constant is unused in
#: the production code path. Reserved for future iterative variants.
MAX_ITER: int = 50

#: Convergence threshold (degrees) — same comment as :data:`MAX_ITER`.
TOL_DEG: float = 1e-7


@register("regiomontanus")
def regiomontanus_cusps(
    armc: np.ndarray,
    lat: np.ndarray,
    eps: np.ndarray,
) -> np.ndarray:
    """Compute the 12 Regiomontanus house cusps.

    Closed-form per swisseph ``swehouse.c`` case ``'R'``. At latitudes
    inside the polar circle (``|lat| ≥ 90° − eps``) the formula becomes
    degenerate; we return ``NaN`` cusps so
    :func:`ketu.houses.calculate_houses` can route via ``polar_fallback``
    per HOU-06.

    Parameters
    ----------
    armc : np.ndarray
        Right Ascension of Medium Coeli (degrees), broadcast-compatible
        with ``lat`` and ``eps``.
    lat : np.ndarray
        Geographic latitude (degrees).
    eps : np.ndarray
        Mean obliquity of the ecliptic (degrees).

    Returns
    -------
    np.ndarray
        Array of shape ``(..., 12)`` with cusp ordering
        ``[asc, c2, c3, ic, c5, c6, desc, c8, c9, mc, c11, c12]``. NaN
        elements where ``|lat| ≥ 90° − eps``.

    Notes
    -----
    The pole heights ``fh1`` and ``fh2`` are intermediate quantities,
    NOT geographic latitudes — see module docstring (Pitfall 4).
    """
    armc_b, lat_b, eps_b = np.broadcast_arrays(armc, lat, eps)
    armc_rad = np.deg2rad(armc_b)
    eps_rad = np.deg2rad(eps_b)
    lat_rad = np.deg2rad(lat_b)
    sin_eps = np.sin(eps_rad)
    cos_eps = np.cos(eps_rad)

    # Closed-form ASC and MC (mirror koch.py:128-138).
    mc = np.rad2deg(np.arctan2(
        np.sin(armc_rad),
        np.cos(armc_rad) * np.cos(eps_rad),
    )) % 360.0
    asc = np.rad2deg(np.arctan2(
        np.cos(armc_rad),
        -(np.sin(eps_rad) * np.tan(lat_rad)
          + np.cos(eps_rad) * np.sin(armc_rad)),
    )) % 360.0
    ic = (mc + 180.0) % 360.0
    desc = (asc + 180.0) % 360.0

    # Polar boundary: at |lat| ≥ 90 − eps the Regiomontanus formula
    # degenerates (tan(lat) diverges, fh1/fh2 lose precision). Mirror
    # Koch's NaN-propagation strategy — Plan 10-06's calculate_houses
    # routes via polar_fallback (D-02 in 15-CONTEXT.md). NO MC↔IC swap.
    polar_mask = np.abs(lat_b) >= (90.0 - eps_b)

    # Pole heights for the great circles defining cusps 11/12/2/3.
    # Named explicitly to ratchet against Pitfall 4 (15-RESEARCH §11):
    # NEVER pass geographic latitude to _asc1 in this file.
    cos_30 = np.cos(np.deg2rad(30.0))  # constant ~0.866
    pole_height_outer = np.arctan(np.tan(lat_rad) / 2.0)        # cusps 11, 3
    pole_height_inner = np.arctan(np.tan(lat_rad) * cos_30)     # cusps 12, 2
    pole_height_outer_deg = np.rad2deg(pole_height_outer)
    pole_height_inner_deg = np.rad2deg(pole_height_inner)

    # 4 non-trivial cusps via _asc1 (per swisseph swehouse.c case 'R').
    # NOTE: each call passes a POLE HEIGHT, not a geographic latitude.
    cusp_11 = _asc1(armc_b + 30.0,  pole_height_outer_deg, sin_eps, cos_eps)
    cusp_12 = _asc1(armc_b + 60.0,  pole_height_inner_deg, sin_eps, cos_eps)
    cusp_2  = _asc1(armc_b + 120.0, pole_height_inner_deg, sin_eps, cos_eps)
    cusp_3  = _asc1(armc_b + 150.0, pole_height_outer_deg, sin_eps, cos_eps)

    # Cusps 5, 6, 8, 9 are opposites by construction (mirror koch.py:163-166).
    cusp_5 = (cusp_11 + 180.0) % 360.0
    cusp_6 = (cusp_12 + 180.0) % 360.0
    cusp_8 = (cusp_2 + 180.0) % 360.0
    cusp_9 = (cusp_3 + 180.0) % 360.0

    cusps = np.stack([
        asc, cusp_2, cusp_3, ic,
        cusp_5, cusp_6, desc, cusp_8,
        cusp_9, mc, cusp_11, cusp_12,
    ], axis=-1)

    # Apply polar mask: NaN out polar elements (mirror koch.py:174-178).
    if polar_mask.any():
        mask_b = np.broadcast_to(polar_mask[..., np.newaxis], cusps.shape)
        cusps = np.where(mask_b, np.nan, cusps)

    result: np.ndarray = cusps
    return result
```

**Notes pour l'implémentateur :**
- **Variables de pole height nommées explicitement** (`pole_height_outer`, `pole_height_inner`) — ratchet visuel anti-Pitfall 4.
- **`cos_30` calculé une fois** comme constante (pas de re-calcul dans la formule de chaque cusp). C'est ~0.866025 ; vectorisable via `np.cos(np.deg2rad(30.0))` une fois.
- **Mode broadcast** : `armc_b + 30.0` est broadcast-compatible avec `pole_height_outer_deg` (même shape). `_asc1` lui-même est vectorisé sur ces inputs (cf `_ecliptic.py:_asc1` body).
- **Polar mask** : utilise `90.0 - eps_b` (pas `66.5607` hardcoded) — Pitfall 4 v1.1, déjà respecté par koch.py.

**Done :**
```bash
python -c "from ketu.houses import regiomontanus_cusps; import numpy as np; r = regiomontanus_cusps(np.float64(45.0), np.float64(48.86), np.float64(23.4393)); print(r.shape, r[:4])"
# affiche (12,) avec 4 valeurs finies
python -c "from ketu.houses import regiomontanus_cusps; import numpy as np; r = regiomontanus_cusps(np.float64(45.0), np.float64(80.0), np.float64(23.4393)); print(np.isnan(r).all())"
# True (polar)
```

### Task 2 — Étendre `ketu/houses/__init__.py`

Édite `/home/loc/workspace/ketu/ketu/houses/__init__.py`. **Append** 1 ligne après les trigger imports existants (placidus, koch, porphyry, et — si Plan 15-02 mergé en premier — whole_sign et equal) :

```python
from . import regiomontanus  # noqa: F401  registers 'regiomontanus' in SYSTEMS
```

**Pitfall 2 (15-RESEARCH §11) :** sans cette ligne, `calculate_houses(system="regiomontanus")` lèverait `ValueError("unknown house system 'regiomontanus'")`.

**Note merge avec Plan 15-02 :** ce plan ajoute 1 ligne, Plan 15-02 en ajoute 2 (whole_sign + equal). Les 3 lignes sont append-only et disjointes. Si les 2 plans landent sur des branches feature distinctes :
- Si 15-02 merge avant 15-03 : `__init__.py` aura 5 trigger imports ; 15-03 ajoute la 6e à la suite, no-conflict.
- Si 15-03 merge avant 15-02 : `__init__.py` aura 4 trigger imports ; 15-02 ajoute 2 lignes après regiomontanus, no-conflict (git append-only merge).
- Si conflit `git rebase` : trivial — ordre alphabétique des trigger imports n'est pas un contrat ; conserver les 3 lignes finales par ordre de plan ou alphabétique.

**Done :** `python -c "from ketu.houses import SYSTEMS; assert 'regiomontanus' in SYSTEMS; print(sorted(SYSTEMS))"` affiche `['koch', 'placidus', 'porphyry', 'regiomontanus']` (4 entrées si Plan 15-02 pas mergé) ou `['equal', 'koch', 'placidus', 'porphyry', 'regiomontanus', 'whole_sign']` (6 entrées si 15-02 mergé en premier).

### Task 3 — Créer `tests/houses/test_regiomontanus.py`

Crée `/home/loc/workspace/ketu/tests/houses/test_regiomontanus.py` en mirorant `tests/houses/test_koch.py`. Structure complète :

```python
"""Regiomontanus tests — closed-form, two-tier oracle, polar NaN propagation.

Two-tier oracle strategy (mirrors Koch v1.1 pattern):

- **Algorithm tier** (`test_regiomontanus_algorithm_matches_oracle_armc`):
  bit-exact comparison vs ``swe.houses_armc``, tolerance ``1e-6°``. Not
  eps-sensitive at the algorithm level (pole heights cancel the eps
  drift between mean and true obliquity).
- **End-to-end tier** (`test_regiomontanus_cusps_match_oracle_at_arcmin`):
  via ``swe_oracle`` (which calls ``swe.houses_ex`` with full sidereal
  time machinery), tolerance ``1.0/60.0°`` (1 arcmin) on 7 tight non-
  polar charts. Reykjavik (lat=64.1°N) gets a relaxed tolerance pinned
  empirically (see ``test_regiomontanus_reykjavik_tolerance_within_pinned_bound``).

Plus invariants and polar contract:

- 4 non-trivial cusps (11/12/2/3) via ``_asc1`` with pole heights
  ``fh1 = atan(tan(lat)/2)`` and ``fh2 = atan(tan(lat)·cos(30°))``.
- Cusps 5/6/8/9 are 180° opposites of 11/12/2/3.
- Cusps 1/4/7/10 = ASC/IC/DESC/MC.
- ``|lat| ≥ 90° − eps`` → NaN propagation (Koch-style, NOT swap).
- Registry registration.
- Vectorisation matches scalar.
"""
from __future__ import annotations

from typing import Any

import numpy as np
import pytest

from ketu.houses.ascmc import compute_ascmc
from ketu.houses.regiomontanus import regiomontanus_cusps

ARCMIN_DEG: float = 1.0 / 60.0
CUSP_TOL_ARCMIN: float = 1.0 * ARCMIN_DEG
ALGO_TOL_DEG: float = 1e-6

#: Reykjavik (lat=64.15°N) sits inside the high-latitude band where
#: eps_mean vs eps_true drift in the snapshot oracle propagates through
#: the pole-height formulas. The exact tolerance is MEASURED EMPIRICALLY
#: during plan execution (see Task 5 of Plan 15-03) and pinned here.
#: 15-RESEARCH §14.3 estimates 2-5 arcmin; the actual measured value is
#: pinned by this constant to ratchet against future drift.
#:
#: TODO: replace the placeholder below with the measured value once Task
#: 5 has been run. The measurement procedure is documented inline in
#: ``test_regiomontanus_reykjavik_drift_measured_and_pinned``.
REYKJAVIK_REGIO_TOL_ARCMIN: float = 5.0 * ARCMIN_DEG  # initial cap; tighten after measurement

#: Charts that should match the oracle within the standard 1-arcmin
#: tolerance (excludes Reykjavik, polar, and equator-degenerate cases).
NON_POLAR_LABELS_TIGHT: tuple[str, ...] = (
    "J2000_Greenwich",
    "J2000_Paris",
    "J2000_Sydney",
    "J2000_Tokyo",
    "J2000_BuenosAires",
    "J2000_Equator",
    "1900_NewYork",
)

NON_POLAR_LABELS: tuple[str, ...] = NON_POLAR_LABELS_TIGHT + ("2050_Reykjavik",)


# --- Algorithm-tier oracle (bit-exact 1e-6°) ---------------------------------


def test_regiomontanus_algorithm_matches_oracle_armc(
    reference_charts: list[dict[str, Any]],
) -> None:
    """Algorithm tier: bit-exact match vs swe.houses_armc on non-polar charts.

    1e-6° tolerance — the algorithm is not eps-sensitive at this tier
    because both Ketu and swisseph use the same eps input. End-to-end
    drift (Reykjavik) appears in the snapshot tier, not here.
    """
    from tests.houses.conftest import swe_oracle_armc

    for chart in reference_charts:
        if chart["label"] not in NON_POLAR_LABELS:
            continue
        ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
        armc = float(np.asarray(ascmc["armc"]))
        eps = float(np.asarray(ascmc["eps"]))
        cusps = regiomontanus_cusps(
            np.float64(armc), np.float64(chart["lat"]), np.float64(eps),
        )
        oracle = swe_oracle_armc(armc, float(chart["lat"]), eps, "regiomontanus")
        if "error" in oracle:
            pytest.skip(f"oracle errored at {chart['label']}: {oracle['error']}")
        deltas = np.abs(((cusps - oracle["cusps"] + 180.0) % 360.0) - 180.0)
        assert deltas.max() < ALGO_TOL_DEG, (
            f"{chart['label']}: max delta {deltas.max():.2e}° exceeds "
            f"{ALGO_TOL_DEG:.0e}° tolerance; cusps[:4]={cusps[:4]} vs "
            f"oracle={oracle['cusps'][:4]}"
        )


# --- End-to-end snapshot tier (1 arcmin) -------------------------------------


@pytest.mark.parametrize("label", NON_POLAR_LABELS_TIGHT)
def test_regiomontanus_cusps_match_oracle_at_arcmin(
    label: str,
    reference_charts: list[dict[str, Any]],
    loaded_reference_snapshot: dict[str, Any],
) -> None:
    """End-to-end: snapshot match within 1 arcmin on tight non-polar charts."""
    chart = next(c for c in reference_charts if c["label"] == label)
    ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(chart["lat"]),
        np.asarray(ascmc["eps"]),
    )
    snap = loaded_reference_snapshot["charts"][label]["systems"]["regiomontanus"]
    if "error" in snap:
        pytest.skip(f"{label}: snapshot has polar error")
    oracle_cusps = np.asarray(snap["cusps"])
    deltas = np.abs(((cusps - oracle_cusps + 180.0) % 360.0) - 180.0)
    assert deltas.max() < CUSP_TOL_ARCMIN, (
        f"{label}: max delta {deltas.max() * 60:.4f}′ exceeds 1 arcmin; "
        f"cusps[:4]={cusps[:4]} vs oracle={oracle_cusps[:4]}"
    )


# --- Reykjavik tolerance — MEASURED EMPIRICALLY then pinned -----------------


def test_regiomontanus_reykjavik_drift_measured_and_pinned(
    reference_charts: list[dict[str, Any]],
    loaded_reference_snapshot: dict[str, Any],
) -> None:
    """Reykjavik (lat=64.15°N) drift must stay within the pinned tolerance.

    Measurement procedure (Task 5 of plan 15-03):
        1. Run this test with REYKJAVIK_REGIO_TOL_ARCMIN = 5.0 (initial).
        2. Inspect the printed delta_max.
        3. Update the constant to (delta_max + 0.5) arcmin (margin).
        4. If delta_max > 5 arcmin → BUG, NOT a tolerance to relax (see
           Pitfall 4: pole-height substitution error). Investigate
           BEFORE pinning.
    """
    chart = next(c for c in reference_charts if c["label"] == "2050_Reykjavik")
    ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(chart["lat"]),
        np.asarray(ascmc["eps"]),
    )
    snap = loaded_reference_snapshot["charts"]["2050_Reykjavik"]["systems"]["regiomontanus"]
    oracle_cusps = np.asarray(snap["cusps"])
    deltas = np.abs(((cusps - oracle_cusps + 180.0) % 360.0) - 180.0)
    delta_max = float(deltas.max())
    print(f"\nReykjavik Regiomontanus drift: max={delta_max * 60:.4f}′")
    assert delta_max < REYKJAVIK_REGIO_TOL_ARCMIN, (
        f"Reykjavik drift {delta_max * 60:.4f}′ exceeds pinned tolerance "
        f"{REYKJAVIK_REGIO_TOL_ARCMIN * 60:.4f}′. If delta < 5′, this is "
        f"likely an eps_mean vs eps_true drift — measure and tighten "
        f"REYKJAVIK_REGIO_TOL_ARCMIN. If delta >= 5′, suspect Pitfall 4 "
        f"(pole-height substitution error in regiomontanus.py)."
    )


# --- Polar contract (NaN propagation) ---------------------------------------


def test_regiomontanus_yields_nan_above_polar_circle() -> None:
    """Above the polar circle (|lat| ≥ 90° − eps), Regiomontanus NaN-propagates."""
    from ketu.houses.porphyry import polar_circle
    jd = 2451545.0
    pc = float(polar_circle(jd))
    lat = pc + 1.0  # 1° beyond polar circle
    ascmc = compute_ascmc(jd, lat, 0.0)
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(lat),
        np.asarray(ascmc["eps"]),
    )
    assert np.isnan(cusps).any(), (
        f"Regiomontanus 1° beyond polar circle (lat={lat}°) must NaN at "
        "least one cusp; calculate_houses uses np.isnan to route via "
        "polar_fallback per HOU-06 (D-02 in 15-CONTEXT.md)."
    )


def test_regiomontanus_polar_lat_80_yields_all_nan() -> None:
    """At lat=80° (well beyond polar), Regiomontanus returns all-NaN."""
    jd = 2451545.0
    ascmc = compute_ascmc(jd, 80.0, 0.0)
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(80.0),
        np.asarray(ascmc["eps"]),
    )
    assert np.isnan(cusps).all(), (
        "At lat=80° all 12 cusps must be NaN (the entire polar mask applies)"
    )


def test_regiomontanus_no_silent_nan_at_mid_latitudes(
    reference_charts: list[dict[str, Any]],
) -> None:
    """At non-polar latitudes, regiomontanus_cusps NEVER returns NaN.

    Regression catcher: if a sign error or domain error introduces NaN
    at a normal latitude, this test fails fast.
    """
    for chart in reference_charts:
        if chart["label"] not in NON_POLAR_LABELS:
            continue
        ascmc = compute_ascmc(chart["jd"], chart["lat"], chart["lon"])
        cusps = regiomontanus_cusps(
            np.asarray(ascmc["armc"]),
            np.asarray(chart["lat"]),
            np.asarray(ascmc["eps"]),
        )
        assert not np.isnan(cusps).any(), (
            f"{chart['label']}: regiomontanus produced NaN at non-polar "
            f"latitude {chart['lat']}° — Pitfall 4 (pole-height) suspected."
        )


# --- Cusp symmetry (5/6/8/9 = 11/12/2/3 + 180°) -----------------------------


def test_regiomontanus_cusps_5_6_8_9_are_opposites_of_11_12_2_3() -> None:
    """Cusps 5,6,8,9 are 180° opposites of 11,12,2,3 by construction."""
    ascmc = compute_ascmc(2451545.0, 48.8566, 2.3522)
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(48.8566),
        np.asarray(ascmc["eps"]),
    )
    # Index map: cusp_5 = idx 4, cusp_11 = idx 10 ; cusp_6 = idx 5, cusp_12 = idx 11 ;
    # cusp_8 = idx 7, cusp_2 = idx 1 ; cusp_9 = idx 8, cusp_3 = idx 2.
    pairs = [(4, 10), (5, 11), (7, 1), (8, 2)]
    for left, right in pairs:
        diff = ((cusps[left] - cusps[right]) % 360.0)
        assert abs(diff - 180.0) < 1e-9, (
            f"cusps[{left}]={cusps[left]} should be 180° from cusps[{right}]={cusps[right]}; "
            f"got delta {diff}°"
        )


# --- Constants invariant -----------------------------------------------------


def test_regiomontanus_constants_unchanged() -> None:
    """API parity constants (MAX_ITER, TOL_DEG) preserved as in Koch."""
    from ketu.houses.regiomontanus import MAX_ITER, TOL_DEG
    assert MAX_ITER == 50
    assert TOL_DEG == 1e-7


# --- Vectorisation -----------------------------------------------------------


def test_regiomontanus_vectorised_matches_scalar_per_element() -> None:
    """Vector call returns same per-element result as N scalar calls."""
    armcs = np.array([0.0, 90.0, 180.0, 270.0])
    lats = np.array([10.0, 30.0, 40.0, 50.0])  # all non-polar
    eps = np.array([23.44, 23.44, 23.44, 23.44])
    vec = regiomontanus_cusps(armcs, lats, eps)
    assert vec.shape == (4, 12)
    for i in range(4):
        scalar = regiomontanus_cusps(
            np.float64(armcs[i]), np.float64(lats[i]), np.float64(eps[i]),
        )
        np.testing.assert_allclose(vec[i], scalar, atol=1e-10)


# --- Registry registration ---------------------------------------------------


def test_regiomontanus_registered_in_systems() -> None:
    """``@register('regiomontanus')`` populates SYSTEMS at module import time."""
    from ketu.houses.registry import SYSTEMS, get_system
    assert "regiomontanus" in SYSTEMS, (
        "regiomontanus not registered — did __init__.py forget the trigger import?"
    )
    assert get_system("regiomontanus") is regiomontanus_cusps
    assert get_system("REGIOMONTANUS") is regiomontanus_cusps
```

**Done :** `pytest tests/houses/test_regiomontanus.py -x -v` passe. **Au moins 10 tests verts.** Le test `test_regiomontanus_reykjavik_drift_measured_and_pinned` peut afficher la mesure réelle de drift en stdout — c'est attendu en Task 5.

### Task 4 — Étendre `tests/houses/test_polar_safety.py` et `test_integration.py`

**`tests/houses/test_polar_safety.py`** : append après les tests Whole Sign/Equal du Plan 15-02 (ou en fin de fichier) :

```python
def test_regiomontanus_yields_nan_above_polar_circle_in_safety_suite() -> None:
    """Regiomontanus follows Koch's NaN-propagation contract above polar circle."""
    from ketu.houses.regiomontanus import regiomontanus_cusps
    jd = 2451545.0
    pc = float(polar_circle(jd))
    lat = pc + 1.0
    ascmc = compute_ascmc(jd, lat, 0.0)
    cusps = regiomontanus_cusps(
        np.asarray(ascmc["armc"]),
        np.asarray(lat),
        np.asarray(ascmc["eps"]),
    )
    assert np.isnan(cusps).any(), (
        "Regiomontanus must NaN-propagate at polar latitudes (D-02 verrouillé)"
    )
```

**`tests/houses/test_integration.py`** : append un test polar_fallback dédié à Regiomontanus :

```python
def test_polar_fallback_routes_regiomontanus_to_porphyry() -> None:
    """At lat=80° with system='regiomontanus' and polar_fallback='porphyry',
    cusps are populated by Porphyry (no NaN, finite values)."""
    from ketu.houses import calculate_houses
    r = calculate_houses(
        2451545.0, 80.0, 0.0,
        system="regiomontanus",
        polar_fallback="porphyry",
    )
    assert not np.isnan(r["cusps"]).any(), (
        "polar_fallback='porphyry' must replace NaN cusps with Porphyry values"
    )
    # The system field reports the user's request (not the fallback).
    assert r["system"] == "regiomontanus"


def test_polar_fallback_raise_for_regiomontanus() -> None:
    """At lat=80° with system='regiomontanus' and polar_fallback='raise',
    HighLatitudeError is raised (default behaviour, HOU-06)."""
    from ketu.houses import HighLatitudeError, calculate_houses
    with pytest.raises(HighLatitudeError) as exc:
        calculate_houses(2451545.0, 80.0, 0.0, system="regiomontanus")
    assert exc.value.system == "regiomontanus"
    assert abs(exc.value.lat - 80.0) < 1e-9
```

**Note :** le test `test_calculate_houses_no_runtime_swisseph_import` existant (`test_integration.py:214-237`) couvre automatiquement `regiomontanus.py` (il scanne `ketu.houses.*`) — pas d'extension requise.

**Done :** `pytest tests/houses/test_polar_safety.py tests/houses/test_integration.py -x -v` reste vert avec les 3 nouveaux tests ajoutés.

### Task 5 — Mesurer la dérive Reykjavik et pinner `REYKJAVIK_REGIO_TOL_ARCMIN`

**Procédure** (D-02 + 15-RESEARCH §14.3 + VALIDATION §Manual-Only) :

1. Implémenter Tasks 1-4.
2. Lancer **uniquement** ce test :
   ```bash
   pytest tests/houses/test_regiomontanus.py::test_regiomontanus_reykjavik_drift_measured_and_pinned -x -v -s
   ```
   Le flag `-s` active stdout — le `print(f"\nReykjavik Regiomontanus drift: max={delta_max * 60:.4f}′")` affichera la mesure.
3. Lire le delta réel.

**Décision-tree :**

| `delta_max` mesuré | Action |
|--------------------|--------|
| < 1 arcmin | Inattendu — Regio est plus précis que prévu. Pinner `REYKJAVIK_REGIO_TOL_ARCMIN = 1.0 * ARCMIN_DEG` (et le test `test_regiomontanus_cusps_match_oracle_at_arcmin` passe déjà sur Reykjavik — l'enlever de `NON_POLAR_LABELS_TIGHT` n'est pas nécessaire). |
| 1-5 arcmin | Plage normale eps-drift. Pinner `REYKJAVIK_REGIO_TOL_ARCMIN = (delta_max + 0.5) * ARCMIN_DEG`, arrondi vers le haut au 0.5 arcmin près. **Update la docstring** du constant pour mentionner la valeur observée et la date de mesure. |
| 5-10 arcmin | Suspicieux. **Vérifier Pitfall 4** : grep `pole_height` dans `regiomontanus.py` ; chaque appel `_asc1` doit recevoir `pole_height_outer_deg` ou `pole_height_inner_deg`, JAMAIS `lat_b` direct. Si correct, pinner avec marge mais documenter dans CHANGELOG. |
| > 10 arcmin | **BUG.** Investigate before pinning. Probable causes : signe inversé sur `cos(30°)` (devrait être positif), `tan(lat)/2` au lieu de `atan(tan(lat)/2)`, ou erreur dans la stack ordering. |

4. Update la valeur `REYKJAVIK_REGIO_TOL_ARCMIN` ligne ~38 de `test_regiomontanus.py`. Update aussi le commentaire docstring du constant pour figer la valeur observée :

```python
#: Reykjavik drift measured at <DATE> : <X.YY>′ max. Pinned tolerance
#: <X.YY + 0.5>′ provides 0.5′ margin against future ephemeris updates.
REYKJAVIK_REGIO_TOL_ARCMIN: float = X.YY * ARCMIN_DEG
```

5. Re-run le test : doit passer avec la valeur pinnée.

**Done :** la valeur `REYKJAVIK_REGIO_TOL_ARCMIN` est figée à une valeur basée sur mesure réelle (pas le placeholder 5.0) ; le commentaire docstring documente la mesure et la date.

### Task 6 — Régression complète

```bash
pytest tests/houses/ -x -v --no-cov
```

Doit rester 100 % vert. Cas particuliers à surveiller :
- `test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle` itère désormais 6 systèmes (Plan 15-01) — doit toujours passer.
- `test_koch.py` (qui importe `_asc1` via `from ._ecliptic import _asc1`) — doit toujours passer.
- `test_polar_safety.py` (étendu Plan 15-02 + ce plan) — toutes les assertions polar pour Whole Sign/Equal/Regio doivent passer.

**Done :** sortie pytest verte ; aucun test marqué `error` ou `failed`.

## Verification gates

```bash
cd /home/loc/workspace/ketu

# 1. Plan 15-01 doit être merged (pour _asc1 dans _ecliptic.py + snapshot étendu)
python -c "from ketu.houses._ecliptic import _asc1; print('15-01 _asc1 OK')"

# 2. Regiomontanus enregistré
python -c "from ketu.houses import SYSTEMS; assert 'regiomontanus' in SYSTEMS; print('OK')"

# 3. Algorithm-tier oracle bit-exact 1e-6° (non-polar charts)
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_algorithm_matches_oracle_armc -x -v

# 4. End-to-end snapshot 1 arcmin sur 7 charts tight
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_cusps_match_oracle_at_arcmin -x -v

# 5. Reykjavik drift mesuré et pinned (Task 5 doit avoir été exécutée)
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_reykjavik_drift_measured_and_pinned -x -v -s

# 6. Polar contract (NaN propagation Koch-style)
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_yields_nan_above_polar_circle -x
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_polar_lat_80_yields_all_nan -x
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_no_silent_nan_at_mid_latitudes -x

# 7. Cusps symmetry (5/6/8/9 opposites)
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_cusps_5_6_8_9_are_opposites_of_11_12_2_3 -x

# 8. polar_fallback integration
pytest tests/houses/test_integration.py::test_polar_fallback_routes_regiomontanus_to_porphyry -x
pytest tests/houses/test_integration.py::test_polar_fallback_raise_for_regiomontanus -x

# 9. Vectorisation
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_vectorised_matches_scalar_per_element -x

# 10. Registry
pytest tests/houses/test_regiomontanus.py::test_regiomontanus_registered_in_systems -x

# 11. Régression complète
pytest tests/houses/ -x

# 12. Coverage ≥95% sur le nouveau module
pytest tests/houses/test_regiomontanus.py --cov=ketu.houses.regiomontanus --cov-report=term-missing --cov-fail-under=95

# 13. mypy --strict clean
python -m mypy --strict ketu/houses/regiomontanus.py

# 14. Doc gates
python -m numpydoc lint ketu/houses/regiomontanus.py
python -m interrogate ketu/houses/regiomontanus.py -f 95

# 15. AGPL boundary
python -c "import ketu.houses.regiomontanus as m; assert not any(n.startswith('swe') for n in dir(m)); print('AGPL OK')"

# 16. Pitfall 4 grep ratchet : chaque _asc1 call dans regiomontanus.py reçoit un pole height
grep -E "_asc1\\(" ketu/houses/regiomontanus.py | grep -vE "pole_height_(outer|inner)_deg" || echo "PITFALL 4 violation: _asc1 called with non-pole-height arg"
# Si le grep retourne du contenu (lignes _asc1 sans pole_height_*), C'EST UN BUG
# Sinon (no output), OK
```

## Done criteria

- [ ] HOU2-03 satisfait : `calculate_houses(jd, lat, lon, system="regiomontanus")` retourne un `HOUSES_DTYPE` valide aux latitudes `|lat| < 90 - eps`.
- [ ] Au-dessus du cercle polaire, `regiomontanus_cusps` propage NaN ; `polar_fallback="raise"` lève `HighLatitudeError`, `polar_fallback="porphyry"` substitue Porphyry.
- [ ] `SYSTEMS` registry contient `'regiomontanus'` au moment de `import ketu.houses` (trigger import dans `__init__.py`).
- [ ] Algorithm-tier oracle bit-exact à 1e-6° sur les 7 charts non-polaires (excluding Reykjavik et polar charts).
- [ ] End-to-end snapshot match à 1 arcmin sur les 7 charts tight ; Reykjavik tolerance pinnée empiriquement (Task 5) — valeur documentée dans le docstring du constant.
- [ ] `cusp[5/6/8/9] = (cusp[11/12/2/3] + 180) mod 360` à 1e-9° (test ratchet).
- [ ] Aucun NaN à mid-latitudes (test `test_regiomontanus_no_silent_nan_at_mid_latitudes`).
- [ ] `_asc1` appelé exclusivement avec `pole_height_outer_deg` ou `pole_height_inner_deg` (Pitfall 4 ratchet par grep).
- [ ] Coverage ≥95 % sur `ketu/houses/regiomontanus.py`.
- [ ] mypy `--strict` clean ; `numpydoc lint` clean ; `interrogate ≥95%`.
- [ ] Régression `pytest tests/houses/ -x` 100 % vert (Plans 10-03..10-06 + 15-01 + (si mergé) 15-02 intacts).

## Atomic commit message

```
feat(15-03): add Regiomontanus house system (HOU2-03)

- ketu/houses/regiomontanus.py: closed-form trig per swisseph
  swehouse.c case 'R'. Four non-trivial cusps (11/12/2/3) computed via
  shared _asc1 helper (factorized in Plan 15-01) with pole heights
  fh1=atan(tan(lat)/2) and fh2=atan(tan(lat)*cos(30°)). Pole-height
  variables named explicitly (pole_height_outer/inner) to ratchet
  against Pitfall 4 (geographic latitude vs pole height).
- Polar safety: |lat| ≥ 90 - eps_mean(jd) → NaN propagation
  Koch-style (D-02 verrouillé in 15-CONTEXT.md). NO swisseph MC↔IC swap.
  calculate_houses existing polar_fallback machinery routes via
  porphyry or raises HighLatitudeError.
- ketu/houses/__init__.py: trigger import for @register decorator.
- tests/houses/test_regiomontanus.py: two-tier oracle (algorithm 1e-6°
  on non-polar charts; snapshot 1 arcmin on 7 tight charts). Reykjavik
  tolerance measured empirically and pinned (REYKJAVIK_REGIO_TOL_ARCMIN
  documented inline). Polar contract ratchet (NaN above polar, no NaN
  at mid-latitudes). Cusps 5/6/8/9 = 180° opposites invariant.
  Vectorisation, registry, constants parity.
- tests/houses/test_polar_safety.py: extend with regiomontanus polar
  ratchet alongside whole_sign/equal (Plan 15-02).
- tests/houses/test_integration.py: 2 new polar_fallback tests
  (porphyry-route and raise-route for regiomontanus).

Covers: HOU2-03 (Regiomontanus)
Refs: D-02 (Koch-style NaN polar), D-04 (one file per system),
      D-05 (_asc1 from Plan 15-01).
Pitfalls addressed: #2 (trigger import), #4 (pole height vs geo lat),
                    #5 (_asc1 callers OK via shared module).
```

## Rollback note

`git revert <commit_sha>` annule complètement ce plan : suppression de `regiomontanus.py`, `test_regiomontanus.py` ; restauration de `__init__.py` (retire `from . import regiomontanus`) ; retrait des tests Regio dans `test_polar_safety.py` et `test_integration.py`. Plan 15-04 (CLI) consomme `SYSTEMS.keys()` dynamiquement — sans Regio, le CLI listerait 5 systèmes au lieu de 6, et le test `test_invalid_system_rejected` (qui assertera `--system regiomontanus` accepté en Plan 15-04) failerait. Donc revert de 15-03 implique aussi revert ou ajustement de 15-04.
