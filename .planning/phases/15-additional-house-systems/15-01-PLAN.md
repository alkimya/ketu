---
phase: 15-additional-house-systems
plan: 01
plan_name: foundation-snapshot-dtype-asc1
type: execute
wave: 1
depends_on: []
files_modified:
  - ketu/houses/core.py
  - ketu/houses/_ecliptic.py
  - ketu/houses/koch.py
  - tests/houses/conftest.py
  - tests/houses/fixtures/reference_charts.json
  - scripts/snapshot_reference_charts.py
  - CHANGELOG.md
autonomous: true
requirements:
  - HOU2-05
parallelizable_with: []

must_haves:
  truths:
    - "Le script `scripts/snapshot_reference_charts.py` régénère `tests/houses/fixtures/reference_charts.json` de manière idempotente (re-run produit exactement le même contenu)"
    - "`HOUSES_DTYPE['system']` accepte la string `'regiomontanus'` (13 chars) sans troncature"
    - "Le helper `_asc1` est défini dans `ketu/houses/_ecliptic.py` et `koch.py` l'importe sans changement de comportement (la suite de tests Koch reste verte)"
    - "Le snapshot JSON contient les 6 systèmes (placidus, koch, porphyry, whole_sign, equal, regiomontanus) pour les 10 reference charts (60 blocs total)"
    - "`tests/houses/conftest.py:SYSTEM_BYTES` mappe les 6 systèmes vers leurs codes swisseph"
    - "`CHANGELOG.md` documente le bump U10→U16 dans `[Unreleased] ### Changed` avec disclaimer non-breaking"
  artifacts:
    - path: "scripts/snapshot_reference_charts.py"
      provides: "Idempotent regeneration of reference_charts.json across all registered systems"
      min_lines: 60
    - path: "ketu/houses/_ecliptic.py"
      provides: "_asc1 helper factorized from koch.py (great-circle ecliptic projection)"
      contains: "def _asc1"
    - path: "ketu/houses/core.py"
      provides: "HOUSES_DTYPE with system field bumped to U16"
      contains: 'np.dtype([... ("system", "U16")'
    - path: "tests/houses/fixtures/reference_charts.json"
      provides: "60 blocks (10 charts × 6 systems) committed snapshot"
      min_lines: 1000
  key_links:
    - from: "ketu/houses/koch.py"
      to: "ketu/houses/_ecliptic.py"
      via: "from ._ecliptic import _asc1"
      pattern: "from \\._ecliptic import _asc1"
    - from: "tests/houses/conftest.py:SYSTEM_BYTES"
      to: "swisseph hsys table"
      via: "dict[str, bytes] keys placidus/koch/porphyry/whole_sign/equal/regiomontanus"
      pattern: "whole_sign.*b\"W\""
    - from: "scripts/snapshot_reference_charts.py"
      to: "tests/houses/fixtures/reference_charts.json"
      via: "swe.houses_ex iteration over (chart, system)"
      pattern: "regiomontanus"
---

# Plan 15-01 — Fondation : snapshot script, DTYPE bump U10→U16, factorisation `_asc1`

## Goal

Poser la fondation invariante de la phase **avant** que les Plans 15-02/03/04 ne touchent au code production. Cinq tâches conjointes :

1. **`scripts/snapshot_reference_charts.py`** — créer le script de régénération qui n'a jamais été committé en v1.1 (référence aspirationnelle dans `tests/houses/conftest.py:248-252`). Idempotent et générique sur les 6 systèmes.
2. **DTYPE bump** — étendre `HOUSES_DTYPE['system']` de `U10` à `U16` pour accommoder `"regiomontanus"` (13 chars) sans troncature ; documenter dans `CHANGELOG.md` sous `[Unreleased] ### Changed` avec disclaimer non-breaking (D-01 verrouillé dans CONTEXT.md).
3. **Extraction `_asc1`** — déplacer le helper `_asc1` de `ketu/houses/koch.py:44-89` vers `ketu/houses/_ecliptic.py` (module interne déjà existant) pour le réutiliser sans duplication dans le futur `regiomontanus.py` (D-05 verrouillé). `koch.py` doit garder `from ._ecliptic import _asc1` ; aucun changement de comportement.
4. **Étendre `SYSTEM_BYTES`** dans `tests/houses/conftest.py:77-81` avec `b"W"`, `b"E"`, `b"R"` (pré-requis pour les Plans 15-02/03 ; permet à `swe_oracle_armc(armc, lat, eps, "whole_sign"/"equal"/"regiomontanus")` de fonctionner avant même que les implémentations Ketu n'existent).
5. **Régénérer `tests/houses/fixtures/reference_charts.json`** avec 6 systèmes × 10 charts = 60 blocs en exécutant le script créé en (1).

Ce plan est **séquentiel et bloquant** pour Wave 2. Aucun des Plans 15-02/03 ne peut tester end-to-end sans le snapshot étendu, et le `_asc1` factorisé est consommé par 15-03 (Regiomontanus). DTYPE bump est nécessaire pour que `out["system"] = "regiomontanus"` fonctionne dans le test du Plan 15-04.

## Pre-flight reading (LIRE INTÉGRALEMENT avant de toucher au code)

1. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-CONTEXT.md` — D-01 (DTYPE U10→U16 + CHANGELOG), D-05 (extraction `_asc1`), D-06 (snapshot script création).
2. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-RESEARCH.md` § 9 (Wave 0 gaps), § 10 (4-plan découpage), § 11 (Pitfalls 5 et 6 spécifiques à ce plan).
3. `/home/loc/workspace/ketu/.planning/phases/15-additional-house-systems/15-PATTERNS.md` § 5 (`SYSTEM_BYTES` extend), § 6 (snapshot regen + script structure).
4. `/home/loc/workspace/ketu/ketu/houses/_ecliptic.py` (intégral, 100 lignes) — gabarit du module interne existant ; on y ajoute `_asc1`.
5. `/home/loc/workspace/ketu/ketu/houses/koch.py` (intégral, 181 lignes) — source de `_asc1` (lignes 44-89) et 4 call sites (lignes 157-160).
6. `/home/loc/workspace/ketu/ketu/houses/core.py` (intégral, 80 lignes) — `HOUSES_DTYPE` ligne 35-45.
7. `/home/loc/workspace/ketu/tests/houses/conftest.py` lignes 67-81 (`SYSTEM_BYTES` à étendre), lignes 196-231 (`reference_charts` fixture qui définit les 10 entrées à itérer).
8. `/home/loc/workspace/ketu/tests/houses/test_oracle_smoke.py` lignes 71-104 (`test_loaded_reference_snapshot_matches_oracle` qui ratchet le format JSON ; **ce test boucle sur `("placidus", "koch", "porphyry")` ligne 84 — à étendre pour 6 systèmes dans le même plan**).
9. `/home/loc/workspace/ketu/tests/houses/test_dtype.py` lignes 41-46 (`test_dtype_string_field_capacity` ratchet `placidus/koch/porphyry/whole_sign` — à étendre pour les 3 nouveaux noms).
10. `/home/loc/workspace/ketu/tests/houses/fixtures/reference_charts.json` (premier 40 lignes pour le format ; le fichier sera régénéré en totalité).
11. `/home/loc/workspace/ketu/CHANGELOG.md` (section `[Unreleased]` — vérifier qu'elle existe ou la créer en haut).
12. `/home/loc/workspace/ketu/scripts/precompute_ephemeris.py` (intégral) — gabarit de style des scripts dans ce repo (shebang, docstring, `if __name__ == "__main__":`).

## Files

**Crée :**
- `/home/loc/workspace/ketu/scripts/snapshot_reference_charts.py` — script de régénération du snapshot.

**Modifie :**
- `/home/loc/workspace/ketu/ketu/houses/core.py` — `HOUSES_DTYPE['system']` : `U10` → `U16` + commentaire docstring updated.
- `/home/loc/workspace/ketu/ketu/houses/_ecliptic.py` — append `_asc1` (signature/body identique à `koch.py:44-89`).
- `/home/loc/workspace/ketu/ketu/houses/koch.py` — supprimer `_asc1` local, ajouter `from ._ecliptic import _asc1` au top.
- `/home/loc/workspace/ketu/tests/houses/conftest.py` — étendre `SYSTEM_BYTES` (3 entrées) + actualiser le commentaire ligne 72-76.
- `/home/loc/workspace/ketu/tests/houses/fixtures/reference_charts.json` — régénéré ; bump `version` à `"v1.2-phase15-snapshot"`.
- `/home/loc/workspace/ketu/tests/houses/test_oracle_smoke.py` — ligne 84 : étendre la tuple à `("placidus", "koch", "porphyry", "whole_sign", "equal", "regiomontanus")`. **Le test passera après que le JSON soit régénéré ; les nouveaux systèmes seront comparés à l'oracle live (qui les supporte déjà).**
- `/home/loc/workspace/ketu/tests/houses/test_dtype.py` ligne 43 : étendre la tuple à `("placidus", "koch", "porphyry", "whole_sign", "equal", "regiomontanus")` pour ratchet la capacité U16.
- `/home/loc/workspace/ketu/CHANGELOG.md` — ajouter sous `[Unreleased] ### Changed` la note sur le bump.

**Lit (référence) :**
- `/home/loc/workspace/ketu/ketu/houses/koch.py` (signatures, formules, callers `_asc1`).
- `/home/loc/workspace/ketu/tests/houses/test_koch.py` (régression — doit rester vert après l'extraction de `_asc1`).

## Tasks

### Task 1 — Bump `HOUSES_DTYPE['system']` U10 → U16 + CHANGELOG

Édite `/home/loc/workspace/ketu/ketu/houses/core.py` :

**Avant** (ligne 39) :
```python
    ("system",  "U10"),
```

**Après** :
```python
    ("system",  "U16"),
```

Mettre à jour le commentaire ligne 26 :
```python
#:     - ``system`` (U16): House system name (e.g. "placidus", "regiomontanus").
```

**Justification (à mettre dans le docstring `HOUSES_DTYPE` ligne 22) :** ajouter une `Notes` finale :
```
#:
#: .. versionchanged:: v1.2 (Phase 15)
#:     ``system`` field width bumped from U10 to U16 to accommodate
#:     ``"regiomontanus"`` (13 chars) without truncation. Read paths
#:     remain compatible (NumPy implicit U10⇄U16 cast on assignment;
#:     equality comparisons by content are unchanged). No public API
#:     change — additive only per v1.2 non-breaking-minor contract.
```

**Étendre `tests/houses/test_dtype.py:test_dtype_string_field_capacity`** (lignes 41-46) :
```python
def test_dtype_string_field_capacity() -> None:
    """system field is U16 — fits all v1.2 system names including 'regiomontanus' (13 chars)."""
    for name in ("placidus", "koch", "porphyry",
                 "whole_sign", "equal", "regiomontanus"):
        arr = np.zeros(1, dtype=HOUSES_DTYPE)
        arr["system"][0] = name
        assert arr["system"][0] == name, (
            f"system field truncated {name!r} to {arr['system'][0]!r}; "
            "did the U16 bump regress?"
        )
```

**Mettre à jour `/home/loc/workspace/ketu/CHANGELOG.md`** : ajouter en haut, sous une section `## [Unreleased]` (créer la section si absente) avec sous-section `### Changed` :

```markdown
## [Unreleased]

### Changed

- `ketu.houses.HOUSES_DTYPE['system']` : largeur étendue de `U10` à `U16` pour accommoder `"regiomontanus"` (13 chars) sans troncature. **Non-breaking** : NumPy cast U10⇄U16 transparent à l'assignation ; les comparaisons par contenu restent identiques ; aucun consommateur Kala ou test ne dépend de la largeur exacte. Premier consommateur : Phase 15 (HOU2-03).
```

**Si `CHANGELOG.md` n'existe pas**, le créer avec en-tête standard :
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed

- ...
```

**Done :** `pytest tests/houses/test_dtype.py::test_dtype_string_field_capacity -x` passe avec les 6 noms.

### Task 2 — Extraire `_asc1` vers `ketu/houses/_ecliptic.py`

Édite `/home/loc/workspace/ketu/ketu/houses/_ecliptic.py` : **append** à la fin du fichier (après `ascensional_difference`, ligne 100) :

```python
def _asc1(
    x: np.ndarray,
    lat: np.ndarray,
    sin_eps: np.ndarray,
    cos_eps: np.ndarray,
) -> np.ndarray:
    """Return the ecliptic longitude crossed by a great circle.

    Mirrors swisseph's ``Asc1(x, f, sine, cose)`` (``swehouse.c`` line
    2056). For ``x = ARMC + 90`` and ``f = lat`` this collapses to the
    standard Ascendant formula::

        asc = atan2(cos(armc), -(tan(lat)·sin(eps) + cos(eps)·sin(armc)))

    Used by Koch (Plan 10-05) and Regiomontanus (Plan 15-03). When the
    caller passes a *pole height* (e.g. ``atan(tan(geo_lat)·X)``) instead
    of the geographic latitude, the result is the cusp of an intermediate
    Regiomontanus great circle.

    Parameters
    ----------
    x : np.ndarray
        Angle (degrees) — for cusp ``k`` this is ``ARMC + offset_k``.
    lat : np.ndarray
        Geographic latitude OR pole height (degrees) depending on caller.
        For Koch this is the geographic latitude; for Regiomontanus this
        is the great-circle pole height (Pitfall 4 from 15-RESEARCH.md).
    sin_eps, cos_eps : np.ndarray
        Pre-computed ``sin``/``cos`` of the obliquity (radians) — passed
        in to avoid repeating the trig inside the per-cusp loop.

    Returns
    -------
    np.ndarray
        Ecliptic longitude (degrees, ``[0, 360)``).

    Notes
    -----
    The leading underscore signals "internal API"; callers within
    ``ketu.houses`` import via ``from ._ecliptic import _asc1``.
    Public consumers should NOT depend on this symbol.
    """
    x_norm = x % 360.0
    x_rad = np.deg2rad(x_norm)
    lat_rad = np.deg2rad(lat)
    num = np.cos(x_rad - np.pi / 2.0)
    den = -(np.tan(lat_rad) * sin_eps + cos_eps * np.sin(x_rad - np.pi / 2.0))
    lam = np.arctan2(num, den)
    result: np.ndarray = np.rad2deg(lam) % 360.0
    return result
```

Le corps est **identique** à `ketu/houses/koch.py:73-89` ; seul le docstring est étoffé pour expliciter l'usage Regiomontanus (pole height vs geographic latitude — Pitfall 4 du 15-RESEARCH.md).

**Édite `/home/loc/workspace/ketu/ketu/houses/koch.py`** :
- **Supprime** la définition `_asc1` (lignes 44-89 entières, du `def _asc1(` au `return result`).
- **Ajoute** sous les imports existants (après ligne 33 `from .registry import register`) :
  ```python
  from ._ecliptic import _asc1
  ```

Garde `MAX_ITER` et `TOL_DEG` (lignes 38-41 — c'est de la documentation API parity).

**Pitfall 5 garde-fou (15-RESEARCH §11) :** vérifier que les 4 sites d'appel (anciennement lignes 157-160) appellent bien le même `_asc1`. Comme on importe `from ._ecliptic import _asc1`, le nom local est inchangé : aucune réécriture des call sites. **Faire un grep final** : `grep -n "_asc1" ketu/houses/koch.py` doit retourner 5 lignes (1 import + 4 call sites), pas plus, pas moins.

**Done :** `pytest tests/houses/test_koch.py -x -v` passe (régression complète Phase 10) ; `grep -c "def _asc1" ketu/houses/koch.py` retourne `0` ; `grep -c "def _asc1" ketu/houses/_ecliptic.py` retourne `1`.

### Task 3 — Étendre `SYSTEM_BYTES` dans `tests/houses/conftest.py`

Édite `/home/loc/workspace/ketu/tests/houses/conftest.py` :

**Lignes 72-76** (commentaire) — étendre la table mentionnée :
```python
# Codes per the Astrodienst hsys table:
#   b"P" = Placidus
#   b"K" = Koch
#   b"O" = Porphyry  (closed-form; finite at all latitudes — used as the
#                    polar fallback in Plan 10-05)
#   b"W" = Whole Sign  (Phase 15 ; sign-floor ; polar-safe by construction)
#   b"E" = Equal       (Phase 15 ; ASC-anchored 30° spacing ; polar-safe)
#   b"R" = Regiomontanus (Phase 15 ; closed-form trig ; NaN at polar boundary)
```

**Lignes 77-81** (dict) :
```python
SYSTEM_BYTES: dict[str, bytes] = {
    "placidus": b"P",
    "koch": b"K",
    "porphyry": b"O",
    "whole_sign": b"W",       # Phase 15 — HOU2-01
    "equal": b"E",            # Phase 15 — HOU2-02
    "regiomontanus": b"R",    # Phase 15 — HOU2-03
}
```

**Done :** `python -c "from tests.houses.conftest import SYSTEM_BYTES; assert set(SYSTEM_BYTES) == {'placidus','koch','porphyry','whole_sign','equal','regiomontanus'}; print('OK')"` retourne `OK`.

### Task 4 — Créer `scripts/snapshot_reference_charts.py`

Crée `/home/loc/workspace/ketu/scripts/snapshot_reference_charts.py` avec **shebang Python 3** (cf `scripts/precompute_ephemeris.py:1` pour la convention) :

```python
#!/usr/bin/env python3
"""Regenerate ``tests/houses/fixtures/reference_charts.json``.

Iterates the 10 reference charts × 6 registered house systems
(placidus, koch, porphyry, whole_sign, equal, regiomontanus) and writes
the snapshot consumed by ``test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle``.

The snapshot pinned in git serves as an environmental ratchet: if a
swisseph version bump or ephemeris-file change drifts oracle output by
more than ``1e-9°``, the test fails — pointing the operator here to
re-run this script intentionally.

Usage
-----
``python scripts/snapshot_reference_charts.py``                  # regenerate in place
``python scripts/snapshot_reference_charts.py --check``           # validate without writing

Idempotency
-----------
Running this script twice in a row produces byte-identical JSON
(deterministic swisseph + sorted dict keys + canonical float repr).

License note
------------
Imports ``swisseph`` (AGPL test-only dep). This script lives under
``scripts/`` (not ``ketu/``) deliberately — the production package
remains swisseph-free.
"""
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path
from typing import Any

# pyswisseph is a test-only AGPL dep; this regen script is part of the
# test infrastructure even though it sits under scripts/.
import swisseph as swe  # type: ignore[import-not-found]


# ---------------------------------------------------------------------------
# Fixtures (mirrors tests/houses/conftest.py:reference_charts)
# ---------------------------------------------------------------------------

REFERENCE_CHARTS: list[dict[str, Any]] = [
    {"label": "J2000_Greenwich",   "jd": 2451545.0, "lat": 51.4779, "lon": 0.0},
    {"label": "J2000_Paris",       "jd": 2451545.0, "lat": 48.8566, "lon": 2.3522},
    {"label": "J2000_Sydney",      "jd": 2451545.0, "lat": -33.8688, "lon": 151.2093},
    {"label": "J2000_Tokyo",       "jd": 2451545.0, "lat": 35.6762, "lon": 139.6503},
    {"label": "J2000_BuenosAires", "jd": 2451545.0, "lat": -34.6037, "lon": -58.3816},
    {"label": "J2000_Equator",     "jd": 2451545.0, "lat": 0.0, "lon": 0.0},
    {"label": "1900_NewYork",      "jd": 2415020.5, "lat": 40.7128, "lon": -74.0060},
    {"label": "2050_Reykjavik",    "jd": 2470204.0, "lat": 64.1466, "lon": -21.9426},
    {"label": "J2000_Lat70_North", "jd": 2451545.0, "lat": 70.0, "lon": 0.0},
    {"label": "J2000_Lat80_North", "jd": 2451545.0, "lat": 80.0, "lon": 0.0},
]

# All 6 v1.2 systems. Iterate ALL of them on every regen — Pitfall 6
# from 15-RESEARCH §11 (don't accidentally drop placidus/koch/porphyry).
SYSTEM_BYTES: dict[str, bytes] = {
    "placidus": b"P",
    "koch": b"K",
    "porphyry": b"O",
    "whole_sign": b"W",
    "equal": b"E",
    "regiomontanus": b"R",
}

OUTPUT_PATH = Path(__file__).resolve().parent.parent / "tests" / "houses" / "fixtures" / "reference_charts.json"
SNAPSHOT_VERSION = "v1.2-phase15-snapshot"


# ---------------------------------------------------------------------------
# Snapshot computation
# ---------------------------------------------------------------------------


def compute_snapshot() -> dict[str, Any]:
    """Build the full snapshot dict for all charts × all systems."""
    out: dict[str, Any] = {"version": SNAPSHOT_VERSION, "charts": {}}
    for chart in REFERENCE_CHARTS:
        block: dict[str, Any] = {
            "meta": {
                "label": chart["label"],
                "jd": chart["jd"],
                "lat": chart["lat"],
                "lon": chart["lon"],
            },
            "systems": {},
        }
        for sys_name, code in SYSTEM_BYTES.items():
            try:
                cusps_t, ascmc_t = swe.houses_ex(
                    chart["jd"], chart["lat"], chart["lon"], code
                )
                block["systems"][sys_name] = {
                    "cusps": [float(c) for c in cusps_t[1:13]],
                    "asc": float(ascmc_t[0]),
                    "mc": float(ascmc_t[1]),
                    "armc": float(ascmc_t[2]),
                    "vertex": float(ascmc_t[3]),
                }
            except swe.Error as exc:
                # Polar boundary (Placidus/Koch/Regiomontanus at lat>=70°)
                block["systems"][sys_name] = {
                    "error": str(exc),
                    "polar": True,
                }
        out["charts"][chart["label"]] = block
    return out


# ---------------------------------------------------------------------------
# I/O
# ---------------------------------------------------------------------------


def write_snapshot(payload: dict[str, Any], path: Path) -> None:
    """Write the snapshot to ``path`` with deterministic formatting."""
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(payload, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )


def check_snapshot(payload: dict[str, Any], path: Path) -> bool:
    """Return ``True`` if the on-disk snapshot matches ``payload`` byte-for-byte."""
    if not path.exists():
        return False
    expected = json.dumps(payload, indent=2, sort_keys=True) + "\n"
    actual = path.read_text(encoding="utf-8")
    return expected == actual


# ---------------------------------------------------------------------------
# Entrypoint
# ---------------------------------------------------------------------------


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(
        "--check",
        action="store_true",
        help="Compare snapshot to disk, exit non-zero if drift; do not write.",
    )
    args = parser.parse_args(argv)

    payload = compute_snapshot()

    if args.check:
        ok = check_snapshot(payload, OUTPUT_PATH)
        if not ok:
            print(
                f"Snapshot drift detected at {OUTPUT_PATH}. "
                "Re-run without --check to regenerate.",
                file=sys.stderr,
            )
            return 1
        print(f"Snapshot {OUTPUT_PATH} matches live oracle output.")
        return 0

    write_snapshot(payload, OUTPUT_PATH)
    print(f"Wrote snapshot to {OUTPUT_PATH} ({len(payload['charts'])} charts × {len(SYSTEM_BYTES)} systems)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

**Notes :**
- Itérer `SYSTEM_BYTES.items()` — qui inclut les 6 systèmes — pas une variable hardcodée à 3. Pitfall 6 du 15-RESEARCH.md.
- `sort_keys=True` garantit l'idempotence (le re-run produit byte-identique).
- Trailing newline `+ "\n"` — convention POSIX, évite les diffs spurious.
- **PAS d'import de `tests.houses.conftest`** — `scripts/` ne dépend pas de la suite de tests pour rester runnable hors environnement pytest. Les 10 charts sont dupliqués (acceptable : c'est une dette mineure ; à factoriser en v1.3 si on en ajoute des nouveaux).

**Done :**
```bash
chmod +x /home/loc/workspace/ketu/scripts/snapshot_reference_charts.py
python /home/loc/workspace/ketu/scripts/snapshot_reference_charts.py
# affiche "Wrote snapshot to … (10 charts × 6 systems)"
python /home/loc/workspace/ketu/scripts/snapshot_reference_charts.py --check
# exit 0, affiche "matches live oracle output"
```

### Task 5 — Régénérer `tests/houses/fixtures/reference_charts.json` + étendre le test ratchet

**Régénération** : déjà couvert par l'exécution de Task 4. Le fichier remplace l'ancien (3 systèmes par chart) par 6 systèmes par chart. Format identique (clé `meta` + clé `systems`), seul le nombre de blocs changé : 30 → 60.

**Bump version dans le JSON :** la clé top-level `"version"` passe (ou est introduite) à `"v1.2-phase15-snapshot"`. Note : le JSON v1.1 actuel commence par `"charts": {...}` sans clé `version` ; le script l'ajoute. Vérifier qu'aucun consommateur ne fait `loaded["charts"]` sans tester `"charts" in loaded` (cf `test_loaded_reference_snapshot_matches_oracle:81` — OK, va chercher `"charts"`, ignore `"version"`).

**Étendre `tests/houses/test_oracle_smoke.py:test_loaded_reference_snapshot_matches_oracle`** ligne 84 :

**Avant** :
```python
        for sys_name in ("placidus", "koch", "porphyry"):
```

**Après** :
```python
        for sys_name in (
            "placidus", "koch", "porphyry",
            "whole_sign", "equal", "regiomontanus",
        ):
```

**Done :**
```bash
pytest tests/houses/test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle -x -v
# verte : itère 10 charts × 6 systèmes = 60 comparaisons à 1e-9° tolérance
```

### Task 6 — Régression : la suite Phase 10 reste verte

Lance la suite complète Phase 10 (Plans 10-03..10-06 et descendants) pour vérifier que ni le bump U10→U16, ni l'extraction `_asc1`, ni l'extension `SYSTEM_BYTES` n'ont introduit de régression :

```bash
pytest tests/houses/ -x -v --no-cov
```

**Doit rester :** 100 % vert. **Cas particulier à surveiller :** `tests/houses/test_koch.py` (qui consomme `_asc1` indirectement via `koch_cusps`) et `tests/houses/test_dtype.py:test_dtype_string_field_capacity` (qui asserte le contenu du champ `system`).

**Done :** sortie pytest verte, aucun test marqué `error` ou `failed`.

## Verification gates

Toutes ces commandes DOIVENT passer (exit 0) avant commit :

```bash
cd /home/loc/workspace/ketu

# 1. Le bump U10→U16 est en place
python -c "from ketu.houses import HOUSES_DTYPE; assert HOUSES_DTYPE.fields['system'][0].itemsize >= 16*4, f'Expected ≥64 bytes (U16), got {HOUSES_DTYPE.fields[\"system\"][0].itemsize}'; print('OK')"

# 2. _asc1 n'est plus dans koch.py mais bien dans _ecliptic.py
grep -c "def _asc1" ketu/houses/koch.py     # MUST be 0
grep -c "def _asc1" ketu/houses/_ecliptic.py # MUST be 1
grep -c "from ._ecliptic import _asc1" ketu/houses/koch.py  # MUST be 1

# 3. SYSTEM_BYTES est étendu
python -c "from tests.houses.conftest import SYSTEM_BYTES; assert set(SYSTEM_BYTES) == {'placidus','koch','porphyry','whole_sign','equal','regiomontanus'}; print('OK')"

# 4. Le snapshot script existe et est idempotent
python scripts/snapshot_reference_charts.py
python scripts/snapshot_reference_charts.py --check  # exit 0 = idempotent

# 5. Le snapshot JSON a les 6 systèmes par chart
python -c "import json; d = json.load(open('tests/houses/fixtures/reference_charts.json')); chart = d['charts']['J2000_Paris']['systems']; assert set(chart) == {'placidus','koch','porphyry','whole_sign','equal','regiomontanus'}, f'Got: {set(chart)}'; print('OK')"

# 6. Le test ratchet du snapshot passe (test_loaded_reference_snapshot_matches_oracle)
pytest tests/houses/test_oracle_smoke.py::test_loaded_reference_snapshot_matches_oracle -x -v

# 7. Le test ratchet du DTYPE U16 passe avec 6 noms
pytest tests/houses/test_dtype.py::test_dtype_string_field_capacity -x -v

# 8. Régression Phase 10 complète : Koch utilise toujours `_asc1` correctement
pytest tests/houses/test_koch.py -x -v

# 9. Suite houses entière reste verte (0 régression)
pytest tests/houses/ -x

# 10. CHANGELOG mentionne le bump
grep -c "U10.*U16\|U16.*regiomontanus" CHANGELOG.md  # MUST be ≥1

# 11. mypy --strict reste clean sur ketu/houses
python -m mypy --strict ketu/houses/

# 12. interrogate ≥95% (le _asc1 docstring étoffé doit garder le ratio)
python -m interrogate ketu/houses/ -f 95

# 13. numpydoc validate clean sur les 2 fichiers touchés
python -m numpydoc lint ketu/houses/_ecliptic.py ketu/houses/core.py
```

## Done criteria

- [ ] `HOUSES_DTYPE['system']` field width = U16 (acceptant `"regiomontanus"` 13 chars sans troncature) — vérifié par `test_dtype_string_field_capacity` étendu.
- [ ] `CHANGELOG.md` contient une entrée `[Unreleased] ### Changed` documentant le bump U10→U16 avec disclaimer non-breaking.
- [ ] `_asc1` est défini dans `ketu/houses/_ecliptic.py` et importé par `koch.py` via `from ._ecliptic import _asc1` ; `koch.py` ne contient plus de définition locale.
- [ ] `SYSTEM_BYTES` dans `tests/houses/conftest.py` mappe les 6 systèmes (placidus/koch/porphyry/whole_sign/equal/regiomontanus → b"P/K/O/W/E/R").
- [ ] `scripts/snapshot_reference_charts.py` existe, est exécutable, et `--check` retourne 0 (idempotence vérifiée).
- [ ] `tests/houses/fixtures/reference_charts.json` contient 10 charts × 6 systèmes = 60 blocs (clés `meta` + `systems`) ; clé top-level `"version": "v1.2-phase15-snapshot"` présente.
- [ ] `test_loaded_reference_snapshot_matches_oracle` itère sur les 6 systèmes et passe à 1e-9° tolérance pour chaque combinaison.
- [ ] `pytest tests/houses/ -x` reste 100 % vert (régression Phase 10 nulle).
- [ ] `mypy --strict ketu/houses/` clean ; `numpydoc lint ketu/houses/_ecliptic.py ketu/houses/core.py` clean ; `interrogate ketu/houses/ -f 95` ≥95 %.

## Atomic commit message

```
feat(15-01): foundation — DTYPE U16, _asc1 extraction, snapshot regen script

- Bump HOUSES_DTYPE['system'] U10 -> U16 to fit "regiomontanus" (13 chars)
  without truncation (CHANGELOG [Unreleased] ### Changed entry, non-breaking
  per v1.2 contract; NumPy implicit U10<->U16 cast keeps reads compatible)
- Extract _asc1 from ketu/houses/koch.py to ketu/houses/_ecliptic.py for
  reuse in upcoming Plan 15-03 (Regiomontanus); koch.py imports via
  `from ._ecliptic import _asc1`. Behavior unchanged (test_koch.py green).
- Extend tests/houses/conftest.py:SYSTEM_BYTES with b"W"/b"E"/b"R" so
  oracle harness supports whole_sign/equal/regiomontanus before Plans
  15-02 and 15-03 land their implementations.
- Create scripts/snapshot_reference_charts.py: idempotent regen of
  tests/houses/fixtures/reference_charts.json across 10 charts × 6
  systems = 60 blocks. Replaces aspirational reference in
  conftest.py:248-252 (never committed in v1.1).
- Regenerate fixtures/reference_charts.json with 6 systems per chart;
  bump top-level "version" to "v1.2-phase15-snapshot".
- test_loaded_reference_snapshot_matches_oracle now iterates all 6
  systems (was 3); test_dtype_string_field_capacity now ratchets the
  U16 capacity with all 6 names.

Covers: HOU2-05 (snapshot foundation for end-to-end gate)
Refs: D-01 (DTYPE bump), D-05 (_asc1 factorization), D-06 (snapshot script)
Pitfalls addressed: #5 (_asc1 caller migration), #6 (regen iterates ALL
                    systems, not just new ones)
```

## Rollback note

`git revert <commit_sha>` annule complètement ce plan : retour à `HOUSES_DTYPE['system'] = "U10"`, `_asc1` ré-injecté dans `koch.py`, suppression de `scripts/snapshot_reference_charts.py`, restauration du snapshot JSON 3-systèmes, restauration de `SYSTEM_BYTES` 3-entrées, et retrait de l'entrée CHANGELOG. Aucun consommateur Phase 15 n'existe encore (Plans 02/03/04 sont les premiers usages), donc le revert est sans effet de bord. **Cas spécial CHANGELOG** : si le fichier a été créé par ce plan (cas où il n'existait pas), le revert le supprime — c'est OK, c'est une création nette.
